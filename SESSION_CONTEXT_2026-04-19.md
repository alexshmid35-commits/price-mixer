# Контекст проекта и выполненные изменения

Файл собран по итогам этой и прошлой сессий, чтобы не потерять рабочий контекст по `Price Mixer`.

## Проект

- Основной файл бэкенда: [app.py](./app.py)
- Ядро парсинга/сводки: [mixer.py](./mixer.py)
- Проект работает как локальный Flask-сервис для:
  - загрузки прайсов
  - сводки товаров
  - подбора `OnlinerID`
  - ручной модерации
  - работы с локальной SQLite-базой
  - экспорта итогового прайса

## Что уже сделано по UI и прайсу

### Новая колонка цены

- Добавлена новая колонка `Цена без скидки`
- Добавлено отдельное поле процента `Без скидки, % от РРЦ`
- По кнопке `Применить наценку`:
  - считается `РРЦ`
  - отдельно считается `Цена без скидки`
- Новая колонка:
  - видна на главной странице
  - сохраняется в `consolidated_price.xlsx`
  - попадает в скачиваемый прайс

### Переименование и выравнивание

- `Лучшая цена` переименована в `Опт цена`
- На главной странице выровнены по центру:
  - `OnlinerID`
  - `Опт цена`
  - `Поставщик`
  - `Гарантия`
  - `Дней доставки`
  - `РРЦ`
  - `Цена без скидки`

### Округление цен

- Для `РРЦ` и `Цена без скидки` внедрено округление к ближайшему десятку:
  - `1,2,3,4` -> вниз
  - `5,6,7,8,9` -> вверх
- Примеры:
  - `302 -> 300`
  - `441 -> 440`
  - `583 -> 580`
  - `1311 -> 1310`

### UI блока наценки

- Причесан блок `Наценка`
- Сделана более аккуратная карточка
- Улучшены отступы, сетка, инпуты, кнопки и общий визуальный вид

## Что уже сделано по очистке ID

### Очистка ID у N-Tech кроме ПЭВМ

- Кнопка теперь работает не глобально, а только для `N-Tech`
- Логика:
  - `N-Tech` не-ПЭВМ -> очищаются `OnlinerID` и `Ссылка`
  - `N-Tech` ПЭВМ -> сохраняются
  - остальные поставщики -> не трогаются

### Усиленная очистка кешей

Для этой же операции теперь дополнительно чистятся:

- `manual_id_bindings`
- `onliner_id_cache`
- `review_queue`

Идея:

- если решили снести `N-Tech` ID и подбирать заново, старые кэши не должны возвращать мусор обратно

## Что уже сделано по ручному подтверждению ID

- Последняя ручная вставка `OnlinerID` в поле имеет приоритет
- Последний ручной выбор кандидата тоже сохраняется
- Ручной ввод побеждает автоматику и историю старых подборов
- Подтверждён пример:
  - монитор `Gigabyte G25F2A`
  - финально закрепился правильный `OnlinerID = 4965564`
  - запись попала в `manual_id_bindings`
  - товар ушёл из `review_queue`

## Важный фикс по JS-шаблону страницы

Была критичная проблема:

- в inline-JS внутри `RESULT_PAGE` использовались regex с `\\b`
- внутри Python-строки это превращалось в скрытый символ `\\x08`
- из-за этого ломался весь script страницы
- следствие:
  - не работали кнопки
  - не грузилась таблица
  - страница выглядела живой, но была фактически мёртвой

Что сделано:

- опасные regex переписаны без `\\b`
- скрытые backspace-символы убраны
- шаблон проверялся отдельно

Важно на будущее:

- в inline-JS шаблонах `RESULT_PAGE` не использовать `\\b`
- лучше применять безопасные границы вида:
  - `(?:^|[^a-z0-9])...(?=$|[^a-z0-9])`

## Фикс по мусорным CPU-ID в кеше

Была найдена причина массово одинаковых `OnlinerID` у Intel CPU:

- в `onliner_id_cache.json` лежали generic-ключи вроде:
  - `SOCKET-1700`
  - `SOCKET-1200`
  - `SOCKET-2011-3`
  - `LGA-4189`
- из-за этого один ID размазывался по всем процессорам с тем же сокетом

Что сделано:

- generic CPU-ключи удалены из `onliner_id_cache.json`
- запрещено сохранять такие generic CPU-ключи снова
- добавлена санация CPU-cache-ключей

## Поиск по нашей локальной БД

Добавлен поиск по локальной SQLite-базе прямо в UI, в блок `База Onliner ID`.

Теперь можно искать:

- по модели
- по бренду
- по `OnlinerID`

Примеры, которые проверялись:

- `24U411A-B` -> находился монитор `LG 24U411A-B`
- `EA241Q` -> находились `ELSA EA241Q`
- `G25F2A` -> находился монитор `Gigabyte G25F2A`
- `24G411A-B` -> находился `LG 24G411A-B`

## Категорийный ручной подбор N-Tech

Общая идея всех кнопок категорий:

- ничего не ставится автоматически
- кандидаты складываются в ручную очередь
- дальше пользователь вручную:
  - подтверждает
  - вставляет ID вручную
  - или пропускает

Для отчётов добавлены:

- отдельные кнопки категорий
- единый модал-отчёт
- счётчики:
  - `В очереди`
  - `Без модели`
  - `Без кандидатов`

## Процессоры N-Tech

### Что сделано

- Добавлена кнопка `Процессоры N-Tech`
- Подбор работает только по:
  - `производителю`
  - `модели`
- Автоназначение отключено
- Всё отправляется в ручную очередь

### Улучшения

- В очереди показываются:
  - `Производитель`
  - `Модель`
- Совпавшая модель подсвечивается
- Добавлена цветовая маркировка кандидатов:
  - зелёный
  - жёлтый
  - красный
- Кандидаты сортируются по качеству

### OEM / BOX

Скоринг CPU усилен:

- если у локального товара есть `OEM`, кандидат `OEM` получает бонус
- `BOX` при `OEM` штрафуется
- если локальный CPU `OEM`, а кандидат без упаковки, он лучше `BOX`

## Материнки N-Tech

### Что сделано

- Добавлена кнопка `Материнки N-Tech`
- Подбор идёт по:
  - бренду
  - модели
  - chipset
  - socket
  - DDR4 / DDR5
  - WiFi
  - модельным суффиксам и сериям

### Усиление правил

Отдельно усилены:

- `DDR4` против `DDR5`
- `WiFi` против без `WiFi`
- серии и суффиксы:
  - `D4`
  - `AX`
  - `EAGLE`
  - `AORUS`
  - `GAMING X`
  - `STEEL LEGEND`
  - `PRO RS`
  - `LIGHTNING`
  - `LIVEMIXER`
  - `DS3H`
  - `D3HP`
  - `HDV`
  - `ELITE`
  - `UD`
  - `RIPTIDE`
  - `TOMAHAWK`
  - `MORTAR`
  - `STRIX`
  - `PRIME`

### WiFi правило

Сделано жёсткое правило:

- признак `WiFi` определяется только по модельной части до `Soc-...`
- если локальная плата без `WiFi`, WiFi-кандидаты отбрасываются
- если локальная плата WiFi, не-WiFi кандидаты тоже отбрасываются

## Мониторы N-Tech

### Что сделано

- Добавлена кнопка `Мониторы N-Tech`
- Подбор идёт по:
  - бренду
  - модели
  - диагонали
  - разрешению
  - герцовке
  - `White`

### Улучшения

- Исправлен отчёт, чтобы он понимал `monitor_issue`
- Для мониторов добавлен прямой поиск в SQLite по `бренд + модель`
- После этого мониторы начали нормально цепляться по модели

## Видеокарты N-Tech

### Что сделано

- Добавлена кнопка `Видеокарты N-Tech`
- Подбор идёт по:
  - вендору
  - GPU-модели
  - серии
  - объёму памяти
  - `White`
  - `OC`
  - точному SKU-коду

### Поддержанные серии

В логике усилены серии GPU, включая:

- `AORUS`
- `AERO`
- `EAGLE`
- `EAGLE MAX`
- `EAGLE OC`
- `EAGLE ICE`
- `WINDFORCE`
- `WINDFORCE MAX`
- `WINDFORCE OC`
- `GAMING`
- `GAMING OC`
- `GAMING OC ICE`
- `PULSE`
- `PULSE OC`
- `PURE`
- `NITRO+`
- `DUAL`
- `DUAL OC`
- `StormX`
- `StormX OC`
- `Infinity 2`
- `Infinity 2 OC`
- `Infinity 3`
- `Infinity 3 OC`
- `GamingPro`
- `GamingPro OC`
- `GamingPro-S`
- `GamingPro-S OC`
- `ZONE Edition`
- `Ventus`

### Точный SKU для GPU

Для GPU сделано жёсткое правило:

- если найден точный SKU, он становится главным ключом
- для `Gigabyte` это особенно важно по кодам вида:
  - `GV-...`
- при точном совпадении SKU:
  - код подсвечивается жёлтым
  - score поднимается до `0.999`
  - шумные похожие кандидаты скрываются

## Оперативная память N-Tech

### Что сделано

- Добавлена кнопка `Оперативка N-Tech`
- Подбор работает по:
  - `DDR3/DDR4/DDR5`
  - бренду
  - коду модуля / SKU
  - объёму
  - `KitOf2`
  - частоте
  - `CL`
  - сериям
  - `White`
  - `RGB`
  - `ECC`
  - `Reg`

### Поддержанные серии памяти

Сейчас усилены серии:

- `FURY Beast`
- `FURY Beast RGB`
- `FURY Renegade`
- `Trident Z`
- `Trident Z RGB`
- `Trident Z5 RGB`
- `Trident Z5 Neo RGB`
- `Flare X5`
- `Ripjaws V`
- `Ripjaws M5`
- `Ripjaws M5 Neo`
- `Aegis`
- `Shadow II`
- `Shadow III`
- `Shadow S`
- `T-Create Expert`
- `Vengeance LPX`
- `Lancer Blade RGB`
- `Lancer Neon RGB`
- `Basic`
- `Signature Line`

### Точный код памяти

Для RAM сделано жёсткое распознавание кодов:

- `KF...`
- `KVR...`
- `KSM...`
- `KCS...`
- `F4-...`
- `F5-...`
- `NT...`
- `TT...`
- `AX...`
- `PSD...`
- `CM...`

### Правила для RAM

- если у памяти совпал точный код модуля:
  - score становится `0.999`
  - этот кандидат становится основным
- соседние коды больше не должны лезть как почти одинаковые

### Статус RAM на момент фиксации

Проверялось на текущем прайсе:

- `scanned: 455`
- `queued: 453`
- `no_model: 2`
- `no_candidates: 0`

## Что ещё подкручено в общей очереди

- `reportIssueKey(item)` расширен на:
  - `gpu_issue`
  - `ram_issue`
- В мета-блоке очереди теперь поддерживаются:
  - `gpu_vendor`
  - `gpu_model`
  - `gpu_sku`
  - `ram_brand`
  - `ram_sku`

Это нужно, чтобы отчёт не показывал нули для новых категорий.

## Важные рабочие принципы проекта

### Приоритеты по ID

Фактический приоритет сейчас такой:

1. Последняя ручная вставка ID в поле
2. Последний ручной выбор кандидата
3. Кэш и автоматика ниже

### Подход по категориям N-Tech

- всё новое делается сначала в ручной режим
- автоназначения избегаются
- сначала лучше строгий матчинг, потом уже можно ослаблять

### Принцип матчинга

- для категорий с точным SKU/кодом:
  - код важнее всего
- для CPU:
  - бренд + модель + упаковка
- для материнок:
  - бренд + модель + chipset/socket + WiFi/DDR
- для мониторов:
  - бренд + модель + техпараметры
- для GPU:
  - бренд + GPU + серия + память + точный SKU
- для RAM:
  - DDR + бренд + точный SKU + объём/кит/частота/CL/серия

## Что важно помнить при следующих правках

- не использовать `\\b` в inline-JS шаблонах страницы
- после изменений в `RESULT_PAGE` обязательно проверять, не сломался ли JS
- для категорий с точным кодом всегда лучше:
  - exact-code матч как абсолютный приоритет
  - соседние варианты скрывать или жёстко штрафовать

## Что можно делать дальше

Потенциальные следующие категории:

- SSD
- блоки питания
- корпуса
- охлаждение
- ноутбуки / мини-ПК по отдельной строгой логике

Потенциальные улучшения:

- сделать фоновые долгие прогоны со статусом и прогрессом
- добавить отдельные отчёты по категориям
- добавить явный признак источника текущего ID:
  - ручной ввод
  - ручной выбор
  - кэш
  - база
  - API

