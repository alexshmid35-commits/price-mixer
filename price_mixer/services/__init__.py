"""Price Mixer business services.

Legacy parser/consolidation functions are available only through the explicit
top-level ``mixer.py`` compatibility facade. Importing this package never
loads ``services._legacy`` implicitly.
"""
