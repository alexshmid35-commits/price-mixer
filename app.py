#!/usr/bin/env python3
"""
Price Mixer Web — веб-интерфейс для сведения прайсов поставщиков.

Запуск: python3 app.py
Открыть: http://localhost:5001
"""

import json
import base64
import math
import os
import re
import shutil
import subprocess
import time
import uuid
import zipfile
from contextlib import contextmanager
from urllib.parse import quote
from werkzeug.utils import secure_filename
from difflib import SequenceMatcher
from pathlib import Path
from concurrent.futures import ThreadPoolExecutor, as_completed

import numpy as np
import pandas as pd
import requests
from flask import (
    Flask, render_template_string, request, redirect,
    url_for, send_file, session, jsonify,
)

import queue
import sqlite3
import threading
import urllib.request
import urllib.parse

from mixer import (
    load_url_cache,
    save_url_cache,
    resolve_onliner_urls,
    parse_generic_excel,
    consolidate_simple,
    find_missing_onliner_ids,
    load_id_cache,
    save_id_cache,
    build_id_fanout_map,
    is_trusted_cached_id,
    prune_negative_id_cache,
    warm_url_cache_from_id_cache,
    extract_article,
    extract_article_candidates,
    lookup_id_from_catalog_sheet,
    lookup_catalog_match_details,
    verify_catalog_id_with_prefix,
    _load_catalog_sheet_index,
)

# Глобальный прогресс резолвинга
resolve_status = {"running": False, "resolved": 0, "total": 0, "cached": 0}
market_refresh_status = {
    "running": False,
    "total": 0,
    "done": 0,
    "success": 0,
    "errors": 0,
    "categories": {},
    "recent_errors": [],
    "started_at": 0,
    "finished_at": 0,
}
MARKET_REFRESH_LOCK = threading.RLock()
# Параллельные запросы B2B/catalog при «Обновить Цены Onliner» (I/O-bound).
MARKET_REFRESH_POOL_WORKERS = 28
source_fetch_statuses = {}
SOURCE_FETCH_LOCK = threading.RLock()
verify_all_ids_status = {
    "running": False,
    "total": 0,
    "done": 0,
    "matched": 0,
    "mismatched": 0,
    "errors": 0,
    "items": [],
    "report_items": [],
    "started_at": 0,
    "finished_at": 0,
    "message": "",
}
VERIFY_ALL_IDS_LOCK = threading.RLock()
validate_clean_ids_status = {
    "running": False,
    "total": 0,
    "done": 0,
    "confirmed": 0,
    "cleared": 0,
    "queued": 0,
    "errors": 0,
    "mode": "api",
    "mode_label": "Onliner API",
    "skipped_label": "Пропуск = API не ответил, ID не меняли.",
    "started_at": 0,
    "finished_at": 0,
    "message": "",
}
VALIDATE_CLEAN_IDS_LOCK = threading.RLock()
B2B_TOKEN_LOCK = threading.RLock()
B2B_TOKEN_CACHE = {"access_token": "", "expires_at": 0}
B2B_CATALOG_LOCK = threading.RLock()
B2B_CATALOG_CACHE = {
    "sections": {"ts": 0, "items": []},
    "manufacturers": {},
    "products": {},
    "articles": {},
}
autofill_tgpc_pc_status = {
    "running": False,
    "total": 0,
    "done": 0,
    "applied": 0,
    "skipped": 0,
    "percent": 0,
    "items": [],
    "started_at": 0,
    "finished_at": 0,
    "message": "",
}
AUTOFILL_TGPC_PC_LOCK = threading.RLock()
autofill_iven_status = {
    "running": False,
    "total": 0,
    "done": 0,
    "applied": 0,
    "skipped": 0,
    "percent": 0,
    "started_at": 0,
    "finished_at": 0,
    "message": "",
    "matches": [],   # [{name, matched_name, score, id, url, source}]
    "no_match": [],  # [{name}] — не нашли
    "report_mode": "iven",
    "report_title": "Отчёт подбора IVEN-бридж",
    "report_subtitle": "Сопоставление N-Tech товаров с базой Onliner ID",
}
AUTOFILL_IVEN_LOCK = threading.RLock()
AUTO_REFRESH_INTERVAL_SEC = 12 * 3600
AUTO_REFRESH_MAX_IDS = 1200
AUTO_REFRESH_POLL_SEC = 20
AUTO_REFRESH_ALLOWED_HOURS = (3, 6, 12)

app = Flask(__name__)
app.secret_key = os.urandom(24)
CONSOLIDATED_IO_LOCK = threading.RLock()
BACKGROUND_STARTED = False
LAST_ACTIVE_SESSION_DIR = None
LAST_UPLOAD_CLEANUP_TS = 0

UPLOAD_DIR = Path(__file__).parent / "uploads"
UPLOAD_DIR.mkdir(exist_ok=True)
UPLOAD_KEEP_LAST_SESSIONS = 20
UPLOAD_KEEP_DAYS = 7
UPLOAD_KEEP_API_FETCH_HOURS = 12

OPENAI_API_KEY = os.getenv("OPENAI_API_KEY", "").strip()
OPENAI_AUTOSORT_MODEL = os.getenv("OPENAI_AUTOSORT_MODEL", "gpt-4o-mini").strip() or "gpt-4o-mini"
OPENAI_AUTOSORT_TIMEOUT_SEC = 9
OPENAI_AUTOSORT_MAX_ITEMS = 320
OPENAI_AUTOSORT_MAX_WORKERS = 10
AI_CATEGORY_CACHE = {}
AI_CATEGORY_CACHE_LOCK = threading.Lock()


def _ensure_background_workers():
    global BACKGROUND_STARTED
    if BACKGROUND_STARTED:
        return
    BACKGROUND_STARTED = True
    threading.Thread(target=_auto_market_refresh_loop, daemon=True).start()


def _cleanup_old_uploads(exclude_dirs=None):
    exclude = {str(Path(p).resolve()) for p in (exclude_dirs or []) if p}
    now = time.time()
    cleanup_cfg = (load_app_settings().get("uploads_cleanup") or {})
    keep_last_sessions = int(cleanup_cfg.get("keep_last_sessions", UPLOAD_KEEP_LAST_SESSIONS) or UPLOAD_KEEP_LAST_SESSIONS)
    keep_days = int(cleanup_cfg.get("keep_days", UPLOAD_KEEP_DAYS) or UPLOAD_KEEP_DAYS)
    keep_api_fetch_hours = int(cleanup_cfg.get("keep_api_fetch_hours", UPLOAD_KEEP_API_FETCH_HOURS) or UPLOAD_KEEP_API_FETCH_HOURS)
    keep_session_sec = keep_days * 24 * 3600
    keep_api_sec = keep_api_fetch_hours * 3600
    try:
        dirs = [p for p in UPLOAD_DIR.iterdir() if p.is_dir()]
    except Exception:
        return {"removed": 0, "skipped": 0}

    session_dirs = []
    api_fetch_dirs = []
    other_dirs = []
    for path in dirs:
        resolved = str(path.resolve())
        if resolved in exclude:
            continue
        if path.name.startswith("_api_fetch_"):
            api_fetch_dirs.append(path)
        elif (path / "consolidated_price.xlsx").exists() or (path / "consolidated.json").exists():
            session_dirs.append(path)
        else:
            other_dirs.append(path)

    removed = 0
    skipped = 0

    session_dirs.sort(key=lambda p: p.stat().st_mtime if p.exists() else 0, reverse=True)
    for idx, path in enumerate(session_dirs):
        try:
            mtime = path.stat().st_mtime
        except Exception:
            skipped += 1
            continue
        age = now - mtime
        if idx < keep_last_sessions:
            continue
        if age < keep_session_sec:
            continue
        try:
            shutil.rmtree(path, ignore_errors=True)
            removed += 1
        except Exception:
            skipped += 1

    for path in api_fetch_dirs + other_dirs:
        try:
            mtime = path.stat().st_mtime
        except Exception:
            skipped += 1
            continue
        age = now - mtime
        if age < keep_api_sec:
            continue
        try:
            shutil.rmtree(path, ignore_errors=True)
            removed += 1
        except Exception:
            skipped += 1

    return {"removed": removed, "skipped": skipped}


def _maybe_cleanup_old_uploads(exclude_dirs=None, min_interval_sec=1800):
    global LAST_UPLOAD_CLEANUP_TS
    now = time.time()
    if now - float(LAST_UPLOAD_CLEANUP_TS or 0) < float(min_interval_sec or 0):
        return {"removed": 0, "skipped": 0, "throttled": True}
    LAST_UPLOAD_CLEANUP_TS = now
    result = _cleanup_old_uploads(exclude_dirs=exclude_dirs)
    result["throttled"] = False
    return result


@app.before_request
def _startup_background_workers():
    global LAST_ACTIVE_SESSION_DIR
    _ensure_background_workers()
    sdir = session.get("session_dir")
    if sdir:
        LAST_ACTIVE_SESSION_DIR = str(sdir)
    exclude = [LAST_ACTIVE_SESSION_DIR, session.get("session_dir")]
    _maybe_cleanup_old_uploads(exclude_dirs=exclude)

# ============================================================
# HTML ШАБЛОНЫ
# ============================================================

BASE_CSS = """
* { box-sizing: border-box; margin: 0; padding: 0; }
body { font-family: Arial, 'Segoe UI', sans-serif; background: #f8fafd; color: #202124; }
.container { max-width: 1400px; margin: 0 auto; padding: 20px; }
h1 { font-size: 28px; margin-bottom: 8px; letter-spacing: -.02em; }
h1 span { color: #1a73e8; }
.subtitle { color: #5f6368; margin-bottom: 30px; font-size: 15px; }
a { color: #1a73e8; text-decoration: none; }
a:hover { text-decoration: underline; }
.btn { display: inline-block; padding: 10px 24px; background: #1a73e8; color: #fff; border: 1px solid #1a73e8; border-radius: 4px; font-size: 14px; font-weight: 500; cursor: pointer; transition: background .15s, border-color .15s; text-decoration: none; box-shadow: none; }
.btn:hover { background: #1765cc; border-color: #1765cc; text-decoration: none; color: #fff; }
.btn-outline { background: #fff; border: 1px solid #cfd8e3; color: #374151; }
.btn-outline:hover { background: #f8f9fa; border-color: #b9c7d8; color: #202124; }
.card { background: #fff; border-radius: 8px; padding: 24px; box-shadow: none; margin-bottom: 20px; border: 1px solid #d9e2f1; }
.upload-head { display: flex; justify-content: space-between; align-items: flex-start; gap: 12px; margin-bottom: 20px; flex-wrap: wrap; }
.upload-head-actions { display: flex; gap: 8px; flex-wrap: wrap; }
.settings-modal { position: fixed; inset: 0; background: rgba(15,23,42,.36); display: none; align-items: center; justify-content: center; z-index: 1200; backdrop-filter: blur(2px); padding: 16px; overflow: auto; }
.settings-modal.active { display: flex; }
.settings-sheet { width: min(860px, calc(100vw - 32px)); max-height: calc(100vh - 32px); overflow: auto; background: #f8fafd; border: 1px solid #d9e2f1; border-radius: 12px; box-shadow: 0 20px 48px rgba(15,23,42,.22); padding: 14px; }
.settings-head { display: flex; justify-content: space-between; align-items: center; gap: 10px; padding-bottom: 8px; margin-bottom: 12px; border-bottom: 1px solid #d9e2f1; }
.settings-title { font-size: 15px; font-weight: 700; color: #1f2937; }
.settings-close { padding: 7px 12px; }
.settings-card { background: #fff; border: 1px solid #d9e2f1; border-radius: 8px; padding: 14px; }
.settings-note { font-size: 13px; color: #5f6368; line-height: 1.45; }
.settings-list { margin-top: 10px; padding-left: 18px; color: #5f6368; font-size: 13px; line-height: 1.5; }
.settings-form { display: grid; gap: 12px; }
.settings-tabs { display: flex; gap: 8px; flex-wrap: wrap; margin-bottom: 2px; }
.settings-tab { padding: 8px 12px; border: 1px solid #cfd8e3; border-radius: 999px; background: #fff; color: #334155; font-size: 13px; font-weight: 600; cursor: pointer; transition: background .15s, border-color .15s, color .15s; }
.settings-tab:hover { background: #f8fafc; border-color: #b9c7d8; }
.settings-tab.active { background: #1a73e8; border-color: #1a73e8; color: #fff; }
.settings-pane { display: none; gap: 12px; }
.settings-pane.active { display: grid; }
.settings-section { border: 1px solid #d9e2f1; border-radius: 8px; background: #fff; padding: 12px; }
.settings-section h3 { font-size: 14px; color: #1f2937; margin-bottom: 8px; }
.settings-grid { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 10px; }
.settings-field { display: flex; flex-direction: column; gap: 5px; }
.settings-field label { font-size: 12px; color: #5f6368; font-weight: 500; }
.settings-field input, .settings-field select, .settings-field textarea { width: 100%; padding: 8px 10px; border: 1px solid #cfd8e3; border-radius: 4px; font-size: 13px; background: #fff; }
.settings-field textarea { min-height: 86px; resize: vertical; }
.settings-field.wide { grid-column: 1 / -1; }
.settings-checkbox { display: flex; align-items: center; gap: 8px; font-size: 13px; color: #374151; }
.settings-checkbox input { width: 15px; height: 15px; }
.settings-help { margin-top: 6px; font-size: 12px; color: #64748b; line-height: 1.45; white-space: pre-wrap; }
.settings-actions { display: flex; justify-content: flex-end; gap: 8px; margin-top: 4px; }
@media (max-width: 900px) {
  .settings-grid { grid-template-columns: 1fr; }
  .settings-tabs { overflow-x: auto; flex-wrap: nowrap; padding-bottom: 2px; }
  .settings-tab { white-space: nowrap; }
  .settings-sheet { width: min(100%, calc(100vw - 20px)); max-height: calc(100vh - 20px); padding: 12px; }
  .settings-modal { padding: 10px; align-items: flex-start; }
}
"""

UPLOAD_PAGE = (
    '<!DOCTYPE html><html lang="ru"><head><meta charset="UTF-8">'
    '<meta name="viewport" content="width=device-width, initial-scale=1.0">'
    '<title>Price Mixer</title><style>'
    + BASE_CSS
    + """
.drop-zone { border: 3px dashed #d0d0e0; border-radius: 16px; padding: 50px 30px; text-align: center; transition: border-color .2s, background .2s; cursor: pointer; }
.drop-zone:hover, .drop-zone.drag-over { border-color: #6c63ff; background: #f8f7ff; }
.drop-zone.has-files { border-color: #4caf50; background: #f0faf0; }
.drop-zone input[type=file] { display: none; }
.drop-zone .icon { font-size: 48px; margin-bottom: 12px; }
.drop-zone .main-text { font-size: 18px; font-weight: 600; margin-bottom: 8px; }
.drop-zone .sub-text { font-size: 14px; color: #888; }
.file-list { margin-top: 20px; text-align: left; }
.file-item { display: flex; align-items: center; gap: 12px; padding: 12px 16px; background: #f8f7ff; border-radius: 8px; margin-bottom: 6px; font-size: 14px; }
.file-item .fname { font-weight: 500; flex: 1; }
.file-item input[type="text"] { padding: 6px 10px; border: 1px solid #ddd; border-radius: 4px; width: 150px; font-size: 13px; }
.file-item input[type="text"]:focus { border-color: #6c63ff; outline: none; }
.file-item .remove { color: #e74c3c; cursor: pointer; font-size: 18px; padding: 0 4px; }
.actions { text-align: center; margin-top: 20px; }
.error { background: #fff0f0; color: #c0392b; padding: 14px 20px; border-radius: 8px; margin-bottom: 20px; border-left: 4px solid #c0392b; }
.spinner { display: none; text-align: center; padding: 40px; }
.spinner.active { display: block; }
.spinner .dot { display: inline-block; width: 12px; height: 12px; border-radius: 50%; background: #6c63ff; margin: 0 4px; animation: bounce .6s infinite alternate; }
.spinner .dot:nth-child(2) { animation-delay: .2s; }
.spinner .dot:nth-child(3) { animation-delay: .4s; }
@keyframes bounce { to { transform: translateY(-10px); opacity: .5; } }
.hint { margin-top: 16px; font-size: 13px; color: #888; }
.api-sources-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(240px, 1fr)); gap: 12px; }
.api-source-item { border: 1px solid #d9e2f1; border-radius: 8px; padding: 16px; background: #fbfdff; display: flex; flex-direction: column; gap: 10px; }
.api-source-title { display: flex; justify-content: space-between; gap: 10px; align-items: flex-start; }
.api-source-title strong { font-size: 16px; color: #202124; }
.api-source-supplier { font-size: 12px; color: #5f6368; }
.api-source-badge { padding: 3px 8px; border-radius: 999px; font-size: 11px; font-weight: 600; background: #eef3fd; color: #1a73e8; white-space: nowrap; }
.api-source-badge.error { background: #fdeeee; color: #d93025; }
.api-source-badge.ready { background: #e6f4ea; color: #137333; }
.api-source-badge.disabled { background: #f1f3f4; color: #5f6368; }
.api-source-progress { height: 8px; border-radius: 999px; background: #e9eef6; overflow: hidden; }
.api-source-progress > span { display: block; height: 100%; width: 0%; background: #1a73e8; transition: width .2s; }
.api-source-meta { display: flex; justify-content: space-between; gap: 8px; font-size: 12px; color: #5f6368; }
.api-source-message { font-size: 12px; color: #5f6368; min-height: 18px; }
.api-source-time { font-size: 12px; color: #64748b; min-height: 16px; }
.api-source-actions { display: flex; gap: 8px; flex-wrap: wrap; }
.api-history-card { margin-top: 14px; border-top: 1px solid #e5eaf3; padding-top: 14px; }
.api-history-head { display: flex; align-items: flex-start; justify-content: space-between; gap: 10px; margin-bottom: 0; flex-wrap: wrap; }
.api-history-title { font-size: 14px; font-weight: 600; color: #1f2937; }
.api-history-note { font-size: 12px; color: #64748b; }
.api-history-toggle { flex-shrink: 0; padding: 6px 12px; font-size: 12px; }
.api-history-body { display: none; margin-top: 10px; }
.api-history-body.is-open { display: block; }
.api-history-list { display: flex; flex-direction: column; gap: 8px; }
.api-history-row { display: grid; grid-template-columns: 120px 90px 90px minmax(0,1fr) 130px 120px; gap: 10px; align-items: center; border: 1px solid #e2e8f0; border-radius: 8px; background: #fff; padding: 10px 12px; }
.api-history-col { font-size: 12px; color: #334155; }
.api-history-col strong { color: #111827; }
.api-history-status { display: inline-flex; align-items: center; padding: 3px 8px; border-radius: 999px; font-size: 11px; font-weight: 600; }
.api-history-status.ok { background: #e6f4ea; color: #137333; }
.api-history-status.error { background: #fdeeee; color: #d93025; }
.api-history-status.run { background: #eef3fd; color: #1a73e8; }
.api-history-empty { font-size: 12px; color: #64748b; padding: 8px 0; }
@media (max-width: 900px) {
  .api-history-row { grid-template-columns: 1fr; gap: 6px; }
}
.busy-overlay { position: fixed; inset: 0; background: rgba(248,250,253,.82); display: none; align-items: center; justify-content: center; z-index: 1400; backdrop-filter: blur(2px); }
.busy-overlay.active { display: flex; }
.busy-box { width: min(420px, calc(100vw - 32px)); border: 1px solid #d9e2f1; border-radius: 12px; background: #fff; box-shadow: 0 20px 48px rgba(15,23,42,.16); padding: 22px; text-align: center; }
.busy-spinner { width: 34px; height: 34px; border-radius: 50%; border: 3px solid #dbe7ff; border-top-color: #1a73e8; margin: 0 auto 14px; animation: spin 0.8s linear infinite; }
.busy-title { font-size: 16px; font-weight: 700; color: #1f2937; }
.busy-text { margin-top: 8px; font-size: 13px; color: #5f6368; line-height: 1.45; }
@keyframes spin { to { transform: rotate(360deg); } }
</style></head><body>
<div class="container">
<h1>Price <span>Mixer</span></h1>
<p class="subtitle">Загрузите прайсы и укажите имя поставщика для каждого файла</p>

{% if error %}<div class="error">{{ error }}</div>{% endif %}

<form method="POST" action="/upload" enctype="multipart/form-data" id="upload-form">
<div class="card">
    <div class="drop-zone" id="drop-zone">
        <div class="icon">&#128193;</div>
        <div class="main-text">Перетащите файлы сюда или нажмите для выбора</div>
        <div class="sub-text">Поддерживаются .xls и .xlsx</div>
        <input type="file" name="files" id="file-input" multiple accept=".xls,.xlsx">
    </div>
    <div class="file-list" id="file-list"></div>
    <div class="hint">Для каждого файла укажите имя поставщика (например: BN, Tradex, TGPC)</div>
    <div class="actions" id="actions-area" style="display:none; margin-top:12px;">
        <button type="submit" class="btn" id="submit-btn">Обработать прайсы</button>
    </div>
</div>
<div class="card" id="api-sources-card">
    <h2 style="font-size:20px; margin-bottom:8px;">Загрузка через API</h2>
    <p class="subtitle" style="margin-bottom:16px;">Выгрузи прайс по ссылке/API, дождись 100% и обработай его тем же пайплайном, что и ручную загрузку.</p>
    <div class="api-sources-grid" id="api-sources-grid"></div>
    <div id="api-process-ready-wrap" style="display:none;justify-content:center;margin:12px 0 0;">
        <button type="button" class="btn btn-outline" id="api-process-ready-btn">Обработать все готовые API-прайсы</button>
    </div>
    <div class="api-history-card">
        <div class="api-history-head">
            <div>
                <div class="api-history-title">История API-выгрузок</div>
                <div class="api-history-note">Последние загрузки и обработки по API-источникам.</div>
            </div>
            <button type="button" class="btn btn-outline api-history-toggle" id="api-history-toggle-btn">Показать историю</button>
        </div>
        <div class="api-history-body" id="api-history-body">
            <div class="api-history-list" id="api-history-list"></div>
        </div>
    </div>
</div>
<div class="spinner" id="spinner">
    <div class="dot"></div><div class="dot"></div><div class="dot"></div>
    <p style="margin-top:16px; color:#666;">Обработка прайсов... Это может занять до минуты.</p>
</div>
</form>
</div>
<div class="busy-overlay" id="busy-overlay">
    <div class="busy-box">
        <div class="busy-spinner"></div>
        <div class="busy-title" id="busy-title">Обрабатываем прайс</div>
        <div class="busy-text" id="busy-text">Это может занять несколько секунд. Окно не зависло, просто идёт обработка.</div>
    </div>
</div>
<script>
var dt = new DataTransfer();
var fileInput = document.getElementById('file-input');
var dropZone = document.getElementById('drop-zone');
var fileList = document.getElementById('file-list');
var actionsArea = document.getElementById('actions-area');

(function setupUploadSettingsUI(){
    var container = document.querySelector('.container');
    if(!container){ return; }
    var h1 = container.querySelector('h1');
    var subtitle = container.querySelector('.subtitle');
    if(h1 && subtitle && !container.querySelector('.upload-head')){
        var head = document.createElement('div');
        head.className = 'upload-head';
        var titleWrap = document.createElement('div');
        h1.parentNode.insertBefore(head, h1);
        titleWrap.appendChild(h1);
        titleWrap.appendChild(subtitle);
        head.appendChild(titleWrap);
        var actions = document.createElement('div');
        actions.className = 'upload-head-actions';
        var btn = document.createElement('button');
        btn.type = 'button';
        btn.className = 'btn btn-outline';
        btn.id = 'open-settings-btn';
        btn.textContent = 'Настройки';
        actions.appendChild(btn);
        head.appendChild(actions);
    }

    if(!document.getElementById('settings-modal')){
        var modal = document.createElement('div');
        modal.id = 'settings-modal';
        modal.className = 'settings-modal';
        modal.innerHTML =
            '<div class="settings-sheet">' +
                '<div class="settings-head">' +
                    '<div class="settings-title">Настройки</div>' +
                    '<button type="button" class="btn btn-outline settings-close" id="close-settings-btn">Закрыть</button>' +
                '</div>' +
                '<div class="settings-card">' +
                    '<div class="settings-note">Здесь будут жить дополнительные настройки миксера.</div>' +
                    '<ul class="settings-list">' +
                        '<li>параметры поиска и проверки ID</li>' +
                        '<li>рабочие переключатели для отдельных поставщиков</li>' +
                        '<li>другие будущие опции, которые ты захочешь вынести отдельно</li>' +
                    '</ul>' +
                '</div>' +
            '</div>';
        document.body.appendChild(modal);
    }

    var settingsModal = document.getElementById('settings-modal');
    var openBtn = document.getElementById('open-settings-btn');
    var closeBtn = document.getElementById('close-settings-btn');
    if(openBtn && settingsModal){
        openBtn.addEventListener('click', function(){
            settingsModal.classList.add('active');
        });
    }
    if(closeBtn && settingsModal){
        closeBtn.addEventListener('click', function(){
            settingsModal.classList.remove('active');
        });
    }
    if(settingsModal){
        settingsModal.addEventListener('click', function(e){
            if(e.target && e.target.id === 'settings-modal'){
                settingsModal.classList.remove('active');
            }
        });
    }
})();

var uploadAppSettings = null;

(function enhanceUploadSettingsUI(){
    var settingsModal = document.getElementById('settings-modal');
    if(!settingsModal){ return; }
    settingsModal.innerHTML = `
        <div class="settings-sheet">
            <div class="settings-head">
                <div class="settings-title">Настройки</div>
                <button type="button" class="btn btn-outline settings-close" id="close-settings-btn">Закрыть</button>
            </div>
            <div class="settings-form">
                <div class="settings-tabs">
                    <button type="button" class="settings-tab active" data-settings-tab="general">Основные</button>
                    <button type="button" class="settings-tab" data-settings-tab="api">API и секреты</button>
                </div>
                <div class="settings-pane active" data-settings-pane="general">
                    <div class="settings-section">
                        <h3>1. Выгрузка</h3>
                        <div class="settings-grid">
                            <div class="settings-field wide"><label class="settings-checkbox"><input type="checkbox" id="st-export-include-without-id"> Выгружать товары без ID</label></div>
                            <div class="settings-field wide"><label class="settings-checkbox"><input type="checkbox" id="st-ui-show-checks-block"> Показывать блок проверок</label></div>
                            <div class="settings-field"><label for="st-export-price-name">Имя выгружаемого прайса</label><input id="st-export-price-name" type="text" placeholder="consolidated_price"></div>
                            <div class="settings-field wide"><label class="settings-checkbox"><input type="checkbox" id="st-export-keep-lowest-price-per-id"> Не выгружать дубли OnlinerID (оставлять строку с минимальной ценой)</label></div>
                            <div class="settings-field"><label for="st-export-only-pc-price-name">Имя прайса при выгрузке только ПЭВМ</label><input id="st-export-only-pc-price-name" type="text" placeholder="N-tech_TGPC_Beznal"></div>
                            <div class="settings-field wide">
                                <label for="st-export-skip-duplicate-suppliers">Не включать в выгрузку товары с одинаковыми OnlinerID для поставщиков</label>
                                <textarea id="st-export-skip-duplicate-suppliers" spellcheck="false" placeholder="Например: TGPC"></textarea>
                                <div class="settings-help">По одному поставщику на строку. Если у поставщика один и тот же OnlinerID стоит у разных товаров, такие конфликтные строки не попадут в скачиваемый прайс.</div>
                            </div>
                            <div class="settings-field wide">
                                <label for="st-export-only-pc-suppliers">Выгружать только ПЭВМ для поставщиков</label>
                                <textarea id="st-export-only-pc-suppliers" spellcheck="false" placeholder="Например: TGPC"></textarea>
                                <div class="settings-help">По одному поставщику на строку. Для этих поставщиков в скачиваемый прайс попадут только строки ПЭВМ / системных блоков.</div>
                            </div>
                            <div class="settings-field wide">
                                <label for="st-google-sheet-url-or-id">Google Таблица — ссылка или ID</label>
                                <input id="st-google-sheet-url-or-id" type="text" placeholder="https://docs.google.com/spreadsheets/d/.../edit или только ID">
                                <div class="settings-help">Для кнопки «В Google Таблицу» на странице результата. Таблицу нужно расшарить на e-mail сервисного аккаунта из JSON (роль «Редактор»).</div>
                            </div>
                            <div class="settings-field"><label for="st-google-sheet-tab">Имя листа (вкладки)</label><input id="st-google-sheet-tab" type="text" placeholder="Прайс N-Tech"></div>
                            <div class="settings-field wide">
                                <label for="st-google-sa-json">Путь к JSON ключу сервисного аккаунта</label>
                                <input id="st-google-sa-json" type="text" placeholder="google-service-account.json">
                                <div class="settings-help">Файл из Google Cloud (IAM → сервисные аккаунты → ключи → JSON). Можно: имя файла рядом с app.py, подпапка (например secrets/key.json), полный путь или ~/Downloads/key.json. После правок обязательно нажмите «Сохранить настройки».</div>
                            </div>
                            <div class="settings-field wide">
                                <label for="st-db-gsheet-id">Onliner DB: ID Google таблицы</label>
                                <input id="st-db-gsheet-id" type="text" placeholder="ID таблицы для импорта каталога Onliner">
                            </div>
                            <div class="settings-field"><label for="st-db-gsheet-sheet">Onliner DB: Лист</label><input id="st-db-gsheet-sheet" type="text" placeholder="All_Catalog"></div>
                        </div>
                    </div>
                    <div class="settings-section">
                        <h3>3. Поставщики</h3>
                        <div class="settings-grid">
                            <div class="settings-field wide">
                                <label for="st-supplier-rules">Правила автоопределения поставщика по имени файла</label>
                                <textarea id="st-supplier-rules" spellcheck="false"></textarea>
                                <div class="settings-help">Каждая строка: часть_имени_файла=ИмяПоставщика
Пример:
price_bn=TGPC
tradex=Tradex
1374=BN-1374</div>
                            </div>
                        </div>
                    </div>
                    <div class="settings-section" style="display:none;">
                        <h3>5. Подбор Без OnlinerID</h3>
                        <div class="settings-grid">
                            <div class="settings-field"><label for="st-noid-max-candidates">Кандидатов показывать</label><input id="st-noid-max-candidates" type="number" min="10" max="150"></div>
                            <div class="settings-field"><label for="st-noid-max-queries">Запросов на поиск</label><input id="st-noid-max-queries" type="number" min="1" max="8"></div>
                            <div class="settings-field wide"><label class="settings-checkbox"><input type="checkbox" id="st-noid-prefer-paren-model"> Сначала учитывать модель в скобках</label></div>
                            <div class="settings-field wide"><label class="settings-checkbox"><input type="checkbox" id="st-noid-prefer-article"> Учитывать article-like токены и модели</label></div>
                            <div class="settings-field wide"><label class="settings-checkbox"><input type="checkbox" id="st-noid-include-brand"> Добавлять бренд в поисковые запросы</label></div>
                            <div class="settings-field wide"><label class="settings-checkbox"><input type="checkbox" id="st-noid-require-category-hint"> Жестко фильтровать кандидатов по категории</label></div>
                            <div class="settings-field wide">
                                <label for="st-noid-category-rules">Правила по категориям и комплектующим</label>
                                <textarea id="st-noid-category-rules" spellcheck="false"></textarea>
                                <div class="settings-help">Это JSON по категориям. Поля:
query_hint - подсказка к запросу
must_contain - слова, которые желательно видеть в Onliner
ignore_words - слова, которые нужно занижать

Редактируй под свои категории.</div>
                            </div>
                        </div>
                    </div>
                    <div class="settings-section">
                        <h3>6. Очистка Uploads</h3>
                        <div class="settings-grid">
                            <div class="settings-field"><label for="st-cleanup-keep-sessions">Хранить последних сессий</label><input id="st-cleanup-keep-sessions" type="number" min="3" max="200"></div>
                            <div class="settings-field"><label for="st-cleanup-keep-days">Хранить сессии, дней</label><input id="st-cleanup-keep-days" type="number" min="1" max="90"></div>
                            <div class="settings-field"><label for="st-cleanup-keep-api-hours">Хранить временные API-файлы, часов</label><input id="st-cleanup-keep-api-hours" type="number" min="1" max="168"></div>
                            <div class="settings-help">Чистим только старые папки в uploads. Глобальные кэши, ручные ID, категории, наценки и сравнение с прошлой выгрузкой не трогаются.</div>
                        </div>
                    </div>
                    <div class="settings-section">
                        <h3>7. Проверка ID</h3>
                        <div class="settings-grid">
                            <div class="settings-field"><label for="st-verify-threshold">Порог совпадения названия</label><input id="st-verify-threshold" type="number" min="0.1" max="0.99" step="0.01"></div>
                            <div class="settings-field"><label for="st-verify-api-no-name-status">Если API не вернул название</label><select id="st-verify-api-no-name-status"><option value="review">Проверить</option><option value="mismatch">Несовпадение</option></select></div>
                            <div class="settings-field wide"><label class="settings-checkbox"><input type="checkbox" id="st-verify-trust-manual"> Ручное подтверждение считать абсолютным OK</label></div>
                            <div class="settings-field wide"><label class="settings-checkbox"><input type="checkbox" id="st-verify-force-refresh"> При проверке ID всегда заново ходить в API</label></div>
                            <div class="settings-field wide"><label class="settings-checkbox"><input type="checkbox" id="st-verify-require-priority"> Совпадением считать только article/model причины</label></div>
                        </div>
                    </div>
                </div>
                <div class="settings-pane" data-settings-pane="api">
                    <div class="settings-section">
                        <h3>2. Кэш и API</h3>
                        <div class="settings-grid">
                            <div class="settings-field wide"><label class="settings-checkbox"><input type="checkbox" id="st-api-allow-direct"> Разрешить прямые запросы к Onliner</label></div>
                            <div class="settings-field"><label for="st-api-retry-attempts">Повторов запроса</label><input id="st-api-retry-attempts" type="number" min="1" max="12"></div>
                            <div class="settings-field"><label for="st-api-backoff-sec">Пауза между повторами, сек</label><input id="st-api-backoff-sec" type="number" min="0" max="5" step="0.1"></div>
                            <div class="settings-field"><label for="st-api-proxy-cooldown">Cooldown прокси, сек</label><input id="st-api-proxy-cooldown" type="number" min="10" max="3600"></div>
                            <div class="settings-field"><label for="st-api-max-workers">Параллельных запросов</label><input id="st-api-max-workers" type="number" min="1" max="24"></div>
                        </div>
                    </div>
                    <div class="settings-section">
                        <h3>2.1 Onliner B2B API</h3>
                        <div class="settings-grid">
                            <div class="settings-field wide"><label class="settings-checkbox"><input type="checkbox" id="st-b2b-enabled"> Включить Onliner B2B API</label></div>
                            <div class="settings-field"><label for="st-b2b-base-url">Base URL</label><input id="st-b2b-base-url" type="text" placeholder="https://b2bapi.onliner.by"></div>
                            <div class="settings-field"><label for="st-b2b-token-url">Token URL</label><input id="st-b2b-token-url" type="text" placeholder="https://b2bapi.onliner.by/oauth/token"></div>
                            <div class="settings-field wide"><label for="st-b2b-price-api-url">Price API (экспорт позиций)</label><input id="st-b2b-price-api-url" type="text" placeholder="https://price.api.onliner.by"></div>
                            <div class="settings-field"><label for="st-b2b-client-id">Client ID</label><input id="st-b2b-client-id" type="text" placeholder="Client ID"></div>
                            <div class="settings-field"><label for="st-b2b-client-secret">Client Secret</label><input id="st-b2b-client-secret" type="password" placeholder="Client Secret"></div>
                            <div class="settings-field"><label for="st-b2b-timeout-sec">Timeout, сек</label><input id="st-b2b-timeout-sec" type="number" min="5" max="120"></div>
                            <div class="settings-field wide"><label class="settings-checkbox"><input type="checkbox" id="st-b2b-verify-ssl"> Проверять SSL у B2B API</label></div>
                            <div class="settings-field wide">
                                <button type="button" class="btn btn-outline" id="test-b2b-btn">Проверить B2B</button>
                                <div class="settings-help" id="st-b2b-test-note">Проверяет получение OAuth-токена и запрос <code>/shop</code>.</div>
                            </div>
                            <div class="settings-field wide">
                                <button type="button" class="btn btn-outline" id="probe-b2b-btn">Тест B2B данных</button>
                                <div class="settings-help" id="st-b2b-probe-note">Пробует получить список разделов, производителей и товаров через B2B API.</div>
                            </div>
                            <div class="settings-help">Ключи из приложения <code>onliner-b2b-api</code> в кабинете Onliner. Рыночные цены в прайсе: сначала быстрый запрос к публичному catalog API; при отсутствии данных — запасной путь через <code>price.api.onliner.by</code>. Подбор без OnlinerID и тесты B2B по-прежнему используют каталог B2B.</div>
                        </div>
                    </div>
                    <div class="settings-section">
                        <h3>4. Источники API</h3>
                        <div class="settings-grid">
                            <div class="settings-field wide">
                                <label class="settings-checkbox"><input type="checkbox" id="st-src-iven-enabled"> IVEN активен</label>
                            </div>
                            <div class="settings-field"><label for="st-src-iven-supplier">Имя поставщика IVEN</label><input id="st-src-iven-supplier" type="text"></div>
                            <div class="settings-field"><label for="st-src-iven-url">URL файла IVEN</label><input id="st-src-iven-url" type="text" placeholder="https://...xlsx"></div>
                            <div class="settings-field wide"><label class="settings-checkbox"><input type="checkbox" id="st-src-iven-verify-ssl"> Проверять SSL у IVEN</label></div>
                            <div class="settings-field wide">
                                <label class="settings-checkbox"><input type="checkbox" id="st-src-tradex-enabled"> Tradex активен</label>
                            </div>
                            <div class="settings-field"><label for="st-src-tradex-supplier">Имя поставщика Tradex</label><input id="st-src-tradex-supplier" type="text"></div>
                            <div class="settings-field"><label for="st-src-tradex-url">URL файла Tradex</label><input id="st-src-tradex-url" type="text" placeholder="https://...xlsx"></div>
                            <div class="settings-field wide"><label class="settings-checkbox"><input type="checkbox" id="st-src-tradex-verify-ssl"> Проверять SSL у Tradex</label></div>
                            <div class="settings-field wide">
                                <label class="settings-checkbox"><input type="checkbox" id="st-src-ntech-enabled"> N-Tech активен</label>
                            </div>
                            <div class="settings-field"><label for="st-src-ntech-supplier">Имя поставщика N-Tech</label><input id="st-src-ntech-supplier" type="text"></div>
                            <div class="settings-field"><label for="st-src-ntech-auth-url">Auth URL N-Tech</label><input id="st-src-ntech-auth-url" type="text"></div>
                            <div class="settings-field"><label for="st-src-ntech-price-url">Price URL N-Tech</label><input id="st-src-ntech-price-url" type="text"></div>
                            <div class="settings-field"><label for="st-src-ntech-username">Логин N-Tech</label><input id="st-src-ntech-username" type="text"></div>
                            <div class="settings-field"><label for="st-src-ntech-password">Пароль N-Tech</label><input id="st-src-ntech-password" type="password"></div>
                            <div class="settings-field wide"><label class="settings-checkbox"><input type="checkbox" id="st-src-ntech-verify-ssl"> Проверять SSL у N-Tech</label></div>
                        </div>
                    </div>
                </div>
                <div class="settings-actions">
                    <button type="button" class="btn btn-outline" id="reset-settings-btn">Сбросить</button>
                    <button type="button" class="btn" id="save-settings-btn">Сохранить настройки</button>
                </div>
                <div class="settings-note" id="settings-save-note"></div>
            </div>
        </div>`;

    function rulesToText(rules){
        if(!Array.isArray(rules)){ return ''; }
        return rules.map(function(it){
            return String((it && it.pattern) || '').trim() + '=' + String((it && it.supplier) || '').trim();
        }).filter(Boolean).join('\\n');
    }

    function textToRules(text){
        return String(text || '').split(/\\r?\\n/).map(function(line){
            var raw = String(line || '').trim();
            if(!raw || raw.indexOf('=') < 0){ return null; }
            var parts = raw.split('=');
            var pattern = String(parts.shift() || '').trim();
            var supplier = String(parts.join('=') || '').trim();
            if(!pattern || !supplier){ return null; }
            return {pattern: pattern, supplier: supplier};
        }).filter(Boolean);
    }

    function applySettingsToForm(settings){
        uploadAppSettings = settings || {};
        var exportCfg = uploadAppSettings.export || {};
        var uiCfg = uploadAppSettings.ui || {};
        var cacheApi = uploadAppSettings.cache_api || {};
        var b2b = uploadAppSettings.onliner_b2b || {};
        var suppliers = uploadAppSettings.suppliers || {};
        var apiSources = uploadAppSettings.api_sources || {};
        var dbGsheet = uploadAppSettings.onliner_db_import || {};
        var uploadsCleanup = uploadAppSettings.uploads_cleanup || {};
        var iven = apiSources.iven || {};
        var tradex = apiSources.tradex || {};
        var ntech = apiSources.ntech || {};
        var noId = uploadAppSettings.no_id_search || {};
        var verifyId = uploadAppSettings.verify_id || {};
        document.getElementById('st-export-include-without-id').checked = !!exportCfg.include_without_id;
        document.getElementById('st-ui-show-checks-block').checked = (uiCfg.show_checks_block !== false);
        document.getElementById('st-export-price-name').value = exportCfg.price_name || 'consolidated_price';
        document.getElementById('st-export-keep-lowest-price-per-id').checked = !!exportCfg.keep_lowest_price_per_onliner_id;
        document.getElementById('st-export-only-pc-price-name').value = exportCfg.only_pc_price_name || 'N-tech_TGPC_Beznal';
        document.getElementById('st-export-skip-duplicate-suppliers').value = Array.isArray(exportCfg.exclude_duplicate_id_suppliers) ? exportCfg.exclude_duplicate_id_suppliers.join('\\n') : '';
        document.getElementById('st-export-only-pc-suppliers').value = Array.isArray(exportCfg.only_pc_suppliers) ? exportCfg.only_pc_suppliers.join('\\n') : '';
        document.getElementById('st-google-sheet-url-or-id').value = exportCfg.google_sheets_spreadsheet_url_or_id || '';
        document.getElementById('st-google-sheet-tab').value = exportCfg.google_sheets_tab || 'Прайс N-Tech';
        document.getElementById('st-google-sa-json').value = exportCfg.google_sheets_service_account_json || '';
        document.getElementById('st-db-gsheet-id').value = dbGsheet.google_sheet_id || '';
        document.getElementById('st-db-gsheet-sheet').value = dbGsheet.google_sheet_name || 'All_Catalog';
        document.getElementById('st-api-allow-direct').checked = !!cacheApi.allow_direct;
        document.getElementById('st-api-retry-attempts').value = cacheApi.retry_attempts || 3;
        document.getElementById('st-api-backoff-sec').value = cacheApi.backoff_sec || 0.6;
        document.getElementById('st-api-proxy-cooldown').value = cacheApi.proxy_cooldown_sec || 180;
        document.getElementById('st-api-max-workers').value = cacheApi.max_parallel_workers || 10;
        document.getElementById('st-b2b-enabled').checked = !!b2b.enabled;
        document.getElementById('st-b2b-base-url').value = b2b.base_url || 'https://b2bapi.onliner.by';
        document.getElementById('st-b2b-token-url').value = b2b.token_url || 'https://b2bapi.onliner.by/oauth/token';
        document.getElementById('st-b2b-price-api-url').value = b2b.price_api_base_url || 'https://price.api.onliner.by';
        document.getElementById('st-b2b-client-id').value = b2b.client_id || '';
        document.getElementById('st-b2b-client-secret').value = b2b.client_secret || '';
        document.getElementById('st-b2b-timeout-sec').value = b2b.timeout_sec || 20;
        document.getElementById('st-b2b-verify-ssl').checked = (b2b.verify_ssl !== false);
        document.getElementById('st-supplier-rules').value = rulesToText(suppliers.filename_rules || []);
        document.getElementById('st-src-iven-enabled').checked = !!iven.enabled;
        document.getElementById('st-src-iven-supplier').value = iven.supplier || 'IVEN';
        document.getElementById('st-src-iven-url').value = iven.file_url || '';
        document.getElementById('st-src-iven-verify-ssl').checked = !!iven.verify_ssl;
        document.getElementById('st-src-tradex-enabled').checked = !!tradex.enabled;
        document.getElementById('st-src-tradex-supplier').value = tradex.supplier || 'Tradex';
        document.getElementById('st-src-tradex-url').value = tradex.file_url || '';
        document.getElementById('st-src-tradex-verify-ssl').checked = !!tradex.verify_ssl;
        document.getElementById('st-src-ntech-enabled').checked = !!ntech.enabled;
        document.getElementById('st-src-ntech-supplier').value = ntech.supplier || 'N-Tech';
        document.getElementById('st-src-ntech-auth-url').value = ntech.auth_url || '';
        document.getElementById('st-src-ntech-price-url').value = ntech.price_url || '';
        document.getElementById('st-src-ntech-username').value = ntech.username || '';
        document.getElementById('st-src-ntech-password').value = ntech.password || '';
        document.getElementById('st-src-ntech-verify-ssl').checked = !!ntech.verify_ssl;
        document.getElementById('st-cleanup-keep-sessions').value = uploadsCleanup.keep_last_sessions || 20;
        document.getElementById('st-cleanup-keep-days').value = uploadsCleanup.keep_days || 7;
        document.getElementById('st-cleanup-keep-api-hours').value = uploadsCleanup.keep_api_fetch_hours || 12;
        document.getElementById('st-noid-max-candidates').value = noId.max_candidates || 80;
        document.getElementById('st-noid-max-queries').value = noId.max_queries || 4;
        document.getElementById('st-noid-prefer-paren-model').checked = !!noId.prefer_paren_model;
        document.getElementById('st-noid-prefer-article').checked = !!noId.prefer_article_tokens;
        document.getElementById('st-noid-include-brand').checked = !!noId.include_brand_token;
        document.getElementById('st-noid-require-category-hint').checked = !!noId.require_category_hint;
        document.getElementById('st-noid-category-rules').value = noId.category_rules_text || '';
        document.getElementById('st-verify-threshold').value = verifyId.match_threshold || 0.74;
        document.getElementById('st-verify-api-no-name-status').value = verifyId.api_no_name_status || 'review';
        document.getElementById('st-verify-trust-manual').checked = !!verifyId.trust_manual_confirmed;
        document.getElementById('st-verify-force-refresh').checked = !!verifyId.force_refresh_api;
        document.getElementById('st-verify-require-priority').checked = !!verifyId.require_article_or_model_priority;
    }

    function collectSettingsForm(){
        return {
            export: {
                include_without_id: document.getElementById('st-export-include-without-id').checked,
                price_name: document.getElementById('st-export-price-name').value,
                keep_lowest_price_per_onliner_id: document.getElementById('st-export-keep-lowest-price-per-id').checked,
                only_pc_price_name: document.getElementById('st-export-only-pc-price-name').value,
                exclude_duplicate_id_suppliers: String(document.getElementById('st-export-skip-duplicate-suppliers').value || '').split(/\\r?\\n/).map(function(v){
                    return String(v || '').trim();
                }).filter(Boolean),
                only_pc_suppliers: String(document.getElementById('st-export-only-pc-suppliers').value || '').split(/\\r?\\n/).map(function(v){
                    return String(v || '').trim();
                }).filter(Boolean),
                google_sheets_spreadsheet_url_or_id: document.getElementById('st-google-sheet-url-or-id').value,
                google_sheets_tab: document.getElementById('st-google-sheet-tab').value,
                google_sheets_service_account_json: document.getElementById('st-google-sa-json').value
            },
            onliner_db_import: {
                google_sheet_id: document.getElementById('st-db-gsheet-id').value,
                google_sheet_name: document.getElementById('st-db-gsheet-sheet').value
            },
            ui: {
                show_checks_block: document.getElementById('st-ui-show-checks-block').checked
            },
            cache_api: {
                allow_direct: document.getElementById('st-api-allow-direct').checked,
                retry_attempts: document.getElementById('st-api-retry-attempts').value,
                backoff_sec: document.getElementById('st-api-backoff-sec').value,
                proxy_cooldown_sec: document.getElementById('st-api-proxy-cooldown').value,
                max_parallel_workers: document.getElementById('st-api-max-workers').value
            },
            onliner_b2b: {
                enabled: document.getElementById('st-b2b-enabled').checked,
                base_url: document.getElementById('st-b2b-base-url').value,
                token_url: document.getElementById('st-b2b-token-url').value,
                price_api_base_url: document.getElementById('st-b2b-price-api-url').value,
                client_id: document.getElementById('st-b2b-client-id').value,
                client_secret: document.getElementById('st-b2b-client-secret').value,
                timeout_sec: document.getElementById('st-b2b-timeout-sec').value,
                verify_ssl: document.getElementById('st-b2b-verify-ssl').checked
            },
            suppliers: {
                filename_rules: textToRules(document.getElementById('st-supplier-rules').value)
            },
            api_sources: {
                iven: {
                    enabled: document.getElementById('st-src-iven-enabled').checked,
                    supplier: document.getElementById('st-src-iven-supplier').value,
                    file_url: document.getElementById('st-src-iven-url').value,
                    verify_ssl: document.getElementById('st-src-iven-verify-ssl').checked
                },
                tradex: {
                    enabled: document.getElementById('st-src-tradex-enabled').checked,
                    supplier: document.getElementById('st-src-tradex-supplier').value,
                    file_url: document.getElementById('st-src-tradex-url').value,
                    verify_ssl: document.getElementById('st-src-tradex-verify-ssl').checked
                },
                ntech: {
                    enabled: document.getElementById('st-src-ntech-enabled').checked,
                    supplier: document.getElementById('st-src-ntech-supplier').value,
                    auth_url: document.getElementById('st-src-ntech-auth-url').value,
                    price_url: document.getElementById('st-src-ntech-price-url').value,
                    username: document.getElementById('st-src-ntech-username').value,
                    password: document.getElementById('st-src-ntech-password').value,
                    verify_ssl: document.getElementById('st-src-ntech-verify-ssl').checked
                }
            },
            uploads_cleanup: {
                keep_last_sessions: document.getElementById('st-cleanup-keep-sessions').value,
                keep_days: document.getElementById('st-cleanup-keep-days').value,
                keep_api_fetch_hours: document.getElementById('st-cleanup-keep-api-hours').value
            },
            no_id_search: {
                max_candidates: document.getElementById('st-noid-max-candidates').value,
                max_queries: document.getElementById('st-noid-max-queries').value,
                prefer_paren_model: document.getElementById('st-noid-prefer-paren-model').checked,
                prefer_article_tokens: document.getElementById('st-noid-prefer-article').checked,
                include_brand_token: document.getElementById('st-noid-include-brand').checked,
                require_category_hint: document.getElementById('st-noid-require-category-hint').checked,
                category_rules_text: document.getElementById('st-noid-category-rules').value
            },
            verify_id: {
                match_threshold: document.getElementById('st-verify-threshold').value,
                api_no_name_status: document.getElementById('st-verify-api-no-name-status').value,
                trust_manual_confirmed: document.getElementById('st-verify-trust-manual').checked,
                force_refresh_api: document.getElementById('st-verify-force-refresh').checked,
                require_article_or_model_priority: document.getElementById('st-verify-require-priority').checked
            }
        };
    }

    function activateSettingsTab(tabName){
        var tabs = settingsModal.querySelectorAll('[data-settings-tab]');
        var panes = settingsModal.querySelectorAll('[data-settings-pane]');
        tabs.forEach(function(tab){
            tab.classList.toggle('active', tab.getAttribute('data-settings-tab') === tabName);
        });
        panes.forEach(function(pane){
            pane.classList.toggle('active', pane.getAttribute('data-settings-pane') === tabName);
        });
    }

    function loadAppSettingsToModal(){
        var note = document.getElementById('settings-save-note');
        if(note){ note.textContent = 'Загрузка настроек...'; }
        fetch('/api/app-settings').then(function(r){ return r.json(); }).then(function(d){
            if(!d || d.status !== 'ok'){ throw new Error('load_failed'); }
            applySettingsToForm(d.settings || {});
            if(note){ note.textContent = ''; }
            updateFileList();
            loadApiSourceStatuses();
        }).catch(function(){
            if(note){ note.textContent = 'Не удалось загрузить настройки.'; }
        });
    }

    function saveAppSettingsFromModal(){
        var note = document.getElementById('settings-save-note');
        if(note){ note.textContent = 'Сохраняю настройки...'; }
        fetch('/api/app-settings', {
            method: 'POST',
            headers: {'Content-Type': 'application/json'},
            body: JSON.stringify(collectSettingsForm())
        }).then(function(r){ return r.json(); }).then(function(d){
            if(!d || d.status !== 'ok'){ throw new Error('save_failed'); }
            applySettingsToForm(d.settings || {});
            updateFileList();
            if(note){ note.textContent = 'Настройки сохранены.'; }
        }).catch(function(){
            if(note){ note.textContent = 'Не удалось сохранить настройки.'; }
        });
    }

    function testOnlinerB2BFromModal(){
        var btn = document.getElementById('test-b2b-btn');
        var note = document.getElementById('st-b2b-test-note');
        if(btn){ btn.disabled = true; }
        if(note){ note.textContent = 'Проверяю B2B OAuth...'; }
        fetch('/api/app-settings', {
            method: 'POST',
            headers: {'Content-Type': 'application/json'},
            body: JSON.stringify(collectSettingsForm())
        }).then(function(r){ return r.json(); }).then(function(d){
            if(!d || d.status !== 'ok'){ throw new Error('save_failed'); }
            applySettingsToForm(d.settings || {});
            return fetch('/api/onliner-b2b-test', {method: 'POST'});
        }).then(function(r){ return r.json(); }).then(function(d){
            if(!d || d.status !== 'ok'){
                throw new Error((d && d.message) || 'b2b_test_failed');
            }
            if(note){
                note.textContent = 'B2B OK. /shop → HTTP ' + String(d.http_status || 0) + ', token TTL: ' + String(d.expires_in || 0) + ' сек.';
            }
        }).catch(function(err){
            if(note){ note.textContent = 'B2B ошибка: ' + String((err && err.message) || err || 'unknown'); }
        }).finally(function(){
            if(btn){ btn.disabled = false; }
        });
    }

    function probeOnlinerB2BDataFromModal(){
        var btn = document.getElementById('probe-b2b-btn');
        var note = document.getElementById('st-b2b-probe-note');
        if(btn){ btn.disabled = true; }
        if(note){ note.textContent = 'Проверяю живые B2B данные...'; }
        fetch('/api/app-settings', {
            method: 'POST',
            headers: {'Content-Type': 'application/json'},
            body: JSON.stringify(collectSettingsForm())
        }).then(function(r){ return r.json(); }).then(function(d){
            if(!d || d.status !== 'ok'){ throw new Error('save_failed'); }
            applySettingsToForm(d.settings || {});
            return fetch('/api/onliner-b2b-probe', {method: 'POST'});
        }).then(function(r){ return r.json(); }).then(function(d){
            if(!d || d.status !== 'ok'){
                throw new Error((d && d.message) || 'b2b_probe_failed');
            }
            if(note){
                var extra = '';
                if((d.sections_count || 0) <= 0 || !(d.manufacturers_count || 0)){
                    extra = ' Тип sections: ' + String(d.sections_payload_type || '-') +
                        ', ключи: ' + String((d.sections_payload_keys || []).join(', ') || '-');
                }
                note.textContent =
                    'B2B данные OK. ' +
                    'Разделов: ' + String(d.sections_count || 0) +
                    ', производителей: ' + String(d.manufacturers_count || 0) +
                    ', товаров: ' + String(d.products_count || 0) +
                    '. ' + String(d.message || '') + extra;
            }
        }).catch(function(err){
            if(note){ note.textContent = 'B2B данные: ошибка: ' + String((err && err.message) || err || 'unknown'); }
        }).finally(function(){
            if(btn){ btn.disabled = false; }
        });
    }

    var openBtn = document.getElementById('open-settings-btn');
    var closeBtn = document.getElementById('close-settings-btn');
    if(openBtn){
        openBtn.onclick = function(){
            settingsModal.classList.add('active');
            loadAppSettingsToModal();
        };
    }
    if(closeBtn){
        closeBtn.onclick = function(){
            settingsModal.classList.remove('active');
        };
    }
    var saveBtn = document.getElementById('save-settings-btn');
    if(saveBtn){ saveBtn.onclick = saveAppSettingsFromModal; }
    var testB2bBtn = document.getElementById('test-b2b-btn');
    if(testB2bBtn){ testB2bBtn.onclick = testOnlinerB2BFromModal; }
    var probeB2bBtn = document.getElementById('probe-b2b-btn');
    if(probeB2bBtn){ probeB2bBtn.onclick = probeOnlinerB2BDataFromModal; }
    var resetBtn = document.getElementById('reset-settings-btn');
    if(resetBtn){ resetBtn.onclick = loadAppSettingsToModal; }
    settingsModal.querySelectorAll('[data-settings-tab]').forEach(function(tab){
        tab.onclick = function(){
            activateSettingsTab(tab.getAttribute('data-settings-tab') || 'general');
        };
    });
    settingsModal.onclick = function(e){
        if(e.target && e.target.id === 'settings-modal'){
            settingsModal.classList.remove('active');
        }
    };
    activateSettingsTab('general');
    loadAppSettingsToModal();
})();

var apiSourcePollTimer = null;
var apiSourceAutoProcessMap = {};
var apiReadySources = [];
var busyOverlay = document.getElementById('busy-overlay');
var busyTitleEl = document.getElementById('busy-title');
var busyTextEl = document.getElementById('busy-text');
var busyStageTimer = null;
var busyStageMessages = [];
var busyStageIndex = 0;

function showBusyOverlay(title, text){
    if(busyTitleEl){ busyTitleEl.textContent = title || 'Обрабатываем прайс'; }
    if(busyTextEl){ busyTextEl.textContent = text || 'Это может занять несколько секунд. Окно не зависло, просто идёт обработка.'; }
    if(busyOverlay){ busyOverlay.classList.add('active'); }
}

function updateBusyOverlay(title, text){
    if(busyTitleEl && title){ busyTitleEl.textContent = title; }
    if(busyTextEl && text){ busyTextEl.textContent = text; }
}

function startBusyStageRotation(title, messages, intervalMs){
    if(busyStageTimer){
        window.clearInterval(busyStageTimer);
        busyStageTimer = null;
    }
    busyStageMessages = Array.isArray(messages) ? messages.slice() : [];
    busyStageIndex = 0;
    showBusyOverlay(title, busyStageMessages[0] || 'Идёт обработка...');
    if(busyStageMessages.length <= 1){ return; }
    busyStageTimer = window.setInterval(function(){
        busyStageIndex = (busyStageIndex + 1) % busyStageMessages.length;
        updateBusyOverlay(title, busyStageMessages[busyStageIndex]);
    }, intervalMs || 1800);
}

function hideBusyOverlay(){
    if(busyStageTimer){
        window.clearInterval(busyStageTimer);
        busyStageTimer = null;
    }
    busyStageMessages = [];
    busyStageIndex = 0;
    if(busyOverlay){ busyOverlay.classList.remove('active'); }
}

function sourceBadgeClass(state){
    if(!state || !state.enabled || !state.configured){ return 'api-source-badge disabled'; }
    if(state.status === 'error'){ return 'api-source-badge error'; }
    if(state.ready){ return 'api-source-badge ready'; }
    return 'api-source-badge';
}

function sourceBadgeText(state){
    if(!state || !state.enabled){ return 'Выключен'; }
    if(!state.configured){ return 'Не настроен'; }
    if(state.status === 'error'){ return 'Ошибка'; }
    if(state.ready){ return 'Готов'; }
    if(state.running){ return 'Выгрузка'; }
    return 'Ожидает';
}

function formatApiSourceTime(ts){
    var num = Number(ts || 0);
    if(!num){ return ''; }
    var d = new Date(num * 1000);
    if(!isFinite(d.getTime())){ return ''; }
    var dd = String(d.getDate()).padStart(2, '0');
    var mm = String(d.getMonth() + 1).padStart(2, '0');
    var yyyy = d.getFullYear();
    var hh = String(d.getHours()).padStart(2, '0');
    var mi = String(d.getMinutes()).padStart(2, '0');
    return dd + '.' + mm + '.' + yyyy + ' ' + hh + ':' + mi;
}

function formatHistorySize(bytes){
    var num = Number(bytes || 0);
    if(!num){ return '—'; }
    if(num >= 1024 * 1024){ return (num / (1024 * 1024)).toFixed(2) + ' MB'; }
    if(num >= 1024){ return (num / 1024).toFixed(1) + ' KB'; }
    return num.toLocaleString() + ' B';
}

function escapeHtml(text){
    return String(text == null ? '' : text)
        .replace(/&/g, '&amp;')
        .replace(/</g, '&lt;')
        .replace(/>/g, '&gt;')
        .replace(/"/g, '&quot;')
        .replace(/'/g, '&#39;');
}

function renderApiHistory(items){
    var box = document.getElementById('api-history-list');
    if(!box){ return; }
    var rows = Array.isArray(items) ? items : [];
    if(!rows.length){
        box.innerHTML = '<div class="api-history-empty">История пока пустая.</div>';
        return;
    }
    box.innerHTML = rows.map(function(item){
        var whenText = formatApiSourceTime(item.finished_at || item.started_at);
        var typeText = item.event_type === 'process' ? 'Обработка' : 'Выгрузка';
        var statusClass = item.status === 'error' ? 'error' : (item.status === 'ok' ? 'ok' : 'run');
        var statusText = item.status === 'error' ? 'Ошибка' : (item.status === 'ok' ? 'Успех' : 'В работе');
        var details = [];
        if(item.file_size){ details.push('Файл: ' + formatHistorySize(item.file_size)); }
        if(item.duration_sec){ details.push('Время: ' + Number(item.duration_sec).toLocaleString() + ' сек'); }
        if(item.items_count){ details.push('Товаров: ' + Number(item.items_count).toLocaleString()); }
        if(item.without_id_count || item.without_id_count === 0){ details.push('Без ID: ' + Number(item.without_id_count).toLocaleString()); }
        var metaText = details.length ? details.join(' • ') : '—';
        var message = item.message || '';
        return '' +
            '<div class="api-history-row">' +
                '<div class="api-history-col"><strong>' + escapeHtml(item.label || item.source_key || 'Источник') + '</strong><div>' + escapeHtml(item.supplier || '') + '</div></div>' +
                '<div class="api-history-col">' + escapeHtml(typeText) + '</div>' +
                '<div class="api-history-col"><span class="api-history-status ' + statusClass + '">' + escapeHtml(statusText) + '</span></div>' +
                '<div class="api-history-col">' + escapeHtml(message || metaText) + (message && metaText !== '—' ? ('<div class="api-history-note">' + escapeHtml(metaText) + '</div>') : '') + '</div>' +
                '<div class="api-history-col">' + escapeHtml(whenText || '—') + '</div>' +
                '<div class="api-history-col">' + escapeHtml(item.file_name || '—') + '</div>' +
            '</div>';
    }).join('');
}

function renderApiSources(items){
    var grid = document.getElementById('api-sources-grid');
    if(!grid){ return; }
    var list = Array.isArray(items) ? items : [];
    if(list.length === 0){
        grid.innerHTML = '<div class="settings-note">Пока нет настроенных API-источников.</div>';
        return;
    }
    grid.innerHTML = list.map(function(item){
        var progress = Math.max(0, Math.min(100, Number(item.progress || 0)));
        var disabledFetch = (!item.enabled || !item.configured || item.running) ? 'disabled' : '';
        var disabledFetchProcess = (!item.enabled || !item.configured || item.running) ? 'disabled' : '';
        var disabledProcess = (!item.ready) ? 'disabled' : '';
        var autoLabel = apiSourceAutoProcessMap[item.source_key] ? 'Жду 100%' : 'Выгрузить и обработать';
        var totalText = item.total_bytes ? (' / ' + item.total_bytes.toLocaleString()) : '';
        var message = item.message || (item.ready ? 'Прайс готов к обработке.' : 'Нажми "Выгрузить", чтобы получить прайс.');
        var readyAt = item.ready ? formatApiSourceTime(item.finished_at) : '';
        var timeText = readyAt ? ('Последняя выгрузка: ' + readyAt) : '';
        var processBtnHtml = item.ready
            ? ('<button type="button" class="btn api-source-process" data-source="' + item.source_key + '" ' + disabledProcess + '>Обработать</button>')
            : '';
        return '' +
            '<div class="api-source-item" data-source="' + item.source_key + '">' +
                '<div class="api-source-title">' +
                    '<div><strong>' + item.label + '</strong><div class="api-source-supplier">Поставщик: ' + item.supplier + '</div></div>' +
                    '<span class="' + sourceBadgeClass(item) + '">' + sourceBadgeText(item) + '</span>' +
                '</div>' +
                '<div class="api-source-progress"><span style="width:' + progress + '%"></span></div>' +
                '<div class="api-source-meta"><span>' + progress + '%</span><span>' + Number(item.downloaded || 0).toLocaleString() + totalText + '</span></div>' +
                '<div class="api-source-message">' + message + '</div>' +
                '<div class="api-source-time">' + timeText + '</div>' +
                '<div class="api-source-actions">' +
                    '<button type="button" class="btn btn-outline api-source-fetch" data-source="' + item.source_key + '" ' + disabledFetch + '>Выгрузить</button>' +
                    '<button type="button" class="btn btn-outline api-source-fetch-process" data-source="' + item.source_key + '" ' + disabledFetchProcess + '>' + autoLabel + '</button>' +
                    processBtnHtml +
                '</div>' +
            '</div>';
    }).join('');
}

function scheduleApiSourcePoll(){
    if(apiSourcePollTimer){ window.clearTimeout(apiSourcePollTimer); }
    apiSourcePollTimer = window.setTimeout(loadApiSourceStatuses, 1500);
}

function loadApiSourceStatuses(){
    fetch('/api/source-fetch-status').then(function(r){ return r.json(); }).then(function(d){
        if(!d || d.status !== 'ok'){ throw new Error('status_failed'); }
        var items = Array.isArray(d.items) ? d.items : [];
        apiReadySources = items.filter(function(it){ return !!it.ready; }).map(function(it){ return String(it.source_key || '').trim(); }).filter(Boolean);
        var processAllBtn = document.getElementById('api-process-ready-btn');
        var processAllWrap = document.getElementById('api-process-ready-wrap');
        if(processAllBtn){
            var readyCount = apiReadySources.length;
            processAllBtn.textContent = 'Обработать все готовые API-прайсы (' + readyCount + ')';
            if(processAllWrap){
                processAllWrap.style.display = readyCount > 0 ? 'flex' : 'none';
            }
        }
        renderApiHistory(Array.isArray(d.history) ? d.history : []);
        items.forEach(function(it){
            if(apiSourceAutoProcessMap[it.source_key]){
                if(it.ready){
                    delete apiSourceAutoProcessMap[it.source_key];
                    processApiSource(it.source_key);
                } else if(it.status === 'error'){
                    delete apiSourceAutoProcessMap[it.source_key];
                    hideBusyOverlay();
                }
            }
        });
        var activeSource = null;
        items.forEach(function(it){
            if(!activeSource && (it.running || apiSourceAutoProcessMap[it.source_key])){ activeSource = it; }
        });
        if(activeSource && busyOverlay && busyOverlay.classList.contains('active')){
            var title = apiSourceAutoProcessMap[activeSource.source_key] ? 'Выгружаем и обрабатываем прайс' : 'Выгружаем прайс';
            var message = activeSource.message || 'Ждём ответ поставщика...';
            if(activeSource.status === 'starting'){
                message = activeSource.message || 'Подготавливаем запрос к поставщику...';
            } else if(activeSource.status === 'downloading'){
                if(Number(activeSource.total_bytes || 0) > 0){
                    message = 'Скачиваем файл: ' + Number(activeSource.downloaded || 0).toLocaleString() + ' / ' + Number(activeSource.total_bytes || 0).toLocaleString();
                } else {
                    message = activeSource.message || 'Скачиваем файл поставщика...';
                }
            }
            updateBusyOverlay(title, message);
        } else if(!Object.keys(apiSourceAutoProcessMap).length && busyOverlay && busyOverlay.classList.contains('active') && !busyStageTimer) {
            hideBusyOverlay();
        }
        renderApiSources(items);
        if(items.some(function(it){ return !!it.running; }) || Object.keys(apiSourceAutoProcessMap).length){
            scheduleApiSourcePoll();
        }
    }).catch(function(){
        hideBusyOverlay();
        renderApiSources([]);
    });
}

function startApiSourceFetch(sourceKey){
    var isIven = String(sourceKey || '').trim().toLowerCase() === 'iven';
    if(!isIven){
        showBusyOverlay('Выгружаем прайс', 'Подготавливаем запрос к поставщику...');
    }
    fetch('/api/source-fetch-start', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify({source: sourceKey})
    }).then(function(r){ return r.json(); }).then(function(d){
        if(!d || d.status !== 'ok'){ throw new Error((d && d.message) || 'fetch_failed'); }
        loadApiSourceStatuses();
        scheduleApiSourcePoll();
    }).catch(function(){
        if(!isIven){
            hideBusyOverlay();
        }
        loadApiSourceStatuses();
    });
}

function startApiSourceFetchAndProcess(sourceKey){
    var card = document.querySelector('.api-source-item[data-source="' + sourceKey + '"]');
    var processBtn = card ? card.querySelector('.api-source-process') : null;
    if(processBtn && !processBtn.disabled){
        showBusyOverlay('Обрабатываем прайс', 'Файл уже выгружен. Сейчас запускаем обработку и откроем результат.');
        processApiSource(sourceKey);
        return;
    }
    var isIven = String(sourceKey || '').trim().toLowerCase() === 'iven';
    apiSourceAutoProcessMap[sourceKey] = true;
    if(!isIven){
        showBusyOverlay('Выгружаем и обрабатываем прайс', 'Сначала получим файл у поставщика, потом автоматически запустим обработку.');
    }
    startApiSourceFetch(sourceKey);
    scheduleApiSourcePoll();
}

function processApiSource(sourceKey){
    startBusyStageRotation('Обрабатываем прайс', [
        'Загружаем файл во внутренний конвейер...',
        'Парсим Excel и определяем колонки...',
        'Сводим товары и применяем сохранённые правила...',
        'Подготавливаем результат к открытию...'
    ], 1700);
    fetch('/api/source-process', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify({source: sourceKey})
    }).then(function(r){ return r.json(); }).then(function(d){
        if(!d || d.status !== 'ok' || !d.redirect_url){ throw new Error((d && d.message) || 'process_failed'); }
        window.location.href = d.redirect_url;
    }).catch(function(err){
        hideBusyOverlay();
        alert((err && err.message) ? err.message : 'Не удалось обработать прайс');
    });
}

function processReadyApiSources(){
    if(!Array.isArray(apiReadySources) || apiReadySources.length === 0){
        alert('Нет готовых API-прайсов для объединенной обработки.');
        return;
    }
    startBusyStageRotation('Объединяем API-прайсы', [
        'Проверяем готовые выгрузки поставщиков...',
        'Загружаем файлы во внутренний конвейер...',
        'Сводим товары по всем выбранным поставщикам...',
        'Подготавливаем объединенный результат...'
    ], 1700);
    fetch('/api/source-process-batch', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify({sources: apiReadySources})
    }).then(function(r){ return r.json(); }).then(function(d){
        if(!d || d.status !== 'ok' || !d.redirect_url){ throw new Error((d && d.message) || 'batch_process_failed'); }
        window.location.href = d.redirect_url;
    }).catch(function(err){
        hideBusyOverlay();
        alert((err && err.message) ? err.message : 'Не удалось объединенно обработать API-прайсы');
    });
}

document.addEventListener('click', function(e){
    var processAllBtn = e.target && e.target.closest ? e.target.closest('#api-process-ready-btn') : null;
    if(processAllBtn){
        processReadyApiSources();
        return;
    }
    var fetchBtn = e.target && e.target.closest ? e.target.closest('.api-source-fetch') : null;
    if(fetchBtn){
        startApiSourceFetch(fetchBtn.dataset.source);
        return;
    }
    var processBtn = e.target && e.target.closest ? e.target.closest('.api-source-process') : null;
    if(processBtn){
        processApiSource(processBtn.dataset.source);
        return;
    }
    var fetchProcessBtn = e.target && e.target.closest ? e.target.closest('.api-source-fetch-process') : null;
    if(fetchProcessBtn){
        startApiSourceFetchAndProcess(fetchProcessBtn.dataset.source);
    }
});

function detectSupplier(fname) {
    var fl = fname.toLowerCase();
    var supplierRules = (((uploadAppSettings || {}).suppliers || {}).filename_rules || []);
    for (var r = 0; r < supplierRules.length; r++) {
        var rule = supplierRules[r] || {};
        var pattern = String(rule.pattern || '').toLowerCase().trim();
        var supplier = String(rule.supplier || '').trim();
        if (pattern && supplier && fl.indexOf(pattern) >= 0) return supplier;
    }
    if (fl.indexOf('tradex') >= 0) return 'Tradex';
    if (fl.indexOf('1030z') >= 0) return 'BN-1030Z';
    if (fl.indexOf('1030') >= 0) return 'BN-1030';
    if (fl.indexOf('1374') >= 0) return 'BN-1374';
    if (fl.indexOf('price_bn') >= 0) return 'TGPC';
    return '';
}

function updateFileList() {
    fileList.innerHTML = '';
    if (dt.files.length === 0) {
        dropZone.classList.remove('has-files');
        actionsArea.style.display = 'none';
        fileInput.value = '';
        return;
    }
    dropZone.classList.add('has-files');
    actionsArea.style.display = 'block';
    
    var fileNames = [];
    for (var i = 0; i < dt.files.length; i++) {
        var f = dt.files[i];
        var detected = detectSupplier(f.name);
        var div = document.createElement('div');
        div.className = 'file-item';
        div.innerHTML = '<span class="fname" data-fname="' + f.name + '">' + f.name + '</span>' +
            '<input type="text" class="supplier-input" data-fname="' + f.name + '" value="' + detected + '" placeholder="Имя поставщика">' +
            '<span class="remove" data-fname="'+f.name+'">&times;</span>';
        fileList.appendChild(div);
        fileNames.push(f.name);
    }
    
    document.querySelectorAll('.remove').forEach(function(el){
        el.addEventListener('click', function(e){
            e.stopPropagation();
            var fname = this.dataset.fname;
            var newDt = new DataTransfer();
            for(var j=0; j<dt.files.length; j++){
                if(dt.files[j].name !== fname) newDt.items.add(dt.files[j]);
            }
            dt = newDt;
            updateFileList();
        });
    });
}

fileInput.addEventListener('change', function(){
    for(var i=0; i<this.files.length; i++) dt.items.add(this.files[i]);
    updateFileList();
});

dropZone.addEventListener('click', function(){ fileInput.click(); });
dropZone.addEventListener('dragover', function(e){ e.preventDefault(); this.classList.add('drag-over'); });
dropZone.addEventListener('dragleave', function(e){ this.classList.remove('drag-over'); });
dropZone.addEventListener('drop', function(e){
    e.preventDefault(); this.classList.remove('drag-over');
    for(var i=0; i<e.dataTransfer.files.length; i++) dt.items.add(e.dataTransfer.files[i]);
    updateFileList();
});

(function setupApiHistoryToggle(){
    var btn = document.getElementById('api-history-toggle-btn');
    var body = document.getElementById('api-history-body');
    if(!btn || !body){ return; }
    btn.addEventListener('click', function(){
        var open = body.classList.toggle('is-open');
        btn.textContent = open ? 'Скрыть историю' : 'Показать историю';
    });
})();

document.getElementById('upload-form').addEventListener('submit', function(e){
    var inputs = document.querySelectorAll('.supplier-input');
    for(var i=0; i<inputs.length; i++){
        var inp = inputs[i];
        var hidden = document.createElement('input');
        hidden.type = 'hidden';
        hidden.name = 'supplier_' + encodeURIComponent(inp.dataset.fname);
        hidden.value = inp.value;
        this.appendChild(hidden);
    }
    
    fileInput.files = dt.files;
    
    document.getElementById('submit-btn').disabled = true;
document.getElementById('spinner').classList.add('active');
    startBusyStageRotation('Обрабатываем прайсы', [
        'Загружаем файлы и проверяем структуру...',
        'Парсим Excel и определяем рабочие колонки...',
        'Сводим товары и подставляем найденные ID...',
        'Применяем категории и наценки...',
        'Подготавливаем результат к открытию...'
    ], 1800);
});
</script>
</body></html>"""
)

RESULT_PAGE = (
    '<!DOCTYPE html><html lang="ru"><head><meta charset="UTF-8">'
    '<meta name="viewport" content="width=device-width, initial-scale=1.0">'
    '<title>Price Mixer — Результат</title>'
    '<link rel="stylesheet" href="https://cdn.datatables.net/1.13.7/css/jquery.dataTables.min.css">'
    '<style>'
    + BASE_CSS
    + """
	.table-card { background: #fff; border-radius: 10px; padding: 18px; box-shadow: none; margin-bottom: 16px; overflow-x: auto; border: 1px solid #d9e2f1; }
	table.dataTable { width: 100% !important; font-size: 12px; border-collapse: collapse !important; }
	table.dataTable thead th { background: #f8f9fa; color: #5f6368; font-weight: 600; white-space: nowrap; border-top: 1px solid #d9e2f1 !important; border-bottom: 1px solid #d9e2f1 !important; }
	table.dataTable tbody td { vertical-align: middle; border-bottom: 1px solid #e6edf5 !important; }
	table.dataTable tbody tr:hover { background: #f8fbff !important; }
	#tbl-main th:nth-child(1),
	#tbl-main th:nth-child(3),
	#tbl-main th:nth-child(4),
	#tbl-main th:nth-child(5),
	#tbl-main th:nth-child(6),
	#tbl-main th:nth-child(7),
	#tbl-main th:nth-child(8),
	#tbl-main td:nth-child(1),
	#tbl-main td:nth-child(3),
	#tbl-main td:nth-child(4),
	#tbl-main td:nth-child(5),
	#tbl-main td:nth-child(6),
	#tbl-main td:nth-child(7),
	#tbl-main td:nth-child(8) { text-align: center; }
	#tbl-main td:nth-child(2) { text-align: left; }
	.dataTables_wrapper .dataTables_length,
	.dataTables_wrapper .dataTables_filter { margin-bottom: 10px; color: #5f6368; font-size: 12px; }
	.dataTables_wrapper .dataTables_filter input,
	.dataTables_wrapper .dataTables_length select { border: 1px solid #cfd8e3; border-radius: 4px; background: #fff; padding: 5px 8px; font-size: 12px; }
	.dataTables_wrapper .dataTables_info,
	.dataTables_wrapper .dataTables_paginate { margin-top: 10px; color: #5f6368 !important; font-size: 12px; }
	.dataTables_wrapper .dataTables_paginate .paginate_button { border-radius: 4px !important; border: 1px solid transparent !important; }
	.dataTables_wrapper .dataTables_paginate .paginate_button.current { background: #e8f0fe !important; border-color: #c6dafc !important; color: #174ea6 !important; }
	.top-bar { display: flex; justify-content: space-between; align-items: flex-start; margin-bottom: 16px; flex-wrap: wrap; gap: 12px; padding: 14px 16px; background: #fff; border: 1px solid #d9e2f1; border-radius: 8px; }
	.top-bar > div:last-child { display: flex; gap: 8px; flex-wrap: wrap; }
	.stats-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 10px; margin-bottom: 16px; }
	.stat-card { background: #fff; border-radius: 8px; padding: 18px 16px; text-align: center; border: 1px solid #d9e2f1; min-height: 148px; display: flex; flex-direction: column; justify-content: center; }
	.stat-card .num { font-size: 24px; font-weight: 700; color: #1a73e8; letter-spacing: -.02em; }
.stat-card .label { font-size: 12px; color: #5f6368; margin-top: 6px; }
.stat-card.highlight { background: #fefbf2; border-color: #f0dfb1; }
.stat-card.highlight .num { color: #d56e0c; }
.stat-card.info { background: #f7faff; border-color: #d9e2f1; }
.stat-card.info .num { color: #174ea6; }
.stat-card .btn { min-width: 108px; padding: 7px 14px; font-size: 12px; }
.stat-card .markup-note { font-size: 12px; color: #5f6368; line-height: 1.35; }
.stat-card.stat-card--dual { min-height: 0; padding: 14px 10px; }
.stat-card--dual .stat-card-dual-inner { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); align-items: stretch; gap: 10px 14px; }
.stat-card--dual .stat-card-dual-item { min-width: 0; text-align: center; display: flex; flex-direction: column; justify-content: center; align-items: center; border: 1px solid #e2e8f0; border-radius: 10px; padding: 10px 8px; background: #fff; }
.stat-card--dual .stat-card-dual-item--noid .num { color: #d56e0c; }
.stat-card--dual .stat-card-dual-sep { display: none; }
.stat-card--dual .stat-card-noid-btn-wrap { margin-top: 8px; width: 100%; display: flex; justify-content: center; }
@media (max-width: 760px){
  .stat-card--dual .stat-card-dual-inner { grid-template-columns: 1fr; }
}
.dup-group-start td,
.dup-group-mid td,
.dup-group-end td,
.dup-group-single td {
  background: #f8fbff !important;
  border-left: 2px solid #93c5fd !important;
  border-right: 2px solid #93c5fd !important;
}
.dup-group-start td,
.dup-group-single td {
  border-top: 2px solid #93c5fd !important;
}
.dup-group-end td,
.dup-group-single td {
  border-bottom: 2px solid #93c5fd !important;
}
.snapshot-card { margin-top: 12px; }
.snapshot-actions { display:flex; gap:8px; flex-wrap:wrap; margin-top:12px; }
.snapshot-grid { display: grid; grid-template-columns: repeat(4, minmax(0, 1fr)); gap: 10px; }
.snapshot-mini { border: 1px solid #d9e2f1; border-radius: 8px; padding: 12px; background: #fff; cursor: pointer; transition: border-color .12s ease, box-shadow .12s ease, background-color .12s ease; }
.snapshot-mini:hover { border-color: #c6dafc; background: #f8fbff; }
.snapshot-mini.active { border-color: #1a73e8; background: #eef4ff; box-shadow: inset 0 0 0 1px #1a73e8; }
.snapshot-mini .k { font-size: 12px; color: #5f6368; }
.snapshot-mini .v { font-size: 22px; font-weight: 700; color: #1a73e8; margin-top: 4px; }
.snapshot-mini.warn .v { color: #d56e0c; }
.snapshot-mini.danger .v { color: #d93025; }
.snapshot-hint { margin-top: 10px; font-size: 12px; color: #64748b; }
.snapshot-lists { display: none; margin-top: 12px; }
.snapshot-lists.active { display: block; }
.snapshot-list { display: none; border: 1px solid #e5e7eb; border-radius: 8px; background: #fff; padding: 12px; }
.snapshot-list.active { display: block; }
.snapshot-list h4 { font-size: 13px; color: #1f2937; margin-bottom: 8px; }
.snapshot-list ul { margin: 0; padding-left: 18px; font-size: 12px; color: #334155; line-height: 1.5; }
.snapshot-empty { font-size: 12px; color: #64748b; }
@media (max-width: 1100px){
  .snapshot-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
}
.table-card .markup-note { color: #5f6368; }
.noid-pick-btn { margin-top: 6px; padding: 4px 8px; font-size: 11px; border-radius: 8px; border: 1px solid #6366f1; background: #fff; color: #4f46e5; cursor: pointer; }
.noid-pick-btn:hover { background: #eef2ff; }
.noid-pick-btn:disabled { opacity: .55; cursor: default; }
.noid-inline-picker { margin-top: 8px; padding: 8px 10px; border: 1px solid #dbe5f1; border-radius: 10px; background: #f8fbff; }
.noid-inline-top { display: flex; justify-content: space-between; gap: 8px; align-items: center; margin-bottom: 6px; }
.noid-inline-note { font-size: 11px; color: #64748b; margin-bottom: 6px; }
.noid-inline-list { display: grid; gap: 6px; }
.noid-inline-item { display: flex; justify-content: space-between; gap: 8px; align-items: center; padding: 6px 7px; border: 1px solid #e5e7eb; border-radius: 8px; background: #fff; }
.noid-inline-meta { min-width: 0; }
.noid-inline-title { font-size: 11px; color: #0f172a; line-height: 1.3; }
.noid-inline-sub { font-size: 10px; color: #64748b; margin-top: 2px; }
.noid-inline-badges { display: flex; flex-wrap: wrap; gap: 4px; margin-top: 4px; }
.noid-inline-badge { font-size: 10px; line-height: 1; padding: 3px 6px; border-radius: 999px; background: #eef2ff; color: #4338ca; }
.noid-inline-actions { display: flex; align-items: center; gap: 8px; flex-shrink: 0; }
.noid-inline-apply { padding: 4px 8px; font-size: 11px; border: none; border-radius: 7px; background: #16a34a; color: #fff; cursor: pointer; }
.noid-inline-apply:hover { background: #15803d; }
.noid-inline-apply:disabled { opacity: .55; cursor: default; }
.noid-inline-open { font-size: 11px; }
.noid-inline-close { padding: 0; border: none; background: transparent; color: #475569; font-size: 11px; cursor: pointer; white-space: nowrap; }
.noid-inline-hit { background: #fff3a3; border-radius: 3px; padding: 0 1px; }
.verify-id-list { display: grid; gap: 10px; margin-top: 10px; }
.verify-id-item { border: 1px solid #e2e8f0; border-radius: 12px; background: #fff; padding: 9px 11px; }
.verify-id-head { display: flex; justify-content: space-between; gap: 10px; align-items: flex-start; }
.verify-id-title { font-size: 14px; font-weight: 700; color: #0f172a; line-height: 1.25; }
.verify-id-sub { font-size: 11px; color: #64748b; margin-top: 3px; line-height: 1.35; }
.verify-id-status { display: inline-flex; align-items: center; justify-content: center; padding: 4px 8px; border-radius: 999px; font-size: 11px; font-weight: 700; background: #fff7ed; color: #c2410c; white-space: nowrap; }
.verify-id-actions { display: flex; gap: 8px; flex-wrap: wrap; margin-top: 6px; }
.verify-id-inline-box { margin-top: 10px; }
.verify-id-load-btn { padding: 5px 10px !important; font-size: 12px !important; line-height: 1.1; }
.verify-id-manual-btn { padding: 5px 10px !important; font-size: 12px !important; line-height: 1.1; }
.verify-id-note-link { cursor: pointer; text-decoration: underline; text-decoration-style: dotted; }
.verify-id-report-wrap { display: none; margin-top: 12px; border-top: 1px solid #e5e7eb; padding-top: 12px; }
.verify-id-report-head { display: flex; justify-content: space-between; align-items: center; gap: 10px; margin-bottom: 8px; }
.verify-id-report-table-wrap { max-height: 420px; overflow: auto; border: 1px solid #e5e7eb; border-radius: 10px; background: #fff; }
.verify-id-report-table { width: 100%; border-collapse: collapse; font-size: 12px; }
.verify-id-report-table th { position: sticky; top: 0; background: #f8fafc; z-index: 1; text-align: left; padding: 8px 10px; border-bottom: 1px solid #e5e7eb; color: #334155; }
.verify-id-report-table td { padding: 8px 10px; border-bottom: 1px solid #eef2f7; color: #334155; vertical-align: top; }
.verify-id-report-table tr:last-child td { border-bottom: none; }
.verify-id-report-status { font-weight: 700; white-space: nowrap; }
.verify-id-report-status.ok { color: #15803d; }
.verify-id-report-status.bad { color: #c2410c; }
.verify-id-report-status.review { color: #92400e; }
.quality-card-head { display: flex; justify-content: space-between; align-items: center; gap: 10px; padding: 12px 14px; border-bottom: 1px solid #d9e2f1; }
.quality-card-toggle { padding: 6px 10px !important; font-size: 12px !important; }
.quality-actions { display: flex; justify-content: flex-end; margin-top: 12px; }
.quality-reapply-btn { padding: 8px 14px !important; font-size: 12px !important; border-radius: 4px !important; border: 1px solid #cfd8e3 !important; background: #fff !important; color: #374151 !important; box-shadow: none !important; }
.quality-reapply-btn:hover { background: #f8f9fa !important; border-color: #b9c7d8 !important; }
.verify-card-head { display: flex; justify-content: space-between; align-items: center; gap: 10px; padding: 12px 14px; border-bottom: 1px solid #e5e7eb; }
.verify-card-toggle { padding: 6px 10px !important; font-size: 12px !important; }
	.markup-card { background: linear-gradient(180deg, #ffffff 0%, #fbfdff 100%); border-radius: 14px; padding: 16px; box-shadow: 0 10px 24px rgba(148, 163, 184, .10); margin-bottom: 12px; border: 1px solid #dbe6f3; }
	.markup-title { font-size: 15px; font-weight: 700; color: #0f172a; margin-bottom: 12px; letter-spacing: -.01em; }
	.markup-top { margin-bottom: 12px; padding-bottom: 12px; border-bottom: 1px solid #e8eef6; }
	.markup-split { display: grid; grid-template-columns: minmax(0, 1.08fr) minmax(0, .92fr); gap: 10px; align-items: start; }
	.markup-left, .markup-right { min-width: 0; }
	.markup-right { display: grid; grid-template-rows: 1fr 1fr; gap: 10px; }
	.markup-right .preview-card { height: 100%; display: flex; flex-direction: column; }
	.markup-row { display: grid; grid-template-columns: 1fr; gap: 8px; align-items: start; }
	.markup-row select, .markup-row input { width: 100%; padding: 7px 9px; border: 1px solid #cfd8e3; border-radius: 4px; font-size: 13px; background: #fff; }
	.markup-row select { min-height: 210px; }
		.markup-controls-shell { border: 1px solid #d9e5f2; border-radius: 12px; background: linear-gradient(180deg, #f8fbff 0%, #f3f8fe 100%); padding: 12px; box-shadow: inset 0 1px 0 rgba(255,255,255,.7); }
		.markup-controls { display: grid; grid-template-columns: repeat(4, minmax(116px, 1fr)) 170px; gap: 10px; align-items: end; justify-content: start; }
		.markup-control-field { display: flex; flex-direction: column; gap: 6px; }
		.markup-control-field input { height: 36px; border: 1px solid #cbd7e6; border-radius: 10px; background: #fff; box-shadow: none; padding: 7px 10px; font-size: 13px; color: #0f172a; }
		.markup-control-field input:focus { outline: none; border-color: #2563eb; box-shadow: 0 0 0 3px rgba(37, 99, 235, .12); }
		.field-label { display: inline-flex; align-items: center; gap: 6px; font-size: 12px; font-weight: 600; color: #475569; min-height: 30px; line-height: 1.25; }
	.info-tip { position: relative; display: inline-flex; align-items: center; justify-content: center; width: 16px; height: 16px; border-radius: 999px; background: #e8eefc; color: #3157d5; font-size: 11px; font-weight: 700; cursor: help; user-select: none; }
	.info-tip::after { content: attr(data-tip); position: absolute; left: 50%; bottom: calc(100% + 8px); transform: translateX(-50%); min-width: 240px; max-width: 320px; padding: 8px 10px; border-radius: 8px; background: rgba(15, 23, 42, .96); color: #fff; font-size: 12px; line-height: 1.35; text-align: left; box-shadow: 0 10px 24px rgba(0,0,0,.22); opacity: 0; pointer-events: none; transition: opacity .15s ease; z-index: 30; white-space: normal; }
	.info-tip::before { content: ""; position: absolute; left: 50%; bottom: calc(100% + 2px); transform: translateX(-50%); border: 6px solid transparent; border-top-color: rgba(15, 23, 42, .96); opacity: 0; transition: opacity .15s ease; pointer-events: none; z-index: 30; }
	.info-tip:hover::after, .info-tip:hover::before { opacity: 1; }
	.apply-markup-btn-wrap { display: flex; align-items: stretch; }
	.apply-markup-btn-small { width: 100%; height: 36px; min-height: 36px; background: linear-gradient(180deg, #22c55e 0%, #16a34a 100%); color: #fff; border: none; font-size: 12px; padding: 6px 12px; border-radius: 10px; font-weight: 700; box-shadow: 0 8px 16px rgba(22, 163, 74, .22); }
	.apply-markup-btn-small:hover { background: linear-gradient(180deg, #1fb857 0%, #15803d 100%); }
	.visibility-row { display: grid; grid-template-columns: 180px 1fr; gap: 10px; align-items: stretch; flex: 1 1 auto; min-height: 0; }
	.visibility-row > div { display: flex; flex-direction: column; min-height: 0; }
	.visibility-row select { width: 100%; padding: 7px 9px; border: 1px solid #cfd8e3; border-radius: 4px; font-size: 13px; min-height: 260px; flex: 1 1 auto; background: #fff; }
	.preview-card { border: 1px solid #dce6f2; border-radius: 12px; background: linear-gradient(180deg, #ffffff 0%, #fbfdff 100%); padding: 12px; margin-top: 10px; box-shadow: 0 6px 16px rgba(148, 163, 184, .08); }
	.preview-title { font-size: 13px; font-weight: 700; color: #1e293b; margin-bottom: 10px; }
	.preview-table { width: 100%; border-collapse: collapse; font-size: 12px; }
	.preview-table th, .preview-table td { border: 1px solid #e6edf5; padding: 6px 8px; text-align: left; }
	.preview-table th { color: #5f6368; background: #f8f9fa; }
	.preview-transfer { display: grid; grid-template-columns: 3fr 2fr; gap: 10px; margin-top: 8px; align-items: start; }
	.preview-transfer select, .preview-transfer input { width: 100%; padding: 7px 9px; border: 1px solid #cfd8e3; border-radius: 4px; font-size: 13px; background: #fff; }
	.preview-transfer select { min-height: 250px; }
	.select-search { width: 100%; padding: 7px 9px; border: 1px solid #cfd8e3; border-radius: 4px; font-size: 13px; margin-bottom: 6px; background: #fff; }
	.transfer-action { display: flex; justify-content: flex-end; margin-top: 8px; }
	.transfer-action .btn { width: auto !important; min-width: 150px; padding: 8px 14px !important; font-size: 13px; }
	.markup-map { margin-top: 10px; border: 1px solid #dce6f2; border-radius: 12px; background: #fff; overflow: hidden; box-shadow: 0 6px 16px rgba(148, 163, 184, .06); }
	.markup-map table { width: 100%; border-collapse: collapse; font-size: 12px; }
	.markup-map th, .markup-map td { border: 1px solid #e6edf5; padding: 7px 9px; text-align: left; }
	.markup-map th { background: #f8f9fa; color: #5f6368; position: sticky; top: 0; z-index: 1; font-weight: 600; }
	.markup-map tbody tr:hover { background: #f8fbff; }
	.full-list-modal { position: fixed; inset: 0; background: rgba(2,8,23,.55); display: none; align-items: center; justify-content: center; z-index: 1300; }
	.full-list-modal.active { display: flex; }
	.full-list-sheet { width: min(1460px, calc(100vw - 20px)); max-height: calc(100vh - 20px); overflow: auto; background: #fff; border-radius: 14px; padding: 14px; box-shadow: 0 24px 60px rgba(0,0,0,.35); border: 1px solid #dbeafe; }
	.full-list-table { width: 100%; border-collapse: collapse; font-size: 13px; margin-top: 8px; }
	.full-list-table th, .full-list-table td { border-bottom: 1px solid #e5e7eb; padding: 6px 8px; text-align: left; }
	.full-list-table th { position: sticky; top: 0; background: #f8fafc; z-index: 1; }
	.autosort-modal { position: fixed; inset: 0; background: rgba(2,8,23,.55); display: none; align-items: center; justify-content: center; z-index: 1325; }
	.autosort-modal.active { display: flex; }
	.autosort-sheet { width: min(1360px, calc(100vw - 20px)); max-height: calc(100vh - 20px); overflow: auto; background: #fff; border-radius: 14px; padding: 14px; box-shadow: 0 24px 60px rgba(0,0,0,.35); border: 1px solid #dbeafe; animation: riseIn .18s ease-out; }
	.autosort-toolbar { display: flex; gap: 8px; flex-wrap: wrap; margin-top: 8px; }
	.autosort-table-wrap { margin-top: 8px; border: 1px solid #e2e8f0; border-radius: 10px; overflow: auto; max-height: 62vh; }
	.autosort-table { width: 100%; border-collapse: collapse; font-size: 13px; table-layout: fixed; }
	.autosort-table th, .autosort-table td { border: 1px solid #e5e7eb; padding: 7px 8px; vertical-align: top; text-align: left; }
	.autosort-table th { position: sticky; top: 0; background: #f8fafc; z-index: 1; color: #334155; }
	.autosort-table tbody tr { transition: background .12s ease; }
	.autosort-table tbody tr:hover { background: #f8fbff; }
	.autosort-table tbody tr.autosort-row-selected { background: #eef4ff; }
	.autosort-check-cell { text-align: center !important; }
	.autosort-check-cell input { width: 15px; height: 15px; }
		.markup-actions { display: flex; gap: 8px; margin-top: 10px; flex-wrap: wrap; }
		.markup-note { font-size: 12px; color: #6b7280; margin-top: 8px; }
		.auto-refresh-card { display: grid; grid-template-columns: 1fr 220px; gap: 10px; align-items: end; }
		.auto-refresh-left { display: flex; flex-direction: column; gap: 8px; }
		.auto-refresh-toggle { display: inline-flex; align-items: center; gap: 8px; font-size: 14px; color: #111827; font-weight: 600; }
		.auto-refresh-interval { width: 100%; padding: 7px 9px; border: 1px solid #cfd8e3; border-radius: 4px; font-size: 13px; background: #fff; }
		.pricing-modal { position: fixed; inset: 0; background: rgba(15,23,42,.36); display: none; align-items: center; justify-content: center; z-index: 1200; backdrop-filter: blur(2px); }
	.pricing-modal.active { display: flex; }
	.pricing-sheet { width: min(1380px, calc(100vw - 20px)); max-height: calc(100vh - 20px); overflow: auto; background: #f8fafd; border-radius: 12px; padding: 14px; box-shadow: 0 20px 48px rgba(15,23,42,.22); border: 1px solid #d9e2f1; animation: riseIn .22s ease-out; }
	.preview-modal { position: fixed; inset: 0; background: rgba(15,23,42,.36); display: none; align-items: center; justify-content: center; z-index: 1350; backdrop-filter: blur(2px); }
	.preview-modal.active { display: flex; }
	.preview-sheet { width: min(1320px, calc(100vw - 24px)); max-height: calc(100vh - 20px); overflow: auto; background: #f8fafd; border-radius: 12px; padding: 14px; box-shadow: 0 20px 48px rgba(15,23,42,.22); border: 1px solid #d9e2f1; animation: riseIn .2s ease-out; }
	.preview-toolbar-card { border: 1px solid #d9e2f1; background: #fff; border-radius: 8px; padding: 10px; margin-bottom: 8px; }
	.preview-grid { display: grid; grid-template-columns: 110px 182px 110px 110px 138px 1fr 174px 144px; gap: 6px; align-items: end; margin-bottom: 0; }
	.preview-grid input,
	.preview-grid select { width: 100%; padding: 7px 9px; border: 1px solid #cfd8e3; border-radius: 4px; font-size: 13px; background: #fff; min-height: 30px; }
	.preview-grid .btn { padding: 7px 10px; font-size: 12px; min-height: 30px; }
	#refresh-market-btn { position: relative; overflow: hidden; isolation: isolate; }
	#refresh-market-btn::before {
		content: "";
		position: absolute;
		inset: 0 auto 0 0;
		width: var(--progress, 0%);
		background: linear-gradient(90deg, rgba(37,99,235,.18), rgba(99,102,241,.28));
		z-index: -1;
		transition: width .25s ease;
	}
	#refresh-market-btn.is-updating {
		border-color: #2563eb;
		color: #1d4ed8;
		box-shadow: inset 0 0 0 1px rgba(37,99,235,.06);
		animation: buttonPulse 1.2s ease-in-out infinite;
	}
	.preview-status-stack { display: grid; grid-template-columns: 1.1fr .9fr; gap: 8px; align-items: stretch; margin-bottom: 8px; }
	.preview-status-card { border: 1px solid #d9e2f1; border-radius: 8px; background: #fff; padding: 8px 10px; min-height: 216px; display:flex; flex-direction:column; justify-content:flex-start; }
	.preview-status-card .markup-note { margin-top: 0; line-height: 1.35; }
	.preview-categories-box { border: 1px solid #d9e2f1; border-radius: 8px; background: #fff; padding: 8px 10px; min-height: 216px; display:flex; flex-direction:column; }
	.preview-categories-box select { min-height: 178px !important; height: 178px; max-height: 178px; flex: 1 1 auto; }
	.preview-full-table { width: 100%; border-collapse: collapse; font-size: 12px; margin-top: 6px; table-layout: fixed; background: #fff; border: 1px solid #d9e2f1; }
	.preview-full-table th, .preview-full-table td { border: 1px solid #e6edf5; padding: 6px 8px; text-align: left; vertical-align: top; }
	.preview-full-table th { position: sticky; top: 0; background: #f8f9fa; z-index: 1; color: #5f6368; font-weight: 600; }
	.preview-full-table th:nth-child(n+5), .preview-full-table td:nth-child(n+5) { text-align: center; }
	.preview-full-table th:nth-child(1), .preview-full-table td:nth-child(1) { width: 110px; }
	.preview-full-table th:nth-child(2), .preview-full-table td:nth-child(2) { width: 95px; }
	.preview-full-table th:nth-child(4), .preview-full-table td:nth-child(4) { width: 90px; }
	.preview-full-table th:nth-child(5), .preview-full-table td:nth-child(5) { width: 70px; }
	.preview-full-table th:nth-child(6), .preview-full-table td:nth-child(6),
	.preview-full-table th:nth-child(7), .preview-full-table td:nth-child(7),
	.preview-full-table th:nth-child(8), .preview-full-table td:nth-child(8) { width: 128px; }
	.preview-full-table th:nth-child(9), .preview-full-table td:nth-child(9),
	.preview-full-table th:nth-child(10), .preview-full-table td:nth-child(10) { width: 92px; }
	.preview-full-table th:nth-child(11), .preview-full-table td:nth-child(11) { width: 110px; }
	.preview-market-cell { display: flex; flex-direction: column; align-items: center; gap: 4px; line-height: 1.15; }
	.preview-market-value { font-weight: 700; color: #0f172a; white-space: nowrap; }
	.preview-market-meta { display: flex; align-items: center; justify-content: center; gap: 4px; flex-wrap: wrap; font-size: 11px; color: #64748b; }
	.preview-market-badge { display: inline-flex; align-items: center; justify-content: center; padding: 1px 6px; border-radius: 999px; background: #eff6ff; color: #1d4ed8; font-weight: 700; }
	.preview-market-pct { font-weight: 700; }
	.preview-market-pct-positive { color: #15803d; }
	.preview-market-pct-negative { color: #dc2626; }
	.preview-market-pct-neutral { color: #64748b; }
	.preview-price-cell { font-variant-numeric: tabular-nums; white-space: nowrap; }
	.preview-full-table tbody tr { cursor: pointer; transition: background .15s ease; }
	.preview-full-table tbody tr:hover { background: #f8fbff; }
	.preview-full-table tbody tr.preview-row-selected { background: #e8f0fe; box-shadow: inset 3px 0 0 #1a73e8; }
	.preview-sheet .preview-status-stack,
	.preview-sheet .preview-full-table { transition: opacity .28s ease, filter .28s ease; }
	.preview-sheet.preview-loading .preview-status-stack,
	.preview-sheet.preview-loading .preview-full-table { opacity: .6; filter: saturate(.92); }
	.preview-sheet .btn { border-radius: 4px !important; box-shadow: none !important; }
	.preview-sheet .btn:not(.btn-outline) { background: #1a73e8; border: 1px solid #1a73e8; color: #fff; }
	.preview-sheet .btn:not(.btn-outline):hover { background: #1765cc; border-color: #1765cc; }
	.preview-sheet .btn.btn-outline { background: #fff; border: 1px solid #cfd8e3; color: #374151; }
	.preview-sheet .btn.btn-outline:hover { background: #f8f9fa; border-color: #b9c7d8; }
	#preview-base-mode,
	#preview-modal-categories { padding: 7px 9px !important; border: 1px solid #cfd8e3 !important; border-radius: 4px !important; font-size: 13px !important; background: #fff !important; }
	#preview-base-mode { min-height: 30px !important; }
	#open-offers-btn { padding: 7px 10px !important; font-size: 12px !important; }
	.market-progress-error { color:#b91c1c; }
	.market-trend { display: inline-block; margin-left: 4px; font-size: 11px; font-weight: 700; }
	.market-trend-up { color: #dc2626; }
	.market-trend-down { color: #16a34a; }
	.market-trend-flat { color: #64748b; }
	.pricing-head { display: flex; justify-content: space-between; align-items: center; gap: 10px; margin-bottom: 10px; position: sticky; top: 0; background: #f8fafd; z-index: 2; padding: 2px 0 8px; border-bottom: 1px solid #d9e2f1; }
	.pricing-close { border: 1px solid #d0d7e2; background: #fff; color: #374151; border-radius: 4px; padding: 7px 12px; cursor: pointer; font-weight: 600; }
	.pricing-close:hover { background: #f8f9fa; }
	.pricing-sheet .btn { border-radius: 4px !important; box-shadow: none !important; }
	.pricing-sheet .btn:not(.btn-outline) { background: #1a73e8; border: 1px solid #1a73e8; color: #fff; }
	.pricing-sheet .btn:not(.btn-outline):hover { background: #1765cc; border-color: #1765cc; }
	.pricing-sheet .btn.btn-outline { background: #fff; border: 1px solid #cfd8e3; color: #374151; }
	.pricing-sheet .btn.btn-outline:hover { background: #f8f9fa; border-color: #b9c7d8; }
	.pricing-sheet label[style],
	.preview-sheet label[style],
	.full-list-sheet label[style] { color: #5f6368 !important; font-size: 12px !important; font-weight: 500; }
	.pricing-sheet select,
	.pricing-sheet input,
	.preview-sheet select,
	.preview-sheet input,
	.full-list-sheet select,
	.full-list-sheet input { transition: border-color .12s ease, box-shadow .12s ease, background-color .12s ease; }
	.pricing-sheet select:focus,
	.pricing-sheet input:focus,
	.preview-sheet select:focus,
	.preview-sheet input:focus,
	.full-list-sheet select:focus,
	.full-list-sheet input:focus { outline: none; border-color: #1a73e8; box-shadow: inset 0 0 0 1px #1a73e8; }
	.pulse { animation: pulse .9s ease; }
	@keyframes riseIn { from { transform: translateY(16px) scale(0.98); opacity: 0; } to { transform: translateY(0) scale(1); opacity: 1; } }
	@keyframes pulse { 0%{ box-shadow: 0 0 0 0 rgba(99,102,241,.35);} 70%{ box-shadow:0 0 0 12px rgba(99,102,241,0);} 100%{ box-shadow:0 0 0 0 rgba(99,102,241,0);} }
	@keyframes buttonPulse { 0%{ box-shadow: 0 0 0 0 rgba(37,99,235,.18);} 70%{ box-shadow: 0 0 0 8px rgba(37,99,235,0);} 100%{ box-shadow: 0 0 0 0 rgba(37,99,235,0);} }
	.progress-modal { position: fixed; inset: 0; background: rgba(17, 24, 39, 0.55); display: none; align-items: center; justify-content: center; z-index: 1000; backdrop-filter: blur(2px); }
	.progress-modal.active { display: flex; }
	.progress-card { width: min(640px, calc(100vw - 32px)); background: linear-gradient(140deg, #ffffff, #f7fbff); border-radius: 18px; box-shadow: 0 24px 60px rgba(15, 23, 42, .28); padding: 22px; border: 1px solid #e8eefc; }
	.progress-head { display: flex; align-items: center; justify-content: space-between; gap: 12px; margin-bottom: 14px; }
	.progress-title { font-size: 18px; font-weight: 700; color: #1f2937; }
	.progress-subtitle { font-size: 13px; color: #64748b; }
	.progress-spinner { width: 22px; height: 22px; border-radius: 50%; border: 3px solid #dbe7ff; border-top-color: #1e88e5; animation: spin 0.8s linear infinite; }
	.progress-spinner.done { border-color: #d1fae5; border-top-color: #2e7d32; animation: none; }
	@keyframes spin { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
	.phase-grid { display: grid; gap: 10px; margin-bottom: 14px; }
	.phase-item { border: 1px solid #e8eaf0; border-radius: 12px; padding: 11px 12px; background: #fff; }
	.phase-item .row { display: flex; justify-content: space-between; align-items: center; gap: 12px; }
	.phase-item .name { font-size: 14px; font-weight: 600; color: #111827; }
	.phase-item .meta { font-size: 12px; color: #6b7280; margin-top: 3px; }
	.phase-pill { font-size: 11px; font-weight: 700; text-transform: uppercase; letter-spacing: .04em; border-radius: 999px; padding: 5px 9px; background: #eef2ff; color: #475569; }
	.phase-pill.active { background: #dbeafe; color: #1d4ed8; }
	.phase-pill.done { background: #dcfce7; color: #166534; }
	.progress-line-wrap { margin-bottom: 10px; }
	.progress-line { height: 10px; border-radius: 999px; background: #e5e7eb; overflow: hidden; }
	.progress-line > div { height: 100%; width: 0%; background: linear-gradient(90deg, #2563eb, #06b6d4); transition: width .25s ease; }
	.progress-stats { display: grid; grid-template-columns: repeat(3, 1fr); gap: 8px; margin-top: 10px; }
	.mini-stat { background: #fff; border: 1px solid #edf2f7; border-radius: 10px; padding: 8px 10px; text-align: center; }
	.mini-stat .v { font-size: 18px; font-weight: 700; color: #0f172a; }
	.mini-stat .t { font-size: 11px; color: #6b7280; text-transform: uppercase; letter-spacing: .03em; }
	.progress-actions { margin-top: 14px; display: flex; justify-content: flex-end; }
	.progress-close { display: none; padding: 8px 14px; border-radius: 9px; border: none; background: #2e7d32; color: #fff; font-weight: 600; cursor: pointer; }
	.offers-modal { position: fixed; inset: 0; background: rgba(15, 23, 42, .55); display: none; align-items: center; justify-content: center; z-index: 1400; }
	.offers-modal.active { display: flex; }
	.offers-sheet { width: min(980px, calc(100vw - 24px)); max-height: calc(100vh - 24px); overflow: auto; background: #fff; border-radius: 14px; padding: 14px; box-shadow: 0 24px 60px rgba(0,0,0,.35); border: 1px solid #dbeafe; }
	.offers-summary { display: grid; grid-template-columns: repeat(4, minmax(0, 1fr)); gap: 8px; margin: 10px 0; }
	.offers-stat { border: 1px solid #e5e7eb; border-radius: 10px; background: #f8fafc; padding: 10px; }
	.offers-stat .k { font-size: 12px; color: #64748b; }
	.offers-stat .v { font-size: 16px; font-weight: 700; color: #0f172a; margin-top: 3px; }
	.offers-table { width: 100%; border-collapse: collapse; font-size: 12px; margin-top: 8px; }
	.offers-table th, .offers-table td { border-bottom: 1px solid #e5e7eb; padding: 6px 8px; text-align: left; vertical-align: top; }
	.offers-table th { position: sticky; top: 0; background: #f8fafc; z-index: 1; }
    .ntech-checks-modal { position: fixed; inset: 0; background: rgba(15, 23, 42, .55); display: none; align-items: center; justify-content: center; z-index: 1410; }
    .ntech-checks-modal.active { display: flex; }
    .ntech-checks-sheet { width: min(760px, calc(100vw - 24px)); max-height: calc(100vh - 24px); overflow: auto; background: #fff; border-radius: 14px; padding: 14px; box-shadow: 0 24px 60px rgba(0,0,0,.35); border: 1px solid #dbeafe; }
    .ntech-checks-grid { margin-top: 10px; display:grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 10px; }
    .ntech-checks-grid .btn { width: 100%; justify-content: center; }
    .ntech-check-badge { display:none; margin-left:8px; padding:1px 7px; border-radius:999px; background:#16a34a; color:#fff; font-size:11px; font-weight:700; line-height:1.4; }
    .ntech-check-badge.active { display:inline-block; }
    @media (max-width: 760px){ .ntech-checks-grid { grid-template-columns: 1fr; } }
    .btn.is-loading { opacity: .95; cursor: wait; }
		@media (max-width: 1100px){
			.markup-split { grid-template-columns: 1fr; }
			.markup-right { grid-template-rows: auto auto; }
			.preview-transfer { grid-template-columns: 1fr; }
			.markup-controls { grid-template-columns: 1fr; }
			.preview-grid { grid-template-columns: 1fr; }
			.preview-status-stack { grid-template-columns: 1fr; }
			.auto-refresh-card { grid-template-columns: 1fr; }
		}
	</style></head><body>
<div class="container">

<div class="top-bar">
    <div>
        <h1>Price <span>Mixer</span> — Результат</h1>
        <p class="subtitle">Сводный прайс: {{ stats.consolidated }} товаров от {{ stats.suppliers }} поставщиков</p>
    </div>
    <div style="display:flex; gap:10px;">
        <button class="btn" id="open-pricing-btn" style="background:#1d4ed8;">Управление Ценами</button>
        <button class="btn btn-outline" id="run-quality-check-btn">Контроль качества</button>
        <div style="display:inline-flex;flex-wrap:wrap;gap:8px;align-items:center;">
            <a href="/download" class="btn">Скачать Excel</a>
            <button type="button" class="btn" id="export-google-sheet-btn" style="background:#0f9d58;border:none;min-width:190px;">В Google Таблицу</button>
        </div>
        <a href="/" class="btn btn-outline">Загрузить заново</a>
    </div>
</div>

<div class="stats-grid">
    <div class="stat-card stat-card--dual">
        <div class="stat-card-dual-inner">
            <div class="stat-card-dual-item"><div class="num">{{ stats.consolidated }}</div><div class="label">Всего товаров</div></div>
            <div class="stat-card-dual-sep" aria-hidden="true"></div>
            <div class="stat-card-dual-item"><div class="num">{{ stats.suppliers }}</div><div class="label">Поставщиков</div></div>
            <div class="stat-card-dual-sep" aria-hidden="true"></div>
            <div class="stat-card-dual-item stat-card-dual-item--noid">
                <div class="num" id="without-id-count">{{ stats.without_id }}</div>
                <div class="label">Без OnlinerID</div>
                <div class="stat-card-noid-btn-wrap">
                    <button class="btn btn-outline" id="toggle-noid-btn" style="padding:8px 16px;font-size:13px;">Показать</button>
                </div>
            </div>
            <div class="stat-card-dual-sep" aria-hidden="true"></div>
            <div class="stat-card-dual-item">
                <div class="num" id="duplicate-id-count" {% if (stats.suppliers|int) > 1 %}style="visibility:hidden;"{% endif %}>{% if (stats.suppliers|int) > 1 %}&nbsp;{% else %}{{ stats.duplicate_id_rows }}{% endif %}</div>
                <div class="label">С одинаковыми ID</div>
                <div class="stat-card-noid-btn-wrap">
                    <button class="btn btn-outline" id="toggle-duplicate-id-btn" style="padding:8px 16px;font-size:13px;">Показать дубли ID</button>
                </div>
            </div>
        </div>
    </div>
    <div class="stat-card highlight" style="min-height:0;">
        <div style="margin-top:0;padding:10px;border:1px solid #fde68a;border-radius:10px;background:#fffbeb;">
            <div style="font-size:12px;font-weight:700;color:#b45309;text-transform:uppercase;letter-spacing:.04em;text-align:center;">TGPC ПЭВМ</div>
            <div style="margin-top:8px;display:flex;gap:8px;justify-content:center;flex-wrap:wrap;">
                <button class="btn btn-outline" id="autofill-tgpc-pc-btn" style="padding:8px 16px;font-size:13px;">Авто TGPC ПЭВМ</button>
            </div>
            <div id="autofill-tgpc-progress-wrap" style="display:none;margin-top:12px;padding:0 2px;">
                <div style="height:6px;background:#f0dfb1;border-radius:4px;overflow:hidden;">
                    <div id="autofill-tgpc-progress-bar" style="height:100%;width:0%;background:linear-gradient(90deg,#f59e0b,#d56e0c);border-radius:4px;transition:width 0.5s ease;"></div>
                </div>
            </div>
            <div id="autofill-tgpc-chips" style="display:none;margin-top:10px;gap:6px;justify-content:center;flex-wrap:wrap;"></div>
            <div class="markup-note" id="autofill-tgpc-pc-note" style="margin-top:8px;">Подбор только по локальной SQLite-базе (без Onliner API). Обрабатываются все TGPC ПЭВМ без OnlinerID.</div>
            <div id="autofill-tgpc-pc-report"></div>
        </div>
        <div style="margin-top:12px;padding:10px;border:1px solid #bfdbfe;border-radius:10px;background:#f8fbff;">
            <div style="font-size:12px;font-weight:700;color:#1d4ed8;text-transform:uppercase;letter-spacing:.04em;text-align:center;">Общий поиск по базе</div>
            <div style="margin-top:8px;display:flex;gap:8px;justify-content:center;flex-wrap:wrap;">
                <button class="btn btn-primary" id="autofill-iven-btn" style="padding:8px 16px;font-size:13px;background:linear-gradient(90deg,#2563eb,#1d4ed8);color:#fff;border:none;border-radius:7px;cursor:pointer;">Найти в базе</button>
                <button class="btn btn-outline" id="autofill-b2b-btn" style="padding:8px 16px;font-size:13px;border-color:#1d4ed8;color:#1d4ed8;">B2B без кеша</button>
                <button class="btn btn-outline" id="open-ntech-checks-modal-btn" style="padding:8px 16px;font-size:13px;border-color:#4f46e5;color:#4f46e5;{% if not stats.show_checks_block %}display:none;{% endif %}">Блок проверок</button>
                <button class="btn btn-outline" id="autofill-iven-report-btn" style="padding:8px 16px;font-size:13px;display:none;">Отчёт</button>
            </div>
            <div id="autofill-iven-progress-wrap" style="display:none;margin-top:12px;padding:0 2px;">
                <div style="height:6px;background:#bfdbfe;border-radius:4px;overflow:hidden;">
                    <div id="autofill-iven-progress-bar" style="height:100%;width:0%;background:linear-gradient(90deg,#2563eb,#1d4ed8);border-radius:4px;transition:width 0.4s ease;"></div>
                </div>
                <div id="autofill-iven-msg" style="margin-top:6px;font-size:12px;color:#1d4ed8;text-align:center;"></div>
            </div>
            <div class="markup-note" id="autofill-iven-note" style="margin-top:8px;">Ищет совпадения для обычных товаров без ID по локальной базе и B2B-каталогу. Кнопки категорий N-Tech кладут позиции в ручную очередь по строгому совпадению бренда и модели.</div>
        </div>
    </div>
    <div class="stat-card" style="border-left:4px solid #0891b2;">
        <div class="num" id="db-products-count">—</div>
        <div class="label">База Onliner ID</div>
        <div id="db-stats-detail" style="margin-top:6px;font-size:12px;color:#6b7280;line-height:1.6;"></div>
        <div style="margin-top:10px;display:flex;gap:8px;justify-content:center;flex-wrap:wrap;">
            <button class="btn btn-outline" id="db-rebuild-btn" style="padding:8px 16px;font-size:13px;">Обновить БД</button>
            <button class="btn btn-outline" id="db-import-catalog-btn" style="padding:8px 16px;font-size:13px;border-color:#0891b2;color:#0891b2;" title="Загрузить CSV/XLSX каталог Onliner (колонки: A=Категория, C=Модель, E=OnlinerID, H=Название)">
                📥 Загрузить каталог
            </button>
            <input type="file" id="db-import-catalog-file" accept=".csv,.xlsx,.xls" style="display:none;">
        </div>
        <!-- Google Sheets import action (sheet/id moved to Settings) -->
        <div id="db-gsheet-row" style="margin-top:10px;display:flex;justify-content:center;">
            <button id="db-gsheet-btn" style="padding:6px 14px;background:linear-gradient(90deg,#0891b2,#0e7490);color:#fff;border:none;border-radius:6px;font-size:12px;font-weight:600;cursor:pointer;white-space:nowrap;">▶ Импорт GSheets</button>
        </div>
        <div id="db-search-row" style="margin-top:10px;display:flex;gap:6px;align-items:center;flex-wrap:wrap;">
            <input id="db-search-query" type="text" placeholder="Поиск по базе: модель, бренд, OnlinerID" style="flex:1;min-width:150px;padding:6px 10px;border:1px solid #e2e8f0;border-radius:6px;font-size:12px;color:#374151;">
            <button id="db-search-btn" style="padding:6px 14px;background:linear-gradient(90deg,#f59e0b,#d97706);color:#fff;border:none;border-radius:6px;font-size:12px;font-weight:600;cursor:pointer;white-space:nowrap;">Найти в БД</button>
        </div>
        <div id="db-search-results" style="display:none;margin-top:10px;border:1px solid #fde68a;border-radius:8px;background:#fffbeb;padding:8px 10px;max-height:220px;overflow:auto;"></div>
        <!-- Import progress -->
        <div id="db-import-progress-wrap" style="display:none;margin-top:10px;">
            <div style="height:6px;background:#cffafe;border-radius:4px;overflow:hidden;">
                <div id="db-import-progress-bar" style="height:100%;width:0%;background:linear-gradient(90deg,#0891b2,#0e7490);border-radius:4px;transition:width 0.4s ease;"></div>
            </div>
            <div id="db-import-msg" style="margin-top:5px;font-size:12px;color:#0e7490;text-align:center;"></div>
        </div>
        <div class="markup-note" id="db-stats-note" style="margin-top:8px;">Локальная SQLite-база товаров Onliner. Пополняется при каждой загрузке прайса IVEN/BN/Tradex.</div>
    </div>
</div>

{% if stats.snapshot_diff and stats.snapshot_diff.available %}
<div class="table-card snapshot-card">
    <div style="padding:12px 14px; border-bottom:1px solid #e5e7eb; font-weight:700; color:#1f2937;">Сравнение с прошлой выгрузкой{% if stats.snapshot_diff.supplier %}: {{ stats.snapshot_diff.supplier }}{% endif %}</div>
    <div style="padding:12px 14px;">
        <div class="markup-note">Показываем разницу относительно предыдущего обработанного прайса этого поставщика.</div>
        <div class="snapshot-actions">
            <button type="button" class="btn btn-outline" id="show-new-items-btn" style="padding:8px 12px;font-size:13px;">Показать только новые</button>
            <button type="button" class="btn btn-outline" id="show-new-noid-items-btn" style="padding:8px 12px;font-size:13px;">Показать новые без ID</button>
            <button type="button" class="btn btn-outline" id="clear-snapshot-filter-btn" style="padding:8px 12px;font-size:13px;">Сбросить фильтр</button>
        </div>
        <div class="snapshot-grid" style="margin-top:12px;">
            <div class="snapshot-mini" data-kind="new"><div class="k">Новых товаров</div><div class="v">{{ stats.snapshot_diff.new_count }}</div></div>
            <div class="snapshot-mini danger" data-kind="removed"><div class="k">Пропало товаров</div><div class="v">{{ stats.snapshot_diff.removed_count }}</div></div>
            <div class="snapshot-mini" data-kind="price_changed"><div class="k">Изменили цену</div><div class="v">{{ stats.snapshot_diff.price_changed_count }}</div></div>
            <div class="snapshot-mini warn" data-kind="new_without_id"><div class="k">Новые без ID</div><div class="v">{{ stats.snapshot_diff.new_without_id_count }}</div></div>
        </div>
        <div class="snapshot-hint">Нажмите на карточку, чтобы открыть примеры только по этому блоку.</div>
        <div class="snapshot-lists" id="snapshot-lists">
            <div class="snapshot-list" data-kind="new">
                <h4>Новые товары</h4>
                {% if stats.snapshot_diff.samples.new_items %}
                <ul>
                    {% for item in stats.snapshot_diff.samples.new_items %}
                    <li>{{ item.name }}{% if item.price is not none %} — {{ item.price }}{% endif %}</li>
                    {% endfor %}
                </ul>
                {% else %}
                <div class="snapshot-empty">Новых товаров нет.</div>
                {% endif %}
            </div>
            <div class="snapshot-list" data-kind="removed">
                <h4>Пропали товары</h4>
                {% if stats.snapshot_diff.samples.removed_items %}
                <ul>
                    {% for item in stats.snapshot_diff.samples.removed_items %}
                    <li>{{ item.name }}{% if item.price is not none %} — {{ item.price }}{% endif %}</li>
                    {% endfor %}
                </ul>
                {% else %}
                <div class="snapshot-empty">Пропавших товаров нет.</div>
                {% endif %}
            </div>
            <div class="snapshot-list" data-kind="price_changed">
                <h4>Изменили цену</h4>
                {% if stats.snapshot_diff.samples.price_changed %}
                <ul>
                    {% for item in stats.snapshot_diff.samples.price_changed %}
                    <li>{{ item.name }} — {{ item.old_price }} → {{ item.new_price }}</li>
                    {% endfor %}
                </ul>
                {% else %}
                <div class="snapshot-empty">Изменений цен нет.</div>
                {% endif %}
            </div>
            <div class="snapshot-list" data-kind="new_without_id">
                <h4>Новые товары без ID</h4>
                {% if stats.snapshot_diff.samples.new_without_id %}
                <ul>
                    {% for item in stats.snapshot_diff.samples.new_without_id %}
                    <li>{{ item.name }}{% if item.price is not none %} — {{ item.price }}{% endif %}</li>
                    {% endfor %}
                </ul>
                {% else %}
                <div class="snapshot-empty">Новых товаров без ID нет.</div>
                {% endif %}
            </div>
        </div>
    </div>
</div>
{% endif %}

<div class="table-card" id="quality-check-card" style="display:none; margin-top:12px;">
    <div style="padding:12px 14px; border-bottom:1px solid #e5e7eb; font-weight:700; color:#1f2937;">Контроль качества перед экспортом</div>
    <div style="padding:12px 14px;">
        <div id="quality-check-summary" class="markup-note">Запустите проверку.</div>
        <div id="quality-check-details" style="margin-top:10px; font-size:13px; color:#334155;"></div>
        <div class="quality-actions">
            <button class="btn btn-outline quality-reapply-btn" id="quality-reapply-markups-btn">Доприменить Наценки</button>
        </div>
    </div>
</div>


<div class="table-card">
<table id="tbl-main" class="display" style="width:100%">
<thead><tr>
    <th>OnlinerID</th><th>\u041d\u0430\u0437\u0432\u0430\u043d\u0438\u0435</th><th>\u041e\u043f\u0442 \u0446\u0435\u043d\u0430</th><th>\u041f\u043e\u0441\u0442\u0430\u0432\u0449\u0438\u043a</th><th>\u0413\u0430\u0440\u0430\u043d\u0442\u0438\u044f</th><th>\u0414\u043d\u0435\u0439 \u0434\u043e\u0441\u0442\u0430\u0432\u043a\u0438</th><th>\u0420\u0420\u0426</th><th>\u0426\u0435\u043d\u0430 \u0431\u0435\u0437 \u0441\u043a\u0438\u0434\u043a\u0438</th>
</tr></thead>
<tbody id="tbl-main-body"></tbody>
</table>
</div>

</div>

<div id="pricing-modal" class="pricing-modal">
  <div class="pricing-sheet">
    <div class="pricing-head">
      <div class="markup-title" style="margin:0;">Управление Ценами И Категориями</div>
      <button class="pricing-close" id="close-pricing-btn">Закрыть</button>
    </div>

    <div class="markup-card">
        <div class="markup-title">Автообновление API Цен</div>
        <div class="auto-refresh-card">
            <div class="auto-refresh-left">
                <label class="auto-refresh-toggle">
                    <input type="checkbox" id="auto-refresh-enabled">
                    Включить автообновление по всем категориям
                </label>
                <div class="markup-note" id="auto-refresh-note">Статус автообновления: загрузка...</div>
            </div>
            <div>
                <label style="font-size:13px;color:#4b5563;">Интервал</label>
                <select id="auto-refresh-interval" class="auto-refresh-interval">
                    <option value="3">Каждые 3 часа</option>
                    <option value="6">Каждые 6 часов</option>
                    <option value="12">Каждые 12 часов</option>
                </select>
            </div>
        </div>
    </div>

    <div class="markup-card">
        <div class="markup-title">Наценка РРЦ И Цены Без Скидки По Категориям</div>
        <div class="markup-top">
            <div class="markup-row">
                <div>
                    <label style="font-size:13px;color:#4b5563;">Категории (можно выбрать несколько)</label>
                    <select id="markup-categories" multiple></select>
                </div>
            </div>
            <div class="markup-actions">
                <button class="btn btn-outline" id="open-preview-modal-btn" style="padding:8px 12px;font-size:13px;">Предпросмотр</button>
                <button class="btn btn-outline" id="open-autosort-btn" style="padding:8px 12px;font-size:13px;">Автосортировка</button>
            </div>
            <div class="markup-note" id="markup-note">Категории загружаются…</div>
        </div>
        <div class="markup-split">
            <div class="markup-left">
                <div class="preview-card" style="margin-top:0;">
                    <div class="preview-title">Наценка</div>
                    <div class="markup-controls-shell">
                    <div class="markup-controls">
                        <div class="markup-control-field">
                            <label class="field-label">Наценка, %
                                <span class="info-tip" data-tip="Основная наценка по выбранной базе расчета. Для товаров выше порога опта работает только это правило.">i</span>
                            </label>
                            <input type="number" id="markup-percent" min="0" step="0.1" value="10">
                        </div>
                        <div class="markup-control-field">
                            <label class="field-label">Порог опта, руб
                                <span class="info-tip" data-tip="Если оптовая цена товара меньше или равна этому порогу, включается спецусловие по минимальной прибыли. Если выше порога — считается только обычная наценка по проценту.">i</span>
                            </label>
                            <input type="number" id="markup-threshold" min="0" step="0.1" value="0">
                        </div>
                        <div class="markup-control-field">
                            <label class="field-label">Мин. прибыль, руб
                                <span class="info-tip" data-tip="Минимальная добавка к оптовой цене. Срабатывает только для товаров в пределах порога опта. Формула: Опт + Мин. прибыль.">i</span>
                            </label>
                            <input type="number" id="markup-min-profit" min="0" step="0.1" value="0">
                        </div>
                        <div class="markup-control-field">
                            <label class="field-label">Без скидки, % от РРЦ
                                <span class="info-tip" data-tip="Дополнительный процент сверху от уже рассчитанной РРЦ. Формирует отдельную колонку &quot;Цена без скидки&quot;.">i</span>
                            </label>
                            <input type="number" id="markup-no-discount-percent" min="0" step="0.1" value="0">
                        </div>
                        <div class="apply-markup-btn-wrap">
                            <button class="btn btn-outline apply-markup-btn-small" id="apply-markup-btn">Применить Наценку</button>
                        </div>
                    </div>
                    </div>
                </div>
                <div class="markup-map">
                    <table id="category-markup-table">
        <thead><tr><th>Категория</th><th>Наценка (%)</th><th>Порог опта</th><th>Мин. прибыль</th><th>Без скидки (%)</th><th>База</th></tr></thead>
                        <tbody></tbody>
                    </table>
                </div>
            </div>
            <div class="markup-right">
                <div class="preview-card" style="margin-top:0;">
                    <div class="preview-title">Перенос Товаров</div>
                    <div class="preview-transfer">
                        <div>
                            <label style="font-size:13px;color:#4b5563;">Товары выбранных категорий (Shift/Ctrl)</label>
                            <select id="preview-items" multiple></select>
                        </div>
                        <div>
                            <label style="font-size:13px;color:#4b5563;">Перенести в категорию</label>
                            <input id="target-category-search" class="select-search" type="text" placeholder="Поиск категории...">
                            <select id="preview-target-category" size="8"></select>
                        </div>
                    </div>
                    <div class="transfer-action">
                        <button class="btn" id="preview-move-btn">Перенести</button>
                    </div>
                    <div class="markup-note" id="preview-items-note">Выберите категорию слева, затем переносите товары справа.</div>
                </div>
                <div class="preview-card">
                    <div class="preview-title">Скрыть/Показать Категории Поставщика</div>
                    <div class="visibility-row">
                        <div>
                            <label style="font-size:13px;color:#4b5563;">Поставщик</label>
                            <select id="supplier-select" size="8"></select>
                        </div>
                        <div>
                            <label style="font-size:13px;color:#4b5563;">Категории поставщика</label>
                            <select id="supplier-categories" multiple></select>
                        </div>
                    </div>
                    <div class="markup-actions">
                        <button class="btn" id="hide-cats-btn" style="padding:8px 12px;font-size:13px;">Скрыть Выбранные</button>
                        <button class="btn btn-outline" id="show-cats-btn" style="padding:8px 12px;font-size:13px;">Показать Выбранные</button>
                    </div>
                    <div class="markup-note" id="visibility-note">Выберите поставщика и категории.</div>
                </div>
            </div>
        </div>
    </div>

  </div>
</div>

<div id="full-list-modal" class="full-list-modal">
  <div class="full-list-sheet">
    <div class="pricing-head">
      <div class="markup-title" style="margin:0;">Все Товары Выбранных Категорий</div>
      <button class="pricing-close" id="close-full-list-btn">Закрыть</button>
    </div>
    <div class="preview-transfer" style="margin-top:0;">
      <div>
        <label style="font-size:13px;color:#4b5563;">Товары (Shift/Ctrl для мультивыбора)</label>
        <select id="full-list-items" multiple></select>
      </div>
      <div>
        <label style="font-size:13px;color:#4b5563;">Перенести в категорию</label>
        <input id="full-target-category-search" class="select-search" type="text" placeholder="Поиск категории...">
        <select id="full-list-target-category" size="8"></select>
      </div>
      <div>
        <label style="font-size:13px;color:#4b5563;">Действие</label>
        <button class="btn" id="full-list-move-btn" style="width:100%;padding:10px 12px;">Перенести Выбранные</button>
      </div>
    </div>
    <div class="markup-note" id="full-list-note">Список загружается…</div>
    <table id="full-list-table" class="full-list-table">
      <thead><tr><th>Категория</th><th>Товар</th><th>Поставщик</th><th>Цена</th><th>РРЦ</th></tr></thead>
      <tbody></tbody>
    </table>
  </div>
</div>

<div id="autosort-modal" class="autosort-modal">
  <div class="autosort-sheet">
    <div class="pricing-head">
      <div class="markup-title" style="margin:0;">Автосортировка Категорий</div>
      <button class="pricing-close" id="close-autosort-btn">Закрыть</button>
    </div>
    <div class="autosort-toolbar">
      <button class="btn btn-outline" id="autosort-select-all-btn" style="padding:8px 12px;font-size:13px;">Выбрать Все</button>
      <button class="btn btn-outline" id="autosort-clear-btn" style="padding:8px 12px;font-size:13px;">Очистить</button>
      <button class="btn" id="autosort-apply-btn" style="padding:8px 12px;font-size:13px;background:#16a34a;">Применить Выбранные</button>
    </div>
    <div class="markup-note" id="autosort-note">Нажмите "Автосортировка", чтобы собрать предложения AI по вашим текущим названиям. Применение только вручную.</div>
    <div class="autosort-table-wrap">
      <table class="autosort-table">
        <thead>
          <tr>
            <th style="width:48px;">✓</th>
            <th style="width:110px;">OnlinerID</th>
            <th>Товар</th>
            <th style="width:110px;">Поставщик</th>
            <th style="width:160px;">Текущая</th>
            <th style="width:170px;">Предлагаемая</th>
            <th style="width:130px;">Источник</th>
          </tr>
        </thead>
        <tbody id="autosort-body"></tbody>
      </table>
    </div>
  </div>
</div>

<div id="ntech-checks-modal" class="ntech-checks-modal" {% if not stats.show_checks_block %}style="display:none;"{% endif %}>
  <div class="ntech-checks-sheet">
    <div class="pricing-head">
      <div class="markup-title" style="margin:0;">Блок проверок</div>
      <button class="pricing-close" id="close-ntech-checks-modal-btn">Закрыть</button>
    </div>
    <div class="markup-note">Все проверки и служебные действия собраны в одном окне.</div>
    <div style="margin-top:10px;padding:10px;border:1px solid #e9d5ff;border-radius:10px;background:#faf5ff;">
      <div style="font-size:12px;font-weight:700;color:#7c3aed;text-transform:uppercase;letter-spacing:.04em;text-align:center;">Проверка ID через API</div>
      <div style="margin-top:4px;font-size:12px;color:#6b7280;text-align:center;">Два режима: быстрая локальная сверка по базе 150k и полная проверка через Onliner API. Неверные ID очищаются, ПЭВМ TGPC пропускаются.</div>
      <div style="margin-top:10px;display:flex;gap:8px;justify-content:center;flex-wrap:wrap;">
        <button class="btn btn-outline" id="validate-ids-db-start-btn" style="padding:8px 16px;font-size:13px;border-color:#0f766e;color:#0f766e;">⚡ Проверить по БД</button>
        <button class="btn btn-outline" id="validate-ids-start-btn" style="padding:8px 16px;font-size:13px;border-color:#7c3aed;color:#7c3aed;">🔍 Проверить ID</button>
        <button class="btn btn-outline" id="validate-ids-report-btn" style="padding:8px 16px;font-size:13px;display:none;">Отчёт</button>
      </div>
      <div id="validate-ids-progress-wrap" style="display:none;margin-top:12px;">
        <div style="height:6px;background:#ede9fe;border-radius:4px;overflow:hidden;">
          <div id="validate-ids-progress-bar" style="height:100%;width:0%;background:linear-gradient(90deg,#7c3aed,#6d28d9);border-radius:4px;transition:width 0.4s ease;"></div>
        </div>
        <div id="validate-ids-msg" style="margin-top:6px;font-size:12px;color:#7c3aed;text-align:center;"></div>
      </div>
    </div>
    <div style="margin-top:10px;font-size:12px;font-weight:700;color:#334155;text-transform:uppercase;letter-spacing:.04em;text-align:center;">Проверки N-Tech</div>
    <div class="ntech-checks-grid">
      <button class="btn btn-outline" id="cpu-review-btn" data-check-key="cpu" style="padding:8px 16px;font-size:13px;border-color:#7c3aed;color:#7c3aed;">Процессоры N-Tech <span class="ntech-check-badge" id="cpu-review-badge"></span></button>
      <button class="btn btn-outline" id="board-review-btn" data-check-key="board" style="padding:8px 16px;font-size:13px;border-color:#0f766e;color:#0f766e;">Материнки N-Tech <span class="ntech-check-badge" id="board-review-badge"></span></button>
      <button class="btn btn-outline" id="monitor-review-btn" data-check-key="monitor" style="padding:8px 16px;font-size:13px;border-color:#c2410c;color:#c2410c;">Мониторы N-Tech <span class="ntech-check-badge" id="monitor-review-badge"></span></button>
      <button class="btn btn-outline" id="gpu-review-btn" data-check-key="gpu" style="padding:8px 16px;font-size:13px;border-color:#be185d;color:#be185d;">Видеокарты N-Tech <span class="ntech-check-badge" id="gpu-review-badge"></span></button>
      <button class="btn btn-outline" id="ram-review-btn" data-check-key="ram" style="padding:8px 16px;font-size:13px;border-color:#0369a1;color:#0369a1;">Оперативка N-Tech <span class="ntech-check-badge" id="ram-review-badge"></span></button>
      <button class="btn btn-outline" id="ssd-review-btn" data-check-key="ssd" style="padding:8px 16px;font-size:13px;border-color:#0f766e;color:#0f766e;">SSD N-Tech <span class="ntech-check-badge" id="ssd-review-badge"></span></button>
      <button class="btn btn-outline" id="psu-review-btn" data-check-key="psu" style="padding:8px 16px;font-size:13px;border-color:#047857;color:#047857;">Блоки питания N-Tech <span class="ntech-check-badge" id="psu-review-badge"></span></button>
      <button class="btn btn-outline" id="case-review-btn" data-check-key="case" style="padding:8px 16px;font-size:13px;border-color:#b45309;color:#b45309;">Корпуса N-Tech <span class="ntech-check-badge" id="case-review-badge"></span></button>
      <button class="btn btn-outline" id="hdd-review-btn" data-check-key="hdd" style="padding:8px 16px;font-size:13px;border-color:#0e7490;color:#0e7490;">HDD N-Tech <span class="ntech-check-badge" id="hdd-review-badge"></span></button>
      <button class="btn btn-outline" id="cooler-review-btn" data-check-key="cooler" style="padding:8px 16px;font-size:13px;border-color:#7c2d12;color:#7c2d12;">Охлаждение N-Tech <span class="ntech-check-badge" id="cooler-review-badge"></span></button>
      <button class="btn btn-outline" id="printer-review-btn" data-check-key="printer" style="padding:8px 16px;font-size:13px;border-color:#6b21a8;color:#6b21a8;">Принтеры / МФУ N-Tech <span class="ntech-check-badge" id="printer-review-badge"></span></button>
      <button class="btn btn-outline" id="peripheral-review-btn" data-check-key="peripheral" style="padding:8px 16px;font-size:13px;border-color:#0f766e;color:#0f766e;">Периферия N-Tech <span class="ntech-check-badge" id="peripheral-review-badge"></span></button>
    </div>
    <div style="margin-top:12px;padding:10px;border:1px solid #fecaca;border-radius:10px;background:#fff1f2;">
      <div style="font-size:12px;font-weight:700;color:#b91c1c;text-transform:uppercase;letter-spacing:.04em;text-align:center;">Опасные действия</div>
      <div style="margin-top:4px;font-size:12px;color:#7f1d1d;text-align:center;">Используйте только при необходимости. Перед выполнением будет дополнительное предупреждение.</div>
      <div style="margin-top:10px;display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px;">
        <button class="btn btn-outline" id="clear-duplicate-ids-btn" style="padding:8px 16px;font-size:13px;border-color:#b91c1c;color:#b91c1c;">Очистить дубли ID N-Tech</button>
        <button class="btn btn-outline" id="clear-nonpc-ids-btn" style="padding:8px 16px;font-size:13px;border-color:#dc2626;color:#dc2626;">Очистить ID N-Tech кроме ПЭВМ</button>
      </div>
    </div>
  </div>
</div>

<div id="preview-modal" class="preview-modal">
  <div class="preview-sheet">
    <div class="pricing-head">
      <div class="markup-title" style="margin:0;">Предпросмотр Наценки По Выбранным Категориям</div>
      <button class="pricing-close" id="close-preview-btn">Закрыть</button>
    </div>
    <div class="preview-toolbar-card">
      <div class="preview-grid">
        <div>
          <label class="field-label">Наценка, %
            <span class="info-tip" data-tip="Основная наценка по выбранной базе расчета. Для товаров выше порога опта работает только это правило.">i</span>
          </label>
          <input type="number" id="preview-percent" min="0" step="0.1" value="10">
        </div>
        <div>
          <label class="field-label">База расчета
            <span class="info-tip" data-tip="От какой цены считать основную наценку: от вашего опта или от минимальной, средней либо максимальной цены Onliner.">i</span>
          </label>
          <select id="preview-base-mode" style="width:100%;padding:8px 10px;border:1px solid #d1d5db;border-radius:8px;font-size:14px;">
            <option value="wholesale">От оптовой (Лучшая цена)</option>
            <option value="onliner_min">От Onliner Минимальной</option>
            <option value="onliner_avg">От Onliner Средней</option>
            <option value="onliner_max">От Onliner Максимальной</option>
          </select>
        </div>
        <div>
          <label class="field-label">Порог опта, руб
            <span class="info-tip" data-tip="Если оптовая цена товара меньше или равна этому порогу, включается спецусловие по минимальной прибыли.">i</span>
          </label>
          <input type="number" id="preview-threshold" min="0" step="0.1" value="0">
        </div>
        <div>
          <label class="field-label">Мин. прибыль, руб
            <span class="info-tip" data-tip="Минимальная добавка к опту. Для дешевых товаров итоговая цена не будет ниже: Опт + Мин. прибыль.">i</span>
          </label>
          <input type="number" id="preview-min-profit" min="0" step="0.1" value="0">
        </div>
        <div>
          <label class="field-label">Без скидки, % от РРЦ
            <span class="info-tip" data-tip="Дополнительный процент сверху от рассчитанной РРЦ. Эта цена попадет в отдельную колонку выгрузки.">i</span>
          </label>
          <input type="number" id="preview-no-discount-percent" min="0" step="0.1" value="0">
        </div>
        <div>
          <button class="btn" id="preview-apply-btn" style="width:100%;">Сохранить И Применить</button>
        </div>
        <div>
          <button class="btn btn-outline" id="refresh-market-btn" style="width:100%;">Обновить Цены Onliner</button>
        </div>
        <div>
          <button class="btn btn-outline" id="open-offers-btn" style="width:100%;padding:9px 10px;font-size:13px;">Проверить офферы</button>
        </div>
      </div>
    </div>
    <div class="preview-status-stack">
      <div class="preview-categories-box">
        <label style="font-size:13px;color:#4b5563;">Категории в этом окне (можно выбрать несколько)</label>
        <select id="preview-modal-categories" multiple style="width:100%;padding:8px 10px;border:1px solid #d1d5db;border-radius:8px;font-size:13px;"></select>
      </div>
      <div class="preview-status-card">
        <div class="markup-note" id="preview-modal-note">Показаны все товары выбранных категорий. При изменении процента таблица пересчитывается сразу.</div>
        <div class="markup-note" id="market-refresh-note" style="margin-top:4px;">Кэш рыночных цен подставляется сразу. Массовое обновление: сначала публичный catalog API (меньше запросов на товар); если цен нет — при включённом B2B дополнительно price.api. До ~28 параллельных загрузок.</div>
      </div>
    </div>
    <table id="preview-full-table" class="preview-full-table">
      <thead><tr><th>OnlinerID</th><th>Категория</th><th>Товар</th><th>Поставщик</th><th>Опт</th><th>Onliner Мин</th><th>Onliner Ср</th><th>Onliner Макс</th><th>РРЦ</th><th>Цена без скидки</th><th>Маржа, %</th><th>Правило</th></tr></thead>
      <tbody></tbody>
    </table>
  </div>
</div>

<div id="offers-modal" class="offers-modal">
  <div class="offers-sheet">
    <div class="pricing-head">
      <div class="markup-title" style="margin:0;">Проверка Офферов Onliner</div>
      <button class="pricing-close" id="close-offers-btn">Закрыть</button>
    </div>
    <div class="markup-note" id="offers-current-note">Выберите строку в таблице предпросмотра и нажмите "Проверить офферы".</div>
    <div class="offers-summary" id="offers-summary">
      <div class="offers-stat"><div class="k">OnlinerID</div><div class="v">—</div></div>
      <div class="offers-stat"><div class="k">API offers.count</div><div class="v">—</div></div>
      <div class="offers-stat"><div class="k">Позиции в API</div><div class="v">—</div></div>
      <div class="offers-stat"><div class="k">Уникальные магазины</div><div class="v">—</div></div>
    </div>
    <div class="markup-note" id="offers-note">Данные еще не загружены.</div>
    <table class="offers-table">
      <thead><tr><th>Магазин</th><th>Seller ID</th><th>Цена</th><th>Гарантия</th><th>Наличие</th><th>Обновлено</th><th>Ссылка</th></tr></thead>
      <tbody id="offers-body"><tr><td colspan="7" style="color:#64748b;">Нет данных</td></tr></tbody>
    </table>
  </div>
</div>

<!-- ══ IVEN Match Report Modal ══════════════════════════════════════════ -->
<div id="iven-report-modal" style="display:none;position:fixed;inset:0;z-index:9999;background:rgba(15,23,42,0.55);backdrop-filter:blur(3px);overflow-y:auto;padding:24px 16px;box-sizing:border-box;">
  <div style="max-width:900px;margin:0 auto;background:#fff;border-radius:16px;box-shadow:0 24px 64px rgba(0,0,0,0.22);overflow:hidden;display:flex;flex-direction:column;max-height:calc(100vh - 48px);">
    <!-- Header -->
    <div style="display:flex;align-items:center;justify-content:space-between;padding:20px 24px 16px;border-bottom:1px solid #e2e8f0;background:linear-gradient(135deg,#1e40af 0%,#2563eb 100%);flex-shrink:0;">
      <div>
        <div style="font-size:18px;font-weight:700;color:#fff;letter-spacing:-0.3px;" id="iven-modal-title">Отчёт подбора IVEN-бридж</div>
        <div style="font-size:13px;color:#bfdbfe;margin-top:2px;" id="iven-modal-subtitle">Сопоставление N-Tech товаров с базой Onliner ID</div>
      </div>
      <button id="iven-report-modal-close" style="background:rgba(255,255,255,0.15);border:none;border-radius:8px;color:#fff;font-size:20px;width:36px;height:36px;cursor:pointer;display:flex;align-items:center;justify-content:center;line-height:1;transition:background 0.15s;" onmouseover="this.style.background='rgba(255,255,255,0.25)'" onmouseout="this.style.background='rgba(255,255,255,0.15)'">&#x2715;</button>
    </div>
    <!-- Summary bar -->
    <div style="display:flex;gap:0;border-bottom:1px solid #e2e8f0;flex-shrink:0;" id="iven-modal-summary-bar">
      <div style="flex:1;padding:14px 20px;border-right:1px solid #e2e8f0;">
        <div style="font-size:24px;font-weight:700;color:#16a34a;" id="iven-modal-stat-matched">—</div>
        <div style="font-size:12px;color:#6b7280;margin-top:2px;" id="iven-modal-label-matched">Совпало</div>
      </div>
      <div style="flex:1;padding:14px 20px;border-right:1px solid #e2e8f0;">
        <div style="font-size:24px;font-weight:700;color:#dc2626;" id="iven-modal-stat-nomatch">—</div>
        <div style="font-size:12px;color:#6b7280;margin-top:2px;" id="iven-modal-label-nomatch">Не найдено</div>
      </div>
      <div style="flex:1;padding:14px 20px;border-right:1px solid #e2e8f0;">
        <div style="font-size:24px;font-weight:700;color:#2563eb;" id="iven-modal-stat-avgsc">—</div>
        <div style="font-size:12px;color:#6b7280;margin-top:2px;" id="iven-modal-label-avgsc">Средний Score</div>
      </div>
      <div style="flex:1;padding:14px 20px;">
        <label style="display:flex;align-items:center;gap:8px;cursor:pointer;font-size:13px;color:#374151;">
          <input type="checkbox" id="iven-modal-show-nomatch" style="accent-color:#2563eb;width:16px;height:16px;">
          <span id="iven-modal-show-nomatch-label">Показать без совпадения</span>
        </label>
      </div>
    </div>
    <!-- Score legend -->
    <div style="display:flex;gap:16px;padding:10px 20px;background:#f8fafc;border-bottom:1px solid #e2e8f0;flex-shrink:0;flex-wrap:wrap;">
      <span style="font-size:11px;color:#374151;font-weight:600;">Цвет оценки:</span>
      <span style="font-size:11px;"><span style="display:inline-block;width:10px;height:10px;background:#16a34a;border-radius:2px;vertical-align:middle;margin-right:4px;"></span>≥ 0.95 — отличное совпадение</span>
      <span style="font-size:11px;"><span style="display:inline-block;width:10px;height:10px;background:#2563eb;border-radius:2px;vertical-align:middle;margin-right:4px;"></span>≥ 0.85 — хорошее</span>
      <span style="font-size:11px;"><span style="display:inline-block;width:10px;height:10px;background:#d97706;border-radius:2px;vertical-align:middle;margin-right:4px;"></span>≥ 0.75 — проверить вручную</span>
      <span style="font-size:11px;"><span style="display:inline-block;width:10px;height:10px;background:#dc2626;border-radius:2px;vertical-align:middle;margin-right:4px;"></span>&lt; 0.75 — низкий (отклонено)</span>
    </div>
    <!-- Table -->
    <div style="overflow-y:auto;flex:1;min-height:0;">
      <table style="width:100%;border-collapse:collapse;font-size:13px;">
        <thead>
          <tr style="background:#f1f5f9;position:sticky;top:0;z-index:2;">
            <th style="padding:10px 14px;text-align:left;color:#374151;font-weight:600;border-bottom:2px solid #e2e8f0;width:32%;">Ваш товар (N-Tech)</th>
            <th style="padding:10px 14px;text-align:left;color:#374151;font-weight:600;border-bottom:2px solid #e2e8f0;">Найдено в базе</th>
            <th style="padding:10px 8px;text-align:center;color:#374151;font-weight:600;border-bottom:2px solid #e2e8f0;white-space:nowrap;width:70px;">Score</th>
            <th style="padding:10px 8px;text-align:center;color:#374151;font-weight:600;border-bottom:2px solid #e2e8f0;width:90px;">Onliner ID</th>
            <th style="padding:10px 8px;text-align:center;color:#374151;font-weight:600;border-bottom:2px solid #e2e8f0;width:110px;">Действие</th>
          </tr>
        </thead>
        <tbody id="iven-modal-tbody"></tbody>
      </table>
    </div>
    <!-- Footer -->
    <div style="padding:14px 24px;border-top:1px solid #e2e8f0;display:flex;justify-content:flex-end;flex-shrink:0;background:#f8fafc;">
      <button id="iven-report-modal-close2" style="padding:9px 24px;background:#2563eb;color:#fff;border:none;border-radius:8px;font-size:14px;font-weight:600;cursor:pointer;transition:background 0.15s;" onmouseover="this.style.background='#1d4ed8'" onmouseout="this.style.background='#2563eb'">Закрыть</button>
    </div>
  </div>
</div>

<!-- Модал: Проверка ID через API -->
<div id="validate-ids-modal" style="display:none;position:fixed;inset:0;z-index:9999;background:rgba(15,23,42,0.55);backdrop-filter:blur(3px);overflow-y:auto;padding:24px 16px;box-sizing:border-box;">
  <div style="max-width:920px;margin:0 auto;background:#fff;border-radius:16px;box-shadow:0 24px 64px rgba(0,0,0,0.22);overflow:hidden;display:flex;flex-direction:column;max-height:calc(100vh - 48px);">
    <div style="display:flex;align-items:center;justify-content:space-between;padding:20px 24px 16px;border-bottom:1px solid #e2e8f0;background:linear-gradient(135deg,#5b21b6 0%,#7c3aed 100%);flex-shrink:0;">
      <div>
        <div style="font-size:20px;font-weight:800;color:#fff;">Отчёт проверки ID через Onliner API</div>
        <div id="validate-modal-subtitle" style="margin-top:4px;font-size:13px;color:#ddd6fe;">Ожидание запуска...</div>
      </div>
      <button id="validate-ids-modal-close" style="background:rgba(255,255,255,0.15);border:none;border-radius:8px;padding:8px 12px;color:#fff;font-size:18px;cursor:pointer;line-height:1;">✕</button>
    </div>
    <!-- Stats row -->
    <div style="display:flex;gap:16px;padding:16px 24px;background:#f8f5ff;border-bottom:1px solid #e2e8f0;flex-shrink:0;flex-wrap:wrap;">
      <div style="text-align:center;min-width:80px;">
        <div id="vld-stat-checked" style="font-size:28px;font-weight:800;color:#7c3aed;">—</div>
        <div style="font-size:11px;color:#6b7280;">Проверено</div>
      </div>
      <div style="text-align:center;min-width:80px;">
        <div id="vld-stat-confirmed" style="font-size:28px;font-weight:800;color:#16a34a;">—</div>
        <div style="font-size:11px;color:#6b7280;">Подтверждено ✓</div>
      </div>
      <div style="text-align:center;min-width:80px;">
        <div id="vld-stat-cleared" style="font-size:28px;font-weight:800;color:#dc2626;">—</div>
        <div style="font-size:11px;color:#6b7280;">Очищено ✗</div>
      </div>
      <div style="text-align:center;min-width:90px;">
        <div id="vld-stat-skipped" style="font-size:28px;font-weight:800;color:#ca8a04;">—</div>
        <div style="font-size:11px;color:#6b7280;">Пропущено<br/>(сбой API)</div>
      </div>
      <div style="text-align:center;min-width:80px;">
        <div id="vld-stat-errors" style="font-size:28px;font-weight:800;color:#9ca3af;">—</div>
        <div style="font-size:11px;color:#6b7280;">Исключений</div>
      </div>
      <div style="flex:1;display:flex;align-items:center;">
        <label style="display:flex;align-items:center;gap:6px;font-size:12px;color:#374151;cursor:pointer;">
          <input type="checkbox" id="vld-show-confirmed"> Показать подтверждённые
        </label>
      </div>
    </div>
    <!-- Table -->
    <div style="overflow-y:auto;flex:1;">
      <table style="width:100%;border-collapse:collapse;">
        <thead>
          <tr style="background:#f8f5ff;font-size:12px;font-weight:700;color:#374151;position:sticky;top:0;z-index:1;">
            <th style="padding:8px 14px;text-align:left;border-bottom:1px solid #e2e8f0;">Ваш товар (N-Tech)</th>
            <th style="padding:8px 14px;text-align:left;border-bottom:1px solid #e2e8f0;">Название по Onliner API</th>
            <th style="padding:8px 8px;text-align:center;border-bottom:1px solid #e2e8f0;min-width:60px;">Score</th>
            <th style="padding:8px 8px;text-align:center;border-bottom:1px solid #e2e8f0;min-width:90px;">OnlinerID</th>
            <th style="padding:8px 8px;text-align:center;border-bottom:1px solid #e2e8f0;min-width:80px;">Статус</th>
          </tr>
        </thead>
        <tbody id="validate-modal-tbody"></tbody>
      </table>
    </div>
    <div style="padding:14px 24px;border-top:1px solid #e2e8f0;display:flex;justify-content:flex-end;flex-shrink:0;">
      <button id="validate-ids-modal-close2" style="padding:9px 24px;background:#7c3aed;color:#fff;border:none;border-radius:8px;font-size:14px;font-weight:600;cursor:pointer;">Закрыть</button>
    </div>
  </div>
</div>

<script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
<script src="https://cdn.datatables.net/1.13.7/js/jquery.dataTables.min.js"></script>
<script>
var dtLang = {url:'//cdn.datatables.net/plug-ins/1.13.7/i18n/ru.json'};
var tblMain = null;
var mainTableRows = [];
var previewTimer = null;
var UI_STATE_KEY = 'price_mixer_ui_state_v1';
var categoryMarkups = {};
var categoryCatalog = [];
var previewModalItems = [];
var previewModalDisplayOrder = [];
var previewModalRenderRafToken = 0;
var previewModalRequestSeq = 0;
var marketRefreshPollTimer = null;
var mainTableReloadTimer = null;
var autoSortItems = [];
var autoSortSelectedKeys = {};
var autoRefreshPollTimer = null;
var autoRefreshUiLock = false;
var showOnlyNoIdRows = false;
var showOnlyDuplicateIdRows = false;
var duplicateIdFilterSet = {};
var duplicateIdPriceStats = {};
var loadedSupplierCount = Number('{{ stats.suppliers }}') || 1;
var noIdInlinePickerState = { rowIdx: -1, loading: false, applyingId: '', message: '', items: [], queryName: '' };
var verifyAllIdsPollTimer = null;
var verifyAllIdsItems = [];
var verifyAllIdsCandidateState = {};
var verifyAllIdsReportItems = [];
var verifyAllIdsReportVisible = false;
var verifyAllIdsCardCollapsed = false;
var duplicateIdIssues = [];
var duplicateIdCandidateState = {};
var duplicateIdCardCollapsed = false;
var duplicateIdProblemCount = 0;
var duplicateIdLastActionMessage = '';
var qualityCheckCardCollapsed = false;
var googleExportProgressTimer = null;
var selectedPreviewRowIdx = -1;
var showOnlySnapshotRows = false;
var snapshotFilterMode = '';
var snapshotFilterNames = [];
var snapshotDetailMode = '';
if (typeof window.showBusyOverlay !== 'function') {
    window.showBusyOverlay = function(){};
}
if (typeof window.updateBusyOverlay !== 'function') {
    window.updateBusyOverlay = function(){};
}
if (typeof window.hideBusyOverlay !== 'function') {
    window.hideBusyOverlay = function(){};
}
var CATEGORY_PRIORITY_JS = [
    'Процессор',
    'Кулер',
    'Охлаждение',
    'Материнская плата',
    'Оперативная память',
    'SSD',
    'Жесткий диск',
    'Видеокарта',
    'Блок питания',
    'Корпус',
    'Монитор'
];

function onResultPageReady(fn){
    if(window.jQuery && typeof window.jQuery === 'function'){
        window.jQuery(fn);
        return;
    }
    if(document.readyState === 'loading'){
        document.addEventListener('DOMContentLoaded', fn);
    } else {
        fn();
    }
}

onResultPageReady(function(){
    window.snapshotDiffData = {{ (stats.snapshot_diff or {})|tojson|safe }};
    if(window.snapshotDiffData && window.snapshotDiffData.filters){
        snapshotFilterNames = [];
    }
    // Сначала кнопки и панели: раньше при падении DataTable() или initMarkupUI выполнение обрывалось — пустая таблица и «мертвые» кнопки (работали только ссылки).
    try { initNoIdFilterUI(); } catch (err) { console.error('[init] initNoIdFilterUI', err); }
    try { initMarkupUI(); } catch (err) { console.error('[init] initMarkupUI', err); }
    try { initSnapshotFilterUI(); } catch (err) { console.error('[init] initSnapshotFilterUI', err); }
    try { initOnlinerDbWidget(); } catch (err) { console.error('[init] initOnlinerDbWidget', err); }
    try { initNtechChecksBadges(); } catch (err) { console.error('[init] initNtechChecksBadges', err); }
    if(window.jQuery && $.fn && $.fn.dataTable && $.fn.dataTable.ext && $.fn.dataTable.ext.search){
        $.fn.dataTable.ext.search.push(function(settings, data, dataIndex, rowData){
            if(settings.nTable && settings.nTable.id !== 'tbl-main'){ return true; }
            if(!showOnlyNoIdRows){ return true; }
            var rawId = '';
            if(Array.isArray(rowData)){
                rawId = String((rowData && rowData[0]) || '').trim();
            } else if(settings && settings.aoData && settings.aoData[dataIndex] && Array.isArray(settings.aoData[dataIndex]._aData)){
                rawId = String((settings.aoData[dataIndex]._aData[0]) || '').trim();
            }
            return !rawId;
        });
        $.fn.dataTable.ext.search.push(function(settings, data, dataIndex, rowData){
            if(settings.nTable && settings.nTable.id !== 'tbl-main'){ return true; }
            if(!showOnlyDuplicateIdRows){ return true; }
            var rawId = '';
            if(Array.isArray(rowData)){
                rawId = String((rowData && rowData[0]) || '').trim();
            } else if(settings && settings.aoData && settings.aoData[dataIndex] && Array.isArray(settings.aoData[dataIndex]._aData)){
                rawId = String((settings.aoData[dataIndex]._aData[0]) || '').trim();
            } else if(Array.isArray(data)) {
                rawId = String((data && data[0]) || '').trim();
            }
            return !!(rawId && duplicateIdFilterSet[rawId]);
        });
        $.fn.dataTable.ext.search.push(function(settings, data, dataIndex, rowData){
            if(settings.nTable && settings.nTable.id !== 'tbl-main'){ return true; }
            if(!showOnlySnapshotRows || !Array.isArray(snapshotFilterNames) || !snapshotFilterNames.length){ return true; }
            var rawName = '';
            if(Array.isArray(rowData)){
                rawName = String((rowData && rowData[1]) || '').trim();
            } else if(settings && settings.aoData && settings.aoData[dataIndex] && Array.isArray(settings.aoData[dataIndex]._aData)){
                rawName = String((settings.aoData[dataIndex]._aData[1]) || '').trim();
            } else if(Array.isArray(data)) {
                rawName = String((data && data[1]) || '').trim();
            }
            return snapshotFilterNames.indexOf(rawName) >= 0;
        });
    }
    if(window.jQuery && $.fn && $.fn.DataTable){
        try {
            tblMain = $('#tbl-main').DataTable({
                ajax: {
                    url: '/api/consolidated',
                    dataSrc: function(json){
                        var rows = (json && json.data) ? json.data : [];
                        mainTableRows = rows;
                        rebuildDuplicateIdFilterSet(rows);
                        updateWithoutIdCount(rows);
                        return rows;
                    }
                },
                deferRender: true,
                pageLength: 100,
                order: [[1, 'asc']],
                language: dtLang,
                drawCallback: function(){
                    var api = this.api();
                    var visibleRows = api.rows({search: 'applied'}).data().toArray();
                    var nodes = api.rows({search: 'applied'}).nodes().toArray();
                    nodes.forEach(function(node){
                        if(!node || !node.classList){ return; }
                        node.classList.remove('dup-group-start', 'dup-group-mid', 'dup-group-end', 'dup-group-single');
                    });
                    visibleRows.forEach(function(r, idx){
                        var cls = getDuplicateGroupRowClass(visibleRows, idx);
                        var node = nodes[idx];
                        if(cls && node && node.classList){ node.classList.add(cls); }
                    });
                    if(!showOnlyNoIdRows){
                        updateWithoutIdCount(mainTableRows || []);
                        return;
                    }
                    updateWithoutIdCount(visibleRows);
                },
                columns: [
                    {data: 0, className: 'dt-center', render: function(d, type, row){return renderMainTableIdCell(String(d || '').trim(), row || []);}}, 
                    {data: 1, render: function(d, type, row){ return renderMainTableNameCell(d, row || []); }},
                    {data: 2, className: 'dt-center', render: function(d, type, row){ return renderMainPriceCell(d, row || []); }}, 
                    {data: 3, className: 'dt-center'},
                    {data: 4, className: 'dt-center'},
                    {data: 5, className: 'dt-center'},
                    {data: 6, className: 'dt-center', render: function(d){ return (d || d===0) ? parseFloat(d).toFixed(2) : ''; }},
                    {data: 7, className: 'dt-center', render: function(d){ return (d || d===0) ? parseFloat(d).toFixed(2) : ''; }}
                ]
            });
        } catch (err) {
            console.error('[init] DataTable', err);
            initMainTableFallback();
        }
    } else {
        initMainTableFallback();
    }
});

function initMainTableFallback(){
    var tbody = document.getElementById('tbl-main-body');
    if(!tbody){ return; }
    fetch('/api/consolidated').then(function(r){ return r.json(); }).then(function(d){
        mainTableRows = (d && d.data) ? d.data : [];
        rebuildDuplicateIdFilterSet(mainTableRows);
        updateWithoutIdCount(mainTableRows);
        renderMainTableFallback();
    }).catch(function(){
        tbody.innerHTML = '<tr><td colspan="8" style="padding:14px;color:#b91c1c;">Ошибка загрузки данных</td></tr>';
    });
    tblMain = {
        ajax: {
            reload: function(cb){
                fetch('/api/consolidated').then(function(r){ return r.json(); }).then(function(d){
                    mainTableRows = (d && d.data) ? d.data : [];
                    rebuildDuplicateIdFilterSet(mainTableRows);
                    updateWithoutIdCount(mainTableRows);
                    renderMainTableFallback();
                    if(typeof cb === 'function'){ cb(); }
                }).catch(function(){
                    if(typeof cb === 'function'){ cb(); }
                });
            }
        }
    };
}

function initNtechChecksBadges(){
    var diff = window.snapshotDiffData || {};
    var samples = (diff && diff.samples) ? diff.samples : {};
    var newItems = Array.isArray(samples.new_items) ? samples.new_items : [];
    if(!newItems.length){ return; }
    var counts = {
        cpu: 0, board: 0, monitor: 0, gpu: 0, ram: 0, ssd: 0, psu: 0, case: 0, hdd: 0, cooler: 0, printer: 0, peripheral: 0
    };
    function mapCategoryToCheckKey(rawCategory){
        var c = String(rawCategory || '').trim().toLowerCase();
        if(!c){ return ''; }
        if(c.indexOf('процессор') >= 0) return 'cpu';
        if(c.indexOf('материн') >= 0) return 'board';
        if(c.indexOf('монитор') >= 0) return 'monitor';
        if(c.indexOf('видеокарт') >= 0) return 'gpu';
        if(c.indexOf('оператив') >= 0 || c === 'ram') return 'ram';
        if(c === 'ssd') return 'ssd';
        if(c.indexOf('блок питания') >= 0 || c === 'бп' || c === 'psu') return 'psu';
        if(c.indexOf('корпус') >= 0) return 'case';
        if(c.indexOf('жестк') >= 0 || c === 'hdd') return 'hdd';
        if(c.indexOf('кулер') >= 0 || c.indexOf('охлажден') >= 0 || c.indexOf('охлаждение') >= 0) return 'cooler';
        if(c.indexOf('принтер') >= 0 || c.indexOf('мфу') >= 0) return 'printer';
        if(c.indexOf('клавиат') >= 0 || c.indexOf('мыш') >= 0 || c.indexOf('науш') >= 0 || c.indexOf('гарнит') >= 0 || c.indexOf('акуст') >= 0) return 'peripheral';
        return '';
    }
    newItems.forEach(function(item){
        var key = mapCategoryToCheckKey(item && item.category);
        if(key && counts[key] !== undefined){
            counts[key] += 1;
        }
    });
    Object.keys(counts).forEach(function(key){
        var badge = document.getElementById(key + '-review-badge');
        if(!badge){ return; }
        var cnt = Number(counts[key] || 0);
        if(cnt > 0){
            badge.textContent = '+' + String(cnt);
            badge.classList.add('active');
        } else {
            badge.textContent = '';
            badge.classList.remove('active');
        }
    });
}

function renderMainTableFallback(){
    var tbody = document.getElementById('tbl-main-body');
    if(!tbody){ return; }
    var rows = (mainTableRows || []).slice().sort(function(a,b){
        if(showOnlyDuplicateIdRows){
            var ao = String((a && a[0]) || '').trim();
            var bo = String((b && b[0]) || '').trim();
            if(ao !== bo){ return ao.localeCompare(bo, 'ru'); }
            var ap = Number((a && a[2]) || NaN);
            var bp = Number((b && b[2]) || NaN);
            var aOk = isFinite(ap);
            var bOk = isFinite(bp);
            if(aOk && bOk && ap !== bp){ return ap - bp; }
            if(aOk && !bOk){ return -1; }
            if(!aOk && bOk){ return 1; }
        }
        return String(a[1] || '').localeCompare(String(b[1] || ''), 'ru');
    });
    if(showOnlyNoIdRows){
        rows = rows.filter(function(r){
            return !String(r[0] || '').trim();
        });
    }
    if(showOnlyDuplicateIdRows){
        rows = rows.filter(function(r){
            var oid = String((r && r[0]) || '').trim();
            return !!(oid && duplicateIdFilterSet[oid]);
        });
    }
    if(showOnlySnapshotRows && Array.isArray(snapshotFilterNames) && snapshotFilterNames.length){
        rows = rows.filter(function(r){
            return snapshotFilterNames.indexOf(String(r[1] || '').trim()) >= 0;
        });
    }
    updateWithoutIdCount(showOnlyNoIdRows ? rows : mainTableRows);
    if(!rows.length){
        tbody.innerHTML = '<tr><td colspan="8" style="padding:14px;color:#64748b;">Нет данных</td></tr>';
        return;
    }
    var html = '';
    rows.forEach(function(r, idx){
        var oid = String(r[0] || '').trim();
        var rowIdx = Number(r[8] || -1);
        var name = renderMainTableNameCell(r[1] || '', r);
        var price = renderMainPriceCell(r[2], r);
        var supplier = escapeHtml(r[3] || '');
        var warranty = escapeHtml(r[4] || '');
        var lead = escapeHtml(r[5] || '');
        var rrc = (r[6] || r[6]===0) ? Number(r[6]).toFixed(2) : '';
        var noDiscount = (r[7] || r[7]===0) ? Number(r[7]).toFixed(2) : '';
        var rowClass = getDuplicateGroupRowClass(rows, idx);
        html += '<tr' + (rowClass ? (' class="' + rowClass + '"') : '') + '>'
            + '<td>' + renderMainTableIdCell(oid, r) + '</td>'
            + '<td>' + name + '</td>'
            + '<td>' + price + '</td>'
            + '<td>' + supplier + '</td>'
            + '<td>' + warranty + '</td>'
            + '<td>' + lead + '</td>'
            + '<td>' + rrc + '</td>'
            + '<td>' + noDiscount + '</td>'
            + '</tr>';
    });
    tbody.innerHTML = html;
}

function redrawMainTable(){
    if(tblMain){
        if(typeof tblMain.draw === 'function'){
            tblMain.draw(false);
        } else if(tblMain.ajax && typeof tblMain.ajax.reload === 'function'){
            tblMain.ajax.reload(null, false);
        }
    } else {
        renderMainTableFallback();
    }
}

function initNoIdFilterUI(){
    var btn = document.getElementById('toggle-noid-btn');
    var duplicateBtn = document.getElementById('toggle-duplicate-id-btn');
    var autoBtn = document.getElementById('autofill-tgpc-pc-btn');
    var autoNote = document.getElementById('autofill-tgpc-pc-note');
    var autoPollTimer = null;
    var autoVisualTimer = null;
    var autoVisualPercent = 0;
    var autoRunLabel = 'Авто TGPC ПЭВМ';
    function renderAutoReport(items, st){
        var host = document.getElementById('autofill-tgpc-pc-report');
        var chipsEl = document.getElementById('autofill-tgpc-chips');
        if(!host){ return; }
        var list = Array.isArray(items) ? items : [];

        // --- chips summary ---
        if(chipsEl && st && (Number(st.total||0) > 0 || st.finished_at)){
            var applied = Number(st.applied||0);
            var skipped = Number(st.skipped||0);
            var notFound = Math.max(0, Number(st.done||0) - applied - skipped);
            var chips = '';
            if(applied > 0) chips += '<span style="display:inline-flex;align-items:center;gap:3px;background:#dcfce7;color:#15803d;border-radius:20px;padding:3px 10px;font-size:12px;font-weight:600;">✓ '+applied+' найдено</span>';
            if(notFound > 0) chips += '<span style="display:inline-flex;align-items:center;gap:3px;background:#fee2e2;color:#dc2626;border-radius:20px;padding:3px 10px;font-size:12px;font-weight:600;">✕ '+notFound+' нет на Onliner</span>';
            if(skipped > 0) chips += '<span style="display:inline-flex;align-items:center;gap:3px;background:#fef3c7;color:#b45309;border-radius:20px;padding:3px 10px;font-size:12px;font-weight:600;">~ '+skipped+' низкий score</span>';
            chipsEl.innerHTML = chips;
            chipsEl.style.display = chips ? 'flex' : 'none';
        }

        if(!list.length){ host.innerHTML = ''; return; }

        var matched = list.filter(function(i){ return i && i.status === 'matched'; });
        var others  = list.filter(function(i){ return i && i.status !== 'matched'; });

        var html = '<div style="margin-top:12px;text-align:left;">';

        // --- найдено ---
        if(matched.length){
            html += '<div style="font-size:10px;font-weight:700;color:#15803d;letter-spacing:.05em;text-transform:uppercase;margin-bottom:5px;">✓ Найдено ('+matched.length+')</div>';
            html += '<div style="max-height:210px;overflow-y:auto;border-radius:8px;border:1px solid #bbf7d0;background:#f0fdf4;margin-bottom:10px;">';
            matched.forEach(function(item, idx){
                var border = idx > 0 ? 'border-top:1px solid #dcfce7;' : '';
                html += '<div style="padding:8px 10px;'+border+'">';
                html += '<div style="font-size:12px;font-weight:600;color:#14532d;line-height:1.3;margin-bottom:4px;">'+ escapeHtml(item.name||'') +'</div>';
                html += '<div style="display:flex;align-items:center;gap:5px;flex-wrap:wrap;">';
                if(item.onliner_id){
                    html += '<span style="background:#d1fae5;color:#065f46;border-radius:5px;padding:2px 7px;font-size:11px;font-weight:700;font-family:monospace;">ID&nbsp;'+ escapeHtml(String(item.onliner_id)) +'</span>';
                }
                if(item.score){
                    var sc = Number(item.score);
                    var scColor = sc >= 0.95 ? '#15803d' : '#b45309';
                    html += '<span style="background:#fff;color:'+scColor+';border-radius:5px;padding:2px 6px;font-size:10px;border:1px solid #e5e7eb;">score&nbsp;'+escapeHtml(String(item.score))+'</span>';
                }
                if(item.onliner_name){
                    html += '<span style="font-size:11px;color:#374151;font-style:italic;">'+ escapeHtml(item.onliner_name) +'</span>';
                }
                html += '</div></div>';
            });
            html += '</div>';
        }

        // --- не найдено / пропущено ---
        if(others.length){
            html += '<div style="font-size:10px;font-weight:700;color:#dc2626;letter-spacing:.05em;text-transform:uppercase;margin-bottom:5px;">✕ Не найдено / пропущено ('+others.length+')</div>';
            html += '<div style="max-height:180px;overflow-y:auto;border-radius:8px;border:1px solid #fecaca;background:#fff8f8;">';
            others.forEach(function(item, idx){
                var border = idx > 0 ? 'border-top:1px solid #fee2e2;' : '';
                var isSkip = item.status === 'skipped';
                html += '<div style="padding:7px 10px;'+border+'">';
                html += '<div style="font-size:12px;color:#374151;line-height:1.3;">'+ escapeHtml(item.name||'') +'</div>';
                if(isSkip && item.onliner_id){
                    html += '<div style="font-size:11px;color:#b45309;margin-top:2px;">Найден похожий · ID '+ escapeHtml(String(item.onliner_id)) +' · score '+ escapeHtml(String(item.score||'')) +' (недостаточно уверен)</div>';
                } else {
                    html += '<div style="font-size:11px;color:#9ca3af;margin-top:2px;">Нет совпадения в каталоге Onliner</div>';
                }
                html += '</div>';
            });
            html += '</div>';
        }

        html += '</div>';
        host.innerHTML = html;
    }
    function stopAutoPoll(){
        if(autoPollTimer){
            clearInterval(autoPollTimer);
            autoPollTimer = null;
        }
    }
    function stopAutoVisual(){
        if(autoVisualTimer){
            clearInterval(autoVisualTimer);
            autoVisualTimer = null;
        }
    }
    function startAutoVisual(){
        stopAutoVisual();
        autoVisualPercent = 1;
        if(autoBtn){
            autoBtn.textContent = 'Подбираю ' + autoVisualPercent + '%';
        }
        autoVisualTimer = setInterval(function(){
            if(autoVisualPercent < 95){
                autoVisualPercent = Math.min(95, autoVisualPercent + (autoVisualPercent < 20 ? 3 : 2));
                if(autoBtn){
                    autoBtn.textContent = 'Подбираю ' + autoVisualPercent + '%';
                }
            }
        }, 1200);
    }
    function pollAutoStatus(){
        fetch('/api/autofill-tgpc-pc-status?_ts=' + Date.now(), {cache:'no-store'}).then(function(r){ return r.json(); }).then(function(st){
            if(!st){ throw new Error('no_status'); }
            var percent = Number(st.percent || 0);
            var total = Number(st.total || 0);
            var done = Number(st.done || 0);
            var applied = Number(st.applied || 0);
            var skipped = Number(st.skipped || 0);
            var items = Array.isArray(st.items) ? st.items : [];
            if(percent > autoVisualPercent){
                autoVisualPercent = percent;
            }
            // progress bar
            var progWrap = document.getElementById('autofill-tgpc-progress-wrap');
            var progBar  = document.getElementById('autofill-tgpc-progress-bar');
            if(progWrap && progBar){
                if(st.running || st.finished_at){
                    progWrap.style.display = 'block';
                    progBar.style.width = Math.max(percent, autoVisualPercent) + '%';
                }
                if(!st.running && st.finished_at){
                    progBar.style.background = applied > 0 ? 'linear-gradient(90deg,#4ade80,#16a34a)' : '#d1d5db';
                }
            }
            if(autoBtn){
                autoBtn.textContent = st.running ? ('Подбираю ' + Math.max(percent, autoVisualPercent) + '%') : (autoRunLabel + (st.finished_at ? (' ' + percent + '%') : ''));
                if(!st.running && !st.finished_at){
                    autoBtn.textContent = autoRunLabel;
                }
            }
            if(autoNote){
                if(st.running){
                    autoNote.textContent = 'Обработано ' + done + ' из ' + total + ' TGPC ПЭВМ. Подставлено: ' + applied + '.';
                } else if(st.message && String(st.message).toLowerCase().indexOf('ошибка') === 0){
                    autoNote.textContent = st.message;
                } else if(st.finished_at){
                    autoNote.textContent = 'Автоподбор завершён: подставлено ' + applied + ' из ' + total + ' TGPC ПЭВМ (' + percent + '%). Пропущено: ' + skipped + '.';
                } else if(st.message){
                    autoNote.textContent = st.message;
                }
            }
            renderAutoReport(items, st);
            if(st.running){
                updateBusyOverlay('Автоподбор TGPC ПЭВМ', 'Обработано ' + done + ' из ' + total + '. Подставлено: ' + applied + '.');
                return;
            }
            stopAutoPoll();
            stopAutoVisual();
            if(autoBtn){ autoBtn.disabled = false; }
            hideBusyOverlay();
            if(st.finished_at){
                closeNoIdInlinePicker(false);
                if(tblMain && tblMain.ajax && typeof tblMain.ajax.reload === 'function'){
                    tblMain.ajax.reload(null, false);
                } else {
                    fetch('/api/consolidated').then(function(r){ return r.json(); }).then(function(resp){
                        mainTableRows = (resp && resp.data) ? resp.data : [];
                        updateWithoutIdCount(mainTableRows);
                        renderMainTableFallback();
                    });
                }
                runPreExportQualityCheck();
            }
        }).catch(function(){
            stopAutoPoll();
            stopAutoVisual();
            if(autoBtn){
                autoBtn.disabled = false;
                autoBtn.textContent = autoRunLabel;
            }
            if(autoNote){
                autoNote.textContent = 'Не удалось получить прогресс автоподбора TGPC ПЭВМ.';
            }
            renderAutoReport([]);
            hideBusyOverlay();
        });
    }
    function startAutoFill(limitValue, labelText){
        if(autoBtn && autoBtn.disabled){ return; }
        autoRunLabel = labelText || 'Авто TGPC ПЭВМ';
        if(autoBtn){
            autoBtn.disabled = true;
            autoBtn.textContent = 'Подбираю 1%';
        }
        if(autoNote){
            autoNote.textContent = 'Запустили подбор TGPC ПЭВМ. Сейчас начнём проверку первых позиций...';
        }
        renderAutoReport([], null);
        var progWrap2 = document.getElementById('autofill-tgpc-progress-wrap');
        var progBar2  = document.getElementById('autofill-tgpc-progress-bar');
        var chips2    = document.getElementById('autofill-tgpc-chips');
        if(progWrap2){ progWrap2.style.display = 'block'; }
        if(progBar2){ progBar2.style.width = '2%'; progBar2.style.background = 'linear-gradient(90deg,#f59e0b,#d56e0c)'; }
        if(chips2){ chips2.innerHTML = ''; chips2.style.display = 'none'; }
        startAutoVisual();
        showBusyOverlay('Автоподбор TGPC ПЭВМ', 'Ищем совпадения в локальной SQLite-базе и сохраняем найденные ID...');
        fetch('/api/autofill-tgpc-pc-ids', {
            method: 'POST',
            headers: {'Content-Type': 'application/json'},
            body: JSON.stringify(limitValue ? {limit: limitValue} : {})
        }).then(function(r){ return r.json(); }).then(function(d){
            if(!d || (d.status !== 'started' && d.status !== 'already_running')){
                throw new Error((d && d.message) || 'autofill_failed');
            }
            stopAutoPoll();
            autoPollTimer = setInterval(pollAutoStatus, 1200);
            pollAutoStatus();
        }).catch(function(){
            if(autoNote){
                autoNote.textContent = 'Не удалось завершить автоподбор TGPC ПЭВМ.';
            }
            stopAutoVisual();
            if(autoBtn){
                autoBtn.disabled = false;
                autoBtn.textContent = autoRunLabel;
            }
            hideBusyOverlay();
        });
    }
    // Не делаем ранний return, если кнопки фильтра нет: ниже инициализируются IVEN / N-Tech — иначе «ломаются» все кнопки блока.
    if(btn){
        btn.addEventListener('click', function(){
            showOnlyNoIdRows = !showOnlyNoIdRows;
            if(showOnlyNoIdRows){
                showOnlyDuplicateIdRows = false;
                if(duplicateBtn){ duplicateBtn.textContent = 'Показать дубли ID'; }
            }
            if(!showOnlyNoIdRows){
                closeNoIdInlinePicker(false);
            }
            btn.textContent = showOnlyNoIdRows ? 'Показать все' : 'Показать';
            if(tblMain){
                if(typeof tblMain.draw === 'function'){
                    if(showOnlyNoIdRows && typeof tblMain.order === 'function'){
                        tblMain.order([[1, 'asc']]);
                    }
                    tblMain.draw(false);
                } else if(tblMain.ajax && typeof tblMain.ajax.reload === 'function'){
                    tblMain.ajax.reload(null, false);
                }
            } else {
                renderMainTableFallback();
            }
        });
    }
    if(duplicateBtn){
        duplicateBtn.addEventListener('click', function(){
            showOnlyDuplicateIdRows = !showOnlyDuplicateIdRows;
            if(showOnlyDuplicateIdRows){
                showOnlyNoIdRows = false;
                if(btn){ btn.textContent = 'Показать'; }
                duplicateBtn.textContent = 'Показать все';
                runDuplicateIdCheck(false);
                var duplicateCard = document.getElementById('duplicate-id-check-card');
                if(duplicateCard){
                    duplicateCard.style.display = 'block';
                    duplicateCard.scrollIntoView({behavior:'smooth', block:'start'});
                }
            } else {
                duplicateBtn.textContent = 'Показать дубли ID';
            }
            if(tblMain){
                if(typeof tblMain.draw === 'function'){
                    if(typeof tblMain.order === 'function'){
                        if(showOnlyDuplicateIdRows){
                            tblMain.order([[0, 'asc'], [2, 'asc']]);
                        } else {
                            tblMain.order([[1, 'asc']]);
                        }
                    }
                    tblMain.draw(false);
                } else if(tblMain.ajax && typeof tblMain.ajax.reload === 'function'){
                    tblMain.ajax.reload(null, false);
                }
            } else {
                renderMainTableFallback();
            }
        });
    }
    if(autoBtn){
        autoBtn.addEventListener('click', function(){
            startAutoFill(0, 'Авто TGPC ПЭВМ');
        });
    }

    // ── IVEN Bridge autofill ──────────────────────────────────────────────────
    (function(){
        var ivenBtn       = document.getElementById('autofill-iven-btn');
        var b2bBtn        = document.getElementById('autofill-b2b-btn');
        var clearNonPcBtn = document.getElementById('clear-nonpc-ids-btn');
        var clearDuplicateIdsBtn = document.getElementById('clear-duplicate-ids-btn');
        var cpuReviewBtn = document.getElementById('cpu-review-btn');
        var boardReviewBtn = document.getElementById('board-review-btn');
        var monitorReviewBtn = document.getElementById('monitor-review-btn');
        var gpuReviewBtn = document.getElementById('gpu-review-btn');
        var ramReviewBtn = document.getElementById('ram-review-btn');
        var ssdReviewBtn = document.getElementById('ssd-review-btn');
        var psuReviewBtn = document.getElementById('psu-review-btn');
        var caseReviewBtn = document.getElementById('case-review-btn');
        var hddReviewBtn = document.getElementById('hdd-review-btn');
        var coolerReviewBtn = document.getElementById('cooler-review-btn');
        var printerReviewBtn = document.getElementById('printer-review-btn');
        var peripheralReviewBtn = document.getElementById('peripheral-review-btn');
        var ivenReportBtn = document.getElementById('autofill-iven-report-btn');
        var ivenWrap      = document.getElementById('autofill-iven-progress-wrap');
        var ivenBar       = document.getElementById('autofill-iven-progress-bar');
        var ivenMsg       = document.getElementById('autofill-iven-msg');
        var ivenTimer     = null;
        var lastStatus    = null;

        function scoreColor(sc){
            if(sc >= 0.95) return '#16a34a';
            if(sc >= 0.85) return '#2563eb';
            if(sc >= 0.75) return '#d97706';
            return '#dc2626';
        }

        function scoreBg(sc){
            if(sc >= 0.95) return '#f0fdf4';
            if(sc >= 0.85) return '#eff6ff';
            if(sc >= 0.75) return '#fffbeb';
            return '#fff1f2';
        }

        function renderInlineDbSearch(host, query, manualInput){
            if(!host) return;
            var q = String(query || '').trim();
            if(!q){
                host.style.display = 'none';
                host.innerHTML = '';
                return;
            }
            host.style.display = 'block';
            host.innerHTML = '<div style="font-size:11px;color:#64748b;">Ищу в базе...</div>';
            fetch('/api/onliner-db-search?q=' + encodeURIComponent(q), {cache:'no-store'})
            .then(function(r){ return r.json(); })
            .then(function(resp){
                var items = (resp && resp.items) || [];
                if(!items.length){
                    host.innerHTML = '<div style="font-size:11px;color:#64748b;">В базе ничего не найдено по этому запросу.</div>';
                    return;
                }
                var html = '';
                items.slice(0, 8).forEach(function(item){
                    var oid = String((item && item.id) || '').trim();
                    var nm = escapeHtml(String((item && item.name) || '').trim());
                    html += '<div style="padding:4px 0;border-bottom:1px solid #f1f5f9;">';
                    html += '<div style="font-size:11px;color:#334155;line-height:1.3;">' + nm + '</div>';
                    html += '<div style="margin-top:3px;display:flex;align-items:center;gap:6px;flex-wrap:wrap;">';
                    html += '<span style="font-family:monospace;font-size:11px;font-weight:700;color:#2563eb;">' + escapeHtml(oid) + '</span>';
                    html += '<button class="iven-inline-db-apply-btn" data-id="' + escapeHtml(oid) + '" type="button" style="margin-left:auto;padding:2px 7px;border:1px solid #2563eb;border-radius:4px;background:#eff6ff;color:#1d4ed8;font-size:10px;font-weight:700;cursor:pointer;">Подставить</button>';
                    html += '</div></div>';
                });
                host.innerHTML = html;
                host.querySelectorAll('.iven-inline-db-apply-btn').forEach(function(btn){
                    btn.addEventListener('click', function(){
                        if(!manualInput) return;
                        var oid = String(btn.getAttribute('data-id') || '').trim();
                        if(!oid) return;
                        manualInput.value = oid;
                        try{
                            manualInput.dispatchEvent(new Event('input', {bubbles:true}));
                            manualInput.dispatchEvent(new Event('change', {bubbles:true}));
                        }catch(_e){}
                        manualInput.focus();
                    });
                });
            })
            .catch(function(){
                host.innerHTML = '<div style="font-size:11px;color:#dc2626;">Ошибка поиска в базе.</div>';
            });
        }

        // Track confirmed/rejected rows: name → state ('confirmed'|'rejected')
        var ivenRowStates = {};

        function syncLastStatusAfterManualSave(name, rowIdx, oid){
            if(!lastStatus || !Array.isArray(lastStatus.no_match)) return;
            var targetName = String(name || '').trim();
            var targetOid = String(oid || '').trim();
            lastStatus.no_match = lastStatus.no_match.filter(function(item){
                var sameName = String((item && item.name) || '').trim() === targetName;
                var sameRow = String((item && item.row_idx) || '') === String(rowIdx == null ? '' : rowIdx);
                return !(sameName && (sameRow || rowIdx == null));
            });
            if(String((lastStatus.report_mode || '')).toLowerCase() !== 'cpu' && targetOid){
                lastStatus.matches = Array.isArray(lastStatus.matches) ? lastStatus.matches : [];
                var exists = lastStatus.matches.some(function(item){
                    return String((item && item.name) || '').trim() === targetName && String((item && item.id) || '').trim() === targetOid;
                });
                if(!exists){
                    lastStatus.matches.push({name: targetName, id: targetOid, score: '1.000', matched_name: targetName, source: 'manual'});
                }
            }
        }

        function reportIssueKey(item){
            return String(((item && item.cpu_issue) || (item && item.board_issue) || (item && item.monitor_issue) || (item && item.gpu_issue) || (item && item.ram_issue) || (item && item.ssd_issue) || (item && item.psu_issue) || (item && item.case_issue) || (item && item.hdd_issue) || (item && item.cooler_issue) || (item && item.printer_issue) || '')).trim();
        }

        function ivenConfirmRow(tr, m){
            // Save to manual bindings via existing batch endpoint
            var items = [{name: m.name, onliner_id: m.id, url: m.url || '', row_idx: m.row_idx}];
            fetch('/api/manual-id-confirm-batch', {
                method:'POST',
                headers:{'Content-Type':'application/json'},
                body: JSON.stringify({source:'iven_report', items: items})
            }).then(function(r){
                return r.json().catch(function(){ return {}; }).then(function(d){
                    if(!r.ok || !d || d.status !== 'ok'){
                        throw new Error((d && d.message) || ('save_failed_' + r.status));
                    }
                    return d;
                });
            }).then(function(){
                ivenRowStates[m.name] = 'confirmed';
                ivenRowStates[(m.name||'') + '_' + (m.id||'')] = 'confirmed';
                syncLastStatusAfterManualSave(m.name, m.row_idx, m.id);
                tr.style.background = '#f0fdf4';
                var actCell = tr.querySelector('.iven-act-cell');
                if(actCell) actCell.innerHTML = '<span style="color:#16a34a;font-weight:600;font-size:12px;">✓ Сохранено</span>';
                if(typeof loadReviewQueue === 'function') loadReviewQueue();
                if(typeof reloadMainTable === 'function') reloadMainTable();
            }).catch(function(err){
                var actCell = tr.querySelector('.iven-act-cell');
                if(actCell){
                    actCell.innerHTML = '<span style="color:#dc2626;font-weight:600;font-size:12px;">Ошибка: ' + escapeHtml(String((err && err.message) || 'save_failed')) + '</span>';
                }
            });
        }

        function ivenRejectRow(tr, m){
            fetch('/api/iven-reject-match', {
                method:'POST',
                headers:{'Content-Type':'application/json'},
                body: JSON.stringify({name: m.name, row_idx: m.row_idx})
            }).then(function(r){ return r.json(); }).then(function(d){
                if(d.status === 'ok'){
                    ivenRowStates[m.name] = 'rejected';
                    tr.style.background = '#fef2f2';
                    tr.style.opacity = '0.55';
                    var actCell = tr.querySelector('.iven-act-cell');
                    if(actCell) actCell.innerHTML = '<span style="color:#dc2626;font-weight:600;font-size:12px;">✗ Удалено</span>';
                    // Update no-id counter
                    var wiel = document.getElementById('without-id-count');
                    if(wiel && !isNaN(parseInt(wiel.textContent))){
                        wiel.textContent = parseInt(wiel.textContent) + 1;
                    }
                }
            }).catch(function(){});
        }

        function ivenSaveManualId(name, rowIdx, rawId, btn, input, caption){
            var finalId = String(rawId || '').trim().replace(/\\.0$/, '');
            if(!/^\\d+$/.test(finalId)){
                if(caption) caption.textContent = 'Нужен числовой Onliner ID';
                if(input) input.focus();
                return;
            }
            if(btn) btn.disabled = true;
            if(caption) caption.textContent = 'Сохраняю...';
            fetch('/api/manual-id-confirm-batch', {
                method:'POST',
                headers:{'Content-Type':'application/json'},
                body: JSON.stringify({
                    source:'iven_manual_input',
                    items:[{name:name, onliner_id:finalId, row_idx: rowIdx}]
                })
            }).then(function(r){
                return r.json().catch(function(){ return {}; }).then(function(d){
                    if(!r.ok || !d || d.status !== 'ok'){
                        throw new Error((d && d.message) || ('save_failed_' + r.status));
                    }
                    return d;
                });
            }).then(function(d){
                if(d && d.status === 'ok'){
                    ivenRowStates[(name||'') + '_' + finalId] = 'confirmed';
                    ivenRowStates[name] = 'confirmed';
                    syncLastStatusAfterManualSave(name, rowIdx, finalId);
                    if(caption) caption.innerHTML = '<span style="color:#16a34a;font-weight:700;">✓ Сохранено в ручные привязки</span>';
                    if(input) input.value = finalId;
                    if(typeof loadReviewQueue === 'function') loadReviewQueue();
                    if(typeof reloadMainTable === 'function') reloadMainTable();
                } else {
                    if(caption) caption.textContent = (d && d.message) || 'Не удалось сохранить';
                }
            }).catch(function(err){
                if(caption) caption.textContent = 'Ошибка сохранения: ' + String((err && err.message) || 'save_failed');
            }).finally(function(){
                if(btn) btn.disabled = false;
            });
        }

        function ivenSourceBadge(source){
            var src = String(source || '').trim();
            if(!src) return '';
            var isExact = (src === 'db_exact' || src === 'b2b_exact');
            var isB2B = src.indexOf('b2b') === 0;
            var bg = isExact ? '#dcfce7' : (isB2B ? '#dbeafe' : '#fef3c7');
            var col = isExact ? '#166534' : (isB2B ? '#1d4ed8' : '#92400e');
            var label = src;
            return '<span style="display:inline-block;margin-top:4px;padding:2px 6px;border-radius:999px;background:' + bg + ';color:' + col + ';font-size:10px;font-weight:700;">' + label + '</span>';
        }

        function renderIvenModal(st){
            var modal  = document.getElementById('iven-report-modal');
            var tbody  = document.getElementById('iven-modal-tbody');
            var showNM = document.getElementById('iven-modal-show-nomatch');
            if(!modal || !tbody) return;

            var reportMode = String((st && st.report_mode) || 'iven').toLowerCase();
            var matches = (st && st.matches)  || [];
            var noMatch = (st && st.no_match) || [];
            // For SSD tuning sessions, show "no candidates" list immediately.
            if(showNM && (reportMode === 'ssd' || reportMode === 'hdd' || reportMode === 'cooler' || reportMode === 'printer') && !showNM.checked){
                showNM.checked = true;
            }
            var showNoMatch = showNM && showNM.checked;
            var queuedCpu = noMatch.filter(function(m){ return reportIssueKey(m) === 'queued'; });
            var noModelCpu = noMatch.filter(function(m){ return reportIssueKey(m) === 'no_model'; });
            var noCandCpu = noMatch.filter(function(m){ return reportIssueKey(m) === 'no_candidates'; });

            // Update summary stats
            var el = function(id){ return document.getElementById(id); };
            if(el('iven-modal-title')) el('iven-modal-title').textContent = (st && st.report_title) || (reportMode === 'cpu' ? 'Отчёт CPU N-Tech' : (reportMode === 'cooler' ? 'Отчёт охлаждения N-Tech' : (reportMode === 'printer' ? 'Отчёт принтеров и МФУ N-Tech' : 'Отчёт подбора IVEN-бридж')));
            if(el('iven-modal-subtitle')){
                el('iven-modal-subtitle').textContent =
                    (st && st.report_subtitle)
                    || (reportMode === 'cpu'
                        ? ('Обработано CPU: ' + (queuedCpu.length + noModelCpu.length + noCandCpu.length) + ' товаров N-Tech')
                        : ('Обработано: ' + (matches.length + noMatch.length) + ' товаров N-Tech'));
            }
            if(reportMode !== 'iven'){
                if(el('iven-modal-stat-matched')) el('iven-modal-stat-matched').textContent = queuedCpu.length;
                if(el('iven-modal-stat-nomatch')) el('iven-modal-stat-nomatch').textContent = noModelCpu.length;
                if(el('iven-modal-stat-avgsc')) el('iven-modal-stat-avgsc').textContent = noCandCpu.length;
                if(el('iven-modal-label-matched')) el('iven-modal-label-matched').textContent = 'В очереди';
                if(el('iven-modal-label-nomatch')) el('iven-modal-label-nomatch').textContent = 'Без модели';
                if(el('iven-modal-label-avgsc')) el('iven-modal-label-avgsc').textContent = 'Без кандидатов';
                if(el('iven-modal-show-nomatch-label')) el('iven-modal-show-nomatch-label').textContent = 'Показать проблемные';
            } else {
                if(el('iven-modal-stat-matched')) el('iven-modal-stat-matched').textContent = matches.length;
                if(el('iven-modal-stat-nomatch')) el('iven-modal-stat-nomatch').textContent = noMatch.length;
                if(el('iven-modal-stat-avgsc')){
                    var avgSc = matches.length > 0
                        ? (matches.reduce(function(s,m){ return s + (parseFloat(m.score)||0); }, 0) / matches.length).toFixed(2)
                        : '—';
                    el('iven-modal-stat-avgsc').textContent = avgSc;
                }
                if(el('iven-modal-label-matched')) el('iven-modal-label-matched').textContent = 'Совпало';
                if(el('iven-modal-label-nomatch')) el('iven-modal-label-nomatch').textContent = 'Не найдено';
                if(el('iven-modal-label-avgsc')) el('iven-modal-label-avgsc').textContent = 'Средний Score';
                if(el('iven-modal-show-nomatch-label')) el('iven-modal-show-nomatch-label').textContent = 'Показать без совпадения';
            }

            tbody.innerHTML = '';
            matches.forEach(function(m, i){
                var tr = document.createElement('tr');
                var state = ivenRowStates[m.name];
                var bg = state === 'confirmed' ? '#f0fdf4'
                       : state === 'rejected'  ? '#fef2f2'
                       : (i % 2 === 0 ? '#fff' : '#f8fafc');
                tr.style.cssText = 'background:' + bg + ';border-bottom:1px solid #f1f5f9;' +
                    (state === 'rejected' ? 'opacity:0.55;' : '');

                var sc = parseFloat(m.score) || 0;
                var scBadge = '<span style="display:inline-block;padding:2px 7px;border-radius:20px;font-weight:700;font-size:12px;background:' +
                    scoreBg(sc) + ';color:' + scoreColor(sc) + ';">' + (m.score || '—') + '</span>';
                var idCell = m.id
                    ? '<a href="https://catalog.onliner.by/p/' + m.id + '" target="_blank" style="color:#2563eb;text-decoration:none;font-weight:600;font-size:12px;">' + m.id + '</a>'
                    : '—';
                var matchSourceHtml = ivenSourceBadge(m.source);

                var actHtml;
                if(state === 'confirmed'){
                    actHtml = '<span style="color:#16a34a;font-weight:600;font-size:12px;">✓ Сохранено</span>';
                } else if(state === 'rejected'){
                    actHtml = '<span style="color:#dc2626;font-weight:600;font-size:12px;">✗ Удалено</span>';
                } else {
                    actHtml = '<button class="iven-confirm-btn" style="padding:3px 8px;border:1px solid #16a34a;border-radius:5px;background:#f0fdf4;color:#16a34a;font-size:11px;font-weight:600;cursor:pointer;margin-right:4px;" title="Подтвердить — сохранить в ручные привязки">✓</button>' +
                              '<button class="iven-reject-btn"  style="padding:3px 8px;border:1px solid #fca5a5;border-radius:5px;background:#fef2f2;color:#dc2626;font-size:11px;font-weight:600;cursor:pointer;" title="Неверно — удалить ID">✗</button>';
                }

                tr.innerHTML =
                    '<td style="padding:8px 14px;color:#1f2937;line-height:1.4;font-size:12px;">' + (m.name || '') + '</td>' +
                    '<td style="padding:8px 14px;color:#374151;line-height:1.4;font-size:12px;">' + (m.matched_name || '') + matchSourceHtml + '</td>' +
                    '<td style="padding:8px;text-align:center;">' + scBadge + '</td>' +
                    '<td style="padding:8px;text-align:center;">' + idCell + '</td>' +
                    '<td class="iven-act-cell" style="padding:6px 8px;text-align:center;white-space:nowrap;">' + actHtml + '</td>';

                // Wire up buttons after DOM insertion
                tbody.appendChild(tr);
                var confirmBtn = tr.querySelector('.iven-confirm-btn');
                var rejectBtn  = tr.querySelector('.iven-reject-btn');
                if(confirmBtn) confirmBtn.addEventListener('click', (function(row, match){ return function(){ ivenConfirmRow(row, match); }; })(tr, m));
                if(rejectBtn)  rejectBtn.addEventListener('click',  (function(row, match){ return function(){ ivenRejectRow(row, match); }; })(tr, m));
            });

            // Count items with and without candidates
            var withCands   = noMatch.filter(function(m){ return m.candidates && m.candidates.length > 0; });
            var withoutCands= noMatch.filter(function(m){ return !m.candidates || m.candidates.length === 0; });
            var withoutNoCandidates = withoutCands.filter(function(m){ return reportIssueKey(m) === 'no_candidates'; });
            var withoutNoModel = withoutCands.filter(function(m){ return reportIssueKey(m) === 'no_model'; });
            var noCandForRender = (reportMode === 'ssd' || reportMode === 'hdd' || reportMode === 'cooler' || reportMode === 'printer') ? withoutNoCandidates : withoutCands;

            function cpuMetaHtml(item){
                if(reportMode === 'iven') return '';
                var parts = [];
                var brandValue = String((item && (item.cpu_brand || item.board_brand || item.monitor_brand || item.gpu_vendor || item.ram_brand || item.ssd_brand || item.psu_brand || item.case_brand || item.hdd_brand || item.cooler_brand || item.printer_brand)) || '').trim();
                var modelValue = String((item && (item.cpu_model || item.board_model || item.monitor_model || item.gpu_model || item.gpu_sku || item.ram_sku || item.ssd_model || item.ssd_code || item.psu_model || item.psu_code || item.psu_watt || item.case_model || item.case_code || item.hdd_code || item.hdd_capacity || item.cooler_code || item.cooler_tdp || item.printer_model || item.printer_article)) || '').trim();
                var issueValue = String((item && (item.cpu_issue_label || item.board_issue_label || item.monitor_issue_label || item.gpu_issue_label || item.ram_issue_label || item.ssd_issue_label || item.psu_issue_label || item.case_issue_label || item.hdd_issue_label || item.cooler_issue_label || item.printer_issue_label)) || '').trim();
                if(brandValue){
                    parts.push('Производитель: <b>' + escapeHtml(brandValue) + '</b>');
                }
                if(modelValue){
                    parts.push('Модель: <b>' + escapeHtml(modelValue) + '</b>');
                }
                if(issueValue){
                    parts.push('Статус: <b>' + escapeHtml(issueValue) + '</b>');
                }
                if(parts.length <= 0) return '';
                return '<div style="margin-top:4px;font-size:11px;color:#64748b;display:flex;gap:10px;flex-wrap:wrap;">' + parts.join('<span style="color:#cbd5e1;">•</span>') + '</div>';
            }

            if(withCands.length > 0){
                var sepC = document.createElement('tr');
                sepC.innerHTML = '<td colspan="5" style="padding:8px 14px;background:#eff6ff;color:#1d4ed8;font-size:12px;font-weight:700;border-top:2px solid #bfdbfe;">' +
                    (reportMode !== 'iven'
                        ? ('Подбор для ручного подтверждения (' + withCands.length + '):')
                        : ('Ручная модерация — автоподстановка запрещена для score &lt; 1 (' + withCands.length + '):'))
                    + '</td>';
                tbody.appendChild(sepC);
                withCands.forEach(function(m, i){
                    var tr = document.createElement('tr');
                    tr.style.cssText = 'background:' + (i%2===0?'#fff':'#f0f9ff') + ';border-bottom:1px solid #e0f2fe;vertical-align:top;';
                    var bestSourceHtml = m.best_source
                        ? '<div style="margin-bottom:6px;font-size:11px;color:#475569;">Лучший матч из базы: ' + ivenSourceBadge(m.best_source) + '</div>'
                        : '';
                    var candsHtml = '<div style="display:flex;flex-direction:column;gap:4px;">' + bestSourceHtml;
                    (m.candidates || []).forEach(function(c){
                        var scCol = c.score >= 0.75 ? '#16a34a' : c.score >= 0.55 ? '#d97706' : '#9ca3af';
                        var keyState = (m.name||'') + '_' + c.id;
                        var savedState = ivenRowStates[keyState];
                        var candSourceHtml = ivenSourceBadge(c.source || (c.id === m.best_id ? m.best_source : ''));
                        if(savedState === 'confirmed'){
                            candsHtml += '<div style="display:flex;align-items:center;gap:6px;padding:3px 0;">' +
                                '<span style="color:#16a34a;font-weight:700;font-size:11px;">✓ Сохранено</span>' +
                                '<span style="font-size:11px;color:#374151;">' + c.name + '</span>' +
                                candSourceHtml +
                                '<a href="https://catalog.onliner.by/p/' + c.id + '" target="_blank" style="color:#2563eb;font-size:11px;font-weight:600;">' + c.id + '</a>' +
                                '</div>';
                        } else {
                            candsHtml += '<div style="display:flex;align-items:center;gap:6px;padding:3px 0;">' +
                                '<button class="iven-manual-pick" data-name="' + (m.name||'').replace(/"/g,'&quot;') + '" data-row="' + (m.row_idx||'') + '" data-id="' + c.id + '" data-cname="' + c.name.replace(/"/g,'&quot;') + '"' +
                                ' style="padding:2px 8px;border:1px solid #3b82f6;border-radius:4px;background:#eff6ff;color:#1d4ed8;font-size:11px;font-weight:600;cursor:pointer;white-space:nowrap;">Выбрать</button>' +
                                '<span style="color:' + scCol + ';font-weight:700;font-size:11px;min-width:36px;">' + c.score + '</span>' +
                                '<span style="font-size:11px;color:#374151;">' + c.name + '</span>' +
                                candSourceHtml +
                                '<a href="https://catalog.onliner.by/p/' + c.id + '" target="_blank" style="color:#2563eb;font-size:11px;font-weight:600;text-decoration:none;">' + c.id + '</a>' +
                                '</div>';
                        }
                    });
                    var manualRowId = 'iven-manual-id-' + (m.row_idx || i);
                    var manualBtnId = 'iven-manual-btn-' + (m.row_idx || i);
                    var manualNoteId = 'iven-manual-note-' + (m.row_idx || i);
                    var inlineQueryId = 'iven-inline-query-' + (m.row_idx || i);
                    var inlineResultsId = 'iven-inline-results-' + (m.row_idx || i);
                    candsHtml += '<div style="display:flex;align-items:center;gap:6px;padding-top:8px;margin-top:6px;border-top:1px dashed #bfdbfe;flex-wrap:wrap;">' +
                        '<input id="' + manualRowId + '" class="iven-manual-id-input" data-item-name="' + escapeHtml(m.name || '') + '" type="text" inputmode="numeric" placeholder="Вставить Onliner ID вручную" ' +
                        'style="width:210px;padding:4px 8px;border:1px solid #93c5fd;border-radius:4px;font-size:11px;">' +
                        '<button id="' + manualBtnId + '" type="button" style="padding:3px 8px;border:1px solid #2563eb;border-radius:4px;background:#2563eb;color:#fff;font-size:11px;font-weight:600;cursor:pointer;">Сохранить ID</button>' +
                        '<span id="' + manualNoteId + '" style="font-size:11px;color:#64748b;">Сохранится в вечный кеш</span>' +
                        '</div>';
                    candsHtml += '</div>';
                    tr.innerHTML =
                        '<td colspan="2" style="padding:7px 14px;color:#1f2937;font-size:12px;max-width:280px;">' + (m.name || '') + cpuMetaHtml(m) +
                        '<div style="margin-top:6px;padding:6px 7px;border:1px solid #e2e8f0;border-radius:8px;background:#fafafa;">' +
                        '<div style="font-size:10px;font-weight:700;color:#64748b;text-transform:uppercase;letter-spacing:.04em;">Поиск по БД для этой позиции</div>' +
                        '<input id="' + inlineQueryId + '" type="text" value="' + escapeHtml(m.name || '') + '" placeholder="Уточнить запрос..." style="margin-top:4px;width:100%;padding:4px 7px;border:1px solid #cbd5e1;border-radius:6px;font-size:11px;color:#334155;">' +
                        '<div id="' + inlineResultsId + '" style="margin-top:6px;display:none;border:1px solid #fde68a;border-radius:6px;background:#fffbeb;padding:5px 7px;max-height:150px;overflow:auto;"></div>' +
                        '</div>' +
                        '</td>' +
                        '<td colspan="3" style="padding:7px 14px;">' + candsHtml + '</td>';
                    tbody.appendChild(tr);
                    // Wire pick buttons
                    tr.querySelectorAll('.iven-manual-pick').forEach(function(btn){
                        btn.addEventListener('click', function(){
                            var payload = [{name: btn.dataset.name, onliner_id: btn.dataset.id,
                                            url: '', row_idx: btn.dataset.row ? parseInt(btn.dataset.row) : null}];
                            fetch('/api/manual-id-confirm-batch', {
                                method:'POST', headers:{'Content-Type':'application/json'},
                                body: JSON.stringify({source:'iven_manual_pick', items: payload})
                            }).then(function(r){
                                return r.json().catch(function(){ return {}; }).then(function(d){
                                    if(!r.ok || !d || d.status !== 'ok'){
                                        throw new Error((d && d.message) || ('save_failed_' + r.status));
                                    }
                                    return d;
                                });
                            }).then(function(){
                                ivenRowStates[(btn.dataset.name||'') + '_' + btn.dataset.id] = 'confirmed';
                                ivenRowStates[btn.dataset.name||''] = 'confirmed';
                                syncLastStatusAfterManualSave(btn.dataset.name||'', btn.dataset.row ? parseInt(btn.dataset.row) : null, btn.dataset.id||'');
                                btn.closest('div').innerHTML = '<span style="color:#16a34a;font-weight:700;font-size:11px;">✓ Сохранено</span>' +
                                    '<span style="font-size:11px;color:#374151;margin-left:6px;">' + btn.dataset.cname + '</span>' +
                                    '<a href="https://catalog.onliner.by/p/' + btn.dataset.id + '" target="_blank" style="color:#2563eb;font-size:11px;font-weight:600;margin-left:6px;">' + btn.dataset.id + '</a>';
                                if(typeof loadReviewQueue === 'function') loadReviewQueue();
                                if(typeof reloadMainTable === 'function') reloadMainTable();
                            }).catch(function(err){
                                if(manualNote){
                                    manualNote.textContent = 'Ошибка сохранения: ' + String((err && err.message) || 'save_failed');
                                    manualNote.style.color = '#dc2626';
                                }
                            });
                        });
                    });
                    var manualInput = document.getElementById(manualRowId);
                    var manualBtn = document.getElementById(manualBtnId);
                    var manualNote = document.getElementById(manualNoteId);
                    var inlineQueryInput = document.getElementById(inlineQueryId);
                    var inlineResultsEl = document.getElementById(inlineResultsId);
                    if(manualBtn){
                        manualBtn.addEventListener('click', function(){
                            ivenSaveManualId(m.name || '', m.row_idx, manualInput ? manualInput.value : '', manualBtn, manualInput, manualNote);
                        });
                    }
                    if(inlineQueryInput && inlineResultsEl){
                        var timer1 = null;
                        var booted1 = false;
                        inlineQueryInput.addEventListener('focus', function(){
                            if(booted1) return;
                            booted1 = true;
                            renderInlineDbSearch(inlineResultsEl, inlineQueryInput.value || (m.name || ''), manualInput);
                        });
                        inlineQueryInput.addEventListener('input', function(){
                            if(timer1){ clearTimeout(timer1); }
                            timer1 = setTimeout(function(){
                                booted1 = true;
                                var q1 = String(inlineQueryInput.value || '').trim();
                                if(q1.length < 3){
                                    inlineResultsEl.style.display = 'none';
                                    inlineResultsEl.innerHTML = '';
                                    return;
                                }
                                renderInlineDbSearch(inlineResultsEl, q1, manualInput);
                            }, 260);
                        });
                    }
                });
            }

            if(showNoMatch && noCandForRender.length > 0){
                var sep = document.createElement('tr');
                sep.innerHTML = '<td colspan="5" style="padding:8px 14px;background:#fef9c3;color:#92400e;font-size:12px;font-weight:700;border-top:2px solid #fde68a;">' +
                    (reportMode !== 'iven'
                        ? (reportMode === 'ssd'
                            ? ('Проблемные SSD — без кандидатов (' + noCandForRender.length + ')' + (withoutNoModel.length ? (', без модели: ' + withoutNoModel.length) : '') + ':')
                            : (reportMode === 'hdd'
                            ? ('Проблемные HDD — без кандидатов (' + noCandForRender.length + ')' + (withoutNoModel.length ? (', без модели: ' + withoutNoModel.length) : '') + ':')
                            : (reportMode === 'cooler'
                            ? ('Проблемное охлаждение — без кандидатов (' + noCandForRender.length + ')' + (withoutNoModel.length ? (', без модели: ' + withoutNoModel.length) : '') + ':')
                            : (reportMode === 'printer'
                            ? ('Принтеры / МФУ — без кандидатов (' + noCandForRender.length + ')' + (withoutNoModel.length ? (', без модели: ' + withoutNoModel.length) : '') + ':')
                            : ('Проблемные позиции — без модели или без кандидатов (' + noCandForRender.length + ')' + (withoutNoModel.length ? (', без модели: ' + withoutNoModel.length) : '') + ':') ))))
                        : ('Без кандидатов из базы — можно вставить ID вручную (' + withoutCands.length + '):'))
                    + '</td>';
                tbody.appendChild(sep);
                noCandForRender.forEach(function(m, i){
                    var tr = document.createElement('tr');
                    tr.style.cssText = 'background:' + (i%2===0?'#fff':'#fafafa') + ';border-bottom:1px solid #f5f5f5;';
                    var manualRowId = 'iven-nomatch-id-' + (m.row_idx || i);
                    var manualBtnId = 'iven-nomatch-btn-' + (m.row_idx || i);
                    var manualNoteId = 'iven-nomatch-note-' + (m.row_idx || i);
                    var inlineQueryId = 'iven-nomatch-inline-query-' + (m.row_idx || i);
                    var inlineResultsId = 'iven-nomatch-inline-results-' + (m.row_idx || i);
                    tr.innerHTML =
                        '<td colspan="2" style="padding:7px 14px;color:#6b7280;font-size:12px;">' + (m.name || '') + cpuMetaHtml(m) +
                        '<div style="margin-top:6px;padding:6px 7px;border:1px solid #e2e8f0;border-radius:8px;background:#fafafa;">' +
                        '<div style="font-size:10px;font-weight:700;color:#64748b;text-transform:uppercase;letter-spacing:.04em;">Поиск по БД для этой позиции</div>' +
                        '<input id="' + inlineQueryId + '" type="text" value="' + escapeHtml(m.name || '') + '" placeholder="Уточнить запрос..." style="margin-top:4px;width:100%;padding:4px 7px;border:1px solid #cbd5e1;border-radius:6px;font-size:11px;color:#334155;">' +
                        '<div id="' + inlineResultsId + '" style="margin-top:6px;display:none;border:1px solid #fde68a;border-radius:6px;background:#fffbeb;padding:5px 7px;max-height:150px;overflow:auto;"></div>' +
                        '</div>' +
                        '</td>' +
                        '<td style="padding:7px;text-align:center;color:#9ca3af;font-size:12px;">—</td>' +
                        '<td style="padding:7px;text-align:center;color:#9ca3af;font-size:12px;">—</td>' +
                        '<td style="padding:7px;">' +
                            '<div style="display:flex;flex-direction:column;gap:6px;align-items:flex-start;">' +
                                '<div style="display:flex;gap:6px;align-items:center;flex-wrap:wrap;">' +
                                    '<input id="' + manualRowId + '" class="iven-manual-id-input" data-item-name="' + escapeHtml(m.name || '') + '" type="text" inputmode="numeric" placeholder="Onliner ID вручную" style="width:170px;padding:4px 8px;border:1px solid #d6d3d1;border-radius:4px;font-size:11px;">' +
                                    '<button id="' + manualBtnId + '" type="button" style="padding:3px 8px;border:1px solid #a16207;border-radius:4px;background:#f59e0b;color:#fff;font-size:11px;font-weight:600;cursor:pointer;">Сохранить ID</button>' +
                                '</div>' +
                                '<span id="' + manualNoteId + '" style="font-size:11px;color:#78716c;">Сохранится в вечный кеш</span>' +
                            '</div>' +
                        '</td>';
                    tbody.appendChild(tr);
                    var manualInput = document.getElementById(manualRowId);
                    var manualBtn = document.getElementById(manualBtnId);
                    var manualNote = document.getElementById(manualNoteId);
                    var inlineQueryInput = document.getElementById(inlineQueryId);
                    var inlineResultsEl = document.getElementById(inlineResultsId);
                    if(manualBtn){
                        manualBtn.addEventListener('click', function(){
                            ivenSaveManualId(m.name || '', m.row_idx, manualInput ? manualInput.value : '', manualBtn, manualInput, manualNote);
                        });
                    }
                    if(inlineQueryInput && inlineResultsEl){
                        var timer2 = null;
                        var booted2 = false;
                        inlineQueryInput.addEventListener('focus', function(){
                            if(booted2) return;
                            booted2 = true;
                            renderInlineDbSearch(inlineResultsEl, inlineQueryInput.value || (m.name || ''), manualInput);
                        });
                        inlineQueryInput.addEventListener('input', function(){
                            if(timer2){ clearTimeout(timer2); }
                            timer2 = setTimeout(function(){
                                booted2 = true;
                                var q2 = String(inlineQueryInput.value || '').trim();
                                if(q2.length < 3){
                                    inlineResultsEl.style.display = 'none';
                                    inlineResultsEl.innerHTML = '';
                                    return;
                                }
                                renderInlineDbSearch(inlineResultsEl, q2, manualInput);
                            }, 260);
                        });
                    }
                });
            }

            modal.style.display = 'block';
            document.body.style.overflow = 'hidden';
        }

        function closeIvenModal(){
            var modal = document.getElementById('iven-report-modal');
            if(modal) modal.style.display = 'none';
            document.body.style.overflow = '';
        }

        function confirmDangerAction(message, keyword){
            if(!window.confirm(message)){
                return false;
            }
            var expected = String(keyword || 'ОЧИСТИТЬ');
            var typed = window.prompt('Подтверждение: введите "' + expected + '"');
            if(String(typed || '').trim().toUpperCase() !== expected.toUpperCase()){
                if(ivenMsg){
                    ivenMsg.textContent = 'Действие отменено: подтверждение не введено.';
                    ivenMsg.style.color = '#b45309';
                }
                return false;
            }
            return true;
        }

        function clearNonPcIdsForRematch(){
            if(!confirmDangerAction(
                'Опасное действие: очистить все OnlinerID у товаров N-Tech, кроме ПЭВМ? Это массовое изменение.',
                'ОЧИСТИТЬ'
            )){
                return;
            }
            if(clearNonPcBtn){ clearNonPcBtn.disabled = true; clearNonPcBtn.textContent = 'Очищаю N-Tech...'; }
            if(ivenMsg){ ivenMsg.textContent = 'Очищаю ID у N-Tech товаров кроме ПЭВМ...'; ivenMsg.style.color = '#dc2626'; }
            fetch('/api/clear-all-nonpc-onliner-ids', {method:'POST'})
            .then(function(r){ return r.json(); })
            .then(function(d){
                if(!d || d.status !== 'ok'){
                    throw new Error((d && d.message) || 'clear_nonpc_failed');
                }
                if(ivenReportBtn){ ivenReportBtn.style.display = 'none'; }
                if(ivenWrap){ ivenWrap.style.display = 'none'; }
                if(ivenBar){ ivenBar.style.width = '0%'; }
                if(ivenMsg){
                    ivenMsg.textContent = d.message || ('Очищено ID: ' + String(d.cleared || 0));
                    ivenMsg.style.color = '#16a34a';
                }
                fetch('/api/stats?_ts=' + Date.now(), {cache:'no-store'})
                .then(function(r){ return r.json(); })
                .then(function(s){
                    var withoutIdEl = document.getElementById('without-id-count');
                    if(withoutIdEl && s && s.without_id !== undefined){
                        withoutIdEl.textContent = s.without_id;
                    }
                    if(s && s.duplicate_id_rows !== undefined){
                        setDuplicateIdCounterValue(s.duplicate_id_rows);
                    }
                    if(typeof reloadMainTable === 'function'){
                        reloadMainTable();
                    }
                }).catch(function(){});
            }).catch(function(err){
                if(ivenMsg){
                    ivenMsg.textContent = 'Ошибка очистки: ' + String((err && err.message) || err || 'unknown');
                    ivenMsg.style.color = '#dc2626';
                }
            }).finally(function(){
                if(clearNonPcBtn){ clearNonPcBtn.disabled = false; clearNonPcBtn.textContent = 'Очистить ID N-Tech кроме ПЭВМ'; }
            });
        }

        function clearDuplicateIdsForNtech(){
            if(!confirmDangerAction(
                'Опасное действие: очистить все дублирующиеся OnlinerID только у товаров N-Tech? ID будет снят у всех строк-дублей N-Tech, включая минимальную цену.',
                'ОЧИСТИТЬ'
            )){
                return;
            }
            if(clearDuplicateIdsBtn){ clearDuplicateIdsBtn.disabled = true; clearDuplicateIdsBtn.textContent = 'Очищаю дубли...'; }
            if(ivenMsg){ ivenMsg.textContent = 'Очищаю дублирующиеся ID у N-Tech...'; ivenMsg.style.color = '#b91c1c'; }
            fetch('/api/clear-ntech-duplicate-onliner-ids', {method:'POST'})
            .then(function(r){ return r.json(); })
            .then(function(d){
                if(!d || d.status !== 'ok'){
                    throw new Error((d && d.message) || 'clear_ntech_duplicates_failed');
                }
                if(ivenMsg){
                    ivenMsg.textContent = d.message || ('Очищено строк N-Tech: ' + String(d.cleared || 0));
                    ivenMsg.style.color = '#16a34a';
                }
                fetch('/api/stats?_ts=' + Date.now(), {cache:'no-store'})
                .then(function(r){ return r.json(); })
                .then(function(s){
                    var withoutIdEl = document.getElementById('without-id-count');
                    if(withoutIdEl && s && s.without_id !== undefined){
                        withoutIdEl.textContent = s.without_id;
                    }
                    if(s && s.duplicate_id_rows !== undefined){
                        setDuplicateIdCounterValue(s.duplicate_id_rows);
                    }
                    if(typeof reloadMainTable === 'function'){
                        reloadMainTable();
                    }
                }).catch(function(){});
            }).catch(function(err){
                if(ivenMsg){
                    ivenMsg.textContent = 'Ошибка очистки дублей N-Tech: ' + String((err && err.message) || err || 'unknown');
                    ivenMsg.style.color = '#dc2626';
                }
            }).finally(function(){
                if(clearDuplicateIdsBtn){ clearDuplicateIdsBtn.disabled = false; clearDuplicateIdsBtn.textContent = 'Очистить дубли ID N-Tech'; }
            });
        }

        function startCpuReviewQueue(){
            var cpuBtn = document.getElementById('cpu-review-btn');
            if(cpuBtn){ cpuBtn.disabled = true; cpuBtn.textContent = 'Собираю CPU...'; }
            if(ivenWrap){ ivenWrap.style.display = 'block'; }
            if(ivenBar){ ivenBar.style.width = '18%'; ivenBar.style.background = 'linear-gradient(90deg,#8b5cf6,#7c3aed)'; }
            if(ivenMsg){ ivenMsg.textContent = 'Ищу процессоры N-Tech по производителю и модели...'; ivenMsg.style.color = '#7c3aed'; }
            showBusyOverlay('Процессоры N-Tech', 'Собираем кандидатов из локальной базы и отправляем в ручную очередь...');
            fetch('/api/cpu-review-queue-start', {method:'POST'})
            .then(function(r){ return r.json(); })
            .then(function(d){
                if(!d || d.status !== 'ok'){
                    throw new Error((d && d.message) || 'cpu_review_failed');
                }
                lastStatus = d;
                if(ivenBar){ ivenBar.style.width = '100%'; ivenBar.style.background = Number(d.queued || 0) > 0 ? 'linear-gradient(90deg,#22c55e,#16a34a)' : 'linear-gradient(90deg,#94a3b8,#64748b)'; }
                if(ivenMsg){
                    ivenMsg.textContent = d.message || ('Процессоров добавлено в очередь: ' + String(d.queued || 0));
                    ivenMsg.style.color = Number(d.queued || 0) > 0 ? '#16a34a' : '#475569';
                }
                var hasData = (d.matches && d.matches.length > 0) || (d.no_match && d.no_match.length > 0);
                if(ivenReportBtn) ivenReportBtn.style.display = hasData ? 'inline-block' : 'none';
                loadReviewQueue();
                var qBtn = document.getElementById('show-review-queue-btn');
                if(qBtn && Number(d.queued || 0) > 0){ qBtn.style.background = '#ede9fe'; }
                if(hasData){
                    setTimeout(function(){ renderIvenModal(lastStatus); }, 120);
                }
            }).catch(function(err){
                if(ivenWrap){ ivenWrap.style.display = 'block'; }
                if(ivenBar){ ivenBar.style.width = '100%'; ivenBar.style.background = 'linear-gradient(90deg,#f87171,#dc2626)'; }
                if(ivenMsg){
                    ivenMsg.textContent = 'Ошибка CPU-подбора: ' + String((err && err.message) || err || 'unknown');
                    ivenMsg.style.color = '#dc2626';
                }
            }).finally(function(){
                hideBusyOverlay();
                if(cpuBtn){ cpuBtn.disabled = false; cpuBtn.textContent = 'Процессоры N-Tech'; }
            });
        }

        function startBoardReviewQueue(){
            var boardBtn = document.getElementById('board-review-btn');
            if(boardBtn){ boardBtn.disabled = true; boardBtn.textContent = 'Собираю платы...'; }
            if(ivenWrap){ ivenWrap.style.display = 'block'; }
            if(ivenBar){ ivenBar.style.width = '18%'; ivenBar.style.background = 'linear-gradient(90deg,#14b8a6,#0f766e)'; }
            if(ivenMsg){ ivenMsg.textContent = 'Ищу материнки N-Tech по бренду и модели...'; ivenMsg.style.color = '#0f766e'; }
            showBusyOverlay('Материнки N-Tech', 'Собираем кандидатов из локальной базы и отправляем в ручную очередь...');
            fetch('/api/motherboard-review-queue-start', {method:'POST'})
            .then(function(r){ return r.json(); })
            .then(function(d){
                if(!d || d.status !== 'ok'){
                    throw new Error((d && d.message) || 'board_review_failed');
                }
                lastStatus = d;
                if(ivenBar){ ivenBar.style.width = '100%'; ivenBar.style.background = Number(d.queued || 0) > 0 ? 'linear-gradient(90deg,#22c55e,#16a34a)' : 'linear-gradient(90deg,#94a3b8,#64748b)'; }
                if(ivenMsg){
                    ivenMsg.textContent = d.message || ('Материнок добавлено в очередь: ' + String(d.queued || 0));
                    ivenMsg.style.color = Number(d.queued || 0) > 0 ? '#16a34a' : '#475569';
                }
                var hasData = (d.matches && d.matches.length > 0) || (d.no_match && d.no_match.length > 0);
                if(ivenReportBtn) ivenReportBtn.style.display = hasData ? 'inline-block' : 'none';
                loadReviewQueue();
                var qBtn = document.getElementById('show-review-queue-btn');
                if(qBtn && Number(d.queued || 0) > 0){ qBtn.style.background = '#ccfbf1'; }
                if(hasData){
                    setTimeout(function(){ renderIvenModal(lastStatus); }, 120);
                }
            }).catch(function(err){
                if(ivenWrap){ ivenWrap.style.display = 'block'; }
                if(ivenBar){ ivenBar.style.width = '100%'; ivenBar.style.background = 'linear-gradient(90deg,#f87171,#dc2626)'; }
                if(ivenMsg){
                    ivenMsg.textContent = 'Ошибка подбора материнок: ' + String((err && err.message) || err || 'unknown');
                    ivenMsg.style.color = '#dc2626';
                }
            }).finally(function(){
                hideBusyOverlay();
                if(boardBtn){ boardBtn.disabled = false; boardBtn.textContent = 'Материнки N-Tech'; }
            });
        }

        function startMonitorReviewQueue(){
            var monitorBtn = document.getElementById('monitor-review-btn');
            if(monitorBtn){ monitorBtn.disabled = true; monitorBtn.textContent = 'Собираю мониторы...'; }
            if(ivenWrap){ ivenWrap.style.display = 'block'; }
            if(ivenBar){ ivenBar.style.width = '18%'; ivenBar.style.background = 'linear-gradient(90deg,#f97316,#c2410c)'; }
            if(ivenMsg){ ivenMsg.textContent = 'Ищу мониторы N-Tech по бренду и модели...'; ivenMsg.style.color = '#c2410c'; }
            showBusyOverlay('Мониторы N-Tech', 'Собираем кандидатов из локальной базы и отправляем в ручную очередь...');
            fetch('/api/monitor-review-queue-start', {method:'POST'})
            .then(function(r){ return r.json(); })
            .then(function(d){
                if(!d || d.status !== 'ok'){
                    throw new Error((d && d.message) || 'monitor_review_failed');
                }
                lastStatus = d;
                if(ivenBar){ ivenBar.style.width = '100%'; ivenBar.style.background = Number(d.queued || 0) > 0 ? 'linear-gradient(90deg,#22c55e,#16a34a)' : 'linear-gradient(90deg,#94a3b8,#64748b)'; }
                if(ivenMsg){
                    ivenMsg.textContent = d.message || ('Мониторов добавлено в очередь: ' + String(d.queued || 0));
                    ivenMsg.style.color = Number(d.queued || 0) > 0 ? '#16a34a' : '#475569';
                }
                var hasData = (d.matches && d.matches.length > 0) || (d.no_match && d.no_match.length > 0);
                if(ivenReportBtn) ivenReportBtn.style.display = hasData ? 'inline-block' : 'none';
                loadReviewQueue();
                var qBtn = document.getElementById('show-review-queue-btn');
                if(qBtn && Number(d.queued || 0) > 0){ qBtn.style.background = '#ffedd5'; }
                if(hasData){
                    setTimeout(function(){ renderIvenModal(lastStatus); }, 120);
                }
            }).catch(function(err){
                if(ivenWrap){ ivenWrap.style.display = 'block'; }
                if(ivenBar){ ivenBar.style.width = '100%'; ivenBar.style.background = 'linear-gradient(90deg,#f87171,#dc2626)'; }
                if(ivenMsg){
                    ivenMsg.textContent = 'Ошибка подбора мониторов: ' + String((err && err.message) || err || 'unknown');
                    ivenMsg.style.color = '#dc2626';
                }
            }).finally(function(){
                hideBusyOverlay();
                if(monitorBtn){ monitorBtn.disabled = false; monitorBtn.textContent = 'Мониторы N-Tech'; }
            });
        }

        function startGpuReviewQueue(){
            var gpuBtn = document.getElementById('gpu-review-btn');
            if(gpuBtn){ gpuBtn.disabled = true; gpuBtn.textContent = 'Собираю GPU...'; }
            if(ivenWrap){ ivenWrap.style.display = 'block'; }
            if(ivenBar){ ivenBar.style.width = '18%'; ivenBar.style.background = 'linear-gradient(90deg,#ec4899,#be185d)'; }
            if(ivenMsg){ ivenMsg.textContent = 'Ищу видеокарты N-Tech по бренду, GPU и серии...'; ivenMsg.style.color = '#be185d'; }
            showBusyOverlay('Видеокарты N-Tech', 'Собираем кандидатов из локальной базы и отправляем в ручную очередь...');
            fetch('/api/gpu-review-queue-start', {method:'POST'})
            .then(function(r){ return r.json(); })
            .then(function(d){
                if(!d || d.status !== 'ok'){
                    throw new Error((d && d.message) || 'gpu_review_failed');
                }
                lastStatus = d;
                if(ivenBar){ ivenBar.style.width = '100%'; ivenBar.style.background = Number(d.queued || 0) > 0 ? 'linear-gradient(90deg,#22c55e,#16a34a)' : 'linear-gradient(90deg,#94a3b8,#64748b)'; }
                if(ivenMsg){
                    ivenMsg.textContent = d.message || ('Видеокарт добавлено в очередь: ' + String(d.queued || 0));
                    ivenMsg.style.color = Number(d.queued || 0) > 0 ? '#16a34a' : '#475569';
                }
                var hasData = (d.matches && d.matches.length > 0) || (d.no_match && d.no_match.length > 0);
                if(ivenReportBtn) ivenReportBtn.style.display = hasData ? 'inline-block' : 'none';
                loadReviewQueue();
                var qBtn = document.getElementById('show-review-queue-btn');
                if(qBtn && Number(d.queued || 0) > 0){ qBtn.style.background = '#fce7f3'; }
                if(hasData){
                    setTimeout(function(){ renderIvenModal(lastStatus); }, 120);
                }
            }).catch(function(err){
                if(ivenWrap){ ivenWrap.style.display = 'block'; }
                if(ivenBar){ ivenBar.style.width = '100%'; ivenBar.style.background = 'linear-gradient(90deg,#f87171,#dc2626)'; }
                if(ivenMsg){
                    ivenMsg.textContent = 'Ошибка подбора видеокарт: ' + String((err && err.message) || err || 'unknown');
                    ivenMsg.style.color = '#dc2626';
                }
            }).finally(function(){
                hideBusyOverlay();
                if(gpuBtn){ gpuBtn.disabled = false; gpuBtn.textContent = 'Видеокарты N-Tech'; }
            });
        }

        function startRamReviewQueue(){
            var ramBtn = document.getElementById('ram-review-btn');
            if(ramBtn){ ramBtn.disabled = true; ramBtn.textContent = 'Собираю RAM...'; }
            if(ivenWrap){ ivenWrap.style.display = 'block'; }
            if(ivenBar){ ivenBar.style.width = '18%'; ivenBar.style.background = 'linear-gradient(90deg,#0ea5e9,#0369a1)'; }
            if(ivenMsg){ ivenMsg.textContent = 'Ищу оперативную память N-Tech по коду и модели...'; ivenMsg.style.color = '#0369a1'; }
            showBusyOverlay('Оперативка N-Tech', 'Собираем кандидатов из локальной базы и отправляем в ручную очередь...');
            fetch('/api/ram-review-queue-start', {method:'POST'})
            .then(function(r){
                var contentType = String((r && r.headers && r.headers.get('content-type')) || '').toLowerCase();
                if(!contentType.includes('application/json')){
                    return r.text().then(function(text){
                        var snippet = String(text || '').replace(/\\s+/g, ' ').trim().slice(0, 180);
                        throw new Error('Сервер вернул не JSON' + (snippet ? ': ' + snippet : ''));
                    });
                }
                return r.json().then(function(d){
                    if(!r.ok){
                        throw new Error((d && d.message) || ('HTTP ' + String(r.status || '500')));
                    }
                    return d;
                });
            })
            .then(function(d){
                if(!d || d.status !== 'ok'){
                    throw new Error((d && d.message) || 'ram_review_failed');
                }
                lastStatus = d;
                if(ivenBar){ ivenBar.style.width = '100%'; ivenBar.style.background = Number(d.queued || 0) > 0 ? 'linear-gradient(90deg,#22c55e,#16a34a)' : 'linear-gradient(90deg,#94a3b8,#64748b)'; }
                if(ivenMsg){
                    ivenMsg.textContent = d.message || ('Оперативки добавлено в очередь: ' + String(d.queued || 0));
                    ivenMsg.style.color = Number(d.queued || 0) > 0 ? '#16a34a' : '#475569';
                }
                var hasData = (d.matches && d.matches.length > 0) || (d.no_match && d.no_match.length > 0);
                if(ivenReportBtn) ivenReportBtn.style.display = hasData ? 'inline-block' : 'none';
                loadReviewQueue();
                var qBtn = document.getElementById('show-review-queue-btn');
                if(qBtn && Number(d.queued || 0) > 0){ qBtn.style.background = '#e0f2fe'; }
                if(hasData){
                    setTimeout(function(){ renderIvenModal(lastStatus); }, 120);
                }
            }).catch(function(err){
                if(ivenWrap){ ivenWrap.style.display = 'block'; }
                if(ivenBar){ ivenBar.style.width = '100%'; ivenBar.style.background = 'linear-gradient(90deg,#f87171,#dc2626)'; }
                if(ivenMsg){
                    ivenMsg.textContent = 'Ошибка подбора RAM: ' + String((err && err.message) || err || 'unknown');
                    ivenMsg.style.color = '#dc2626';
                }
            }).finally(function(){
                hideBusyOverlay();
                if(ramBtn){ ramBtn.disabled = false; ramBtn.textContent = 'Оперативка N-Tech'; }
            });
        }

        function startSsdReviewQueue(){
            var ssdBtn = document.getElementById('ssd-review-btn');
            if(ssdBtn){ ssdBtn.disabled = true; ssdBtn.textContent = 'Собираю SSD...'; }
            if(ivenWrap){ ivenWrap.style.display = 'block'; }
            if(ivenBar){ ivenBar.style.width = '18%'; ivenBar.style.background = 'linear-gradient(90deg,#14b8a6,#0f766e)'; }
            if(ivenMsg){ ivenMsg.textContent = 'Ищу SSD N-Tech по точному коду модели в скобках...'; ivenMsg.style.color = '#0f766e'; }
            showBusyOverlay('SSD N-Tech', 'Собираем кандидатов из локальной базы. Ключ матчинга: точный код модели в скобках.');
            fetch('/api/ssd-review-queue-start', {method:'POST'})
            .then(function(r){ return r.json(); })
            .then(function(d){
                if(!d || d.status !== 'ok'){
                    throw new Error((d && d.message) || 'ssd_review_failed');
                }
                lastStatus = d;
                if(ivenBar){ ivenBar.style.width = '100%'; ivenBar.style.background = Number(d.queued || 0) > 0 ? 'linear-gradient(90deg,#22c55e,#16a34a)' : 'linear-gradient(90deg,#94a3b8,#64748b)'; }
                if(ivenMsg){
                    ivenMsg.textContent = d.message || ('SSD добавлено в очередь: ' + String(d.queued || 0));
                    ivenMsg.style.color = Number(d.queued || 0) > 0 ? '#16a34a' : '#475569';
                }
                var hasData = (d.matches && d.matches.length > 0) || (d.no_match && d.no_match.length > 0);
                if(ivenReportBtn) ivenReportBtn.style.display = hasData ? 'inline-block' : 'none';
                loadReviewQueue();
                var qBtn = document.getElementById('show-review-queue-btn');
                if(qBtn && Number(d.queued || 0) > 0){ qBtn.style.background = '#ccfbf1'; }
                if(hasData){
                    setTimeout(function(){ renderIvenModal(lastStatus); }, 120);
                }
            }).catch(function(err){
                if(ivenWrap){ ivenWrap.style.display = 'block'; }
                if(ivenBar){ ivenBar.style.width = '100%'; ivenBar.style.background = 'linear-gradient(90deg,#f87171,#dc2626)'; }
                if(ivenMsg){
                    ivenMsg.textContent = 'Ошибка подбора SSD: ' + String((err && err.message) || err || 'unknown');
                    ivenMsg.style.color = '#dc2626';
                }
            }).finally(function(){
                hideBusyOverlay();
                if(ssdBtn){ ssdBtn.disabled = false; ssdBtn.textContent = 'SSD N-Tech'; }
            });
        }

        function startPsuReviewQueue(){
            var psuBtn = document.getElementById('psu-review-btn');
            if(psuBtn){ psuBtn.disabled = true; psuBtn.textContent = 'Собираю БП...'; }
            if(ivenWrap){ ivenWrap.style.display = 'block'; }
            if(ivenBar){ ivenBar.style.width = '18%'; ivenBar.style.background = 'linear-gradient(90deg,#10b981,#047857)'; }
            if(ivenMsg){ ivenMsg.textContent = 'Ищу блоки питания N-Tech по бренду, мощности и серии...'; ivenMsg.style.color = '#047857'; }
            showBusyOverlay('Блоки питания N-Tech', 'Собираем кандидатов из локальной базы и отправляем в ручную очередь...');
            fetch('/api/psu-review-queue-start', {method:'POST'})
            .then(function(r){ return r.json(); })
            .then(function(d){
                if(!d || d.status !== 'ok'){
                    throw new Error((d && d.message) || 'psu_review_failed');
                }
                lastStatus = d;
                if(ivenBar){ ivenBar.style.width = '100%'; ivenBar.style.background = Number(d.queued || 0) > 0 ? 'linear-gradient(90deg,#22c55e,#16a34a)' : 'linear-gradient(90deg,#94a3b8,#64748b)'; }
                if(ivenMsg){
                    ivenMsg.textContent = d.message || ('Блоков питания добавлено в очередь: ' + String(d.queued || 0));
                    ivenMsg.style.color = Number(d.queued || 0) > 0 ? '#16a34a' : '#475569';
                }
                var hasData = (d.matches && d.matches.length > 0) || (d.no_match && d.no_match.length > 0);
                if(ivenReportBtn) ivenReportBtn.style.display = hasData ? 'inline-block' : 'none';
                loadReviewQueue();
                var qBtn = document.getElementById('show-review-queue-btn');
                if(qBtn && Number(d.queued || 0) > 0){ qBtn.style.background = '#d1fae5'; }
                if(hasData){
                    setTimeout(function(){ renderIvenModal(lastStatus); }, 120);
                }
            }).catch(function(err){
                if(ivenWrap){ ivenWrap.style.display = 'block'; }
                if(ivenBar){ ivenBar.style.width = '100%'; ivenBar.style.background = 'linear-gradient(90deg,#f87171,#dc2626)'; }
                if(ivenMsg){
                    ivenMsg.textContent = 'Ошибка подбора БП: ' + String((err && err.message) || err || 'unknown');
                    ivenMsg.style.color = '#dc2626';
                }
            }).finally(function(){
                hideBusyOverlay();
                if(psuBtn){ psuBtn.disabled = false; psuBtn.textContent = 'Блоки питания N-Tech'; }
            });
        }

        function startCaseReviewQueue(){
            var caseBtn = document.getElementById('case-review-btn');
            if(caseBtn){ caseBtn.disabled = true; caseBtn.textContent = 'Собираю корпуса...'; }
            if(ivenWrap){ ivenWrap.style.display = 'block'; }
            if(ivenBar){ ivenBar.style.width = '18%'; ivenBar.style.background = 'linear-gradient(90deg,#f59e0b,#b45309)'; }
            if(ivenMsg){ ivenMsg.textContent = 'Ищу корпуса N-Tech по бренду, серии и коду...'; ivenMsg.style.color = '#b45309'; }
            showBusyOverlay('Корпуса N-Tech', 'Собираем кандидатов из локальной базы и отправляем в ручную очередь...');
            fetch('/api/case-review-queue-start', {method:'POST'})
            .then(function(r){ return r.json(); })
            .then(function(d){
                if(!d || d.status !== 'ok'){
                    throw new Error((d && d.message) || 'case_review_failed');
                }
                lastStatus = d;
                if(ivenBar){ ivenBar.style.width = '100%'; ivenBar.style.background = Number(d.queued || 0) > 0 ? 'linear-gradient(90deg,#22c55e,#16a34a)' : 'linear-gradient(90deg,#94a3b8,#64748b)'; }
                if(ivenMsg){
                    ivenMsg.textContent = d.message || ('Корпусов добавлено в очередь: ' + String(d.queued || 0));
                    ivenMsg.style.color = Number(d.queued || 0) > 0 ? '#16a34a' : '#475569';
                }
                var hasData = (d.matches && d.matches.length > 0) || (d.no_match && d.no_match.length > 0);
                if(ivenReportBtn) ivenReportBtn.style.display = hasData ? 'inline-block' : 'none';
                loadReviewQueue();
                var qBtn = document.getElementById('show-review-queue-btn');
                if(qBtn && Number(d.queued || 0) > 0){ qBtn.style.background = '#fef3c7'; }
                if(hasData){
                    setTimeout(function(){ renderIvenModal(lastStatus); }, 120);
                }
            }).catch(function(err){
                if(ivenWrap){ ivenWrap.style.display = 'block'; }
                if(ivenBar){ ivenBar.style.width = '100%'; ivenBar.style.background = 'linear-gradient(90deg,#f87171,#dc2626)'; }
                if(ivenMsg){
                    ivenMsg.textContent = 'Ошибка подбора корпусов: ' + String((err && err.message) || err || 'unknown');
                    ivenMsg.style.color = '#dc2626';
                }
            }).finally(function(){
                hideBusyOverlay();
                if(caseBtn){ caseBtn.disabled = false; caseBtn.textContent = 'Корпуса N-Tech'; }
            });
        }

        function startHddReviewQueue(){
            var hddBtn = document.getElementById('hdd-review-btn');
            if(hddBtn){ hddBtn.disabled = true; hddBtn.textContent = 'Собираю HDD...'; }
            if(ivenWrap){ ivenWrap.style.display = 'block'; }
            if(ivenBar){ ivenBar.style.width = '18%'; ivenBar.style.background = 'linear-gradient(90deg,#0e7490,#155e75)'; }
            if(ivenMsg){ ivenMsg.textContent = 'Ищу HDD N-Tech по артикулу в скобках и бренду...'; ivenMsg.style.color = '#155e75'; }
            showBusyOverlay('HDD N-Tech', 'Собираем кандидатов из локальной базы (внутренние и внешние диски)...');
            fetch('/api/hdd-review-queue-start', {method:'POST'})
            .then(function(r){ return r.json(); })
            .then(function(d){
                if(!d || d.status !== 'ok'){
                    throw new Error((d && d.message) || 'hdd_review_failed');
                }
                lastStatus = d;
                if(ivenBar){ ivenBar.style.width = '100%'; ivenBar.style.background = Number(d.queued || 0) > 0 ? 'linear-gradient(90deg,#22c55e,#16a34a)' : 'linear-gradient(90deg,#94a3b8,#64748b)'; }
                if(ivenMsg){
                    ivenMsg.textContent = d.message || ('HDD добавлено в очередь: ' + String(d.queued || 0));
                    ivenMsg.style.color = Number(d.queued || 0) > 0 ? '#16a34a' : '#475569';
                }
                var hasData = (d.matches && d.matches.length > 0) || (d.no_match && d.no_match.length > 0);
                if(ivenReportBtn) ivenReportBtn.style.display = hasData ? 'inline-block' : 'none';
                loadReviewQueue();
                var qBtn = document.getElementById('show-review-queue-btn');
                if(qBtn && Number(d.queued || 0) > 0){ qBtn.style.background = '#cffafe'; }
                if(hasData){
                    setTimeout(function(){ renderIvenModal(lastStatus); }, 120);
                }
            }).catch(function(err){
                if(ivenWrap){ ivenWrap.style.display = 'block'; }
                if(ivenBar){ ivenBar.style.width = '100%'; ivenBar.style.background = 'linear-gradient(90deg,#f87171,#dc2626)'; }
                if(ivenMsg){
                    ivenMsg.textContent = 'Ошибка подбора HDD: ' + String((err && err.message) || err || 'unknown');
                    ivenMsg.style.color = '#dc2626';
                }
            }).finally(function(){
                hideBusyOverlay();
                if(hddBtn){ hddBtn.disabled = false; hddBtn.textContent = 'HDD N-Tech'; }
            });
        }

        function startCoolerReviewQueue(){
            var cBtn = document.getElementById('cooler-review-btn');
            if(cBtn){ cBtn.disabled = true; cBtn.textContent = 'Собираю охлаждение...'; }
            if(ivenWrap){ ivenWrap.style.display = 'block'; }
            if(ivenBar){ ivenBar.style.width = '18%'; ivenBar.style.background = 'linear-gradient(90deg,#9a3412,#7c2d12)'; }
            if(ivenMsg){ ivenMsg.textContent = 'Ищу охлаждение N-Tech (кулер / СЖО) по бренду и коду...'; ivenMsg.style.color = '#7c2d12'; }
            showBusyOverlay('Охлаждение N-Tech', 'Собираем кандидатов из локальной базы...');
            fetch('/api/cooler-review-queue-start', {method:'POST'})
            .then(function(r){ return r.json(); })
            .then(function(d){
                if(!d || d.status !== 'ok'){
                    throw new Error((d && d.message) || 'cooler_review_failed');
                }
                lastStatus = d;
                if(ivenBar){ ivenBar.style.width = '100%'; ivenBar.style.background = Number(d.queued || 0) > 0 ? 'linear-gradient(90deg,#22c55e,#16a34a)' : 'linear-gradient(90deg,#94a3b8,#64748b)'; }
                if(ivenMsg){
                    ivenMsg.textContent = d.message || ('Позиций охлаждения в очереди: ' + String(d.queued || 0));
                    ivenMsg.style.color = Number(d.queued || 0) > 0 ? '#16a34a' : '#475569';
                }
                var hasData = (d.matches && d.matches.length > 0) || (d.no_match && d.no_match.length > 0);
                if(ivenReportBtn) ivenReportBtn.style.display = hasData ? 'inline-block' : 'none';
                loadReviewQueue();
                var qBtn = document.getElementById('show-review-queue-btn');
                if(qBtn && Number(d.queued || 0) > 0){ qBtn.style.background = '#ffedd5'; }
                if(hasData){
                    setTimeout(function(){ renderIvenModal(lastStatus); }, 120);
                }
            }).catch(function(err){
                if(ivenWrap){ ivenWrap.style.display = 'block'; }
                if(ivenBar){ ivenBar.style.width = '100%'; ivenBar.style.background = 'linear-gradient(90deg,#f87171,#dc2626)'; }
                if(ivenMsg){
                    ivenMsg.textContent = 'Ошибка подбора охлаждения: ' + String((err && err.message) || err || 'unknown');
                    ivenMsg.style.color = '#dc2626';
                }
            }).finally(function(){
                hideBusyOverlay();
                if(cBtn){ cBtn.disabled = false; cBtn.textContent = 'Охлаждение N-Tech'; }
            });
        }

        function startPrinterReviewQueue(){
            var pBtn = document.getElementById('printer-review-btn');
            if(pBtn){ pBtn.disabled = true; pBtn.textContent = 'Собираю принтеры / МФУ...'; }
            if(ivenWrap){ ivenWrap.style.display = 'block'; }
            if(ivenBar){ ivenBar.style.width = '18%'; ivenBar.style.background = 'linear-gradient(90deg,#7e22ce,#6b21a8)'; }
            if(ivenMsg){ ivenMsg.textContent = 'Ищу принтеры и МФУ N-Tech по бренду и артикулу...'; ivenMsg.style.color = '#6b21a8'; }
            showBusyOverlay('Принтеры / МФУ N-Tech', 'Собираем кандидатов из локальной базы...');
            fetch('/api/printer-review-queue-start', {method:'POST'})
            .then(function(r){ return r.json(); })
            .then(function(d){
                if(!d || d.status !== 'ok'){
                    throw new Error((d && d.message) || 'printer_review_failed');
                }
                lastStatus = d;
                if(ivenBar){ ivenBar.style.width = '100%'; ivenBar.style.background = Number(d.queued || 0) > 0 ? 'linear-gradient(90deg,#22c55e,#16a34a)' : 'linear-gradient(90deg,#94a3b8,#64748b)'; }
                if(ivenMsg){
                    ivenMsg.textContent = d.message || ('Принтеров / МФУ в очереди: ' + String(d.queued || 0));
                    ivenMsg.style.color = Number(d.queued || 0) > 0 ? '#16a34a' : '#475569';
                }
                var hasData = (d.matches && d.matches.length > 0) || (d.no_match && d.no_match.length > 0);
                if(ivenReportBtn) ivenReportBtn.style.display = hasData ? 'inline-block' : 'none';
                loadReviewQueue();
                var qBtn = document.getElementById('show-review-queue-btn');
                if(qBtn && Number(d.queued || 0) > 0){ qBtn.style.background = '#f3e8ff'; }
                if(hasData){
                    setTimeout(function(){ renderIvenModal(lastStatus); }, 120);
                }
            }).catch(function(err){
                if(ivenWrap){ ivenWrap.style.display = 'block'; }
                if(ivenBar){ ivenBar.style.width = '100%'; ivenBar.style.background = 'linear-gradient(90deg,#f87171,#dc2626)'; }
                if(ivenMsg){
                    ivenMsg.textContent = 'Ошибка подбора принтеров / МФУ: ' + String((err && err.message) || err || 'unknown');
                    ivenMsg.style.color = '#dc2626';
                }
            }).finally(function(){
                hideBusyOverlay();
                if(pBtn){ pBtn.disabled = false; pBtn.textContent = 'Принтеры / МФУ N-Tech'; }
            });
        }

        function startPeripheralReviewQueue(){
            var pBtn = document.getElementById('peripheral-review-btn');
            if(pBtn){ pBtn.disabled = true; pBtn.textContent = 'Собираю периферию...'; }
            if(ivenWrap){ ivenWrap.style.display = 'block'; }
            if(ivenBar){ ivenBar.style.width = '18%'; ivenBar.style.background = 'linear-gradient(90deg,#0f766e,#115e59)'; }
            if(ivenMsg){ ivenMsg.textContent = 'Ищу периферию N-Tech (клавиатуры, мыши, гарнитуры, акустика)...'; ivenMsg.style.color = '#115e59'; }
            showBusyOverlay('Периферия N-Tech', 'Собираем кандидатов из локальной базы...');
            fetch('/api/peripheral-review-queue-start', {method:'POST'})
            .then(function(r){ return r.json(); })
            .then(function(d){
                if(!d || d.status !== 'ok'){
                    throw new Error((d && d.message) || 'peripheral_review_failed');
                }
                lastStatus = d;
                if(ivenBar){ ivenBar.style.width = '100%'; ivenBar.style.background = Number(d.queued || 0) > 0 ? 'linear-gradient(90deg,#22c55e,#16a34a)' : 'linear-gradient(90deg,#94a3b8,#64748b)'; }
                if(ivenMsg){
                    ivenMsg.textContent = d.message || ('Периферии в очереди: ' + String(d.queued || 0));
                    ivenMsg.style.color = Number(d.queued || 0) > 0 ? '#16a34a' : '#475569';
                }
                var hasData = (d.matches && d.matches.length > 0) || (d.no_match && d.no_match.length > 0);
                if(ivenReportBtn) ivenReportBtn.style.display = hasData ? 'inline-block' : 'none';
                loadReviewQueue();
                var qBtn = document.getElementById('show-review-queue-btn');
                if(qBtn && Number(d.queued || 0) > 0){ qBtn.style.background = '#ccfbf1'; }
                if(hasData){
                    setTimeout(function(){ renderIvenModal(lastStatus); }, 120);
                }
            }).catch(function(err){
                if(ivenWrap){ ivenWrap.style.display = 'block'; }
                if(ivenBar){ ivenBar.style.width = '100%'; ivenBar.style.background = 'linear-gradient(90deg,#f87171,#dc2626)'; }
                if(ivenMsg){
                    ivenMsg.textContent = 'Ошибка подбора периферии: ' + String((err && err.message) || err || 'unknown');
                    ivenMsg.style.color = '#dc2626';
                }
            }).finally(function(){
                hideBusyOverlay();
                if(pBtn){ pBtn.disabled = false; pBtn.textContent = 'Периферия N-Tech'; }
            });
        }

        function startIvenBridge(runMode){
            var useB2B = runMode === 'b2b';
            var triggerBtn = useB2B ? b2bBtn : ivenBtn;
            if(triggerBtn && triggerBtn.disabled) return;
            if(ivenBtn){ ivenBtn.disabled = true; }
            if(b2bBtn){ b2bBtn.disabled = true; }
            if(clearNonPcBtn){ clearNonPcBtn.disabled = true; }
            if(ivenBtn){ ivenBtn.textContent = useB2B ? 'Найти в базе' : 'Запускаю...'; }
            if(b2bBtn){ b2bBtn.textContent = useB2B ? 'Запускаю...' : 'B2B без кеша'; }
            if(ivenReportBtn) ivenReportBtn.style.display = 'none';
            if(ivenWrap){ ivenWrap.style.display = 'block'; }
            if(ivenBar){ ivenBar.style.width = '2%'; }
            if(ivenMsg){
                ivenMsg.textContent = useB2B ? 'Готовлю B2B-прогон без вечного кеша...' : 'Готовлю IVEN-бридж...';
                ivenMsg.style.color = '#1d4ed8';
            }
            showBusyOverlay(
                useB2B ? 'B2B без кеша' : 'Поиск в базе',
                useB2B
                    ? 'Сравниваем товары без участия вечного кеша и пытаемся добрать совпадения через B2B...'
                    : 'Сопоставляем N-Tech товары с локальной базой по модели и артикулу...'
            );
            fetch('/api/autofill-iven-bridge', {
                method:'POST',
                headers:{'Content-Type':'application/json'},
                body: JSON.stringify(useB2B ? {ignore_manual_cache:true, prefer_b2b:true} : {})
            })
            .then(function(r){ return r.json(); })
            .then(function(d){
                if(!d || (d.status !== 'started' && d.status !== 'already_running')){
                    throw new Error((d && d.message) || 'iven_bridge_failed');
                }
                clearInterval(ivenTimer);
                ivenTimer = setInterval(pollIvenStatus, 1000);
                pollIvenStatus();
            }).catch(function(e){
                if(ivenBtn){ ivenBtn.disabled = false; ivenBtn.textContent = 'Найти в базе'; }
                if(b2bBtn){ b2bBtn.disabled = false; b2bBtn.textContent = 'B2B без кеша'; }
                if(clearNonPcBtn){ clearNonPcBtn.disabled = false; }
                if(ivenMsg){ ivenMsg.textContent = 'Ошибка: ' + (e.message || e); ivenMsg.style.color = '#dc2626'; }
                hideBusyOverlay();
            });
        }

        // Close buttons
        var mc1 = document.getElementById('iven-report-modal-close');
        var mc2 = document.getElementById('iven-report-modal-close2');
        if(mc1) mc1.addEventListener('click', closeIvenModal);
        if(mc2) mc2.addEventListener('click', closeIvenModal);
        // Click on backdrop closes
        var ivenModalEl = document.getElementById('iven-report-modal');
        if(ivenModalEl){
            ivenModalEl.addEventListener('click', function(e){
                if(e.target === ivenModalEl) closeIvenModal();
            });
        }
        // Escape key
        document.addEventListener('keydown', function(e){
            if(e.key === 'Escape') closeIvenModal();
        });
        // Show-no-match checkbox
        var ivenModalNoMatch = document.getElementById('iven-modal-show-nomatch');
        if(ivenModalNoMatch){
            ivenModalNoMatch.addEventListener('change', function(){ renderIvenModal(lastStatus); });
        }

        function pollIvenStatus(){
            fetch('/api/autofill-iven-status?_ts=' + Date.now(), {cache:'no-store'})
            .then(function(r){ return r.json(); })
            .then(function(st){
                lastStatus = st;
                var pct = parseInt(st.percent || 0);
                if(ivenBar){ ivenBar.style.width = Math.max(2, pct) + '%'; }
                if(ivenMsg){ ivenMsg.textContent = st.message || ''; }
                if(!st.running){
                    clearInterval(ivenTimer); ivenTimer = null;
                    if(ivenBtn){ ivenBtn.disabled = false; ivenBtn.textContent = 'Найти в базе'; }
                    if(b2bBtn){ b2bBtn.disabled = false; b2bBtn.textContent = 'B2B без кеша'; }
                    if(clearNonPcBtn){ clearNonPcBtn.disabled = false; }
                    hideBusyOverlay();
                    if(st.applied > 0){
                        if(ivenMsg){ ivenMsg.style.color = '#16a34a'; }
                        setTimeout(function(){ if(typeof reloadMainTable === 'function') reloadMainTable(); }, 600);
                    }
                    // Показываем кнопку "Отчёт" если есть данные
                    var hasData = (st.matches && st.matches.length > 0) || (st.no_match && st.no_match.length > 0);
                    if(ivenReportBtn) ivenReportBtn.style.display = hasData ? 'inline-block' : 'none';
                    var withoutIdEl = document.getElementById('without-id-count');
                    if(withoutIdEl){ fetch('/api/stats?_ts='+Date.now(),{cache:'no-store'}).then(function(r){return r.json();}).then(function(s){ if(s && s.without_id !== undefined) withoutIdEl.textContent = s.without_id; if(s && s.duplicate_id_rows !== undefined) setDuplicateIdCounterValue(s.duplicate_id_rows); }).catch(function(){}); }
                }
            }).catch(function(){});
        }

        if(ivenBtn){
            ivenBtn.addEventListener('click', function(){
                startIvenBridge('default');
            });
        }

        if(b2bBtn){
            b2bBtn.addEventListener('click', function(){
                startIvenBridge('b2b');
            });
        }

        if(clearNonPcBtn){
            clearNonPcBtn.addEventListener('click', clearNonPcIdsForRematch);
        }
        if(clearDuplicateIdsBtn){
            clearDuplicateIdsBtn.addEventListener('click', clearDuplicateIdsForNtech);
        }
        if(cpuReviewBtn){
            cpuReviewBtn.addEventListener('click', startCpuReviewQueue);
        }
        if(boardReviewBtn){
            boardReviewBtn.addEventListener('click', startBoardReviewQueue);
        }
        if(monitorReviewBtn){
            monitorReviewBtn.addEventListener('click', startMonitorReviewQueue);
        }
        if(gpuReviewBtn){
            gpuReviewBtn.addEventListener('click', startGpuReviewQueue);
        }
        if(ramReviewBtn){
            ramReviewBtn.addEventListener('click', startRamReviewQueue);
        }
        if(ssdReviewBtn){
            ssdReviewBtn.addEventListener('click', startSsdReviewQueue);
        }
        if(psuReviewBtn){
            psuReviewBtn.addEventListener('click', startPsuReviewQueue);
        }
        if(caseReviewBtn){
            caseReviewBtn.addEventListener('click', startCaseReviewQueue);
        }
        if(hddReviewBtn){
            hddReviewBtn.addEventListener('click', startHddReviewQueue);
        }
        if(coolerReviewBtn){
            coolerReviewBtn.addEventListener('click', startCoolerReviewQueue);
        }
        if(printerReviewBtn){
            printerReviewBtn.addEventListener('click', startPrinterReviewQueue);
        }
        if(peripheralReviewBtn){
            peripheralReviewBtn.addEventListener('click', startPeripheralReviewQueue);
        }

        if(ivenReportBtn){
            ivenReportBtn.addEventListener('click', function(){
                renderIvenModal(lastStatus);
            });
        }

        // Загружаем последний статус при открытии страницы
        fetch('/api/autofill-iven-status?_ts=' + Date.now(), {cache:'no-store'})
        .then(function(r){ return r.json(); })
        .then(function(st){
            lastStatus = st;
            var hasData = (st.matches && st.matches.length > 0) || (st.no_match && st.no_match.length > 0);
            if(ivenReportBtn) ivenReportBtn.style.display = hasData ? 'inline-block' : 'none';
            if(st.message && ivenMsg){ ivenMsg.textContent = st.message; }
            if(st.percent && ivenBar){ ivenBar.style.width = st.percent + '%'; ivenWrap.style.display = 'block'; }
        }).catch(function(){});
    })();
    // ─────────────────────────────────────────────────────────────────────────

    var tableEl = document.getElementById('tbl-main');
    if(tableEl){
        tableEl.addEventListener('click', handleNoIdTableClick);
    }
}

function initOnlinerDbWidget(){
    var countEl  = document.getElementById('db-products-count');
    var detailEl = document.getElementById('db-stats-detail');
    var noteEl   = document.getElementById('db-stats-note');
    var rebuildBtn = document.getElementById('db-rebuild-btn');
    var searchInput = document.getElementById('db-search-query');
    var searchBtn = document.getElementById('db-search-btn');
    var searchResults = document.getElementById('db-search-results');

    function loadDbStats(){
        fetch('/api/onliner-db-stats?_ts=' + Date.now(), {cache:'no-store'})
        .then(function(r){ return r.json(); })
        .then(function(s){
            if(countEl) countEl.textContent = (s.total_products || 0).toLocaleString();
            var lines = [];
            lines.push((s.total_names || 0).toLocaleString() + ' вариантов названий');
            var src = s.by_source || {};
            Object.keys(src).slice(0,4).forEach(function(k){
                lines.push(k + ': ' + src[k].toLocaleString());
            });
            if(detailEl) detailEl.innerHTML = lines.join('<br>');
        }).catch(function(){ if(countEl) countEl.textContent = '?'; });
    }

    if(rebuildBtn){
        rebuildBtn.addEventListener('click', function(){
            rebuildBtn.disabled = true;
            rebuildBtn.textContent = 'Обновляю...';
            if(noteEl) noteEl.textContent = 'Пополняю БД из текущего прайса...';
            fetch('/api/onliner-db-rebuild', {method:'POST', headers:{'Content-Type':'application/json'}, body:'{}'})
            .then(function(r){ return r.json(); })
            .then(function(d){
                rebuildBtn.disabled = false;
                rebuildBtn.textContent = 'Обновить БД';
                if(noteEl) noteEl.textContent = d.status === 'ok'
                    ? 'Добавлено: ' + (d.products||0) + ' товаров, ' + (d.names||0) + ' имён.'
                    : 'Ошибка: ' + (d.message || '');
                loadDbStats();
            }).catch(function(){
                rebuildBtn.disabled = false;
                rebuildBtn.textContent = 'Обновить БД';
            });
        });
    }

    // ── Import full Onliner catalog (CSV/XLSX) ──────────────────────────
    var importBtn  = document.getElementById('db-import-catalog-btn');
    var importFile = document.getElementById('db-import-catalog-file');
    var importWrap = document.getElementById('db-import-progress-wrap');
    var importBar  = document.getElementById('db-import-progress-bar');
    var importMsg  = document.getElementById('db-import-msg');
    var importPollTimer = null;

    function stopImportPoll(){ if(importPollTimer){ clearInterval(importPollTimer); importPollTimer = null; } }

    function pollImportStatus(){
        fetch('/api/onliner-db-import-status?_ts='+Date.now(), {cache:'no-store'})
        .then(function(r){ return r.json(); })
        .then(function(st){
            var pct = parseInt(st.percent || 0);
            if(importBar)  importBar.style.width  = Math.max(2, pct) + '%';
            if(importMsg)  importMsg.textContent  = st.message || '';
            if(importWrap) importWrap.style.display = 'block';
            if(!st.running && st.finished_at){
                stopImportPoll();
                if(importBtn){ importBtn.disabled = false; importBtn.textContent = '📥 Загрузить каталог'; }
                importBar && (importBar.style.background = st.message && st.message.indexOf('Ошибка') === 0
                    ? 'linear-gradient(90deg,#f87171,#dc2626)'
                    : 'linear-gradient(90deg,#0891b2,#0e7490)');
                loadDbStats();
            }
        }).catch(function(){});
    }

    if(importBtn && importFile){
        importBtn.addEventListener('click', function(){
            importFile.value = '';
            importFile.click();
        });
        importFile.addEventListener('change', function(){
            var file = importFile.files[0];
            if(!file) return;
            var sizeMb = (file.size / 1024 / 1024).toFixed(1);
            if(file.size > 120 * 1024 * 1024){
                alert('Файл слишком большой (' + sizeMb + ' МБ). Максимум 120 МБ.');
                return;
            }
            importBtn.disabled = true;
            importBtn.textContent = 'Загружаю...';
            if(importWrap) importWrap.style.display = 'block';
            if(importBar)  importBar.style.width = '2%';
            if(importMsg)  importMsg.textContent  = 'Отправляю файл (' + sizeMb + ' МБ)...';

            var fd = new FormData();
            fd.append('file', file);
            fetch('/api/onliner-db-import-csv', {method:'POST', body: fd})
            .then(function(r){ return r.json(); })
            .then(function(d){
                if(d.status !== 'started' && d.status !== 'already_running'){
                    throw new Error(d.message || 'start_failed');
                }
                importBtn.textContent = 'Импортирую...';
                stopImportPoll();
                importPollTimer = setInterval(pollImportStatus, 1000);
                pollImportStatus();
            }).catch(function(e){
                importBtn.disabled = false;
                importBtn.textContent = '📥 Загрузить каталог';
                if(importMsg) importMsg.textContent = 'Ошибка: ' + (e.message || e);
            });
        });
    }

    // ── Google Sheets direct import ─────────────────────────────────────
    var gsheetBtn        = document.getElementById('db-gsheet-btn');

    if(gsheetBtn){
        gsheetBtn.addEventListener('click', function(){
            var cfg = (uploadAppSettings && uploadAppSettings.onliner_db_import) ? uploadAppSettings.onliner_db_import : {};
            var sid = String((cfg && cfg.google_sheet_id) || '').trim();
            var sname = String((cfg && cfg.google_sheet_name) || '').trim() || 'All_Catalog';
            if(!sid){
                alert('Укажи ID Google таблицы в Настройках: "Onliner DB: ID Google таблицы".');
                return;
            }

            gsheetBtn.disabled = true;
            gsheetBtn.textContent = 'Скачиваю...';
            if(importWrap) importWrap.style.display = 'block';
            if(importBar)  importBar.style.width = '2%';
            if(importMsg)  importMsg.textContent  = 'Подключаюсь к Google Sheets…';

            fetch('/api/onliner-db-import-gsheet', {
                method: 'POST',
                headers: {'Content-Type': 'application/json'},
                body: JSON.stringify({sheet_id: sid, sheet_name: sname})
            })
            .then(function(r){ return r.json(); })
            .then(function(d){
                if(d.status !== 'started' && d.status !== 'already_running'){
                    throw new Error(d.message || 'start_failed');
                }
                gsheetBtn.textContent = 'Импортирую...';
                stopImportPoll();
                importPollTimer = setInterval(pollImportStatus, 1200);
                pollImportStatus();
            }).catch(function(e){
                gsheetBtn.disabled = false;
                gsheetBtn.textContent = '▶ Импорт GSheets';
                if(importMsg) importMsg.textContent = 'Ошибка: ' + (e.message || e);
            });
        });
    }

    function renderDbSearchResults(items, query){
        if(!searchResults) return;
        var rows = Array.isArray(items) ? items : [];
        if(!query){
            searchResults.style.display = 'none';
            searchResults.innerHTML = '';
            return;
        }
        searchResults.style.display = 'block';
        if(!rows.length){
            searchResults.innerHTML = '<div style="font-size:12px;color:#92400e;">По запросу <b>' + escapeHtml(query) + '</b> ничего не найдено.</div>';
            return;
        }
        var html = '<div style="font-size:12px;color:#92400e;font-weight:700;margin-bottom:6px;">Найдено: ' + rows.length + '</div>';
        rows.forEach(function(item){
            var oid = escapeHtml(String(item.id || ''));
            var name = escapeHtml(String(item.name || ''));
            var source = escapeHtml(String(item.source || 'db'));
            var url = String(item.url || '').trim();
            html += '<div style="padding:6px 0;border-top:1px dashed #fcd34d;">';
            html += '<div style="display:flex;gap:8px;align-items:flex-start;">';
            html += '<span style="display:inline-flex;align-items:center;gap:4px;background:#fff;color:#92400e;border:1px solid #fcd34d;border-radius:999px;padding:2px 8px;font-size:11px;font-weight:700;white-space:nowrap;">' + oid + '</span>';
            html += '<div style="flex:1;min-width:0;font-size:12px;color:#374151;line-height:1.35;">' + name + '<div style="margin-top:2px;font-size:11px;color:#a16207;">Источник: ' + source + '</div></div>';
            if(url){
                html += '<a href="' + escapeHtml(url) + '" target="_blank" style="font-size:11px;color:#2563eb;text-decoration:none;white-space:nowrap;">Открыть</a>';
            }
            html += '</div></div>';
        });
        searchResults.innerHTML = html;
    }

    function runDbSearch(){
        var query = (searchInput ? searchInput.value : '').trim();
        if(!query){
            renderDbSearchResults([], '');
            return;
        }
        if(searchBtn){ searchBtn.disabled = true; searchBtn.textContent = 'Ищу...'; }
        if(searchResults){
            searchResults.style.display = 'block';
            searchResults.innerHTML = '<div style="font-size:12px;color:#92400e;">Ищу в локальной базе…</div>';
        }
        fetch('/api/onliner-db-search?q=' + encodeURIComponent(query) + '&_ts=' + Date.now(), {cache:'no-store'})
        .then(function(r){ return r.json(); })
        .then(function(data){
            renderDbSearchResults((data && data.items) || [], query);
        }).catch(function(err){
            if(searchResults){
                searchResults.style.display = 'block';
                searchResults.innerHTML = '<div style="font-size:12px;color:#b91c1c;">Ошибка поиска: ' + escapeHtml(String((err && err.message) || err || 'unknown')) + '</div>';
            }
        }).finally(function(){
            if(searchBtn){ searchBtn.disabled = false; searchBtn.textContent = 'Найти в БД'; }
        });
    }

    if(searchBtn){
        searchBtn.addEventListener('click', runDbSearch);
    }
    if(searchInput){
        searchInput.addEventListener('keydown', function(e){
            if(e.key === 'Enter'){
                e.preventDefault();
                runDbSearch();
            }
        });
    }

    // Also re-enable gsheetBtn when import finishes (patch pollImportStatus)
    var _origPollImport = pollImportStatus;
    pollImportStatus = function(){
        fetch('/api/onliner-db-import-status?_ts='+Date.now(), {cache:'no-store'})
        .then(function(r){ return r.json(); })
        .then(function(st){
            var pct = parseInt(st.percent || 0);
            if(importBar)  importBar.style.width  = Math.max(2, pct) + '%';
            if(importMsg)  importMsg.textContent  = st.message || '';
            if(importWrap) importWrap.style.display = 'block';
            if(!st.running && st.finished_at){
                stopImportPoll();
                if(importBtn){ importBtn.disabled = false; importBtn.textContent = '📥 Загрузить каталог'; }
                if(gsheetBtn){ gsheetBtn.disabled = false; gsheetBtn.textContent = '▶ Импорт GSheets'; }
                importBar && (importBar.style.background = st.message && st.message.indexOf('Ошибка') === 0
                    ? 'linear-gradient(90deg,#f87171,#dc2626)'
                    : 'linear-gradient(90deg,#0891b2,#0e7490)');
                loadDbStats();
            }
        }).catch(function(){});
    };

    // Resume poll if import was running before page load
    fetch('/api/onliner-db-import-status?_ts='+Date.now(), {cache:'no-store'})
    .then(function(r){ return r.json(); })
    .then(function(st){
        if(st.running){
            if(importBtn){ importBtn.disabled = true; importBtn.textContent = 'Импортирую...'; }
            if(gsheetBtn){ gsheetBtn.disabled = true; gsheetBtn.textContent = 'Импортирую...'; }
            if(importWrap) importWrap.style.display = 'block';
            if(importBar)  importBar.style.width = Math.max(2, parseInt(st.percent||0)) + '%';
            if(importMsg)  importMsg.textContent  = st.message || '';
            stopImportPoll();
            importPollTimer = setInterval(pollImportStatus, 1200);
        } else if(st.finished_at && st.message){
            if(importWrap) importWrap.style.display = 'block';
            if(importMsg)  importMsg.textContent  = st.message;
            if(importBar)  importBar.style.width  = '100%';
        }
    }).catch(function(){});

    loadDbStats();
}

function initSnapshotFilterUI(){
    function syncSnapshotDetailUI(){
        var wrap = document.getElementById('snapshot-lists');
        if(!wrap){ return; }
        var mode = String(snapshotDetailMode || '').trim();
        wrap.classList.toggle('active', !!mode);
        wrap.querySelectorAll('.snapshot-list').forEach(function(card){
            var kind = String(card.getAttribute('data-kind') || '').trim();
            card.classList.toggle('active', !!mode && kind === mode);
        });
        document.querySelectorAll('.snapshot-mini[data-kind]').forEach(function(card){
            var kind = String(card.getAttribute('data-kind') || '').trim();
            card.classList.toggle('active', !!mode && kind === mode);
        });
    }
    function toggleSnapshotDetail(mode){
        var next = String(mode || '').trim();
        snapshotDetailMode = (snapshotDetailMode === next) ? '' : next;
        syncSnapshotDetailUI();
    }
    function setSnapshotFilter(mode){
        var filters = (window.snapshotDiffData && window.snapshotDiffData.filters) ? window.snapshotDiffData.filters : {};
        snapshotFilterMode = mode || '';
        if(mode === 'new'){
            snapshotFilterNames = Array.isArray(filters.new_names) ? filters.new_names.slice() : [];
        } else if(mode === 'new_without_id'){
            snapshotFilterNames = Array.isArray(filters.new_without_id_names) ? filters.new_without_id_names.slice() : [];
        } else {
            snapshotFilterNames = [];
        }
        showOnlySnapshotRows = !!snapshotFilterNames.length;
        redrawMainTable();
    }
    var btnNew = document.getElementById('show-new-items-btn');
    if(btnNew){
        btnNew.addEventListener('click', function(){ setSnapshotFilter('new'); });
    }
    var btnNewNoId = document.getElementById('show-new-noid-items-btn');
    if(btnNewNoId){
        btnNewNoId.addEventListener('click', function(){ setSnapshotFilter('new_without_id'); });
    }
    var btnClear = document.getElementById('clear-snapshot-filter-btn');
    if(btnClear){
        btnClear.addEventListener('click', function(){ setSnapshotFilter(''); });
    }
    document.querySelectorAll('.snapshot-mini[data-kind]').forEach(function(card){
        card.addEventListener('click', function(){
            toggleSnapshotDetail(card.getAttribute('data-kind'));
        });
    });
    syncSnapshotDetailUI();
}

function getNoIdInlineState(rowIdx){
    var idx = Number(rowIdx || -1);
    if(idx >= 0 && noIdInlinePickerState && Number(noIdInlinePickerState.rowIdx) === idx){
        return noIdInlinePickerState;
    }
    return null;
}

function renderNoIdInlinePicker(row){
    var rowIdx = Number(row[8] || -1);
    var state = getNoIdInlineState(rowIdx);
    if(!state){ return ''; }
    var html = '<div class="noid-inline-picker">';
    html += '<div class="noid-inline-top">';
    html += '<div class="noid-inline-note" style="margin-bottom:0;">' + escapeHtml(state.message || 'Ищу точные совпадения по Onliner...') + '</div>';
    html += '<button type="button" class="noid-inline-close" data-close-row-idx="' + rowIdx + '">Скрыть</button>';
    html += '</div>';
    if(state.loading){
        html += '<div class="noid-inline-note">Загрузка кандидатов...</div>';
    } else if(Array.isArray(state.items) && state.items.length){
        html += '<div class="noid-inline-list">';
        state.items.forEach(function(item){
            var candidateId = String((item && item.id) || '').trim();
            var candidateName = String((item && item.name) || '').trim();
            var candidateUrl = String((item && item.url) || '').trim();
            var scoreNum = Number((item && item.score) || 0);
            var score = isNaN(scoreNum) ? '' : scoreNum.toFixed(3);
            var isApplying = !!(state.applyingId && state.applyingId === candidateId);
            html += '<div class="noid-inline-item">';
            html += '<div class="noid-inline-meta">';
            html += '<div class="noid-inline-title">' + highlightCandidateName(state.queryName || '', candidateName || candidateId || 'Кандидат без названия') + '</div>';
            html += renderCandidateBadges(item || {});
            html += '<div class="noid-inline-sub">ID: ' + escapeHtml(candidateId || '—') + (score ? (' · score ' + escapeHtml(score)) : '') + '</div>';
            html += '</div>';
            html += '<div class="noid-inline-actions">';
            if(candidateUrl){
                html += '<a href="' + escapeHtml(candidateUrl) + '" target="_blank" rel="noopener noreferrer" class="noid-inline-open">Открыть</a>';
            }
            html += '<button type="button" class="noid-inline-apply" data-row-idx="' + rowIdx + '" data-oid="' + escapeHtml(candidateId) + '" data-url="' + escapeHtml(candidateUrl) + '"' + (isApplying ? ' disabled' : '') + '>' + (isApplying ? 'Сохраняю...' : 'Подставить ID') + '</button>';
            html += '</div>';
            html += '</div>';
        });
        html += '</div>';
    } else {
        html += '<div class="noid-inline-note">Ничего достаточно точного не нашли. Окно можно скрыть и перейти к следующей строке.</div>';
    }
    html += '</div>';
    return html;
}

function renderMainTableIdCell(oid, row){
    var hasId = !!String(oid || '').trim();
    var rowIdx = Number((row && row[8]) || -1);
    if(hasId){
        var canClear = loadedSupplierCount <= 1;
        return '<b style="color:#2e7d32">' + escapeHtml(oid) + '</b>'
            + (canClear
                ? ('<div><button type="button" class="noid-pick-btn id-clear-btn" data-row-idx="' + rowIdx + '" style="border-color:#ef4444;color:#b91c1c;">Снять ID</button></div>')
                : '');
    }
    var state = getNoIdInlineState(rowIdx);
    var btnText = (state && state.loading) ? 'Ищу...' : 'Подобрать';
    var disabled = (state && (state.loading || !!state.applyingId)) ? ' disabled' : '';
    return '<span style="color:#e65100">нет</span><div><button type="button" class="noid-pick-btn" data-row-idx="' + rowIdx + '"' + disabled + '>' + btnText + '</button></div>';
}

function renderMainTableNameCell(name, row){
    var html = escapeHtml(name || '');
    var oid = String((row && row[0]) || '').trim();
    if(!oid){
        html += renderNoIdInlinePicker(row || []);
    }
    return html;
}

function redrawMainTablePreservePage(){
    if(tblMain && typeof tblMain.rows === 'function' && typeof tblMain.draw === 'function'){
        tblMain.rows().invalidate();
        tblMain.draw(false);
        return;
    }
    renderMainTableFallback();
}

function resetNoIdInlinePicker(){
    noIdInlinePickerState = { rowIdx: -1, loading: false, applyingId: '', message: '', items: [], queryName: '' };
}

function closeNoIdInlinePicker(redraw){
    resetNoIdInlinePicker();
    if(redraw !== false){
        redrawMainTablePreservePage();
    }
}

function openNoIdInlinePicker(rowIdx){
    var idx = Number(rowIdx || -1);
    if(idx < 0){ return; }
    var row = (mainTableRows || []).find(function(r){
        return Number((r && r[8]) || -1) === idx;
    });
    if(!row){ return; }
    noIdInlinePickerState = {
        rowIdx: idx,
        loading: true,
        applyingId: '',
        message: 'Ищу совпадения по Onliner для: ' + String(row[1] || ''),
        items: [],
        queryName: String(row[1] || '')
    };
    redrawMainTablePreservePage();
    var basePayload = {
        name: String(row[1] || ''),
        category: String(row[9] || ''),
        onliner_id: '',
        query: '',
        limit: 12
    };
    fetchNoIdCandidates(basePayload).then(function(items){
        if(items.length){ return items; }
        var retryQuery = compactBrandModelQuery(basePayload.name);
        if(!retryQuery){ return items; }
        return fetchNoIdCandidates({
            name: basePayload.name,
            category: basePayload.category,
            onliner_id: '',
            query: retryQuery,
            limit: 12
        });
    }).then(function(items){
        if(Number(noIdInlinePickerState.rowIdx) !== idx){ return; }
        noIdInlinePickerState.loading = false;
        noIdInlinePickerState.items = Array.isArray(items) ? items : [];
        noIdInlinePickerState.message = noIdInlinePickerState.items.length
            ? 'Найдены кандидаты Onliner. Выбери нужную позицию.'
            : 'Точных кандидатов не найдено.';
        redrawMainTablePreservePage();
    }).catch(function(){
        if(Number(noIdInlinePickerState.rowIdx) !== idx){ return; }
        noIdInlinePickerState.loading = false;
        noIdInlinePickerState.items = [];
        noIdInlinePickerState.message = 'Ошибка поиска кандидатов Onliner.';
        redrawMainTablePreservePage();
    });
}

function applyNoIdCandidate(rowIdx, oid, url){
    var idx = Number(rowIdx || -1);
    var finalId = String(oid || '').trim();
    if(idx < 0 || !finalId){ return; }
    var row = (mainTableRows || []).find(function(r){
        return Number((r && r[8]) || -1) === idx;
    });
    if(!row){ return; }
    noIdInlinePickerState.applyingId = finalId;
    noIdInlinePickerState.message = 'Сохраняю ID в постоянный серверный кэш...';
    redrawMainTablePreservePage();
    fetch('/api/manual-id-confirm-batch', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify({
            source: 'inline_noid_picker',
            items: [{
                row_idx: idx,
                name: String(row[1] || ''),
                onliner_id: finalId,
                url: String(url || '')
            }]
        })
    }).then(function(r){ return r.json(); }).then(function(d){
        if(!d || d.status !== 'ok'){
            throw new Error((d && d.message) || 'save_failed');
        }
        row[0] = finalId;
        if(showOnlyNoIdRows){
            mainTableRows = (mainTableRows || []).filter(function(r){
                return Number((r && r[8]) || -1) !== idx;
            });
        }
        closeNoIdInlinePicker(false);
        redrawMainTablePreservePage();
        if(tblMain && tblMain.ajax && typeof tblMain.ajax.reload === 'function'){
            tblMain.ajax.reload(null, false);
        } else {
            fetch('/api/consolidated').then(function(r){ return r.json(); }).then(function(resp){
                mainTableRows = (resp && resp.data) ? resp.data : [];
                updateWithoutIdCount(mainTableRows);
                renderMainTableFallback();
            });
        }
        runPreExportQualityCheck();
    }).catch(function(){
        if(Number(noIdInlinePickerState.rowIdx) !== idx){ return; }
        noIdInlinePickerState.applyingId = '';
        noIdInlinePickerState.message = 'Не удалось сохранить ID. Попробуй еще раз.';
        redrawMainTablePreservePage();
    });
}

function clearMainTableId(rowIdx){
    var idx = Number(rowIdx || -1);
    if(idx < 0){ return; }
    var row = (mainTableRows || []).find(function(r){
        return Number((r && r[8]) || -1) === idx;
    });
    if(!row){ return; }
    var currentId = String((row && row[0]) || '').trim();
    if(!currentId){ return; }
    var itemName = String((row && row[1]) || '').trim();
    if(!window.confirm('Снять OnlinerID у товара "' + itemName + '"?\\nID ' + currentId + ' будет удалён из строки и заблокирован в вечном кеше.')){ return; }
    fetch('/api/manual-id-clear', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify({
            source: 'inline_id_clear',
            item: {
                row_idx: idx,
                name: itemName
            }
        })
    }).then(function(r){ return r.json(); }).then(function(d){
        if(!d || d.status !== 'ok'){
            throw new Error((d && d.message) || 'clear_failed');
        }
        row[0] = '';
        if(tblMain && tblMain.ajax && typeof tblMain.ajax.reload === 'function'){
            tblMain.ajax.reload(null, false);
        } else {
            renderMainTableFallback();
        }
        runPreExportQualityCheck();
    }).catch(function(err){
        alert('Не удалось снять ID: ' + String((err && err.message) || err || 'ошибка'));
    });
}

function handleNoIdTableClick(e){
    var clearBtn = e.target.closest('.id-clear-btn');
    if(clearBtn){
        e.preventDefault();
        clearMainTableId(clearBtn.getAttribute('data-row-idx'));
        return;
    }
    var pickBtn = e.target.closest('.noid-pick-btn');
    if(pickBtn){
        e.preventDefault();
        openNoIdInlinePicker(pickBtn.getAttribute('data-row-idx'));
        return;
    }
    var applyBtn = e.target.closest('.noid-inline-apply');
    if(applyBtn){
        e.preventDefault();
        applyNoIdCandidate(
            applyBtn.getAttribute('data-row-idx'),
            applyBtn.getAttribute('data-oid'),
            applyBtn.getAttribute('data-url')
        );
        return;
    }
    var closeBtn = e.target.closest('.noid-inline-close');
    if(closeBtn){
        e.preventDefault();
        closeNoIdInlinePicker();
    }
}

function updateWithoutIdCount(rows){
    var el = document.getElementById('without-id-count');
    if(!el){ return; }
    var list = Array.isArray(rows) ? rows : [];
    var count = list.reduce(function(acc, r){
        return acc + (!String((r && r[0]) || '').trim() ? 1 : 0);
    }, 0);
    el.textContent = String(count);
}

function setDuplicateIdCounterValue(v){
    var el = document.getElementById('duplicate-id-count');
    if(!el){ return; }
    if(Number(loadedSupplierCount || 1) > 1){
        el.style.visibility = 'hidden';
        el.textContent = '\u00a0';
        return;
    }
    el.style.visibility = '';
    el.textContent = String(v == null ? 0 : v);
}

function rebuildDuplicateIdFilterSet(rows){
    var counts = {};
    var supplierSet = {};
    var stats = {};
    (Array.isArray(rows) ? rows : []).forEach(function(r){
        var supplier = String((r && r[3]) || '').trim();
        if(supplier){ supplierSet[supplier] = true; }
        var oid = String((r && r[0]) || '').trim();
        if(!oid){ return; }
        counts[oid] = (counts[oid] || 0) + 1;
        var price = Number((r && r[2]) || NaN);
        if(isFinite(price)){
            if(!stats[oid]){
                stats[oid] = {min: price, max: price};
            } else {
                if(price < stats[oid].min){ stats[oid].min = price; }
                if(price > stats[oid].max){ stats[oid].max = price; }
            }
        }
    });
    var next = {};
    var priceMap = {};
    Object.keys(counts).forEach(function(oid){
        if(counts[oid] > 1){
            next[oid] = true;
            if(stats[oid]){ priceMap[oid] = stats[oid]; }
        }
    });
    duplicateIdFilterSet = next;
    duplicateIdPriceStats = priceMap;
    loadedSupplierCount = Math.max(1, Object.keys(supplierSet).length || loadedSupplierCount || 1);
    setDuplicateIdCounterValue(Object.keys(next).length);
}

function getDuplicatePriceTone(oid, priceValue){
    var id = String(oid || '').trim();
    if(!showOnlyDuplicateIdRows || !id || !duplicateIdFilterSet[id]){
        return '';
    }
    var price = Number(priceValue);
    var stats = duplicateIdPriceStats[id];
    if(!isFinite(price) || !stats || !isFinite(stats.min)){
        return '';
    }
    if(Math.abs(price - stats.min) < 0.0001){ return 'min'; }
    return 'high';
}

function renderMainPriceCell(value, row){
    var n = Number(value);
    if(!isFinite(n)){ return ''; }
    var oid = String((row && row[0]) || '').trim();
    var tone = getDuplicatePriceTone(oid, n);
    if(tone === 'min'){
        return '<span style="display:inline-block;padding:2px 7px;border-radius:999px;background:#dcfce7;color:#166534;font-weight:700;">' + n.toFixed(2) + '</span>';
    }
    if(tone === 'high'){
        return '<span style="display:inline-block;padding:2px 7px;border-radius:999px;background:#fee2e2;color:#b91c1c;font-weight:700;">' + n.toFixed(2) + '</span>';
    }
    return '<b style="color:#2e7d32">' + n.toFixed(2) + '</b>';
}

function getDuplicateGroupRowClass(rows, idx){
    if(!showOnlyDuplicateIdRows){ return ''; }
    var cur = String((rows[idx] && rows[idx][0]) || '').trim();
    if(!cur || !duplicateIdFilterSet[cur]){ return ''; }
    var prev = idx > 0 ? String((rows[idx - 1] && rows[idx - 1][0]) || '').trim() : '';
    var next = idx < rows.length - 1 ? String((rows[idx + 1] && rows[idx + 1][0]) || '').trim() : '';
    var hasPrev = prev === cur;
    var hasNext = next === cur;
    if(hasPrev && hasNext){ return 'dup-group-mid'; }
    if(hasPrev){ return 'dup-group-end'; }
    if(hasNext){ return 'dup-group-start'; }
    return 'dup-group-single';
}

function escapeHtml(text){
    return String(text || '')
        .replace(/&/g, '&amp;')
        .replace(/</g, '&lt;')
        .replace(/>/g, '&gt;')
        .replace(/"/g, '&quot;')
        .replace(/'/g, '&#39;');
}

function normalizeCompactMatch(text){
    return String(text || '').toLowerCase().replace(/[^a-zа-я0-9]+/g, '');
}

function extractInlineHighlightTokens(localName){
    var raw = String(localName || '');
    var lower = raw.toLowerCase();
    var tokens = {};
    var colorWords = ['black','white','blue','red','green','grey','gray','silver','gold','pink','purple','orange','черный','чёрный','белый','синий','голубой','красный','зеленый','зелёный','серый','серебристый','золотой','желтый','жёлтый','розовый'];
    colorWords.forEach(function(word){
        if(lower.indexOf(word) >= 0){
            tokens[normalizeCompactMatch(word)] = true;
        }
    });
    var capacities = String(raw).match(/\\b\\d+(?:[.,]\\d+)?\\s*(?:tb|gb)\\b/gi) || [];
    capacities.forEach(function(token){
        tokens[normalizeCompactMatch(token)] = true;
    });
    var latinModelTokens = String(raw).match(/\\b[A-Za-z][A-Za-z0-9-]{2,}\\b/g) || [];
    latinModelTokens.forEach(function(token){
        if(/\\d/.test(token) || token.length >= 5){
            tokens[normalizeCompactMatch(token)] = true;
        }
    });
    return tokens;
}

function highlightCandidateName(localName, candidateName){
    var text = String(candidateName || '');
    var needles = extractInlineHighlightTokens(localName);
    if(!text || !Object.keys(needles).length){
        return escapeHtml(text);
    }
    var parts = [];
    var lastIdx = 0;
    var re = /[A-Za-zА-Яа-яЁё0-9.,-]+/g;
    var match;
    while((match = re.exec(text)) !== null){
        var token = match[0];
        var normalized = normalizeCompactMatch(token);
        parts.push(escapeHtml(text.slice(lastIdx, match.index)));
        if(normalized && needles[normalized]){
            parts.push('<span class="noid-inline-hit">' + escapeHtml(token) + '</span>');
        } else {
            parts.push(escapeHtml(token));
        }
        lastIdx = match.index + token.length;
    }
    parts.push(escapeHtml(text.slice(lastIdx)));
    return parts.join('');
}

function renderCandidateBadges(item){
    var badges = [];
    var reason = String((item && item.reason) || '').trim();
    var scoreNum = Number((item && item.score) || 0);
    var score = isNaN(scoreNum) ? 0 : scoreNum;
    var name = String((item && item.name) || '');
    if(score >= 0.97){
        badges.push('точное');
    } else if(reason === 'model_token' || reason === 'paren_model' || score >= 0.84){
        badges.push('модель');
    } else if(reason === 'article_like' || reason === 'article'){
        badges.push('артикул');
    }
    if(/\((черный|чёрный|белый|зеленый|зелёный|розовый|синий|красный)\)/i.test(name)){
        badges.push('цвет');
    }
    if(/(?:^|[^a-z0-9])(?:xbox|playstation|usb)(?=$|[^a-z0-9])/i.test(name)){
        badges.push('версия');
    }
    if(!badges.length){ return ''; }
    return '<div class="noid-inline-badges">' + badges.map(function(b){
        return '<span class="noid-inline-badge">' + escapeHtml(b) + '</span>';
    }).join('') + '</div>';
}

function compactBrandModelQuery(name){
    var text = String(name || '').replace(/\\([^)]*\\)/g, ' ');
    text = text.replace(/[,"']/g, ' ');
    text = text.replace(/\\s+/g, ' ').trim();
    if(!text){ return ''; }
    var generic = {
        'гарнитура':1,'наушники':1,'наушникисмикрофоном':1,'с':1,'микрофоном':1,'мониторные':1,
        'охватывающие':1,'геймерские':1,'кабель':1,'черный':1,'чёрный':1,'белый':1,'зеленый':1,
        'зелёный':1,'розовый':1,'накладные':1,'usb':1
    };
    var parts = text.split(/\\s+/);
    var picked = [];
    for(var i = 0; i < parts.length; i++){
        var token = parts[i];
        var norm = normalizeCompactMatch(token);
        if(!norm || generic[norm]){ continue; }
        if(picked.length < 2){
            picked.push(token);
            continue;
        }
        if(/[A-Za-z]/.test(token) && /\d/.test(token)){
            picked.push(token);
            continue;
        }
        if(/^v\d+$/i.test(token) || /^[xvspro]+$/i.test(token) || /^h\d{3,4}$/i.test(token)){
            picked.push(token);
            continue;
        }
        if(picked.length >= 4){ break; }
    }
    return picked.slice(0, 5).join(' ').trim();
}

function bracketModelQuery(name){
    var text = String(name || '');
    if(!text){ return ''; }
    var generic = {
        'hdd':1,'ssd':1,'sata':1,'sataiii':1,'sataii':1,'nvme':1,'m2':1,'usb':1,'typec':1,
        'tb':1,'gb':1,'mb':1,'rpm':1
    };
    var brand = '';
    var words = text.replace(/[,"']/g, ' ').split(/\s+/);
    for(var i = 0; i < words.length; i++){
        var token = String(words[i] || '').trim();
        var norm = normalizeCompactMatch(token);
        if(!norm || generic[norm] || /\d/.test(token)){ continue; }
        if(/[A-Za-zА-Яа-я]/.test(token)){
            brand = token;
            break;
        }
    }
    var parts = text.match(/\(([^)]+)\)/g) || [];
    for(var j = 0; j < parts.length; j++){
        var inside = String(parts[j] || '').replace(/[()]/g, '').trim();
        var normInside = normalizeCompactMatch(inside);
        if(normInside.length < 5){ continue; }
        if(/[A-Za-z]/.test(inside) && /\d/.test(inside)){
            return brand ? (brand + ' ' + inside) : inside;
        }
    }
    return '';
}

function processorModelQuery(name){
    var text = String(name || '');
    if(!text){ return ''; }
    var patterns = [
        /(?:^|[^a-z0-9])(i[3579]-\d{4,5}[a-z]{0,2})(?=$|[^a-z0-9])/i,
        /(?:^|[^a-z0-9])(ryzen\s*[3579]\s*\d{4,5}[a-z]{0,2})(?=$|[^a-z0-9])/i,
        /(?:^|[^a-z0-9])(pentium\s+[a-z]?\d{4,5})(?=$|[^a-z0-9])/i,
        /(?:^|[^a-z0-9])(celeron\s+[a-z]?\d{4,5})(?=$|[^a-z0-9])/i,
        /(?:^|[^a-z0-9])(athlon\s+\d{4,5}[a-z]{0,2})(?=$|[^a-z0-9])/i
    ];
    for(var i = 0; i < patterns.length; i++){
        var m = text.match(patterns[i]);
        if(m && m[1]){
            return String(m[1]).replace(/\s+/g, ' ').trim();
        }
    }
    return '';
}

function fetchNoIdCandidates(payload){
    return fetch('/api/id-replace-candidates', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify(payload || {})
    }).then(function(r){ return r.json(); }).then(function(d){
        return Array.isArray(d && d.items) ? d.items : [];
    });
}

function getVerifyAllIdCandidateState(rowIdx){
    var key = String(Number(rowIdx || -1));
    if(!verifyAllIdsCandidateState[key]){
        verifyAllIdsCandidateState[key] = { loading: false, applyingId: '', items: [], message: '', manualOpen: false, manualValue: '' };
    }
    return verifyAllIdsCandidateState[key];
}

function renderVerifyAllIdCandidates(issue){
    var idx = Number((issue && issue.row_idx) || -1);
    if(idx < 0){ return ''; }
    var state = getVerifyAllIdCandidateState(idx);
    var html = '<div class="verify-id-inline-box">';
    if(state.loading){
        html += '<div class="noid-inline-note">Ищу варианты замены...</div>';
    } else if(state.message){
        html += '<div class="noid-inline-note">' + escapeHtml(state.message) + '</div>';
    }
    if(state.manualOpen){
        html += '<div class="noid-inline-picker" style="margin-bottom:8px;">';
        html += '<div class="noid-inline-note">Введите правильный Onliner ID для этого товара.</div>';
        html += '<div class="noid-inline-top">';
        html += '<input type="text" class="verify-id-manual-input" data-row-idx="' + idx + '" value="' + escapeHtml(state.manualValue || '') + '" placeholder="Например: 123456" style="flex:1; min-width:180px; padding:7px 10px; border:1px solid #cfd8e3; border-radius:8px; font-size:12px;">';
        html += '<div style="display:flex; gap:8px;">';
        html += '<button type="button" class="noid-inline-apply verify-id-manual-save" data-row-idx="' + idx + '">Сохранить ID</button>';
        html += '<button type="button" class="noid-inline-close verify-id-manual-cancel" data-row-idx="' + idx + '">Отмена</button>';
        html += '</div>';
        html += '</div>';
        html += '</div>';
    }
    if(Array.isArray(state.items) && state.items.length){
        html += '<div class="noid-inline-list">';
        state.items.forEach(function(item){
            var candidateId = String((item && item.id) || '').trim();
            var candidateName = String((item && item.name) || '').trim();
            var candidateUrl = String((item && item.url) || '').trim();
            var scoreNum = Number((item && item.score) || 0);
            var score = isNaN(scoreNum) ? '' : scoreNum.toFixed(3);
            var isApplying = !!(state.applyingId && state.applyingId === candidateId);
            html += '<div class="noid-inline-item">';
            html += '<div class="noid-inline-meta">';
            html += '<div class="noid-inline-title">' + highlightCandidateName(issue.name || '', candidateName || candidateId || 'Кандидат без названия') + '</div>';
            html += renderCandidateBadges(item || {});
            html += '<div class="noid-inline-sub">ID: ' + escapeHtml(candidateId || '—') + (score ? (' · score ' + escapeHtml(score)) : '') + '</div>';
            html += '</div>';
            html += '<div class="noid-inline-actions">';
            if(candidateUrl){
                html += '<a href="' + escapeHtml(candidateUrl) + '" target="_blank" rel="noopener noreferrer" class="noid-inline-open">Открыть</a>';
            }
            html += '<button type="button" class="noid-inline-apply verify-id-inline-apply" data-row-idx="' + idx + '" data-oid="' + escapeHtml(candidateId) + '" data-url="' + escapeHtml(candidateUrl) + '"' + (isApplying ? ' disabled' : '') + '>' + (isApplying ? 'Сохраняю...' : 'Заменить ID') + '</button>';
            html += '</div>';
            html += '</div>';
        });
        html += '</div>';
    }
    html += '</div>';
    return html;
}

function promptVerifyAllIdManual(rowIdx){
    var idx = Number(rowIdx || -1);
    if(idx < 0){ return; }
    var issue = (verifyAllIdsItems || []).find(function(item){
        return Number((item && item.row_idx) || -1) === idx;
    });
    if(!issue){ return; }
    var currentId = String((issue && issue.onliner_id) || '').trim();
    var state = getVerifyAllIdCandidateState(idx);
    state.manualOpen = true;
    state.manualValue = state.manualValue || currentId || '';
    state.message = 'Введите ID и сохраните его для этого товара.';
    renderVerifyAllIdsStatus({ items: verifyAllIdsItems, total: verifyAllIdsItems.length, done: verifyAllIdsItems.length, mismatched: verifyAllIdsItems.length });
}

function saveVerifyAllIdManual(rowIdx){
    var idx = Number(rowIdx || -1);
    if(idx < 0){ return; }
    var state = getVerifyAllIdCandidateState(idx);
    var input = document.querySelector('.verify-id-manual-input[data-row-idx="' + idx + '"]');
    var finalId = String(input ? input.value : state.manualValue || '').replace(/\D+/g, '').trim();
    if(!finalId){
        state.manualOpen = true;
        state.message = 'Нужно вставить числовой Onliner ID.';
        renderVerifyAllIdsStatus({ items: verifyAllIdsItems, total: verifyAllIdsItems.length, done: verifyAllIdsItems.length, mismatched: verifyAllIdsItems.length });
        return;
    }
    state.manualValue = finalId;
    state.manualOpen = false;
    applyVerifyAllIdCandidate(idx, finalId, '');
}

function cancelVerifyAllIdManual(rowIdx){
    var idx = Number(rowIdx || -1);
    if(idx < 0){ return; }
    var state = getVerifyAllIdCandidateState(idx);
    state.manualOpen = false;
    state.message = '';
    renderVerifyAllIdsStatus({ items: verifyAllIdsItems, total: verifyAllIdsItems.length, done: verifyAllIdsItems.length, mismatched: verifyAllIdsItems.length });
}

function getDuplicateIdCandidateState(rowIdx){
    var key = String(Number(rowIdx || -1));
    if(!duplicateIdCandidateState[key]){
        duplicateIdCandidateState[key] = { loading: false, applyingId: '', items: [], message: '', manualOpen: false, manualValue: '' };
    }
    return duplicateIdCandidateState[key];
}

function renderDuplicateIdCandidates(issue){
    var idx = Number((issue && issue.row_idx) || -1);
    if(idx < 0){ return ''; }
    var state = getDuplicateIdCandidateState(idx);
    var html = '<div class="verify-id-inline-box">';
    if(state.loading){
        html += '<div class="noid-inline-note">Ищу варианты замены...</div>';
    } else if(state.message){
        html += '<div class="noid-inline-note">' + escapeHtml(state.message) + '</div>';
    }
    if(state.manualOpen){
        html += '<div class="noid-inline-picker" style="margin-bottom:8px;">';
        html += '<div class="noid-inline-note">Введите правильный Onliner ID для этого товара.</div>';
        html += '<div class="noid-inline-top">';
        html += '<input type="text" class="duplicate-id-manual-input" data-row-idx="' + idx + '" value="' + escapeHtml(state.manualValue || '') + '" placeholder="Например: 123456" style="flex:1; min-width:180px; padding:7px 10px; border:1px solid #cfd8e3; border-radius:8px; font-size:12px;">';
        html += '<div style="display:flex; gap:8px;">';
        html += '<button type="button" class="noid-inline-apply duplicate-id-manual-save" data-row-idx="' + idx + '">Сохранить ID</button>';
        html += '<button type="button" class="noid-inline-close duplicate-id-manual-cancel" data-row-idx="' + idx + '">Отмена</button>';
        html += '</div>';
        html += '</div>';
        html += '</div>';
    }
    if(Array.isArray(state.items) && state.items.length){
        html += '<div class="noid-inline-list">';
        state.items.forEach(function(item){
            var candidateId = String((item && item.id) || '').trim();
            var candidateName = String((item && item.name) || '').trim();
            var candidateUrl = String((item && item.url) || '').trim();
            var scoreNum = Number((item && item.score) || 0);
            var score = isNaN(scoreNum) ? '' : scoreNum.toFixed(3);
            var isApplying = !!(state.applyingId && state.applyingId === candidateId);
            html += '<div class="noid-inline-item">';
            html += '<div class="noid-inline-meta">';
            html += '<div class="noid-inline-title">' + highlightCandidateName(issue.name || '', candidateName || candidateId || 'Кандидат без названия') + '</div>';
            html += renderCandidateBadges(item || {});
            html += '<div class="noid-inline-sub">ID: ' + escapeHtml(candidateId || '—') + (score ? (' · score ' + escapeHtml(score)) : '') + '</div>';
            html += '</div>';
            html += '<div class="noid-inline-actions">';
            if(candidateUrl){
                html += '<a href="' + escapeHtml(candidateUrl) + '" target="_blank" rel="noopener noreferrer" class="noid-inline-open">Открыть</a>';
            }
            html += '<button type="button" class="noid-inline-apply duplicate-id-inline-apply" data-row-idx="' + idx + '" data-oid="' + escapeHtml(candidateId) + '" data-url="' + escapeHtml(candidateUrl) + '"' + (isApplying ? ' disabled' : '') + '>' + (isApplying ? 'Сохраняю...' : 'Заменить ID') + '</button>';
            html += '</div>';
            html += '</div>';
        });
        html += '</div>';
    }
    html += '</div>';
    return html;
}

function renderDuplicateIdCheckStatus(data){
    var btn = document.getElementById('run-duplicate-id-check-btn');
    var note = document.getElementById('duplicate-id-note');
    var card = document.getElementById('duplicate-id-check-card');
    var summary = document.getElementById('duplicate-id-check-summary');
    var results = document.getElementById('duplicate-id-check-results');
    if(!btn || !note || !card || !summary || !results){ return; }

    var items = Array.isArray(data && data.items) ? data.items.slice() : duplicateIdIssues.slice();
    var totalIds = Number((data && data.problem_ids) || duplicateIdProblemCount || 0);
    var totalRows = Number((data && data.problem_rows) || items.length || 0);
    var message = String((data && data.message) || '').trim();
    duplicateIdIssues = items;
    duplicateIdProblemCount = totalIds;

    btn.disabled = false;
    btn.textContent = 'Одинаковые ID';
    note.textContent = duplicateIdLastActionMessage || message || (totalRows ? ('Найдено строк с одинаковыми ID: ' + String(totalRows) + '.') : 'Поиск случаев, когда один OnlinerID стоит у разных товаров.');

    if(totalRows || message){
        card.style.display = 'block';
    }
    syncDuplicateIdCheckCardCollapsed();
    summary.textContent = message || (totalRows
        ? ('Найдено проблемных OnlinerID: ' + String(totalIds) + '. Строк для проверки: ' + String(totalRows) + '.')
        : 'Одинаковых OnlinerID у разных товаров не найдено.');

    if(!items.length){
        results.innerHTML = totalRows ? '' : '<div class="markup-note" style="margin-top:10px;">Проблемных одинаковых ID не найдено.</div>';
        return;
    }

    results.innerHTML = '<div class="verify-id-list">' + items.map(function(issue){
        var rowIdx = Number((issue && issue.row_idx) || -1);
        var scoreNum = Number((issue && issue.score) || 0);
        var score = isNaN(scoreNum) ? '' : scoreNum.toFixed(3);
        var apiName = String((issue && issue.api_name) || '').trim();
        var apiUrl = String((issue && issue.api_url) || '').trim();
        var reason = String((issue && issue.reason_label) || (issue && issue.reason) || '').trim();
        var currentId = String((issue && issue.onliner_id) || '').trim();
        var statusLabel = String((issue && issue.status_label) || 'Одинаковый ID').trim();
        var html = '<div class="verify-id-item">';
        html += '<div class="verify-id-head">';
        html += '<div>';
        html += '<div class="verify-id-title">' + escapeHtml(String((issue && issue.name) || '')) + '</div>';
        html += '<div class="verify-id-sub">Текущий ID: <b>' + escapeHtml(currentId || '—') + '</b> | Поставщик: ' + escapeHtml(String((issue && issue.supplier) || '')) + '</div>';
        html += '<div class="verify-id-sub">Onliner по ID: ' + (apiUrl ? ('<a href="' + escapeHtml(apiUrl) + '" target="_blank" rel="noopener noreferrer">' + escapeHtml(apiName || currentId) + '</a>') : escapeHtml(apiName || 'Не удалось получить название')) + '</div>';
        html += '<div class="verify-id-sub">Причина: ' + escapeHtml(reason || 'Одинаковый OnlinerID') + (score ? (' | score: ' + escapeHtml(score)) : '') + '</div>';
        html += '</div>';
        html += '<div class="verify-id-status">' + escapeHtml(statusLabel) + '</div>';
        html += '</div>';
        html += '<div class="verify-id-actions">';
        html += '<button type="button" class="btn btn-outline duplicate-id-load-btn" data-row-idx="' + rowIdx + '" style="padding:8px 12px;font-size:13px;">Подобрать замену</button>';
        html += '<button type="button" class="btn btn-outline duplicate-id-manual-btn" data-row-idx="' + rowIdx + '" style="padding:8px 12px;font-size:13px;">Вставить ID</button>';
        html += '</div>';
        html += renderDuplicateIdCandidates(issue || {});
        html += '</div>';
        return html;
    }).join('') + '</div>';
    compactVerifyIdCards('duplicate-id-check-results');
}

function runDuplicateIdCheck(silent){
    var btn = document.getElementById('run-duplicate-id-check-btn');
    var card = document.getElementById('duplicate-id-check-card');
    var summary = document.getElementById('duplicate-id-check-summary');
    var results = document.getElementById('duplicate-id-check-results');
    if(btn){
        btn.disabled = true;
        btn.textContent = 'Проверяю...';
    }
    if(!silent && card){ card.style.display = 'block'; }
    if(summary){ summary.textContent = 'Ищу одинаковые OnlinerID в текущем прайсе...'; }
    if(results && !silent){ results.innerHTML = ''; }
    if(!silent){ duplicateIdLastActionMessage = ''; }
    duplicateIdCardCollapsed = false;
    syncDuplicateIdCheckCardCollapsed();
    fetch('/api/check-duplicate-onliner-ids', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'}
    }).then(function(r){ return r.json(); }).then(function(d){
        if(!d || d.status !== 'ok'){
            throw new Error((d && d.message) || 'check_failed');
        }
        renderDuplicateIdCheckStatus(d || {});
    }).catch(function(){
        if(btn){
            btn.disabled = false;
            btn.textContent = 'Одинаковые ID';
        }
        var note = document.getElementById('duplicate-id-note');
        if(note){ note.textContent = 'Не удалось проверить одинаковые ID.'; }
        if(summary){ summary.textContent = 'Не удалось выполнить проверку одинаковых OnlinerID.'; }
    });
}

function loadDuplicateIdCandidates(rowIdx){
    var idx = Number(rowIdx || -1);
    if(idx < 0){ return; }
    var issue = (duplicateIdIssues || []).find(function(item){
        return Number((item && item.row_idx) || -1) === idx;
    });
    if(!issue){ return; }
    var state = getDuplicateIdCandidateState(idx);
    var basePayload = {
        name: String(issue.name || ''),
        category: String(issue.category || ''),
        onliner_id: String(issue.onliner_id || ''),
        exclude_current: true,
        query: '',
        limit: 12
    };
    state.loading = true;
    state.items = [];
    state.message = 'Ищу кандидатов для замены...';
    renderDuplicateIdCheckStatus({ items: duplicateIdIssues, problem_rows: duplicateIdIssues.length, problem_ids: duplicateIdProblemCount });

    var compactQuery = compactBrandModelQuery(basePayload.name);
    var modelQuery = bracketModelQuery(basePayload.name);
    var cpuQuery = processorModelQuery(basePayload.name);
    var requests = [
        fetchNoIdCandidates(basePayload).catch(function(){ return []; })
    ];
    if(compactQuery){
        requests.push(fetchNoIdCandidates({
            name: basePayload.name,
            category: basePayload.category,
            onliner_id: basePayload.onliner_id,
            exclude_current: true,
            query: compactQuery,
            limit: 12
        }).catch(function(){ return []; }));
    }
    if(modelQuery && modelQuery !== compactQuery){
        requests.push(fetchNoIdCandidates({
            name: basePayload.name,
            category: basePayload.category,
            onliner_id: basePayload.onliner_id,
            exclude_current: true,
            query: modelQuery,
            limit: 12
        }).catch(function(){ return []; }));
    }
    if(cpuQuery && cpuQuery !== compactQuery && cpuQuery !== modelQuery){
        requests.push(fetchNoIdCandidates({
            name: basePayload.name,
            category: basePayload.category,
            onliner_id: basePayload.onliner_id,
            exclude_current: true,
            query: cpuQuery,
            limit: 12
        }).catch(function(){ return []; }));
    }

    Promise.all(requests).then(function(results){
        state.loading = false;
        state.items = mergeVerifyIdCandidateLists(results, 12);
        state.message = state.items.length ? 'Выберите правильный ID.' : 'Подходящих кандидатов не найдено.';
        renderDuplicateIdCheckStatus({ items: duplicateIdIssues, problem_rows: duplicateIdIssues.length, problem_ids: duplicateIdProblemCount });
    }).catch(function(){
        state.loading = false;
        state.items = [];
        state.message = 'Ошибка поиска кандидатов.';
        renderDuplicateIdCheckStatus({ items: duplicateIdIssues, problem_rows: duplicateIdIssues.length, problem_ids: duplicateIdProblemCount });
    });
}

function promptDuplicateIdManual(rowIdx){
    var idx = Number(rowIdx || -1);
    if(idx < 0){ return; }
    var issue = (duplicateIdIssues || []).find(function(item){
        return Number((item && item.row_idx) || -1) === idx;
    });
    if(!issue){ return; }
    var currentId = String((issue && issue.onliner_id) || '').trim();
    var state = getDuplicateIdCandidateState(idx);
    state.manualOpen = true;
    state.manualValue = state.manualValue || currentId || '';
    state.message = 'Введите новый правильный ID для этого товара.';
    renderDuplicateIdCheckStatus({ items: duplicateIdIssues, problem_rows: duplicateIdIssues.length, problem_ids: duplicateIdProblemCount });
}

function saveDuplicateIdManual(rowIdx){
    var idx = Number(rowIdx || -1);
    if(idx < 0){ return; }
    var state = getDuplicateIdCandidateState(idx);
    var input = document.querySelector('.duplicate-id-manual-input[data-row-idx="' + idx + '"]');
    var finalId = String(input ? input.value : state.manualValue || '').replace(/\D+/g, '').trim();
    var issue = (duplicateIdIssues || []).find(function(item){
        return Number((item && item.row_idx) || -1) === idx;
    });
    if(!finalId){
        state.manualOpen = true;
        state.message = 'Нужно вставить числовой Onliner ID.';
        renderDuplicateIdCheckStatus({ items: duplicateIdIssues, problem_rows: duplicateIdIssues.length, problem_ids: duplicateIdProblemCount });
        return;
    }
    state.manualValue = finalId;
    state.manualOpen = false;
    applyDuplicateIdCandidate(idx, finalId, '');
}

function cancelDuplicateIdManual(rowIdx){
    var idx = Number(rowIdx || -1);
    if(idx < 0){ return; }
    var state = getDuplicateIdCandidateState(idx);
    state.manualOpen = false;
    state.message = '';
    renderDuplicateIdCheckStatus({ items: duplicateIdIssues, problem_rows: duplicateIdIssues.length, problem_ids: duplicateIdProblemCount });
}

function applyDuplicateIdCandidate(rowIdx, oid, url){
    var idx = Number(rowIdx || -1);
    var finalId = String(oid || '').trim();
    if(idx < 0 || !finalId){ return; }
    var issue = (duplicateIdIssues || []).find(function(item){
        return Number((item && item.row_idx) || -1) === idx;
    });
    if(!issue){ return; }
    var state = getDuplicateIdCandidateState(idx);
    var currentId = String((issue && issue.onliner_id) || '').trim();
    state.applyingId = finalId;
    state.message = (currentId && finalId === currentId)
        ? 'Подтверждаю текущий ID и сохраняю его в вечный кеш...'
        : 'Сохраняю новый ID в постоянный кеш...';
    renderDuplicateIdCheckStatus({ items: duplicateIdIssues, problem_rows: duplicateIdIssues.length, problem_ids: duplicateIdProblemCount });
    fetch('/api/manual-id-confirm-batch', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify({
            source: 'duplicate_id_replace',
            items: [{
                row_idx: idx,
                name: String(issue.name || ''),
                onliner_id: finalId,
                url: String(url || '')
            }]
        })
    }).then(function(r){ return r.json(); }).then(function(d){
        if(!d || d.status !== 'ok'){
            throw new Error((d && d.message) || 'save_failed');
        }
        delete duplicateIdCandidateState[String(idx)];
        (mainTableRows || []).forEach(function(row){
            if(Number((row && row[8]) || -1) === idx){
                row[0] = finalId;
            }
        });
        if(tblMain && tblMain.ajax && typeof tblMain.ajax.reload === 'function'){
            tblMain.ajax.reload(null, false);
        } else {
            redrawMainTablePreservePage();
        }
        runPreExportQualityCheck();
        duplicateIdLastActionMessage = (currentId && finalId === currentId)
            ? ('Текущий ID ' + finalId + ' подтверждён и сохранён в вечный кеш.')
            : ('ID ' + finalId + ' сохранён в прайс и вечный кеш.');
        var note = document.getElementById('duplicate-id-note');
        if(note){
            note.textContent = duplicateIdLastActionMessage + ' Обновляю список конфликтов...';
        }
        var summary = document.getElementById('duplicate-id-check-summary');
        if(summary){
            summary.textContent = 'Новый ID сохранён. Перепроверяю одинаковые OnlinerID...';
        }
        runDuplicateIdCheck(true);
    }).catch(function(){
        state.applyingId = '';
        state.message = 'Не удалось заменить ID. Попробуйте еще раз.';
        renderDuplicateIdCheckStatus({ items: duplicateIdIssues, problem_rows: duplicateIdIssues.length, problem_ids: duplicateIdProblemCount });
    });
}

function renderVerifyAllIdsReport(data){
    var wrap = document.getElementById('verify-all-ids-report-wrap');
    var summary = document.getElementById('verify-all-ids-report-summary');
    var tableWrap = document.getElementById('verify-all-ids-report-table-wrap');
    if(!wrap || !summary || !tableWrap){ return; }

    if(Array.isArray(data && data.report_items)){
        verifyAllIdsReportItems = data.report_items.slice();
    }

    if(!verifyAllIdsReportVisible){
        wrap.style.display = 'none';
        return;
    }
    wrap.style.display = 'block';

    var total = verifyAllIdsReportItems.length;
    summary.textContent = total
        ? ('Полная таблица проверки: ' + String(total) + ' строк.')
        : 'Полная таблица проверки пока пуста.';

    if(!total){
        tableWrap.innerHTML = '<div class="markup-note" style="padding:12px;">Нет данных для отображения.</div>';
        return;
    }

    tableWrap.innerHTML = '<table class="verify-id-report-table"><thead><tr>'
        + '<th>Статус</th>'
        + '<th>ID</th>'
        + '<th>Локальное название</th>'
        + '<th>Onliner</th>'
        + '<th>Причина</th>'
        + '<th>Score</th>'
        + '</tr></thead><tbody>'
        + verifyAllIdsReportItems.map(function(item){
            var status = String((item && item.status_label) || '').trim() || 'Проверить';
            var statusKey = String((item && item.status) || '').trim().toLowerCase();
            var statusCls = statusKey === 'match' ? 'ok' : (statusKey === 'mismatch' ? 'bad' : 'review');
            var oid = String((item && item.onliner_id) || '').trim();
            var localName = String((item && item.name) || '').trim();
            var apiName = String((item && item.api_name) || '').trim();
            var apiUrl = String((item && item.api_url) || '').trim();
            var reason = String((item && item.reason_label) || (item && item.reason) || '').trim();
            var scoreNum = Number((item && item.score) || 0);
            var score = isNaN(scoreNum) ? '' : scoreNum.toFixed(3);
            return '<tr>'
                + '<td><span class="verify-id-report-status ' + statusCls + '">' + escapeHtml(status) + '</span></td>'
                + '<td>' + escapeHtml(oid || '—') + '</td>'
                + '<td>' + escapeHtml(localName || '—') + '</td>'
                + '<td>' + (apiUrl ? ('<a href="' + escapeHtml(apiUrl) + '" target="_blank" rel="noopener noreferrer">' + escapeHtml(apiName || oid || 'Открыть') + '</a>') : escapeHtml(apiName || '—')) + '</td>'
                + '<td>' + escapeHtml(reason || '—') + '</td>'
                + '<td>' + escapeHtml(score || '—') + '</td>'
                + '</tr>';
        }).join('')
        + '</tbody></table>';
}

function toggleVerifyAllIdsReport(forceVisible){
    if(typeof forceVisible === 'boolean'){
        verifyAllIdsReportVisible = forceVisible;
    } else {
        verifyAllIdsReportVisible = !verifyAllIdsReportVisible;
    }
    renderVerifyAllIdsReport({});
}

function syncVerifyAllIdsCardCollapsed(){
    var body = document.getElementById('verify-all-ids-card-body');
    var btn = document.getElementById('toggle-verify-all-ids-card-btn');
    if(!body || !btn){ return; }
    body.style.display = verifyAllIdsCardCollapsed ? 'none' : 'block';
    btn.textContent = verifyAllIdsCardCollapsed ? 'Показать' : 'Скрыть';
}

function toggleVerifyAllIdsCard(forceCollapsed){
    if(typeof forceCollapsed === 'boolean'){
        verifyAllIdsCardCollapsed = forceCollapsed;
    } else {
        verifyAllIdsCardCollapsed = !verifyAllIdsCardCollapsed;
    }
    syncVerifyAllIdsCardCollapsed();
}

function syncDuplicateIdCheckCardCollapsed(){
    var body = document.getElementById('duplicate-id-check-card-body');
    var btn = document.getElementById('toggle-duplicate-id-check-card-btn');
    if(!body || !btn){ return; }
    body.style.display = duplicateIdCardCollapsed ? 'none' : 'block';
    btn.textContent = duplicateIdCardCollapsed ? 'Показать' : 'Скрыть';
}

function toggleDuplicateIdCheckCard(forceCollapsed){
    if(typeof forceCollapsed === 'boolean'){
        duplicateIdCardCollapsed = forceCollapsed;
    } else {
        duplicateIdCardCollapsed = !duplicateIdCardCollapsed;
    }
    syncDuplicateIdCheckCardCollapsed();
}

function ensureQualityCheckCardUI(){
    var card = document.getElementById('quality-check-card');
    if(!card){ return; }
    var header = card.firstElementChild;
    var body = card.children.length > 1 ? card.children[1] : null;
    if(header && !header.classList.contains('quality-card-head')){
        header.classList.add('quality-card-head');
    }
    if(body && !body.id){
        body.id = 'quality-check-card-body';
    }
    if(body){
        body.style.padding = '12px 14px';
    }
    var btn = document.getElementById('toggle-quality-check-card-btn');
    if(!btn && header){
        btn = document.createElement('button');
        btn.type = 'button';
        btn.id = 'toggle-quality-check-card-btn';
        btn.className = 'btn btn-outline quality-card-toggle';
        btn.textContent = 'Скрыть';
        header.appendChild(btn);
        btn.addEventListener('click', function(){
            toggleQualityCheckCard();
        });
    }
    var reapplyBtn = document.getElementById('quality-reapply-markups-btn');
    var details = document.getElementById('quality-check-details');
    if(reapplyBtn){
        reapplyBtn.classList.add('quality-reapply-btn');
        reapplyBtn.classList.remove('btn-outline');
        reapplyBtn.style.marginTop = '12px';
    }
    if(reapplyBtn && details && reapplyBtn.parentElement !== body){
        body.appendChild(reapplyBtn);
    } else if(reapplyBtn && body && reapplyBtn.parentElement === body){
        body.appendChild(reapplyBtn);
    }
    var actionsWrap = card.querySelector('.quality-actions');
    if(actionsWrap){
        actionsWrap.style.display = 'none';
    }
    syncQualityCheckCardCollapsed();
}

function syncQualityCheckCardCollapsed(){
    var body = document.getElementById('quality-check-card-body');
    var btn = document.getElementById('toggle-quality-check-card-btn');
    if(!body || !btn){ return; }
    body.style.display = qualityCheckCardCollapsed ? 'none' : 'block';
    btn.textContent = qualityCheckCardCollapsed ? 'Показать' : 'Скрыть';
}

function toggleQualityCheckCard(forceCollapsed){
    if(typeof forceCollapsed === 'boolean'){
        qualityCheckCardCollapsed = forceCollapsed;
    } else {
        qualityCheckCardCollapsed = !qualityCheckCardCollapsed;
    }
    syncQualityCheckCardCollapsed();
}

function renderVerifyAllIdsStatus(data){
    var btn = document.getElementById('run-verify-all-ids-btn');
    var note = document.getElementById('verify-all-id-note');
    var countEl = document.getElementById('verify-all-id-count');
    var card = document.getElementById('verify-all-ids-card');
    var summary = document.getElementById('verify-all-ids-summary');
    var results = document.getElementById('verify-all-ids-results');
    if(!btn || !note || !countEl || !card || !summary || !results){ return; }

    var running = !!(data && data.running);
    var total = Number((data && data.total) || 0);
    var done = Number((data && data.done) || 0);
    var mismatched = Number((data && data.mismatched) || 0);
    var matched = Number((data && data.matched) || 0);
    var errors = Number((data && data.errors) || 0);
    var message = String((data && data.message) || '').trim();

    btn.disabled = running;
    btn.textContent = running ? 'Проверяю...' : 'Запустить';
    countEl.textContent = running ? (String(done) + '/' + String(total || 0)) : (total ? String(mismatched) : '—');
    note.textContent = running
        ? ('Проверено ' + String(done) + ' из ' + String(total || 0) + '. Несовпадений: ' + String(mismatched))
        : (message || (total ? ('Проверено: ' + String(total) + '. Несовпадений: ' + String(mismatched)) : 'Сверка текущих ID с товаром Onliner по API.'));
    note.classList.toggle('verify-id-note-link', !running && !!total);

    if(running || total || mismatched || errors){
        card.style.display = 'block';
    }
    syncVerifyAllIdsCardCollapsed();
    summary.textContent = running
        ? ('Идет проверка ID: ' + String(done) + ' / ' + String(total || 0))
        : (message || ('Проверено: ' + String(total) + ', совпало: ' + String(matched) + ', не совпало: ' + String(mismatched) + ', ошибок API: ' + String(errors)));

    if(Array.isArray(data && data.items)){
        verifyAllIdsItems = data.items.slice();
    }
    if(Array.isArray(data && data.report_items)){
        verifyAllIdsReportItems = data.report_items.slice();
    }

    renderVerifyAllIdsReport(data || {});

    if(running){
        return;
    }

    if(!verifyAllIdsItems.length){
        results.innerHTML = total ? '<div class="markup-note" style="margin-top:10px;">Проблемных ID не найдено.</div>' : '';
        return;
    }

    results.innerHTML = '<div class="verify-id-list">' + verifyAllIdsItems.map(function(issue){
        var rowIdx = Number((issue && issue.row_idx) || -1);
        var scoreNum = Number((issue && issue.score) || 0);
        var score = isNaN(scoreNum) ? '' : scoreNum.toFixed(3);
        var apiName = String((issue && issue.api_name) || '').trim();
        var apiUrl = String((issue && issue.api_url) || '').trim();
        var reason = String((issue && issue.reason_label) || (issue && issue.reason) || '').trim();
        var currentId = String((issue && issue.onliner_id) || '').trim();
        var statusLabel = String((issue && issue.status_label) || 'Проверить').trim();
        var html = '<div class="verify-id-item">';
        html += '<div class="verify-id-head">';
        html += '<div>';
        html += '<div class="verify-id-title">' + escapeHtml(String((issue && issue.name) || '')) + '</div>';
        html += '<div class="verify-id-sub">Текущий ID: <b>' + escapeHtml(currentId || '—') + '</b> | Поставщик: ' + escapeHtml(String((issue && issue.supplier) || '')) + '</div>';
        html += '<div class="verify-id-sub">Onliner по ID: ' + (apiUrl ? ('<a href="' + escapeHtml(apiUrl) + '" target="_blank" rel="noopener noreferrer">' + escapeHtml(apiName || currentId) + '</a>') : escapeHtml(apiName || 'Не удалось получить название')) + '</div>';
        html += '<div class="verify-id-sub">Причина: ' + escapeHtml(reason || 'Несовпадение') + (score ? (' | score: ' + escapeHtml(score)) : '') + '</div>';
        html += '</div>';
        html += '<div class="verify-id-status">' + escapeHtml(statusLabel) + '</div>';
        html += '</div>';
        html += '<div class="verify-id-actions">';
        html += '<button type="button" class="btn btn-outline verify-id-load-btn" data-row-idx="' + rowIdx + '" style="padding:8px 12px;font-size:13px;">Подобрать замену</button>';
        html += '<button type="button" class="btn btn-outline verify-id-manual-btn" data-row-idx="' + rowIdx + '" style="padding:8px 12px;font-size:13px;">Вставить ID</button>';
        html += '</div>';
        html += renderVerifyAllIdCandidates(issue || {});
        html += '</div>';
        return html;
    }).join('') + '</div>';
}

function pollVerifyAllIdsStatus(){
    fetch('/api/verify-all-ids-status').then(function(r){ return r.json(); }).then(function(d){
        renderVerifyAllIdsStatus(d || {});
        if(d && d.running){
            clearTimeout(verifyAllIdsPollTimer);
            verifyAllIdsPollTimer = setTimeout(pollVerifyAllIdsStatus, 1200);
        }
    }).catch(function(){
        clearTimeout(verifyAllIdsPollTimer);
        verifyAllIdsPollTimer = null;
        var note = document.getElementById('verify-all-id-note');
        if(note){ note.textContent = 'Ошибка связи с сервером при проверке ID.'; }
        var btn = document.getElementById('run-verify-all-ids-btn');
        if(btn){ btn.disabled = false; btn.textContent = 'Запустить'; }
    });
}

function runVerifyAllIds(){
    var card = document.getElementById('verify-all-ids-card');
    var summary = document.getElementById('verify-all-ids-summary');
    var results = document.getElementById('verify-all-ids-results');
    verifyAllIdsItems = [];
    verifyAllIdsCandidateState = {};
    verifyAllIdsReportItems = [];
    verifyAllIdsReportVisible = false;
    verifyAllIdsCardCollapsed = false;
    if(card){ card.style.display = 'block'; }
    qualityCheckCardCollapsed = false;
    syncQualityCheckCardCollapsed();
    qualityCheckCardCollapsed = false;
    syncQualityCheckCardCollapsed();
    if(summary){ summary.textContent = 'Запускаю проверку всех ID...'; }
    if(results){ results.innerHTML = ''; }
    syncVerifyAllIdsCardCollapsed();
    renderVerifyAllIdsReport({});
    fetch('/api/verify-all-ids-start', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'}
    }).then(function(r){ return r.json(); }).then(function(d){
        if(!d || (d.status !== 'started' && d.status !== 'already_running')){
            throw new Error((d && d.message) || 'start_failed');
        }
        pollVerifyAllIdsStatus();
    }).catch(function(){
        var note = document.getElementById('verify-all-id-note');
        if(note){ note.textContent = 'Не удалось запустить проверку ID.'; }
        var btn = document.getElementById('run-verify-all-ids-btn');
        if(btn){ btn.disabled = false; btn.textContent = 'Запустить'; }
    });
}

function loadVerifyAllIdCandidates(rowIdx){
    var idx = Number(rowIdx || -1);
    if(idx < 0){ return; }
    var issue = (verifyAllIdsItems || []).find(function(item){
        return Number((item && item.row_idx) || -1) === idx;
    });
    if(!issue){ return; }
    var state = getVerifyAllIdCandidateState(idx);
    state.loading = true;
    state.items = [];
    state.message = 'Ищу кандидатов для замены...';
    renderVerifyAllIdsStatus({ items: verifyAllIdsItems, total: verifyAllIdsItems.length, done: verifyAllIdsItems.length, mismatched: verifyAllIdsItems.length });
    fetchNoIdCandidates({
        name: String(issue.name || ''),
        category: String(issue.category || ''),
        onliner_id: String(issue.onliner_id || ''),
        query: '',
        limit: 12
    }).then(function(items){
        state.loading = false;
        state.items = Array.isArray(items) ? items : [];
        state.message = state.items.length ? 'Выберите правильный ID.' : 'Подходящих кандидатов не найдено.';
        renderVerifyAllIdsStatus({ items: verifyAllIdsItems, total: verifyAllIdsItems.length, done: verifyAllIdsItems.length, mismatched: verifyAllIdsItems.length });
    }).catch(function(){
        state.loading = false;
        state.items = [];
        state.message = 'Ошибка поиска кандидатов.';
        renderVerifyAllIdsStatus({ items: verifyAllIdsItems, total: verifyAllIdsItems.length, done: verifyAllIdsItems.length, mismatched: verifyAllIdsItems.length });
    });
}

function applyVerifyAllIdCandidate(rowIdx, oid, url){
    var idx = Number(rowIdx || -1);
    var finalId = String(oid || '').trim();
    if(idx < 0 || !finalId){ return; }
    var issue = (verifyAllIdsItems || []).find(function(item){
        return Number((item && item.row_idx) || -1) === idx;
    });
    if(!issue){ return; }
    var state = getVerifyAllIdCandidateState(idx);
    state.applyingId = finalId;
    state.message = 'Сохраняю новый ID в постоянный кеш...';
    renderVerifyAllIdsStatus({ items: verifyAllIdsItems, total: verifyAllIdsItems.length, done: verifyAllIdsItems.length, mismatched: verifyAllIdsItems.length });
    fetch('/api/manual-id-confirm-batch', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify({
            source: 'verify_all_ids_replace',
            items: [{
                row_idx: idx,
                name: String(issue.name || ''),
                onliner_id: finalId,
                url: String(url || '')
            }]
        })
    }).then(function(r){ return r.json(); }).then(function(d){
        if(!d || d.status !== 'ok'){
            throw new Error((d && d.message) || 'save_failed');
        }
        verifyAllIdsItems = (verifyAllIdsItems || []).filter(function(item){
            return Number((item && item.row_idx) || -1) !== idx;
        });
        delete verifyAllIdsCandidateState[String(idx)];
        (mainTableRows || []).forEach(function(row){
            if(Number((row && row[8]) || -1) === idx){
                row[0] = finalId;
            }
        });
        renderVerifyAllIdsStatus({
            items: verifyAllIdsItems,
            total: verifyAllIdsItems.length,
            done: verifyAllIdsItems.length,
            mismatched: verifyAllIdsItems.length,
            message: 'ID обновлен и сохранен в постоянный кеш.'
        });
        if(tblMain && tblMain.ajax && typeof tblMain.ajax.reload === 'function'){
            tblMain.ajax.reload(null, false);
        } else {
            redrawMainTablePreservePage();
        }
        runPreExportQualityCheck();
    }).catch(function(){
        state.applyingId = '';
        state.message = 'Не удалось заменить ID. Попробуйте еще раз.';
        renderVerifyAllIdsStatus({ items: verifyAllIdsItems, total: verifyAllIdsItems.length, done: verifyAllIdsItems.length, mismatched: verifyAllIdsItems.length });
    });
}

function mergeVerifyIdCandidateLists(lists, limit){
    var out = [];
    var seen = {};
    var maxItems = Number(limit || 12);
    (lists || []).forEach(function(items){
        (Array.isArray(items) ? items : []).forEach(function(item){
            var cid = String((item && item.id) || '').trim();
            if(!cid || seen[cid]){ return; }
            seen[cid] = true;
            out.push(item);
        });
    });
    return out.slice(0, Math.max(1, maxItems));
}

function compactVerifyIdCards(rootId){
    var root = document.getElementById(rootId || 'verify-all-ids-results');
    if(!root){ return; }
    root.querySelectorAll('.verify-id-item').forEach(function(card){
        var subs = card.querySelectorAll('.verify-id-sub');
        if(!subs || subs.length < 2){ return; }
        if(subs.length >= 3){
            var metaText = String(subs[0].textContent || '').trim();
            metaText = metaText.replace(/^Текущий ID:\s*/i, 'ID ');
            metaText = metaText.replace(/\s*\|\s*Поставщик:\s*/i, ' · ');
            var reasonText = String(subs[2].textContent || '').replace(/^Причина:\s*/i, '').trim();
            reasonText = reasonText.replace(/\s*\|\s*score:.*$/i, '').trim();
            subs[0].textContent = reasonText ? (metaText + ' · ' + reasonText) : metaText;
            subs[1].innerHTML = subs[1].innerHTML.replace('Onliner по ID:', 'Onliner:');
            for(var i = subs.length - 1; i >= 2; i--){
                subs[i].remove();
            }
        }
        card.querySelectorAll('.verify-id-load-btn, .verify-id-manual-btn, .duplicate-id-load-btn, .duplicate-id-manual-btn').forEach(function(btn){
            btn.style.padding = '5px 10px';
            btn.style.fontSize = '12px';
            btn.style.lineHeight = '1.1';
        });
    });
}

var _renderVerifyAllIdsStatusOriginal = renderVerifyAllIdsStatus;
renderVerifyAllIdsStatus = function(data){
    _renderVerifyAllIdsStatusOriginal(data);
    compactVerifyIdCards('verify-all-ids-results');
};

loadVerifyAllIdCandidates = function(rowIdx){
    var idx = Number(rowIdx || -1);
    if(idx < 0){ return; }
    var issue = (verifyAllIdsItems || []).find(function(item){
        return Number((item && item.row_idx) || -1) === idx;
    });
    if(!issue){ return; }
    var state = getVerifyAllIdCandidateState(idx);
    var basePayload = {
        name: String(issue.name || ''),
        category: String(issue.category || ''),
        onliner_id: String(issue.onliner_id || ''),
        query: '',
        limit: 12
    };
    state.loading = true;
    state.items = [];
    state.message = 'Ищу кандидатов для замены...';
    renderVerifyAllIdsStatus({ items: verifyAllIdsItems, total: verifyAllIdsItems.length, done: verifyAllIdsItems.length, mismatched: verifyAllIdsItems.length });

    var compactQuery = compactBrandModelQuery(basePayload.name);
    var modelQuery = bracketModelQuery(basePayload.name);
    var cpuQuery = processorModelQuery(basePayload.name);
    var requests = [
        fetchNoIdCandidates(basePayload).catch(function(){ return []; })
    ];
    if(compactQuery){
        requests.push(fetchNoIdCandidates({
            name: basePayload.name,
            category: basePayload.category,
            onliner_id: basePayload.onliner_id,
            query: compactQuery,
            limit: 12
        }).catch(function(){ return []; }));
    }
    if(modelQuery && modelQuery !== compactQuery){
        requests.push(fetchNoIdCandidates({
            name: basePayload.name,
            category: basePayload.category,
            onliner_id: basePayload.onliner_id,
            query: modelQuery,
            limit: 12
        }).catch(function(){ return []; }));
    }
    if(cpuQuery && cpuQuery !== compactQuery && cpuQuery !== modelQuery){
        requests.push(fetchNoIdCandidates({
            name: basePayload.name,
            category: basePayload.category,
            onliner_id: basePayload.onliner_id,
            query: cpuQuery,
            limit: 12
        }).catch(function(){ return []; }));
    }

    Promise.all(requests).then(function(results){
        state.loading = false;
        state.items = mergeVerifyIdCandidateLists(results, 12);
        state.message = state.items.length ? 'Выберите правильный ID.' : 'Подходящих кандидатов не найдено.';
        renderVerifyAllIdsStatus({ items: verifyAllIdsItems, total: verifyAllIdsItems.length, done: verifyAllIdsItems.length, mismatched: verifyAllIdsItems.length });
    }).catch(function(){
        state.loading = false;
        state.items = [];
        state.message = 'Ошибка поиска кандидатов.';
        renderVerifyAllIdsStatus({ items: verifyAllIdsItems, total: verifyAllIdsItems.length, done: verifyAllIdsItems.length, mismatched: verifyAllIdsItems.length });
    });
};

function normalizeTextForAiMatch(s){
    return String(s || '')
        .toLowerCase()
        .replace(/[^a-zа-я0-9\\s\\-\\/]/gi, ' ')
            .replace(/\\s+/g, ' ')
        .trim();
}

function tokenListForAiMatch(s){
    var stop = {
        'система':1,'охлаждения':1,'водяного':1,'для':1,'и':1,'the':1,'with':1,
        'black':1,'white':1,'черный':1,'чёрный':1,'белый':1,'argb':1,'rgb':1
    };
    return normalizeTextForAiMatch(s).split(' ').filter(function(t){
        return t && t.length >= 3 && !stop[t];
    });
}

function isAiTextMatch(localName, apiName){
    var lNorm = normalizeTextForAiMatch(localName);
    var aNorm = normalizeTextForAiMatch(apiName);
    if(!lNorm || !aNorm){ return false; }
    if(aNorm.length >= 12 && lNorm.indexOf(aNorm) >= 0){ return true; }
    var lTokens = tokenListForAiMatch(localName);
    var aTokens = tokenListForAiMatch(apiName);
    if(!lTokens.length || !aTokens.length){ return false; }
    var lSet = {};
    lTokens.forEach(function(t){ lSet[t] = 1; });
    var common = 0;
    aTokens.forEach(function(t){ if(lSet[t]){ common += 1; } });
    var overlap = common / Math.max(1, aTokens.length);
    return overlap >= 0.60 && common >= 2;
}

function runPreExportQualityCheck(){
    var card = document.getElementById('quality-check-card');
    var summary = document.getElementById('quality-check-summary');
    var details = document.getElementById('quality-check-details');
    if(card){ card.style.display = 'block'; }
    if(summary){ summary.textContent = 'Проверка качества выполняется...'; }
    if(details){ details.innerHTML = ''; }
    fetch('/api/preexport-quality-check').then(function(r){ return r.json(); }).then(function(d){
        if(!d || d.status !== 'ok'){
            if(summary){ summary.textContent = (d && d.message) ? d.message : 'Не удалось выполнить проверку качества.'; }
            return;
        }
        if(summary){
            summary.textContent = 'Проверено строк: ' + String(d.checked || 0)
                + '. Без ID: ' + String(d.missing_id_count || 0)
                + ', подозрительные цены: ' + String(d.suspicious_price_count || 0)
                + ', дубли: ' + String(d.duplicate_count || 0) + '.';
        }
        var html = '';
        function block(title, arr){
            var items = Array.isArray(arr) ? arr : [];
            if(!items.length){ return '<div style="margin-top:6px;"><b>' + title + ':</b> нет</div>'; }
            return '<div style="margin-top:8px;"><b>' + title + ':</b><ul style="margin:6px 0 0 18px;">'
                + items.map(function(it){ return '<li>' + escapeHtml(String(it)) + '</li>'; }).join('')
                + '</ul></div>';
        }
        html += block('Примеры подозрительных цен', d.suspicious_price_samples || []);
        html += block('Примеры дублей', d.duplicate_samples || []);
        if(details){ details.innerHTML = html; }
    }).catch(function(){
        if(summary){ summary.textContent = 'Ошибка запуска проверки качества.'; }
    });
}

function reapplySavedMarkupsFromQuality(){
    var summary = document.getElementById('quality-check-summary');
    var btn = document.getElementById('quality-reapply-markups-btn');
    if(summary){ summary.textContent = 'Доприменяю сохранённые наценки по категориям...'; }
    if(btn){ btn.disabled = true; }
    fetch('/api/reapply-saved-markups', {
        method: 'POST',
        headers: {'Content-Type':'application/json'},
        body: JSON.stringify({source: 'quality_check'})
    }).then(function(r){
        return r.json().then(function(d){ return {ok:r.ok, data:d}; });
    }).then(function(resp){
        var d = resp.data || {};
        if(!resp.ok || d.status !== 'ok'){
            if(summary){ summary.textContent = d.message || 'Не удалось доприменить наценки.'; }
            if(btn){ btn.disabled = false; }
            return;
        }
        if(summary){
            summary.textContent = 'Наценки доприменены. Обновлено строк: ' + String(d.updated_rows || 0) + '. Перепроверяю...';
        }
        if(tblMain && tblMain.ajax && tblMain.ajax.reload){ tblMain.ajax.reload(null, false); }
        runPreExportQualityCheck();
        if(btn){ btn.disabled = false; }
    }).catch(function(){
        if(summary){ summary.textContent = 'Ошибка доприменения наценок.'; }
        if(btn){ btn.disabled = false; }
    });
}

function getSelectedValues(selectId){
    var sel = document.getElementById(selectId);
    return Array.from(sel.options).filter(function(o){ return o.selected; }).map(function(o){ return o.value; });
}

function getAllValues(selectId){
    var sel = document.getElementById(selectId);
    if(!sel){ return []; }
    return Array.from(sel.options).map(function(o){ return o.value; }).filter(function(v){ return !!v; });
}

function saveUiState(){
    var state = {
        categories: getSelectedValues('markup-categories'),
        percent: document.getElementById('markup-percent').value || '10',
        threshold: document.getElementById('markup-threshold').value || '0',
        minProfit: document.getElementById('markup-min-profit').value || '0',
        noDiscountPercent: document.getElementById('markup-no-discount-percent').value || '0',
        previewPercent: document.getElementById('preview-percent').value || '10',
        previewThreshold: document.getElementById('preview-threshold').value || '0',
        previewMinProfit: document.getElementById('preview-min-profit').value || '0',
        previewNoDiscountPercent: document.getElementById('preview-no-discount-percent').value || '0',
        previewBaseMode: document.getElementById('preview-base-mode').value || 'wholesale',
        supplier: document.getElementById('supplier-select').value || '',
        targetCategory: document.getElementById('preview-target-category').value || '',
        fullTargetCategory: document.getElementById('full-list-target-category').value || ''
    };
    localStorage.setItem(UI_STATE_KEY, JSON.stringify(state));
}

function loadUiState(){
    try{
        return JSON.parse(localStorage.getItem(UI_STATE_KEY) || '{}');
    }catch(e){
        return {};
    }
}

function applySelection(selectId, values){
    var valSet = new Set(values || []);
    var sel = document.getElementById(selectId);
    Array.from(sel.options).forEach(function(o){ o.selected = valSet.has(o.value); });
}

function formatRelativeSeconds(sec){
    var s = Math.max(0, Number(sec || 0));
    var h = Math.floor(s / 3600);
    var m = Math.floor((s % 3600) / 60);
    if(h > 0){ return h + 'ч ' + m + 'м'; }
    if(m > 0){ return m + 'м'; }
    return 'менее минуты';
}

function formatTs(ts){
    var n = Number(ts || 0);
    if(!n){ return '—'; }
    var d = new Date(n * 1000);
    if(isNaN(d.getTime())){ return '—'; }
    return d.toLocaleString('ru-RU');
}

function setAutoRefreshPoll(active){
    if(autoRefreshPollTimer){
        clearInterval(autoRefreshPollTimer);
        autoRefreshPollTimer = null;
    }
    if(active){
        autoRefreshPollTimer = setInterval(loadAutoRefreshSettings, 10000);
    }
}

function loadAutoRefreshSettings(){
    fetch('/api/auto-refresh-settings').then(function(r){ return r.json(); }).then(function(d){
        autoRefreshUiLock = true;
        var enabledEl = document.getElementById('auto-refresh-enabled');
        var intervalEl = document.getElementById('auto-refresh-interval');
        var noteEl = document.getElementById('auto-refresh-note');
        if(enabledEl){ enabledEl.checked = !!d.enabled; }
        if(intervalEl){ intervalEl.value = String(d.interval_hours || 12); }
        var statusMap = {
            'running': 'в работе',
            'ok': 'выполнено',
            'error': 'ошибка',
            'idle': 'ожидание'
        };
        var statusTxt = statusMap[String(d.last_status || 'idle')] || String(d.last_status || 'idle');
        var nextTxt = d.enabled ? ('следующий запуск через ' + formatRelativeSeconds(d.next_in_sec || 0)) : 'выключено';
        var lastTxt = formatTs(d.last_run_ts || 0);
        var cnt = Number(d.last_count || 0);
        var msg = String(d.last_message || '').trim();
        if(noteEl){
            noteEl.textContent = 'Статус: ' + statusTxt + '. Последний запуск: ' + lastTxt
                + '. Обновлено ID: ' + cnt + '. ' + nextTxt + (msg ? ('. ' + msg) : '');
        }
        autoRefreshUiLock = false;
    }).catch(function(){
        autoRefreshUiLock = false;
        var noteEl = document.getElementById('auto-refresh-note');
        if(noteEl){ noteEl.textContent = 'Не удалось получить статус автообновления.'; }
    });
}

function saveAutoRefreshSettings(){
    if(autoRefreshUiLock){ return; }
    var enabledEl = document.getElementById('auto-refresh-enabled');
    var intervalEl = document.getElementById('auto-refresh-interval');
    var enabled = !!(enabledEl && enabledEl.checked);
    var interval = Number((intervalEl && intervalEl.value) || 12);
    fetch('/api/auto-refresh-settings', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify({enabled: enabled, interval_hours: interval})
    }).then(function(r){ return r.json(); }).then(function(d){
        if(d.status !== 'ok'){
            var noteEl = document.getElementById('auto-refresh-note');
            if(noteEl){ noteEl.textContent = d.message || 'Не удалось сохранить настройки автообновления.'; }
            return;
        }
        loadAutoRefreshSettings();
    }).catch(function(){
        var noteEl = document.getElementById('auto-refresh-note');
        if(noteEl){ noteEl.textContent = 'Ошибка сохранения настроек автообновления.'; }
    });
}

function initMarkupUI(){
    var state = loadUiState();
    if(state.percent){ document.getElementById('markup-percent').value = state.percent; }
    if(state.threshold){ document.getElementById('markup-threshold').value = state.threshold; }
    if(state.minProfit){ document.getElementById('markup-min-profit').value = state.minProfit; }
    if(state.noDiscountPercent){ document.getElementById('markup-no-discount-percent').value = state.noDiscountPercent; }
    if(state.previewPercent){ document.getElementById('preview-percent').value = state.previewPercent; }
    if(state.previewThreshold){ document.getElementById('preview-threshold').value = state.previewThreshold; }
    if(state.previewMinProfit){ document.getElementById('preview-min-profit').value = state.previewMinProfit; }
    if(state.previewNoDiscountPercent){ document.getElementById('preview-no-discount-percent').value = state.previewNoDiscountPercent; }
    if(state.previewBaseMode){ document.getElementById('preview-base-mode').value = state.previewBaseMode; }
    loadCategoryMarkups();
    loadCategories(state.categories || []);
    loadSuppliers();
    loadAutoRefreshSettings();
    document.getElementById('open-pricing-btn').addEventListener('click', function(){
        document.getElementById('pricing-modal').classList.add('active');
        loadAutoRefreshSettings();
        setAutoRefreshPoll(true);
    });
    ensureQualityCheckCardUI();
    var exportGoogleBtn = document.getElementById('export-google-sheet-btn');
    var exportGoogleBtnDefaultText = exportGoogleBtn ? exportGoogleBtn.textContent : 'В Google Таблицу';
    function startGoogleExportProgress(){
        if(googleExportProgressTimer){ clearInterval(googleExportProgressTimer); googleExportProgressTimer = null; }
        if(exportGoogleBtn){
            exportGoogleBtn.classList.add('is-loading');
            exportGoogleBtn.textContent = 'Подключаюсь к Google Sheets...';
        }
        var ticks = 0;
        googleExportProgressTimer = setInterval(function(){
            ticks += 1;
            if(exportGoogleBtn){
                if(ticks < 4){
                    exportGoogleBtn.textContent = 'Подключаюсь к Google Sheets' + '.'.repeat((ticks % 3) + 1);
                } else {
                    exportGoogleBtn.textContent = 'Записываю в лист' + '.'.repeat((ticks % 3) + 1);
                }
            }
        }, 420);
    }
    function finishGoogleExportProgress(isSuccess, text){
        if(googleExportProgressTimer){ clearInterval(googleExportProgressTimer); googleExportProgressTimer = null; }
        if(exportGoogleBtn){
            exportGoogleBtn.classList.remove('is-loading');
            exportGoogleBtn.textContent = text || (isSuccess ? 'Готово' : 'Ошибка выгрузки');
        }
        setTimeout(function(){
            if(exportGoogleBtn){
                exportGoogleBtn.textContent = exportGoogleBtnDefaultText || 'В Google Таблицу';
            }
        }, isSuccess ? 1600 : 2600);
    }
    if(exportGoogleBtn){
        exportGoogleBtn.addEventListener('click', function(){
            if(!confirm('Записать текущий сводный прайс в Google Таблицу по настройкам (ссылка/ID и имя листа)? Данные на этом листе будут полностью заменены.')){ return; }
            exportGoogleBtn.disabled = true;
            startGoogleExportProgress();
            fetch('/api/export-google-sheets', {method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify({})})
            .then(function(r){ return r.json(); })
            .then(function(d){
                exportGoogleBtn.disabled = false;
                if(d && d.status === 'ok'){
                    finishGoogleExportProgress(true, 'Готово: выгрузка в Google Sheets завершена.');
                    alert(d.message || 'Готово.');
                } else {
                    finishGoogleExportProgress(false, (d && d.message) || 'Ошибка выгрузки.');
                    alert((d && d.message) || 'Ошибка выгрузки.');
                }
            }).catch(function(){
                exportGoogleBtn.disabled = false;
                finishGoogleExportProgress(false, 'Ошибка сети при выгрузке в Google Таблицу.');
                alert('Ошибка сети при выгрузке в Google Таблицу.');
            });
        });
    }
    document.getElementById('run-quality-check-btn').addEventListener('click', runPreExportQualityCheck);
    var qualityToggleBtn = document.getElementById('toggle-quality-check-card-btn');
    if(qualityToggleBtn){
        qualityToggleBtn.onclick = function(){
            toggleQualityCheckCard();
        };
    }
    document.getElementById('quality-reapply-markups-btn').addEventListener('click', reapplySavedMarkupsFromQuality);
    document.getElementById('close-pricing-btn').addEventListener('click', function(){
        document.getElementById('pricing-modal').classList.remove('active');
        setAutoRefreshPoll(false);
    });
    document.getElementById('pricing-modal').addEventListener('click', function(e){
        if(e.target.id === 'pricing-modal'){
            this.classList.remove('active');
            setAutoRefreshPoll(false);
        }
    });
    document.getElementById('auto-refresh-enabled').addEventListener('change', saveAutoRefreshSettings);
    document.getElementById('auto-refresh-interval').addEventListener('change', saveAutoRefreshSettings);
    document.getElementById('apply-markup-btn').addEventListener('click', applyMarkup);
    document.getElementById('supplier-select').addEventListener('change', loadSupplierCategories);
    document.getElementById('hide-cats-btn').addEventListener('click', function(){ setCategoryVisibility(true); });
    document.getElementById('show-cats-btn').addEventListener('click', function(){ setCategoryVisibility(false); });
    document.getElementById('markup-categories').addEventListener('change', function(){ applyStoredPercentForSelection(); syncPreviewModalCategorySelector(); saveUiState(); requestPreview(); });
    document.getElementById('markup-percent').addEventListener('input', function(){ saveUiState(); requestPreview(); });
    document.getElementById('markup-threshold').addEventListener('input', function(){ saveUiState(); requestPreview(); });
    document.getElementById('markup-min-profit').addEventListener('input', function(){ saveUiState(); requestPreview(); });
    document.getElementById('markup-no-discount-percent').addEventListener('input', function(){ saveUiState(); requestPreview(); });
    document.getElementById('preview-move-btn').addEventListener('click', moveSelectedItemsToCategory);
    document.getElementById('preview-target-category').addEventListener('change', saveUiState);
    document.getElementById('target-category-search').addEventListener('input', function(){ filterTargetCategories('preview-target-category', this.value); });
    document.getElementById('close-full-list-btn').addEventListener('click', closeFullListModal);
    document.getElementById('full-list-modal').addEventListener('click', function(e){
        if(e.target.id === 'full-list-modal'){ closeFullListModal(); }
    });
    document.getElementById('open-autosort-btn').addEventListener('click', openAutoSortModal);
    document.getElementById('close-autosort-btn').addEventListener('click', closeAutoSortModal);
    document.getElementById('autosort-modal').addEventListener('click', function(e){
        if(e.target.id === 'autosort-modal'){ closeAutoSortModal(); }
    });
    var openNtechChecksBtn = document.getElementById('open-ntech-checks-modal-btn');
    var closeNtechChecksBtn = document.getElementById('close-ntech-checks-modal-btn');
    var ntechChecksModal = document.getElementById('ntech-checks-modal');
    if(openNtechChecksBtn && ntechChecksModal){
        openNtechChecksBtn.addEventListener('click', function(){
            ntechChecksModal.classList.add('active');
        });
    }
    if(closeNtechChecksBtn && ntechChecksModal){
        closeNtechChecksBtn.addEventListener('click', function(){
            ntechChecksModal.classList.remove('active');
        });
    }
    if(ntechChecksModal){
        ntechChecksModal.addEventListener('click', function(e){
            if(e.target.id === 'ntech-checks-modal'){
                ntechChecksModal.classList.remove('active');
            }
        });
        document.addEventListener('keydown', function(e){
            if(e.key === 'Escape' && ntechChecksModal.classList.contains('active')){
                ntechChecksModal.classList.remove('active');
            }
        });
    }
    document.getElementById('autosort-select-all-btn').addEventListener('click', function(){
        autoSortItems.forEach(function(it){
            var key = String((it || {}).item_key || '');
            if(key){ autoSortSelectedKeys[key] = true; }
        });
        renderAutoSortRows();
    });
    document.getElementById('autosort-clear-btn').addEventListener('click', function(){
        autoSortSelectedKeys = {};
        renderAutoSortRows();
    });
    document.getElementById('autosort-apply-btn').addEventListener('click', applyAutoSortSelection);
    document.getElementById('autosort-body').addEventListener('click', function(e){
        var tr = e.target.closest('tr[data-key]');
        if(!tr){ return; }
        var key = String(tr.getAttribute('data-key') || '');
        if(!key){ return; }
        if(e.target && e.target.matches('input[type="checkbox"]')){
            if(e.target.checked){ autoSortSelectedKeys[key] = true; }
            else { delete autoSortSelectedKeys[key]; }
            renderAutoSortRows();
            return;
        }
        if(autoSortSelectedKeys[key]){ delete autoSortSelectedKeys[key]; }
        else { autoSortSelectedKeys[key] = true; }
        renderAutoSortRows();
    });
    document.getElementById('full-list-target-category').addEventListener('change', saveUiState);
    document.getElementById('full-target-category-search').addEventListener('input', function(){ filterTargetCategories('full-list-target-category', this.value); });
    document.getElementById('full-list-move-btn').addEventListener('click', moveSelectedItemsToCategoryFromFullList);
    document.getElementById('open-preview-modal-btn').addEventListener('click', openPreviewModal);
    document.getElementById('close-preview-btn').addEventListener('click', closePreviewModal);
    document.getElementById('preview-modal').addEventListener('click', function(e){
        if(e.target.id === 'preview-modal'){ closePreviewModal(); }
    });
    document.getElementById('preview-percent').addEventListener('input', function(){
        saveUiState();
        renderPreviewModalRows();
    });
    document.getElementById('preview-threshold').addEventListener('input', function(){
        saveUiState();
        renderPreviewModalRows();
    });
    document.getElementById('preview-min-profit').addEventListener('input', function(){
        saveUiState();
        renderPreviewModalRows();
    });
    document.getElementById('preview-no-discount-percent').addEventListener('input', function(){
        saveUiState();
        renderPreviewModalRows();
    });
    document.getElementById('preview-base-mode').addEventListener('change', function(){
        saveUiState();
        renderPreviewModalRows();
    });
    document.getElementById('preview-apply-btn').addEventListener('click', function(){
        var previewSelected = getSelectedValues('preview-modal-categories');
        if(previewSelected.length){
            applySelection('markup-categories', previewSelected);
            syncPreviewModalCategorySelector();
        }
        document.getElementById('markup-percent').value = document.getElementById('preview-percent').value || '0';
        document.getElementById('markup-threshold').value = document.getElementById('preview-threshold').value || '0';
        document.getElementById('markup-min-profit').value = document.getElementById('preview-min-profit').value || '0';
        document.getElementById('markup-no-discount-percent').value = document.getElementById('preview-no-discount-percent').value || '0';
        applyMarkup();
    });
    document.getElementById('refresh-market-btn').addEventListener('click', startMarketRefresh);
    document.getElementById('open-offers-btn').addEventListener('click', openOffersModal);
    document.getElementById('close-offers-btn').addEventListener('click', closeOffersModal);
    document.getElementById('offers-modal').addEventListener('click', function(e){
        if(e.target.id === 'offers-modal'){ closeOffersModal(); }
    });
    document.querySelector('#preview-full-table tbody').addEventListener('click', function(e){
        var tr = e.target.closest('tr[data-row-idx]');
        if(!tr){ return; }
        setSelectedPreviewRow(parseInt(tr.getAttribute('data-row-idx') || '-1', 10));
    });
    document.getElementById('preview-modal-categories').addEventListener('change', function(){
        applyStoredPercentForPreviewSelection();
        loadPreviewModalItems();
    });
    loadTargetCategoryCatalog();
}

function loadCategoryMarkups(){
    fetch('/api/category-markups').then(function(r){ return r.json(); }).then(function(d){
        categoryMarkups = d.markups || {};
        renderCategoryMarkupTable();
        applyStoredPercentForSelection();
        applyStoredPercentForPreviewSelection();
    }).catch(function(){});
}

function getCategoryMarkupConfigJs(category){
    var raw = (categoryMarkups || {})[category];
    if(raw && typeof raw === 'object' && !Array.isArray(raw)){
        var percent = Number(raw.percent);
        var threshold = Number(raw.threshold);
        var minProfit = Number(raw.min_profit);
        var noDiscountPercent = Number(raw.no_discount_percent);
        return {
            percent: isNaN(percent) ? null : percent,
            threshold: isNaN(threshold) ? 0 : threshold,
            min_profit: isNaN(minProfit) ? 0 : minProfit,
            no_discount_percent: isNaN(noDiscountPercent) ? 0 : noDiscountPercent,
            base_mode: String(raw.base_mode || 'wholesale')
        };
    }
    var percentLegacy = Number(raw);
    return {
        percent: isNaN(percentLegacy) ? null : percentLegacy,
        threshold: 0,
        min_profit: 0,
        no_discount_percent: 0,
        base_mode: 'wholesale'
    };
}

function renderCategoryMarkupTable(){
    var tbody = document.querySelector('#category-markup-table tbody');
    if(!tbody){ return; }
    var names = Object.keys(categoryMarkups || {}).sort(compareCategoriesByUiOrder);
    tbody.innerHTML = '';
    if(!names.length){
        var trEmpty = document.createElement('tr');
        trEmpty.innerHTML = '<td colspan="6" style="color:#6b7280;">Наценки по категориям пока не заданы.</td>';
        tbody.appendChild(trEmpty);
        return;
    }
    names.forEach(function(name){
        var tr = document.createElement('tr');
        var cfg = getCategoryMarkupConfigJs(name);
        var val = Number(cfg.percent);
        var baseMap = {
            wholesale: 'Опт',
            onliner_min: 'Onliner Мин',
            onliner_avg: 'Onliner Ср',
            onliner_max: 'Onliner Макс'
        };
        tr.innerHTML = '<td>' + name + '</td>'
            + '<td>' + (isNaN(val) ? '' : val.toFixed(2)) + '</td>'
            + '<td>' + Number(cfg.threshold || 0).toFixed(2) + '</td>'
            + '<td>' + Number(cfg.min_profit || 0).toFixed(2) + '</td>'
            + '<td>' + Number(cfg.no_discount_percent || 0).toFixed(2) + '</td>'
            + '<td>' + escapeHtml(baseMap[cfg.base_mode] || 'Опт') + '</td>';
        tbody.appendChild(tr);
    });
}

function compareCategoriesByUiOrder(a, b){
    var idxA = getCategoryOrderIndex(a);
    var idxB = getCategoryOrderIndex(b);
    if(idxA !== idxB){ return idxA - idxB; }
    return String(a).localeCompare(String(b), 'ru');
}

function getCategoryOrderIndex(name){
    var sel = document.getElementById('markup-categories');
    if(sel && sel.options && sel.options.length){
        for(var i=0;i<sel.options.length;i++){
            if(sel.options[i].value === name){ return i; }
        }
        return 10000;
    }
    var p = CATEGORY_PRIORITY_JS.indexOf(name);
    if(p >= 0){ return p; }
    return 10000;
}

function applyStoredPercentForSelection(){
    var selected = getSelectedValues('markup-categories');
    if(!selected.length){ return; }
    var configs = selected.map(getCategoryMarkupConfigJs).filter(function(v){ return v && v.percent !== null; });
    var vals = configs.map(function(v){ return v.percent; });
    if(!vals.length){ return; }
    var allSame = vals.every(function(v){ return Number(v) === Number(vals[0]); });
    if(allSame){
        var value = Number(vals[0]);
        document.getElementById('markup-percent').value = value;
        document.getElementById('preview-percent').value = value;
    }
    var profits = configs.map(function(v){ return Number(v.min_profit || 0); });
    var thresholds = configs.map(function(v){ return Number(v.threshold || 0); });
    var noDiscountPercents = configs.map(function(v){ return Number(v.no_discount_percent || 0); });
    if(thresholds.length && thresholds.every(function(v){ return Number(v) === Number(thresholds[0]); })){
        document.getElementById('markup-threshold').value = Number(thresholds[0]);
        document.getElementById('preview-threshold').value = Number(thresholds[0]);
    }
    if(profits.length && profits.every(function(v){ return Number(v) === Number(profits[0]); })){
        document.getElementById('markup-min-profit').value = Number(profits[0]);
        document.getElementById('preview-min-profit').value = Number(profits[0]);
    }
    if(noDiscountPercents.length && noDiscountPercents.every(function(v){ return Number(v) === Number(noDiscountPercents[0]); })){
        document.getElementById('markup-no-discount-percent').value = Number(noDiscountPercents[0]);
        document.getElementById('preview-no-discount-percent').value = Number(noDiscountPercents[0]);
    }
    var modes = configs.map(function(v){ return v.base_mode || 'wholesale'; });
    if(modes.length && modes.every(function(v){ return v === modes[0]; })){
        document.getElementById('preview-base-mode').value = modes[0];
    }
}

function applyStoredPercentForPreviewSelection(){
    var selected = getSelectedValues('preview-modal-categories');
    if(!selected.length){ selected = getSelectedValues('markup-categories'); }
    if(!selected.length){ return; }
    var configs = selected.map(getCategoryMarkupConfigJs).filter(function(v){ return v && v.percent !== null; });
    var vals = configs.map(function(v){ return v.percent; });
    if(!vals.length){ return; }
    var allSame = vals.every(function(v){ return Number(v) === Number(vals[0]); });
    if(allSame){
        document.getElementById('preview-percent').value = Number(vals[0]);
    }
    var thresholds = configs.map(function(v){ return Number(v.threshold || 0); });
    if(thresholds.length && thresholds.every(function(v){ return Number(v) === Number(thresholds[0]); })){
        document.getElementById('preview-threshold').value = Number(thresholds[0]);
    }
    var profits = configs.map(function(v){ return Number(v.min_profit || 0); });
    var noDiscountPercents = configs.map(function(v){ return Number(v.no_discount_percent || 0); });
    if(profits.length && profits.every(function(v){ return Number(v) === Number(profits[0]); })){
        document.getElementById('preview-min-profit').value = Number(profits[0]);
    }
    if(noDiscountPercents.length && noDiscountPercents.every(function(v){ return Number(v) === Number(noDiscountPercents[0]); })){
        document.getElementById('preview-no-discount-percent').value = Number(noDiscountPercents[0]);
    }
    var modes = configs.map(function(v){ return v.base_mode || 'wholesale'; });
    if(modes.length && modes.every(function(v){ return v === modes[0]; })){
        document.getElementById('preview-base-mode').value = modes[0];
    }
}

function loadCategories(preselected){
    fetch('/api/categories').then(function(r){ return r.json(); }).then(function(d){
        var sel = document.getElementById('markup-categories');
        sel.innerHTML = '';
        var categories = d.categories || [];
        categories.forEach(function(c){
            var o = document.createElement('option');
            o.value = c.name;
            o.textContent = c.name + ' (' + c.count + ')';
            sel.appendChild(o);
        });
        renderCategoryMarkupTable();
        var available = new Set(categories.map(function(c){ return c.name; }));
        var desired = (preselected && preselected.length) ? preselected : (loadUiState().categories || []);
        desired = desired.filter(function(name){ return available.has(name); });
        applySelection('markup-categories', desired);
        syncPreviewModalCategorySelector();
        applyStoredPercentForSelection();
        saveUiState();
        document.getElementById('markup-note').textContent = categories.length
            ? 'Выберите категории и процент. Будут пересчитаны колонки "РРЦ" и "Цена без скидки".'
            : 'Категории не найдены.';
        requestPreview();
    }).catch(function(){
        document.getElementById('markup-note').textContent = 'Не удалось загрузить категории.';
    });
}

function syncPreviewModalCategorySelector(){
    var src = document.getElementById('markup-categories');
    var dst = document.getElementById('preview-modal-categories');
    if(!src || !dst){ return; }
    dst.innerHTML = '';
    Array.from(src.options).forEach(function(o){
        var opt = document.createElement('option');
        opt.value = o.value;
        opt.textContent = o.textContent;
        opt.selected = !!o.selected;
        dst.appendChild(opt);
    });
}

function requestPreview(){
    if(previewTimer){ clearTimeout(previewTimer); }
    previewTimer = setTimeout(function(){
        loadPreviewItems();
        if(document.getElementById('preview-modal').classList.contains('active')){
            loadPreviewModalItems();
        }
    }, 220);
}

function loadPreviewItems(){
    var categories = getSelectedValues('markup-categories');
    var sel = document.getElementById('preview-items');
    var note = document.getElementById('preview-items-note');
    sel.innerHTML = '';
    if(!categories.length){
        note.textContent = 'Выберите категории выше, чтобы увидеть товары этих категорий.';
        return;
    }
    fetch('/api/category-preview-items', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify({categories: categories, limit: 10000})
    }).then(function(r){ return r.json(); }).then(function(d){
        var items = d.items || [];
        items.forEach(function(it){
            var o = document.createElement('option');
            o.value = it.key;
            o.textContent = '[' + it.category + '] ' + it.name + ' (' + it.supplier + ')';
            sel.appendChild(o);
        });
        note.textContent = 'Найдено товаров: ' + items.length + '. Можно выделять несколько (Shift/Ctrl) и переносить в новую категорию.';
    }).catch(function(){
        note.textContent = 'Не удалось загрузить товары выбранных категорий.';
    });
}

function applyMarkup(){
    var btn = document.getElementById('apply-markup-btn');
    var percent = parseFloat(document.getElementById('markup-percent').value);
    var threshold = parseFloat(document.getElementById('markup-threshold').value);
    var minProfit = parseFloat(document.getElementById('markup-min-profit').value);
    var noDiscountPercent = parseFloat(document.getElementById('markup-no-discount-percent').value);
    if(isNaN(percent) || percent < 0){
        document.getElementById('markup-note').textContent = 'Укажите корректный процент наценки (>= 0).';
        return;
    }
    if(isNaN(threshold) || threshold < 0){ threshold = 0; }
    if(isNaN(minProfit) || minProfit < 0){ minProfit = 0; }
    if(isNaN(noDiscountPercent) || noDiscountPercent < 0){ noDiscountPercent = 0; }
    var selected = getSelectedValues('markup-categories');
    if(!selected.length){
        document.getElementById('markup-note').textContent = 'Выберите хотя бы одну категорию.';
        return;
    }
    saveUiState();
    var baseMode = document.getElementById('preview-base-mode').value || 'wholesale';
    btn.disabled = true;
    btn.textContent = 'Применение...';
    btn.classList.add('pulse');
    fetch('/api/apply-markup', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify({categories: selected, percent: percent, threshold: threshold, min_profit: minProfit, no_discount_percent: noDiscountPercent, base_mode: baseMode})
    }).then(function(r){ return r.json(); }).then(function(d){
        if(d.status === 'ok'){
            document.getElementById('markup-note').textContent =
                'Готово. Обновлено: ' + d.updated + ' из ' + (d.eligible || 0) +
                ' подходящих (всего в прайсе: ' + (d.total || 0) + '). База: ' + (d.base_mode || 'wholesale')
                + '. Порог опта: ' + Number(d.threshold || 0).toFixed(2)
                + '. Мин. прибыль: ' + Number(d.min_profit || 0).toFixed(2)
                + '. Без скидки: ' + Number(d.no_discount_percent || 0).toFixed(2) + '%.';
            selected.forEach(function(c){
                categoryMarkups[c] = {
                    percent: d.percent,
                    threshold: d.threshold || 0,
                    min_profit: d.min_profit || 0,
                    no_discount_percent: d.no_discount_percent || 0,
                    base_mode: d.base_mode || 'wholesale'
                };
            });
            document.getElementById('preview-percent').value = d.percent;
            document.getElementById('preview-threshold').value = d.threshold || 0;
            document.getElementById('preview-min-profit').value = d.min_profit || 0;
            document.getElementById('preview-no-discount-percent').value = d.no_discount_percent || 0;
            renderCategoryMarkupTable();
            if(tblMain){ tblMain.ajax.reload(null, false); }
            loadCategories(selected);
            if(document.getElementById('preview-modal').classList.contains('active')){
                loadPreviewModalItems();
            }
        } else {
            document.getElementById('markup-note').textContent = d.message || 'Ошибка применения наценки.';
        }
    }).catch(function(){
        document.getElementById('markup-note').textContent = 'Ошибка связи с сервером.';
    }).finally(function(){
        btn.disabled = false;
        btn.classList.remove('pulse');
        btn.textContent = 'Применить Наценку';
        requestPreview();
    });
}

function loadSuppliers(){
    fetch('/api/suppliers').then(function(r){ return r.json(); }).then(function(d){
        var sel = document.getElementById('supplier-select');
        sel.innerHTML = '';
        var state = loadUiState();
        (d.suppliers || []).forEach(function(s){
            var o = document.createElement('option');
            o.value = s;
            o.textContent = s;
            sel.appendChild(o);
        });
        if(sel.options.length){
            var chosen = state.supplier || sel.options[0].value;
            for(var i=0;i<sel.options.length;i++){
                sel.options[i].selected = (sel.options[i].value === chosen);
            }
            loadSupplierCategories();
        } else {
            document.getElementById('visibility-note').textContent = 'Поставщики не найдены.';
        }
    }).catch(function(){
        document.getElementById('visibility-note').textContent = 'Не удалось загрузить поставщиков.';
    });
}

function loadSupplierCategories(){
    var selSup = document.getElementById('supplier-select');
    if(!selSup.value){ return; }
    saveUiState();
    fetch('/api/supplier-categories?supplier=' + encodeURIComponent(selSup.value))
    .then(function(r){ return r.json(); })
    .then(function(d){
        var sel = document.getElementById('supplier-categories');
        sel.innerHTML = '';
        (d.categories || []).forEach(function(c){
            var o = document.createElement('option');
            o.value = c.name;
            o.textContent = (c.hidden ? '[скрыто] ' : '') + c.name + ' (' + c.count + ')';
            sel.appendChild(o);
        });
        document.getElementById('visibility-note').textContent = 'Скрытые категории помечены префиксом [скрыто].';
    }).catch(function(){
        document.getElementById('visibility-note').textContent = 'Не удалось загрузить категории поставщика.';
    });
}

function setCategoryVisibility(hidden){
    var sup = document.getElementById('supplier-select').value;
    if(!sup){
        document.getElementById('visibility-note').textContent = 'Выберите поставщика.';
        return;
    }
    var sel = document.getElementById('supplier-categories');
    var categories = Array.from(sel.options).filter(function(o){ return o.selected; }).map(function(o){ return o.value; });
    if(!categories.length){
        document.getElementById('visibility-note').textContent = 'Выберите хотя бы одну категорию.';
        return;
    }
    fetch('/api/category-visibility', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify({supplier: sup, categories: categories, hidden: hidden})
    }).then(function(r){ return r.json(); }).then(function(d){
        if(d.status === 'ok'){
            document.getElementById('visibility-note').textContent = hidden ? 'Категории скрыты.' : 'Категории показаны.';
            loadSupplierCategories();
            loadCategories(getSelectedValues('markup-categories'));
            if(tblMain){ tblMain.ajax.reload(null, false); }
        } else {
            document.getElementById('visibility-note').textContent = d.message || 'Ошибка изменения видимости.';
        }
    }).catch(function(){
        document.getElementById('visibility-note').textContent = 'Ошибка связи с сервером.';
    });
}

function loadTargetCategoryCatalog(){
    fetch('/api/category-catalog').then(function(r){ return r.json(); }).then(function(d){
        var state = loadUiState();
        categoryCatalog = (d.categories || []).map(function(c){ return (typeof c === 'string') ? c : c.name; });
        fillTargetCategorySelect('preview-target-category', categoryCatalog, state.targetCategory || '');
        fillTargetCategorySelect('full-list-target-category', categoryCatalog, state.fullTargetCategory || '');
        saveUiState();
    });
}

function fillTargetCategorySelect(selectId, categories, selected){
    var sel = document.getElementById(selectId);
    if(!sel){ return; }
    sel.innerHTML = '';
    categories.forEach(function(c){
        var o = document.createElement('option');
        var val = (typeof c === 'string') ? c : c.name;
        o.value = val;
        o.textContent = val;
        if(val === selected){ o.selected = true; }
        sel.appendChild(o);
    });
    if(!sel.value && sel.options.length){ sel.options[0].selected = true; }
}

function filterTargetCategories(selectId, query){
    var selected = document.getElementById(selectId).value || '';
    var q = String(query || '').trim().toLowerCase();
    var filtered = categoryCatalog.filter(function(name){
        return !q || name.toLowerCase().indexOf(q) >= 0;
    });
    fillTargetCategorySelect(selectId, filtered, selected);
}

function moveSelectedItemsToCategory(){
    var items = getSelectedValues('preview-items');
    var target = document.getElementById('preview-target-category').value;
    if(!items.length){
        document.getElementById('preview-items-note').textContent = 'Выберите хотя бы один товар в списке.';
        return;
    }
    if(!target){
        document.getElementById('preview-items-note').textContent = 'Выберите целевую категорию.';
        return;
    }
    fetch('/api/category-override-bulk', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify({item_keys: items, target_category: target})
    }).then(function(r){ return r.json(); }).then(function(d){
        if(d.status === 'ok'){
            document.getElementById('preview-items-note').textContent = 'Перенесено товаров: ' + d.updated + '. Изменение сохранено постоянно.';
            loadCategories(getSelectedValues('markup-categories'));
            loadSupplierCategories();
            loadTargetCategoryCatalog();
            loadFullListItems();
            requestPreview();
            if(tblMain){ tblMain.ajax.reload(null, false); }
        } else {
            document.getElementById('preview-items-note').textContent = d.message || 'Не удалось перенести категории.';
        }
    }).catch(function(){
        document.getElementById('preview-items-note').textContent = 'Ошибка связи с сервером.';
    });
}

function openFullListModal(){
    document.getElementById('full-list-modal').classList.add('active');
    loadFullListItems();
}

function closeFullListModal(){
    document.getElementById('full-list-modal').classList.remove('active');
}

function openAutoSortModal(){
    document.getElementById('autosort-modal').classList.add('active');
    loadAutoSortPreview();
}

function closeAutoSortModal(){
    document.getElementById('autosort-modal').classList.remove('active');
}

function loadAutoSortPreview(){
    var note = document.getElementById('autosort-note');
    var tbody = document.getElementById('autosort-body');
    var categories = getSelectedValues('markup-categories');
    autoSortItems = [];
    autoSortSelectedKeys = {};
    if(tbody){
        tbody.innerHTML = '<tr><td colspan="7" style="color:#64748b;">Собираю предложения...</td></tr>';
    }
    if(note){ note.textContent = 'Собираю AI-предложения по категориям...'; }
    fetch('/api/category-autosort-preview', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify({categories: categories})
    }).then(function(r){ return r.json(); }).then(function(d){
        autoSortItems = Array.isArray(d.items) ? d.items : [];
        autoSortItems.forEach(function(it){
            var key = String((it || {}).item_key || '');
            if(key){ autoSortSelectedKeys[key] = true; }
        });
        renderAutoSortRows();
        if(note){
            note.textContent = 'Найдено предложений: ' + autoSortItems.length
                + '. Проверено строк: ' + Number(d.checked || 0)
                + '. Пропущено (ручные/скрытые/без ID): ' + Number(d.skipped || 0)
                + '. AI проверил: ' + Number(d.ai_checked || 0)
                + ', AI предложил: ' + Number(d.ai_suggested || 0)
                + (Number(d.ai_unavailable || 0) > 0 ? ', AI ключ не задан.' : '')
                + '.';
        }
    }).catch(function(){
        autoSortItems = [];
        autoSortSelectedKeys = {};
        renderAutoSortRows();
        if(note){ note.textContent = 'Не удалось собрать предложения автосортировки.'; }
    });
}

function renderAutoSortRows(){
    var tbody = document.getElementById('autosort-body');
    if(!tbody){ return; }
    tbody.innerHTML = '';
    if(!autoSortItems.length){
        tbody.innerHTML = '<tr><td colspan="7" style="color:#64748b;">Нет предложений. Либо все уже отсортировано, либо для выбранных категорий нет совпадений по ID.</td></tr>';
        return;
    }
    autoSortItems.forEach(function(it){
        var key = String((it || {}).item_key || '');
        var checked = !!autoSortSelectedKeys[key];
        var tr = document.createElement('tr');
        tr.setAttribute('data-key', key);
        if(checked){ tr.classList.add('autosort-row-selected'); }
        var source = String(it.source || 'id');
        var affectedRows = Number(it.affected_rows || 1);
        tr.innerHTML = ''
            + '<td class="autosort-check-cell"><input type="checkbox" ' + (checked ? 'checked' : '') + '></td>'
            + '<td>' + escapeHtml(String(it.onliner_id || '')) + '</td>'
            + '<td>' + escapeHtml(String(it.name || ''))
            + '<div style="color:#64748b;font-size:12px;margin-top:3px;">строк: ' + affectedRows + '</div></td>'
            + '<td>' + escapeHtml(String(it.supplier || '')) + '</td>'
            + '<td>' + escapeHtml(String(it.current_category || '')) + '</td>'
            + '<td>' + escapeHtml(String(it.target_category || '')) + '</td>'
            + '<td>' + escapeHtml(source) + '</td>';
        tbody.appendChild(tr);
    });
}

function applyAutoSortSelection(){
    var note = document.getElementById('autosort-note');
    var btn = document.getElementById('autosort-apply-btn');
    var selected = [];
    autoSortItems.forEach(function(it){
        var key = String((it || {}).item_key || '');
        if(!key || !autoSortSelectedKeys[key]){ return; }
        selected.push({
            item_key: key,
            target_category: String(it.target_category || '')
        });
    });
    if(!selected.length){
        if(note){ note.textContent = 'Выберите хотя бы одно предложение для применения.'; }
        return;
    }
    var preserveCats = getSelectedValues('markup-categories');
    if(btn){
        btn.disabled = true;
        btn.textContent = 'Применение...';
    }
    if(note){ note.textContent = 'Применяю автосортировку...'; }
    fetch('/api/category-autosort-apply', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify({items: selected})
    }).then(function(r){ return r.json(); }).then(function(d){
        if(d.status !== 'ok'){
            if(note){ note.textContent = d.message || 'Не удалось применить автосортировку.'; }
            return;
        }
        if(note){
            note.textContent = 'Готово. Обновлено ключей: ' + Number(d.updated_keys || 0)
                + ', строк в прайсе: ' + Number(d.updated_rows || 0) + '.';
        }
        loadAutoSortPreview();
        loadCategories(preserveCats);
        loadSupplierCategories();
        loadTargetCategoryCatalog();
        requestPreview();
        if(document.getElementById('preview-modal').classList.contains('active')){
            loadPreviewModalItems();
        }
        if(document.getElementById('full-list-modal').classList.contains('active')){
            loadFullListItems();
        }
        if(tblMain){ tblMain.ajax.reload(null, false); }
    }).catch(function(){
        if(note){ note.textContent = 'Ошибка связи с сервером при автосортировке.'; }
    }).finally(function(){
        if(btn){
            btn.disabled = false;
            btn.textContent = 'Применить Выбранные';
        }
    });
}

function loadFullListItems(){
    var categories = getSelectedValues('markup-categories');
    var note = document.getElementById('full-list-note');
    var listSel = document.getElementById('full-list-items');
    var tbody = document.querySelector('#full-list-table tbody');
    listSel.innerHTML = '';
    tbody.innerHTML = '';
    if(!categories.length){
        note.textContent = 'Выберите категории в блоке наценки, затем нажмите "Смотреть Все".';
        return;
    }
    note.textContent = 'Загрузка полного списка...';
    fetch('/api/category-preview-items', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify({categories: categories, limit: 10000})
    }).then(function(r){ return r.json(); }).then(function(d){
        var items = d.items || [];
        items.forEach(function(it){
            var o = document.createElement('option');
            o.value = it.key;
            o.textContent = '[' + it.category + '] ' + it.name + ' (' + it.supplier + ')';
            listSel.appendChild(o);
            var tr = document.createElement('tr');
            tr.innerHTML = '<td>' + it.category + '</td>'
                + '<td>' + it.name + '</td>'
                + '<td>' + it.supplier + '</td>'
                + '<td>' + (it.price || '') + '</td>'
                + '<td>' + (it.rrc || '') + '</td>';
            tbody.appendChild(tr);
        });
        note.textContent = 'Найдено товаров: ' + items.length + '. Можно выделить позиции и перенести в другую категорию.';
    }).catch(function(){
        note.textContent = 'Не удалось загрузить полный список товаров.';
    });
}

function roundPriceTo90(value){
    var v = Number(value);
    if(!isFinite(v)){ return ''; }
    if(v <= 0){ return 0.00; }
    return Math.floor(v);
}

function ensurePreviewTableLayout(){
    var table = document.getElementById('preview-full-table');
    if(!table){ return; }
    var thead = table.querySelector('thead');
    if(!thead){ return; }
    thead.innerHTML = '<tr>'
        + '<th>OnlinerID</th>'
        + '<th>Категория</th>'
        + '<th>Товар</th>'
        + '<th>Поставщик</th>'
        + '<th>Опт</th>'
        + '<th>Onliner Мин</th>'
        + '<th>Onliner Ср</th>'
        + '<th>Onliner Макс</th>'
        + '<th>РРЦ</th>'
        + '<th>Цена без скидки</th>'
        + '<th>Маржа, %</th>'
        + '<th>Правило</th>'
        + '</tr>';
    var tbody = table.querySelector('tbody');
    if(tbody){
        Array.from(tbody.rows).forEach(function(row){
            while(row.cells.length > 12){
                row.deleteCell(9);
            }
        });
    }
}

function openPreviewModal(){
    var categories = getSelectedValues('markup-categories');
    if(!categories.length){
        document.getElementById('markup-note').textContent = 'Сначала выберите хотя бы одну категорию.';
        return;
    }
    ensurePreviewTableLayout();
    document.getElementById('preview-percent').value = document.getElementById('markup-percent').value || '0';
    document.getElementById('preview-threshold').value = document.getElementById('markup-threshold').value || '0';
    document.getElementById('preview-min-profit').value = document.getElementById('markup-min-profit').value || '0';
    document.getElementById('preview-no-discount-percent').value = document.getElementById('markup-no-discount-percent').value || '0';
    syncPreviewModalCategorySelector();
    applyStoredPercentForPreviewSelection();
    document.getElementById('preview-modal').classList.add('active');
    loadPreviewModalItems();
}

function closePreviewModal(){
    document.getElementById('preview-modal').classList.remove('active');
    if(marketRefreshPollTimer){ clearTimeout(marketRefreshPollTimer); marketRefreshPollTimer = null; }
    var btn = document.getElementById('refresh-market-btn');
    if(btn){ btn.disabled = false; btn.textContent = 'Обновить Цены Onliner'; }
}

function setSelectedPreviewRow(idx){
    selectedPreviewRowIdx = idx;
    document.querySelectorAll('#preview-full-table tbody tr[data-row-idx]').forEach(function(tr){
        tr.classList.toggle('preview-row-selected', Number(tr.getAttribute('data-row-idx')) === idx);
    });
}

function closeOffersModal(){
    document.getElementById('offers-modal').classList.remove('active');
}

function setPreviewModalLoading(isLoading){
    var sheet = document.querySelector('#preview-modal .preview-sheet');
    if(!sheet){ return; }
    sheet.classList.toggle('preview-loading', !!isLoading);
}

function openOffersModal(){
    var note = document.getElementById('offers-note');
    var current = document.getElementById('offers-current-note');
    var tbody = document.getElementById('offers-body');
    var summary = document.getElementById('offers-summary');
    var previewOrder = (previewModalDisplayOrder && previewModalDisplayOrder.length) ? previewModalDisplayOrder : previewModalItems;
    if(selectedPreviewRowIdx < 0 || selectedPreviewRowIdx >= previewOrder.length){
        note.textContent = 'Сначала выберите товар в таблице предпросмотра.';
        document.getElementById('offers-modal').classList.add('active');
        return;
    }
    var item = previewOrder[selectedPreviewRowIdx] || {};
    var oid = String(item.onliner_id || '').trim();
    current.textContent = 'Товар: ' + String(item.name || '') + ' | OnlinerID: ' + (oid || 'нет');
    if(!oid){
        note.textContent = 'У выбранного товара нет OnlinerID.';
        tbody.innerHTML = '<tr><td colspan="7" style="color:#64748b;">Нет данных</td></tr>';
        document.getElementById('offers-modal').classList.add('active');
        return;
    }
    note.textContent = 'Загружаю офферы Onliner...';
    tbody.innerHTML = '<tr><td colspan="7" style="color:#64748b;">Загрузка...</td></tr>';
    summary.innerHTML = ''
        + '<div class="offers-stat"><div class="k">OnlinerID</div><div class="v">' + escapeHtml(oid) + '</div></div>'
        + '<div class="offers-stat"><div class="k">API offers.count</div><div class="v">—</div></div>'
        + '<div class="offers-stat"><div class="k">Позиции в API</div><div class="v">—</div></div>'
        + '<div class="offers-stat"><div class="k">Уникальные магазины</div><div class="v">—</div></div>';
    document.getElementById('offers-modal').classList.add('active');
    fetch('/api/onliner-offers/' + encodeURIComponent(oid))
        .then(function(r){ return r.json(); })
        .then(function(d){
            if(d.status !== 'ok'){ throw new Error(d.message || 'offer load failed'); }
            summary.innerHTML = ''
                + '<div class="offers-stat"><div class="k">OnlinerID</div><div class="v">' + escapeHtml(oid) + '</div></div>'
                + '<div class="offers-stat"><div class="k">API offers.count</div><div class="v">' + Number(d.offers_count || 0) + '</div></div>'
                + '<div class="offers-stat"><div class="k">Позиции в API</div><div class="v">' + Number(d.positions_count || 0) + '</div></div>'
                + '<div class="offers-stat"><div class="k">Уникальные магазины</div><div class="v">' + Number(d.unique_sellers_count || 0) + '</div></div>';
            note.textContent = d.note || 'Проверка офферов выполнена.';
            var rows = Array.isArray(d.offers) ? d.offers : [];
            if(!rows.length){
                tbody.innerHTML = '<tr><td colspan="7" style="color:#64748b;">API не вернул детализацию офферов.</td></tr>';
                return;
            }
            var html = '';
            rows.forEach(function(it){
                var sellerCell = escapeHtml(it.seller_name || '—');
                if(it.seller_url){
                    sellerCell = '<a href="' + escapeHtml(it.seller_url) + '" target="_blank" rel="noopener noreferrer">' + sellerCell + '</a>';
                }
                html += '<tr>'
                    + '<td>' + sellerCell + '</td>'
                    + '<td>' + escapeHtml(it.seller_id || '') + '</td>'
                    + '<td>' + escapeHtml((it.price || it.price === 0) ? Number(it.price).toFixed(2) : '') + '</td>'
                    + '<td>' + escapeHtml(it.warranty || '') + '</td>'
                    + '<td>' + escapeHtml(it.stock || '') + '</td>'
                    + '<td>' + escapeHtml((it.updated_at || '').replace('T', ' ').replace('+03:00', '')) + '</td>'
                    + '<td>' + (it.url ? ('<a href="' + escapeHtml(it.url) + '" target="_blank" rel="noopener noreferrer">Открыть</a>') : '') + '</td>'
                    + '</tr>';
            });
            tbody.innerHTML = html;
        })
        .catch(function(err){
            note.textContent = 'Не удалось загрузить офферы: ' + (err && err.message ? err.message : 'ошибка');
            tbody.innerHTML = '<tr><td colspan="7" style="color:#b91c1c;">Ошибка загрузки офферов.</td></tr>';
        });
}

function loadPreviewModalItems(options){
    options = options || {};
    var marketOnly = !!options.marketOnly;
    var categories = getSelectedValues('preview-modal-categories');
    if(!categories.length){ categories = getSelectedValues('markup-categories'); }
    var note = document.getElementById('preview-modal-note');
    var requestSeq = ++previewModalRequestSeq;
    var previousMarketMap = buildPreviewMarketMap(previewModalItems);
    if(!categories.length){
        previewModalItems = [];
        selectedPreviewRowIdx = -1;
        renderPreviewModalRows();
        note.textContent = 'Выберите хотя бы одну категорию в основном блоке.';
        setPreviewModalLoading(false);
        return;
    }
    setPreviewModalLoading(true);
    note.textContent = marketOnly ? 'Обновляю цены Onliner...' : 'Переключаю категории...';
    var catsPreview = categories.slice(0, 4).join(', ') + (categories.length > 4 ? '...' : '');
    fetch('/api/category-preview-items', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify({categories: categories, limit: 10000, with_market: true, allow_stale_market: true, max_market_checks: 400})
    }).then(function(r){ return r.json(); }).then(function(d){
        if(requestSeq !== previewModalRequestSeq){ return; }
        previewModalItems = applyPreviewMarketTrend((d.items || []), previousMarketMap);
        selectedPreviewRowIdx = -1;
        renderPreviewModalRows({
            onFirstChunk: function(){ setPreviewModalLoading(false); }
        });
        var allMissing = previewModalItems.length > 0 && previewModalItems.every(function(it){
            return !it.market_min && !it.market_avg && !it.market_max;
        });
        var rowsOid = (d.market_rows_with_onliner_id != null && d.market_rows_with_onliner_id !== undefined)
            ? d.market_rows_with_onliner_id : previewModalItems.length;
        var uniqIds = (d.market_unique_onliner_ids != null && d.market_unique_onliner_ids !== undefined)
            ? d.market_unique_onliner_ids : (d.market_checked || 0);
        note.textContent = 'Категории: ' + catsPreview + '. Строк в таблице: ' + previewModalItems.length
            + '. С OnlinerID: ' + rowsOid + ' (уникальных ID в кэше: ' + uniqIds + ')'
            + ', без данных по ID: ' + (d.missing_market_ids || 0)
            + ', без OnlinerID: ' + (d.no_onliner_id || 0)
            + '. Несколько строк с одним ID делят одни и те же рыночные цены.'
            + (allMissing ? ' Сейчас API/кэш не вернули рыночные цены ни по одному товару.' : '');
    }).catch(function(){
        if(requestSeq !== previewModalRequestSeq){ return; }
        note.textContent = 'Не удалось загрузить данные для предпросмотра.';
        setPreviewModalLoading(false);
    });
}

function buildPreviewMarketMap(items){
    var map = {};
    (items || []).forEach(function(it){
        var key = String((it && (it.row_idx || it.row_idx === 0)) ? it.row_idx : (it.onliner_id || '')) + '|' + String((it && it.name) || '');
        if(!key){ return; }
        map[key] = {
            min: Number(it.market_min),
            avg: Number(it.market_avg),
            max: Number(it.market_max),
        };
    });
    return map;
}

function trendByValues(prev, current){
    var p = Number(prev);
    var c = Number(current);
    if(!isFinite(p) || !isFinite(c)){ return ''; }
    if(Math.abs(c - p) < 0.0001){ return 'flat'; }
    return c > p ? 'up' : 'down';
}

function applyPreviewMarketTrend(items, previousMap){
    previousMap = previousMap || {};
    return (items || []).map(function(it){
        var rowIdx = (it && (it.row_idx || it.row_idx === 0)) ? it.row_idx : '';
        var key = String(rowIdx) + '|' + String((it && it.name) || '');
        var prev = previousMap[key] || null;
        var out = Object.assign({}, it || {});
        out.market_trend_min = prev ? trendByValues(prev.min, out.market_min) : '';
        out.market_trend_avg = prev ? trendByValues(prev.avg, out.market_avg) : '';
        out.market_trend_max = prev ? trendByValues(prev.max, out.market_max) : '';
        return out;
    });
}

function formatMarketTrend(trend){
    if(trend === 'up'){ return '<span class="market-trend market-trend-up">↑</span>'; }
    if(trend === 'down'){ return '<span class="market-trend market-trend-down">↓</span>'; }
    if(trend === 'flat'){ return '<span class="market-trend market-trend-flat">→</span>'; }
    return '';
}

function formatMarketCell(value, trend, competitors, wholesale){
    var marketValue = Number(value);
    if(!isFinite(marketValue)){ return ''; }
    var txt = String(value);
    var cnt = Number(competitors || 0);
    var baseWholesale = Number(wholesale);
    var pctClass = 'preview-market-pct-neutral';
    var pctText = '';
    if(isFinite(marketValue) && isFinite(baseWholesale) && baseWholesale > 0){
        var pct = ((marketValue - baseWholesale) / baseWholesale) * 100;
        pctText = (pct >= 0 ? '+' : '') + pct.toFixed(2) + '%';
        if(pct > 0.01){ pctClass = 'preview-market-pct-positive'; }
        else if(pct < -0.01){ pctClass = 'preview-market-pct-negative'; }
    }
    var meta = '';
    if(cnt > 0){
        meta += '<span class="preview-market-badge">' + cnt + '</span>';
    }
    if(pctText){
        meta += '<span class="preview-market-pct ' + pctClass + '">' + pctText + '</span>';
    }
    return '<div class="preview-market-cell">'
        + '<div class="preview-market-value">' + txt + formatMarketTrend(trend) + '</div>'
        + (meta ? '<div class="preview-market-meta">' + meta + '</div>' : '')
        + '</div>';
}

function startMarketRefresh(){
    var categories = getAllValues('preview-modal-categories');
    if(!categories.length){ categories = getAllValues('markup-categories'); }
    var note = document.getElementById('market-refresh-note');
    var btn = document.getElementById('refresh-market-btn');
    if(!categories.length){
        note.textContent = 'Сначала выберите категории для обновления цен.';
        return;
    }
    btn.disabled = true;
    btn.classList.add('is-updating');
    btn.style.setProperty('--progress', '0%');
    btn.textContent = 'Обновление 0%';
    fetch('/api/market-refresh-start', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify({categories: categories})
    }).then(function(r){ return r.json(); }).then(function(d){
        if(d.status === 'started' || d.status === 'already_running'){
            renderMarketRefreshTable({categories:{}});
            pollMarketRefreshStatus();
        } else {
            note.textContent = d.message || 'Не удалось запустить обновление цен.';
            btn.disabled = false;
            btn.classList.remove('is-updating');
            btn.style.setProperty('--progress', '0%');
            btn.textContent = 'Обновить Цены Onliner';
        }
    }).catch(function(){
        note.textContent = 'Ошибка запуска обновления цен.';
        btn.disabled = false;
        btn.classList.remove('is-updating');
        btn.style.setProperty('--progress', '0%');
        btn.textContent = 'Обновить Цены Onliner';
    });
}

function renderMarketRefreshTable(d){
    return;
}

function pollMarketRefreshStatus(){
    if(marketRefreshPollTimer){ clearTimeout(marketRefreshPollTimer); }
    var note = document.getElementById('market-refresh-note');
    var btn = document.getElementById('refresh-market-btn');
    fetch('/api/market-refresh-status').then(function(r){ return r.json(); }).then(function(d){
        var cats = d.categories || {};
        renderMarketRefreshTable(d);
        btn.classList.add('is-updating');
        btn.style.setProperty('--progress', String(d.overall_percent || 0) + '%');
        btn.textContent = 'Обновление ' + String(d.overall_percent || 0) + '%';
        var lines = Object.keys(cats).sort(compareCategoriesByUiOrder).map(function(name){
            var s = cats[name];
            var part = name + ': ' + (s.percent || 0) + '% (' + (s.done || 0) + '/' + (s.total || 0) + ')';
            if((s.errors || 0) > 0){
                part += ', ошибок: ' + (s.errors || 0);
            }
            return part;
        });
        var recentErrors = (d.recent_errors || []).slice(-6);
        var html = 'Обновление кэша Onliner: ' + (d.overall_percent || 0) + '% (' + (d.done || 0) + '/' + (d.total || 0) + '), успешно: ' + (d.success || 0) + ', ошибок: ' + (d.errors || 0) + '.';
        html += '<br><span style="color:#64748b;font-size:11px;">done/total и «Категории: …/…» — по <b>уникальным</b> Onliner ID, не по числу строк в прайсе.</span>';
        if(lines.length){
            html += '<br>Категории: ' + lines.join(' | ');
        }
        if(recentErrors.length){
            html += '<br>Лог ошибок: ' + recentErrors.map(escapeHtml).join(' | ');
        }
        note.innerHTML = html;
        if(d.running){
            marketRefreshPollTimer = setTimeout(pollMarketRefreshStatus, 1200);
            return;
        }
        btn.disabled = false;
        btn.classList.remove('is-updating');
        btn.style.setProperty('--progress', '100%');
        btn.textContent = 'Обновить Цены Onliner';
        var doneHtml = 'Кэш Onliner обработан: ' + (d.done || 0) + ' уникальных ID, успешно: ' + (d.success || 0) + ', ошибок: ' + (d.errors || 0) + '. Обновляю предпросмотр...';
        doneHtml += '<br><span style="color:#64748b;font-size:11px;">Число уникальных ID обычно меньше строк в категории, если у разных позиций один и тот же OnlinerID.</span>';
        if(lines.length){
            doneHtml += '<br>Категории: ' + lines.join(' | ');
        }
        if(recentErrors.length){
            doneHtml += '<br>Лог ошибок: ' + recentErrors.map(escapeHtml).join(' | ');
        }
        note.innerHTML = doneHtml;
        loadPreviewModalItems({marketOnly:true});
    }).catch(function(){
        btn.disabled = false;
        btn.classList.remove('is-updating');
        btn.style.setProperty('--progress', '0%');
        btn.textContent = 'Обновить Цены Onliner';
        note.textContent = 'Ошибка чтения прогресса обновления.';
    });
}

function renderPreviewModalRows(options){
    options = options || {};
    var onFirstChunk = typeof options.onFirstChunk === 'function' ? options.onFirstChunk : null;
    var onComplete = typeof options.onComplete === 'function' ? options.onComplete : null;
    var chunkSize = Number(options.chunkSize);
    if(!isFinite(chunkSize) || chunkSize < 8){ chunkSize = 48; }
    ensurePreviewTableLayout();
    var tbody = document.querySelector('#preview-full-table tbody');
    if(!tbody){ return; }
    var percent = Number(document.getElementById('preview-percent').value);
    var threshold = Number(document.getElementById('preview-threshold').value);
    var minProfit = Number(document.getElementById('preview-min-profit').value);
    var noDiscountPercent = Number(document.getElementById('preview-no-discount-percent').value);
    var baseMode = document.getElementById('preview-base-mode').value || 'wholesale';
    if(!isFinite(percent) || percent < 0){ percent = 0; }
    if(!isFinite(threshold) || threshold < 0){ threshold = 0; }
    if(!isFinite(minProfit) || minProfit < 0){ minProfit = 0; }
    if(!isFinite(noDiscountPercent) || noDiscountPercent < 0){ noDiscountPercent = 0; }
    tbody.innerHTML = '';
    var sortedItems = (previewModalItems || []).slice().sort(function(a, b){
        var av = Number(a && a.price);
        var bv = Number(b && b.price);
        var aOk = isFinite(av);
        var bOk = isFinite(bv);
        if(aOk && bOk && av !== bv){ return av - bv; }
        if(aOk && !bOk){ return -1; }
        if(!aOk && bOk){ return 1; }
        return String((a && a.name) || '').localeCompare(String((b && b.name) || ''), 'ru');
    });
    previewModalDisplayOrder = sortedItems;
    var firstChunkNotified = false;
    function notifyFirstChunk(){
        if(firstChunkNotified){ return; }
        firstChunkNotified = true;
        if(onFirstChunk){ onFirstChunk(); }
    }
    function notifyComplete(){
        if(onComplete){ onComplete(); }
    }
    if(!sortedItems.length){
        notifyFirstChunk();
        notifyComplete();
        return;
    }
    var token = ++previewModalRenderRafToken;
    var i = 0;
    function appendRowAt(rowIdx){
        var it = sortedItems[rowIdx];
        var wholesale = Number(it.price);
        var marketMin = Number(it.market_min);
        var marketAvg = Number(it.market_avg);
        var marketMax = Number(it.market_max);
        var hasMarketMin = isFinite(marketMin);
        var hasMarketAvg = isFinite(marketAvg);
        var hasMarketMax = isFinite(marketMax);
        var hasWholesale = isFinite(wholesale);
        var basePrice = hasWholesale ? wholesale : NaN;
        if(baseMode === 'onliner_min'){
            basePrice = hasMarketMin ? marketMin : NaN;
        } else if(baseMode === 'onliner_avg'){
            basePrice = hasMarketAvg ? marketAvg : NaN;
        } else if(baseMode === 'onliner_max'){
            basePrice = hasMarketMax ? marketMax : NaN;
        }
        var newRrc = '';
        var noDiscountPrice = '';
        var appliedRule = '%';
        if(isFinite(basePrice)){
            var priceByPercent = basePrice * (1 + percent / 100);
            var finalCandidate = priceByPercent;
            if(hasWholesale && threshold > 0 && wholesale <= threshold){
                var priceByMinProfit = wholesale + minProfit;
                if(isFinite(priceByMinProfit) && priceByMinProfit > finalCandidate){
                    finalCandidate = priceByMinProfit;
                    appliedRule = 'Мин. прибыль';
                }
            }
            newRrc = roundPriceTo90(finalCandidate);
            if(isFinite(newRrc)){
                noDiscountPrice = roundPriceTo90(newRrc * (1 + noDiscountPercent / 100));
            }
        }
        var marginPct = '';
        if(isFinite(newRrc) && hasWholesale && wholesale > 0){
            marginPct = ((newRrc - wholesale) / wholesale) * 100;
        }
        var avgCompetitors = Number(it.avg_competitors || 0);
        var minCompetitors = Number(it.min_competitors || 0);
        var rrcColor = '#1d4ed8';
        if(isFinite(newRrc) && hasMarketAvg){
            if(newRrc > marketAvg * 1.15){
                rrcColor = '#dc2626'; // high vs market average
            } else if(avgCompetitors > minCompetitors){
                rrcColor = '#15803d'; // market mostly sits near average
            } else {
                rrcColor = '#ca8a04'; // cautious / close to min zone
            }
        }
        var tr = document.createElement('tr');
        tr.setAttribute('data-row-idx', String(rowIdx));
        var minTxt = formatMarketCell(it.market_min, it.market_trend_min, it.min_competitors, wholesale);
        var avgTxt = formatMarketCell(it.market_avg, it.market_trend_avg, it.avg_competitors, wholesale);
        var maxTxt = formatMarketCell(it.market_max, it.market_trend_max, it.market_offers, wholesale);
        tr.innerHTML = '<td>' + (it.onliner_id || '') + '</td>'
            + '<td>' + it.category + '</td>'
            + '<td>' + it.name + '</td>'
            + '<td>' + it.supplier + '</td>'
            + '<td class="preview-price-cell">' + (it.price || '') + '</td>'
            + '<td>' + minTxt + '</td>'
            + '<td>' + avgTxt + '</td>'
            + '<td>' + maxTxt + '</td>'
            + '<td class="preview-price-cell"><b style="color:' + rrcColor + '">' + (newRrc === '' ? '—' : Number(newRrc).toFixed(2)) + '</b></td>'
            + '<td class="preview-price-cell">' + (noDiscountPrice === '' ? '—' : Number(noDiscountPrice).toFixed(2)) + '</td>'
            + '<td class="preview-price-cell">' + (marginPct === '' ? '' : Number(marginPct).toFixed(2)) + '</td>'
            + '<td class="preview-price-cell">' + escapeHtml(appliedRule) + '</td>';
        tbody.appendChild(tr);
    }
    function step(){
        if(token !== previewModalRenderRafToken){ return; }
        var end = Math.min(i + chunkSize, sortedItems.length);
        for(; i < end; i++){
            appendRowAt(i);
        }
        if(i > 0){
            notifyFirstChunk();
        }
        if(i < sortedItems.length){
            requestAnimationFrame(step);
        } else {
            notifyComplete();
        }
    }
    requestAnimationFrame(step);
}

function moveSelectedItemsToCategoryFromFullList(){
    var items = getSelectedValues('full-list-items');
    var target = document.getElementById('full-list-target-category').value;
    var note = document.getElementById('full-list-note');
    if(!items.length){
        note.textContent = 'Выберите хотя бы один товар в списке.';
        return;
    }
    if(!target){
        note.textContent = 'Выберите целевую категорию.';
        return;
    }
    fetch('/api/category-override-bulk', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify({item_keys: items, target_category: target})
    }).then(function(r){ return r.json(); }).then(function(d){
        if(d.status === 'ok'){
            note.textContent = 'Перенесено товаров: ' + d.updated + '. Изменение сохранено.';
            loadCategories(getSelectedValues('markup-categories'));
            loadSupplierCategories();
            loadTargetCategoryCatalog();
            loadPreviewItems();
            loadFullListItems();
            if(tblMain){ tblMain.ajax.reload(null, false); }
        } else {
            note.textContent = d.message || 'Не удалось перенести выбранные товары.';
        }
    }).catch(function(){
        note.textContent = 'Ошибка связи с сервером.';
    });
}

// ============================================================
// ─── Проверка ID через API ────────────────────────────────────────────────
(function(){
    var startBtn    = document.getElementById('validate-ids-start-btn');
    var startDbBtn  = document.getElementById('validate-ids-db-start-btn');
    var reportBtn   = document.getElementById('validate-ids-report-btn');
    var progressWrap= document.getElementById('validate-ids-progress-wrap');
    var progressBar = document.getElementById('validate-ids-progress-bar');
    var msgEl       = document.getElementById('validate-ids-msg');
    var modal       = document.getElementById('validate-ids-modal');
    var closeBtn1   = document.getElementById('validate-ids-modal-close');
    var closeBtn2   = document.getElementById('validate-ids-modal-close2');
    var tbody       = document.getElementById('validate-modal-tbody');
    var showConfEl  = document.getElementById('vld-show-confirmed');

    var vldStatus   = null;
    var pollTimer   = null;
    var activeValidateMode = 'api';

    function openModal(){ if(modal){ modal.style.display='block'; document.body.style.overflow='hidden'; } }
    function closeModal(){ if(modal){ modal.style.display='none'; document.body.style.overflow=''; } }
    if(closeBtn1) closeBtn1.addEventListener('click', closeModal);
    if(closeBtn2) closeBtn2.addEventListener('click', closeModal);
    if(modal) modal.addEventListener('click', function(e){ if(e.target===modal) closeModal(); });
    document.addEventListener('keydown', function(e){ if(e.key==='Escape') closeModal(); });

    function scoreBadge(sc){
        var bg = sc>=0.95?'#dcfce7':sc>=0.75?'#dbeafe':sc>=0.60?'#fef9c3':'#fee2e2';
        var col= sc>=0.95?'#166534':sc>=0.75?'#1d4ed8':sc>=0.60?'#92400e':'#b91c1c';
        return '<span style="display:inline-block;padding:2px 7px;border-radius:20px;font-weight:700;font-size:12px;background:'+bg+';color:'+col+';">'+(sc||'—')+'</span>';
    }

    function renderModal(){
        var st = vldStatus;
        if(!st || !tbody) return;
        var allRows   = (st.cleared_rows  || []);
        var confRows  = (st.confirmed_rows || []);
        var showConf  = showConfEl && showConfEl.checked;

        var el = function(id){ return document.getElementById(id); };
        var setTxt = function(id,v){ var e=el(id); if(e) e.textContent=v; };
        setTxt('vld-stat-checked',  (st.done||0));
        setTxt('vld-stat-confirmed',(st.confirmed||0));
        setTxt('vld-stat-cleared',  (st.cleared||0));
        setTxt('vld-stat-skipped',  (st.skipped_api||0));
        setTxt('vld-stat-errors',   (st.errors||0));
        var sub = el('validate-modal-subtitle');
        var modeLabel = st.mode_label || 'Onliner API';
        var skippedLabel = st.skipped_label || 'Пропуск = API не ответил, ID не меняли.';
        if(sub) sub.textContent = 'Режим: ' + modeLabel + '. Проверено: ' + (st.done||0) + ' из ' + (st.total||0) + ' товаров с ID (ПЭВМ TGPC пропущены). ' + skippedLabel;

        tbody.innerHTML = '';

        var skipRows = (st.skipped_rows || []);
        if(skipRows.length > 0){
            var hdrS = document.createElement('tr');
            hdrS.innerHTML = '<td colspan="5" style="padding:8px 14px;background:#fef9c3;color:#854d0e;font-size:12px;font-weight:700;border-top:2px solid #fde047;">Пропущено без изменений — ' + skipRows.length + ':</td>';
            tbody.appendChild(hdrS);
            skipRows.forEach(function(r, i){
                var tr = document.createElement('tr');
                tr.style.cssText = 'background:'+(i%2===0?'#fff':'#fefce8')+';border-bottom:1px solid #fef9c3;vertical-align:top;';
                var idCell = r.onliner_id
                    ? '<a href="https://catalog.onliner.by/p/'+r.onliner_id+'" target="_blank" style="color:#2563eb;font-size:11px;font-weight:600;">'+r.onliner_id+'</a>'
                    : '—';
                tr.innerHTML =
                    '<td style="padding:7px 14px;color:#1f2937;font-size:12px;max-width:240px;line-height:1.4;">'+(r.name||'')+'</td>'+
                    '<td colspan="2" style="padding:7px 14px;color:#92400e;font-size:11px;">'+(r.reason||'api_unreachable')+'</td>'+
                    '<td style="padding:7px;text-align:center;">'+idCell+'</td>'+
                    '<td style="padding:7px;text-align:center;"><span style="color:#ca8a04;font-weight:700;font-size:12px;">— пропуск</span></td>';
                tbody.appendChild(tr);
            });
        }

        // Cleared / wrong IDs section
        if(allRows.length > 0){
            var hdr = document.createElement('tr');
            hdr.innerHTML = '<td colspan="5" style="padding:8px 14px;background:#fee2e2;color:#991b1b;font-size:12px;font-weight:700;border-top:2px solid #fca5a5;">Очищено (неверный ID) — ' + allRows.length + ':</td>';
            tbody.appendChild(hdr);
            allRows.forEach(function(r, i){
                var tr = document.createElement('tr');
                tr.style.cssText = 'background:'+(i%2===0?'#fff':'#fff7f7')+';border-bottom:1px solid #fee2e2;vertical-align:top;';
                var sc = parseFloat(r.score)||0;
                var idCell = r.onliner_id
                    ? '<a href="https://catalog.onliner.by/p/'+r.onliner_id+'" target="_blank" style="color:#dc2626;text-decoration:line-through;font-size:11px;">'+r.onliner_id+'</a>'
                    : '—';
                tr.innerHTML =
                    '<td style="padding:7px 14px;color:#1f2937;font-size:12px;max-width:240px;line-height:1.4;">'+(r.name||'')+'</td>'+
                    '<td style="padding:7px 14px;color:#6b7280;font-size:12px;">'+(r.api_name||'—')+'</td>'+
                    '<td style="padding:7px;text-align:center;">'+scoreBadge(sc)+'</td>'+
                    '<td style="padding:7px;text-align:center;">'+idCell+'</td>'+
                    '<td style="padding:7px;text-align:center;"><span style="color:#dc2626;font-weight:700;font-size:12px;">✗ Очищен</span></td>';
                tbody.appendChild(tr);
            });
        }

        // Confirmed section (optional)
        if(showConf && confRows.length > 0){
            var hdr2 = document.createElement('tr');
            hdr2.innerHTML = '<td colspan="5" style="padding:8px 14px;background:#dcfce7;color:#166534;font-size:12px;font-weight:700;border-top:2px solid #86efac;">Подтверждено — ' + confRows.length + ':</td>';
            tbody.appendChild(hdr2);
            confRows.forEach(function(r, i){
                var tr = document.createElement('tr');
                tr.style.cssText = 'background:'+(i%2===0?'#fff':'#f0fdf4')+';border-bottom:1px solid #dcfce7;vertical-align:top;';
                var sc = parseFloat(r.score)||0;
                var idCell = r.onliner_id
                    ? '<a href="https://catalog.onliner.by/p/'+r.onliner_id+'" target="_blank" style="color:#2563eb;font-size:11px;font-weight:600;">'+r.onliner_id+'</a>'
                    : '—';
                tr.innerHTML =
                    '<td style="padding:7px 14px;color:#1f2937;font-size:12px;max-width:240px;line-height:1.4;">'+(r.name||'')+'</td>'+
                    '<td style="padding:7px 14px;color:#374151;font-size:12px;">'+(r.api_name||'')+'</td>'+
                    '<td style="padding:7px;text-align:center;">'+scoreBadge(sc)+'</td>'+
                    '<td style="padding:7px;text-align:center;">'+idCell+'</td>'+
                    '<td style="padding:7px;text-align:center;"><span style="color:#16a34a;font-weight:700;font-size:12px;">✓ Верно</span></td>';
                tbody.appendChild(tr);
            });
        }

        if(allRows.length === 0 && skipRows.length === 0 && (!showConf || confRows.length === 0)){
            var empty = document.createElement('tr');
            empty.innerHTML = '<td colspan="5" style="padding:20px;text-align:center;color:#9ca3af;font-size:13px;">'+(st.running?'Идёт проверка...':'Нет строк для отчёта (всё подтверждено или нет изменений)')+'</td>';
            tbody.appendChild(empty);
        }
    }

    if(showConfEl) showConfEl.addEventListener('change', renderModal);

    function stopPoll(){ if(pollTimer){ clearTimeout(pollTimer); pollTimer=null; } }

    function pollValidate(){
        fetch('/api/validate-clean-ids-status?_ts='+Date.now(), {cache:'no-store'})
        .then(function(r){ return r.json(); })
        .then(function(st){
            vldStatus = st;
            var pct = st.total>0 ? Math.round((st.done||0)/st.total*100) : 0;
            if(progressBar) progressBar.style.width = pct + '%';
            if(msgEl) msgEl.textContent = st.message || '';
            if(st.running){
                if(progressWrap) progressWrap.style.display='block';
                if(startBtn){ startBtn.disabled=true; startBtn.textContent='Проверяю API...'; }
                if(startDbBtn){ startDbBtn.disabled=true; startDbBtn.textContent='Проверяю БД...'; }
                pollTimer = setTimeout(pollValidate, 2000);
            } else {
                if(progressWrap) progressWrap.style.display='block';
                if(startBtn){ startBtn.disabled=false; startBtn.textContent='🔍 Проверить ID'; }
                if(startDbBtn){ startDbBtn.disabled=false; startDbBtn.textContent='⚡ Проверить по БД'; }
                if(st.done > 0 && reportBtn){
                    reportBtn.style.display='inline-flex';
                    stopPoll();
                }
                // После валидации сервер уже перезаписал consolidated — перезагружаем таблицу,
                // иначе в DataTables остаются старые OnlinerID («очищен», а в таблице есть).
                if(st.finished_at && (Number(st.cleared||0) > 0 || Number(st.confirmed||0) > 0 || Number(st.skipped_api||0) > 0)){
                    if(typeof tblMain !== 'undefined' && tblMain && tblMain.ajax && typeof tblMain.ajax.reload === 'function'){
                        tblMain.ajax.reload(null, false);
                    }
                    fetch('/api/stats?_ts='+Date.now(), {cache:'no-store'}).then(function(r){ return r.json(); }).then(function(s){
                        var w = document.getElementById('without-id-count');
                        if(w && s && s.without_id !== undefined) w.textContent = s.without_id;
                        if(s && s.duplicate_id_rows !== undefined) setDuplicateIdCounterValue(s.duplicate_id_rows);
                    }).catch(function(){});
                }
            }
        }).catch(function(){ pollTimer = setTimeout(pollValidate, 4000); });
    }

    function launchValidate(mode){
        var isDb = mode === 'db';
        var btn = isDb ? startDbBtn : startBtn;
        var endpoint = isDb ? '/api/validate-clean-ids-db-start' : '/api/validate-clean-ids-start';
        if(!btn || btn.disabled) return;
        activeValidateMode = mode;
        btn.disabled = true;
        btn.textContent = 'Запускаю...';
        if(!isDb && startDbBtn){ startDbBtn.disabled = true; }
        if(isDb && startBtn){ startBtn.disabled = true; }
        if(progressWrap) progressWrap.style.display='block';
        if(progressBar) progressBar.style.width='0%';
        if(msgEl) msgEl.textContent = isDb ? 'Подготовка локальной сверки...' : 'Подготовка API-проверки...';
        if(reportBtn) reportBtn.style.display='none';
        fetch(endpoint, {method:'POST'})
        .then(function(r){ return r.json(); })
        .then(function(d){
            if(d.status==='already_running' || d.status==='started'){
                stopPoll();
                pollValidate();
            } else {
                if(startBtn){ startBtn.disabled=false; startBtn.textContent='🔍 Проверить ID'; }
                if(startDbBtn){ startDbBtn.disabled=false; startDbBtn.textContent='⚡ Проверить по БД'; }
                alert('Ошибка запуска: '+(d.message||d.status||'?'));
            }
        }).catch(function(err){
            if(startBtn){ startBtn.disabled=false; startBtn.textContent='🔍 Проверить ID'; }
            if(startDbBtn){ startDbBtn.disabled=false; startDbBtn.textContent='⚡ Проверить по БД'; }
            alert('Ошибка: '+err);
        });
    }

    if(startBtn){
        startBtn.addEventListener('click', function(){
            launchValidate('api');
        });
    }

    if(startDbBtn){
        startDbBtn.addEventListener('click', function(){
            launchValidate('db');
        });
    }

    if(reportBtn){
        reportBtn.addEventListener('click', function(){
            renderModal();
            openModal();
        });
    }

    // Check if validation was running on page load
    fetch('/api/validate-clean-ids-status?_ts='+Date.now(), {cache:'no-store'})
    .then(function(r){ return r.json(); })
    .then(function(st){
        vldStatus = st;
        if(st.running){
            if(progressWrap) progressWrap.style.display='block';
            if(startBtn){ startBtn.disabled=true; startBtn.textContent='Проверяю API...'; }
            if(startDbBtn){ startDbBtn.disabled=true; startDbBtn.textContent='Проверяю БД...'; }
            pollValidate();
        } else if(st.done > 0){
            if(progressWrap) progressWrap.style.display='block';
            if(progressBar) progressBar.style.width='100%';
            if(msgEl) msgEl.textContent = st.message || '';
            if(reportBtn) reportBtn.style.display='inline-flex';
            if(startBtn){ startBtn.textContent='🔍 Проверить ID'; }
            if(startDbBtn){ startDbBtn.textContent='⚡ Проверить по БД'; }
        }
    }).catch(function(){});
})();
// ============================================================
(function(){
    var validatePollTimer = null;
    var validateLabel = 'Запустить';

    function stopValidatePoll(){
        if(validatePollTimer){ clearInterval(validatePollTimer); validatePollTimer = null; }
    }

    function renderValidateChips(st){
        var chips = document.getElementById('validate-clean-chips');
        if(!chips) return;
        var confirmed = Number(st.confirmed||0);
        var cleared   = Number(st.cleared||0);
        var queued    = Number(st.queued||0);
        var errors    = Number(st.errors||0);
        var html = '';
        if(confirmed > 0) html += '<span style="display:inline-flex;align-items:center;gap:3px;background:#dcfce7;color:#15803d;border-radius:20px;padding:3px 10px;font-size:12px;font-weight:600;">✓ '+confirmed+' подтверждено</span>';
        if(cleared > 0)   html += '<span style="display:inline-flex;align-items:center;gap:3px;background:#fee2e2;color:#dc2626;border-radius:20px;padding:3px 10px;font-size:12px;font-weight:600;">✕ '+cleared+' очищено</span>';
        if(queued > 0)    html += '<span style="display:inline-flex;align-items:center;gap:3px;background:#ede9fe;color:#5b21b6;border-radius:20px;padding:3px 10px;font-size:12px;font-weight:600;">⋯ '+queued+' в очереди</span>';
        if(errors > 0)    html += '<span style="display:inline-flex;align-items:center;gap:3px;background:#fef3c7;color:#b45309;border-radius:20px;padding:3px 10px;font-size:12px;font-weight:600;">! '+errors+' ошибок</span>';
        chips.innerHTML = html;
        chips.style.display = html ? 'flex' : 'none';
    }

    function pollValidateStatus(){
        fetch('/api/validate-clean-ids-status?_ts='+Date.now(), {cache:'no-store'})
        .then(function(r){ return r.json(); })
        .then(function(st){
            var btn  = document.getElementById('run-validate-clean-btn');
            var note = document.getElementById('validate-clean-note');
            var bar  = document.getElementById('validate-clean-progress-bar');
            var wrap = document.getElementById('validate-clean-progress-wrap');
            var total = Number(st.total||0);
            var done  = Number(st.done||0);
            var pct   = total > 0 ? Math.round(done/total*100) : 0;
            if(wrap) wrap.style.display = 'block';
            if(bar)  bar.style.width = pct + '%';
            if(note && st.message) note.textContent = st.message;
            renderValidateChips(st);
            if(btn && st.running){
                var phase2 = st.message && String(st.message).indexOf('Фаза 2') === 0;
                btn.textContent = phase2 ? 'Кандидаты...' : ('Проверяю ' + pct + '%');
            }
            if(!st.running){
                stopValidatePoll();
                if(btn){ btn.disabled = false; btn.textContent = validateLabel; }
                hideBusyOverlay();
                // Обновляем таблицу и счётчик
                if(st.finished_at && Number(st.cleared||0) > 0){
                    if(tblMain && typeof tblMain.ajax !== 'undefined'){
                        tblMain.ajax.reload(null, false);
                    } else {
                        fetch('/api/consolidated').then(function(r){ return r.json(); }).then(function(resp){
                            mainTableRows = (resp && resp.data) ? resp.data : [];
                            updateWithoutIdCount(mainTableRows);
                            renderMainTableFallback();
                        });
                    }
                }
                if(st.finished_at && Number(st.queued||0) > 0){
                    // Показываем кнопку очереди подсвеченной
                    var qBtn = document.getElementById('show-review-queue-btn');
                    if(qBtn) qBtn.style.background = '#ede9fe';
                    loadReviewQueue();
                }
                if(bar && st.finished_at){
                    bar.style.background = Number(st.cleared||0) > 0 ? 'linear-gradient(90deg,#f87171,#dc2626)' : 'linear-gradient(90deg,#6366f1,#4f46e5)';
                }
                runPreExportQualityCheck();
            } else {
                updateBusyOverlay('Валидация ID', st.message || ('Проверено ' + done + ' из ' + total + '...'));
            }
        }).catch(function(){
            stopValidatePoll();
            var btn = document.getElementById('run-validate-clean-btn');
            if(btn){ btn.disabled = false; btn.textContent = validateLabel; }
            hideBusyOverlay();
        });
    }

    window.startValidateClean = function(){
        var btn = document.getElementById('run-validate-clean-btn');
        if(btn && btn.disabled) return;
        if(btn){ btn.disabled = true; btn.textContent = 'Проверяю 0%'; }
        var chips = document.getElementById('validate-clean-chips');
        if(chips){ chips.innerHTML = ''; chips.style.display = 'none'; }
        var bar = document.getElementById('validate-clean-progress-bar');
        var wrap = document.getElementById('validate-clean-progress-wrap');
        if(wrap) wrap.style.display = 'block';
        if(bar){ bar.style.width = '2%'; bar.style.background = 'linear-gradient(90deg,#6366f1,#4f46e5)'; }
        var note = document.getElementById('validate-clean-note');
        if(note) note.textContent = 'Запускаем валидацию...';
        showBusyOverlay('Валидация ID', 'Проверяем каждый OnlinerID по Onliner API...');
        fetch('/api/validate-clean-ids-start', {method:'POST'})
        .then(function(r){ return r.json(); })
        .then(function(d){
            if(!d || (d.status !== 'started' && d.status !== 'already_running')){
                throw new Error((d&&d.message)||'start_failed');
            }
            stopValidatePoll();
            validatePollTimer = setInterval(pollValidateStatus, 1500);
            pollValidateStatus();
        }).catch(function(err){
            if(btn){ btn.disabled = false; btn.textContent = validateLabel; }
            hideBusyOverlay();
            if(note) note.textContent = 'Не удалось запустить валидацию.';
        });
    };
})();

// ============================================================
// ОЧЕРЕДЬ РУЧНОЙ ПРОВЕРКИ ID
// ============================================================
var _reviewQueueData = {};

function highlightCpuModelMatch(candidateName, cpuModel){
    var text = String(candidateName || '');
    var model = String(cpuModel || '').trim();
    if(!model){
        return escapeHtml(text);
    }
    var escapedText = escapeHtml(text);
    var compactModel = model.toLowerCase().replace(/[^a-z0-9]+/g, '');
    if(!compactModel){
        return escapedText;
    }
    var patterns = [
        model,
        model.replace(/\s+/g, ''),
        model.replace(/([a-z]+)(\d)/ig, '$1 $2'),
        model.replace(/([a-z]+)\s*(\d)/ig, '$1-$2')
    ].filter(function(v, idx, arr){
        return v && arr.indexOf(v) === idx;
    });
    var lowerText = text.toLowerCase();
    for(var i = 0; i < patterns.length; i++){
        var raw = String(patterns[i] || '');
        if(!raw){ continue; }
        var idx = lowerText.indexOf(raw.toLowerCase());
        if(idx >= 0){
            var before = escapeHtml(text.slice(0, idx));
            var hit = escapeHtml(text.slice(idx, idx + raw.length));
            var after = escapeHtml(text.slice(idx + raw.length));
            return before + '<span style="background:#fef3c7;color:#92400e;border-radius:4px;padding:1px 4px;font-weight:700;">' + hit + '</span>' + after;
        }
    }
    var compactText = text.toLowerCase().replace(/[^a-z0-9]+/g, '');
    if(compactText.indexOf(compactModel) >= 0){
        return escapedText + ' <span style="display:inline-flex;align-items:center;gap:4px;background:#dcfce7;color:#166534;border-radius:999px;padding:2px 7px;font-size:10px;font-weight:700;vertical-align:middle;">модель совпала</span>';
    }
    return escapedText;
}

function parseCpuBrandModel(candidateName){
    var text = String(candidateName || '').toLowerCase();
    var brand = '';
    if(/intel|xeon|pentium|celeron/.test(text)){ brand = 'INTEL'; }
    else if(/amd|ryzen|athlon/.test(text)){ brand = 'AMD'; }
    var patterns = [
        /(?:^|[^a-z0-9])(i[3579]-\d{4,5}[a-z]{0,2})(?=$|[^a-z0-9])/i,
        /(?:^|[^a-z0-9])(ryzen\s*[3579]\s*\d{4,5}[a-z]{0,2})(?=$|[^a-z0-9])/i,
        /(?:^|[^a-z0-9])(pentium\s+[a-z]?\d{4,5})(?=$|[^a-z0-9])/i,
        /(?:^|[^a-z0-9])(celeron\s+[a-z]?\d{4,5})(?=$|[^a-z0-9])/i,
        /(?:^|[^a-z0-9])(athlon\s+\d{4,5}[a-z]{0,2})(?=$|[^a-z0-9])/i,
        /(?:^|[^a-z0-9])(xeon\s+[ew]?-?\d{1,2}-?\d{4,5}\s*v?\d?)(?=$|[^a-z0-9])/i
    ];
    var model = '';
    for(var i = 0; i < patterns.length; i++){
        var m = text.match(patterns[i]);
        if(m && m[1]){
            model = String(m[1]).toUpperCase().replace(/\s+/g, ' ').trim();
            break;
        }
    }
    var compactModel = model.toLowerCase().replace(/[^a-z0-9]+/g, '');
    return { brand: brand, model: model, compactModel: compactModel };
}

function parseBoardBrandModel(candidateName){
    var text = String(candidateName || '');
    var low = text.toLowerCase();
    var brand = '';
    if(/(?:^|[^a-z0-9])asrock(?=$|[^a-z0-9])/i.test(low)){ brand = 'ASROCK'; }
    else if(/(?:^|[^a-z0-9])gigabyte(?=$|[^a-z0-9])/i.test(low)){ brand = 'GIGABYTE'; }
    else if(/(?:^|[^a-z0-9])asus(?=$|[^a-z0-9])/i.test(low)){ brand = 'ASUS'; }
    else if(/(?:^|[^a-z0-9])msi(?=$|[^a-z0-9])/i.test(low)){ brand = 'MSI'; }
    else if(/(?:^|[^a-z0-9])biostar(?=$|[^a-z0-9])/i.test(low)){ brand = 'BIOSTAR'; }
    else if(/(?:^|[^a-z0-9])colorful(?=$|[^a-z0-9])/i.test(low)){ brand = 'COLORFUL'; }
    else if(/(?:^|[^a-z0-9])maxsun(?=$|[^a-z0-9])/i.test(low)){ brand = 'MAXSUN'; }
    var model = '';
    if(brand){
        model = text.replace(/^\s*MB\s+/i, '');
        model = model.replace(new RegExp('^\\s*' + brand + '\\s+', 'i'), '');
        model = model.split(/(?:\s+Soc[-\s]|\s+Socket[-\s])/i)[0] || model;
        model = model.replace(/\([^)]+\)\s*$/,'').trim();
    }
    var compactModel = model.toLowerCase().replace(/[^a-z0-9]+/g, '');
    return { brand: brand, model: model.toUpperCase(), compactModel: compactModel };
}

function parseMonitorBrandModel(candidateName){
    var text = String(candidateName || '').replace(/″/g, '"');
    var low = text.toLowerCase();
    var brand = '';
    if(/(?:^|[^a-z0-9])elsa(?=$|[^a-z0-9])/i.test(low)){ brand = 'ELSA'; }
    else if(/(?:^|[^a-z0-9])lg(?=$|[^a-z0-9])/i.test(low)){ brand = 'LG'; }
    else if(/(?:^|[^a-z0-9])xiaomi(?=$|[^a-z0-9])/i.test(low)){ brand = 'XIAOMI'; }
    else if(/(?:^|[^a-z0-9])asrock(?=$|[^a-z0-9])/i.test(low)){ brand = 'ASROCK'; }
    else if(/(?:^|[^a-z0-9])gigabyte(?=$|[^a-z0-9])/i.test(low)){ brand = 'GIGABYTE'; }
    else if(/(?:^|[^a-z0-9])msi(?=$|[^a-z0-9])/i.test(low)){ brand = 'MSI'; }
    else if(/(?:^|[^a-z0-9])asus(?=$|[^a-z0-9])/i.test(low)){ brand = 'ASUS'; }
    var sizeMatch = text.match(/^\s*(\d{2}(?:\.\d)?)\s*"/i);
    var size = sizeMatch && sizeMatch[1] ? sizeMatch[1] : '';
    var model = text;
    if(size){
        model = model.replace(/^\s*\d{2}(?:\.\d)?\s*"\s*/i, '');
    }
    if(brand){
        model = model.replace(new RegExp('^\\s*' + brand + '\\s+', 'i'), '');
    }
    model = model.split(/\s*\(/, 1)[0] || model;
    model = model.trim();
    var compactModel = model.toLowerCase().replace(/[^a-z0-9]+/g, '');
    return { brand: brand, model: model.toUpperCase(), compactModel: compactModel, size: size };
}

function parseGpuBrandModel(candidateName){
    var text = String(candidateName || '');
    var low = text.toLowerCase();
    var vendor = '';
    if(/(?:^|[^a-z0-9])gigabyte(?=$|[^a-z0-9])/i.test(low)){ vendor = 'GIGABYTE'; }
    else if(/(?:^|[^a-z0-9])sapphire(?=$|[^a-z0-9])/i.test(low)){ vendor = 'SAPPHIRE'; }
    else if(/(?:^|[^a-z0-9])asus(?=$|[^a-z0-9])/i.test(low)){ vendor = 'ASUS'; }
    else if(/(?:^|[^a-z0-9])msi(?=$|[^a-z0-9])/i.test(low)){ vendor = 'MSI'; }
    else if(/(?:^|[^a-z0-9])palit(?=$|[^a-z0-9])/i.test(low)){ vendor = 'PALIT'; }
    else if(/(?:^|[^a-z0-9])gainward(?=$|[^a-z0-9])/i.test(low)){ vendor = 'GAINWARD'; }
    else if(/(?:^|[^a-z0-9])zotac(?=$|[^a-z0-9])/i.test(low)){ vendor = 'ZOTAC'; }
    else if(/(?:^|[^a-z0-9])inno3d(?=$|[^a-z0-9])/i.test(low)){ vendor = 'INNO3D'; }
    else if(/(?:^|[^a-z0-9])ocpc(?=$|[^a-z0-9])/i.test(low)){ vendor = 'OCPC'; }
    var gpuBrand = /geforce|rtx|gtx/i.test(low) ? 'NVIDIA' : ((/radeon|(?:^|[^a-z0-9])rx\s*\d{3,4}/i.test(low)) ? 'AMD' : '');
    var model = '';
    var patterns = [
        /((?:rtx|gtx)\s*\d{4}\s*ti?)/i,
        /((?:rx)\s*\d{3,4}\s*xt?)/i,
        /((?:rx)\s*\d{3,4})/i
    ];
    for(var i = 0; i < patterns.length; i++){
        var m = text.match(patterns[i]);
        if(m && m[1]){
            model = String(m[1]).toUpperCase().replace(/\s+/g, ' ').trim();
            break;
        }
    }
    var compactModel = model.toLowerCase().replace(/[^a-z0-9]+/g, '');
    var sku = '';
    var skuMatch = text.match(/\(([A-Za-z0-9+\- ]{6,40})\)/);
    if(skuMatch && skuMatch[1]){
        sku = String(skuMatch[1]).toLowerCase().replace(/[^a-z0-9]+/g, '');
    }
    return { vendor: vendor, gpuBrand: gpuBrand, model: model, compactModel: compactModel, sku: sku };
}

function parseRamBrandModel(candidateName){
    var text = String(candidateName || '');
    var low = text.toLowerCase();
    var brand = '';
    if(/(?:^|[^a-z0-9])kingston(?=$|[^a-z0-9])/i.test(low)){ brand = 'KINGSTON'; }
    else if(/(?:^|[^a-z0-9])g\.?skill(?=$|[^a-z0-9])/i.test(low)){ brand = 'GSKILL'; }
    else if(/(?:^|[^a-z0-9])netac(?=$|[^a-z0-9])/i.test(low)){ brand = 'NETAC'; }
    else if(/(?:^|[^a-z0-9])team(?=$|[^a-z0-9])/i.test(low)){ brand = 'TEAM'; }
    else if(/(?:^|[^a-z0-9])a-?data|(?:^|[^a-z0-9])adata|(?:^|[^a-z0-9])xpg(?=$|[^a-z0-9])/i.test(low)){ brand = 'ADATA'; }
    else if(/(?:^|[^a-z0-9])patriot(?=$|[^a-z0-9])/i.test(low)){ brand = 'PATRIOT'; }
    else if(/(?:^|[^a-z0-9])corsair(?=$|[^a-z0-9])/i.test(low)){ brand = 'CORSAIR'; }
    var sku = '';
    var skuMatch = text.match(/\(([A-Za-z0-9+\-\/ ]{6,80})\)/);
    if(skuMatch && skuMatch[1]){
        sku = String(skuMatch[1]).toLowerCase().replace(/[^a-z0-9]+/g, '');
    }
    return { brand: brand, sku: sku };
}

function queueCandidateTone(item, candidate){
    var expectedHddCode = String((item && item.hdd_code) || '').trim().toLowerCase().replace(/[^a-z0-9]+/g, '');
    var candHddCode = String((candidate && candidate.code) || '').trim().toLowerCase().replace(/[^a-z0-9]+/g, '');
    if(expectedHddCode && candHddCode && expectedHddCode === candHddCode){
        return { bg: '#dcfce7', color: '#166534', border: '#86efac', label: 'точный HDD' };
    }
    var expectedCoolerCode = String((item && item.cooler_code) || '').trim().toLowerCase().replace(/[^a-z0-9]+/g, '');
    var candCoolerCode = String((candidate && candidate.code) || '').trim().toLowerCase().replace(/[^a-z0-9]+/g, '');
    if(expectedCoolerCode && candCoolerCode && expectedCoolerCode === candCoolerCode){
        return { bg: '#dcfce7', color: '#166534', border: '#86efac', label: 'точное охлаждение' };
    }
    var expectedPrinterArt = String((item && item.printer_article) || '').trim().toLowerCase().replace(/[^a-z0-9]+/g, '');
    var candPrinterCode = String((candidate && candidate.code) || '').trim().toLowerCase().replace(/[^a-z0-9]+/g, '');
    if(expectedPrinterArt && candPrinterCode && expectedPrinterArt === candPrinterCode){
        return { bg: '#dcfce7', color: '#166534', border: '#86efac', label: 'точный принтер / МФУ' };
    }
    var expectedRamBrand = String((item && item.ram_brand) || '').trim().toUpperCase();
    var expectedRamSku = String((item && item.ram_sku) || '').trim().toUpperCase();
    if(expectedRamBrand || expectedRamSku){
        var parsedRam = parseRamBrandModel(String((candidate && candidate.name) || ''));
        var sameRamBrand = !!expectedRamBrand && parsedRam.brand === expectedRamBrand;
        var sameRamSku = !!expectedRamSku && parsedRam.sku === expectedRamSku.toLowerCase().replace(/[^a-z0-9]+/g, '');
        if(sameRamBrand && sameRamSku){
            return { bg: '#dcfce7', color: '#166534', border: '#86efac', label: 'точная RAM SKU' };
        }
        if(sameRamBrand){
            return { bg: '#fef3c7', color: '#92400e', border: '#fcd34d', label: 'бренд ок' };
        }
        return { bg: '#fee2e2', color: '#991b1b', border: '#fca5a5', label: 'проверить' };
    }
    var expectedGpuVendor = String((item && item.gpu_vendor) || '').trim().toUpperCase();
    var expectedGpuModel = String((item && item.gpu_model) || '').trim().toUpperCase();
    if(expectedGpuVendor || expectedGpuModel){
        var parsedGpu = parseGpuBrandModel(String((candidate && candidate.name) || ''));
        var sameGpuVendor = !!expectedGpuVendor && parsedGpu.vendor === expectedGpuVendor;
        var sameGpuModel = !!expectedGpuModel && !!parsedGpu.compactModel && parsedGpu.compactModel === expectedGpuModel.toLowerCase().replace(/[^a-z0-9]+/g, '');
        var expectedGpuSku = String((item && item.gpu_sku) || '').trim().toLowerCase().replace(/[^a-z0-9]+/g, '');
        var sameGpuSku = !!expectedGpuSku && parsedGpu.sku === expectedGpuSku;
        if(sameGpuVendor && sameGpuModel && sameGpuSku){
            return { bg: '#dcfce7', color: '#166534', border: '#86efac', label: 'точная GPU SKU' };
        }
        if(sameGpuVendor && sameGpuModel){
            return { bg: '#dcfce7', color: '#166534', border: '#86efac', label: 'точная GPU' };
        }
        if(sameGpuModel){
            return { bg: '#fef3c7', color: '#92400e', border: '#fcd34d', label: 'модель ок' };
        }
        return { bg: '#fee2e2', color: '#991b1b', border: '#fca5a5', label: 'проверить' };
    }
    var expectedMonitorBrand = String((item && item.monitor_brand) || '').trim().toUpperCase();
    var expectedMonitorModel = String((item && item.monitor_model) || '').trim().toUpperCase();
    if(expectedMonitorBrand || expectedMonitorModel){
        var parsedMon = parseMonitorBrandModel(String((candidate && candidate.name) || ''));
        var sameMonitorBrand = !!expectedMonitorBrand && parsedMon.brand === expectedMonitorBrand;
        var sameMonitorModel = !!expectedMonitorModel && !!parsedMon.compactModel && parsedMon.compactModel === expectedMonitorModel.toLowerCase().replace(/[^a-z0-9]+/g, '');
        if(sameMonitorBrand && sameMonitorModel){
            return { bg: '#dcfce7', color: '#166534', border: '#86efac', label: 'точный монитор' };
        }
        if(sameMonitorModel){
            return { bg: '#fef3c7', color: '#92400e', border: '#fcd34d', label: 'модель ок' };
        }
        return { bg: '#fee2e2', color: '#991b1b', border: '#fca5a5', label: 'проверить' };
    }
    var expectedBoardBrand = String((item && item.board_brand) || '').trim().toUpperCase();
    var expectedBoardModel = String((item && item.board_model) || '').trim().toUpperCase();
    if(expectedBoardBrand || expectedBoardModel){
        var parsedBoard = parseBoardBrandModel(String((candidate && candidate.name) || ''));
        var sameBoardBrand = !!expectedBoardBrand && parsedBoard.brand === expectedBoardBrand;
        var sameBoardModel = !!expectedBoardModel && !!parsedBoard.compactModel && parsedBoard.compactModel === expectedBoardModel.toLowerCase().replace(/[^a-z0-9]+/g, '');
        if(sameBoardBrand && sameBoardModel){
            return { bg: '#dcfce7', color: '#166534', border: '#86efac', label: 'точная плата' };
        }
        if(sameBoardModel){
            return { bg: '#fef3c7', color: '#92400e', border: '#fcd34d', label: 'модель ок' };
        }
        return { bg: '#fee2e2', color: '#991b1b', border: '#fca5a5', label: 'проверить' };
    }
    var expectedBrand = String((item && item.cpu_brand) || '').trim().toUpperCase();
    var expectedModel = String((item && item.cpu_model) || '').trim().toUpperCase();
    var parsed = parseCpuBrandModel(String((candidate && candidate.name) || ''));
    var sameBrand = !!expectedBrand && parsed.brand === expectedBrand;
    var sameModel = !!expectedModel && !!parsed.compactModel && parsed.compactModel === expectedModel.toLowerCase().replace(/[^a-z0-9]+/g, '');
    if(sameBrand && sameModel){
        return {
            bg: '#dcfce7',
            color: '#166534',
            border: '#86efac',
            label: 'точное CPU'
        };
    }
    if(sameModel){
        return {
            bg: '#fef3c7',
            color: '#92400e',
            border: '#fcd34d',
            label: 'модель ок'
        };
    }
    return {
        bg: '#fee2e2',
        color: '#991b1b',
        border: '#fca5a5',
        label: 'проверить'
    };
}

function cpuCandidatePriority(item, candidate){
    var tone = queueCandidateTone(item, candidate);
    if(tone.label === 'точное CPU' || tone.label === 'точная плата' || tone.label === 'точный монитор' || tone.label === 'точная GPU SKU' || tone.label === 'точная GPU' || tone.label === 'точная RAM SKU' || tone.label === 'точный HDD' || tone.label === 'точное охлаждение' || tone.label === 'точный принтер / МФУ'){ return 0; }
    if(tone.label === 'модель ок' || tone.label === 'бренд ок'){ return 1; }
    return 2;
}

window.loadReviewQueue = function(){
    fetch('/api/review-queue?_ts='+Date.now(), {cache:'no-store'})
    .then(function(r){ return r.json(); })
    .then(function(data){
        var items = Array.isArray(data.items) ? data.items : [];
        var card  = document.getElementById('review-queue-card');
        var badge = document.getElementById('review-queue-count-badge');
        var list  = document.getElementById('review-queue-list');
        var note  = document.getElementById('review-queue-note');
        var qBtn  = document.getElementById('show-review-queue-btn');

        if(badge){
            if(items.length > 0){ badge.textContent = items.length; badge.style.display = 'inline-block'; }
            else { badge.style.display = 'none'; }
        }
        if(qBtn){ qBtn.style.background = items.length > 0 ? '#ede9fe' : ''; }
        if(!list) return;

        if(!items.length){
            list.innerHTML = '<div class="markup-note" style="color:#6b7280;">Очередь пуста — нет товаров ожидающих ручной проверки.</div>';
            if(note) note.textContent = 'Запустите «Валидация и очистка ID» — неверно сопоставленные товары появятся здесь с кандидатами на выбор.';
            return;
        }
        if(card) card.style.display = 'block';
        if(note) note.textContent = 'Выберите правильный ID из кандидатов или нажмите «Пропустить». После выбора — запустите автоподбор снова.';

        // Сохраняем данные в глобальный map по индексу — без небезопасных inline-аргументов
        _reviewQueueData = {};
        items.forEach(function(item, idx){ _reviewQueueData[idx] = item; });

        var html = '';
        items.forEach(function(item, idx){
            var name      = escapeHtml(item.name||'');
            var cpuBrand  = escapeHtml(String((item.cpu_brand || item.board_brand || item.monitor_brand || item.gpu_vendor || item.ram_brand || item.ssd_brand || item.psu_brand || item.case_brand || item.hdd_brand || item.cooler_brand || item.printer_brand)||'').trim());
            var cpuModel  = escapeHtml(String((item.cpu_model || item.board_model || item.monitor_model || item.gpu_model || item.gpu_sku || item.ram_sku || item.ssd_model || item.ssd_code || item.psu_model || item.psu_code || item.case_model || item.case_code || item.hdd_code || item.hdd_capacity || item.cooler_code || item.cooler_tdp || item.printer_model || item.printer_article)||'').trim());
            var clearedId = escapeHtml(String(item.cleared_id||''));
            var clearedSc = item.cleared_score !== undefined ? String(Math.round(Number(item.cleared_score)*100))+'%' : '';
            var onlName   = escapeHtml(item.onliner_name||'');
            var cands     = Array.isArray(item.candidates) ? item.candidates : [];

            html += '<div class="review-queue-item" data-rqi="'+idx+'" style="border:1px solid #e5e7eb;border-radius:10px;padding:14px 16px;margin-bottom:12px;background:#fafaf9;">';
            html += '<div style="display:flex;justify-content:space-between;align-items:flex-start;gap:8px;flex-wrap:wrap;margin-bottom:10px;">';
            html += '<div style="font-size:13px;font-weight:700;color:#111827;flex:1;min-width:0;">'+ name +'</div>';
            html += '<button data-rqi-skip="'+idx+'" style="padding:4px 12px;font-size:11px;border:1px solid #d1d5db;border-radius:6px;background:#fff;cursor:pointer;color:#6b7280;white-space:nowrap;">Пропустить</button>';
            html += '</div>';

            if(cpuBrand || cpuModel){
                html += '<div style="display:flex;gap:6px;flex-wrap:wrap;margin-bottom:8px;">';
                if(cpuBrand){
                    html += '<span style="display:inline-flex;align-items:center;gap:4px;background:#eef2ff;color:#4338ca;border-radius:999px;padding:3px 9px;font-size:11px;font-weight:700;">Производитель: '+cpuBrand+'</span>';
                }
                if(cpuModel){
                    html += '<span style="display:inline-flex;align-items:center;gap:4px;background:#f5f3ff;color:#6d28d9;border-radius:999px;padding:3px 9px;font-size:11px;font-weight:700;font-family:monospace;">Модель: '+cpuModel+'</span>';
                }
                html += '</div>';
            }

            if(clearedId){
                html += '<div style="font-size:11px;color:#9ca3af;margin-bottom:8px;">Был ID: <span style="font-family:monospace;color:#dc2626;">'+clearedId+'</span>';
                if(clearedSc) html += ' <span style="color:#b45309;">(совпадение '+clearedSc+')</span>';
                if(onlName)   html += ' — Onliner: <em>'+onlName+'</em>';
                html += '</div>';
            }

            if(cands.length){
                cands = cands.slice().sort(function(a, b){
                    var pa = cpuCandidatePriority(item, a);
                    var pb = cpuCandidatePriority(item, b);
                    if(pa !== pb){ return pa - pb; }
                    var sa = Number((a && a.score) || 0);
                    var sb = Number((b && b.score) || 0);
                    if(sa !== sb){ return sb - sa; }
                    return String((a && a.name) || '').localeCompare(String((b && b.name) || ''), 'ru');
                });
                html += '<div style="font-size:11px;color:#374151;font-weight:600;margin-bottom:6px;">Кандидаты:</div>';
                html += '<div style="display:flex;flex-direction:column;gap:6px;">';
                cands.forEach(function(c, ci){
                    var cId    = escapeHtml(String(c.id||''));
                    var gpuHighlight = String((item.gpu_sku || item.gpu_model || item.ram_sku) || '');
                    var cName  = highlightCpuModelMatch(String(c.name||''), String((item.cpu_model || item.board_model || item.monitor_model || gpuHighlight)||''));
                    var cSc    = c.score !== undefined ? Math.round(Number(c.score)*100) : 0;
                    var scCol  = cSc >= 90 ? '#15803d' : cSc >= 70 ? '#b45309' : '#dc2626';
                    var cUrl   = escapeHtml(String(c.url||''));
                    var tone   = queueCandidateTone(item, c);
                    html += '<div style="display:flex;align-items:center;gap:8px;background:#fff;border:1px solid #e5e7eb;border-radius:8px;padding:8px 10px;">';
                    html += '<span style="background:#ede9fe;color:#5b21b6;border-radius:5px;padding:2px 8px;font-size:11px;font-weight:700;font-family:monospace;white-space:nowrap;">'+cId+'</span>';
                    html += '<span style="flex:1;font-size:12px;color:#1f2937;">'+cName+'</span>';
                    html += '<span style="display:inline-flex;align-items:center;gap:4px;background:'+tone.bg+';color:'+tone.color+';border:1px solid '+tone.border+';border-radius:999px;padding:2px 7px;font-size:10px;font-weight:700;white-space:nowrap;">'+escapeHtml(tone.label)+'</span>';
                    html += '<span style="font-size:11px;color:'+scCol+';font-weight:600;white-space:nowrap;">'+cSc+'%</span>';
                    if(cUrl){ html += '<a href="'+cUrl+'" target="_blank" style="font-size:11px;color:#6366f1;text-decoration:none;white-space:nowrap;">↗</a>'; }
                    html += '<button data-rqi-pick="'+idx+'" data-rqi-ci="'+ci+'" style="padding:4px 12px;font-size:11px;border:none;border-radius:6px;background:#6366f1;color:#fff;cursor:pointer;white-space:nowrap;">Выбрать</button>';
                    html += '</div>';
                });
                html += '</div>';
            } else {
                html += '<div class="markup-note">Кандидатов не найдено — запустите автоподбор после валидации.</div>';
            }
            html += '</div>';
        });
        list.innerHTML = html;

        // Навешиваем обработчики один раз через делегирование
        list.onclick = function(e){
            var skipBtn = e.target.closest('[data-rqi-skip]');
            var pickBtn = e.target.closest('[data-rqi-pick]');
            if(skipBtn){
                var idx = Number(skipBtn.getAttribute('data-rqi-skip'));
                var item = _reviewQueueData[idx];
                if(item) _doReviewPick(item.name_key||'', '', '', '', item.name||'');
                var wrap = skipBtn.closest('[data-rqi]');
                if(wrap){ wrap.style.opacity='0.4'; wrap.style.pointerEvents='none'; }
            } else if(pickBtn){
                var idx = Number(pickBtn.getAttribute('data-rqi-pick'));
                var ci  = Number(pickBtn.getAttribute('data-rqi-ci'));
                var item = _reviewQueueData[idx];
                if(item){
                    var cand = (item.candidates||[])[ci] || {};
                    _doReviewPick(item.name_key||'', cand.id||'', cand.name||'', cand.url||'', item.name||'');
                    var wrap = pickBtn.closest('[data-rqi]');
                    if(wrap){ wrap.style.opacity='0.4'; wrap.style.pointerEvents='none'; }
                }
            }
        };
    }).catch(function(){
        var list = document.getElementById('review-queue-list');
        if(list) list.innerHTML = '<div class="markup-note" style="color:#dc2626;">Не удалось загрузить очередь.</div>';
    });
};

function _doReviewPick(nameKey, oid, candName, url, itemName){
    fetch('/api/review-queue-pick', {
        method: 'POST',
        headers: {'Content-Type':'application/json'},
        body: JSON.stringify({name_key: nameKey, onliner_id: oid, url: url, name: itemName||''})
    }).then(function(r){ return r.json(); }).then(function(d){
        var badge = document.getElementById('review-queue-count-badge');
        var remaining = Number((d&&d.remaining)||0);
        if(badge){ badge.textContent = remaining; if(!remaining) badge.style.display='none'; }
        if(oid && tblMain && typeof tblMain.ajax !== 'undefined'){
            tblMain.ajax.reload(null, false);
        }
        var list = document.getElementById('review-queue-list');
        if(list){
            var done = list.querySelectorAll('[data-rqi]');
            var allFaded = true;
            done.forEach(function(el){ if(el.style.opacity !== '0.4') allFaded = false; });
            if(allFaded) list.innerHTML = '<div class="markup-note" style="color:#6b7280;">Очередь пуста.</div>';
        }
    }).catch(function(){});
}

window.pickReviewCandidate = _doReviewPick;

</script>
</body></html>"""
)


# ============================================================
# ROUTES
# ============================================================

@app.route("/")
def index():
    error = request.args.get("error")
    return render_template_string(UPLOAD_PAGE, error=error)


@app.route("/result")
def result_page():
    sid = str(request.args.get("sid", "") or "").strip()
    app_settings = load_app_settings()
    session_dir = session.get("session_dir")
    if sid:
        candidate_dir = UPLOAD_DIR / sid
        try:
            candidate_dir.resolve().relative_to(UPLOAD_DIR.resolve())
        except ValueError:
            return redirect(url_for("index", error="Недопустимый идентификатор сессии"))
        candidate_file = candidate_dir / "consolidated_price.xlsx"
        if candidate_file.exists():
            session_dir = str(candidate_dir)
            _finalize_processed_session(sid, candidate_dir, candidate_file)
    if not session_dir:
        return redirect(url_for("index", error="Нет активного прайса"))
    cons_path = Path(session_dir) / "consolidated_price.xlsx"
    if not cons_path.exists():
        return redirect(url_for("index", error="Файл результата не найден"))
    df = read_consolidated_df(session_dir)
    total_suppliers = len(set(str(v).strip() for v in df.get("Поставщик", pd.Series(dtype=str)).tolist() if str(v).strip()))
    without_id = _count_rows_without_onliner_id(df)
    duplicate_id_rows = _count_rows_with_duplicate_onliner_id(df)
    with_id = len(df) - without_id
    stats = {
        "total": len(df),
        "suppliers": total_suppliers,
        "consolidated": len(df),
        "matched": with_id,
        "with_id": with_id,
        "without_id": without_id,
        "duplicate_id_rows": duplicate_id_rows,
        "show_checks_block": _coerce_bool((((app_settings or {}).get("ui") or {}).get("show_checks_block", True)), default=True),
        "snapshot_diff": load_session_supplier_diff(session_dir),
    }
    return render_template_string(RESULT_PAGE, stats=stats)


@app.after_request
def add_no_cache_headers(response):
    try:
        response.headers["Cache-Control"] = "no-store, no-cache, must-revalidate, max-age=0"
        response.headers["Pragma"] = "no-cache"
        response.headers["Expires"] = "0"
    except Exception:
        pass
    return response


def get_article_from_name(name):
    """Извлечь артикул из названия."""
    if not name:
        return ""
    article = extract_article(name)
    if article:
        return article
    # No fallback to name fragments: it causes cache-key collisions
    # for similar long names (notably motherboards) and can propagate one ID.
    return ""


def _is_generic_cpu_cache_key(key):
    raw = str(key or "").strip()
    if not raw:
        return True
    low = raw.lower()
    compact = re.sub(r"[\s_]+", "", low)
    generic_patterns = [
        r"^socket[-\s]?[a-z0-9-]+$",
        r"^lga[-\s]?\d{3,5}$",
        r"^am\d+$",
        r"^fm\d+$",
        r"^tr\d+[a-z]*$",
        r"^s\d{3,5}$",
    ]
    for pattern in generic_patterns:
        if re.match(pattern, low, flags=re.IGNORECASE) or re.match(pattern, compact, flags=re.IGNORECASE):
            return True
    return False


def _is_generic_id_cache_key(key):
    raw = str(key or "").strip()
    if not raw:
        return True
    low = raw.lower()
    compact = re.sub(r"[^a-z0-9]+", "", low)
    generic_patterns = [
        r"^soc[-\s]?\d{3,5}[a-z]*$",
        r"^socket[-\s]?[a-z0-9-]{3,}$",
        r"^lga[-\s]?\d{3,5}[a-z]*$",
        r"^am\d+[a-z]*$",
        r"^fm\d+[a-z]*$",
        r"^tr\d+[a-z]*$",
    ]
    for pattern in generic_patterns:
        if re.match(pattern, low, flags=re.IGNORECASE) or re.match(pattern, compact, flags=re.IGNORECASE):
            return True
    return False


def _get_id_cache_key_for_name(name):
    article = str(get_article_from_name(name) or "").strip()
    if not article:
        return ""
    name_low = str(name or "").strip().lower()
    is_cpu_like = any(token in name_low for token in ["процессор", "intel", "amd", "ryzen", "xeon", "celeron", "pentium", "athlon", "core i"])
    if _is_generic_id_cache_key(article):
        return ""
    if is_cpu_like and _is_generic_cpu_cache_key(article):
        return ""
    return article


def _sanitize_id_cache(cache):
    if not isinstance(cache, dict):
        return {}, False
    cleaned = {}
    changed = False
    for key, value in cache.items():
        if _is_generic_cpu_cache_key(key) or _is_generic_id_cache_key(key):
            changed = True
            continue
        cleaned[key] = value
    return cleaned, changed


CATEGORY_PRIORITY = [
    "Процессор",
    "Кулер",
    "Охлаждение",
    "Материнская плата",
    "Оперативная память",
    "SSD",
    "Жесткий диск",
    "Видеокарта",
    "Блок питания",
    "Корпус",
    "Монитор",
]

CATEGORY_OVERRIDE_FILE = Path(__file__).parent / "category_overrides.json"
CATEGORY_MARKUPS_FILE = Path(__file__).parent / "category_markups.json"
CATEGORY_VISIBILITY_FILE = Path(__file__).parent / "category_visibility.json"
ONLINER_MARKET_CACHE_FILE = Path(__file__).parent / "onliner_market_cache.json"
ONLINER_PRODUCT_CACHE_FILE = Path(__file__).parent / "onliner_product_cache.json"
ONLINER_DB_FILE = Path(__file__).parent / "onliner_products.db"
_DB_WRITE_LOCK = threading.RLock()
MANUAL_ID_BINDINGS_FILE = Path(__file__).parent / "manual_id_bindings.json"
ID_CHANGE_JOURNAL_FILE = Path(__file__).parent / "id_change_journal.json"
REVIEW_QUEUE_FILE = Path(__file__).parent / "id_review_queue.json"
AUTO_REFRESH_SETTINGS_FILE = Path(__file__).parent / "auto_refresh_settings.json"
ONLINER_API_SETTINGS_FILE = Path(__file__).parent / "onliner_api_settings.json"
APP_SETTINGS_FILE = Path(__file__).parent / "app_settings.json"
SUPPLIER_SNAPSHOTS_FILE = Path(__file__).parent / "supplier_snapshots.json"
API_FETCH_HISTORY_FILE = Path(__file__).parent / "api_fetch_history.json"
ONLINER_MARKET_CACHE_TTL = 24 * 3600
ONLINER_PRODUCT_CACHE_TTL = 7 * 24 * 3600
ID_REPLACE_QUERY_CACHE_TTL = 3600
ID_REPLACE_QUERY_CACHE = {}
ID_REPLACE_QUERY_CACHE_LOCK = threading.RLock()
ID_REPLACE_QUERY_CACHE_VERSION = "v7"
ONLINER_PRODUCT_CACHE_LOCK = threading.RLock()
ONLINER_API_PROXY_STATE_LOCK = threading.RLock()
ONLINER_API_SETTINGS_LOCK = threading.RLock()
ONLINER_API_SETTINGS_CACHE = {"loaded_at": 0.0, "mtime": 0.0, "data": None}
ONLINER_API_SESSION_LOCAL = threading.local()
ONLINER_API_RETRY_STATUSES = {403, 408, 409, 425, 429, 500, 502, 503, 504, 520, 521, 522, 524}
ONLINER_API_DEFAULT_SETTINGS = {
    "proxy_pool": [],
    "allow_direct": True,
    "retry_attempts": 3,
    "backoff_sec": 0.6,
    "proxy_cooldown_sec": 180,
    "max_parallel_workers": 10,
}
ONLINER_API_PROXY_STATE = {}


def _category_sort_key(category_name):
    if category_name in CATEGORY_PRIORITY:
        return (0, CATEGORY_PRIORITY.index(category_name), category_name)
    return (1, 999, category_name.lower())


def _coerce_bool(value, default=False):
    if isinstance(value, bool):
        return value
    if value is None:
        return bool(default)
    text = str(value).strip().lower()
    if text in {"1", "true", "yes", "y", "on"}:
        return True
    if text in {"0", "false", "no", "n", "off"}:
        return False
    return bool(default)


def _coerce_int(value, default, min_value=None, max_value=None):
    try:
        num = int(value)
    except Exception:
        num = int(default)
    if min_value is not None:
        num = max(int(min_value), num)
    if max_value is not None:
        num = min(int(max_value), num)
    return num


def _coerce_float(value, default, min_value=None, max_value=None):
    try:
        num = float(value)
    except Exception:
        num = float(default)
    if min_value is not None:
        num = max(float(min_value), num)
    if max_value is not None:
        num = min(float(max_value), num)
    return num


def _normalize_proxy_entry(entry):
    if isinstance(entry, str):
        proxy_url = str(entry).strip()
        if not proxy_url:
            return None
        return {
            "key": proxy_url,
            "label": proxy_url,
            "proxies": {"http": proxy_url, "https": proxy_url},
        }
    if not isinstance(entry, dict):
        return None

    http_proxy = str(entry.get("http") or entry.get("all") or "").strip()
    https_proxy = str(entry.get("https") or entry.get("all") or http_proxy).strip()
    if not http_proxy and not https_proxy:
        return None
    key = str(entry.get("key") or entry.get("label") or http_proxy or https_proxy).strip()
    label = str(entry.get("label") or key).strip() or key
    proxies = {}
    if http_proxy:
        proxies["http"] = http_proxy
    if https_proxy:
        proxies["https"] = https_proxy
    if not proxies:
        return None
    return {"key": key, "label": label, "proxies": proxies}


def _normalize_proxy_pool(raw_pool):
    pool = []
    if isinstance(raw_pool, str):
        raw_pool = re.split(r"[\r\n,;]+", raw_pool)
    if not isinstance(raw_pool, list):
        return pool
    seen = set()
    for item in raw_pool:
        norm = _normalize_proxy_entry(item)
        if not norm:
            continue
        key = norm["key"].strip()
        if not key or key in seen:
            continue
        seen.add(key)
        pool.append(norm)
    return pool


def _read_onliner_api_settings():
    data = {}
    if ONLINER_API_SETTINGS_FILE.exists():
        try:
            with open(ONLINER_API_SETTINGS_FILE, "r", encoding="utf-8") as f:
                payload = json.load(f)
            if isinstance(payload, dict):
                data = payload
        except Exception:
            data = {}

    env_proxy_list = os.getenv("ONLINER_PROXY_LIST", "").strip()
    env_single_proxy = os.getenv("ONLINER_PROXY", "").strip()
    raw_pool = data.get("proxy_pool", [])
    if env_proxy_list:
        raw_pool = env_proxy_list
    elif env_single_proxy:
        raw_pool = [env_single_proxy]

    allow_direct = data.get("allow_direct", True)
    env_allow_direct = os.getenv("ONLINER_ALLOW_DIRECT", "").strip()
    if env_allow_direct:
        allow_direct = _coerce_bool(env_allow_direct, default=True)

    retry_attempts = data.get("retry_attempts", ONLINER_API_DEFAULT_SETTINGS["retry_attempts"])
    if os.getenv("ONLINER_RETRY_ATTEMPTS", "").strip():
        retry_attempts = os.getenv("ONLINER_RETRY_ATTEMPTS")

    backoff_sec = data.get("backoff_sec", ONLINER_API_DEFAULT_SETTINGS["backoff_sec"])
    if os.getenv("ONLINER_BACKOFF_SEC", "").strip():
        backoff_sec = os.getenv("ONLINER_BACKOFF_SEC")

    proxy_cooldown_sec = data.get("proxy_cooldown_sec", ONLINER_API_DEFAULT_SETTINGS["proxy_cooldown_sec"])
    if os.getenv("ONLINER_PROXY_COOLDOWN_SEC", "").strip():
        proxy_cooldown_sec = os.getenv("ONLINER_PROXY_COOLDOWN_SEC")

    max_parallel_workers = data.get("max_parallel_workers", ONLINER_API_DEFAULT_SETTINGS["max_parallel_workers"])
    if os.getenv("ONLINER_MAX_PARALLEL_WORKERS", "").strip():
        max_parallel_workers = os.getenv("ONLINER_MAX_PARALLEL_WORKERS")

    return {
        "proxy_pool": _normalize_proxy_pool(raw_pool),
        "allow_direct": _coerce_bool(allow_direct, default=True),
        "retry_attempts": _coerce_int(retry_attempts, ONLINER_API_DEFAULT_SETTINGS["retry_attempts"], min_value=1, max_value=12),
        "backoff_sec": _coerce_float(backoff_sec, ONLINER_API_DEFAULT_SETTINGS["backoff_sec"], min_value=0.0, max_value=5.0),
        "proxy_cooldown_sec": _coerce_int(proxy_cooldown_sec, ONLINER_API_DEFAULT_SETTINGS["proxy_cooldown_sec"], min_value=10, max_value=3600),
        "max_parallel_workers": _coerce_int(max_parallel_workers, ONLINER_API_DEFAULT_SETTINGS["max_parallel_workers"], min_value=1, max_value=24),
    }


def load_onliner_api_settings(force_reload=False):
    with ONLINER_API_SETTINGS_LOCK:
        now = time.time()
        try:
            mtime = ONLINER_API_SETTINGS_FILE.stat().st_mtime if ONLINER_API_SETTINGS_FILE.exists() else 0.0
        except Exception:
            mtime = 0.0
        cached = ONLINER_API_SETTINGS_CACHE.get("data")
        if (
            (not force_reload)
            and isinstance(cached, dict)
            and ONLINER_API_SETTINGS_CACHE.get("mtime") == mtime
            and (now - float(ONLINER_API_SETTINGS_CACHE.get("loaded_at", 0.0) or 0.0) <= 5.0)
        ):
            return dict(cached)
        settings = _read_onliner_api_settings()
        ONLINER_API_SETTINGS_CACHE["loaded_at"] = now
        ONLINER_API_SETTINGS_CACHE["mtime"] = mtime
        ONLINER_API_SETTINGS_CACHE["data"] = dict(settings)
        return dict(settings)


def save_onliner_api_settings(settings):
    payload = _read_onliner_api_settings()
    if isinstance(settings, dict):
        payload.update(settings)
    payload = {
        "proxy_pool": _normalize_proxy_pool(payload.get("proxy_pool", [])),
        "allow_direct": _coerce_bool(payload.get("allow_direct", True), default=True),
        "retry_attempts": _coerce_int(payload.get("retry_attempts", ONLINER_API_DEFAULT_SETTINGS["retry_attempts"]), ONLINER_API_DEFAULT_SETTINGS["retry_attempts"], min_value=1, max_value=12),
        "backoff_sec": _coerce_float(payload.get("backoff_sec", ONLINER_API_DEFAULT_SETTINGS["backoff_sec"]), ONLINER_API_DEFAULT_SETTINGS["backoff_sec"], min_value=0.0, max_value=5.0),
        "proxy_cooldown_sec": _coerce_int(payload.get("proxy_cooldown_sec", ONLINER_API_DEFAULT_SETTINGS["proxy_cooldown_sec"]), ONLINER_API_DEFAULT_SETTINGS["proxy_cooldown_sec"], min_value=10, max_value=3600),
        "max_parallel_workers": _coerce_int(payload.get("max_parallel_workers", ONLINER_API_DEFAULT_SETTINGS["max_parallel_workers"]), ONLINER_API_DEFAULT_SETTINGS["max_parallel_workers"], min_value=1, max_value=24),
    }
    with open(ONLINER_API_SETTINGS_FILE, "w", encoding="utf-8") as f:
        json.dump(payload, f, ensure_ascii=False, indent=2)
    with ONLINER_API_SETTINGS_LOCK:
        ONLINER_API_SETTINGS_CACHE["loaded_at"] = time.time()
        ONLINER_API_SETTINGS_CACHE["mtime"] = ONLINER_API_SETTINGS_FILE.stat().st_mtime if ONLINER_API_SETTINGS_FILE.exists() else 0.0
        ONLINER_API_SETTINGS_CACHE["data"] = dict(payload)
    return dict(payload)


def get_onliner_api_max_workers(default=10):
    settings = load_onliner_api_settings()
    return _coerce_int(settings.get("max_parallel_workers", default), default, min_value=1, max_value=24)


def _get_onliner_session():
    session_obj = getattr(ONLINER_API_SESSION_LOCAL, "session", None)
    if session_obj is None:
        session_obj = requests.Session()
        session_obj.headers.update({"User-Agent": "Mozilla/5.0", "Accept": "application/json"})
        adapter = requests.adapters.HTTPAdapter(pool_connections=32, pool_maxsize=32)
        session_obj.mount("http://", adapter)
        session_obj.mount("https://", adapter)
        ONLINER_API_SESSION_LOCAL.session = session_obj
    return session_obj


def _onliner_route_key(route):
    if not route:
        return "direct"
    return str(route.get("key") or route.get("label") or "proxy").strip() or "proxy"


def _get_onliner_routes(settings):
    routes = []
    if _coerce_bool(settings.get("allow_direct", True), default=True):
        routes.append(None)
    routes.extend(list(settings.get("proxy_pool") or []))

    now = time.time()
    with ONLINER_API_PROXY_STATE_LOCK:
        def _rank(route):
            key = _onliner_route_key(route)
            state = ONLINER_API_PROXY_STATE.get(key, {})
            blocked_until = float(state.get("blocked_until", 0.0) or 0.0)
            failures = int(state.get("failures", 0) or 0)
            available = 0 if blocked_until <= now else 1
            return (available, blocked_until, failures, key)

        return sorted(routes, key=_rank)


def _mark_onliner_route_success(route):
    key = _onliner_route_key(route)
    with ONLINER_API_PROXY_STATE_LOCK:
        state = ONLINER_API_PROXY_STATE.setdefault(key, {})
        state["blocked_until"] = 0.0
        state["last_error"] = ""
        state["last_status"] = 200
        state["successes"] = int(state.get("successes", 0) or 0) + 1
        state["failures"] = 0
        state["updated_at"] = time.time()


def _mark_onliner_route_failure(route, reason, cooldown_sec, status_code=0):
    key = _onliner_route_key(route)
    with ONLINER_API_PROXY_STATE_LOCK:
        state = ONLINER_API_PROXY_STATE.setdefault(key, {})
        state["blocked_until"] = time.time() + max(0, int(cooldown_sec or 0))
        state["last_error"] = str(reason or "").strip()
        state["last_status"] = int(status_code or 0)
        state["failures"] = int(state.get("failures", 0) or 0) + 1
        state["updated_at"] = time.time()


def onliner_api_get(url, timeout=8, headers=None):
    settings = load_onliner_api_settings()
    routes = _get_onliner_routes(settings)
    if not routes:
        routes = [None]

    max_attempts = max(1, min(len(routes), _coerce_int(settings.get("retry_attempts", 3), 3, min_value=1, max_value=12)))
    cooldown_sec = _coerce_int(settings.get("proxy_cooldown_sec", 180), 180, min_value=10, max_value=3600)
    backoff_sec = _coerce_float(settings.get("backoff_sec", 0.6), 0.6, min_value=0.0, max_value=5.0)
    merged_headers = {"User-Agent": "Mozilla/5.0", "Accept": "application/json"}
    if isinstance(headers, dict):
        merged_headers.update(headers)

    last_response = None
    last_error = None
    session_obj = _get_onliner_session()
    for attempt_index, route in enumerate(routes[:max_attempts]):
        try:
            response = session_obj.get(
                url,
                timeout=timeout,
                headers=merged_headers,
                proxies=(route or {}).get("proxies") if route else None,
            )
            if response.ok:
                _mark_onliner_route_success(route)
                return response
            last_response = response
            if int(response.status_code or 0) in ONLINER_API_RETRY_STATUSES and attempt_index + 1 < max_attempts:
                _mark_onliner_route_failure(route, f"http_{response.status_code}", cooldown_sec, status_code=response.status_code)
                if backoff_sec > 0:
                    time.sleep(backoff_sec * (attempt_index + 1))
                continue
            return response
        except Exception as exc:
            last_error = exc
            _mark_onliner_route_failure(route, exc.__class__.__name__, cooldown_sec)
            if attempt_index + 1 < max_attempts and backoff_sec > 0:
                time.sleep(backoff_sec * (attempt_index + 1))

    if last_response is not None:
        return last_response
    raise last_error if last_error is not None else RuntimeError("onliner_api_get_failed")


def load_category_overrides():
    if not CATEGORY_OVERRIDE_FILE.exists():
        return {}
    try:
        with open(CATEGORY_OVERRIDE_FILE, "r", encoding="utf-8") as f:
            data = json.load(f)
        if isinstance(data, dict):
            # Legacy cleanup: keys by article caused cross-category contamination.
            cleaned = {k: v for k, v in data.items() if not str(k).startswith("art:")}
            # Defensive cleanup: drop obviously conflicting saved mappings.
            # Example: keys containing "корпус"/"кулер"/"сжо" mapped to "Блок питания".
            suspicious = []
            for k, v in cleaned.items():
                kk = str(k or "").lower()
                vv = str(v or "").strip()
                if vv == "Блок питания":
                    if re.search(r"\bкорпус\b|\bcase\b|\bкулер\b|cooler|охлажден|сжо|водян|fan", kk):
                        suspicious.append(k)
            for k in suspicious:
                cleaned.pop(k, None)
            if len(cleaned) != len(data):
                try:
                    save_category_overrides(cleaned)
                except Exception:
                    pass
            return cleaned
    except Exception:
        pass
    return {}


def save_category_overrides(overrides):
    with open(CATEGORY_OVERRIDE_FILE, "w", encoding="utf-8") as f:
        json.dump(overrides, f, ensure_ascii=False, indent=2)


def load_category_markups():
    if not CATEGORY_MARKUPS_FILE.exists():
        return {}
    try:
        with open(CATEGORY_MARKUPS_FILE, "r", encoding="utf-8") as f:
            data = json.load(f)
        if isinstance(data, dict):
            return data
    except Exception:
        pass
    return {}


def save_category_markups(markups):
    with open(CATEGORY_MARKUPS_FILE, "w", encoding="utf-8") as f:
        json.dump(markups, f, ensure_ascii=False, indent=2)


def get_category_markup_config(markups, category):
    raw = (markups or {}).get(category)
    if isinstance(raw, dict):
        try:
            percent = float(raw.get("percent", 0))
        except Exception:
            percent = 0.0
        try:
            threshold = float(raw.get("threshold", 0))
        except Exception:
            threshold = 0.0
        try:
            min_profit = float(raw.get("min_profit", 0))
        except Exception:
            min_profit = 0.0
        try:
            no_discount_percent = float(raw.get("no_discount_percent", 0))
        except Exception:
            no_discount_percent = 0.0
        base_mode = str(raw.get("base_mode", "wholesale")).strip().lower()
    else:
        try:
            percent = float(raw)
        except Exception:
            percent = 0.0
        threshold = 0.0
        min_profit = 0.0
        no_discount_percent = 0.0
        base_mode = "wholesale"
    if base_mode not in {"wholesale", "onliner_min", "onliner_avg", "onliner_max"}:
        base_mode = "wholesale"
    return {
        "percent": percent,
        "threshold": max(0.0, threshold),
        "min_profit": max(0.0, min_profit),
        "no_discount_percent": max(0.0, no_discount_percent),
        "base_mode": base_mode,
    }


def calc_rrc_and_no_discount(base_price, percent, threshold=0.0, min_profit=0.0, no_discount_percent=0.0):
    base_price = pd.to_numeric(base_price, errors="coerce")
    if pd.isna(base_price):
        return np.nan, np.nan
    base_price = float(base_price)
    percent = max(0.0, float(percent or 0.0))
    threshold = max(0.0, float(threshold or 0.0))
    min_profit = max(0.0, float(min_profit or 0.0))
    no_discount_percent = max(0.0, float(no_discount_percent or 0.0))

    calc_rrc = base_price * (1.0 + percent / 100.0)
    if base_price <= threshold and threshold > 0:
        calc_rrc = max(calc_rrc, base_price + min_profit)
    rrc = round_price_to_90(calc_rrc)

    no_discount_price = np.nan
    if not pd.isna(rrc):
        no_discount_price = round_price_to_90(float(rrc) * (1.0 + no_discount_percent / 100.0))

    return rrc, no_discount_price


def _normalize_auto_refresh_settings(data=None):
    data = data or {}
    if not isinstance(data, dict):
        data = {}
    enabled = bool(data.get("enabled", False))
    interval = int(data.get("interval_hours", 12) or 12)
    if interval not in AUTO_REFRESH_ALLOWED_HOURS:
        interval = 12
    out = {
        "enabled": enabled,
        "interval_hours": interval,
        "last_run_ts": int(data.get("last_run_ts", 0) or 0),
        "last_started_ts": int(data.get("last_started_ts", 0) or 0),
        "last_status": str(data.get("last_status", "idle") or "idle"),
        "last_count": int(data.get("last_count", 0) or 0),
        "last_message": str(data.get("last_message", "") or ""),
    }
    return out


def load_auto_refresh_settings():
    if not AUTO_REFRESH_SETTINGS_FILE.exists():
        return _normalize_auto_refresh_settings()
    try:
        with open(AUTO_REFRESH_SETTINGS_FILE, "r", encoding="utf-8") as f:
            data = json.load(f)
        return _normalize_auto_refresh_settings(data)
    except Exception:
        return _normalize_auto_refresh_settings()


def save_auto_refresh_settings(settings):
    payload = _normalize_auto_refresh_settings(settings)
    with open(AUTO_REFRESH_SETTINGS_FILE, "w", encoding="utf-8") as f:
        json.dump(payload, f, ensure_ascii=False, indent=2)


APP_SETTINGS_DEFAULTS = {
    "export": {
        "include_without_id": True,
        "price_name": "consolidated_price",
        "keep_lowest_price_per_onliner_id": False,
        "exclude_duplicate_id_suppliers": [],
        "only_pc_suppliers": [],
        "only_pc_price_name": "N-tech_TGPC_Beznal",
        "google_sheets_spreadsheet_url_or_id": "",
        "google_sheets_tab": "Прайс N-Tech",
        "google_sheets_service_account_json": "",
    },
    "onliner_db_import": {
        "google_sheet_id": "",
        "google_sheet_name": "All_Catalog",
    },
    "ui": {
        "show_checks_block": True,
    },
    "cache_api": {
        "allow_direct": True,
        "retry_attempts": 3,
        "backoff_sec": 0.6,
        "proxy_cooldown_sec": 180,
        "max_parallel_workers": 10,
    },
    "onliner_b2b": {
        "enabled": False,
        "base_url": "https://b2bapi.onliner.by",
        "price_api_base_url": "https://price.api.onliner.by",
        "client_id": "",
        "client_secret": "",
        "token_url": "https://b2bapi.onliner.by/oauth/token",
        "verify_ssl": True,
        "timeout_sec": 20,
    },
    "suppliers": {
        "filename_rules": [
            {"pattern": "tradex", "supplier": "Tradex"},
            {"pattern": "1030z", "supplier": "BN-1030Z"},
            {"pattern": "1030", "supplier": "BN-1030"},
            {"pattern": "1374", "supplier": "BN-1374"},
            {"pattern": "price_bn", "supplier": "TGPC"},
        ],
    },
    "api_sources": {
        "iven": {
            "enabled": False,
            "label": "IVEN",
            "supplier": "IVEN",
            "mode": "direct_file",
            "file_url": "",
            "verify_ssl": False,
        },
        "tradex": {
            "enabled": False,
            "label": "Tradex",
            "supplier": "Tradex",
            "mode": "direct_file",
            "file_url": "",
            "verify_ssl": False,
        },
        "ntech": {
            "enabled": False,
            "label": "N-Tech",
            "supplier": "N-Tech",
            "mode": "ntech_json",
            "auth_url": "",
            "price_url": "",
            "username": "",
            "password": "",
            "verify_ssl": False,
        },
    },
    "uploads_cleanup": {
        "keep_last_sessions": 20,
        "keep_days": 7,
        "keep_api_fetch_hours": 12,
    },
    "no_id_search": {
        "max_candidates": 80,
        "max_queries": 4,
        "prefer_paren_model": True,
        "prefer_article_tokens": True,
        "include_brand_token": True,
        "require_category_hint": False,
        "category_rules_text": json.dumps({
            "Процессор": {"query_hint": "cpu processor", "must_contain": ["ryzen", "intel", "xeon"], "ignore_words": ["cooler"]},
            "Видеокарта": {"query_hint": "videocard gpu", "must_contain": ["rtx", "gtx", "radeon"], "ignore_words": ["holder"]},
            "Оперативная память": {"query_hint": "dram ddr memory", "must_contain": ["ddr", "dimm", "sodimm"], "ignore_words": ["ssd"]},
            "SSD": {"query_hint": "ssd nvme", "must_contain": ["ssd", "nvme", "m.2"], "ignore_words": ["dram"]},
            "Жесткий диск": {"query_hint": "hdd hard drive", "must_contain": ["hdd", "sata", "7200"], "ignore_words": ["ssd"]},
        }, ensure_ascii=False, indent=2),
    },
    "verify_id": {
        "match_threshold": 0.74,
        "trust_manual_confirmed": True,
        "force_refresh_api": True,
        "api_no_name_status": "review",
        "require_article_or_model_priority": False,
    },
}


def _deep_merge_dict(base, extra):
    out = dict(base or {})
    for key, value in (extra or {}).items():
        if isinstance(value, dict) and isinstance(out.get(key), dict):
            out[key] = _deep_merge_dict(out.get(key), value)
        else:
            out[key] = value
    return out


def _normalize_filename_rules(rules):
    out = []
    if not isinstance(rules, list):
        return list(APP_SETTINGS_DEFAULTS["suppliers"]["filename_rules"])
    for item in rules:
        if not isinstance(item, dict):
            continue
        pattern = str(item.get("pattern", "")).strip()
        supplier = str(item.get("supplier", "")).strip()
        if not pattern or not supplier:
            continue
        out.append({"pattern": pattern, "supplier": supplier})
    return out or list(APP_SETTINGS_DEFAULTS["suppliers"]["filename_rules"])


def _normalize_api_sources(sources):
    defaults = APP_SETTINGS_DEFAULTS["api_sources"]
    out = {}
    raw = sources if isinstance(sources, dict) else {}
    for key, default_cfg in defaults.items():
        cfg = raw.get(key, {}) if isinstance(raw.get(key), dict) else {}
        mode = str(cfg.get("mode", default_cfg.get("mode", "direct_file"))).strip() or default_cfg.get("mode", "direct_file")
        item = {
            "enabled": _coerce_bool(cfg.get("enabled", default_cfg.get("enabled", False)), default=bool(default_cfg.get("enabled", False))),
            "label": str(cfg.get("label", default_cfg.get("label", key.upper()))).strip()[:40] or str(default_cfg.get("label", key.upper())),
            "supplier": str(cfg.get("supplier", default_cfg.get("supplier", key.upper()))).strip()[:80] or str(default_cfg.get("supplier", key.upper())),
            "mode": mode,
            "verify_ssl": _coerce_bool(cfg.get("verify_ssl", default_cfg.get("verify_ssl", False)), default=bool(default_cfg.get("verify_ssl", False))),
        }
        if mode == "ntech_json":
            item.update({
                "auth_url": str(cfg.get("auth_url", default_cfg.get("auth_url", ""))).strip(),
                "price_url": str(cfg.get("price_url", default_cfg.get("price_url", ""))).strip(),
                "username": str(cfg.get("username", default_cfg.get("username", ""))).strip(),
                "password": str(cfg.get("password", default_cfg.get("password", ""))),
            })
        else:
            item.update({
                "file_url": str(cfg.get("file_url", default_cfg.get("file_url", ""))).strip(),
            })
        out[key] = item
    return out


def _normalize_supplier_name_list(value):
    if isinstance(value, list):
        raw_items = value
    else:
        raw_items = re.split(r"[\r\n,;]+", str(value or ""))
    out = []
    seen = set()
    for item in raw_items:
        name = str(item or "").strip()
        low = name.lower()
        if not name or low in seen:
            continue
        seen.add(low)
        out.append(name[:80])
    return out


def _normalize_app_settings(data=None):
    merged = _deep_merge_dict(APP_SETTINGS_DEFAULTS, data if isinstance(data, dict) else {})
    export = merged.get("export", {})
    onliner_db_import = merged.get("onliner_db_import", {})
    ui = merged.get("ui", {})
    cache_api = merged.get("cache_api", {})
    onliner_b2b = merged.get("onliner_b2b", {})
    uploads_cleanup = merged.get("uploads_cleanup", {})
    no_id_search = merged.get("no_id_search", {})
    verify_id = merged.get("verify_id", {})
    suppliers = merged.get("suppliers", {})
    api_sources = merged.get("api_sources", {})
    return {
        "export": {
            "include_without_id": _coerce_bool(export.get("include_without_id", True), default=True),
            "price_name": re.sub(r"[^A-Za-zА-Яа-я0-9._ -]+", "", str(export.get("price_name", "consolidated_price")).strip())[:80] or "consolidated_price",
            "keep_lowest_price_per_onliner_id": _coerce_bool(export.get("keep_lowest_price_per_onliner_id", False), default=False),
            "exclude_duplicate_id_suppliers": _normalize_supplier_name_list(export.get("exclude_duplicate_id_suppliers", [])),
            "only_pc_suppliers": _normalize_supplier_name_list(export.get("only_pc_suppliers", [])),
            "only_pc_price_name": re.sub(r"[^A-Za-zА-Яа-я0-9._ -]+", "", str(export.get("only_pc_price_name", "N-tech_TGPC_Beznal")).strip())[:80] or "N-tech_TGPC_Beznal",
            "google_sheets_spreadsheet_url_or_id": str(export.get("google_sheets_spreadsheet_url_or_id", "") or "").strip()[:500],
            "google_sheets_tab": str(export.get("google_sheets_tab", "Прайс N-Tech") or "Прайс N-Tech").strip()[:99] or "Прайс N-Tech",
            "google_sheets_service_account_json": str(export.get("google_sheets_service_account_json", "") or "").strip()[:500],
        },
        "onliner_db_import": {
            "google_sheet_id": str(onliner_db_import.get("google_sheet_id", "") or "").strip()[:240],
            "google_sheet_name": str(onliner_db_import.get("google_sheet_name", "All_Catalog") or "All_Catalog").strip()[:120] or "All_Catalog",
        },
        "ui": {
            "show_checks_block": _coerce_bool(ui.get("show_checks_block", True), default=True),
        },
        "cache_api": {
            "allow_direct": _coerce_bool(cache_api.get("allow_direct", True), default=True),
            "retry_attempts": _coerce_int(cache_api.get("retry_attempts", 3), 3, min_value=1, max_value=12),
            "backoff_sec": _coerce_float(cache_api.get("backoff_sec", 0.6), 0.6, min_value=0.0, max_value=5.0),
            "proxy_cooldown_sec": _coerce_int(cache_api.get("proxy_cooldown_sec", 180), 180, min_value=10, max_value=3600),
            "max_parallel_workers": _coerce_int(cache_api.get("max_parallel_workers", 10), 10, min_value=1, max_value=24),
        },
        "onliner_b2b": {
            "enabled": _coerce_bool(onliner_b2b.get("enabled", False), default=False),
            "base_url": str(onliner_b2b.get("base_url", "https://b2bapi.onliner.by") or "https://b2bapi.onliner.by").strip()[:200],
            "price_api_base_url": str(onliner_b2b.get("price_api_base_url") or "https://price.api.onliner.by").strip()[:200],
            "client_id": str(onliner_b2b.get("client_id", "") or "").strip()[:200],
            "client_secret": str(onliner_b2b.get("client_secret", "") or "").strip()[:400],
            "token_url": str(onliner_b2b.get("token_url", "https://b2bapi.onliner.by/oauth/token") or "https://b2bapi.onliner.by/oauth/token").strip()[:240],
            "verify_ssl": _coerce_bool(onliner_b2b.get("verify_ssl", True), default=True),
            "timeout_sec": _coerce_int(onliner_b2b.get("timeout_sec", 20), 20, min_value=5, max_value=120),
        },
        "suppliers": {
            "filename_rules": _normalize_filename_rules(suppliers.get("filename_rules", [])),
        },
        "api_sources": _normalize_api_sources(api_sources),
        "uploads_cleanup": {
            "keep_last_sessions": _coerce_int(uploads_cleanup.get("keep_last_sessions", 20), 20, min_value=3, max_value=200),
            "keep_days": _coerce_int(uploads_cleanup.get("keep_days", 7), 7, min_value=1, max_value=90),
            "keep_api_fetch_hours": _coerce_int(uploads_cleanup.get("keep_api_fetch_hours", 12), 12, min_value=1, max_value=168),
        },
        "no_id_search": {
            "max_candidates": _coerce_int(no_id_search.get("max_candidates", 80), 80, min_value=10, max_value=150),
            "max_queries": _coerce_int(no_id_search.get("max_queries", 4), 4, min_value=1, max_value=8),
            "prefer_paren_model": _coerce_bool(no_id_search.get("prefer_paren_model", True), default=True),
            "prefer_article_tokens": _coerce_bool(no_id_search.get("prefer_article_tokens", True), default=True),
            "include_brand_token": _coerce_bool(no_id_search.get("include_brand_token", True), default=True),
            "require_category_hint": _coerce_bool(no_id_search.get("require_category_hint", False), default=False),
            "category_rules_text": str(no_id_search.get("category_rules_text", APP_SETTINGS_DEFAULTS["no_id_search"]["category_rules_text"]) or APP_SETTINGS_DEFAULTS["no_id_search"]["category_rules_text"]).strip(),
        },
        "verify_id": {
            "match_threshold": _coerce_float(verify_id.get("match_threshold", 0.74), 0.74, min_value=0.1, max_value=0.99),
            "trust_manual_confirmed": _coerce_bool(verify_id.get("trust_manual_confirmed", True), default=True),
            "force_refresh_api": _coerce_bool(verify_id.get("force_refresh_api", True), default=True),
            "api_no_name_status": "mismatch" if str(verify_id.get("api_no_name_status", "review")).strip().lower() == "mismatch" else "review",
            "require_article_or_model_priority": _coerce_bool(verify_id.get("require_article_or_model_priority", False), default=False),
        },
        "uploads_cleanup": {
            "keep_last_sessions": _coerce_int(uploads_cleanup.get("keep_last_sessions", 20), 20, min_value=3, max_value=200),
            "keep_days": _coerce_int(uploads_cleanup.get("keep_days", 7), 7, min_value=1, max_value=90),
            "keep_api_fetch_hours": _coerce_int(uploads_cleanup.get("keep_api_fetch_hours", 12), 12, min_value=1, max_value=168),
        },
    }


def load_app_settings():
    if not APP_SETTINGS_FILE.exists():
        return _normalize_app_settings()
    try:
        with open(APP_SETTINGS_FILE, "r", encoding="utf-8") as f:
            data = json.load(f)
    except Exception:
        data = {}
    return _normalize_app_settings(data)


def save_app_settings(settings):
    payload = _normalize_app_settings(settings)
    invalidate_onliner_b2b_token()
    with open(APP_SETTINGS_FILE, "w", encoding="utf-8") as f:
        json.dump(payload, f, ensure_ascii=False, indent=2)
    save_onliner_api_settings(payload.get("cache_api", {}))
    return payload


def get_onliner_b2b_settings():
    settings = load_app_settings()
    cfg = (settings.get("onliner_b2b") or {})
    return {
        "enabled": _coerce_bool(cfg.get("enabled", False), default=False),
        "base_url": str(cfg.get("base_url", "https://b2bapi.onliner.by") or "https://b2bapi.onliner.by").strip(),
        "price_api_base_url": str(cfg.get("price_api_base_url") or "https://price.api.onliner.by").strip(),
        "client_id": str(cfg.get("client_id", "") or "").strip(),
        "client_secret": str(cfg.get("client_secret", "") or "").strip(),
        "token_url": str(cfg.get("token_url", "https://b2bapi.onliner.by/oauth/token") or "https://b2bapi.onliner.by/oauth/token").strip(),
        "verify_ssl": _coerce_bool(cfg.get("verify_ssl", True), default=True),
        "timeout_sec": _coerce_int(cfg.get("timeout_sec", 20), 20, min_value=5, max_value=120),
    }


def invalidate_onliner_b2b_token():
    with B2B_TOKEN_LOCK:
        B2B_TOKEN_CACHE["access_token"] = ""
        B2B_TOKEN_CACHE["expires_at"] = 0


def onliner_b2b_get_token(force_refresh=False):
    cfg = get_onliner_b2b_settings()
    if not cfg.get("enabled"):
        raise RuntimeError("Onliner B2B API выключен в настройках.")
    client_id = str(cfg.get("client_id", "") or "").strip()
    client_secret = str(cfg.get("client_secret", "") or "").strip()
    if not client_id or not client_secret:
        raise RuntimeError("Не заполнены Client ID / Client Secret для Onliner B2B API.")

    now = int(time.time())
    with B2B_TOKEN_LOCK:
        cached_token = str(B2B_TOKEN_CACHE.get("access_token", "") or "").strip()
        cached_expires_at = int(B2B_TOKEN_CACHE.get("expires_at", 0) or 0)
        if (not force_refresh) and cached_token and cached_expires_at - now > 30:
            return {
                "access_token": cached_token,
                "expires_at": cached_expires_at,
                "expires_in": max(0, cached_expires_at - now),
                "token_type": "Bearer",
                "source": "cache",
            }

    pair = f"{client_id}:{client_secret}"
    basic = base64.b64encode(pair.encode("utf-8")).decode("ascii")
    headers = {
        "Accept": "application/json",
        "Authorization": f"Basic {basic}",
    }
    resp = requests.post(
        str(cfg.get("token_url") or "https://b2bapi.onliner.by/oauth/token"),
        headers=headers,
        data={"grant_type": "client_credentials"},
        verify=bool(cfg.get("verify_ssl", True)),
        timeout=int(cfg.get("timeout_sec", 20) or 20),
    )
    resp.raise_for_status()
    payload = resp.json() or {}
    access_token = str(payload.get("access_token", "") or "").strip()
    expires_in = _coerce_int(payload.get("expires_in", 300), 300, min_value=60, max_value=86400)
    token_type = str(payload.get("token_type", "Bearer") or "Bearer").strip() or "Bearer"
    if not access_token:
        raise RuntimeError("Onliner B2B API не вернул access_token.")
    expires_at = int(time.time()) + int(expires_in)
    with B2B_TOKEN_LOCK:
        B2B_TOKEN_CACHE["access_token"] = access_token
        B2B_TOKEN_CACHE["expires_at"] = expires_at
    return {
        "access_token": access_token,
        "expires_at": expires_at,
        "expires_in": expires_in,
        "token_type": token_type,
        "source": "oauth",
    }


def onliner_b2b_request(method, path, params=None, json_body=None, force_token_refresh=False):
    cfg = get_onliner_b2b_settings()
    token_info = onliner_b2b_get_token(force_refresh=force_token_refresh)
    base_url = str(cfg.get("base_url", "https://b2bapi.onliner.by") or "https://b2bapi.onliner.by").rstrip("/")
    rel_path = "/" + str(path or "").lstrip("/")
    headers = {
        "Accept": "application/json",
        "Authorization": f"Bearer {token_info['access_token']}",
    }
    resp = requests.request(
        method=str(method or "GET").upper(),
        url=base_url + rel_path,
        headers=headers,
        params=params,
        json=json_body,
        verify=bool(cfg.get("verify_ssl", True)),
        timeout=int(cfg.get("timeout_sec", 20) or 20),
    )
    return resp


def onliner_b2b_price_request(method, path, params=None, json_body=None, force_token_refresh=False):
    """HTTP к API прайс-листов (экспорт позиций / цены конкурентов), хост по умолчанию price.api.onliner.by."""
    cfg = get_onliner_b2b_settings()
    token_info = onliner_b2b_get_token(force_refresh=force_token_refresh)
    base_url = str(cfg.get("price_api_base_url", "https://price.api.onliner.by") or "https://price.api.onliner.by").rstrip("/")
    rel_path = "/" + str(path or "").lstrip("/")
    headers = {
        "Accept": "application/json",
        "Authorization": f"Bearer {token_info['access_token']}",
    }
    return requests.request(
        method=str(method or "GET").upper(),
        url=base_url + rel_path,
        headers=headers,
        params=params,
        json=json_body,
        verify=bool(cfg.get("verify_ssl", True)),
        timeout=int(cfg.get("timeout_sec", 20) or 20),
    )


def _normalize_b2b_dict_items(payload):
    if isinstance(payload, list):
        out = []
        for idx, item in enumerate(payload):
            if isinstance(item, dict):
                entry = dict(item)
                if "id" not in entry and idx is not None:
                    entry["id"] = str(idx)
                out.append(entry)
        return out
    if not isinstance(payload, dict):
        return []
    out = []
    numeric_like_keys = []
    for raw_key, raw_value in payload.items():
        key = str(raw_key).strip()
        if re.fullmatch(r"\d+", key or ""):
            numeric_like_keys.append(key)
        if isinstance(raw_value, dict):
            entry = dict(raw_value)
            entry.setdefault("id", key)
            out.append(entry)
        else:
            out.append({"id": key, "name": str(raw_value or "").strip()})
    if numeric_like_keys and len(numeric_like_keys) == len(payload):
        return out
    for nested_key in ("items", "data", "results", "sections", "manufacturers", "products"):
        nested = payload.get(nested_key)
        if isinstance(nested, (list, dict)):
            items = _normalize_b2b_dict_items(nested)
            if items:
                return items
    return out


def _b2b_cache_get(bucket, key=None, ttl=3600):
    now = int(time.time())
    with B2B_CATALOG_LOCK:
        holder = B2B_CATALOG_CACHE.get(bucket)
        if key is None:
            if isinstance(holder, dict) and now - int(holder.get("ts", 0) or 0) <= ttl:
                return holder.get("items")
            return None
        if isinstance(holder, dict):
            item = holder.get(str(key))
            if isinstance(item, dict) and now - int(item.get("ts", 0) or 0) <= ttl:
                return item.get("items")
    return None


def _b2b_cache_set(bucket, items, key=None):
    payload = {"ts": int(time.time()), "items": items if isinstance(items, list) else []}
    with B2B_CATALOG_LOCK:
        if key is None:
            B2B_CATALOG_CACHE[bucket] = payload
        else:
            if not isinstance(B2B_CATALOG_CACHE.get(bucket), dict):
                B2B_CATALOG_CACHE[bucket] = {}
            B2B_CATALOG_CACHE[bucket][str(key)] = payload


def onliner_b2b_get_sections(force_refresh=False):
    cached = None if force_refresh else _b2b_cache_get("sections", ttl=12 * 3600)
    if isinstance(cached, list):
        return cached
    resp = onliner_b2b_request("GET", "/sections", force_token_refresh=force_refresh)
    resp.raise_for_status()
    items = _normalize_b2b_dict_items(resp.json() if resp.content else {})
    _b2b_cache_set("sections", items)
    return items


def onliner_b2b_get_manufacturers(section_id, force_refresh=False):
    sid = str(section_id or "").strip()
    if not sid:
        return []
    cache_key = sid
    cached = None if force_refresh else _b2b_cache_get("manufacturers", key=cache_key, ttl=12 * 3600)
    if isinstance(cached, list):
        return cached
    resp = onliner_b2b_request("GET", f"/sections/{sid}/manufacturers", force_token_refresh=force_refresh)
    resp.raise_for_status()
    items = _normalize_b2b_dict_items(resp.json() if resp.content else {})
    _b2b_cache_set("manufacturers", items, key=cache_key)
    return items


def onliner_b2b_get_products(section_id, manufacturer_id, title="", force_refresh=False):
    sid = str(section_id or "").strip()
    mid = str(manufacturer_id or "").strip()
    title = str(title or "").strip()
    if not sid or not mid:
        return []
    cache_key = f"{sid}|{mid}|{title.lower()}"
    cached = None if force_refresh else _b2b_cache_get("products", key=cache_key, ttl=6 * 3600)
    if isinstance(cached, list):
        return cached
    params = {"title": title} if title else None
    resp = onliner_b2b_request("GET", f"/sections/{sid}/manufacturers/{mid}/products", params=params, force_token_refresh=force_refresh)
    resp.raise_for_status()
    items = _normalize_b2b_dict_items(resp.json() if resp.content else {})
    _b2b_cache_set("products", items, key=cache_key)
    return items


def onliner_b2b_get_articles(section_id, manufacturer_id, product_id, force_refresh=False):
    sid = str(section_id or "").strip()
    mid = str(manufacturer_id or "").strip()
    pid = normalize_onliner_id(product_id)
    if not sid or not mid or not pid:
        return []
    cache_key = f"{sid}|{mid}|{pid}"
    cached = None if force_refresh else _b2b_cache_get("articles", key=cache_key, ttl=24 * 3600)
    if isinstance(cached, list):
        return cached
    resp = onliner_b2b_request("GET", f"/sections/{sid}/manufacturers/{mid}/products/{pid}/articles", force_token_refresh=force_refresh)
    resp.raise_for_status()
    raw_items = resp.json() if resp.content else []
    items = []
    for item in (raw_items if isinstance(raw_items, list) else []):
        if isinstance(item, dict):
            art = str(item.get("article", "") or "").strip()
        else:
            art = str(item or "").strip()
        if art:
            items.append(art)
    _b2b_cache_set("articles", items, key=cache_key)
    return items


def onliner_b2b_resolve_catalog_path_for_product(target_oid, product_name="", category_name="", force_refresh=False):
    """По Onliner ID и названию находит sectionId и manufacturerId в B2B-каталоге (кешируется)."""
    cfg = get_onliner_b2b_settings()
    if not cfg.get("enabled"):
        return None, None
    oid = normalize_onliner_id(target_oid)
    if not oid:
        return None, None
    cache_key = str(oid)
    if not force_refresh:
        cached = _b2b_cache_get("product_path", key=cache_key, ttl=7 * 24 * 3600)
        if isinstance(cached, list) and len(cached) >= 2 and cached[0] and cached[1]:
            return str(cached[0]), str(cached[1])

    name = str(product_name or "").strip()
    if not name:
        dbp = db_get_product_by_id(oid)
        if isinstance(dbp, dict):
            name = str(dbp.get("name", "") or "").strip()
    if not name:
        return None, None

    try:
        sections = onliner_b2b_get_sections()
    except Exception:
        return None, None

    cat_hint = str(category_name or "").strip()
    section_tokens = _b2b_section_tokens(cat_hint, name)
    candidate_sections = []
    for section in sections:
        sec_id = str(section.get("id", "") or "").strip()
        sec_name = str(section.get("name", "") or "").strip()
        if not sec_id or not sec_name:
            continue
        low_name = sec_name.lower()
        score = 0
        for token in section_tokens:
            if token and token in low_name:
                score = max(score, len(token))
        if score > 0:
            candidate_sections.append((score, sec_id, sec_name))
    candidate_sections.sort(key=lambda x: x[0], reverse=True)
    candidate_sections = candidate_sections[:6] if candidate_sections else []
    if not candidate_sections and sections:
        candidate_sections = [
            (
                0,
                str(sections[i].get("id", "") or "").strip(),
                str(sections[i].get("name", "") or ""),
            )
            for i in range(min(8, len(sections)))
            if str(sections[i].get("id", "") or "").strip()
        ]

    brand = _preferred_brand_token(name)
    queries = []
    seen_queries = set()

    def _add_query(value):
        q = str(value or "").strip()
        if not q:
            return
        key = q.lower()
        if key in seen_queries:
            return
        seen_queries.add(key)
        queries.append(q)

    article = str(extract_article(name) or "").strip()
    if article:
        _add_query(article)
    for q in _priority_model_queries(name)[:2]:
        _add_query(q)
    _add_query(" ".join(_name_tokens(name)[:6]))
    _add_query(name[:90])
    queries = queries[:4]

    for _score, sec_id, _sec_name in candidate_sections:
        try:
            manufacturers = onliner_b2b_get_manufacturers(sec_id)
        except Exception:
            continue
        if brand:
            matching_manufacturers = [
                m for m in manufacturers
                if brand.lower() in str(m.get("name", "") or "").lower()
            ]
        else:
            matching_manufacturers = list(manufacturers[:6])
        if not matching_manufacturers:
            matching_manufacturers = list(manufacturers[:8])
        for manufacturer in matching_manufacturers[:10]:
            mid = str(manufacturer.get("id", "") or "").strip()
            if not mid:
                continue
            # Быстрый путь: один запрос полного списка товаров производителя (кеш sid|mid|),
            # вместо нескольких запросов с title — так в разы быстрее при массовом обновлении.
            try:
                products_full = onliner_b2b_get_products(sec_id, mid, title="")
            except Exception:
                products_full = []
            _scan_cap = 12000
            for i, product in enumerate(products_full):
                if i >= _scan_cap:
                    break
                if not isinstance(product, dict):
                    continue
                pid = normalize_onliner_id(product.get("id", ""))
                if pid == oid:
                    _b2b_cache_set("product_path", [sec_id, mid], key=cache_key)
                    return str(sec_id), str(mid)
            for query in queries:
                try:
                    products = onliner_b2b_get_products(sec_id, mid, title=query)
                except Exception:
                    continue
                for product in products[:160]:
                    if not isinstance(product, dict):
                        continue
                    pid = normalize_onliner_id(product.get("id", ""))
                    if pid == oid:
                        _b2b_cache_set("product_path", [sec_id, mid], key=cache_key)
                        return str(sec_id), str(mid)
    return None, None


def onliner_b2b_fetch_product_positions_export(section_id, manufacturer_id, product_id, force_token_refresh=False):
    """GET …/products/{productId}/positions — позиции (цены конкурентов) в формате прайс-листа."""
    sid = str(section_id or "").strip()
    mid = str(manufacturer_id or "").strip()
    pid = normalize_onliner_id(product_id)
    if not sid or not mid or not pid:
        return []
    rel = f"/sections/{sid}/manufacturers/{mid}/products/{pid}/positions"
    resp = onliner_b2b_price_request("GET", rel, force_token_refresh=force_token_refresh)
    resp.raise_for_status()
    payload = resp.json() if resp.content else []
    if isinstance(payload, list):
        return [x for x in payload if isinstance(x, dict)]
    if isinstance(payload, dict):
        pos = payload.get("positions")
        if isinstance(pos, list):
            return [x for x in pos if isinstance(x, dict)]
    return _normalize_b2b_dict_items(payload)


def _parse_b2b_price_string(raw):
    if raw is None:
        return None
    if isinstance(raw, (int, float)) and not isinstance(raw, bool):
        v = float(raw)
        return v if v > 0 else None
    if isinstance(raw, dict):
        for k in ("amount", "value", "BYN", "byn", "converted"):
            nested = raw.get(k)
            if isinstance(nested, dict):
                pv = _parse_b2b_price_string(nested.get("amount") or nested.get("value"))
                if pv is not None and pv > 0:
                    return pv
            else:
                pv = _parse_b2b_price_string(nested)
                if pv is not None and pv > 0:
                    return pv
        return None
    s = str(raw).strip()
    if not s:
        return None
    s = s.replace(" ", "").replace(",", ".")
    return _safe_float(s)


def _b2b_row_price_value(row):
    """Цена из строки экспорта позиций (разные варианты полей JSON/XML)."""
    if not isinstance(row, dict):
        return None
    for key in ("pricePromo", "price_promo", "promoPrice"):
        pv = _parse_b2b_price_string(row.get(key))
        if pv is not None and pv > 0:
            return float(pv)
    for key in ("price", "Price", "cost", "amount"):
        pv = _parse_b2b_price_string(row.get(key))
        if pv is not None and pv > 0:
            return float(pv)
    return None


def _market_stats_from_b2b_position_rows(rows):
    if not isinstance(rows, list):
        rows = []
    if not rows:
        return {
            "min": None, "avg": None, "max": None, "offers": 0,
            "min_competitors": 0, "avg_competitors": 0,
            "_error": True,
            "_error_reason": "b2b: нет позиций в ответе",
        }
    prices = []
    for row in rows:
        if not isinstance(row, dict):
            continue
        pv = _b2b_row_price_value(row)
        if pv is not None and pv > 0:
            prices.append(float(pv))
    if not prices:
        return {
            "min": None, "avg": None, "max": None, "offers": 0,
            "min_competitors": 0, "avg_competitors": 0,
            "_error": True,
            "_error_reason": "b2b: позиции есть, но не удалось прочитать цену (поля price / pricePromo)",
        }
    offers_count = len(prices)
    min_price = float(min(prices))
    max_price = float(max(prices))
    avg_price = round(float(sum(prices)) / len(prices), 2)
    min_competitors = sum(1 for p in prices if p <= min_price * 1.02)
    avg_competitors = sum(1 for p in prices if abs(p - avg_price) <= max(1.0, avg_price * 0.05))
    return {
        "min": round(min_price, 2),
        "avg": avg_price,
        "max": round(max_price, 2),
        "offers": offers_count,
        "min_competitors": int(min_competitors),
        "avg_competitors": int(avg_competitors),
        "_error": False,
        "_error_reason": "",
    }


def _fetch_onliner_market_stats_b2b(onliner_id, product_name="", category_name=""):
    oid = normalize_onliner_id(onliner_id)
    if not oid:
        return {
            "min": None, "avg": None, "max": None, "offers": 0,
            "min_competitors": 0, "avg_competitors": 0,
            "_error": True, "_error_reason": "пустой onliner id",
        }
    cfg = get_onliner_b2b_settings()
    if not cfg.get("enabled"):
        return {
            "min": None, "avg": None, "max": None, "offers": 0,
            "min_competitors": 0, "avg_competitors": 0,
            "_error": True, "_error_reason": "b2b выключен в настройках",
        }
    if not str(cfg.get("client_id", "") or "").strip() or not str(cfg.get("client_secret", "") or "").strip():
        return {
            "min": None, "avg": None, "max": None, "offers": 0,
            "min_competitors": 0, "avg_competitors": 0,
            "_error": True, "_error_reason": "b2b: не заданы client_id / client_secret",
        }
    try:
        sid, mid = onliner_b2b_resolve_catalog_path_for_product(
            oid, product_name=product_name, category_name=category_name,
        )
    except Exception as e:
        return {
            "min": None, "avg": None, "max": None, "offers": 0,
            "min_competitors": 0, "avg_competitors": 0,
            "_error": True,
            "_error_reason": f"b2b resolve: {str(e)[:120]}",
        }
    if not sid or not mid:
        return {
            "min": None, "avg": None, "max": None, "offers": 0,
            "min_competitors": 0, "avg_competitors": 0,
            "_error": True,
            "_error_reason": "b2b: не найден раздел/производитель для товара (нужно название в прайсе или в локальной базе)",
        }
    try:
        rows = onliner_b2b_fetch_product_positions_export(sid, mid, oid)
    except Exception as e:
        return {
            "min": None, "avg": None, "max": None, "offers": 0,
            "min_competitors": 0, "avg_competitors": 0,
            "_error": True,
            "_error_reason": f"b2b positions: {str(e)[:160]}",
        }
    return _market_stats_from_b2b_position_rows(rows)


def _b2b_section_tokens(category_name, product_name=""):
    tokens = []
    inferred = normalize_catalog_category_name(category_name or infer_category(product_name or ""))
    alias_map = {
        "Процессор": ["процессор", "cpu"],
        "Кулер": ["кулер", "cooler"],
        "Охлаждение": ["охлаждение", "водяное охлаждение"],
        "Материнская плата": ["материнская плата"],
        "Оперативная память": ["оперативная память", "память ddr", "dram"],
        "SSD": ["ssd"],
        "Жесткий диск": ["жесткий диск", "hdd"],
        "Видеокарта": ["видеокарта"],
        "Блок питания": ["блок питания", "psu"],
        "Корпус": ["корпус"],
        "Монитор": ["монитор"],
        "Ноутбук": ["ноутбук"],
        "Системный блок": ["системный блок", "компьютер"],
        "Клавиатура": ["клавиатура"],
        "Мышь": ["мышь"],
        "Наушники": ["наушники", "гарнитура"],
        "Акустика": ["акустика", "колонки"],
        "Сеть": ["роутер", "сеть", "wifi"],
        "Накопители USB": ["usb накопитель", "флешка"],
        "Кабели и переходники": ["кабель", "переходник", "адаптер"],
        "Аксессуары": ["аксессуары"],
    }
    if inferred:
        tokens.extend(alias_map.get(inferred, [inferred.lower()]))
    raw_cat = str(category_name or "").strip().lower()
    if raw_cat and raw_cat not in tokens:
        tokens.append(raw_cat)
    out = []
    seen = set()
    for token in tokens:
        key = str(token or "").strip().lower()
        if key and key not in seen:
            seen.add(key)
            out.append(key)
    return out


def onliner_b2b_search_candidates(local_name, category_name="", limit=30):
    cfg = get_onliner_b2b_settings()
    if not cfg.get("enabled"):
        return []
    name = str(local_name or "").strip()
    if not name:
        return []
    try:
        sections = onliner_b2b_get_sections()
    except Exception:
        return []

    section_tokens = _b2b_section_tokens(category_name, name)
    candidate_sections = []
    for section in sections:
        sec_id = str(section.get("id", "") or "").strip()
        sec_name = str(section.get("name", "") or "").strip()
        if not sec_id or not sec_name:
            continue
        low_name = sec_name.lower()
        score = 0
        for token in section_tokens:
            if token and token in low_name:
                score = max(score, len(token))
        if score > 0:
            candidate_sections.append((score, sec_id, sec_name))
    candidate_sections.sort(key=lambda x: x[0], reverse=True)
    candidate_sections = candidate_sections[:3] if candidate_sections else []
    if not candidate_sections and sections:
        candidate_sections = [(0, str(sections[0].get("id", "")).strip(), str(sections[0].get("name", "")).strip())]

    brand = _preferred_brand_token(name)
    queries = []
    seen_queries = set()

    def _add_query(value):
        q = str(value or "").strip()
        if not q:
            return
        key = q.lower()
        if key in seen_queries:
            return
        seen_queries.add(key)
        queries.append(q)

    article = str(extract_article(name) or "").strip()
    if article:
        _add_query(article)
    for q in _priority_model_queries(name)[:2]:
        _add_query(q)
    _add_query(" ".join(_name_tokens(name)[:6]))
    _add_query(name[:90])
    queries = queries[:3]

    local_articles = _article_like_tokens(name)
    candidates = {}

    for _, sec_id, _sec_name in candidate_sections:
        try:
            manufacturers = onliner_b2b_get_manufacturers(sec_id)
        except Exception:
            continue
        if brand:
            matching_manufacturers = [
                m for m in manufacturers
                if brand.lower() in str(m.get("name", "") or "").lower()
            ]
        else:
            matching_manufacturers = list(manufacturers[:4])
        if not matching_manufacturers:
            matching_manufacturers = list(manufacturers[:4])

        for manufacturer in matching_manufacturers[:4]:
            mid = str(manufacturer.get("id", "") or "").strip()
            if not mid:
                continue
            for query in queries:
                try:
                    products = onliner_b2b_get_products(sec_id, mid, title=query)
                except Exception:
                    continue
                for product in products[:20]:
                    pid = normalize_onliner_id(product.get("id", ""))
                    pname = str(product.get("name", "") or "").strip()
                    if not pid or not pname:
                        continue
                    if pid in candidates:
                        continue
                    allowed, _reason = _strict_candidate_allowed(name, pname)
                    if not allowed:
                        continue
                    cmp = calc_name_match(name, pname)
                    score = float(cmp.get("score", 0.0) or 0.0)
                    if cmp.get("match"):
                        score = max(score, 0.76)
                    purl = ""
                    cached_product = db_get_product_by_id(pid)
                    if isinstance(cached_product, dict):
                        purl = str(cached_product.get("url", "") or "").strip()
                    article_hits = []
                    if local_articles and score >= 0.45:
                        try:
                            article_hits = onliner_b2b_get_articles(sec_id, mid, pid)
                        except Exception:
                            article_hits = []
                    remote_articles = {_normalize_compact_name(x) for x in article_hits if str(x or "").strip()}
                    if local_articles and remote_articles:
                        if local_articles.intersection(remote_articles):
                            score = max(score, 0.98)
                        else:
                            score *= 0.55
                    if score < 0.34:
                        continue
                    candidates[pid] = {
                        "id": pid,
                        "name": pname,
                        "url": purl,
                        "score": round(float(score), 3),
                        "source": "b2b",
                        "reason": str(cmp.get("reason", "") or ""),
                    }
                    db_upsert_product(pid, pname, purl, source="b2b")
                if len(candidates) >= limit:
                    break
            if len(candidates) >= limit:
                break
        if len(candidates) >= limit:
            break

    items = list(candidates.values())
    items.sort(key=lambda x: float(x.get("score", 0.0) or 0.0), reverse=True)
    return items[:limit]


def load_supplier_snapshots():
    if not SUPPLIER_SNAPSHOTS_FILE.exists():
        return {"suppliers": {}}
    try:
        with open(SUPPLIER_SNAPSHOTS_FILE, "r", encoding="utf-8") as f:
            data = json.load(f)
    except Exception:
        return {"suppliers": {}}
    suppliers = data.get("suppliers") if isinstance(data, dict) else {}
    return {"suppliers": suppliers if isinstance(suppliers, dict) else {}}


def save_supplier_snapshots(data):
    payload = data if isinstance(data, dict) else {"suppliers": {}}
    if "suppliers" not in payload or not isinstance(payload.get("suppliers"), dict):
        payload = {"suppliers": {}}
    with open(SUPPLIER_SNAPSHOTS_FILE, "w", encoding="utf-8") as f:
        json.dump(payload, f, ensure_ascii=False, indent=2)


def load_api_fetch_history():
    if not API_FETCH_HISTORY_FILE.exists():
        return []
    try:
        with open(API_FETCH_HISTORY_FILE, "r", encoding="utf-8") as f:
            data = json.load(f)
        return data if isinstance(data, list) else []
    except Exception:
        return []


def save_api_fetch_history(rows):
    payload = rows if isinstance(rows, list) else []
    with open(API_FETCH_HISTORY_FILE, "w", encoding="utf-8") as f:
        json.dump(payload[-300:], f, ensure_ascii=False, indent=2)


def append_api_fetch_history(entry):
    if not isinstance(entry, dict):
        return
    rows = load_api_fetch_history()
    rows.append(entry)
    rows.sort(key=lambda x: int((x or {}).get("finished_at", 0) or (x or {}).get("started_at", 0) or 0), reverse=True)
    save_api_fetch_history(rows)


def get_api_fetch_history(limit=20):
    rows = load_api_fetch_history()
    rows.sort(key=lambda x: int((x or {}).get("finished_at", 0) or (x or {}).get("started_at", 0) or 0), reverse=True)
    return rows[:max(1, int(limit or 20))]


def _snapshot_price(value):
    num = pd.to_numeric(value, errors="coerce")
    if pd.isna(num):
        return None
    return round(float(num), 2)


def _snapshot_item_key(row):
    oid = normalize_onliner_id(row.get("OnlinerID", ""))
    if oid:
        return f"oid:{oid}"
    name_key = _normalize_name_key(row.get("Название", ""))
    if name_key:
        return f"name:{name_key}"
    return ""


def build_supplier_snapshot(df, supplier_name):
    items = {}
    for _, row in df.iterrows():
        supplier = str(row.get("Поставщик", "")).strip()
        if supplier_name and supplier and supplier != supplier_name:
            continue
        key = _snapshot_item_key(row)
        if not key:
            continue
        items[key] = {
            "name": str(row.get("Название", "")).strip(),
            "price": _snapshot_price(row.get("Цена", "")),
            "onliner_id": normalize_onliner_id(row.get("OnlinerID", "")),
            "category": str(row_category(row)).strip(),
            "has_id": bool(normalize_onliner_id(row.get("OnlinerID", ""))),
        }
    return {
        "updated_at": int(time.time()),
        "items": items,
    }


def compare_supplier_snapshot(previous_snapshot, current_snapshot):
    prev_items = previous_snapshot.get("items") if isinstance(previous_snapshot, dict) else {}
    curr_items = current_snapshot.get("items") if isinstance(current_snapshot, dict) else {}
    if not isinstance(prev_items, dict):
        prev_items = {}
    if not isinstance(curr_items, dict):
        curr_items = {}

    def _coerce_snapshot_item(item):
        if isinstance(item, dict):
            return item
        if isinstance(item, str):
            text = item.strip()
            return {
                "name": text,
                "price": None,
                "onliner_id": "",
                "category": "",
            }
        return {
            "name": "",
            "price": None,
            "onliner_id": "",
            "category": "",
        }

    prev_keys = set(prev_items.keys())
    curr_keys = set(curr_items.keys())
    new_keys = sorted(curr_keys - prev_keys)
    removed_keys = sorted(prev_keys - curr_keys)
    shared_keys = sorted(curr_keys & prev_keys)

    price_changed = []
    for key in shared_keys:
        old_item = _coerce_snapshot_item(prev_items.get(key))
        new_item = _coerce_snapshot_item(curr_items.get(key))
        old_price = old_item.get("price")
        new_price = new_item.get("price")
        if old_price is None or new_price is None:
            continue
        try:
            if abs(float(new_price) - float(old_price)) >= 0.01:
                price_changed.append({
                    "name": str(new_item.get("name") or old_item.get("name") or "").strip(),
                    "old_price": float(old_price),
                    "new_price": float(new_price),
                    "onliner_id": str(new_item.get("onliner_id") or old_item.get("onliner_id") or "").strip(),
                })
        except Exception:
            continue

    new_items = []
    new_without_id = []
    for key in new_keys:
        item = _coerce_snapshot_item(curr_items.get(key))
        payload = {
            "name": str(item.get("name") or "").strip(),
            "price": item.get("price"),
            "onliner_id": str(item.get("onliner_id") or "").strip(),
            "category": str(item.get("category") or "").strip(),
        }
        new_items.append(payload)
        if not payload["onliner_id"]:
            new_without_id.append(payload)

    removed_items = []
    for key in removed_keys:
        item = _coerce_snapshot_item(prev_items.get(key))
        removed_items.append({
            "name": str(item.get("name") or "").strip(),
            "price": item.get("price"),
            "onliner_id": str(item.get("onliner_id") or "").strip(),
            "category": str(item.get("category") or "").strip(),
        })

    return {
        "available": True,
        "previous_updated_at": int(previous_snapshot.get("updated_at", 0) or 0) if isinstance(previous_snapshot, dict) else 0,
        "current_updated_at": int(current_snapshot.get("updated_at", 0) or 0) if isinstance(current_snapshot, dict) else 0,
        "new_count": len(new_items),
        "removed_count": len(removed_items),
        "price_changed_count": len(price_changed),
        "new_without_id_count": len(new_without_id),
        "samples": {
            "new_items": new_items[:10],
            "removed_items": removed_items[:10],
            "price_changed": price_changed[:10],
            "new_without_id": new_without_id[:10],
        },
        "filters": {
            "new_names": [str(item.get("name") or "").strip() for item in new_items if str(item.get("name") or "").strip()],
            "new_without_id_names": [str(item.get("name") or "").strip() for item in new_without_id if str(item.get("name") or "").strip()],
        },
    }


def save_session_supplier_diff(session_dir, diff_data):
    if not session_dir:
        return
    try:
        with open(Path(session_dir) / "supplier_diff.json", "w", encoding="utf-8") as f:
            json.dump(diff_data or {}, f, ensure_ascii=False, indent=2)
    except Exception as e:
        print(f"Не удалось сохранить supplier diff: {e}")


def load_session_supplier_diff(session_dir):
    if not session_dir:
        return {}
    path = Path(session_dir) / "supplier_diff.json"
    if not path.exists():
        return {}
    try:
        with open(path, "r", encoding="utf-8") as f:
            data = json.load(f)
        return data if isinstance(data, dict) else {}
    except Exception:
        return {}


def _get_api_source_status_key():
    key = str(session.get("api_fetch_key", "") or "").strip()
    if not key:
        key = str(uuid.uuid4())[:12]
        session["api_fetch_key"] = key
    return key


def _get_source_runtime(source_key, client_key=None):
    client_key = str(client_key or "").strip() or _get_api_source_status_key()
    with SOURCE_FETCH_LOCK:
        client_state = source_fetch_statuses.setdefault(client_key, {})
        return client_state.setdefault(source_key, {
            "running": False,
            "progress": 0,
            "downloaded": 0,
            "total_bytes": 0,
            "status": "idle",
            "message": "",
            "ready": False,
            "source_key": source_key,
            "label": source_key.upper(),
            "supplier": "",
            "file_path": "",
            "file_name": "",
            "started_at": 0,
            "finished_at": 0,
        })


def _update_source_runtime(source_key, client_key=None, **kwargs):
    with SOURCE_FETCH_LOCK:
        state = _get_source_runtime(source_key, client_key=client_key)
        state.update(kwargs)
        return dict(state)


def _serialize_source_runtime(state):
    data = dict(state or {})
    for key in ("started_at", "finished_at", "progress", "downloaded", "total_bytes"):
        if key in data:
            try:
                data[key] = int(data[key])
            except Exception:
                pass
    return data


def _iter_api_sources_for_ui(settings=None):
    settings = settings or load_app_settings()
    sources = ((settings or {}).get("api_sources") or {})
    items = []
    for key, cfg in sources.items():
        if not isinstance(cfg, dict):
            continue
        item = {
            "key": key,
            "label": str(cfg.get("label", key.upper()) or key.upper()),
            "supplier": str(cfg.get("supplier", key.upper()) or key.upper()),
            "enabled": bool(cfg.get("enabled")),
            "configured": False,
            "mode": str(cfg.get("mode", "direct_file") or "direct_file"),
        }
        if item["mode"] == "ntech_json":
            item["configured"] = bool(str(cfg.get("auth_url", "")).strip() and str(cfg.get("price_url", "")).strip() and str(cfg.get("username", "")).strip() and str(cfg.get("password", "")))
        else:
            item["configured"] = bool(str(cfg.get("file_url", "")).strip())
        items.append(item)
    return items


def _source_temp_dir(client_key=None):
    client_key = str(client_key or "").strip() or _get_api_source_status_key()
    path = UPLOAD_DIR / f"_api_fetch_{client_key}"
    path.mkdir(exist_ok=True)
    return path


def _default_source_headers(source_key):
    headers = {
        "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/122.0.0.0 Safari/537.36",
        "Accept": "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet,application/octet-stream,*/*",
    }
    return headers


def _resolve_curl_cmd():
    """Return available curl executable for current OS."""
    cmd = shutil.which("curl") or shutil.which("curl.exe")
    if cmd:
        return cmd
    raise RuntimeError("Системный curl не найден. Установите curl и перезапустите приложение.")


def _head_content_length_via_curl(url, verify_ssl, headers=None):
    headers = headers or {}
    cmd = [_resolve_curl_cmd(), "-I", "-L", "--silent", "--show-error"]
    if not verify_ssl:
        cmd.append("-k")
    for key, value in headers.items():
        cmd.extend(["-H", f"{key}: {value}"])
    cmd.append(url)
    try:
        result = subprocess.run(cmd, capture_output=True, text=True, timeout=60, check=False)
        if result.returncode != 0:
            return 0
        match = re.search(r"(?im)^Content-Length:\s*(\d+)\s*$", result.stdout or "")
        return int(match.group(1)) if match else 0
    except Exception:
        return 0


def _curl_download_to_path(url, target_path, verify_ssl, source_key, client_key=None, headers=None):
    headers = headers or {}
    is_iven = str(source_key).strip().lower() == "iven"
    total = _head_content_length_via_curl(url, verify_ssl, headers=headers)
    download_message = "IVEN отвечает медленно, это нормально. Идет скачивание через системный клиент." if is_iven else "Скачиваю прайс через системный клиент..."
    _update_source_runtime(source_key, client_key=client_key, total_bytes=total, status="downloading", message=download_message, progress=5)
    cmd = [_resolve_curl_cmd(), "-L", "--silent", "--show-error", "--output", str(target_path)]
    if not verify_ssl:
        cmd.append("-k")
    if is_iven:
        cmd.append("--http1.1")
        if total > 0:
            cmd.extend(["-r", f"0-{total - 1}"])
    for key, value in headers.items():
        cmd.extend(["-H", f"{key}: {value}"])
    cmd.append(url)
    proc = subprocess.Popen(cmd, stdout=subprocess.PIPE, stderr=subprocess.PIPE)
    completed_early = False
    last_size = -1
    stable_count = 0
    try:
        while proc.poll() is None:
            size = target_path.stat().st_size if target_path.exists() else 0
            if total > 0:
                progress = min(95, int((size / total) * 100))
            else:
                progress = min(95, max(5, int(size / 65536))) if size else 5
            _update_source_runtime(source_key, client_key=client_key, downloaded=size, progress=progress)
            if is_iven:
                if size == last_size and size > 0:
                    stable_count += 1
                else:
                    stable_count = 0
                last_size = size
                if stable_count >= 6:
                    try:
                        if zipfile.is_zipfile(target_path):
                            completed_early = True
                            proc.terminate()
                            break
                    except Exception:
                        pass
            time.sleep(0.35)
        stdout, stderr = proc.communicate(timeout=10)
    except Exception:
        proc.kill()
        raise
    if proc.returncode != 0 and not completed_early:
        err = (stderr or b"").decode("utf-8", errors="ignore").strip()
        raise RuntimeError(err or f"curl exited with code {proc.returncode}")
    size = target_path.stat().st_size if target_path.exists() else 0
    _update_source_runtime(source_key, client_key=client_key, downloaded=size, total_bytes=total or size, progress=100)


def _stream_download_to_path(url, target_path, verify_ssl, source_key, client_key=None, headers=None):
    headers = headers or {}
    downloaded = 0
    with requests.get(url, stream=True, verify=verify_ssl, timeout=120, headers=headers) as resp:
        resp.raise_for_status()
        total = 0
        try:
            total = int(resp.headers.get("Content-Length", "0") or 0)
        except Exception:
            total = 0
        _update_source_runtime(source_key, client_key=client_key, total_bytes=total, status="downloading", message="Скачиваю прайс...")
        with open(target_path, "wb") as f:
            for chunk in resp.iter_content(chunk_size=262144):
                if not chunk:
                    continue
                f.write(chunk)
                downloaded += len(chunk)
                progress = int((downloaded / total) * 100) if total > 0 else min(95, max(5, int(downloaded / 65536)))
                _update_source_runtime(source_key, client_key=client_key, downloaded=downloaded, progress=progress)
    _update_source_runtime(source_key, client_key=client_key, downloaded=downloaded, total_bytes=downloaded or _get_source_runtime(source_key, client_key=client_key).get("total_bytes", 0))


def _ntech_reserve_url_from_price_url(price_url):
    raw = str(price_url or "").strip()
    if not raw:
        return ""
    if raw.endswith("/price"):
        return raw[:-len("/price")] + "/list"
    return raw.rstrip("/") + "/list"


def _ntech_api_json(method, url, token, verify_ssl=False, json_body=None, timeout=120):
    headers = {"Authorization": f"Bearer {token}"}
    method = str(method or "get").strip().lower() or "get"
    if method == "post":
        resp = requests.post(url, headers=headers, json=json_body, verify=verify_ssl, timeout=timeout)
    else:
        resp = requests.get(url, headers=headers, verify=verify_ssl, timeout=timeout)
    if resp.status_code == 401:
        raise PermissionError("unauthorized")
    resp.raise_for_status()
    data = resp.json()
    return data if isinstance(data, dict) else {}


def _load_ntech_orders(token, reserve_url, verify_ssl=False):
    if not str(reserve_url or "").strip():
        return {"orders": [], "warning": None}
    try:
        payload = _ntech_api_json("get", reserve_url, token, verify_ssl=verify_ssl, timeout=120)
    except requests.HTTPError as e:
        response = getattr(e, "response", None)
        if response is not None and response.status_code == 404:
            return {"orders": [], "warning": "API резервов N-Tech временно недоступно (HTTP 404). Продолжаю без резервов."}
        raise
    orders = payload.get("orders") or []
    return {"orders": orders if isinstance(orders, list) else [], "warning": None}


def _build_ntech_dataframe(payload, reserve_orders=None):
    products = []
    if isinstance(payload, dict):
        products = payload.get("products") or []
    rows = []
    last_category = ""
    for item in products:
        if not isinstance(item, dict):
            continue
        category = str(item.get("category") or item.get("group") or "").strip()
        if category and category != last_category:
            rows.append({
                "код": "",
                "": category,
                "Наименование": "",
                "Гарантия": "",
                "Рекомендуемая\n розничная\n цена": "",
                "Цена без НДС": "",
                "Цена с НДС": "",
            })
            last_category = category
        rows.append({
            "код": item.get("id", ""),
            "": "",
            "Наименование": item.get("name", ""),
            "Гарантия": item.get("warranty", ""),
            "Рекомендуемая\n розничная\n цена": item.get("recommended_price") or item.get("retail_price") or "",
            "Цена без НДС": item.get("price", ""),
            "Цена с НДС": item.get("price_with_vat", ""),
        })
    return pd.DataFrame(rows)


def _fetch_api_source_worker(source_key, client_key):
    settings = load_app_settings()
    cfg = (((settings or {}).get("api_sources") or {}).get(source_key) or {})
    label = str(cfg.get("label", source_key.upper()) or source_key.upper())
    supplier = str(cfg.get("supplier", label) or label)
    mode = str(cfg.get("mode", "direct_file") or "direct_file")
    verify_ssl = bool(cfg.get("verify_ssl"))
    temp_dir = _source_temp_dir(client_key=client_key)
    safe_name = re.sub(r"[^A-Za-z0-9._-]+", "_", source_key).strip("_") or source_key
    target_path = temp_dir / f"{safe_name}.xlsx"
    _update_source_runtime(
        source_key,
        client_key=client_key,
        running=True,
        progress=0,
        downloaded=0,
        total_bytes=0,
        ready=False,
        status="starting",
        message="IVEN отвечает медленно, это нормально. Подготовка может занять до 2 минут." if str(source_key).strip().lower() == "iven" else "Подготавливаю выгрузку...",
        label=label,
        supplier=supplier,
        file_path="",
        file_name=target_path.name,
        started_at=time.time(),
        finished_at=0,
    )
    started_at = int(time.time())
    try:
        if mode == "ntech_json":
            auth_url = str(cfg.get("auth_url", "")).strip()
            price_url = str(cfg.get("price_url", "")).strip()
            username = str(cfg.get("username", "")).strip()
            password = str(cfg.get("password", ""))
            if not (auth_url and price_url and username and password):
                raise ValueError("Источник не настроен")
            _update_source_runtime(source_key, client_key=client_key, progress=10, status="auth", message="Авторизация...")
            auth_resp = requests.post(auth_url, json={"username": username, "password": password}, verify=verify_ssl, timeout=60)
            auth_resp.raise_for_status()
            token = (auth_resp.json() or {}).get("token")
            if not token:
                raise ValueError("Токен не получен")
            _update_source_runtime(source_key, client_key=client_key, progress=45, status="fetching", message="Получаю прайс N-Tech...")
            price_resp = None
            last_err = None
            # 1) POST — legacy API JSON (/orders/v1/price)
            # 2) GET — compatible with cabinet-like direct file endpoints (xlsx/csv/attachment)
            for method in ("post", "get"):
                try:
                    req_fn = requests.post if method == "post" else requests.get
                    resp = req_fn(
                        price_url,
                        headers={"Authorization": f"Bearer {token}"},
                        verify=verify_ssl,
                        timeout=120,
                    )
                    if resp.status_code == 401:
                        _update_source_runtime(source_key, client_key=client_key, progress=20, status="auth", message="Обновляю токен...")
                        auth_resp = requests.post(auth_url, json={"username": username, "password": password}, verify=verify_ssl, timeout=60)
                        auth_resp.raise_for_status()
                        token = (auth_resp.json() or {}).get("token")
                        if not token:
                            raise ValueError("Токен не получен")
                        _update_source_runtime(source_key, client_key=client_key, progress=45, status="fetching", message="Повторно получаю прайс...")
                        resp = req_fn(
                            price_url,
                            headers={"Authorization": f"Bearer {token}"},
                            verify=verify_ssl,
                            timeout=120,
                        )
                    resp.raise_for_status()
                    price_resp = resp
                    break
                except Exception as e:
                    last_err = e
            if price_resp is None:
                raise last_err or RuntimeError("Не удалось получить прайс N-Tech")

            content_type = str(price_resp.headers.get("Content-Type", "") or "").lower()
            content_disp = str(price_resp.headers.get("Content-Disposition", "") or "").lower()
            body = price_resp.content or b""
            looks_like_file = (
                "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet" in content_type
                or "application/vnd.ms-excel" in content_type
                or "text/csv" in content_type
                or "application/octet-stream" in content_type
                or "attachment" in content_disp
                or body.startswith(b"PK\x03\x04")
            )
            if looks_like_file:
                _update_source_runtime(source_key, client_key=client_key, progress=75, status="building", message="Сохраняю файл прайса N-Tech...")
                with open(target_path, "wb") as f:
                    f.write(body)
            else:
                payload = price_resp.json() or {}
                _update_source_runtime(source_key, client_key=client_key, progress=75, status="building", message="Собираю Excel...")
                df = _build_ntech_dataframe(payload)
                df.to_excel(target_path, index=False, sheet_name="Прайс N-Tech")
        else:
            file_url = str(cfg.get("file_url", "")).strip()
            if not file_url:
                raise ValueError("Источник не настроен")
            headers = _default_source_headers(source_key)
            if str(source_key).strip().lower() == "iven":
                _curl_download_to_path(file_url, target_path, verify_ssl, source_key, client_key=client_key, headers=headers)
            else:
                try:
                    _stream_download_to_path(file_url, target_path, verify_ssl, source_key, client_key=client_key, headers=headers)
                except requests.exceptions.RequestException:
                    _curl_download_to_path(file_url, target_path, verify_ssl, source_key, client_key=client_key, headers=headers)
        _update_source_runtime(
            source_key,
            client_key=client_key,
            running=False,
            progress=100,
            status="ready",
            message="Прайс получен. Можно обрабатывать.",
            ready=True,
            file_path=str(target_path),
            file_name=target_path.name,
            finished_at=time.time(),
        )
        final_state = _get_source_runtime(source_key, client_key=client_key)
        file_size = target_path.stat().st_size if target_path.exists() else int(final_state.get("downloaded", 0) or 0)
        finished_at = int(final_state.get("finished_at", time.time()) or time.time())
        append_api_fetch_history({
            "source_key": source_key,
            "label": label,
            "supplier": supplier,
            "event_type": "fetch",
            "status": "ok",
            "message": "Прайс получен",
            "file_name": target_path.name,
            "file_size": int(file_size or 0),
            "started_at": started_at,
            "finished_at": finished_at,
            "duration_sec": max(0, finished_at - started_at),
        })
    except Exception as e:
        error_message = str(e)[:240] or "Ошибка выгрузки"
        _update_source_runtime(
            source_key,
            client_key=client_key,
            running=False,
            ready=False,
            status="error",
            message=error_message,
            finished_at=time.time(),
        )
        finished_at = int(time.time())
        file_size = target_path.stat().st_size if target_path.exists() else 0
        append_api_fetch_history({
            "source_key": source_key,
            "label": label,
            "supplier": supplier,
            "event_type": "fetch",
            "status": "error",
            "message": error_message,
            "file_name": target_path.name,
            "file_size": int(file_size or 0),
            "started_at": started_at,
            "finished_at": finished_at,
            "duration_sec": max(0, finished_at - started_at),
        })


def get_no_id_category_rules(settings=None):
    settings = settings or load_app_settings()
    raw = str((((settings or {}).get("no_id_search") or {}).get("category_rules_text", "")) or "").strip()
    if not raw:
        return {}
    try:
        parsed = json.loads(raw)
        return parsed if isinstance(parsed, dict) else {}
    except Exception:
        return {}


def load_onliner_market_cache():
    if not ONLINER_MARKET_CACHE_FILE.exists():
        return {}
    try:
        with open(ONLINER_MARKET_CACHE_FILE, "r", encoding="utf-8") as f:
            data = json.load(f)
        if isinstance(data, dict):
            return data
    except Exception:
        pass
    return {}


def save_onliner_market_cache(cache):
    with open(ONLINER_MARKET_CACHE_FILE, "w", encoding="utf-8") as f:
        json.dump(cache, f, ensure_ascii=False, indent=2)


def load_onliner_product_cache():
    if not ONLINER_PRODUCT_CACHE_FILE.exists():
        return {}
    try:
        with open(ONLINER_PRODUCT_CACHE_FILE, "r", encoding="utf-8") as f:
            data = json.load(f)
        if isinstance(data, dict):
            return data
    except Exception:
        pass
    return {}


def save_onliner_product_cache(cache):
    with open(ONLINER_PRODUCT_CACHE_FILE, "w", encoding="utf-8") as f:
        json.dump(cache, f, ensure_ascii=False, indent=2)


# ============================================================
# ONLINER PRODUCTS DATABASE  (SQLite — единый источник истины)
# Файл: onliner_products.db (рядом с app.py)
#
# onliner_catalog  → onliner_id, name, url, source, updated_at
# name_index       → name_key (нормализованный), onliner_id, raw_name
#
# Пополняется автоматически при каждой загрузке прайса.
# Используется для автоподбора ID без Onliner API.
# ============================================================

def _db_connect():
    """Открывает соединение с SQLite БД с WAL-режимом для безопасного многопоточного доступа."""
    conn = sqlite3.connect(str(ONLINER_DB_FILE), check_same_thread=False, timeout=15)
    conn.execute("PRAGMA journal_mode=WAL")
    conn.execute("PRAGMA synchronous=NORMAL")
    conn.row_factory = sqlite3.Row
    return conn


@contextmanager
def _db_connection():
    """sqlite3 Connection as context manager does not close the handle, so close explicitly."""
    conn = _db_connect()
    try:
        yield conn
        conn.commit()
    except Exception:
        conn.rollback()
        raise
    finally:
        conn.close()


def init_onliner_db():
    """Создаёт таблицы БД если они ещё не существуют. Вызывается один раз при старте."""
    try:
        with _db_connection() as conn:
            conn.executescript("""
                CREATE TABLE IF NOT EXISTS onliner_catalog (
                    onliner_id  TEXT PRIMARY KEY,
                    name        TEXT NOT NULL,
                    url         TEXT DEFAULT '',
                    source      TEXT DEFAULT '',
                    updated_at  INTEGER DEFAULT 0
                );
                CREATE TABLE IF NOT EXISTS name_index (
                    name_key    TEXT PRIMARY KEY,
                    onliner_id  TEXT NOT NULL,
                    raw_name    TEXT NOT NULL,
                    source      TEXT DEFAULT '',
                    updated_at  INTEGER DEFAULT 0
                );
                CREATE INDEX IF NOT EXISTS idx_ni_oid
                    ON name_index(onliner_id);
                CREATE INDEX IF NOT EXISTS idx_ni_rawname
                    ON name_index(raw_name);
            """)
        print(f"[db] SQLite БД инициализирована: {ONLINER_DB_FILE}", flush=True)
    except Exception as e:
        print(f"[db] Ошибка инициализации БД: {e}", flush=True)


def db_populate_from_df(df, source_label, skip_suppliers=None):
    """Пополняет БД из DataFrame (колонки: Название, OnlinerID, Ссылка, Поставщик).
    Вызывать после каждой загрузки прайса IVEN / BN / Tradex.
    skip_suppliers — список поставщиков-целей, которые не используются как источник (N-Tech, TGPC).
    Возвращает (добавлено_продуктов, добавлено_имён).
    """
    if df is None or df.empty:
        return 0, 0
    skip_upper = {str(s).upper() for s in (skip_suppliers or [])}
    supplier_col = "Поставщик" if "Поставщик" in df.columns else None
    now = int(time.time())

    products = {}   # onliner_id → (name, url)
    names    = {}   # name_key  → (onliner_id, raw_name)

    for _, row in df.iterrows():
        oid = normalize_onliner_id(row.get("OnlinerID", ""))
        if not oid:
            continue
        name = str(row.get("Название", "")).strip()
        if not name:
            continue
        if supplier_col:
            sup = str(row.get(supplier_col, "")).strip().upper()
            if sup in skip_upper:
                continue
        url = str(row.get("Ссылка", "")).strip()
        name_key = _normalize_name_key(name)
        if oid not in products:
            products[oid] = (name, url)
        if name_key and name_key not in names:
            names[name_key] = (oid, name)

    if not products:
        return 0, 0

    try:
        with _DB_WRITE_LOCK:
            with _db_connection() as conn:
                conn.executemany(
                    "INSERT OR REPLACE INTO onliner_catalog"
                    "(onliner_id, name, url, source, updated_at) VALUES(?,?,?,?,?)",
                    [(oid, nm, url, source_label, now) for oid, (nm, url) in products.items()],
                )
                conn.executemany(
                    "INSERT OR REPLACE INTO name_index"
                    "(name_key, onliner_id, raw_name, source, updated_at) VALUES(?,?,?,?,?)",
                    [(k, oid, raw, source_label, now) for k, (oid, raw) in names.items()],
                )
        print(f"[db] Пополнено из «{source_label}»: {len(products)} товаров, {len(names)} имён", flush=True)
        return len(products), len(names)
    except Exception as e:
        print(f"[db] Ошибка записи в БД: {e}", flush=True)
        return 0, 0


def db_upsert_product(onliner_id, name, url, source="manual"):
    """Добавляет/обновляет один товар в БД (например, после подтверждения вручную)."""
    oid = normalize_onliner_id(onliner_id)
    name = str(name or "").strip()
    if not oid or not name:
        return
    name_key = _normalize_name_key(name)
    now = int(time.time())
    try:
        with _DB_WRITE_LOCK:
            with _db_connection() as conn:
                conn.execute(
                    "INSERT OR REPLACE INTO onliner_catalog"
                    "(onliner_id, name, url, source, updated_at) VALUES(?,?,?,?,?)",
                    (oid, name, url or "", source, now),
                )
                if name_key:
                    conn.execute(
                        "INSERT OR REPLACE INTO name_index"
                        "(name_key, onliner_id, raw_name, source, updated_at) VALUES(?,?,?,?,?)",
                        (name_key, oid, name, source, now),
                    )
    except Exception as e:
        print(f"[db] db_upsert_product error: {e}", flush=True)


def db_get_product_by_id(onliner_id):
    oid = normalize_onliner_id(onliner_id)
    if not oid:
        return None
    try:
        with _db_connection() as conn:
            row = conn.execute(
                "SELECT onliner_id, name, url, source, updated_at "
                "FROM onliner_catalog WHERE onliner_id = ? LIMIT 1",
                (oid,),
            ).fetchone()
        if not row:
            return None
        return {
            "id": normalize_onliner_id(row["onliner_id"]),
            "name": str(row["name"] or "").strip(),
            "url": str(row["url"] or "").strip(),
            "source": str(row["source"] or "").strip(),
            "updated_at": int(row["updated_at"] or 0),
        }
    except Exception as e:
        print(f"[db] db_get_product_by_id error: {e}", flush=True)
        return None


def db_find_exact_id_for_name(product_name):
    name = str(product_name or "").strip()
    if not name:
        return None
    name_key = _normalize_name_key(name)
    if not name_key:
        return None
    try:
        with _db_connection() as conn:
            row = conn.execute(
                "SELECT ni.onliner_id, ni.raw_name, oc.url "
                "FROM name_index ni "
                "LEFT JOIN onliner_catalog oc ON oc.onliner_id = ni.onliner_id "
                "WHERE ni.name_key = ? LIMIT 1",
                (name_key,),
            ).fetchone()
        if not row:
            return None
        return {
            "id": normalize_onliner_id(row["onliner_id"]),
            "name": str(row["raw_name"] or "").strip(),
            "url": str(row["url"] or "").strip(),
            "score": 1.0,
            "source": "db_exact",
        }
    except Exception as e:
        print(f"[db] db_find_exact_id_for_name error: {e}", flush=True)
        return None


def db_find_id_for_name(product_name, threshold=0.75, allow_b2b=True):
    """Ищет лучший OnlinerID для товара по локальной БД.
    1. Точное совпадение по нормализованному ключу.
    2. Фильтрация по article-токенам (SQL LIKE) → calc_name_match по кандидатам.
    Возвращает dict {id, name, url, score, source} или None.
    """
    name = str(product_name or "").strip()
    if not name:
        return None
    name_key = _normalize_name_key(name)

    try:
        with _db_connection() as conn:
            # 1. Точное совпадение по нормализованному ключу
            if name_key:
                row = conn.execute(
                    "SELECT ni.onliner_id, ni.raw_name, oc.url "
                    "FROM name_index ni "
                    "JOIN onliner_catalog oc ON oc.onliner_id = ni.onliner_id "
                    "WHERE ni.name_key = ? LIMIT 1",
                    (name_key,),
                ).fetchone()
                if row:
                    return {"id": row[0], "name": row[1], "url": row[2],
                            "score": 1.0, "source": "db_exact"}

            # 2. Build search candidates using RAW tokens (preserving hyphens for LIKE)
            raw_tokens = _raw_search_tokens(name)          # e.g. ["GK-240L", "45240"]
            model_tokens = list(_model_hint_tokens(name))  # normalized, as fallback

            candidates = {}  # onliner_id → (raw_name, url)

            def _add_candidates(rows):
                for r in rows:
                    if r[0] not in candidates:
                        candidates[r[0]] = (r[1], r[2])

            for tok in raw_tokens:
                if len(tok) < 4:
                    continue
                _add_candidates(conn.execute(
                    "SELECT ni.onliner_id, ni.raw_name, oc.url "
                    "FROM name_index ni "
                    "JOIN onliner_catalog oc ON oc.onliner_id = ni.onliner_id "
                    "WHERE ni.raw_name LIKE ? LIMIT 80",
                    (f"%{tok}%",),
                ).fetchall())
                # Also try without trailing alpha suffix for broader match (GK-240L -> GK-240)
                m_num = re.match(r'^([A-Za-z]{1,4}[-]?\d{3,5})', tok)
                if m_num and m_num.group(1) != tok:
                    _add_candidates(conn.execute(
                        "SELECT ni.onliner_id, ni.raw_name, oc.url "
                        "FROM name_index ni "
                        "JOIN onliner_catalog oc ON oc.onliner_id = ni.onliner_id "
                        "WHERE ni.raw_name LIKE ? LIMIT 40",
                        (f"%{m_num.group(1)}%",),
                    ).fetchall())

            # Fallback: model hint tokens if raw_tokens found nothing
            if len(candidates) < 3:
                for tok in model_tokens[:3]:
                    if len(tok) < 4:
                        continue
                    _add_candidates(conn.execute(
                        "SELECT ni.onliner_id, ni.raw_name, oc.url "
                        "FROM name_index ni "
                        "JOIN onliner_catalog oc ON oc.onliner_id = ni.onliner_id "
                        "WHERE ni.raw_name LIKE ? LIMIT 60",
                        (f"%{tok}%",),
                    ).fetchall())

            # 3. Фолбэк: поиск по бренду + опционально по нескольким числовым токенам
            if len(candidates) < 5:
                brand = _preferred_brand_token(name)
                if brand and len(brand) >= 3:
                    _add_candidates(conn.execute(
                        "SELECT ni.onliner_id, ni.raw_name, oc.url "
                        "FROM name_index ni "
                        "JOIN onliner_catalog oc ON oc.onliner_id = ni.onliner_id "
                        "WHERE ni.raw_name LIKE ? LIMIT 150",
                        (f"%{brand}%",),
                    ).fetchall())

            # 4. Фолбэк по числовому токену (например, 5700 из "RX 5700 XT") + бренд
            if len(candidates) < 5:
                nums = re.findall(r"\b(\d{3,5})\b", name)
                brand = _preferred_brand_token(name)
                for num in nums[:2]:
                    if brand:
                        rows = conn.execute(
                            "SELECT ni.onliner_id, ni.raw_name, oc.url "
                            "FROM name_index ni "
                            "JOIN onliner_catalog oc ON oc.onliner_id = ni.onliner_id "
                            "WHERE ni.raw_name LIKE ? AND ni.raw_name LIKE ? LIMIT 80",
                            (f"%{brand}%", f"%{num}%"),
                        ).fetchall()
                        _add_candidates(rows)

        if not candidates:
            return None

        best_score = 0.0
        best = None
        for oid, (cand_name, cand_url) in candidates.items():
            allowed, _reason = _strict_candidate_allowed(name, cand_name)
            if not allowed:
                continue
            cmp = calc_name_match(name, cand_name)
            sc = float(cmp.get("score", 0.0) or 0.0)
            if sc > best_score:
                best_score = sc
                best = {"id": oid, "name": cand_name, "url": cand_url}

        if best_score >= threshold and best:
            return {**best, "score": round(best_score, 3), "source": "db_fuzzy"}

        if allow_b2b:
            for cand in onliner_b2b_search_candidates(name, category_name=infer_category(name), limit=8):
                sc = float(cand.get("score", 0.0) or 0.0)
                if sc >= float(threshold):
                    return {
                        "id": normalize_onliner_id(cand.get("id", "")),
                        "name": str(cand.get("name", "") or "").strip(),
                        "url": str(cand.get("url", "") or "").strip(),
                        "score": round(sc, 3),
                        "source": "b2b_fuzzy",
                    }
        return None

    except Exception as e:
        print(f"[db] db_find_id_for_name error: {e}", flush=True)
        return None


def db_find_top_candidates(product_name, top_n=5, min_score=0.40, allow_b2b=True):
    """Return top N candidates from DB for manual selection.
    Filtered by same brand and category when possible.
    Used when auto-match score is below threshold."""
    name = str(product_name or "").strip()
    if not name:
        return []
    try:
        with _db_connection() as conn:
            brand = _preferred_brand_token(name)
            candidates = {}

            def _add(rows):
                for r in rows:
                    if r[0] not in candidates:
                        candidates[r[0]] = (r[1], r[2])

            # Search by brand (most targeted)
            if brand and len(brand) >= 3:
                _add(conn.execute(
                    "SELECT ni.onliner_id, ni.raw_name, oc.url "
                    "FROM name_index ni JOIN onliner_catalog oc ON oc.onliner_id = ni.onliner_id "
                    "WHERE ni.raw_name LIKE ? LIMIT 200",
                    (f"%{brand}%",),
                ).fetchall())

            # Also try raw tokens
            for tok in _raw_search_tokens(name)[:3]:
                if len(tok) >= 4:
                    _add(conn.execute(
                        "SELECT ni.onliner_id, ni.raw_name, oc.url "
                        "FROM name_index ni JOIN onliner_catalog oc ON oc.onliner_id = ni.onliner_id "
                        "WHERE ni.raw_name LIKE ? LIMIT 100",
                        (f"%{tok}%",),
                    ).fetchall())

        if not candidates:
            return []

        scored = []
        for oid, (cand_name, cand_url) in candidates.items():
            allowed, _reason = _strict_candidate_allowed(name, cand_name)
            if not allowed:
                continue
            cmp = calc_name_match(name, cand_name)
            sc = float(cmp.get("score", 0.0) or 0.0)
            if sc >= min_score:
                scored.append({"id": oid, "name": cand_name, "url": cand_url,
                               "score": round(sc, 3)})

        scored.sort(key=lambda x: x["score"], reverse=True)
        top_scored = scored[:top_n]
        seen_ids = {normalize_onliner_id(x.get("id", "")) for x in top_scored}
        if allow_b2b and len(top_scored) < top_n:
            for cand in onliner_b2b_search_candidates(name, category_name=infer_category(name), limit=top_n * 2):
                cid = normalize_onliner_id(cand.get("id", ""))
                sc = float(cand.get("score", 0.0) or 0.0)
                if not cid or cid in seen_ids or sc < min_score:
                    continue
                top_scored.append({
                    "id": cid,
                    "name": str(cand.get("name", "") or "").strip(),
                    "url": str(cand.get("url", "") or "").strip(),
                    "score": round(sc, 3),
                    "source": "b2b_fuzzy",
                })
                seen_ids.add(cid)
                if len(top_scored) >= top_n:
                    break
        top_scored.sort(key=lambda x: float(x.get("score", 0.0) or 0.0), reverse=True)
        return top_scored[:top_n]

    except Exception as e:
        print(f"[db] db_find_top_candidates error: {e}", flush=True)
        return []


def db_search_tgpc_pc_candidates(local_name, limit=12):
    """Кандидаты для TGPC ПЭВМ только из локальной SQLite (без Onliner catalog API и B2B)."""
    name = str(local_name or "").strip()
    if not name or not _is_tgpc_pc_name(name):
        return []
    limit = max(5, min(int(limit or 12), 80))

    pool = {}
    exact = db_find_exact_id_for_name(name)
    if isinstance(exact, dict):
        eid = normalize_onliner_id(exact.get("id", ""))
        if eid:
            pool[eid] = (
                str(exact.get("name", "") or "").strip(),
                str(exact.get("url", "") or "").strip(),
            )

    try:
        with _db_connection() as conn:
            for q in _tgpc_pc_code_queries(name):
                qstrip = str(q or "").strip()
                if len(qstrip) < 2:
                    continue
                rows = conn.execute(
                    "SELECT ni.onliner_id, ni.raw_name, oc.url "
                    "FROM name_index ni JOIN onliner_catalog oc ON oc.onliner_id = ni.onliner_id "
                    "WHERE ni.raw_name LIKE ? LIMIT 180",
                    (f"%{qstrip}%",),
                ).fetchall()
                for r in rows:
                    oid = normalize_onliner_id(r["onliner_id"] if isinstance(r, sqlite3.Row) else r[0])
                    raw = str((r["raw_name"] if isinstance(r, sqlite3.Row) else r[1]) or "").strip()
                    url = str((r["url"] if isinstance(r, sqlite3.Row) else r[2]) or "").strip()
                    if oid and raw:
                        pool[oid] = (raw, url)

            code = _extract_tgpc_pc_code(name)
            if code and len(code) >= 4:
                rows = conn.execute(
                    "SELECT ni.onliner_id, ni.raw_name, oc.url "
                    "FROM name_index ni JOIN onliner_catalog oc ON oc.onliner_id = ni.onliner_id "
                    "WHERE ni.raw_name LIKE ? LIMIT 220",
                    (f"%{code}%",),
                ).fetchall()
                for r in rows:
                    oid = normalize_onliner_id(r["onliner_id"] if isinstance(r, sqlite3.Row) else r[0])
                    raw = str((r["raw_name"] if isinstance(r, sqlite3.Row) else r[1]) or "").strip()
                    url = str((r["url"] if isinstance(r, sqlite3.Row) else r[2]) or "").strip()
                    if oid and raw:
                        pool[oid] = (raw, url)
    except Exception as e:
        print(f"[db] db_search_tgpc_pc_candidates error: {e}", flush=True)
        return []

    local_tgpc_code = _extract_tgpc_pc_code(name)
    items = []
    seen = set()
    for oid, (cname, curl) in pool.items():
        if not oid or not cname or oid in seen:
            continue
        clower = cname.lower()
        curl_l = str(curl or "").strip().lower()
        candidate_is_tgpc = "tgpc" in clower
        candidate_is_pc_url = bool(curl_l and any(h in curl_l for h in ("/desktop/", "/computer/", "/tgpc/")))
        if not candidate_is_tgpc and not candidate_is_pc_url:
            continue

        cmp = calc_name_match(name, cname)
        score = float(cmp.get("score", 0.0) or 0.0)
        if local_tgpc_code:
            cand_code = _extract_tgpc_pc_code(cname)
            if cand_code and cand_code != local_tgpc_code:
                score = min(score, 0.12)
            elif not cand_code and local_tgpc_code not in (cname + str(curl or "")):
                score = min(score, 0.68)
        if not candidate_is_tgpc:
            score *= 0.70
        if curl_l and not any(h in curl_l for h in ("/desktop/", "/computer/", "/tgpc/")):
            score *= 0.80

        if score < 0.34:
            continue
        seen.add(oid)
        items.append({
            "id": oid,
            "name": cname,
            "url": str(curl or "").strip(),
            "score": round(score, 3),
            "source": "db_tgpc_pc",
            "reason": str(cmp.get("reason", "") or ""),
        })

    items.sort(key=lambda x: float(x.get("score", 0.0) or 0.0), reverse=True)
    return items[:limit]


def db_stats():
    """Статистика БД: количество товаров, имён, по источникам."""
    try:
        with _db_connection() as conn:
            total_products = conn.execute("SELECT COUNT(*) FROM onliner_catalog").fetchone()[0]
            total_names    = conn.execute("SELECT COUNT(*) FROM name_index").fetchone()[0]
            by_source = {
                r[0]: r[1]
                for r in conn.execute(
                    "SELECT source, COUNT(*) FROM onliner_catalog GROUP BY source ORDER BY COUNT(*) DESC"
                ).fetchall()
            }
        return {"total_products": total_products, "total_names": total_names, "by_source": by_source}
    except Exception:
        return {"total_products": 0, "total_names": 0, "by_source": {}}


# ── Onliner catalog bulk import ───────────────────────────────────────────
_catalog_import_status = {
    "running": False, "total": 0, "done": 0, "inserted": 0,
    "skipped": 0, "message": "", "percent": 0, "finished_at": None,
}
_CATALOG_IMPORT_LOCK = threading.RLock()
ONLINER_DB_GSHEET_CACHE_DIR = Path(__file__).parent / "uploads" / "_onliner_db_cache"
ONLINER_DB_GSHEET_CACHE_TTL_SEC = 12 * 60 * 60


def _catalog_import_worker(filepath: str, file_ext: str, cleanup_file: bool = True):
    """Background worker: parse CSV/XLSX and bulk-insert into SQLite."""
    global _catalog_import_status
    with _CATALOG_IMPORT_LOCK:
        _catalog_import_status.update({
            "running": True, "total": 0, "done": 0, "inserted": 0,
            "skipped": 0, "message": "Читаю файл...", "percent": 0, "finished_at": None,
        })

    try:
        # ── Read file ────────────────────────────────────────────────────
        if file_ext in (".xlsx", ".xls"):
            import openpyxl
            wb = openpyxl.load_workbook(filepath, read_only=True, data_only=True)
            ws = wb.active
            rows_iter = ([str(c.value or "") for c in row] for row in ws.iter_rows())
        else:
            import csv
            f_obj = open(filepath, "r", encoding="utf-8-sig", newline="")
            rows_iter = ([str(c) for c in row] for row in csv.reader(f_obj))

        # Skip header row (first row)
        rows = list(rows_iter)
        if rows:
            rows = rows[1:]  # skip header

        total = len(rows)
        with _CATALOG_IMPORT_LOCK:
            _catalog_import_status["total"] = total
            _catalog_import_status["message"] = f"Импортирую {total:,} строк..."

        # ── Bulk insert in batches ───────────────────────────────────────
        BATCH = 2000
        inserted = 0
        skipped = 0
        now_ts = int(time.time())

        for batch_start in range(0, total, BATCH):
            batch = rows[batch_start: batch_start + BATCH]
            prod_rows = []   # (onliner_id, name, url, source, ts)
            name_rows = []   # (name_key, onliner_id, raw_name)

            for row in batch:
                # Pad row to at least 8 columns
                while len(row) < 8:
                    row.append("")
                category  = row[0].strip()
                model_short = row[2].strip()   # C: короткая модель
                oid_raw   = row[4].strip()     # E: onliner_id
                full_name = row[7].strip()     # H: полное название по Onliner

                oid = normalize_onliner_id(oid_raw)
                if not oid:
                    skipped += 1
                    continue

                # Primary name = full Onliner name (H), fallback = category + short model
                name = full_name if full_name else (f"{category} {model_short}".strip())
                if not name:
                    skipped += 1
                    continue

                url = ""  # URL not needed for matching
                prod_rows.append((oid, name, url, "onliner_catalog", now_ts))

                # Index: full name
                nk = _normalize_name_key(name)
                if nk:
                    name_rows.append((nk, oid, name))

                # Index: short model (alternate name variant)
                if model_short and model_short.lower() not in name.lower():
                    alt_name = f"{category} {model_short}".strip() if category else model_short
                    nk2 = _normalize_name_key(alt_name)
                    if nk2 and nk2 != nk:
                        name_rows.append((nk2, oid, alt_name))

            try:
                with _DB_WRITE_LOCK:
                    with _db_connection() as conn:
                        conn.executemany(
                            "INSERT OR REPLACE INTO onliner_catalog "
                            "(onliner_id, name, url, source, updated_at) VALUES (?,?,?,?,?)",
                            prod_rows,
                        )
                        conn.executemany(
                            "INSERT OR REPLACE INTO name_index "
                            "(name_key, onliner_id, raw_name) VALUES (?,?,?)",
                            name_rows,
                        )
                        conn.commit()
                inserted += len(prod_rows)
            except Exception as e:
                print(f"[db_import] batch error: {e}", flush=True)

            done = batch_start + len(batch)
            pct = round(done / total * 100) if total else 100
            with _CATALOG_IMPORT_LOCK:
                _catalog_import_status.update({
                    "done": done, "inserted": inserted, "skipped": skipped,
                    "percent": pct,
                    "message": f"Импортировано {inserted:,} из {total:,} ({pct}%)",
                })

        with _CATALOG_IMPORT_LOCK:
            _catalog_import_status.update({
                "running": False, "done": total, "inserted": inserted,
                "skipped": skipped, "percent": 100,
                "message": f"Готово! Добавлено {inserted:,} товаров, пропущено {skipped:,}.",
                "finished_at": int(time.time()),
            })
        print(f"[db_import] Done. inserted={inserted}, skipped={skipped}", flush=True)

    except Exception as e:
        with _CATALOG_IMPORT_LOCK:
            _catalog_import_status.update({
                "running": False, "message": f"Ошибка: {e}", "finished_at": int(time.time()),
            })
        print(f"[db_import] Error: {e}", flush=True)
    finally:
        if cleanup_file:
            try:
                import os
                os.remove(filepath)
            except Exception:
                pass


def load_manual_id_bindings():
    if not MANUAL_ID_BINDINGS_FILE.exists():
        return {}
    try:
        with open(MANUAL_ID_BINDINGS_FILE, "r", encoding="utf-8") as f:
            data = json.load(f)
        if isinstance(data, dict):
            return data
    except Exception:
        pass
    return {}


def save_manual_id_bindings(bindings):
    with open(MANUAL_ID_BINDINGS_FILE, "w", encoding="utf-8") as f:
        json.dump(bindings, f, ensure_ascii=False, indent=2)


def load_review_queue():
    if not REVIEW_QUEUE_FILE.exists():
        return {}
    try:
        with open(REVIEW_QUEUE_FILE, "r", encoding="utf-8") as f:
            data = json.load(f)
        if isinstance(data, dict):
            return data
    except Exception:
        pass
    return {}


def save_review_queue(queue):
    with open(REVIEW_QUEUE_FILE, "w", encoding="utf-8") as f:
        json.dump(queue, f, ensure_ascii=False, indent=2)


def load_id_change_journal():
    if not ID_CHANGE_JOURNAL_FILE.exists():
        return []
    try:
        with open(ID_CHANGE_JOURNAL_FILE, "r", encoding="utf-8") as f:
            data = json.load(f)
        if isinstance(data, list):
            return data[-5000:]
    except Exception:
        pass
    return []


def save_id_change_journal(rows):
    data = rows if isinstance(rows, list) else []
    with open(ID_CHANGE_JOURNAL_FILE, "w", encoding="utf-8") as f:
        json.dump(data[-5000:], f, ensure_ascii=False, indent=2)


def append_id_change_journal(entry):
    if not isinstance(entry, dict):
        return
    rows = load_id_change_journal()
    rows.append(entry)
    save_id_change_journal(rows)


def is_manually_confirmed_id(name, onliner_id):
    oid = normalize_onliner_id(onliner_id)
    name_key = _normalize_name_key(name)
    if not oid or not name_key:
        return False
    bindings = load_manual_id_bindings()
    rec = bindings.get(name_key)
    if not isinstance(rec, dict):
        return False
    return normalize_onliner_id(rec.get("id", "")) == oid


def _safe_float(value):
    try:
        return float(str(value).replace(",", "."))
    except Exception:
        return None


def _extract_position_prices(payload):
    prices = []
    if isinstance(payload, dict):
        for k, v in payload.items():
            if k == "position_price" and isinstance(v, dict):
                amount = _safe_float(v.get("amount"))
                if amount is not None and amount > 0:
                    prices.append(amount)
            else:
                prices.extend(_extract_position_prices(v))
    elif isinstance(payload, list):
        for x in payload:
            prices.extend(_extract_position_prices(x))
    return prices


def _fetch_onliner_product_payload(onliner_id):
    oid = normalize_onliner_id(onliner_id)
    if not oid:
        return None, "пустой onliner id"
    headers = {"User-Agent": "Mozilla/5.0", "Accept": "application/json"}
    product = None
    direct_error = ""
    search_error = ""
    try:
        rp = onliner_api_get(
            f"https://catalog.api.onliner.by/products/{oid}",
            timeout=12,
            headers=headers,
        )
        if rp.ok:
            payload = rp.json() or {}
            payload_id = normalize_onliner_id(payload.get("id", ""))
            if payload_id == oid:
                product = payload
            else:
                direct_error = f"products/{oid}: mismatched id {payload_id or 'empty'}"
        else:
            direct_error = f"products/{oid}: http {rp.status_code}"
    except Exception:
        direct_error = f"products/{oid}: timeout/connection"

    if not product:
        search_url = f"https://catalog.api.onliner.by/search/products?query={oid}"
        try:
            r = onliner_api_get(search_url, timeout=12, headers=headers)
            if r.ok:
                products = (r.json() or {}).get("products", [])
                for p in products:
                    if str(p.get("id", "")).strip() == oid:
                        product = p
                        break
                if product is None:
                    search_error = "search: товар не найден"
            else:
                search_error = f"search: http {r.status_code}"
        except Exception:
            search_error = "search: timeout/connection"

    if product:
        return product, ""
    reason = "; ".join([x for x in [direct_error, search_error] if x]) or "товар не найден"
    return None, reason


def _extract_offer_rows(payload):
    payload = payload or {}
    rows = []

    shops_map = payload.get("shops") or {}
    if not isinstance(shops_map, dict):
        shops_map = {}

    def _shop_from_map(shop_id):
        sid = str(shop_id or "").strip()
        if not sid:
            return {}
        if sid in shops_map and isinstance(shops_map[sid], dict):
            return shops_map[sid]
        if sid.isdigit() and int(sid) in shops_map and isinstance(shops_map[int(sid)], dict):
            return shops_map[int(sid)]
        return {}

    def _extract_price(node):
        if isinstance(node.get("position_price"), dict):
            price = _safe_float((((node.get("position_price") or {}).get("converted") or {}).get("BYN") or {}).get("amount"))
            if price is None:
                price = _safe_float((node.get("position_price") or {}).get("amount"))
            if price is not None:
                return price
        if isinstance(node.get("price"), dict):
            price = _safe_float((((node.get("price") or {}).get("converted") or {}).get("BYN") or {}).get("amount"))
            if price is None:
                price = _safe_float((node.get("price") or {}).get("amount"))
            if price is not None:
                return price
        if "price" in node:
            return _safe_float(node.get("price"))
        return None

    def _seller_info(node):
        seller_name = ""
        seller_id = str(node.get("seller_id") or node.get("shop_id") or node.get("vendor_id") or "").strip()
        seller_url = str(node.get("shop_url") or node.get("seller_url") or "").strip()

        for key in ("shop", "seller", "vendor", "merchant", "store"):
            val = node.get(key)
            if isinstance(val, dict):
                seller_name = str(val.get("title") or val.get("name") or val.get("full_name") or seller_name).strip()
                seller_id = str(val.get("id") or val.get("key") or seller_id).strip()
                seller_url = str(val.get("html_url") or val.get("url") or seller_url).strip()
                if seller_name or seller_id or seller_url:
                    break

        mapped = _shop_from_map(seller_id)
        if mapped:
            if not seller_name:
                seller_name = str(mapped.get("title") or mapped.get("name") or "").strip()
            if not seller_url:
                seller_url = str(mapped.get("html_url") or mapped.get("url") or "").strip()
            if not seller_id:
                seller_id = str(mapped.get("id") or "").strip()

        if not seller_name:
            seller_name = str(node.get("seller_name") or node.get("shop_name") or node.get("vendor_name") or "").strip()

        return seller_name, seller_id, seller_url

    positions = payload.get("positions") or {}
    if isinstance(positions, dict):
        iter_lists = [v for v in positions.values() if isinstance(v, list)]
    elif isinstance(positions, list):
        iter_lists = [positions]
    else:
        iter_lists = []

    for items in iter_lists:
        for node in items:
            if not isinstance(node, dict):
                continue
            price = _extract_price(node)
            if price is None:
                continue
            seller_name, seller_id, seller_url = _seller_info(node)
            rows.append({
                "seller_name": seller_name or "—",
                "seller_id": seller_id,
                "seller_url": seller_url,
                "price": round(float(price), 2),
                "url": str(node.get("product_url") or node.get("html_url") or node.get("url") or seller_url or "").strip(),
                "warranty": str(node.get("warranty") or "").strip(),
                "stock": str(((node.get("stock_status") or {}).get("text") or "")).strip(),
                "updated_at": str(node.get("date_update") or "").strip(),
            })

    if not rows:
        def _walk(node):
            if isinstance(node, dict):
                price = _extract_price(node)
                if price is not None:
                    seller_name, seller_id, seller_url = _seller_info(node)
                    rows.append({
                        "seller_name": seller_name or "—",
                        "seller_id": seller_id,
                        "seller_url": seller_url,
                        "price": round(float(price), 2),
                        "url": str(node.get("product_url") or node.get("html_url") or node.get("url") or seller_url or "").strip(),
                        "warranty": str(node.get("warranty") or "").strip(),
                        "stock": str(((node.get("stock_status") or {}).get("text") or "")).strip(),
                        "updated_at": str(node.get("date_update") or "").strip(),
                    })
                for v in node.values():
                    _walk(v)
            elif isinstance(node, list):
                for item in node:
                    _walk(item)
        _walk(payload)

    dedup = []
    seen = set()
    for row in rows:
        key = (row.get("seller_id", ""), row.get("seller_name", ""), row.get("price", ""), row.get("url", ""))
        if key in seen:
            continue
        seen.add(key)
        dedup.append(row)
    return dedup


def _fetch_onliner_market_stats_catalog_api(onliner_id):
    """Публичный catalog.api.onliner.by (fallback, если B2B выключен)."""
    oid = normalize_onliner_id(onliner_id)
    if not oid:
        return {"min": None, "avg": None, "max": None, "offers": 0, "min_competitors": 0, "avg_competitors": 0, "_error": True, "_error_reason": "пустой onliner id"}
    headers = {"User-Agent": "Mozilla/5.0", "Accept": "application/json"}
    product, product_error = _fetch_onliner_product_payload(oid)
    if not product:
        return {
            "min": None, "avg": None, "max": None, "offers": 0,
            "min_competitors": 0, "avg_competitors": 0,
            "_error": True,
            "_error_reason": product_error or "товар не найден",
        }

    prices_obj = product.get("prices") or {}
    min_price = _safe_float((((prices_obj.get("price_min") or {}).get("converted") or {}).get("BYN") or {}).get("amount"))
    if min_price is None:
        min_price = _safe_float((prices_obj.get("price_min") or {}).get("amount"))
    offers_count = int(((prices_obj.get("offers") or {}).get("count") or 0))
    avg_price = None
    max_price = None
    min_competitors = 0
    avg_competitors = 0

    # Step 2: compute average from all current positions.
    positions_url = str(prices_obj.get("url", "")).strip()
    if positions_url:
        try:
            rp = onliner_api_get(positions_url, timeout=12, headers=headers)
            if rp.ok:
                position_prices = _extract_position_prices(rp.json())
                if position_prices:
                    avg_price = round(float(sum(position_prices)) / len(position_prices), 2)
                    max_price = round(float(max(position_prices)), 2)
                    min_price = round(float(min(position_prices)), 2)
                    min_competitors = sum(1 for p in position_prices if p <= min_price * 1.02)
                    avg_competitors = sum(1 for p in position_prices if abs(p - avg_price) <= max(1.0, avg_price * 0.05))
                    if not offers_count:
                        offers_count = len(position_prices)
                else:
                    positions_error = "positions: пустой список цен"
            else:
                positions_error = f"positions: http {rp.status_code}"
        except Exception:
            positions_error = "positions: timeout/connection"

    # Показываем рыночные цены уже при 1+ конкуренте.
    # Если API не отдал offers.count, но min цена есть — считаем как минимум 1 предложение.
    if offers_count <= 0 and min_price is not None:
        offers_count = 1

    if offers_count < 1:
        min_price = None
        avg_price = None
        max_price = None
        min_competitors = 0
        avg_competitors = 0
    elif min_price is not None:
        if avg_price is None:
            avg_price = min_price
        if max_price is None:
            max_price = min_price

    return {
        "min": None if min_price is None else round(float(min_price), 2),
        "avg": None if avg_price is None else round(float(avg_price), 2),
        "max": None if max_price is None else round(float(max_price), 2),
        "offers": offers_count,
        "min_competitors": int(min_competitors),
        "avg_competitors": int(avg_competitors),
        "_error": False,
        "_error_reason": "" if offers_count >= 1 else str(locals().get("positions_error", "") or ""),
    }


def fetch_onliner_market_stats(onliner_id, product_name="", category_name=""):
    """Рыночные цены: сначала публичный catalog API (быстрее при массовом обновлении); при пустых данных — B2B price.api."""
    oid = normalize_onliner_id(onliner_id)
    if not oid:
        return {"min": None, "avg": None, "max": None, "offers": 0, "min_competitors": 0, "avg_competitors": 0, "_error": True, "_error_reason": "пустой onliner id"}
    cat_stats = _fetch_onliner_market_stats_catalog_api(oid)
    if market_stats_has_values(cat_stats):
        return cat_stats
    cfg = get_onliner_b2b_settings()
    if (
        cfg.get("enabled")
        and str(cfg.get("client_id", "") or "").strip()
        and str(cfg.get("client_secret", "") or "").strip()
    ):
        b2b_stats = _fetch_onliner_market_stats_b2b(oid, product_name=product_name, category_name=category_name)
        if market_stats_has_values(b2b_stats):
            return b2b_stats
    return cat_stats


def get_onliner_market_stats_cached(onliner_id, cache=None):
    oid = normalize_onliner_id(onliner_id)
    if not oid:
        return {"min": None, "avg": None, "offers": 0}
    if cache is None:
        cache = load_onliner_market_cache()
    now = int(time.time())
    with ONLINER_PRODUCT_CACHE_LOCK:
        cached = cache.get(oid)
    if isinstance(cached, dict) and (now - int(cached.get("updated_at", 0)) <= ONLINER_MARKET_CACHE_TTL):
        return {
            "min": _safe_float(cached.get("min")),
            "avg": _safe_float(cached.get("avg")),
            "max": _safe_float(cached.get("max")),
            "offers": int(cached.get("offers", 0) or 0),
            "min_competitors": int(cached.get("min_competitors", 0) or 0),
            "avg_competitors": int(cached.get("avg_competitors", 0) or 0),
        }
    hint_name = ""
    hint_cat = ""
    dbp = db_get_product_by_id(oid)
    if isinstance(dbp, dict):
        hint_name = str(dbp.get("name", "") or "").strip()
    if hint_name:
        hint_cat = infer_category(hint_name)
    stats = fetch_onliner_market_stats(oid, product_name=hint_name, category_name=hint_cat)
    cache[oid] = {"updated_at": now, **stats}
    return stats


def get_onliner_market_stats_from_cache_only(onliner_id, cache=None, allow_stale=True):
    oid = normalize_onliner_id(onliner_id)
    if not oid:
        return {"min": None, "avg": None, "max": None, "offers": 0, "min_competitors": 0, "avg_competitors": 0}
    if cache is None:
        cache = load_onliner_market_cache()
    cached = cache.get(oid)
    if not isinstance(cached, dict):
        return {"min": None, "avg": None, "max": None, "offers": 0, "min_competitors": 0, "avg_competitors": 0}
    if not allow_stale:
        now = int(time.time())
        if now - int(cached.get("updated_at", 0)) > ONLINER_MARKET_CACHE_TTL:
            return {"min": None, "avg": None, "max": None, "offers": 0, "min_competitors": 0, "avg_competitors": 0}
    return {
        "min": _safe_float(cached.get("min")),
        "avg": _safe_float(cached.get("avg")),
        "max": _safe_float(cached.get("max")),
        "offers": int(cached.get("offers", 0) or 0),
        "min_competitors": int(cached.get("min_competitors", 0) or 0),
        "avg_competitors": int(cached.get("avg_competitors", 0) or 0),
    }


def market_stats_has_values(stats):
    if not isinstance(stats, dict):
        return False
    if _safe_float(stats.get("min")) is not None:
        return True
    if _safe_float(stats.get("avg")) is not None:
        return True
    if _safe_float(stats.get("max")) is not None:
        return True
    if int(stats.get("offers", 0) or 0) > 0:
        return True
    return False


def get_onliner_market_stats_bulk(onliner_ids, max_workers=22, id_hints=None):
    ids = [normalize_onliner_id(x) for x in onliner_ids]
    ids = [x for x in ids if x]
    if not ids:
        return {}
    cache = load_onliner_market_cache()
    result = {}
    pending = []
    now = int(time.time())
    for oid in ids:
        cached = cache.get(oid)
        if isinstance(cached, dict) and (now - int(cached.get("updated_at", 0)) <= ONLINER_MARKET_CACHE_TTL):
            result[oid] = {
                "min": _safe_float(cached.get("min")),
                "avg": _safe_float(cached.get("avg")),
                "max": _safe_float(cached.get("max")),
                "offers": int(cached.get("offers", 0) or 0),
                "min_competitors": int(cached.get("min_competitors", 0) or 0),
                "avg_competitors": int(cached.get("avg_competitors", 0) or 0),
            }
        else:
            pending.append(oid)

    def _fetch_one_market(oid):
        hint = (id_hints or {}).get(oid) if id_hints else None
        if isinstance(hint, dict):
            return fetch_onliner_market_stats(
                oid,
                product_name=str(hint.get("name", "") or ""),
                category_name=str(hint.get("category", "") or ""),
            )
        return fetch_onliner_market_stats(oid)

    if pending:
        with ThreadPoolExecutor(max_workers=max_workers) as ex:
            fut_to_oid = {ex.submit(_fetch_one_market, oid): oid for oid in pending}
            for fut in as_completed(fut_to_oid):
                oid = fut_to_oid[fut]
                try:
                    stats = fut.result()
                except Exception:
                    stats = {
                        "min": None, "avg": None, "max": None, "offers": 0,
                        "min_competitors": 0, "avg_competitors": 0, "_error": True,
                    }
                result[oid] = stats
                cache[oid] = {"updated_at": now, **stats}
        save_onliner_market_cache(cache)
    return result


def fetch_onliner_product_info(onliner_id, cache=None, force_refresh=False,
                               use_cache_on_error=True, product_name_hint=None):
    oid = normalize_onliner_id(onliner_id)
    if not oid:
        return {"name": "", "url": "", "source": "empty"}
    if cache is None:
        cache = load_onliner_product_cache()
    now = int(time.time())
    cached = cache.get(oid)
    if (not force_refresh) and isinstance(cached, dict) and now - int(cached.get("updated_at", 0)) <= ONLINER_PRODUCT_CACHE_TTL:
        return {
            "name": str(cached.get("name", "")).strip(),
            "url": str(cached.get("url", "")).strip(),
            "source": "cache",
        }

    headers = {"User-Agent": "Mozilla/5.0", "Accept": "application/json"}

    def _cache_and_return(name, url, source):
        with ONLINER_PRODUCT_CACHE_LOCK:
            cache[oid] = {"updated_at": now, "name": name, "url": url}
        return {"name": name, "url": url, "source": source}

    db_product = db_get_product_by_id(oid)
    if isinstance(db_product, dict) and str(db_product.get("name", "")).strip():
        return _cache_and_return(
            str(db_product.get("name", "")).strip(),
            str(db_product.get("url", "")).strip(),
            "db",
        )

    def _search_by_id_fallback():
        """Search by numeric ID — works when /products/{oid} returns 404."""
        try:
            rs = onliner_api_get(
                f"https://catalog.api.onliner.by/search/products?query={oid}",
                timeout=12,
                headers=headers,
            )
            if not rs.ok:
                return None
            products = (rs.json() or {}).get("products") or []
            # Exact numeric ID match
            for p in products:
                if str(p.get("id", "")).strip() == oid:
                    return {
                        "name": str(p.get("full_name") or p.get("name") or "").strip(),
                        "url": str(p.get("html_url") or "").strip(),
                    }
            return None
        except Exception:
            return None

    def _search_by_name_fallback(name_hint):
        """Search by product name — fallback when ID search fails."""
        if not name_hint:
            return None
        try:
            from urllib.parse import quote as _quote
            rs = onliner_api_get(
                f"https://catalog.api.onliner.by/search/products?query={_quote(name_hint[:80])}",
                timeout=12,
                headers=headers,
            )
            if not rs.ok:
                return None
            products = (rs.json() or {}).get("products") or []
            # Exact numeric ID match in name search results
            for p in products:
                if str(p.get("id", "")).strip() == oid:
                    return {
                        "name": str(p.get("full_name") or p.get("name") or "").strip(),
                        "url": str(p.get("html_url") or "").strip(),
                    }
            return None
        except Exception:
            return None

    try:
        # Primary path: direct product endpoint.
        # Onliner /products/{id} accepts URL key-slugs (e.g. "95834") OR numeric IDs.
        # The JSON response has "id" (internal numeric) and "key" (URL slug).
        # We accept the result if EITHER field matches our queried oid.
        r = onliner_api_get(
            f"https://catalog.api.onliner.by/products/{oid}",
            timeout=8,
            headers=headers,
        )
        if r.ok:
            d = r.json() or {}
            payload_numeric_id = normalize_onliner_id(d.get("id", ""))
            payload_key = str(d.get("key", "")).strip()
            id_match = (payload_numeric_id == oid) or (payload_key == oid)
            name = str(d.get("full_name") or d.get("name") or "").strip()
            url = str(d.get("html_url") or "").strip()
            if id_match and name:
                return _cache_and_return(name, url, "api")
            # If direct endpoint returned OK but ID does not match,
            # it means the oid is a numeric ID that the endpoint treated as a key and
            # returned a different product — fall through to search.

        # Fallback: search by numeric ID
        fb = _search_by_id_fallback()
        if fb and fb.get("name"):
            return _cache_and_return(fb["name"], fb.get("url", ""), "search_by_id")

        # Second fallback: search by product name (helps when ID search times out)
        fb2 = _search_by_name_fallback(product_name_hint)
        if fb2 and fb2.get("name"):
            return _cache_and_return(fb2["name"], fb2.get("url", ""), "search_by_name")

        if use_cache_on_error and isinstance(cached, dict):
            return {
                "name": str(cached.get("name", "")).strip(),
                "url": str(cached.get("url", "")).strip(),
                "source": "cache_fallback_http_error",
            }
        return {"name": "", "url": "", "source": "http_error"}

    except Exception:
        fb = _search_by_id_fallback()
        if fb and fb.get("name"):
            return _cache_and_return(fb["name"], fb.get("url", ""), "search_by_id_after_error")
        fb2 = _search_by_name_fallback(product_name_hint)
        if fb2 and fb2.get("name"):
            return _cache_and_return(fb2["name"], fb2.get("url", ""), "search_by_name_after_error")
        if use_cache_on_error and isinstance(cached, dict):
            return {
                "name": str(cached.get("name", "")).strip(),
                "url": str(cached.get("url", "")).strip(),
                "source": "cache_fallback_error",
            }
        return {"name": "", "url": "", "source": "error"}


def search_onliner_product_by_name(local_name):
    """
    Fallback-поиск товара в Onliner API по названию локального товара.
    Возвращает лучший кандидат по score.
    """
    name = str(local_name or "").strip()
    if not name:
        return {"id": "", "name": "", "url": "", "score": 0.0, "source": "empty_query"}
    headers = {"User-Agent": "Mozilla/5.0", "Accept": "application/json"}

    candidates = []
    art = str(extract_article(name) or "").strip()
    if art:
        candidates.append(art)
    tokens = _name_tokens(name)
    if tokens:
        candidates.append(" ".join(tokens[:6]))
    candidates.append(name[:120])

    seen = set()
    queries = []
    for q in candidates:
        q = str(q).strip()
        if not q or q.lower() in seen:
            continue
        seen.add(q.lower())
        queries.append(q)

    best = {"id": "", "name": "", "url": "", "score": 0.0, "source": "not_found"}
    for q in queries[:3]:
        try:
            rs = onliner_api_get(
                f"https://catalog.api.onliner.by/search/products?query={quote(q)}",
                timeout=12,
                headers=headers,
            )
            if not rs.ok:
                continue
            data = rs.json() or {}
            products = data.get("products") or []
            for p in products[:15]:
                pid = normalize_onliner_id(p.get("id", ""))
                pname = str(p.get("full_name") or p.get("name") or "").strip()
                purl = str(p.get("html_url") or "").strip()
                if not pid or not pname:
                    continue
                cmp = calc_name_match(name, pname)
                score = float(cmp.get("score", 0.0) or 0.0)
                if cmp.get("match"):
                    score = max(score, 0.75)
                if score > float(best.get("score", 0.0)):
                    best = {"id": pid, "name": pname, "url": purl, "score": score, "source": "search_name"}
        except Exception:
            continue
        if float(best.get("score", 0.0)) >= 0.78:
            break
    return best


def search_onliner_product_by_name_deep(local_name, category_name=""):
    """
    Более агрессивный поиск кандидата:
    - пробует обычный быстрый поиск,
    - затем расширенный поиск по нескольким запросам,
    - затем перепроверяет лучшие кандидаты по карточке товара.
    """
    name = str(local_name or "").strip()
    if not name:
        return {"id": "", "name": "", "url": "", "score": 0.0, "source": "empty_query"}

    best = search_onliner_product_by_name(name)
    best_score = float(best.get("score", 0.0) or 0.0)

    candidates = search_onliner_candidates(
        name,
        category_name=category_name,
        query="",
        limit=10,
        max_queries=3,
        timeout_sec=5,
    )
    if not candidates:
        return best

    cache = load_onliner_product_cache()
    touched = False
    for cand in candidates[:4]:
        cid = normalize_onliner_id(cand.get("id", ""))
        if not cid:
            continue
        info = fetch_onliner_product_info(cid, cache=cache, force_refresh=False, use_cache_on_error=True)
        if info.get("source") == "api":
            touched = True
        pname = str(info.get("name", "") or cand.get("name", "")).strip()
        purl = str(info.get("url", "") or cand.get("url", "")).strip()
        if not pname:
            continue
        cmp = calc_name_match(name, pname)
        score = float(cmp.get("score", 0.0) or 0.0)
        if cmp.get("match"):
            score = max(score, 0.80)
        # бонус за совпадение артикульных токенов
        try:
            art_local = _article_like_tokens(name)
            art_remote = _article_like_tokens(pname)
            if art_local and art_remote and art_local.intersection(art_remote):
                score = max(score, 0.86)
        except Exception:
            pass
        if score > best_score:
            best_score = score
            best = {
                "id": cid,
                "name": pname,
                "url": purl,
                "score": score,
                "source": "search_name_deep",
            }
        if best_score >= 0.86:
            break

    if touched:
        try:
            save_onliner_product_cache(cache)
        except Exception:
            pass
    return best


def _category_path_hints(category_name):
    c = str(category_name or "").strip().lower()
    if c == "процессор":
        return ["/cpu/"]
    if c == "видеокарта":
        return ["/videocard/"]
    if c == "оперативная память":
        return ["/dram/"]
    if c == "материнская плата":
        return ["/motherboard/"]
    if c == "ssd":
        return ["/ssd/"]
    if c == "жесткий диск":
        return ["/hdd/"]
    if c == "блок питания":
        return ["/powersupply/", "/psu/"]
    if c == "корпус":
        return ["/case/"]
    if c == "кулер":
        return ["/cooler/"]
    if c == "монитор":
        return ["/display/"]
    if c == "системный блок":
        return ["/desktop/", "/computer/", "/tgpc/"]
    return []


def _preferred_brand_token(text):
    """Return the first significant word that is NOT a category label and has no digits."""
    raw = str(text or "")
    skip = {
        "hdd", "ssd", "sata", "sataii", "sataiii", "usb", "typea", "typec", "ddr3", "ddr4", "ddr5",
        "pc", "pcie", "nvme", "bulk", "tb", "gb", "mb", "mhz", "rpm", "mm", "cl", "pc25600",
        # Form-factor / descriptor words often following category names
        "atx", "matx", "eatx", "itx", "microatx", "miniitx", "midiтower", "minitower",
        # Russian prepositions/fillers common in product names
        "без", "для", "под", "про", "как", "из", "по", "на", "со", "за",
        # Common descriptor words
        "desktop", "mini", "midi", "tower", "slim", "ultra",
        # Russian category abbreviations that may follow form-factor
        "бп", "ибп", "мфу", "узу", "сзу",
        # "Без БП" = "without PSU" — skip both words
        "корпус", "кейс",
        # Russian adjective qualifiers that prefix the product category
        "игровая", "игровой", "игровое", "беспроводная", "беспроводной",
        "проводная", "проводной", "механическая", "механический",
        "мембранная", "мембранный", "оптическая", "оптический",
        "лазерная", "лазерный", "ультратонкая", "ультратонкий",
        "портативная", "портативный", "внешний", "внешняя",
        "встроенный", "встроенная", "цветной", "цветная",
        "черно-белый", "черно-белая", "монохромный", "монохромная",
        # Russian category nouns (when they appear as first token after adjective)
        "мышь", "мышка", "клавиатура", "гарнитура", "наушники", "монитор",
        "принтер", "сканер", "колонки", "колонка", "акустика", "камера",
        "ноутбук", "планшет", "смартфон", "телефон", "роутер", "коммутатор",
        "адаптер", "переходник", "кабель", "шнур", "разветвитель",
        "вентилятор", "кулер", "охладитель", "стабилизатор",
    }
    # How many words to skip if name starts with a known multi-word category
    words_to_skip = 0
    stripped = raw.strip().lower()
    for n in (3, 2, 1):
        prefix_words = re.split(r"[\s,.(]+", stripped)[:n]
        prefix = " ".join(prefix_words)
        if prefix in _CATEGORY_LOOKUP:
            words_to_skip = n
            break

    idx = 0
    for token in re.findall(r"[A-Za-zА-Яа-я][A-Za-zА-Яа-я0-9.+_-]{1,}", raw):
        norm = _normalize_compact_name(token)
        if not norm or norm in skip:
            idx += 1
            continue
        if any(ch.isdigit() for ch in norm):
            idx += 1
            continue
        if idx < words_to_skip:
            idx += 1
            continue
        return token
    return ""


def _normalized_brand_token(text):
    return _normalize_compact_name(_preferred_brand_token(text))


def _normalized_category_name(text):
    category = str(normalize_catalog_category_name(infer_category(text)) or "").strip()
    if category == "Без категории":
        return ""
    return category


def _strict_candidate_allowed(local_name, candidate_name):
    local = str(local_name or "").strip()
    candidate = str(candidate_name or "").strip()
    if not local or not candidate:
        return False, "empty"

    local_category = _normalized_category_name(local)
    candidate_category = _normalized_category_name(candidate)
    if local_category and candidate_category and local_category != candidate_category:
        return False, "category"

    local_brand = _normalized_brand_token(local)
    candidate_brand = _normalized_brand_token(candidate)
    if local_brand:
        if not candidate_brand:
            return False, "brand_missing"
        if local_brand != candidate_brand:
            return False, "brand"

    return True, "ok"


def _priority_model_queries(text):
    raw = str(text or "").strip()
    if not raw:
        return []

    seen = set()
    out = []
    brand = _preferred_brand_token(raw)

    def _add(query):
        q = str(query or "").strip()
        key = q.lower()
        if not q or key in seen:
            return
        seen.add(key)
        out.append(q)

    def _push_token(token):
        token = str(token or "").strip()
        norm = _normalize_compact_name(token)
        token_lower = token.lower()
        if len(norm) < 5:
            return
        if not any(ch.isdigit() for ch in norm):
            return
        if not any(ch.isalpha() for ch in norm):
            return
        if re.match(r"^\d{2,4}x\d{2,4}$", token_lower):
            return
        _add(token)
        if brand and norm != _normalize_compact_name(brand):
            _add(f"{brand} {token}")

    for chunk in _paren_chunks(raw):
        for token in extract_article_candidates(chunk):
            _push_token(token)
        for token in re.findall(r"[A-Za-z0-9][A-Za-z0-9._\-/]{4,}", chunk):
            _push_token(token)

    for token in extract_article_candidates(raw):
        _push_token(token)

    for token in re.findall(r"[A-Za-z0-9][A-Za-z0-9._\-/]{4,}", raw):
        _push_token(token)

    return out


def _tgpc_pc_code_queries(text):
    raw = str(text or "").strip()
    if not raw:
        return []

    queries = []
    seen = set()

    def _add(value):
        q = str(value or "").strip()
        key = q.lower()
        if not q or key in seen:
            return
        seen.add(key)
        queries.append(q)

    # TGPC ПЭВМ: "ПЭВМ TGPC Action 5 81872 A-X Ryzen 5 ..."
    # Приоритетный ключ для Onliner: "81872 A-X"
    m = re.search(r"\b(\d{4,6})\s+([A-ZА-Я]-X)\b", raw, flags=re.IGNORECASE)
    if m:
        code = m.group(1).strip()
        suffix = m.group(2).upper().replace("А", "A")
        _add(f"{code} {suffix}")
        _add(f"{code}{suffix.replace('-', '')}")

    # Иногда формат бывает слитный: 81872A-X
    m2 = re.search(r"\b(\d{4,6})([A-ZА-Я]-X)\b", raw, flags=re.IGNORECASE)
    if m2:
        code = m2.group(1).strip()
        suffix = m2.group(2).upper().replace("А", "A")
        _add(f"{code} {suffix}")
        _add(f"{code}{suffix.replace('-', '')}")

    return queries


def _is_tgpc_pc_name(text):
    raw = str(text or "").strip().lower()
    if "tgpc" not in raw:
        return False
    if _tgpc_pc_code_queries(raw):
        return True
    return any(token in raw for token in ["action", "mesh", "osprey", "xtreme", "valise", "gaming"])


def _cpu_brand_model_key(text):
    raw = str(text or "").strip()
    if not raw:
        return "", ""
    low = raw.lower()
    brand = ""
    if "intel" in low or "xeon" in low or "pentium" in low or "celeron" in low:
        brand = "intel"
    elif "amd" in low or "ryzen" in low or "athlon" in low:
        brand = "amd"

    patterns = [
        # Intel Core classic: i5-12400F
        r"\b(i[3579]-\d{4,5}[a-z]{0,3})\b",
        # Intel Core Ultra: Core Ultra 5 225 / 225F / 285K
        r"\b(core\s*ultra\s*[3579]\s*\d{3,4}[a-z]{0,3})\b",
        # AMD Ryzen / Ryzen PRO / X3D etc: Ryzen 5 7600X3D, Ryzen 5 PRO 5655G
        r"\b(ryzen\s*[3579]\s*(?:pro\s*)?\d{3,5}[a-z0-9]{0,4})\b",
        # Pentium / Celeron (incl. "Pentium Gold G6405")
        r"\b(pentium\s+(?:gold\s+)?[a-z]?\d{3,5}[a-z]{0,2})\b",
        r"\b(celeron\s+[a-z]?\d{3,5}[a-z]{0,2})\b",
        # Athlon / Athlon Pro 300GE
        r"\b(athlon\s*(?:pro\s*)?[a-z]?\d{3,5}[a-z]{0,3})\b",
        # Xeon E-2378G / W-xxxx / E5-xxxx variants
        r"\b(xeon\s+[a-z]{0,2}-?\d{3,5}[a-z]{0,3}(?:\s*v?\d)?)\b",
        # EPYC families: EPYC 7282
        r"\b(epyc\s+\d{3,4}[a-z]{0,2})\b",
    ]
    model = ""
    # EPYC strings often contain "Series ... Model 7282" — prefer explicit model token.
    m_epyc_model = re.search(r"\bmodel\s+(\d{3,4}[a-z]{0,2})\b", low, flags=re.IGNORECASE)
    if m_epyc_model and m_epyc_model.group(1) and ("epyc" in low):
        model = _normalize_compact_name("epyc " + m_epyc_model.group(1))
    for pattern in patterns:
        if model:
            break
        m = re.search(pattern, low, flags=re.IGNORECASE)
        if m and m.group(1):
            model = _normalize_compact_name(m.group(1))
            break
    if model.startswith("pentiumgold"):
        model = "pentium" + model[len("pentiumgold"):]
    return brand, model


def _cpu_article_code(text):
    raw = str(text or "").strip()
    if not raw:
        return ""
    for m in re.finditer(r"\(([A-Za-z0-9][A-Za-z0-9.\-/]{5,40})\)", raw):
        token = str(m.group(1) or "").strip()
        norm = _normalize_compact_name(token)
        if len(norm) < 8:
            continue
        if not any(ch.isalpha() for ch in norm) or not any(ch.isdigit() for ch in norm):
            continue
        if re.match(r"^\d{2,4}$", norm):
            continue
        # skip pure package markers
        if norm in {"oem", "box", "tray", "multipack"}:
            continue
        return norm
    return ""


def _cpu_package_type(text):
    raw = str(text or "").strip().lower()
    if not raw:
        return ""
    if re.search(r"(?:^|[^a-z0-9])(box|boxed)(?=$|[^a-z0-9])", raw, flags=re.IGNORECASE):
        return "box"
    if re.search(r"(?:^|[^a-z0-9])(oem|tray)(?=$|[^a-z0-9])", raw, flags=re.IGNORECASE):
        return "oem"
    return ""


def _looks_like_cpu_name(text):
    raw = str(text or "").strip()
    if not raw:
        return False
    low = raw.lower()
    if re.match(r"^\s*процессор\b", low, flags=re.IGNORECASE):
        return True
    cpu_markers = [
        r"(?:^|[^a-z0-9])intel(?=$|[^a-z0-9])",
        r"(?:^|[^a-z0-9])amd(?=$|[^a-z0-9])",
        r"(?:^|[^a-z0-9])ryzen(?=$|[^a-z0-9])",
        r"(?:^|[^a-z0-9])epyc(?=$|[^a-z0-9])",
        r"(?:^|[^a-z0-9])xeon(?=$|[^a-z0-9])",
        r"(?:^|[^a-z0-9])pentium(?=$|[^a-z0-9])",
        r"(?:^|[^a-z0-9])celeron(?=$|[^a-z0-9])",
        r"(?:^|[^a-z0-9])athlon(?=$|[^a-z0-9])",
        r"(?:^|[^a-z0-9])core\s*ultra(?=$|[^a-z0-9])",
        r"socket[-\s]?\d{3,5}",
    ]
    if any(re.search(p, low, flags=re.IGNORECASE) for p in cpu_markers):
        # avoid false positives from whole PCs
        if re.search(r"\bпэвм\b|\bсистемный блок\b|\bкомпьютер\b", low, flags=re.IGNORECASE):
            return False
        return True
    return False


def _find_cpu_review_candidates(product_name, top_n=5):
    name = str(product_name or "").strip()
    if not name:
        return []
    local_brand, local_model = _cpu_brand_model_key(name)
    if not local_brand or not local_model:
        return []
    local_package = _cpu_package_type(name)
    local_code = _cpu_article_code(name)
    pool = []
    try:
        with _db_connection() as conn:
            rows = []
            if local_model:
                rows.extend(conn.execute(
                    "SELECT ni.onliner_id, ni.raw_name, oc.url "
                    "FROM name_index ni "
                    "JOIN onliner_catalog oc ON oc.onliner_id = ni.onliner_id "
                    "WHERE replace(replace(replace(replace(lower(ni.raw_name), '-', ''), ' ', ''), '/', ''), '.', '') LIKE ? "
                    "LIMIT 120",
                    (f"%{local_model}%",),
                ).fetchall())
            if local_code and len(rows) < 80:
                rows.extend(conn.execute(
                    "SELECT ni.onliner_id, ni.raw_name, oc.url "
                    "FROM name_index ni "
                    "JOIN onliner_catalog oc ON oc.onliner_id = ni.onliner_id "
                    "WHERE replace(replace(replace(replace(lower(ni.raw_name), '-', ''), ' ', ''), '/', ''), '.', '') LIKE ? "
                    "LIMIT 120",
                    (f"%{local_code}%",),
                ).fetchall())
            seen_seed = set()
            for oid, raw_name, url in rows:
                oid = normalize_onliner_id(oid)
                if not oid or oid in seen_seed:
                    continue
                seen_seed.add(oid)
                pool.append({"id": oid, "name": raw_name, "url": url, "score": 0.93, "source": "cpu_db_seed"})
    except Exception:
        pass
    pool.extend(db_find_top_candidates(name, top_n=16, min_score=0.10, allow_b2b=False))
    exact = db_find_exact_id_for_name(name)
    if exact:
        pool = [exact] + list(pool or [])

    items = []
    seen = set()
    for cand in pool:
        if not isinstance(cand, dict):
            continue
        cid = normalize_onliner_id(cand.get("id", ""))
        cname = str(cand.get("name", "") or "").strip()
        if not cid or not cname or cid in seen:
            continue
        if normalize_catalog_category_name(infer_category(cname)) != "Процессор":
            continue
        cand_brand, cand_model = _cpu_brand_model_key(cname)
        if cand_brand != local_brand or cand_model != local_model:
            continue
        cand_package = _cpu_package_type(cname)
        package_delta = 0.0
        if local_package == "oem":
            if cand_package == "oem":
                package_delta = 0.03
            elif cand_package == "box":
                package_delta = -0.12
            else:
                package_delta = 0.01
        elif local_package == "box":
            if cand_package == "box":
                package_delta = 0.03
            elif cand_package == "oem":
                package_delta = -0.12
            else:
                package_delta = 0.01
        else:
            if cand_package:
                package_delta = 0.005
        base_score = max(float(cand.get("score", 0.0) or 0.0), 0.94)
        final_score = round(min(0.999, max(0.0, base_score + package_delta)), 3)
        seen.add(cid)
        items.append({
            "id": cid,
            "name": cname,
            "url": str(cand.get("url", "") or "").strip(),
            "score": final_score,
            "source": str(cand.get("source", "cpu_db")).strip() or "cpu_db",
            "package": cand_package,
        })
    items.sort(key=lambda item: (
        0 if local_package and item.get("package") == local_package else 1,
        0 if local_package == "oem" and item.get("package") == "" else 1,
        -(float(item.get("score", 0.0) or 0.0)),
        str(item.get("name", "") or "").lower(),
    ))
    return items[:max(1, int(top_n))]


def _board_brand_model_key(text):
    raw = str(text or "").strip()
    if not raw:
        return {
            "brand": "",
            "model": "",
            "model_text": "",
            "chipset": "",
            "socket": "",
            "ddr": "",
            "wifi": None,
            "features": set(),
        }
    cleaned = re.sub(r"^\s*MB\s+", "", raw, flags=re.IGNORECASE).strip()
    low = cleaned.lower()
    brand = ""
    brand_patterns = [
        ("asrock", r"\basrock\b"),
        ("gigabyte", r"\bgigabyte\b"),
        ("asus", r"\basus\b"),
        ("msi", r"\bmsi\b"),
        ("biostar", r"\bbiostar\b"),
        ("colorful", r"\bcolorful\b"),
        ("maxsun", r"\bmaxsun\b"),
    ]
    for value, pattern in brand_patterns:
        if re.search(pattern, low, flags=re.IGNORECASE):
            brand = value
            break

    socket = ""
    m_socket = re.search(r"\b(?:soc|socket)[-\s]?([a-z0-9-]+)\b", low, flags=re.IGNORECASE)
    if m_socket and m_socket.group(1):
        socket = re.sub(r"[^a-z0-9]+", "", m_socket.group(1).lower())

    chipset = ""
    m_chip = re.search(r"\(([a-z0-9-]{2,12})\)", low, flags=re.IGNORECASE)
    if m_chip and m_chip.group(1):
        chip_candidate = re.sub(r"[^a-z0-9]+", "", m_chip.group(1).lower())
        if re.match(r"^[a-z]\d{2,4}[a-z0-9]*$", chip_candidate):
            chipset = chip_candidate

    ddr = ""
    m_ddr = re.search(r"\bddr\s*([345])\b", low, flags=re.IGNORECASE)
    if m_ddr and m_ddr.group(1):
        ddr = f"ddr{m_ddr.group(1)}"

    model = ""
    model_text = ""
    if brand:
        model_text = re.split(r"\b(?:soc|socket)[-\s]?[a-z0-9-]+\b", cleaned, maxsplit=1, flags=re.IGNORECASE)[0]
        model_text = re.sub(rf"^\s*{re.escape(brand)}\s+", "", model_text, flags=re.IGNORECASE).strip()
        model_text = re.sub(r"\([^)]+\)\s*$", "", model_text).strip()
        model_text = re.sub(r"\s+", " ", model_text)
        model = re.sub(r"[^a-z0-9]+", "", model_text.lower())

    model_feature_source = model_text.lower()
    wifi = bool(
        re.search(r"(?:^|[^a-z0-9])wi[\s-]?fi(?=$|[^a-z0-9])", model_feature_source, flags=re.IGNORECASE)
        or re.search(r"(?:^|[^a-z0-9])ax(?=$|[^a-z0-9])", model_feature_source, flags=re.IGNORECASE)
    )

    feature_patterns = [
        ("d4", r"(?:^|[^a-z0-9])d4(?=$|[^a-z0-9])"),
        ("ax", r"(?:^|[^a-z0-9])ax(?=$|[^a-z0-9])"),
        ("wifi", r"wi[\s-]?fi"),
        ("eagle", r"(?:^|[^a-z0-9])eagle(?=$|[^a-z0-9])"),
        ("aorus", r"(?:^|[^a-z0-9])aorus(?=$|[^a-z0-9])"),
        ("gamingx", r"gaming\s*x"),
        ("steellegend", r"steel\s*legend"),
        ("prors", r"pro\s*rs"),
        ("lightning", r"(?:^|[^a-z0-9])lightning(?=$|[^a-z0-9])"),
        ("livemixer", r"live\s*mixer"),
        ("ds3h", r"(?:^|[^a-z0-9])ds3h(?=$|[^a-z0-9])"),
        ("d3hp", r"(?:^|[^a-z0-9])d3hp(?=$|[^a-z0-9])"),
        ("hdv", r"(?:^|[^a-z0-9])hdv(?=$|[^a-z0-9])"),
        ("elite", r"(?:^|[^a-z0-9])elite(?=$|[^a-z0-9])"),
        ("ud", r"(?:^|[^a-z0-9])ud(?=$|[^a-z0-9])"),
        ("riptide", r"(?:^|[^a-z0-9])riptide(?=$|[^a-z0-9])"),
        ("tomahawk", r"(?:^|[^a-z0-9])tomahawk(?=$|[^a-z0-9])"),
        ("mortar", r"(?:^|[^a-z0-9])mortar(?=$|[^a-z0-9])"),
        ("strix", r"(?:^|[^a-z0-9])strix(?=$|[^a-z0-9])"),
        ("prime", r"(?:^|[^a-z0-9])prime(?=$|[^a-z0-9])"),
    ]
    features = set()
    feature_source = model_feature_source
    for feature_key, pattern in feature_patterns:
        if re.search(pattern, feature_source, flags=re.IGNORECASE):
            features.add(feature_key)

    return {
        "brand": brand,
        "model": model,
        "model_text": model_text.upper(),
        "chipset": chipset,
        "socket": socket,
        "ddr": ddr,
        "wifi": wifi,
        "features": features,
    }


def _find_board_review_candidates(product_name, top_n=5):
    name = str(product_name or "").strip()
    if not name:
        return []
    local = _board_brand_model_key(name)
    local_brand = local.get("brand", "")
    local_model = local.get("model", "")
    if not local_brand or not local_model:
        return []

    pool = db_find_top_candidates(name, top_n=15, min_score=0.10, allow_b2b=False)
    exact = db_find_exact_id_for_name(name)
    if exact:
        pool = [exact] + list(pool or [])

    items = []
    seen = set()
    for cand in pool:
        if not isinstance(cand, dict):
            continue
        cid = normalize_onliner_id(cand.get("id", ""))
        cname = str(cand.get("name", "") or "").strip()
        if not cid or not cname or cid in seen:
            continue
        if normalize_catalog_category_name(infer_category(cname)) != "Материнская плата":
            continue
        cand_board = _board_brand_model_key(cname)
        if cand_board.get("brand") != local_brand:
            continue
        cand_model = cand_board.get("model", "")
        model_exact = cand_model == local_model
        model_close = bool(cand_model and local_model and (cand_model in local_model or local_model in cand_model))
        if not model_exact and not model_close:
            continue

        score = max(float(cand.get("score", 0.0) or 0.0), 0.90)
        if model_exact:
            score += 0.06
        elif model_close:
            score += 0.03
        if local.get("chipset") and cand_board.get("chipset") == local.get("chipset"):
            score += 0.03
        elif local.get("chipset") and cand_board.get("chipset") and cand_board.get("chipset") != local.get("chipset"):
            score -= 0.08
        if local.get("socket") and cand_board.get("socket") == local.get("socket"):
            score += 0.02
        elif local.get("socket") and cand_board.get("socket") and cand_board.get("socket") != local.get("socket"):
            score -= 0.08
        if local.get("ddr") and cand_board.get("ddr") == local.get("ddr"):
            score += 0.03
        elif local.get("ddr") and cand_board.get("ddr") and cand_board.get("ddr") != local.get("ddr"):
            score -= 0.12
        local_wifi = bool(local.get("wifi"))
        cand_wifi = bool(cand_board.get("wifi"))
        if local_wifi != cand_wifi:
            continue
        if local_wifi and cand_wifi:
            score += 0.03

        local_features = set(local.get("features") or set())
        cand_features = set(cand_board.get("features") or set())
        if local_features or cand_features:
            shared_features = local_features & cand_features
            missing_features = local_features - cand_features
            extra_features = cand_features - local_features
            score += min(0.04, 0.012 * len(shared_features))
            score -= min(0.14, 0.035 * len(missing_features))
            score -= min(0.06, 0.015 * len(extra_features - {"wifi"}))

        # If the local board explicitly has a specific suffix/family token, don't trust "close" model too much.
        if model_close and not model_exact and local_features:
            strong_family = {"d4", "ax", "aorus", "eagle", "steellegend", "prors", "lightning", "livemixer", "ds3h", "d3hp", "hdv", "elite", "ud", "riptide", "tomahawk", "mortar", "strix", "prime", "gamingx"}
            if (local_features & strong_family) - cand_features:
                score -= 0.10

        seen.add(cid)
        items.append({
            "id": cid,
            "name": cname,
            "url": str(cand.get("url", "") or "").strip(),
            "score": round(min(0.999, max(0.0, score)), 3),
            "source": str(cand.get("source", "mb_db")).strip() or "mb_db",
            "chipset": cand_board.get("chipset", ""),
            "socket": cand_board.get("socket", ""),
            "ddr": cand_board.get("ddr", ""),
            "wifi": cand_board.get("wifi"),
            "features": sorted(cand_features),
        })

    items.sort(key=lambda item: (
        -(float(item.get("score", 0.0) or 0.0)),
        str(item.get("name", "") or "").lower(),
    ))
    local_sku = str(local.get("sku", "") or "").strip()
    if local_sku:
        exact_items = [item for item in items if str(item.get("sku", "") or "").strip() == local_sku]
        if exact_items:
            items = exact_items
    return items[:max(1, int(top_n))]


def _monitor_brand_model_key(text):
    raw = str(text or "").strip()
    if not raw:
        return {
            "brand": "",
            "model": "",
            "model_text": "",
            "code": "",
            "size": "",
            "resolution": "",
            "hz": "",
            "white": False,
        }
    cleaned = raw.replace("″", '"').replace("“", '"').replace("”", '"').strip()
    low = cleaned.lower()
    size = ""
    m_size = re.match(r'^\s*(\d{2}(?:\.\d)?)\s*"', cleaned)
    if m_size and m_size.group(1):
        size = m_size.group(1)

    brand = ""
    brand_patterns = [
        ("elsa", r"(?:^|[^a-z0-9])elsa(?=$|[^a-z0-9])"),
        ("lg", r"(?:^|[^a-z0-9])lg(?=$|[^a-z0-9])"),
        ("xiaomi", r"(?:^|[^a-z0-9])xiaomi(?=$|[^a-z0-9])"),
        ("asrock", r"(?:^|[^a-z0-9])asrock(?=$|[^a-z0-9])"),
        ("gigabyte", r"(?:^|[^a-z0-9])gigabyte(?=$|[^a-z0-9])"),
        ("msi", r"(?:^|[^a-z0-9])msi(?=$|[^a-z0-9])"),
        ("asus", r"(?:^|[^a-z0-9])asus(?=$|[^a-z0-9])"),
        ("aoc", r"(?:^|[^a-z0-9])aoc(?=$|[^a-z0-9])"),
        ("acer", r"(?:^|[^a-z0-9])acer(?=$|[^a-z0-9])"),
        ("benq", r"(?:^|[^a-z0-9])benq(?=$|[^a-z0-9])"),
        ("philips", r"(?:^|[^a-z0-9])philips(?=$|[^a-z0-9])"),
        ("viewsonic", r"(?:^|[^a-z0-9])viewsonic(?=$|[^a-z0-9])"),
        ("samsung", r"(?:^|[^a-z0-9])samsung(?=$|[^a-z0-9])"),
        ("dell", r"(?:^|[^a-z0-9])dell(?=$|[^a-z0-9])"),
        ("hp", r"(?:^|[^a-z0-9])hp(?=$|[^a-z0-9])"),
    ]
    for value, pattern in brand_patterns:
        if re.search(pattern, low, flags=re.IGNORECASE):
            brand = value
            break

    after_brand = cleaned
    # Remove leading descriptor words often present in DB names.
    after_brand = re.sub(r'^\s*(игровой\s+)?монитор\s+', '', after_brand, flags=re.IGNORECASE).strip()
    if size:
        after_brand = re.sub(r'^\s*\d{2}(?:\.\d)?\s*"\s*', '', after_brand, flags=re.IGNORECASE).strip()
    if brand:
        after_brand = re.sub(rf'^\s*{re.escape(brand)}\s+', '', after_brand, flags=re.IGNORECASE).strip()

    resolution = ""
    m_res = re.search(r'(\d{3,4}\s*x\s*\d{3,4})', cleaned, flags=re.IGNORECASE)
    if m_res and m_res.group(1):
        resolution = re.sub(r"\s+", "", m_res.group(1).lower())

    hz = ""
    m_hz = re.search(r'(\d{2,3})\s*(?:гц|hz)\b', low, flags=re.IGNORECASE)
    if m_hz and m_hz.group(1):
        hz = m_hz.group(1)

    white = bool(re.search(r'(?:^|[^a-z0-9])white(?=$|[^a-z0-9])|бел', low, flags=re.IGNORECASE))

    model_text = re.split(r'\s*\(', after_brand, maxsplit=1)[0].strip()
    model_text = re.sub(r"\s+", " ", model_text)
    model = re.sub(r"[^a-z0-9]+", "", model_text.lower())
    code = ""
    # Prefer vendor article in parentheses: (P27UCB-RAGL), (ELA5444EU)
    for m_code in re.finditer(r"\(([A-Za-z0-9][A-Za-z0-9\-]{4,40})\)", cleaned):
        tok = str(m_code.group(1) or "").strip()
        norm = re.sub(r"[^a-z0-9]+", "", tok.lower())
        if len(norm) >= 6 and any(ch.isalpha() for ch in norm) and any(ch.isdigit() for ch in norm):
            code = norm
            break
    if not code:
        m_inline_code = re.search(r"\b((?:p|ela)\d[A-Za-z0-9\-]{4,32})\b", cleaned, flags=re.IGNORECASE)
        if m_inline_code and m_inline_code.group(1):
            code = re.sub(r"[^a-z0-9]+", "", m_inline_code.group(1).lower())

    return {
        "brand": brand,
        "model": model,
        "model_text": model_text.upper(),
        "code": code,
        "size": size,
        "resolution": resolution,
        "hz": hz,
        "white": white,
    }


def _find_monitor_review_candidates(product_name, top_n=5):
    name = str(product_name or "").strip()
    if not name:
        return []
    local = _monitor_brand_model_key(name)
    local_brand = local.get("brand", "")
    local_model = local.get("model", "")
    local_code = str(local.get("code", "") or "").strip()
    if not local_brand or not local_model:
        return []

    pool = []
    try:
        with _db_connection() as conn:
            rows = []
            model_text = str(local.get("model_text", "") or "").strip()
            brand_sql = local_brand
            if local_code:
                rows.extend(conn.execute(
                    "SELECT ni.onliner_id, ni.raw_name, oc.url "
                    "FROM name_index ni "
                    "JOIN onliner_catalog oc ON oc.onliner_id = ni.onliner_id "
                    "WHERE replace(replace(replace(replace(lower(ni.raw_name), '-', ''), ' ', ''), '/', ''), '.', '') LIKE ? "
                    "LIMIT 120",
                    (f"%{local_code}%",),
                ).fetchall())
            if brand_sql and model_text:
                rows.extend(conn.execute(
                    "SELECT ni.onliner_id, ni.raw_name, oc.url "
                    "FROM name_index ni "
                    "JOIN onliner_catalog oc ON oc.onliner_id = ni.onliner_id "
                    "WHERE lower(ni.raw_name) LIKE ? AND lower(ni.raw_name) LIKE ? "
                    "LIMIT 120",
                    (f"%{brand_sql.lower()}%", f"%{model_text.lower()}%"),
                ).fetchall())
            compact_model = str(local.get("model", "") or "").strip()
            if compact_model and len(rows) < 20:
                tail = re.sub(r"[^a-z0-9]+", "", model_text.lower())
                model_token = model_text.lower()
                rows.extend(conn.execute(
                    "SELECT ni.onliner_id, ni.raw_name, oc.url "
                    "FROM name_index ni "
                    "JOIN onliner_catalog oc ON oc.onliner_id = ni.onliner_id "
                    "WHERE lower(ni.raw_name) LIKE ? "
                    "LIMIT 120",
                    (f"%{model_token}%",),
                ).fetchall())
                if tail and tail != model_token:
                    rows.extend(conn.execute(
                        "SELECT ni.onliner_id, ni.raw_name, oc.url "
                        "FROM name_index ni "
                        "JOIN onliner_catalog oc ON oc.onliner_id = ni.onliner_id "
                        "WHERE replace(replace(replace(lower(ni.raw_name), '-', ''), ' ', ''), '\"', '') LIKE ? "
                        "LIMIT 120",
                        (f"%{tail}%",),
                    ).fetchall())
            seen_seed = set()
            for oid, raw_name, url in rows:
                oid = normalize_onliner_id(oid)
                if not oid or oid in seen_seed:
                    continue
                seen_seed.add(oid)
                pool.append({"id": oid, "name": raw_name, "url": url, "score": 0.91, "source": "mon_db_exact"})
    except Exception:
        pass

    for cand in db_find_top_candidates(name, top_n=15, min_score=0.10, allow_b2b=False):
        pool.append(cand)
    exact = db_find_exact_id_for_name(name)
    if exact:
        pool = [exact] + list(pool or [])

    items = []
    seen = set()
    for cand in pool:
        if not isinstance(cand, dict):
            continue
        cid = normalize_onliner_id(cand.get("id", ""))
        cname = str(cand.get("name", "") or "").strip()
        if not cid or not cname or cid in seen:
            continue
        if normalize_catalog_category_name(infer_category(cname)) != "Монитор":
            continue
        cand_mon = _monitor_brand_model_key(cname)
        if cand_mon.get("brand") != local_brand:
            continue
        cand_model = cand_mon.get("model", "")
        cand_code = str(cand_mon.get("code", "") or "").strip()
        code_exact = bool(local_code and cand_code and cand_code == local_code)
        model_exact = cand_model == local_model
        model_close = bool(cand_model and local_model and (cand_model in local_model or local_model in cand_model))
        if not code_exact and not model_exact and not model_close:
            continue

        score = max(float(cand.get("score", 0.0) or 0.0), 0.90)
        if code_exact:
            score = max(score, 0.97)
            score += 0.05
        if model_exact:
            score += 0.07
        elif model_close:
            score += 0.04
        if local.get("size") and cand_mon.get("size") == local.get("size"):
            score += 0.03
        elif local.get("size") and cand_mon.get("size") and cand_mon.get("size") != local.get("size"):
            score -= 0.10
        if local.get("resolution") and cand_mon.get("resolution") == local.get("resolution"):
            score += 0.03
        elif local.get("resolution") and cand_mon.get("resolution") and cand_mon.get("resolution") != local.get("resolution"):
            score -= 0.10
        if local.get("hz") and cand_mon.get("hz") == local.get("hz"):
            score += 0.03
        elif local.get("hz") and cand_mon.get("hz"):
            try:
                diff_hz = abs(int(local.get("hz")) - int(cand_mon.get("hz")))
            except Exception:
                diff_hz = 999
            score += 0.005 if diff_hz <= 20 else -0.07
        if bool(local.get("white")) != bool(cand_mon.get("white")):
            score -= 0.05

        seen.add(cid)
        items.append({
            "id": cid,
            "name": cname,
            "url": str(cand.get("url", "") or "").strip(),
            "score": round(min(0.999, max(0.0, score)), 3),
            "source": str(cand.get("source", "mon_db")).strip() or "mon_db",
            "code": cand_code,
            "size": cand_mon.get("size", ""),
            "resolution": cand_mon.get("resolution", ""),
            "hz": cand_mon.get("hz", ""),
            "white": cand_mon.get("white"),
        })

    items.sort(key=lambda item: (
        -(float(item.get("score", 0.0) or 0.0)),
        str(item.get("name", "") or "").lower(),
    ))
    return items[:max(1, int(top_n))]


def _gpu_brand_model_key(text):
    raw = str(text or "").strip()
    if not raw:
        return {
            "gpu_brand": "",
            "vendor": "",
            "gpu_model": "",
            "series": "",
            "sku": "",
            "memory_gb": "",
            "white": False,
            "oc": False,
        }
    low = raw.lower()
    gpu_brand = "nvidia" if "geforce" in low or "rtx" in low or "gtx" in low else ("amd" if "radeon" in low or re.search(r"(?:^|[^a-z0-9])rx\s*\d{3,4}", low) else "")
    vendor = ""
    vendor_patterns = [
        ("gigabyte", r"(?:^|[^a-z0-9])gigabyte(?=$|[^a-z0-9])"),
        ("sapphire", r"(?:^|[^a-z0-9])sapphire(?=$|[^a-z0-9])"),
        ("asus", r"(?:^|[^a-z0-9])asus(?=$|[^a-z0-9])"),
        ("msi", r"(?:^|[^a-z0-9])msi(?=$|[^a-z0-9])"),
        ("palit", r"(?:^|[^a-z0-9])palit(?=$|[^a-z0-9])"),
        ("gainward", r"(?:^|[^a-z0-9])gainward(?=$|[^a-z0-9])"),
        ("zotac", r"(?:^|[^a-z0-9])zotac(?=$|[^a-z0-9])"),
        ("inno3d", r"(?:^|[^a-z0-9])inno3d(?=$|[^a-z0-9])"),
        ("ocpc", r"(?:^|[^a-z0-9])ocpc(?=$|[^a-z0-9])"),
        ("colorful", r"(?:^|[^a-z0-9])colorful(?=$|[^a-z0-9])"),
    ]
    for value, pattern in vendor_patterns:
        if re.search(pattern, low, flags=re.IGNORECASE):
            vendor = value
            break

    gpu_model = ""
    patterns = [
        r"(rtx\s*\d{4}(?:\s*ti)?)",
        r"(gtx\s*\d{3,4}(?:\s*ti)?)",
        r"(gt\s*\d{3,4})",
        r"(rx\s*\d{3,4}\s*xt?)",
        r"(rx\s*\d{3,4})",
    ]
    for pattern in patterns:
        m = re.search(pattern, low, flags=re.IGNORECASE)
        if m and m.group(1):
            gpu_model = re.sub(r"[^a-z0-9]+", "", m.group(1).lower())
            break

    sku = ""
    m_sku = re.search(r"\(([A-Za-z0-9+\- ]{6,40})\)", raw)
    if m_sku and m_sku.group(1):
        sku = re.sub(r"[^a-z0-9]+", "", m_sku.group(1).lower())
    if not sku:
        sku_patterns = [
            r"(gv-[a-z0-9+\- ]{6,40})",
            r"(ne[a-z0-9+\-]{8,40})",
            r"(zt-[a-z0-9+\-]{6,40})",
            r"(ocvn[a-z0-9+\-]{6,40})",
            r"(113\d{2}-\d{2}-\d{2}g)",
        ]
        for pattern in sku_patterns:
            m2 = re.search(pattern, low, flags=re.IGNORECASE)
            if m2 and m2.group(1):
                sku = re.sub(r"[^a-z0-9]+", "", m2.group(1).lower())
                break

    memory_gb = ""
    m_mem = re.search(r"(\d{1,2})\s*gb\b", low, flags=re.IGNORECASE)
    if m_mem and m_mem.group(1):
        memory_gb = m_mem.group(1)

    white = bool(re.search(r"(?:^|[^a-z0-9])white(?=$|[^a-z0-9])|\bбел(?:ый|ая|ое|ые)?\b", low, flags=re.IGNORECASE))
    oc = bool(re.search(r"(?:^|[^a-z0-9])oc(?=$|[^a-z0-9])", low, flags=re.IGNORECASE))

    series_tokens = [
        ("aoruselite", r"aorus\s*elite"),
        ("aero", r"(?:^|[^a-z0-9])aero(?=$|[^a-z0-9])"),
        ("eaglemax", r"eagle\s*max"),
        ("eagleocice", r"eagle\s*oc\s*ice"),
        ("eagleoc", r"eagle\s*oc"),
        ("eagleice", r"eagle\s*ice"),
        ("eagle", r"(?:^|[^a-z0-9])eagle(?=$|[^a-z0-9])"),
        ("windforcemax", r"windforce\s*max"),
        ("windforceoc", r"windforce\s*oc"),
        ("windforce", r"windforce"),
        ("gamingocice", r"gaming\s*oc\s*ice"),
        ("gamingoc", r"gaming\s*oc"),
        ("gaming", r"(?:^|[^a-z0-9])gaming(?=$|[^a-z0-9])"),
        ("pulseoc", r"pulse\s*oc"),
        ("pulse", r"(?:^|[^a-z0-9])pulse(?=$|[^a-z0-9])"),
        ("pure", r"(?:^|[^a-z0-9])pure(?=$|[^a-z0-9])"),
        ("nitro", r"nitro\+?"),
        ("dualoc", r"dual\s*oc"),
        ("dual", r"(?:^|[^a-z0-9])dual(?=$|[^a-z0-9])"),
        ("stormxoc", r"stormx\s*oc"),
        ("stormx", r"(?:^|[^a-z0-9])stormx(?=$|[^a-z0-9])"),
        ("infinity3oc", r"infinity\s*3\s*oc"),
        ("infinity3", r"infinity\s*3"),
        ("infinity2oc", r"infinity\s*2\s*oc"),
        ("infinity2", r"infinity\s*2"),
        ("gamingprosoc", r"gamingpro-s\s*oc|gaming\s*pro-s\s*oc"),
        ("gamingpros", r"gamingpro-s|gaming\s*pro-s"),
        ("gamingprooc", r"gamingpro\s*oc|gaming\s*pro\s*oc"),
        ("gamingpro", r"gamingpro|gaming\s*pro"),
        ("zoneedition", r"zone\s*edition"),
        ("ventus", r"(?:^|[^a-z0-9])ventus(?=$|[^a-z0-9])"),
    ]
    series = ""
    for key, pattern in series_tokens:
        if re.search(pattern, low, flags=re.IGNORECASE):
            series = key
            break

    return {
        "gpu_brand": gpu_brand,
        "vendor": vendor,
        "gpu_model": gpu_model,
        "series": series,
        "sku": sku,
        "memory_gb": memory_gb,
        "white": white,
        "oc": oc,
    }


def _find_gpu_review_candidates(product_name, top_n=5):
    name = str(product_name or "").strip()
    if not name:
        return []
    local = _gpu_brand_model_key(name)
    if not local.get("vendor") or not local.get("gpu_model"):
        return []

    pool = []
    try:
        with _db_connection() as conn:
            rows = []
            model_query = local.get("gpu_model", "")
            vendor_query = local.get("vendor", "")
            sku_query = str(local.get("sku", "") or "").strip()
            if vendor_query and model_query:
                spaced_model = re.sub(r"([a-z]+)(\d)", r"\1 \2", model_query)
                if sku_query:
                    rows.extend(conn.execute(
                        "SELECT ni.onliner_id, ni.raw_name, oc.url "
                        "FROM name_index ni "
                        "JOIN onliner_catalog oc ON oc.onliner_id = ni.onliner_id "
                        "WHERE replace(replace(replace(replace(lower(ni.raw_name), '-', ''), ' ', ''), '+', ''), '(', '') LIKE ? "
                        "LIMIT 80",
                        (f"%{sku_query.replace('+','')}%",),
                    ).fetchall())
                rows.extend(conn.execute(
                    "SELECT ni.onliner_id, ni.raw_name, oc.url "
                    "FROM name_index ni "
                    "JOIN onliner_catalog oc ON oc.onliner_id = ni.onliner_id "
                    "WHERE lower(ni.raw_name) LIKE ? AND lower(ni.raw_name) LIKE ? "
                    "LIMIT 160",
                    (f"%{vendor_query.lower()}%", f"%{spaced_model.lower()}%"),
                ).fetchall())
                rows.extend(conn.execute(
                    "SELECT ni.onliner_id, ni.raw_name, oc.url "
                    "FROM name_index ni "
                    "JOIN onliner_catalog oc ON oc.onliner_id = ni.onliner_id "
                    "WHERE replace(replace(replace(lower(ni.raw_name), '-', ''), ' ', ''), '+', '') LIKE ? "
                    "LIMIT 160",
                    (f"%{model_query.replace('+','')}%",),
                ).fetchall())
            seen_seed = set()
            for oid, raw_name, url in rows:
                oid = normalize_onliner_id(oid)
                if not oid or oid in seen_seed:
                    continue
                seen_seed.add(oid)
                pool.append({"id": oid, "name": raw_name, "url": url, "score": 0.91, "source": "gpu_db_exact"})
    except Exception:
        pass

    for cand in db_find_top_candidates(name, top_n=18, min_score=0.10, allow_b2b=False):
        pool.append(cand)

    exact = db_find_exact_id_for_name(name)
    if exact:
        pool = [exact] + list(pool or [])

    items = []
    seen = set()
    for cand in pool:
        if not isinstance(cand, dict):
            continue
        cid = normalize_onliner_id(cand.get("id", ""))
        cname = str(cand.get("name", "") or "").strip()
        if not cid or not cname or cid in seen:
            continue
        if normalize_catalog_category_name(infer_category(cname)) != "Видеокарта":
            continue
        cand_gpu = _gpu_brand_model_key(cname)
        if cand_gpu.get("vendor") != local.get("vendor"):
            continue
        if cand_gpu.get("gpu_brand") != local.get("gpu_brand"):
            continue
        if cand_gpu.get("gpu_model") != local.get("gpu_model"):
            continue

        score = max(float(cand.get("score", 0.0) or 0.0), 0.90)
        local_sku = str(local.get("sku", "") or "").strip()
        cand_sku = str(cand_gpu.get("sku", "") or "").strip()
        if local_sku:
            if cand_sku == local_sku:
                score = 1.0
            elif cand_sku:
                # For GPU vendors with reliable bracket codes (especially Gigabyte), a different SKU is effectively a different card.
                if local.get("vendor") == "gigabyte":
                    continue
                score -= 0.14
        if local.get("series") and cand_gpu.get("series") == local.get("series"):
            score += 0.05
        elif local.get("series") and cand_gpu.get("series") and cand_gpu.get("series") != local.get("series"):
            score -= 0.10
        if local.get("memory_gb") and cand_gpu.get("memory_gb") == local.get("memory_gb"):
            score += 0.04
        elif local.get("memory_gb") and cand_gpu.get("memory_gb") and cand_gpu.get("memory_gb") != local.get("memory_gb"):
            score -= 0.12
        if local.get("white") != cand_gpu.get("white"):
            score -= 0.06
        if local.get("oc") and cand_gpu.get("oc"):
            score += 0.015
        elif local.get("oc") != cand_gpu.get("oc"):
            score -= 0.04
        if local.get("sku") and cand_gpu.get("sku") == local.get("sku"):
            score += 0.06
        elif local.get("sku") and cand_gpu.get("sku") and local.get("sku") != cand_gpu.get("sku"):
            score -= 0.08

        seen.add(cid)
        items.append({
            "id": cid,
            "name": cname,
            "url": str(cand.get("url", "") or "").strip(),
            "score": round(min(0.999, max(0.0, score)), 3),
            "source": str(cand.get("source", "gpu_db")).strip() or "gpu_db",
            "series": cand_gpu.get("series", ""),
            "sku": cand_gpu.get("sku", ""),
            "memory_gb": cand_gpu.get("memory_gb", ""),
            "white": cand_gpu.get("white"),
        })

    items.sort(key=lambda item: (
        -(float(item.get("score", 0.0) or 0.0)),
        str(item.get("name", "") or "").lower(),
    ))
    return items[:max(1, int(top_n))]


def _ram_brand_model_key(text):
    raw = str(text or "").strip()
    if not raw:
        return {
            "ddr": "",
            "brand": "",
            "sku": "",
            "capacity_gb": "",
            "kit_modules": "",
            "mhz": "",
            "cl": "",
            "series": "",
            "white": False,
            "rgb": False,
            "ecc": False,
            "reg": False,
        }
    low = raw.lower()
    ddr = ""
    m_ddr = re.search(r"\bddr\s*([345]|iii|iv|v)\b", low, flags=re.IGNORECASE)
    if m_ddr and m_ddr.group(1):
        token = str(m_ddr.group(1) or "").lower()
        if token in {"v", "5"}:
            ddr = "ddr5"
        elif token in {"iv", "4"}:
            ddr = "ddr4"
        elif token in {"iii", "3"}:
            ddr = "ddr3"

    brand = ""
    brand_patterns = [
        ("kingston", r"(?:^|[^a-z0-9])kingston(?=$|[^a-z0-9])"),
        ("gskill", r"(?:^|[^a-z0-9])g\.?skill(?=$|[^a-z0-9])"),
        ("netac", r"(?:^|[^a-z0-9])netac(?=$|[^a-z0-9])"),
        ("team", r"(?:^|[^a-z0-9])team(?=$|[^a-z0-9])"),
        ("adata", r"(?:^|[^a-z0-9])a-?data|(?:^|[^a-z0-9])adata|(?:^|[^a-z0-9])xpg(?=$|[^a-z0-9])"),
        ("patriot", r"(?:^|[^a-z0-9])patriot(?=$|[^a-z0-9])"),
        ("corsair", r"(?:^|[^a-z0-9])corsair(?=$|[^a-z0-9])"),
        ("crucial", r"(?:^|[^a-z0-9])crucial(?=$|[^a-z0-9])"),
    ]
    for value, pattern in brand_patterns:
        if re.search(pattern, low, flags=re.IGNORECASE):
            brand = value
            break

    def _ram_norm_sku(value):
        return re.sub(r"[^a-z0-9]+", "", str(value or "").lower())

    def _is_strong_ram_sku(value):
        v = _ram_norm_sku(value)
        if len(v) < 8:
            return False
        if not any(ch.isalpha() for ch in v) or not any(ch.isdigit() for ch in v):
            return False
        blocked = {
            "ddr3", "ddr4", "ddr5", "rgb", "argb", "mhz", "cl",
            "kitof2", "kitof4", "intel", "amd", "oem", "box"
        }
        if v in blocked:
            return False
        return True

    sku = ""
    m_sku = re.search(r"\(([A-Za-z0-9+\-\/ ]{6,80})\)", raw)
    if m_sku and m_sku.group(1):
        sku = _ram_norm_sku(m_sku.group(1))
    if not sku:
        sku_patterns = [
            r"(KF[0-9A-Z\-\/]{7,})",
            r"(KSM[0-9A-Z\-\/]{6,})",
            r"(KVR[0-9A-Z\-\/]{6,})",
            r"(KCS[0-9A-Z\-\/]{6,})",
            r"(F[45]-[0-9A-Z\-\/]{8,})",
            r"(NT[SA-Z0-9\-\/]{8,})",
            r"(TT[CA-Z0-9\-\/]{8,})",
            r"(AX[0-9A-Z\-\/]{8,})",
            r"(PS[DPV][0-9A-Z\-\/]{7,})",
            r"(CM[A-Z0-9\-\/]{8,})",
            r"(PVV[A-Z0-9\-\/]{6,})",
            r"(PVE[A-Z0-9\-\/]{6,})",
            r"(VEU[A-Z0-9\-\/]{6,})",
        ]
        for pattern in sku_patterns:
            m_inline = re.search(pattern, raw, flags=re.IGNORECASE)
            if m_inline and m_inline.group(1):
                sku = _ram_norm_sku(m_inline.group(1))
                break
    if not sku:
        # Fallback: often RAM code is the last uppercase token (e.g. VEUR532G6028K, PVVR564G600C30K)
        for token in reversed(re.findall(r"\b([A-Za-z0-9\-\/]{8,24})\b", raw)):
            if _is_strong_ram_sku(token):
                sku = _ram_norm_sku(token)
                break

    capacity_gb = ""
    m_cap = re.search(r"(\d{1,3})\s*(?:g|г)\s*(?:b|б)\b", low, flags=re.IGNORECASE)
    if m_cap and m_cap.group(1):
        capacity_gb = m_cap.group(1)
    if not capacity_gb:
        m_cap = re.search(r"\b(\d{1,2})\s*[xх]\s*(\d{1,3})\s*(?:g|г)\s*(?:b|б)\b", low, flags=re.IGNORECASE)
        if m_cap and m_cap.group(1) and m_cap.group(2):
            try:
                capacity_gb = str(int(m_cap.group(1)) * int(m_cap.group(2)))
            except Exception:
                capacity_gb = ""

    kit_modules = ""
    m_kit = re.search(r"kitof\s*(\d+)|kit\s*[xх]\s*(\d+)|(\d+)\s*[xх]\s*\d+\s*(?:g|г)\s*(?:b|б)", low, flags=re.IGNORECASE)
    if m_kit:
        for grp in m_kit.groups():
            if grp:
                kit_modules = grp
                break

    mhz = ""
    m_mhz = re.search(r"(\d{4,5})\s*mhz", low, flags=re.IGNORECASE)
    if m_mhz and m_mhz.group(1):
        mhz = m_mhz.group(1)

    cl = ""
    m_cl = re.search(r"\bcl\s*([0-9]{2})\b", low, flags=re.IGNORECASE)
    if m_cl and m_cl.group(1):
        cl = m_cl.group(1)

    series_patterns = [
        ("furybeastrgb", r"fury\s*beast\s*rgb"),
        ("furybeast", r"fury\s*beast"),
        ("furyrenegade", r"fury\s*renegade"),
        ("tridentzneorgb", r"trident\s*z5\s*neo\s*rgb"),
        ("tridentz5rgb", r"trident\s*z5\s*rgb"),
        ("tridentzrgb", r"trident\s*z\s*rgb"),
        ("tridentz", r"trident\s*z"),
        ("flarex5", r"flare\s*x5"),
        ("ripjawsv", r"ripjaws\s*v"),
        ("ripjawsm5neo", r"ripjaws\s*m5\s*neo\s*rgb"),
        ("ripjawsm5", r"ripjaws\s*m5\s*rgb"),
        ("aegis", r"(?:^|[^a-z0-9])aegis(?=$|[^a-z0-9])"),
        ("shadowiii", r"shadow\s*iii"),
        ("shadowii", r"shadow\s*ii"),
        ("shadows", r"shadow\s*s"),
        ("tcreateexpert", r"t-?create\s*expert"),
        ("vengeancelpx", r"vengeance\s*lpx"),
        ("lancerbladergb", r"lancer\s*blade\s*rgb"),
        ("lancerneonrgb", r"lancer\s*neon\s*rgb"),
        ("basic", r"(?:^|[^a-z0-9])basic(?=$|[^a-z0-9])"),
        ("signatureline", r"signature\s*(premium\s*)?line"),
    ]
    series = ""
    for key, pattern in series_patterns:
        if re.search(pattern, low, flags=re.IGNORECASE):
            series = key
            break

    white = bool(re.search(r"(?:^|[^a-z0-9])white(?=$|[^a-z0-9])|\bбел(?:ый|ая|ое|ые)?\b", low, flags=re.IGNORECASE))
    rgb = bool(re.search(r"(?:^|[^a-z0-9])rgb(?=$|[^a-z0-9])", low, flags=re.IGNORECASE))
    ecc = bool(re.search(r"(?:^|[^a-z0-9])ecc(?=$|[^a-z0-9])", low, flags=re.IGNORECASE))
    reg = bool(re.search(r"registered|reg\b|rdimm|lrdimm", low, flags=re.IGNORECASE))

    return {
        "ddr": ddr,
        "brand": brand,
        "sku": sku,
        "capacity_gb": capacity_gb,
        "kit_modules": kit_modules,
        "mhz": mhz,
        "cl": cl,
        "series": series,
        "white": white,
        "rgb": rgb,
        "ecc": ecc,
        "reg": reg,
    }


def _find_ram_review_candidates(product_name, top_n=5):
    name = str(product_name or "").strip()
    if not name:
        return []
    local = _ram_brand_model_key(name)
    if not local.get("brand") or not local.get("ddr"):
        return []

    pool = []
    try:
        with _db_connection() as conn:
            rows = []
            sku_query = str(local.get("sku", "") or "").strip()
            brand_query = str(local.get("brand", "") or "").strip()
            if sku_query:
                rows.extend(conn.execute(
                    "SELECT ni.onliner_id, ni.raw_name, oc.url "
                    "FROM name_index ni "
                    "JOIN onliner_catalog oc ON oc.onliner_id = ni.onliner_id "
                    "WHERE replace(replace(replace(replace(lower(ni.raw_name), '-', ''), ' ', ''), '/', ''), '+', '') LIKE ? "
                    "LIMIT 120",
                    (f"%{sku_query}%",),
                ).fetchall())
            if brand_query and len(rows) < 20:
                rows.extend(conn.execute(
                    "SELECT ni.onliner_id, ni.raw_name, oc.url "
                    "FROM name_index ni "
                    "JOIN onliner_catalog oc ON oc.onliner_id = ni.onliner_id "
                    "WHERE lower(ni.raw_name) LIKE ? "
                    "LIMIT 120",
                    (f"%{brand_query.lower()}%",),
                ).fetchall())
            seen_seed = set()
            for oid, raw_name, url in rows:
                oid = normalize_onliner_id(oid)
                if not oid or oid in seen_seed:
                    continue
                seen_seed.add(oid)
                pool.append({"id": oid, "name": raw_name, "url": url, "score": 0.91, "source": "ram_db_exact"})
    except Exception:
        pass

    for cand in db_find_top_candidates(name, top_n=20, min_score=0.10, allow_b2b=False):
        pool.append(cand)

    exact = db_find_exact_id_for_name(name)
    if exact:
        pool = [exact] + list(pool or [])

    items = []
    seen = set()
    for cand in pool:
        if not isinstance(cand, dict):
            continue
        cid = normalize_onliner_id(cand.get("id", ""))
        cname = str(cand.get("name", "") or "").strip()
        if not cid or not cname or cid in seen:
            continue
        if normalize_catalog_category_name(infer_category(cname)) != "Оперативная память":
            continue
        cand_ram = _ram_brand_model_key(cname)
        if cand_ram.get("brand") != local.get("brand"):
            continue

        score = max(float(cand.get("score", 0.0) or 0.0), 0.90)
        local_sku = str(local.get("sku", "") or "").strip()
        local_has_strong_sku = bool(local_sku and len(local_sku) >= 8)
        cand_sku = str(cand_ram.get("sku", "") or "").strip()
        local_ddr = str(local.get("ddr", "") or "").strip()
        cand_ddr = str(cand_ram.get("ddr", "") or "").strip()
        exact_sku = False
        if local_sku:
            if cand_sku == local_sku:
                exact_sku = True
                score = 1.0
            elif cand_sku:
                continue
            elif local_has_strong_sku:
                # If local RAM has a reliable SKU (usually in brackets), do not keep candidates without SKU.
                continue
        if local_ddr and cand_ddr and cand_ddr != local_ddr and not exact_sku:
            continue
        if local_ddr and not cand_ddr and not exact_sku:
            # Candidate is missing DDR marker text; allow only exact-SKU path to avoid junk.
            continue
        if local.get("capacity_gb") and cand_ram.get("capacity_gb") == local.get("capacity_gb"):
            score += 0.03
        elif local.get("capacity_gb") and cand_ram.get("capacity_gb") and cand_ram.get("capacity_gb") != local.get("capacity_gb"):
            score -= 0.10
        if local.get("kit_modules") and cand_ram.get("kit_modules") == local.get("kit_modules"):
            score += 0.025
        elif local.get("kit_modules") and cand_ram.get("kit_modules") and cand_ram.get("kit_modules") != local.get("kit_modules"):
            score -= 0.08
        if local.get("mhz") and cand_ram.get("mhz") == local.get("mhz"):
            score += 0.03
        elif local.get("mhz") and cand_ram.get("mhz") and cand_ram.get("mhz") != local.get("mhz"):
            score -= 0.08
        if local.get("cl") and cand_ram.get("cl") == local.get("cl"):
            score += 0.02
        elif local.get("cl") and cand_ram.get("cl") and cand_ram.get("cl") != local.get("cl"):
            score -= 0.05
        if local.get("series") and cand_ram.get("series") == local.get("series"):
            score += 0.04
        elif local.get("series") and cand_ram.get("series") and cand_ram.get("series") != local.get("series"):
            score -= 0.08
        for flag_key in ["white", "rgb", "ecc", "reg"]:
            if bool(local.get(flag_key)) != bool(cand_ram.get(flag_key)):
                score -= 0.05

        if exact_sku:
            score = 1.0

        seen.add(cid)
        items.append({
            "id": cid,
            "name": cname,
            "url": str(cand.get("url", "") or "").strip(),
            "score": round(min(0.999, max(0.0, score)), 3),
            "source": str(cand.get("source", "ram_db")).strip() or "ram_db",
            "sku": cand_ram.get("sku", ""),
            "mhz": cand_ram.get("mhz", ""),
            "capacity_gb": cand_ram.get("capacity_gb", ""),
        })

    items.sort(key=lambda item: (
        -(float(item.get("score", 0.0) or 0.0)),
        str(item.get("name", "") or "").lower(),
    ))
    local_sku = str(local.get("sku", "") or "").strip()
    if local_sku:
        exact_items = [item for item in items if str(item.get("sku", "") or "").strip() == local_sku]
        if exact_items:
            items = exact_items
    return items[:max(1, int(top_n))]


def _ssd_brand_model_key(text):
    raw = str(text or "").strip()
    if not raw:
        return {"brand": "", "code": "", "model": "", "capacity": "", "external": False}
    low = raw.lower()
    brand = ""
    brand_patterns = [
        ("adata", r"(?:^|[^a-z0-9])a-?data|(?:^|[^a-z0-9])adata|(?:^|[^a-z0-9])xpg(?=$|[^a-z0-9])"),
        ("team", r"(?:^|[^a-z0-9])team(?=$|[^a-z0-9])"),
        ("netac", r"(?:^|[^a-z0-9])netac(?=$|[^a-z0-9])"),
        ("samsung", r"(?:^|[^a-z0-9])samsung(?=$|[^a-z0-9])"),
        ("kingston", r"(?:^|[^a-z0-9])kingston(?=$|[^a-z0-9])"),
        ("crucial", r"(?:^|[^a-z0-9])crucial(?=$|[^a-z0-9])"),
        ("wd", r"(?:^|[^a-z0-9])wd(?:$|[^a-z0-9])|western\s*digital"),
        ("transcend", r"(?:^|[^a-z0-9])transcend(?=$|[^a-z0-9])"),
        ("patriot", r"(?:^|[^a-z0-9])patriot(?=$|[^a-z0-9])"),
        ("hp", r"(?:^|[^a-z0-9])hp(?=$|[^a-z0-9])"),
    ]
    for value, pattern in brand_patterns:
        if re.search(pattern, low, flags=re.IGNORECASE):
            brand = value
            break

    def _ssd_norm(value):
        return re.sub(r"[^a-z0-9]+", "", str(value or "").lower())

    def _is_strong_ssd_code(value):
        v = _ssd_norm(value)
        if len(v) < 8:
            return False
        if not any(ch.isalpha() for ch in v) or not any(ch.isdigit() for ch in v):
            return False
        blocked = {
            "ssd", "nvme", "m2", "pcie", "sata", "usb", "typec",
            "mbps", "tb", "gb", "rtl", "oem", "bulk", "series"
        }
        if v in blocked:
            return False
        return True

    code = ""
    # Parentheses may contain speeds "(500/400)" — pick only strong article-like tokens.
    for m_code in re.finditer(r"\(([A-Za-z0-9][A-Za-z0-9.\-/]{4,80})\)", raw):
        token = str(m_code.group(1) or "").strip()
        if _is_strong_ssd_code(token):
            code = _ssd_norm(token)
            break
    if not code:
        tokens = _raw_paren_article_tokens(raw)
        for tok in tokens:
            if _is_strong_ssd_code(tok):
                code = _ssd_norm(tok)
                break
    if not code:
        # Inline article fallback: "... SU800 ASU800SS-512GT-C ..."
        for token in re.findall(r"\b([A-Za-z0-9][A-Za-z0-9.\-/]{6,40})\b", raw):
            if _is_spec_code(_ssd_norm(token).upper()):
                continue
            if _is_strong_ssd_code(token):
                code = _ssd_norm(token)
                break

    model = ""
    for pattern in [
        r"\b(sa\d{3,4})\b",
        r"\b(su\d{3,4})\b",
        r"\b(n\d{3,4}[a-z]?)\b",
        r"\b(nv\d{3,5}(?:-q)?)\b",
        r"\b(sd\d{2,4})\b",
        r"\b(bx\d{3,4})\b",
        r"\b(a\d{3,4})\b",
        r"\b(skc\d{3,5})\b",
        r"\b(snv\d+[a-z0-9]*)\b",
        r"\b(n\d{3,4}[a-z]?)\b",
        r"\b(91\d{2}\s*pro\s*series)\b",
        r"\b(mars\s*980\s*blade)\b",
        r"\b(mars\s*980\s*pro)\b",
        r"\b(fury\s*renegade\s*g5)\b",
        r"\b(cardea\s*z\d{3})\b",
        r"\b(g\d{2}\s*pro)\b",
        r"\b(nv\d{3,5})\b",
        r"\b(p\d{1,4})\b",
        r"\b(sd\d{2,4})\b",
        r"\b(z\s*slim)\b",
    ]:
        m = re.search(pattern, low, flags=re.IGNORECASE)
        if m and m.group(1):
            model = _normalize_compact_name(m.group(1))
            break

    capacity = ""
    m_cap = re.search(r"\b(\d+(?:\.\d+)?)\s*(tb|gb|тб|гб)\b", low, flags=re.IGNORECASE)
    if m_cap and m_cap.group(1) and m_cap.group(2):
        unit = m_cap.group(2).lower()
        if unit in {"тб"}:
            unit = "tb"
        elif unit in {"гб"}:
            unit = "gb"
        capacity = f"{m_cap.group(1)}{unit}"

    external = bool(re.search(r"внешн|portable|usb", low, flags=re.IGNORECASE))
    return {"brand": brand, "code": code, "model": model, "capacity": capacity, "external": external}


def _find_ssd_review_candidates(product_name, top_n=5):
    name = str(product_name or "").strip()
    if not name:
        return []
    local = _ssd_brand_model_key(name)
    local_code = str(local.get("code", "") or "").strip()
    local_brand = str(local.get("brand", "") or "").strip()
    local_model = str(local.get("model", "") or "").strip()
    local_capacity = str(local.get("capacity", "") or "").strip()
    local_external = bool(local.get("external"))
    if not local_code and not local_brand:
        return []

    pool = []
    try:
        with _db_connection() as conn:
            rows = []
            if local_code:
                rows.extend(conn.execute(
                    "SELECT ni.onliner_id, ni.raw_name, oc.url "
                    "FROM name_index ni "
                    "JOIN onliner_catalog oc ON oc.onliner_id = ni.onliner_id "
                    "WHERE replace(replace(replace(replace(lower(ni.raw_name), '-', ''), ' ', ''), '/', ''), '.', '') LIKE ? "
                    "LIMIT 150",
                    (f"%{local_code}%",),
                ).fetchall())
            if local_brand and len(rows) < 80:
                rows.extend(conn.execute(
                    "SELECT ni.onliner_id, ni.raw_name, oc.url "
                    "FROM name_index ni "
                    "JOIN onliner_catalog oc ON oc.onliner_id = ni.onliner_id "
                    "WHERE lower(ni.raw_name) LIKE ? "
                    "LIMIT 220",
                    (f"%{local_brand.lower()}%",),
                ).fetchall())
            seen_seed = set()
            for oid, raw_name, url in rows:
                oid = normalize_onliner_id(oid)
                if not oid or oid in seen_seed:
                    continue
                seen_seed.add(oid)
                pool.append({"id": oid, "name": raw_name, "url": url, "score": 0.91, "source": "ssd_db_seed"})
    except Exception:
        pass

    for cand in db_find_top_candidates(name, top_n=25, min_score=0.10, allow_b2b=False):
        pool.append(cand)

    exact = db_find_exact_id_for_name(name)
    if exact:
        pool = [exact] + list(pool or [])

    exact_items = []
    soft_items = []
    seen = set()
    for cand in pool:
        if not isinstance(cand, dict):
            continue
        cid = normalize_onliner_id(cand.get("id", ""))
        cname = str(cand.get("name", "") or "").strip()
        if not cid or not cname or cid in seen:
            continue
        if "ssd" not in cname.lower():
            continue

        cand_ssd = _ssd_brand_model_key(cname)
        cand_code = str(cand_ssd.get("code", "") or "").strip()
        cand_brand = str(cand_ssd.get("brand", "") or "").strip()
        cand_model = str(cand_ssd.get("model", "") or "").strip()
        cand_capacity = str(cand_ssd.get("capacity", "") or "").strip()
        cand_external = bool(cand_ssd.get("external"))

        if local_brand and cand_brand and cand_brand != local_brand:
            continue

        is_exact_code = bool(local_code and cand_code and cand_code == local_code)
        model_match = bool(local_model and cand_model and cand_model == local_model)
        capacity_match = bool(local_capacity and cand_capacity and cand_capacity == local_capacity)
        external_mismatch = (local_external != cand_external)

        if is_exact_code:
            score = max(float(cand.get("score", 0.0) or 0.0), 0.97)
            score = 1.0
            if model_match:
                score += 0.01
            if capacity_match:
                score += 0.01
        else:
            if local_code and cand_code and cand_code != local_code:
                # If both sides have explicit SKU/article and they differ — this is another SSD.
                continue
            # Fallback path when exact code is unavailable in DB:
            # require at least one strong anchor (model/capacity) to avoid junk.
            if not (model_match or capacity_match):
                continue
            if local_code and not cand_code and not (model_match and capacity_match):
                # Local has exact code but candidate has no extracted code:
                # keep only very strong structural match to avoid SU650/SU800 mixups.
                continue
            score = max(float(cand.get("score", 0.0) or 0.0), 0.82)
            if model_match:
                score += 0.08
            if capacity_match:
                score += 0.07
            if local_code and not cand_code:
                score -= 0.05
        if external_mismatch:
            score -= 0.08

        seen.add(cid)
        item = {
            "id": cid,
            "name": cname,
            "url": str(cand.get("url", "") or "").strip(),
            "score": round(min(0.999, max(0.0, score)), 3),
            "source": str(cand.get("source", "ssd_db")).strip() or ("ssd_db_code_exact" if is_exact_code else "ssd_db_fallback"),
            "code": cand_code,
            "model": cand_model,
            "capacity": cand_capacity,
        }
        if is_exact_code:
            exact_items.append(item)
        else:
            soft_items.append(item)

    exact_items.sort(key=lambda item: (
        -(float(item.get("score", 0.0) or 0.0)),
        str(item.get("name", "") or "").lower(),
    ))
    soft_items.sort(key=lambda item: (
        -(float(item.get("score", 0.0) or 0.0)),
        str(item.get("name", "") or "").lower(),
    ))
    items = exact_items if exact_items else soft_items
    return items[:max(1, int(top_n))]


def _psu_brand_model_key(text):
    raw = str(text or "").strip()
    if not raw:
        return {
            "brand": "",
            "watt": "",
            "eff": "",
            "modular": "",
            "code": "",
            "series": "",
            "form_factor": "",
            "atx": "",
            "white": False,
        }
    low = raw.lower()
    brand = ""
    brand_patterns = [
        ("1stplayer", r"(?:^|[^a-z0-9])1st\s*player(?=$|[^a-z0-9])|(?:^|[^a-z0-9])1stplayer(?=$|[^a-z0-9])"),
        ("adataxpg", r"(?:^|[^a-z0-9])(?:adata|xpg)(?=$|[^a-z0-9])"),
        ("chieftec", r"(?:^|[^a-z0-9])chieftec(?=$|[^a-z0-9])"),
        ("cougar", r"(?:^|[^a-z0-9])cougar(?=$|[^a-z0-9])"),
        ("deepcool", r"(?:^|[^a-z0-9])deepcool(?=$|[^a-z0-9])"),
        ("lianli", r"(?:^|[^a-z0-9])lian\s*li(?=$|[^a-z0-9])|(?:^|[^a-z0-9])lianli(?=$|[^a-z0-9])"),
        ("gamemax", r"(?:^|[^a-z0-9])gamemax(?=$|[^a-z0-9])"),
        ("montech", r"(?:^|[^a-z0-9])montech(?=$|[^a-z0-9])"),
        ("ntech", r"(?:^|[^a-z0-9])n-?tech(?=$|[^a-z0-9])"),
        ("powercase", r"(?:^|[^a-z0-9])powercase(?=$|[^a-z0-9])"),
        ("projectx", r"(?:^|[^a-z0-9])project\s*x(?=$|[^a-z0-9])"),
        ("vicsone", r"(?:^|[^a-z0-9])vicsone(?=$|[^a-z0-9])"),
        ("zalman", r"(?:^|[^a-z0-9])zalman(?=$|[^a-z0-9])"),
    ]
    for value, pattern in brand_patterns:
        if re.search(pattern, low, flags=re.IGNORECASE):
            brand = value
            break

    watt = ""
    m_watt = re.search(r"\b(\d{3,4})\s*(?:w|вт)\b", low, flags=re.IGNORECASE)
    if m_watt and m_watt.group(1):
        watt = m_watt.group(1)

    eff = ""
    if re.search(r"80\s*plus\s*titanium|\btitanium\b", low, flags=re.IGNORECASE):
        eff = "titanium"
    elif re.search(r"80\s*plus\s*platinum|\bplatinum\b", low, flags=re.IGNORECASE):
        eff = "platinum"
    elif re.search(r"80\s*plus\s*gold|\bgold\b", low, flags=re.IGNORECASE):
        eff = "gold"
    elif re.search(r"80\s*plus\s*silver|\bsilver\b", low, flags=re.IGNORECASE):
        eff = "silver"
    elif re.search(r"80\s*plus\s*bronze|\bbronze\b", low, flags=re.IGNORECASE):
        eff = "bronze"
    elif re.search(r"80\s*plus\s*standard|\bstandard\b|80\s*plus\b", low, flags=re.IGNORECASE):
        eff = "standard"

    modular = ""
    if re.search(r"non[-\s]?modular|немодуль|не\s*модуль", low, flags=re.IGNORECASE):
        modular = "non"
    elif re.search(r"semi[-\s]?modular|полу[-\s]?модуль", low, flags=re.IGNORECASE):
        modular = "semi"
    elif re.search(r"full[-\s]?modular|полностью\s*модуль", low, flags=re.IGNORECASE):
        modular = "full"

    def _psu_norm(value):
        return re.sub(r"[^a-z0-9]+", "", str(value or "").lower())

    def _is_strong_psu_code(value):
        norm = _psu_norm(value)
        if len(norm) < 6:
            return False
        if not any(ch.isalpha() for ch in norm) or not any(ch.isdigit() for ch in norm):
            return False
        blocked = {
            "atx", "atx20", "atx23", "atx24", "atx30", "atx31",
            "nonmodular", "semimodular", "fullmodular", "modular",
            "activepfc", "apfc", "llcdc", "dcdc", "ret", "oem", "bulk",
        }
        if norm in blocked:
            return False
        if norm.startswith("atx"):
            return False
        return True

    code = ""
    block_words = {"black", "white", "bronze", "gold", "silver", "platinum", "modular", "nonmodular"}
    preferred_prefixes = ("ps", "ha", "pps", "ppx", "r", "zm", "pn", "pb", "vte", "sr")

    def _token_rank(token):
        t = str(token or "").strip()
        norm = _psu_norm(t)
        if not _is_strong_psu_code(t):
            return -999
        if any(w in norm for w in block_words):
            return -999
        parts = [p for p in re.split(r"-+", t) if p]
        hyphens = max(0, len(parts) - 1)
        score = 0
        if 1 <= hyphens <= 3:
            score += 20
        if len(norm) <= 18:
            score += 12
        if norm.startswith(preferred_prefixes):
            score += 18
        if any(ch.isdigit() for ch in (parts[0] if parts else "")):
            score += 6
        if len(parts) >= 4:
            score -= 6
        return score

    token_candidates = []
    for m in re.finditer(r"\b([A-Za-z0-9]{1,10}(?:[.-][A-Za-z0-9]{1,16}){1,6})\b", raw):
        token = str(m.group(1) or "").strip()
        rank = _token_rank(token)
        if rank > -999:
            token_candidates.append((rank, token))
    if token_candidates:
        token_candidates.sort(key=lambda x: (-x[0], len(_psu_norm(x[1]))))
        code = _psu_norm(token_candidates[0][1])
    if not code:
        # Fallback: parentheses token (if inline heuristic found nothing).
        for m in re.finditer(r"\(([A-Za-z0-9][A-Za-z0-9\-]{3,64})\)", raw):
            token = str(m.group(1) or "").strip()
            if _is_strong_psu_code(token):
                code = _psu_norm(token)
                break

    series_patterns = [
        ("ngdpgold", r"ngdp\s*gold"),
        ("ackbronze", r"ack\s*bronze"),
        ("ackgold", r"ack\s*gold"),
        ("dkpremium", r"dk\s*premium"),
        ("core_reactor_ii_ve", r"core\s*reactor\s*ii\s*ve"),
        ("core_reactor_ii", r"core\s*reactor\s*ii"),
        ("cyber_core_ii", r"cyber\s*core\s*ii"),
        ("pylonii", r"pylon\s*ii"),
        ("pylon", r"(?:^|[^a-z0-9])pylon(?=$|[^a-z0-9])"),
        ("kyber", r"(?:^|[^a-z0-9])kyber(?=$|[^a-z0-9])"),
        ("fusion", r"(?:^|[^a-z0-9])fusion(?=$|[^a-z0-9])"),
        ("probe", r"(?:^|[^a-z0-9])probe(?=$|[^a-z0-9])"),
        ("pymcore", r"(?:^|[^a-z0-9])pymcore(?=$|[^a-z0-9])"),
        ("polarispro", r"polaris\s*pro"),
        ("polaris", r"(?:^|[^a-z0-9])polaris(?=$|[^a-z0-9])"),
        ("core", r"(?:^|[^a-z0-9])core(?=$|[^a-z0-9])"),
        ("proton", r"(?:^|[^a-z0-9])proton(?=$|[^a-z0-9])"),
        ("gamerstorm", r"(?:^|[^a-z0-9])gamerstorm(?=$|[^a-z0-9])"),
        ("centuryii", r"century\s*ii"),
        ("centuryg5", r"century\s*g5"),
        ("titangold", r"titan\s*gold"),
        ("titanpla", r"titan\s*pla"),
        ("fk", r"(?:^|[^a-z0-9])fk(?=$|[^a-z0-9])"),
    ]
    series = ""
    for key, pattern in series_patterns:
        if re.search(pattern, low, flags=re.IGNORECASE):
            series = key
            break

    form_factor = "sfx" if re.search(r"(?:^|[^a-z0-9])sfx(?=$|[^a-z0-9])", low, flags=re.IGNORECASE) else "atx"
    atx = ""
    m_atx = re.search(r"\batx\s*([0-9](?:\.[0-9]{1,2})?)\b", low, flags=re.IGNORECASE)
    if m_atx and m_atx.group(1):
        atx = m_atx.group(1)
    white = bool(re.search(r"(?:^|[^a-z0-9])white(?=$|[^a-z0-9])|\bбел(?:ый|ая|ое|ые)?\b", low, flags=re.IGNORECASE))
    return {
        "brand": brand,
        "watt": watt,
        "eff": eff,
        "modular": modular,
        "code": code,
        "series": series,
        "form_factor": form_factor,
        "atx": atx,
        "white": white,
    }


def _find_psu_review_candidates(product_name, top_n=5):
    name = str(product_name or "").strip()
    if not name:
        return []
    local = _psu_brand_model_key(name)
    if not local.get("brand") or not local.get("watt"):
        return []
    local_code = str(local.get("code", "") or "").strip()

    def _psu_code_match(a, b):
        a = re.sub(r"[^a-z0-9]+", "", str(a or "").lower())
        b = re.sub(r"[^a-z0-9]+", "", str(b or "").lower())
        if not a or not b:
            return False
        if a == b:
            return True
        # common suffixes in suppliers/DB variants
        strip_suffixes = ("bulk", "oem", "ret", "wgeu", "eu", "fa0b", "fc0b", "fc0w", "bk", "wh")
        for sfx in strip_suffixes:
            if a.endswith(sfx):
                a = a[: -len(sfx)]
            if b.endswith(sfx):
                b = b[: -len(sfx)]
        if a == b:
            return True
        if len(a) >= 6 and len(b) >= 6 and (a.startswith(b) or b.startswith(a)):
            return True
        if len(a) >= 8 and len(b) >= 8 and (a in b or b in a):
            return True
        return False

    pool = []
    try:
        with _db_connection() as conn:
            rows = []
            brand_query = str(local.get("brand", "") or "").strip()
            watt_query = str(local.get("watt", "") or "").strip()
            code_query = str(local.get("code", "") or "").strip()
            if code_query:
                rows.extend(conn.execute(
                    "SELECT ni.onliner_id, ni.raw_name, oc.url "
                    "FROM name_index ni "
                    "JOIN onliner_catalog oc ON oc.onliner_id = ni.onliner_id "
                    "WHERE replace(replace(replace(replace(lower(ni.raw_name), '-', ''), ' ', ''), '/', ''), '.', '') LIKE ? "
                    "LIMIT 180",
                    (f"%{code_query}%",),
                ).fetchall())
            if brand_query and watt_query:
                rows.extend(conn.execute(
                    "SELECT ni.onliner_id, ni.raw_name, oc.url "
                    "FROM name_index ni "
                    "JOIN onliner_catalog oc ON oc.onliner_id = ni.onliner_id "
                    "WHERE lower(ni.raw_name) LIKE ? AND lower(ni.raw_name) LIKE ? "
                    "LIMIT 220",
                    (f"%{brand_query.lower()}%", f"%{watt_query}%"),
                ).fetchall())
            seen_seed = set()
            for oid, raw_name, url in rows:
                oid = normalize_onliner_id(oid)
                if not oid or oid in seen_seed:
                    continue
                seen_seed.add(oid)
                pool.append({"id": oid, "name": raw_name, "url": url, "score": 0.91, "source": "psu_db_exact"})
    except Exception:
        pass

    for cand in db_find_top_candidates(name, top_n=22, min_score=0.10, allow_b2b=False):
        pool.append(cand)
    exact = db_find_exact_id_for_name(name)
    if exact:
        pool = [exact] + list(pool or [])

    items = []
    seen = set()
    for cand in pool:
        if not isinstance(cand, dict):
            continue
        cid = normalize_onliner_id(cand.get("id", ""))
        cname = str(cand.get("name", "") or "").strip()
        if not cid or not cname or cid in seen:
            continue
        if normalize_catalog_category_name(infer_category(cname)) != "Блок питания":
            continue
        cand_psu = _psu_brand_model_key(cname)
        if cand_psu.get("brand") != local.get("brand"):
            continue
        if local.get("watt") and cand_psu.get("watt") and cand_psu.get("watt") != local.get("watt"):
            continue
        if local.get("eff") and cand_psu.get("eff") and cand_psu.get("eff") != local.get("eff"):
            continue
        if local.get("modular") and cand_psu.get("modular") and cand_psu.get("modular") != local.get("modular"):
            continue
        if local.get("form_factor") and cand_psu.get("form_factor") and cand_psu.get("form_factor") != local.get("form_factor"):
            continue
        cand_code = str(cand_psu.get("code", "") or "").strip()
        if local_code and cand_code and not _psu_code_match(local_code, cand_code):
            continue

        score = max(float(cand.get("score", 0.0) or 0.0), 0.90)
        if local_code and cand_code and _psu_code_match(local_code, cand_code):
            score = 0.999
        if local.get("series") and cand_psu.get("series") == local.get("series"):
            score += 0.05
        elif local.get("series") and cand_psu.get("series") and cand_psu.get("series") != local.get("series"):
            score -= 0.08
        if local.get("atx") and cand_psu.get("atx"):
            try:
                score += 0.015 if abs(float(local.get("atx")) - float(cand_psu.get("atx"))) <= 0.11 else -0.05
            except Exception:
                pass
        if bool(local.get("white")) != bool(cand_psu.get("white")):
            score -= 0.05

        seen.add(cid)
        items.append({
            "id": cid,
            "name": cname,
            "url": str(cand.get("url", "") or "").strip(),
            "score": round(min(0.999, max(0.0, score)), 3),
            "source": str(cand.get("source", "psu_db")).strip() or "psu_db",
            "watt": cand_psu.get("watt", ""),
            "eff": cand_psu.get("eff", ""),
            "modular": cand_psu.get("modular", ""),
            "code": cand_code,
            "series": cand_psu.get("series", ""),
        })
    items.sort(key=lambda item: (
        -(float(item.get("score", 0.0) or 0.0)),
        str(item.get("name", "") or "").lower(),
    ))
    if local_code:
        exact_items = [item for item in items if _psu_code_match(local_code, str(item.get("code", "") or "").strip())]
        if exact_items:
            items = exact_items
    return items[:max(1, int(top_n))]


def _case_brand_model_key(text):
    raw = str(text or "").strip()
    if not raw:
        return {
            "brand": "",
            "code": "",
            "series": "",
            "form_factor": "",
            "with_psu": False,
            "watt": "",
            "white": False,
            "colors": set(),
        }
    low = raw.lower()
    brand = ""
    brand_patterns = [
        ("aerocool", r"(?:^|[^a-z0-9])aerocool(?=$|[^a-z0-9])"),
        ("adataxpg", r"(?:^|[^a-z0-9])(?:adata|xpg)(?=$|[^a-z0-9])"),
        ("cougar", r"(?:^|[^a-z0-9])cougar(?=$|[^a-z0-9])"),
        ("deepcool", r"(?:^|[^a-z0-9])deepcool(?=$|[^a-z0-9])"),
        ("gamemax", r"(?:^|[^a-z0-9])gamemax(?=$|[^a-z0-9])"),
        ("geometricfuture", r"(?:^|[^a-z0-9])geometric\s*future(?=$|[^a-z0-9])"),
        ("montech", r"(?:^|[^a-z0-9])montech(?=$|[^a-z0-9])"),
        ("powercase", r"(?:^|[^a-z0-9])powercase(?=$|[^a-z0-9])"),
        ("projectx", r"(?:^|[^a-z0-9])project\s*x(?=$|[^a-z0-9])"),
        ("segotep", r"(?:^|[^a-z0-9])segotep(?=$|[^a-z0-9])"),
        ("vicsone", r"(?:^|[^a-z0-9])vicsone(?=$|[^a-z0-9])"),
        ("zalman", r"(?:^|[^a-z0-9])zalman(?=$|[^a-z0-9])"),
    ]
    for value, pattern in brand_patterns:
        if re.search(pattern, low, flags=re.IGNORECASE):
            brand = value
            break

    form_factor = ""
    if re.search(r"(?:^|[^a-z0-9])e-?atx(?=$|[^a-z0-9])", low, flags=re.IGNORECASE):
        form_factor = "eatx"
    elif re.search(r"(?:^|[^a-z0-9])microatx|m-?atx(?=$|[^a-z0-9])", low, flags=re.IGNORECASE):
        form_factor = "matx"
    elif re.search(r"(?:^|[^a-z0-9])mini-?itx|miniitx(?=$|[^a-z0-9])", low, flags=re.IGNORECASE):
        form_factor = "itx"
    elif re.search(r"(?:^|[^a-z0-9])atx(?=$|[^a-z0-9])", low, flags=re.IGNORECASE):
        form_factor = "atx"

    without_psu = bool(re.search(r"без\s*б/?п|без\s*блока\s*пит", low, flags=re.IGNORECASE))
    with_psu = False
    if not without_psu:
        if re.search(r"\b\d{3,4}\s*w\b", low, flags=re.IGNORECASE):
            with_psu = True
        elif re.search(r"(?:^|[^a-zа-я0-9])с\s*б/?п(?=$|[^a-zа-я0-9])", low, flags=re.IGNORECASE):
            with_psu = True
        elif re.search(r"(?:^|[^a-zа-я0-9])б/?п\s+[a-zа-я0-9]", low, flags=re.IGNORECASE):
            with_psu = True
    watt = ""
    m_watt = re.search(r"\b(\d{3,4})\s*w\b", low, flags=re.IGNORECASE)
    if m_watt and m_watt.group(1):
        watt = m_watt.group(1)

    code = ""
    m_paren = re.search(r"\(([A-Za-z0-9][A-Za-z0-9\-]{3,40})\)", raw)
    if m_paren and m_paren.group(1):
        cand = str(m_paren.group(1) or "").strip()
        if not re.match(r"^\d+\s*[xх]", cand.lower()):
            code = re.sub(r"[^a-z0-9]+", "", cand.lower())
    if not code:
        for pattern in [
            r"\b([A-Za-z]{2,6}-[A-Za-z0-9]{2,16}(?:-[A-Za-z0-9]{1,10})?)\b",
            r"\b([A-Za-z]{2,6}[0-9]{2,5}[A-Za-z0-9\-]{0,8})\b",
        ]:
            m_code = re.search(pattern, raw)
            if m_code and m_code.group(1):
                code = re.sub(r"[^a-z0-9]+", "", m_code.group(1).lower())
                break

    series = ""
    series_patterns = [
        ("invaderx", r"invader\s*x"),
        ("defender", r"(?:^|[^a-z0-9])defender(?=$|[^a-z0-9])"),
        ("lander", r"(?:^|[^a-z0-9])lander(?=$|[^a-z0-9])"),
        ("valorairplus", r"valor\s*air\s*plus"),
        ("valorair", r"valor\s*air"),
        ("airface", r"(?:^|[^a-z0-9])airface(?=$|[^a-z0-9])"),
        ("cc560", r"(?:^|[^a-z0-9])cc560(?=$|[^a-z0-9])"),
        ("cg580", r"(?:^|[^a-z0-9])cg580(?=$|[^a-z0-9])"),
        ("ch780", r"(?:^|[^a-z0-9])ch780(?=$|[^a-z0-9])"),
        ("ch170", r"(?:^|[^a-z0-9])ch170(?=$|[^a-z0-9])"),
        ("ch360", r"(?:^|[^a-z0-9])ch360(?=$|[^a-z0-9])"),
        ("ch160", r"(?:^|[^a-z0-9])ch160(?=$|[^a-z0-9])"),
        ("matrexx30", r"matrexx\s*30"),
        ("matrexx55", r"matrexx\s*55"),
        ("dragonknight", r"dragon\s*knight"),
        ("meshbox", r"(?:^|[^a-z0-9])meshbox(?=$|[^a-z0-9])"),
        ("precision", r"(?:^|[^a-z0-9])precision(?=$|[^a-z0-9])"),
        ("air1000", r"air\s*1000"),
        ("hs01pro", r"hs01\s*pro"),
        ("hs02pro", r"hs02\s*pro"),
        ("king95pro", r"king\s*95\s*pro"),
        ("skyone", r"sky\s*one"),
        ("skytwo", r"sky\s*two"),
        ("x3mesh", r"x3\s*mesh"),
        ("xrwood", r"xr\s*wood"),
    ]
    for key, pattern in series_patterns:
        if re.search(pattern, low, flags=re.IGNORECASE):
            series = key
            break

    color_map = {
        "black": [r"(?:^|[^a-z0-9])black(?=$|[^a-z0-9])", r"черн", r"ч[её]рн"],
        "white": [r"(?:^|[^a-z0-9])white(?=$|[^a-z0-9])", r"бел"],
        "gray": [r"(?:^|[^a-z0-9])gray(?=$|[^a-z0-9])", r"(?:^|[^a-z0-9])grey(?=$|[^a-z0-9])", r"сер"],
        "red": [r"(?:^|[^a-z0-9])red(?=$|[^a-z0-9])", r"красн"],
        "blue": [r"(?:^|[^a-z0-9])blue(?=$|[^a-z0-9])", r"син"],
        "green": [r"(?:^|[^a-z0-9])green(?=$|[^a-z0-9])", r"зел"],
        "yellow": [r"(?:^|[^a-z0-9])yellow(?=$|[^a-z0-9])", r"желт"],
        "pink": [r"(?:^|[^a-z0-9])pink(?=$|[^a-z0-9])", r"роз"],
        "orange": [r"(?:^|[^a-z0-9])orange(?=$|[^a-z0-9])", r"оранж"],
        "silver": [r"(?:^|[^a-z0-9])silver(?=$|[^a-z0-9])", r"серебр"],
    }
    colors = set()
    for color_key, patterns in color_map.items():
        for pat in patterns:
            if re.search(pat, low, flags=re.IGNORECASE):
                colors.add(color_key)
                break
    white = "white" in colors
    return {
        "brand": brand,
        "code": code,
        "series": series,
        "form_factor": form_factor,
        "with_psu": with_psu,
        "watt": watt,
        "white": white,
        "colors": colors,
    }


def _case_code_match(a, b):
    """Loose equality for case SKUs (R-… / DP-… vs short series codes in catalog names)."""
    a = re.sub(r"[^a-z0-9]+", "", str(a or "").lower())
    b = re.sub(r"[^a-z0-9]+", "", str(b or "").lower())
    if not a or not b:
        return False
    if a == b:
        return True

    def _strip_leading_r_sku(x):
        if x.startswith("r") and len(x) > 6 and x[1:2].isalpha():
            return x[1:]
        return x

    variants_a = {_strip_leading_r_sku(a), a}
    variants_b = {_strip_leading_r_sku(b), b}
    if variants_a & variants_b:
        return True
    shorter, longer = (a, b) if len(a) <= len(b) else (b, a)
    if len(shorter) >= 5 and len(longer) >= 10 and shorter in longer:
        return True
    if len(a) >= 6 and len(b) >= 6 and (a.startswith(b) or b.startswith(a)):
        return True
    if len(a) >= 8 and len(b) >= 8 and (a in b or b in a):
        return True
    return False


def _case_form_factor_compatible(local_ff, cand_ff):
    if not local_ff or not cand_ff or local_ff == cand_ff:
        return True
    pair = {local_ff, cand_ff}
    if pair <= {"atx", "eatx"}:
        return True
    return False


def _find_case_review_candidates(product_name, top_n=5):
    name = str(product_name or "").strip()
    if not name:
        return []
    local = _case_brand_model_key(name)
    if not local.get("brand"):
        return []

    pool = []
    try:
        with _db_connection() as conn:
            rows = []
            brand_query = str(local.get("brand", "") or "").strip()
            code_query = str(local.get("code", "") or "").strip()
            series_query = str(local.get("series", "") or "").strip()
            if code_query:
                rows.extend(conn.execute(
                    "SELECT ni.onliner_id, ni.raw_name, oc.url "
                    "FROM name_index ni "
                    "JOIN onliner_catalog oc ON oc.onliner_id = ni.onliner_id "
                    "WHERE replace(replace(replace(replace(lower(ni.raw_name), '-', ''), ' ', ''), '/', ''), '.', '') LIKE ? "
                    "LIMIT 200",
                    (f"%{code_query}%",),
                ).fetchall())
            if brand_query:
                brand_tokens = [brand_query.lower()]
                if brand_query == "adataxpg":
                    brand_tokens = ["adata xpg", "xpg", "adata"]
                elif brand_query == "projectx":
                    brand_tokens = ["project x", "projectx"]
                elif brand_query == "geometricfuture":
                    brand_tokens = ["geometric future", "geometricfuture"]
                for bt in brand_tokens:
                    rows.extend(conn.execute(
                        "SELECT ni.onliner_id, ni.raw_name, oc.url "
                        "FROM name_index ni "
                        "JOIN onliner_catalog oc ON oc.onliner_id = ni.onliner_id "
                        "WHERE lower(ni.raw_name) LIKE ? "
                        "LIMIT 220",
                        (f"%{bt}%",),
                    ).fetchall())
            if series_query:
                series_tokens = {
                    "invaderx": ["invader x", "invaderx"],
                    "defender": ["defender"],
                    "lander": ["lander"],
                    "valorairplus": ["valor air plus", "valorairplus"],
                    "valorair": ["valor air", "valorair"],
                    "air1000": ["air 1000", "air1000"],
                    "king95pro": ["king 95 pro", "king95 pro", "king95pro"],
                    "x3mesh": ["x3 mesh", "x3mesh"],
                    "xrwood": ["xr wood", "xrwood"],
                    "cg580": ["cg 580", "cg580"],
                    "ch170": ["ch 170", "ch170"],
                    "ch360": ["ch 360", "ch360"],
                    "ch160": ["ch 160", "ch160"],
                    "matrexx30": ["matrexx 30", "matrexx30"],
                }.get(series_query, [series_query])
                for st in series_tokens:
                    rows.extend(conn.execute(
                        "SELECT ni.onliner_id, ni.raw_name, oc.url "
                        "FROM name_index ni "
                        "JOIN onliner_catalog oc ON oc.onliner_id = ni.onliner_id "
                        "WHERE lower(ni.raw_name) LIKE ? "
                        "LIMIT 180",
                        (f"%{st.lower()}%",),
                    ).fetchall())
            seen_seed = set()
            for oid, raw_name, url in rows:
                oid = normalize_onliner_id(oid)
                if not oid or oid in seen_seed:
                    continue
                seen_seed.add(oid)
                pool.append({"id": oid, "name": raw_name, "url": url, "score": 0.90, "source": "case_db_exact"})
    except Exception:
        pass

    for cand in db_find_top_candidates(name, top_n=24, min_score=0.10, allow_b2b=False):
        pool.append(cand)
    exact = db_find_exact_id_for_name(name)
    if exact:
        pool = [exact] + list(pool or [])

    items = []
    seen = set()
    for cand in pool:
        if not isinstance(cand, dict):
            continue
        cid = normalize_onliner_id(cand.get("id", ""))
        cname = str(cand.get("name", "") or "").strip()
        if not cid or not cname or cid in seen:
            continue
        if normalize_catalog_category_name(infer_category(cname)) != "Корпус":
            continue
        cand_case = _case_brand_model_key(cname)
        if cand_case.get("brand") != local.get("brand"):
            continue
        if local.get("form_factor") and cand_case.get("form_factor"):
            if not _case_form_factor_compatible(local.get("form_factor"), cand_case.get("form_factor")):
                continue
        if local.get("code") and cand_case.get("code") and not _case_code_match(local.get("code"), cand_case.get("code")):
            continue
        if local.get("with_psu") != cand_case.get("with_psu"):
            continue
        if local.get("with_psu") and local.get("watt") and cand_case.get("watt") and local.get("watt") != cand_case.get("watt"):
            continue
        local_colors = set(local.get("colors") or set())
        cand_colors = set(cand_case.get("colors") or set())
        if local_colors and cand_colors and not (local_colors & cand_colors):
            continue

        score = max(float(cand.get("score", 0.0) or 0.0), 0.88)
        if local.get("code") and cand_case.get("code") and _case_code_match(local.get("code"), cand_case.get("code")):
            score = 0.999
        if local.get("series") and cand_case.get("series") == local.get("series"):
            score += 0.05
        elif local.get("series") and cand_case.get("series") and cand_case.get("series") != local.get("series"):
            score -= 0.08
        if local_colors and cand_colors:
            shared_colors = local_colors & cand_colors
            extra_colors = cand_colors - local_colors
            score += min(0.05, 0.02 * len(shared_colors))
            score -= min(0.08, 0.02 * len(extra_colors))
        elif bool(local.get("white")) != bool(cand_case.get("white")):
            score -= 0.06

        seen.add(cid)
        items.append({
            "id": cid,
            "name": cname,
            "url": str(cand.get("url", "") or "").strip(),
            "score": round(min(0.999, max(0.0, score)), 3),
            "source": str(cand.get("source", "case_db")).strip() or "case_db",
            "code": cand_case.get("code", ""),
            "series": cand_case.get("series", ""),
            "form_factor": cand_case.get("form_factor", ""),
            "colors": sorted(cand_colors),
        })
    items.sort(key=lambda item: (
        -(float(item.get("score", 0.0) or 0.0)),
        str(item.get("name", "") or "").lower(),
    ))
    local_code = str(local.get("code", "") or "").strip()
    if local_code:
        exact_items = [item for item in items if _case_code_match(local_code, str(item.get("code", "") or "").strip())]
        if exact_items:
            items = exact_items
    return items[:max(1, int(top_n))]


def _hdd_norm_article(value):
    return re.sub(r"[^a-z0-9]+", "", str(value or "").lower())


def _is_strong_hdd_paren_code(token):
    v = _hdd_norm_article(token)
    if len(v) < 6:
        return False
    if not any(ch.isalpha() for ch in v) or not any(ch.isdigit() for ch in v):
        return False
    blocked = {
        "sataiii", "usb300", "usb301", "usb302", "usb310", "usb311", "usb312", "usb320",
        "6gbps", "12gbps", "7200rpm", "5400rpm", "5640rpm", "1000rpm",
        "256mb", "512mb", "128mb", "64mb",
        "rtl", "oem", "bulk",
    }
    if v in blocked:
        return False
    return True


def _hdd_brand_model_key(text):
    raw = str(text or "").strip()
    if not raw:
        return {"brand": "", "code": "", "capacity": "", "external": False, "form": ""}
    low = raw.lower()
    if re.search(r"\bnvme\b|\bm\.2\b|твердотельн", low, flags=re.IGNORECASE):
        return {"brand": "", "code": "", "capacity": "", "external": False, "form": ""}

    brand = ""
    brand_patterns = [
        ("wd", r"(?:^|[^a-z0-9])wd(?:$|[^a-z0-9])|western\s*digital"),
        ("seagate", r"(?:^|[^a-z0-9])seagate(?=$|[^a-z0-9])"),
        ("toshiba", r"(?:^|[^a-z0-9])toshiba(?=$|[^a-z0-9])"),
        ("adata", r"(?:^|[^a-z0-9])a-?data(?=$|[^a-z0-9])|(?:^|[^a-z0-9])adata(?=$|[^a-z0-9])"),
        ("netac", r"(?:^|[^a-z0-9])netac(?=$|[^a-z0-9])"),
        ("hgst", r"(?:^|[^a-z0-9])hgst(?=$|[^a-z0-9])"),
        ("hitachi", r"(?:^|[^a-z0-9])hitachi(?=$|[^a-z0-9])"),
    ]
    for value, pattern in brand_patterns:
        if re.search(pattern, low, flags=re.IGNORECASE):
            brand = value
            break

    code = ""
    for m_code in re.finditer(r"\(([A-Za-z0-9][A-Za-z0-9.\-/]{4,80})\)", raw):
        token = str(m_code.group(1) or "").strip()
        if _is_strong_hdd_paren_code(token):
            code = _hdd_norm_article(token)
            break
    if not code:
        for tok in _raw_paren_article_tokens(raw):
            if _is_strong_hdd_paren_code(tok):
                code = _hdd_norm_article(tok)
                break
    if not code:
        for token in re.findall(r"\b([A-Za-z0-9][A-Za-z0-9.\-/]{6,40})\b", raw):
            if _is_spec_code(_hdd_norm_article(token).upper()):
                continue
            if _is_strong_hdd_paren_code(token):
                code = _hdd_norm_article(token)
                break

    if not brand and code:
        if code.startswith("st") and len(code) >= 8:
            brand = "seagate"
        elif code.startswith(("wd", "wu")):
            brand = "wd"
        elif code.startswith(("mg", "mq", "hdwd", "hdtb", "dt")):
            brand = "toshiba"
        elif code.startswith(("ahd", "ahv")):
            brand = "adata"
        elif code.startswith("nt0"):
            brand = "netac"

    capacity = ""
    for m_cap in re.finditer(r"\b(\d+(?:\.\d+)?)\s*(tb|gb|тб|гб)\b", low, flags=re.IGNORECASE):
        if not m_cap.group(1) or not m_cap.group(2):
            continue
        tail = low[m_cap.end():m_cap.end() + 3]
        if tail.startswith("/"):
            continue
        unit = m_cap.group(2).lower()
        if unit in {"тб"}:
            unit = "tb"
        elif unit in {"гб"}:
            unit = "gb"
        capacity = f"{m_cap.group(1)}{unit}"

    form = ""
    if re.search(r"2\s*[.,]\s*5\s*\"|2\s*,\s*5\s*\"", low, flags=re.IGNORECASE):
        form = "25"
    elif re.search(r"3\s*[.,]\s*5\s*\"|3\s*,\s*5\s*\"", low, flags=re.IGNORECASE):
        form = "35"

    external = bool(re.search(r"внешн|portable", low, flags=re.IGNORECASE))
    return {
        "brand": brand,
        "code": code,
        "capacity": capacity,
        "external": external,
        "form": form,
    }


def _looks_like_hdd_name(text):
    raw = str(text or "").strip()
    if not raw:
        return False
    low = raw.lower()
    if re.search(r"\bnvme\b|\bm\.2\b|твердотельн", low, flags=re.IGNORECASE):
        return False
    if re.search(r"(?:^|[^a-z0-9])ssd(?:$|[^a-z0-9])", low, flags=re.IGNORECASE) and not re.search(
            r"\bhdd\b|жестк|винчест|hard\s*drive", low, flags=re.IGNORECASE):
        return False
    if re.search(r"\bhdd\b|жестк|винчест|hard\s*drive", low, flags=re.IGNORECASE):
        return True
    if re.search(r"внешний\s+накопитель", low, flags=re.IGNORECASE) and re.search(
            r"\bhdd\b", low, flags=re.IGNORECASE):
        return True
    return False


def _find_hdd_review_candidates(product_name, top_n=5):
    name = str(product_name or "").strip()
    if not name:
        return []
    local = _hdd_brand_model_key(name)
    local_code = str(local.get("code", "") or "").strip()
    local_brand = str(local.get("brand", "") or "").strip()
    local_capacity = str(local.get("capacity", "") or "").strip()
    local_external = bool(local.get("external"))
    local_form = str(local.get("form", "") or "").strip()
    if not local_code and not local_brand:
        return []

    pool = []
    try:
        with _db_connection() as conn:
            rows = []
            if local_code:
                rows.extend(conn.execute(
                    "SELECT ni.onliner_id, ni.raw_name, oc.url "
                    "FROM name_index ni "
                    "JOIN onliner_catalog oc ON oc.onliner_id = ni.onliner_id "
                    "WHERE replace(replace(replace(replace(lower(ni.raw_name), '-', ''), ' ', ''), '/', ''), '.', '') LIKE ? "
                    "LIMIT 180",
                    (f"%{local_code}%",),
                ).fetchall())
            if local_brand and len(rows) < 90:
                rows.extend(conn.execute(
                    "SELECT ni.onliner_id, ni.raw_name, oc.url "
                    "FROM name_index ni "
                    "JOIN onliner_catalog oc ON oc.onliner_id = ni.onliner_id "
                    "WHERE lower(ni.raw_name) LIKE ? "
                    "LIMIT 220",
                    (f"%{local_brand.lower()}%",),
                ).fetchall())
            seed_best = {}
            for oid, raw_name, url in rows:
                oid = normalize_onliner_id(oid)
                if not oid:
                    continue
                cat_ok = normalize_catalog_category_name(infer_category(raw_name)) == "Жесткий диск"
                prev = seed_best.get(oid)
                if prev is None:
                    seed_best[oid] = (raw_name, url, cat_ok)
                    continue
                pn, pu, pok = prev
                if cat_ok and not pok:
                    seed_best[oid] = (raw_name, url, cat_ok)
                elif cat_ok == pok and len(str(raw_name or "")) > len(str(pn or "")):
                    seed_best[oid] = (raw_name, url, cat_ok)
            for oid, (raw_name, url, cat_ok) in seed_best.items():
                if not cat_ok:
                    continue
                pool.append({"id": oid, "name": raw_name, "url": url, "score": 0.91, "source": "hdd_db_seed"})
    except Exception:
        pass

    for cand in db_find_top_candidates(name, top_n=25, min_score=0.10, allow_b2b=False):
        pool.append(cand)

    exact = db_find_exact_id_for_name(name)
    if exact:
        pool = [exact] + list(pool or [])

    items = []
    seen = set()
    for cand in pool:
        if not isinstance(cand, dict):
            continue
        cid = normalize_onliner_id(cand.get("id", ""))
        cname = str(cand.get("name", "") or "").strip()
        if not cid or not cname or cid in seen:
            continue
        if normalize_catalog_category_name(infer_category(cname)) != "Жесткий диск":
            continue
        cl = cname.lower()
        if re.search(r"\bnvme\b|\bm\.2\b|твердотельн", cl, flags=re.IGNORECASE):
            continue
        cand_h = _hdd_brand_model_key(cname)
        cand_code = str(cand_h.get("code", "") or "").strip()
        cand_brand = str(cand_h.get("brand", "") or "").strip()
        cand_capacity = str(cand_h.get("capacity", "") or "").strip()
        cand_external = bool(cand_h.get("external"))
        cand_form = str(cand_h.get("form", "") or "").strip()

        if local_brand and cand_brand and cand_brand != local_brand:
            continue
        if local_code and cand_code and not _case_code_match(local_code, cand_code):
            continue
        if local_capacity and cand_capacity and local_capacity.lower() != cand_capacity.lower():
            continue
        if local_external != cand_external:
            continue
        if local_form and cand_form and local_form != cand_form:
            continue

        score = max(float(cand.get("score", 0.0) or 0.0), 0.88)
        if local_code and cand_code and _case_code_match(local_code, cand_code):
            score = 0.999

        seen.add(cid)
        items.append({
            "id": cid,
            "name": cname,
            "url": str(cand.get("url", "") or "").strip(),
            "score": round(min(0.999, max(0.0, score)), 3),
            "source": str(cand.get("source", "hdd_db")).strip() or "hdd_db",
            "code": cand_code,
            "capacity": cand_capacity,
        })
    items.sort(key=lambda item: (
        -(float(item.get("score", 0.0) or 0.0)),
        str(item.get("name", "") or "").lower(),
    ))
    if local_code:
        exact_items = [item for item in items if _case_code_match(local_code, str(item.get("code", "") or "").strip())]
        if exact_items:
            items = exact_items
    return items[:max(1, int(top_n))]


def _printer_mfp_norm_article(value):
    return re.sub(r"[^a-z0-9]+", "", str(value or "").lower())


def _looks_like_printer_or_mfp_name(text):
    raw = str(text or "").strip()
    if not raw:
        return False
    low = raw.lower()
    if re.search(r"\bпринтер\b|\bпринтеры\b|\bмфу\b|\bmfp\b", low, flags=re.IGNORECASE):
        return True
    return False


def _printer_mfp_catalog_category_ok(raw_name):
    rn = str(raw_name or "").strip()
    if not rn:
        return False
    low = rn.lower()
    if re.search(r"\bпринтер\b|\bпринтеры\b|\bмфу\b|\bmfp\b|\bmultifunction\b", low, flags=re.IGNORECASE):
        return True
    try:
        cat = normalize_catalog_category_name(infer_category(rn))
    except Exception:
        cat = ""
    return cat == "Принтер и МФУ"


def _printer_mfp_brand_model_key(text):
    raw = str(text or "").strip()
    if not raw:
        return {"brand": "", "article": "", "model_compact": "", "model_display": ""}
    low = raw.lower()
    brand = ""
    brand_patterns = [
        ("canon", r"\bcanon\b"),
        ("pantum", r"\bpantum\b"),
        ("hp", r"(?:^|[^a-z0-9])hp(?=$|[^a-z0-9])"),
        ("epson", r"\bepson\b"),
        ("brother", r"\bbrother\b"),
        ("xerox", r"\bxerox\b"),
        ("kyocera", r"\bkyocera\b"),
        ("ricoh", r"\bricoh\b"),
        ("samsung", r"\bsamsung\b"),
        ("lexmark", r"\blexmark\b"),
        ("oki", r"\boki\b"),
        ("sharp", r"\bsharp\b"),
        ("konica", r"\bkonica\b"),
        ("develop", r"\bdevelop\b"),
    ]
    for value, pattern in brand_patterns:
        if re.search(pattern, low, flags=re.IGNORECASE):
            brand = value
            break

    work = re.sub(r"^\s*(?:принтер|мфу)\s+", "", raw, flags=re.IGNORECASE).strip()
    if brand:
        work = re.sub(rf"^.*?\b{re.escape(brand)}\b\s*", "", work, count=1, flags=re.IGNORECASE).strip()

    head = work.split("(", 1)[0].strip()
    head = re.split(r",\s*(?:лазерный|струйный|цветн|черно|формат)", head, 1, flags=re.IGNORECASE)[0].strip()
    model_display = head[:160] if head else ""
    model_compact = re.sub(r"[^a-z0-9]+", "", head.lower())

    article = ""
    for m_code in re.finditer(r"\(([A-Za-z0-9][A-Za-z0-9.\-/]{4,40})\)", raw):
        token = str(m_code.group(1) or "").strip()
        if re.match(r"^\d+\s*x\s*\d+", token, flags=re.IGNORECASE):
            continue
        if re.search(r"(?:dpi|мм|стр/мин|lan|wifi)", token, flags=re.IGNORECASE):
            continue
        av = _printer_mfp_norm_article(token)
        if len(av) < 6:
            continue
        if not any(ch.isdigit() for ch in av) or not any(ch.isalpha() for ch in av):
            continue
        article = av
        break

    return {
        "brand": brand,
        "article": article,
        "model_compact": model_compact,
        "model_display": model_display,
    }


def _find_printer_review_candidates(product_name, top_n=5):
    name = str(product_name or "").strip()
    if not name:
        return []
    local = _printer_mfp_brand_model_key(name)
    local_article = str(local.get("article", "") or "").strip()
    local_brand = str(local.get("brand", "") or "").strip()
    local_mc = str(local.get("model_compact", "") or "").strip()
    if not local_brand and not local_article and len(local_mc) < 5:
        return []

    pool = []
    try:
        with _db_connection() as conn:
            rows = []
            if local_article:
                rows.extend(conn.execute(
                    "SELECT ni.onliner_id, ni.raw_name, oc.url "
                    "FROM name_index ni "
                    "JOIN onliner_catalog oc ON oc.onliner_id = ni.onliner_id "
                    "WHERE replace(replace(replace(replace(lower(ni.raw_name), '-', ''), ' ', ''), '/', ''), '.', '') LIKE ? "
                    "LIMIT 180",
                    (f"%{local_article}%",),
                ).fetchall())
            if local_brand and len(rows) < 90:
                rows.extend(conn.execute(
                    "SELECT ni.onliner_id, ni.raw_name, oc.url "
                    "FROM name_index ni "
                    "JOIN onliner_catalog oc ON oc.onliner_id = ni.onliner_id "
                    "WHERE lower(ni.raw_name) LIKE ? "
                    "LIMIT 220",
                    (f"%{local_brand.lower()}%",),
                ).fetchall())
            if local_mc and len(local_mc) >= 6 and len(rows) < 140:
                rows.extend(conn.execute(
                    "SELECT ni.onliner_id, ni.raw_name, oc.url "
                    "FROM name_index ni "
                    "JOIN onliner_catalog oc ON oc.onliner_id = ni.onliner_id "
                    "WHERE replace(replace(replace(replace(lower(ni.raw_name), '-', ''), ' ', ''), '/', ''), '.', '') LIKE ? "
                    "LIMIT 180",
                    (f"%{local_mc}%",),
                ).fetchall())
            seed_best = {}
            for oid, raw_name, url in rows:
                oid = normalize_onliner_id(oid)
                if not oid:
                    continue
                if not _printer_mfp_catalog_category_ok(raw_name):
                    continue
                prev = seed_best.get(oid)
                if prev is None:
                    seed_best[oid] = (raw_name, url, True)
                    continue
                pn, pu, _ = prev
                if len(str(raw_name or "")) > len(str(pn or "")):
                    seed_best[oid] = (raw_name, url, True)
            for oid, (raw_name, url, _cat_ok) in seed_best.items():
                pool.append({"id": oid, "name": raw_name, "url": url, "score": 0.91, "source": "printer_db_seed"})
    except Exception:
        pass

    for cand in db_find_top_candidates(name, top_n=25, min_score=0.10, allow_b2b=False):
        pool.append(cand)

    exact = db_find_exact_id_for_name(name)
    if exact:
        pool = [exact] + list(pool or [])

    items = []
    seen = set()
    for cand in pool:
        if not isinstance(cand, dict):
            continue
        cid = normalize_onliner_id(cand.get("id", ""))
        cname = str(cand.get("name", "") or "").strip()
        if not cid or not cname or cid in seen:
            continue
        if not _printer_mfp_catalog_category_ok(cname):
            continue
        cand_local = _printer_mfp_brand_model_key(cname)
        cand_article = str(cand_local.get("article", "") or "").strip()
        cand_mc = str(cand_local.get("model_compact", "") or "").strip()
        cand_brand = str(cand_local.get("brand", "") or "").strip()

        if local_brand and cand_brand and local_brand != cand_brand:
            continue

        match_ok = False
        if local_article:
            if cand_article and _case_code_match(local_article, cand_article):
                match_ok = True
            elif local_article in _printer_mfp_norm_article(cname):
                match_ok = True
        if not match_ok and local_mc and len(local_mc) >= 5 and cand_mc:
            if local_mc in cand_mc or cand_mc in local_mc or local_mc == cand_mc:
                match_ok = True
        if not match_ok:
            continue

        score = max(float(cand.get("score", 0.0) or 0.0), 0.86)
        if local_article and cand_article and _case_code_match(local_article, cand_article):
            score = 0.999
        elif local_mc and cand_mc and (local_mc in cand_mc or cand_mc in local_mc):
            score = max(score, 0.95)

        seen.add(cid)
        items.append({
            "id": cid,
            "name": cname,
            "url": str(cand.get("url", "") or "").strip(),
            "score": round(min(0.999, max(0.0, score)), 3),
            "source": str(cand.get("source", "printer_db")).strip() or "printer_db",
            "code": cand_article or cand_mc,
        })
    items.sort(key=lambda item: (
        -(float(item.get("score", 0.0) or 0.0)),
        str(item.get("name", "") or "").lower(),
    ))
    if local_article:
        exact_items = [item for item in items if _case_code_match(local_article, str(item.get("code", "") or "").strip())]
        if exact_items:
            items = exact_items
    return items[:max(1, int(top_n))]


def _cooler_norm_article(value):
    return re.sub(r"[^a-z0-9]+", "", str(value or "").lower())


def _cooler_paren_looks_socket_bundle(token):
    t = str(token or "").strip().lower()
    if not t:
        return True
    if re.match(r"^\d", t):
        return True
    if t.count("/") >= 2 and re.search(r"\b(lga|am\d|fm\d)\b", t):
        return True
    if re.search(r"\d{2,4}\s*шт\s*/", t):
        return True
    return False


def _is_strong_cooler_paren_code(token):
    raw_t = str(token or "").strip()
    # Разрешаем артикулы вроде 1C251B1361000 / 4N005-01-20G (с цифры в начале), отсекаем только «чисто числовые» скобки.
    if not raw_t or re.match(r"^\d+$", raw_t):
        return False
    v = _cooler_norm_article(raw_t)
    if len(v) < 4 or not any(ch.isalpha() for ch in v):
        return False
    blocked = {"sataiii", "usb320", "usb310", "rtl", "oem", "bulk", "ret", "box"}
    if v in blocked:
        return False
    return True


def _cooler_brand_model_key(text):
    raw = str(text or "").strip()
    if not raw:
        return {"brand": "", "code": "", "tdp": "", "colors": set(), "white": False}
    low = raw.lower()
    if re.search(r"\bкорпус\b|\bбез\s+бп\b", low, flags=re.IGNORECASE):
        return {"brand": "", "code": "", "tdp": "", "colors": set(), "white": False}

    brand = ""
    brand_patterns = [
        ("deepcool", r"deep\s*cool|deepcool"),
        ("cryorig", r"(?:^|[^a-z0-9])cryorig(?=$|[^a-z0-9])"),
        ("idcooling", r"id[\s\-]*cooling|(?:^|[^a-z0-9])id\s+cooling"),
        ("montech", r"(?:^|[^a-z0-9])montech(?=$|[^a-z0-9])"),
        ("xpg", r"\bxpg\b"),
        ("adata", r"\badata\b"),
        ("geometricfuture", r"geometric\s*future|geometricfuture"),
        ("sapphire", r"\bsapphire\b"),
        ("thermalright", r"thermal\s*right|thermalright"),
        ("arctic", r"arctic\s*cooling|(?:^|[^a-z0-9])arctic(?=$|[^a-z0-9])"),
        ("alseye", r"(?:^|[^a-z0-9])alseye(?=$|[^a-z0-9])"),
        ("noctua", r"(?:^|[^a-z0-9])noctua(?=$|[^a-z0-9])"),
        ("bequiet", r"be\s*quiet|bequiet"),
        ("zalman", r"(?:^|[^a-z0-9])zalman(?=$|[^a-z0-9])"),
    ]
    for value, pattern in brand_patterns:
        if re.search(pattern, low, flags=re.IGNORECASE):
            brand = value
            break

    code = ""
    for m_code in re.finditer(r"\(([A-Za-z0-9][A-Za-z0-9.\-/]{3,80})\)", raw):
        token = str(m_code.group(1) or "").strip()
        if _cooler_paren_looks_socket_bundle(token):
            continue
        if _is_strong_cooler_paren_code(token):
            code = _cooler_norm_article(token)
            break
    if not code:
        for tok in _raw_paren_article_tokens(raw):
            if _cooler_paren_looks_socket_bundle(tok):
                continue
            if _is_strong_cooler_paren_code(tok):
                code = _cooler_norm_article(tok)
                break
    if not code:
        for pattern in [
            r"\b(R-[A-Za-z0-9\-]{6,40})\b",
            r"\b(CR-[A-Za-z0-9\-]{2,24})\b",
            r"\b(MACHO-[A-Z0-9\-]{2,24})\b",
            r"\b(ACALP[a-z0-9]{4,20})\b",
            r"\b(AS-[A-Z0-9\-]{2,20})\b",
            r"\b(SE-\d{3}(?:-[A-Z0-9]{2,})+)\b",
            r"\b(IS-\d{2}[A-Z]*(?:-[A-Z]{2,})?)\b",
            r"\b(DK-\d{2}[A-Z]?)\b",
            r"\b(AG\d{3})\b",
            r"\b(AK\d{3})\b",
            r"\b(NX\d{3})\b",
            r"\b(FROZN\s+A\d{3}(?:\s+[A-Z]+)*)\b",
            r"\b(LEVANTE[A-Z0-9\-]{4,36})\b",
            r"\b(ACFRE\d{5}[A-Z]?)\b",
            r"\b(F-[A-Z0-9\-]{4,40})\b",
            r"\b(A-ELITE[A-Z0-9\-]{2,36})\b",
            r"\b(C-MATRIX[A-Z0-9\-]{2,24})\b",
            r"\b(TURBO-RIGHT-[A-Z0-9\-]{2,24})\b",
            r"\b(4N\d{3}-\d{2}-\d{2}[A-Z])\b",
            r"\b(1C[0-9A-Z]{8,14})\b",
        ]:
            m = re.search(pattern, raw, flags=re.IGNORECASE)
            if m and m.group(1):
                cand = str(m.group(1) or "").strip()
                if _is_strong_cooler_paren_code(cand):
                    code = _cooler_norm_article(cand)
                    break

    if not brand and code:
        if re.search(r"deep\s*cool|deepcool", low, flags=re.IGNORECASE):
            brand = "deepcool"
        elif re.search(r"id[\s\-]*cooling|id\s+cooling", low, flags=re.IGNORECASE):
            brand = "idcooling"
        elif re.search(r"cryorig", low, flags=re.IGNORECASE) or code.startswith("cr"):
            brand = "cryorig"
        elif re.search(r"thermal\s*right|thermalright|macho", low, flags=re.IGNORECASE) or code.startswith("macho"):
            brand = "thermalright"
        elif code.startswith("acalp") or re.search(r"alpine", low, flags=re.IGNORECASE):
            brand = "arctic"
        elif re.search(r"montech", low, flags=re.IGNORECASE) or code.startswith("nx"):
            brand = "montech"
        elif re.search(r"alseye", low, flags=re.IGNORECASE):
            brand = "alseye"
        elif re.search(r"\blevante\b|levanteii", low, flags=re.IGNORECASE) or (code and "levante" in code):
            brand = "xpg"
        elif (code and str(code).upper().startswith("ACFRE")) or re.search(r"liquid\s+freezer|freezer\s+iii", low, flags=re.IGNORECASE):
            brand = "arctic"
        elif re.search(r"geometric|eskimo", low, flags=re.IGNORECASE) or (code and re.match(r"^1c", str(code), flags=re.IGNORECASE)):
            brand = "geometricfuture"
        elif re.search(r"\bsapphire\b|nitro\+", low, flags=re.IGNORECASE) or (code and str(code).upper().startswith("4n")):
            brand = "sapphire"

    tdp = ""
    m_tdp = re.search(r"\btdp\s*(\d{2,4})\s*w\b", low, flags=re.IGNORECASE)
    if m_tdp and m_tdp.group(1):
        tdp = str(int(m_tdp.group(1)))
    if not tdp:
        m_tdp2 = re.search(r"\b(\d{2,4})\s*w\s*tdp\b", low, flags=re.IGNORECASE)
        if m_tdp2 and m_tdp2.group(1):
            tdp = str(int(m_tdp2.group(1)))

    color_map = {
        "black": [r"(?:^|[^a-z0-9])black(?=$|[^a-z0-9])", r"\bbk\b", r"черн"],
        "white": [r"(?:^|[^a-z0-9])white(?=$|[^a-z0-9])", r"\bwh\b", r"\bwh\s", r"бел"],
        "gray": [r"(?:^|[^a-z0-9])grey(?=$|[^a-z0-9])", r"(?:^|[^a-z0-9])gray(?=$|[^a-z0-9])", r"сер"],
    }
    colors = set()
    for color_key, patterns in color_map.items():
        for pat in patterns:
            if re.search(pat, low, flags=re.IGNORECASE):
                colors.add(color_key)
                break
    white = "white" in colors
    return {"brand": brand, "code": code, "tdp": tdp, "colors": colors, "white": white}


def _cooler_catalog_category_ok(raw_name):
    cat = normalize_catalog_category_name(infer_category(str(raw_name or "")))
    low = str(raw_name or "").lower()
    if cat == "Кулер":
        return True
    if cat == "Охлаждение":
        if re.search(r"кулер|для\s*процессора|cpu\s*cooler", low, flags=re.IGNORECASE):
            return True
        # СЖО / водянка для CPU в каталоге Onliner
        if re.search(
            r"жидкостн|\bсжо\b|водян|water\s*cool|all[\s\-]in[\s\-]one|"
            r"freezer|levante|dashflow|eskimo|frozen\s+|aqua\s+elite|"
            r"насос|\bpump\b|240mm|280mm|360mm|420mm|2x120mm|3x120mm|3x140mm",
            low,
            flags=re.IGNORECASE,
        ):
            return True
    return False


def _cooler_seed_rank(raw_name):
    cat = normalize_catalog_category_name(infer_category(str(raw_name or "")))
    if cat == "Кулер":
        base = 100
    elif cat == "Охлаждение":
        base = 50
    else:
        base = 0
    return base + min(80, len(str(raw_name or "")))


def _looks_like_cooler_name(text):
    raw = str(text or "").strip()
    if not raw:
        return False
    low = raw.lower()
    if re.search(r"\bкорпус\b|\bжестк|\bhdd\b", low, flags=re.IGNORECASE):
        return False
    if re.match(r"^\s*кулер\b", low, flags=re.IGNORECASE):
        return True
    if re.search(r"\bcpu\s*cooler\b|\bдля\s*процессора\b.*\bкулер\b", low, flags=re.IGNORECASE):
        return True
    return False


def _looks_like_liquid_cpu_cooling_name(text):
    """СЖО / AIO для процессора в прайсе (не вентиляторы в корпус)."""
    raw = str(text or "").strip()
    if not raw:
        return False
    low = raw.lower()
    if re.search(r"\bкорпус\b|\bжестк|\bhdd\b|\bssd\b", low, flags=re.IGNORECASE):
        return False
    if re.search(
        r"система\s+водяного\s+охлаждения|водяного\s+охлаждения\b|"
        r"\bсжо\b|жидкостн(?:ое|ая|ые)?\s+охлажден|водян(?:ое|ая|ые)?\s+охлажден",
        low,
        flags=re.IGNORECASE,
    ):
        return True
    if re.search(r"\ball[\s\-]in[\s\-]one\b|\baio\b|liquid\s+(freezer|cool)|water\s*cool", low, flags=re.IGNORECASE):
        return True
    if re.search(
        r"\b(dashflow|levante|eskimo|lightflow|liquid\s+freezer|freezer\s+iii)\b|"
        r"\bfrozen\s+(edge|horizon|infinity|magic|notte|prism|warframe)\b|"
        r"\baqua\s+elite\b|\bcore\s+matrix\b|\bturbo\s+right\b|\bnitro\+|"
        r"\bid[\s\-]*cooling\s+(sl|dashflow|dx|fx)\w",
        low,
        flags=re.IGNORECASE,
    ):
        return True
    return False


def _find_cooler_review_candidates(product_name, top_n=5):
    name = str(product_name or "").strip()
    if not name:
        return []
    local = _cooler_brand_model_key(name)
    local_code = str(local.get("code", "") or "").strip()
    local_brand = str(local.get("brand", "") or "").strip()
    local_tdp = str(local.get("tdp", "") or "").strip()
    local_is_liquid = _looks_like_liquid_cpu_cooling_name(name)
    if not local_code and not local_brand:
        return []

    pool = []
    try:
        with _db_connection() as conn:
            rows = []
            if local_code:
                rows.extend(conn.execute(
                    "SELECT ni.onliner_id, ni.raw_name, oc.url "
                    "FROM name_index ni "
                    "JOIN onliner_catalog oc ON oc.onliner_id = ni.onliner_id "
                    "WHERE replace(replace(replace(replace(lower(ni.raw_name), '-', ''), ' ', ''), '/', ''), '.', '') LIKE ? "
                    "LIMIT 200",
                    (f"%{local_code}%",),
                ).fetchall())
            if local_brand and len(rows) < 90:
                bt = {
                    "bequiet": ["be quiet", "bequiet"],
                    "idcooling": ["id-cooling", "id cooling", "idcooling"],
                    "xpg": ["xpg", "adata xpg"],
                    "geometricfuture": ["geometric future", "geometricfuture"],
                }.get(local_brand, [local_brand])
                for brand_q in bt:
                    rows.extend(conn.execute(
                        "SELECT ni.onliner_id, ni.raw_name, oc.url "
                        "FROM name_index ni "
                        "JOIN onliner_catalog oc ON oc.onliner_id = ni.onliner_id "
                        "WHERE lower(ni.raw_name) LIKE ? "
                        "LIMIT 220",
                        (f"%{brand_q.lower()}%",),
                    ).fetchall())
            seed_map = {}
            for oid, raw_name, url in rows:
                oid = normalize_onliner_id(oid)
                if not oid:
                    continue
                if not _cooler_catalog_category_ok(raw_name):
                    continue
                rank = _cooler_seed_rank(raw_name)
                prev = seed_map.get(oid)
                if prev is None or rank > prev[2]:
                    seed_map[oid] = (raw_name, url, rank)
            for oid, (raw_name, url, _) in seed_map.items():
                pool.append({"id": oid, "name": raw_name, "url": url, "score": 0.91, "source": "cooler_db_seed"})
    except Exception:
        pass

    for cand in db_find_top_candidates(name, top_n=25, min_score=0.10, allow_b2b=False):
        pool.append(cand)

    exact = db_find_exact_id_for_name(name)
    if exact:
        pool = [exact] + list(pool or [])

    items = []
    seen = set()
    for cand in pool:
        if not isinstance(cand, dict):
            continue
        cid = normalize_onliner_id(cand.get("id", ""))
        cname = str(cand.get("name", "") or "").strip()
        if not cid or not cname or cid in seen:
            continue
        if not _cooler_catalog_category_ok(cname):
            continue
        cl = cname.lower()
        if not local_is_liquid:
            if re.search(r"жидкостн|\bсжо\b|водян", cl, flags=re.IGNORECASE) and "кулер" not in cl:
                continue
        cand_c = _cooler_brand_model_key(cname)
        cand_code = str(cand_c.get("code", "") or "").strip()
        cand_brand = str(cand_c.get("brand", "") or "").strip()
        cand_tdp = str(cand_c.get("tdp", "") or "").strip()

        if local_brand and cand_brand and cand_brand != local_brand:
            continue
        nm_lc = _cooler_norm_article(cname)
        if local_code:
            if cand_code:
                if not _case_code_match(local_code, cand_code):
                    continue
            elif len(local_code) >= 5:
                # Parsed catalog code is often missing; avoid unrelated same-brand hits (e.g. Thermalright Assassin vs Macho).
                if local_code not in nm_lc:
                    if local_brand == "cryorig":
                        m_h = re.match(r"^crh(\d+)", local_code)
                        if not (m_h and f"h{m_h.group(1)}" in nm_lc):
                            continue
                    else:
                        continue
        local_colors = set(local.get("colors") or set())
        cand_colors = set(cand_c.get("colors") or set())
        if local_colors and cand_colors and not (local_colors & cand_colors):
            continue
        if local_tdp and cand_tdp:
            try:
                if abs(int(local_tdp) - int(cand_tdp)) > 25:
                    continue
            except Exception:
                pass

        score = max(float(cand.get("score", 0.0) or 0.0), 0.88)
        if local_code and cand_code and _case_code_match(local_code, cand_code):
            score = 0.999

        seen.add(cid)
        items.append({
            "id": cid,
            "name": cname,
            "url": str(cand.get("url", "") or "").strip(),
            "score": round(min(0.999, max(0.0, score)), 3),
            "source": str(cand.get("source", "cooler_db")).strip() or "cooler_db",
            "code": cand_code,
            "tdp": cand_tdp,
        })
    items.sort(key=lambda item: (
        -(float(item.get("score", 0.0) or 0.0)),
        str(item.get("name", "") or "").lower(),
    ))
    if local_code:
        exact_items = [item for item in items if _case_code_match(local_code, str(item.get("code", "") or "").strip())]
        if exact_items:
            items = exact_items
    return items[:max(1, int(top_n))]


def _looks_like_peripheral_name(text):
    raw = str(text or "").strip()
    if not raw:
        return False
    low = raw.lower()
    return bool(re.search(
        r"клавиатур|keyboard|мышь|мыши|mouse|гарнитур|наушник|headset|headphones|колонк|акустик|speaker|soundbar",
        low,
        flags=re.IGNORECASE,
    ))


def _peripheral_catalog_category_ok(raw_name):
    rn = str(raw_name or "").strip()
    if not rn:
        return False
    try:
        cat = normalize_catalog_category_name(infer_category(rn))
    except Exception:
        cat = ""
    if cat in {"Клавиатура", "Мышь", "Наушники", "Акустика"}:
        return True
    return _looks_like_peripheral_name(rn)


def _find_peripheral_review_candidates(product_name, top_n=5):
    name = str(product_name or "").strip()
    if not name:
        return []
    pool = []
    exact = db_find_exact_id_for_name(name)
    if exact:
        pool.append(exact)
    pool.extend(db_find_top_candidates(name, top_n=25, min_score=0.18, allow_b2b=False))
    items = []
    seen = set()
    for cand in pool:
        if not isinstance(cand, dict):
            continue
        cid = normalize_onliner_id(cand.get("id", ""))
        cname = str(cand.get("name", "") or "").strip()
        if not cid or not cname or cid in seen:
            continue
        if not _peripheral_catalog_category_ok(cname):
            continue
        score = float(cand.get("score", 0.0) or 0.0)
        if score < 0.25 and not str(cand.get("source", "")).startswith("exact"):
            continue
        seen.add(cid)
        items.append({
            "id": cid,
            "name": cname,
            "url": str(cand.get("url", "") or "").strip(),
            "score": round(min(0.999, max(0.0, score)), 3),
            "source": str(cand.get("source", "peripheral_db")).strip() or "peripheral_db",
        })
    items.sort(key=lambda item: (
        -(float(item.get("score", 0.0) or 0.0)),
        str(item.get("name", "") or "").lower(),
    ))
    return items[:max(1, int(top_n))]


def _looks_like_case_name(text):
    raw = str(text or "").strip()
    if not raw:
        return False
    low = raw.lower()
    if re.match(r"^\s*корпус\b", low, flags=re.IGNORECASE):
        return True
    # Typical case descriptors in full RU/EN names
    case_markers = [
        r"tempered\s*glass",
        r"vga\s*max",
        r"cpu\s*max",
        r"(?:^|[^a-z0-9])mesh(?=$|[^a-z0-9])",
        r"(?:^|[^a-z0-9])без\s*б/?п(?=$|[^a-z0-9])",
        r"(?:^|[^a-z0-9])mini-?itx(?=$|[^a-z0-9])",
        r"(?:^|[^a-z0-9])microatx(?=$|[^a-z0-9])",
        r"(?:^|[^a-z0-9])e-?atx(?=$|[^a-z0-9])",
    ]
    if any(re.search(p, low, flags=re.IGNORECASE) for p in case_markers):
        return True

    # Short catalog names without "Корпус": Brand + recognizable case model line
    brand_prefix = r"^\s*(deepcool|montech|gamemax|zalman|adata|xpg|aerocool|powercase|cougar|vicsone|segotep|project\s*x)\b"
    model_tokens = r"(cc560|ch780|ch160|matrexx|invader|defender|lander|valor\s*air|air\s*1000|king\s*95|x3\s*mesh|xr\s*wood|hs0[12]\s*pro|sky\s*one|sky\s*two|meshbox|dragon\s*knight|precision)"
    if re.search(brand_prefix, low, flags=re.IGNORECASE) and re.search(model_tokens, low, flags=re.IGNORECASE):
        return True
    return False


def search_onliner_candidates(local_name, category_name="", query="", limit=80, max_queries=4, timeout_sec=6):
    app_settings = load_app_settings()
    search_cfg = (app_settings.get("no_id_search") or {})
    name = str(local_name or "").strip()
    text_query = str(query or "").strip()
    headers = {"User-Agent": "Mozilla/5.0", "Accept": "application/json"}
    hints = _category_path_hints(category_name)
    limit = max(5, min(int(limit or search_cfg.get("max_candidates", 80) or 80), 150))
    max_queries = max(1, min(int(max_queries or search_cfg.get("max_queries", 4) or 4), 8))
    category_rules = get_no_id_category_rules(app_settings)
    category_rule = category_rules.get(str(category_name or "").strip(), {}) if isinstance(category_rules, dict) else {}
    require_category_hint = _coerce_bool(search_cfg.get("require_category_hint", False), default=False)
    if not name and not text_query:
        return []

    queries = []
    seen_queries = set()

    def add_query(value):
        q = str(value or "").strip()
        key = q.lower()
        if not q or key in seen_queries:
            return
        seen_queries.add(key)
        queries.append(q)

    if text_query:
        add_query(text_query)
    for q in _tgpc_pc_code_queries(name or text_query):
        add_query(q)
    if _is_tgpc_pc_name(name or text_query):
        for q in _tgpc_pc_code_queries(name or text_query):
            add_query(f"TGPC {q}")
            add_query(f"Компьютеры TGPC {q}")
    if _coerce_bool(search_cfg.get("prefer_paren_model", True), default=True):
        for q in _priority_model_queries(name):
            add_query(q)
    art = str(extract_article(name) or "").strip()
    if _coerce_bool(search_cfg.get("prefer_article_tokens", True), default=True):
        add_query(art)
    token_pool = list(_name_tokens(name)[:8])
    if not _coerce_bool(search_cfg.get("include_brand_token", True), default=True):
        brand = _preferred_brand_token(name)
        brand_norm = _normalize_compact_name(brand)
        token_pool = [tok for tok in token_pool if _normalize_compact_name(tok) != brand_norm]
    token_query = " ".join(token_pool).strip()
    add_query(token_query)
    if isinstance(category_rule, dict):
        add_query(category_rule.get("query_hint", ""))
    if name:
        add_query(name[:130])
    queries = queries[:max(1, int(max_queries or 1))]

    cache_key = f"{ID_REPLACE_QUERY_CACHE_VERSION}|{str(category_name or '').strip().lower()}|{(text_query or token_query or name[:80]).strip().lower()}|{int(limit)}"
    now_ts = int(time.time())
    with ID_REPLACE_QUERY_CACHE_LOCK:
        cached = ID_REPLACE_QUERY_CACHE.get(cache_key)
        if isinstance(cached, dict) and now_ts - int(cached.get("ts", 0)) <= ID_REPLACE_QUERY_CACHE_TTL:
            return list(cached.get("items") or [])[:limit]

    seen_ids = set()
    candidates = []

    def _push_candidate(item):
        if not isinstance(item, dict):
            return
        pid = normalize_onliner_id(item.get("id", ""))
        pname = str(item.get("name", "") or "").strip()
        if not pid or not pname or pid in seen_ids:
            return
        score = round(float(item.get("score", 0.0) or 0.0), 3)
        if score < 0.34:
            return
        payload = {
            "id": pid,
            "name": pname,
            "url": str(item.get("url", "") or "").strip(),
            "score": score,
            "source": str(item.get("source", "") or "api"),
            "reason": str(item.get("reason", "") or ""),
        }
        candidates.append(payload)
        seen_ids.add(pid)

    for c in onliner_b2b_search_candidates(name or text_query, category_name=category_name, limit=min(limit, 24)):
        _push_candidate(c)
        if len(candidates) >= limit:
            break

    local_models = _model_hint_tokens(name or text_query)
    local_paren_models = _model_hint_tokens(" ".join(_paren_chunks(name or text_query)))
    local_articles = _article_like_tokens(name or text_query)
    local_text = str(name or text_query).lower()
    local_has_special_edition = bool(re.search(r"\b(xbox|playstation|usb)\b", local_text))
    local_is_tgpc_pc = _is_tgpc_pc_name(name or text_query)
    local_tgpc_code = _extract_tgpc_pc_code(name or text_query) if local_is_tgpc_pc else ""
    for q in queries:
        try:
            rs = onliner_api_get(
                f"https://catalog.api.onliner.by/search/products?query={quote(q)}",
                timeout=max(2, int(timeout_sec or 6)),
                headers=headers,
            )
            if not rs.ok:
                continue
            data = rs.json() or {}
            products = data.get("products") or []
        except Exception:
            continue
        for p in products[:40]:
            pid = normalize_onliner_id(p.get("id", ""))
            pname = str(p.get("full_name") or p.get("name") or "").strip()
            purl = str(p.get("html_url") or "").strip()
            if not pid or not pname or pid in seen_ids:
                continue
            allowed, _reason = _strict_candidate_allowed(name or text_query, pname)
            if not allowed:
                continue
            if require_category_hint and hints and purl and not any(h in purl for h in hints):
                continue
            cmp = calc_name_match(name or text_query, pname)
            score = float(cmp.get("score", 0.0) or 0.0)
            candidate_models = _model_hint_tokens(pname)
            candidate_articles = _article_like_tokens(pname)
            paren_hits = _token_family_match(local_paren_models, candidate_models)
            if cmp.get("match"):
                score = max(score, 0.74)
            if paren_hits:
                score = max(score, 0.95)
            if local_articles and candidate_articles and not _token_family_match(local_articles, candidate_articles):
                score = min(score, 0.18)
            elif local_models and candidate_models and not _token_family_match(local_models, candidate_models):
                score *= 0.62
            candidate_lower = pname.lower()
            if local_is_tgpc_pc:
                candidate_is_tgpc = "tgpc" in candidate_lower
                candidate_is_pc_url = bool(purl and any(h in purl for h in ["/desktop/", "/computer/", "/tgpc/"]))
                if not candidate_is_tgpc and not candidate_is_pc_url:
                    continue
                if not candidate_is_tgpc:
                    score *= 0.70
                if not candidate_is_pc_url:
                    score *= 0.80
                # Если знаем конкретный код конфигурации (91479, 91482, ...),
                # кандидат без этого кода в названии/URL — ненадёжный вариант.
                if local_tgpc_code:
                    cand_code = _extract_tgpc_pc_code(pname)
                    if cand_code and cand_code != local_tgpc_code:
                        # Код есть, но другой — точно не тот товар
                        score = min(score, 0.12)
                    elif not cand_code and local_tgpc_code not in (pname + purl):
                        # Кода нет нигде — возможно общая страница серии, не конкретная конфиг
                        score = min(score, 0.68)
            if isinstance(category_rule, dict):
                must_contain = [str(x).strip().lower() for x in (category_rule.get("must_contain") or []) if str(x).strip()]
                ignore_words = [str(x).strip().lower() for x in (category_rule.get("ignore_words") or []) if str(x).strip()]
                if must_contain and not any(x in candidate_lower for x in must_contain):
                    score *= 0.55
                if ignore_words and any(x in candidate_lower for x in ignore_words):
                    score *= 0.35
            candidate_has_special_edition = bool(re.search(r"\b(xbox|playstation|usb)\b", candidate_lower))
            candidate_has_color = bool(re.search(r"\((черный|чёрный|белый|зеленый|зелёный|розовый|синий|красный|yellow|pink|white|black|green|blue)\)", candidate_lower))
            if not local_has_special_edition and candidate_has_special_edition:
                score *= 0.78
            elif not local_has_special_edition and candidate_has_color:
                score = max(score, score + 0.03)
            if hints and purl and not any(h in purl for h in hints):
                score *= 0.78
            if score < 0.34:
                continue
            _push_candidate({
                "id": pid,
                "name": pname,
                "url": purl,
                "score": round(float(score), 3),
                "source": "api",
                "reason": str(cmp.get("reason", "") or ""),
            })
        if len(candidates) >= limit:
            break

    candidates.sort(key=lambda x: float(x.get("score", 0.0) or 0.0), reverse=True)
    top_score = float(candidates[0].get("score", 0.0) or 0.0) if candidates else 0.0
    if top_score >= 0.90:
        min_score = 0.52
    elif top_score >= 0.78:
        min_score = 0.46
    else:
        min_score = 0.40
    final_items = [c for c in candidates if float(c.get("score", 0.0) or 0.0) >= min_score][:limit]
    with ID_REPLACE_QUERY_CACHE_LOCK:
        ID_REPLACE_QUERY_CACHE[cache_key] = {"ts": now_ts, "items": final_items}
        if len(ID_REPLACE_QUERY_CACHE) > 400:
            keys = list(ID_REPLACE_QUERY_CACHE.keys())[:120]
            for k in keys:
                ID_REPLACE_QUERY_CACHE.pop(k, None)
    return final_items


def _name_tokens(text):
    words = re.findall(r"[a-zа-я0-9]+", str(text or "").lower())
    stop = {
        "для", "с", "и", "на", "по", "ret", "rtl", "oem", "box",
        "black", "white", "blue", "red", "green", "grey", "gray", "silver", "gold",
        "черный", "чёрный", "белый", "синий", "голубой", "красный", "зеленый", "зелёный",
        "серый", "серебристый", "золотой", "желтый", "жёлтый", "orange", "pink", "purple",
    }
    out = []
    seen = set()

    def _push(token):
        token = str(token or "").strip()
        if len(token) < 3 or token in stop or token in seen:
            return
        seen.add(token)
        out.append(token)

    for w in words:
        _push(w)
        m = re.match(r"^(\d{2,4})(w|mhz|gb|tb)$", w)
        if m:
            _push(m.group(1))
            continue
        m = re.match(r"^(\d{2,4})(mm)$", w)
        if m:
            _push(m.group(1))
    return out


def _normalize_compact_name(text):
    return re.sub(r"[^a-zа-я0-9]+", "", str(text or "").lower())


def _normalize_match_text(text):
    raw = str(text or "").strip().lower()
    if not raw:
        return ""
    raw = re.sub(r"\(([^()]*)\)", lambda m: " " if _is_color_only_chunk(m.group(1)) else f" {m.group(1)} ", raw)
    raw = re.sub(r"[^a-zа-я0-9]+", " ", raw)
    return re.sub(r"\s+", " ", raw).strip()


def _paren_chunks(text):
    return [str(x).strip() for x in re.findall(r"\(([^()]*)\)", str(text or "")) if str(x).strip()]


def _is_color_only_chunk(text):
    words = [w for w in re.findall(r"[a-zа-я0-9]+", str(text or "").lower()) if len(w) >= 3]
    if not words:
        return False
    return all(w in {
        "black", "white", "blue", "red", "green", "grey", "gray", "silver", "gold",
        "черный", "чёрный", "белый", "синий", "голубой", "красный", "зеленый", "зелёный",
        "серый", "серебристый", "золотой", "желтый", "жёлтый", "orange", "pink", "purple",
    } for w in words)


def _model_hint_tokens(text):
    raw = str(text or "")
    out = set()
    for token in _article_like_tokens(raw):
        out.add(token)
    for chunk in _paren_chunks(raw):
        for token in re.findall(r"[A-Za-z0-9][A-Za-z0-9._\-/]{3,}", chunk):
            norm = _normalize_compact_name(token)
            upper = norm.upper()
            if (len(norm) >= 4
                    and any(ch.isdigit() for ch in norm)
                    and any(ch.isalpha() for ch in norm)
                    and not re.match(r'^\d+[xX][A-Z]', upper)
                    and not _is_spec_code(upper)):
                out.add(norm)
    for token in _name_tokens(raw):
        if len(token) >= 4 and any(ch.isdigit() for ch in token) and any(ch.isalpha() for ch in token):
            upper = token.upper()
            if not re.match(r'^\d+[xX][A-Z]', upper) and not _is_spec_code(upper):
                out.add(token)
    refined = set()
    for token in out:
        refined.add(token)
        m = re.match(r"([a-z]{1,5}\d{2,5}[a-z]{0,3})", token)
        if m:
            refined.add(m.group(1))
    return refined


def _token_family_match(left_tokens, right_tokens):
    a = set(left_tokens or [])
    b = set(right_tokens or [])
    if not a or not b:
        return set()
    hits = set()
    for left in a:
        for right in b:
            if left == right:
                hits.add(left)
                continue
            short, long = (left, right) if len(left) <= len(right) else (right, left)
            if len(short) >= 8 and long.startswith(short):
                hits.add(short)
    return hits


def _capacity_tokens(text):
    raw = _normalize_match_text(text)
    hits = set()
    for num, unit in re.findall(r"(\d+(?:[\.,]\d+)?)\s*(tb|gb)", raw):
        norm_num = str(num).replace(",", ".").rstrip("0").rstrip(".")
        hits.add(f"{norm_num}{unit}")
    return hits


def _important_name_tokens(text):
    raw = str(text or "").strip().lower()
    if not raw:
        return []
    generic = {
        "беспроводная", "беспроводной", "беспроводное", "wireless",
        "проводная", "проводной", "wired",
        "игровая", "игровой", "gaming",
        "гарнитура", "наушники", "headset", "headphones",
        "мышь", "мышка", "mouse",
        "клавиатура", "keyboard",
        "колонки", "колонка", "акустика", "speakers",
        "черный", "чёрный", "белый", "синий", "красный", "зеленый", "зелёный",
        "black", "white", "blue", "red", "green", "grey", "gray", "pink", "purple",
    }
    tokens = []
    seen = set()
    for token in re.findall(r"[a-zа-я0-9]+", raw):
        if len(token) < 3:
            continue
        if token in generic:
            continue
        if token.isdigit():
            continue
        if token in seen:
            continue
        seen.add(token)
        tokens.append(token)
    return tokens


def _ordered_token_hits(left_tokens, right_tokens):
    right_set = set(right_tokens or [])
    return [tok for tok in (left_tokens or []) if tok in right_set]


# Color groups: each group contains all equivalent color tokens (EN + RU).
# The first element of each tuple is the canonical key.
_COLOR_GROUPS = [
    ("black",     {"black", "черный", "чёрный", "черн", "blk"}),
    ("white",     {"white", "белый", "wht"}),
    ("silver",    {"silver", "grey", "gray", "серый", "серебристый", "серебро"}),
    ("blue",      {"blue", "синий", "голубой", "dark blue"}),
    ("red",       {"red", "красный"}),
    ("green",     {"green", "зеленый", "зелёный"}),
    ("gold",      {"gold", "золотой", "золото"}),
    ("yellow",    {"yellow", "желтый", "жёлтый"}),
    ("orange",    {"orange", "оранжевый"}),
    ("purple",    {"purple", "violet", "фиолетовый"}),
    ("pink",      {"pink", "розовый"}),
    ("brown",     {"brown", "коричневый"}),
]
# Flat map: any color word → canonical key
_COLOR_CANON = {}
for _ckey, _cset in _COLOR_GROUPS:
    for _cv in _cset:
        _COLOR_CANON[_cv] = _ckey


def _color_tokens(text):
    """Return set of CANONICAL color keys found in text (cross-language)."""
    raw = re.sub(r"[^a-zа-яёa-z0-9]+", " ", str(text or "").lower())
    hits = set()
    for word, canon in _COLOR_CANON.items():
        if f" {word} " in f" {raw} ":
            hits.add(canon)
    return hits


# ── Product category detection ────────────────────────────────────────────
# Each group is a frozenset of all known forms (RU abbreviation, RU full, EN).
# The matching key is the frozenset itself — two names share a category only
# if they map to the same frozenset.
_CATEGORY_GROUPS = [
    frozenset(["бп", "блок питания", "блоки питания", "psu", "power supply"]),
    frozenset(["ибп", "ибп ", "источник бесперебойного питания", "ups"]),
    frozenset(["мфу", "мфу ", "multifunctional", "принтер-сканер"]),
    frozenset(["принтер", "printer"]),
    frozenset(["сканер", "scanner"]),
    frozenset(["монитор", "monitor", "дисплей", "display"]),
    frozenset(["клавиатура", "keyboard"]),
    frozenset(["мышь", "мышка", "mouse"]),
    frozenset(["гарнитура", "наушники", "headset", "headphones"]),
    frozenset(["колонки", "колонка", "акустика", "speakers"]),
    frozenset(["корпус", "case"]),
    frozenset(["кулер", "cooler", "охладитель", "cooling"]),
    frozenset(["видеокарта", "видеоадаптер", "gpu", "graphics"]),
    frozenset(["процессор", "cpu", "processor"]),
    frozenset(["материнская плата", "материнка", "motherboard", "mainboard"]),
    frozenset(["оперативная память", "память ddr", "озу", "ram"]),
    frozenset(["ssd", "ссд накопитель", "твердотельный накопитель"]),
    frozenset(["hdd", "жесткий диск", "жёсткий диск", "hdd накопитель"]),
    frozenset(["ноутбук", "laptop", "notebook"]),
    frozenset(["планшет", "tablet"]),
    frozenset(["смартфон", "телефон", "phone", "smartphone"]),
    frozenset(["кабель", "cable", "шнур", "провод"]),
    frozenset(["разветвитель", "разветвитель для", "хаб", "hub", "splitter",
               "usb-хаб", "usb хаб", "usb-hub"]),
    frozenset(["dok-stantsiya", "dok stantsiya", "dock station", "док-станция", "док станция"]),
    frozenset(["адаптер", "adapter", "переходник", "конвертер"]),
    frozenset(["флеш", "флешка", "usb накопитель", "usb flash"]),
    frozenset(["роутер", "маршрутизатор", "router", "wi-fi роутер"]),
    frozenset(["коммутатор", "switch", "свитч"]),
    frozenset(["зарядное устройство", "сзу", "зарядка", "charger"]),
    frozenset(["источник питания", "блок питания для ноутбука"]),
    frozenset(["стабилизатор", "стабилизатор напряжения"]),
    frozenset(["термопаста", "термопрокладка", "thermal paste"]),
    frozenset(["кронштейн", "крепление", "bracket", "mount"]),
    frozenset(["внешний накопитель", "внешний жесткий диск", "portable hdd"]),
    frozenset(["картридж", "cartridge", "тонер"]),
    frozenset(["веб-камера", "вебкамера", "webcam"]),
    frozenset(["микрофон", "microphone"]),
    frozenset(["удлинитель", "сетевой фильтр", "surge protector"]),
    frozenset(["охлаждающая подставка", "подставка для ноутбука"]),
    frozenset(["кулер для процессора", "процессорный кулер"]),
    frozenset(["система охлаждения", "водяное охлаждение", "сжо", "aio cooler"]),
    frozenset(["вентилятор", "fan", "корпусной вентилятор"]),
    frozenset(["память", "модуль памяти"]),  # generic fallback for RAM
]
# Flat map: normalized-lowercase form → frozenset (canonical group)
_CATEGORY_LOOKUP = {}
for _grp in _CATEGORY_GROUPS:
    for _form in _grp:
        _CATEGORY_LOOKUP[_form.strip().lower()] = _grp


def _extract_product_category(name: str):
    """Return the canonical category frozenset for name, or None if unknown.

    Tries to match the first 1–3 words against the category map.
    """
    raw = str(name or "").strip().lower()
    # Try progressively shorter prefixes: 3 words, 2 words, 1 word
    words = re.split(r"[\s,.(]+", raw)
    for n in (3, 2, 1):
        prefix = " ".join(words[:n])
        if prefix in _CATEGORY_LOOKUP:
            return _CATEGORY_LOOKUP[prefix]
    return None


_SPEC_CODE_PREFIXES = {
    # Storage/RAID connector standards — not unique product articles
    "SFF", "SAS", "SATA", "SASATA",
    # PCIe form-factor codes
    "PCIE", "PCIEX", "M2",
    # USB / display standards
    "USB", "HDMI", "DISPLAYPORT", "THUNDERBOLT",
    # Memory standards
    "DIMM", "SODIMM", "DDR", "LPDDR",
    # Power connector codes
    "ATX", "EPS",
    # NVMe / NVME
    "NVME",
    # Common spec-only prefixes that appear in cable/adapter names
    "OCU", "OCULINK",
}


def _is_spec_code(norm_upper: str) -> bool:
    """Return True if the token is a well-known interface/spec code rather than a unique article."""
    for prefix in _SPEC_CODE_PREFIXES:
        if norm_upper.startswith(prefix):
            return True
    return False


def _raw_paren_article_tokens(text):
    """Extract vendor article codes from parentheses — RAM/GPU/SSD (e.g. (KF436C16RB12/16), (KF560C36BBE-32))."""
    raw_text = str(text or "")
    found = []
    seen = set()
    # Must start with a letter; mix of letters+digits; ≥6 chars — skips (Black), (OEM), (52502)
    for m in re.finditer(
        r"\(\s*([A-Za-z][A-Za-z0-9.\-/]{5,})\s*\)",
        raw_text,
    ):
        tok = m.group(1).strip()
        if not (any(ch.isdigit() for ch in tok) and any(ch.isalpha() for ch in tok)):
            continue
        norm = _normalize_compact_name(tok)
        if _is_spec_code(norm.upper()):
            continue
        if norm in seen:
            continue
        seen.add(norm)
        found.append(tok)
    return found


def _raw_search_tokens(text):
    """Return raw tokens (preserving hyphens/dots) for SQL LIKE queries.
    E.g. 'GK-240L' stays 'GK-240L' so LIKE '%GK-240L%' can match the DB entry."""
    raw_text = str(text or "")
    out = []
    seen_norm = set()
    # Highest priority: explicit article in parentheses (ОЗУ Kingston и т.п.)
    for tok in _raw_paren_article_tokens(raw_text):
        norm = _normalize_compact_name(tok)
        if norm not in seen_norm:
            seen_norm.add(norm)
            out.append(tok)
    for t in re.findall(r"[A-Za-z0-9][A-Za-z0-9._\-/]{3,}", raw_text):
        if not (any(ch.isdigit() for ch in t) and any(ch.isalpha() for ch in t)):
            continue
        upper = t.upper()
        norm = _normalize_compact_name(t)
        if _is_spec_code(norm.upper()):
            continue
        if re.match(r'^\d+[xX][A-Z]', upper):
            continue
        m = re.match(r'^\d+([A-Z]{1,4})$', upper)
        if m and m.group(1) in {"MM", "CM", "W", "V", "A", "MHZ", "GHZ", "DPI", "HZ", "RPM"}:
            continue
        if norm not in seen_norm:
            seen_norm.add(norm)
            out.append(t)  # keep original form with hyphens
    # Also add GPU combined tokens (raw, so "RX5700XT" still fine for LIKE)
    for gm in re.finditer(
        r"\b(RX|GTX|RTX|R[0-9]|HD|VEGA|ARC)\s+(\d{3,4})\s*(XT|Ti|TI|XTX|SUPER|M|S)?\b",
        raw_text, re.IGNORECASE
    ):
        combined = gm.group(1) + gm.group(2) + (gm.group(3) or "")
        if combined not in out:
            out.append(combined)
    return out[:5]


def _article_like_tokens(text):
    raw = str(text or "")
    out = set()

    _MEASURE_UNITS = {"MM", "CM", "M", "W", "V", "A", "MHZ", "GHZ",
                      "TB", "GB", "MB", "KB", "DPI", "PPI", "RPM", "HZ",
                      "BIT", "BITS", "MS", "NM", "G", "KG", "LM"}

    def _add(norm):
        upper = norm.upper()
        # Reject multiplier-connector patterns: 1xUSB, 2xHDMI, 4xSATA
        if re.match(r'^\d+[xX][A-Z]', upper):
            return
        # Reject pure-measurement tokens: 350MM, 1200DPI, 120MM, 450W
        m = re.match(r'^\d+([A-Z]{1,4})$', upper)
        if m and m.group(1) in _MEASURE_UNITS:
            return
        if (len(norm) >= 5
                and any(ch.isdigit() for ch in norm)
                and any(ch.isalpha() for ch in norm)
                and not _is_spec_code(upper)):
            out.add(norm)

    for tok in _raw_paren_article_tokens(raw):
        _add(_normalize_compact_name(tok))

    for q in _tgpc_pc_code_queries(raw):
        _add(_normalize_compact_name(q))
    for t in extract_article_candidates(raw):
        _add(_normalize_compact_name(t))
    for t in re.findall(r"[A-Za-z0-9][A-Za-z0-9._\-/]{3,}", raw):
        _add(_normalize_compact_name(t))
        # Also try each segment of hyphenated compound codes:
        # "DP-ATX-MATREXX55-V3" → also add "MATREXX55" as a token
        segs = re.split(r'[-/]', t)
        if len(segs) > 1:
            for seg in segs:
                _add(_normalize_compact_name(seg))

    # Склеиваем GPU-паттерны: "RX 5700 XT" → "RX5700XT", "GTX 1660 Super" → "GTX1660SUPER"
    for m in re.finditer(
        r"\b(RX|GTX|RTX|R[0-9]|HD|VEGA|ARC)\s+(\d{3,4})\s*(XT|Ti|TI|XTX|SUPER|M|S)?\b",
        raw, re.IGNORECASE
    ):
        combined = m.group(1) + m.group(2) + (m.group(3) or "")
        _add(_normalize_compact_name(combined))

    # Процессорные суффиксы: "5600X", "5900X", "i9-13900K" — короткие коды в скобках
    for m in re.finditer(r"\b([A-Za-z]{1,4}[-]?\d{3,5}[A-Za-z]{0,3})\b", raw):
        _add(_normalize_compact_name(m.group(1)))

    return out


def _extract_tgpc_pc_code(text):
    """
    Извлекает 5-значный числовой код конфигурации TGPC ПК (напр. '91479' из '91479 I-X').
    Каждый код уникален — разные коды означают разные товары.
    """
    raw = str(text or "").strip()
    for pat in [
        r"\b(\d{4,6})\s+[A-ZА-Яa-zа-я]-[Xx]\b",
        r"\((\d{4,6})\s+[A-ZА-Яa-zа-я]-[Xx]\)",
        r"\b(\d{4,6})[A-ZА-Яa-zа-я]-[Xx]\b",
    ]:
        m = re.search(pat, raw, flags=re.IGNORECASE)
        if m:
            return m.group(1).strip()
    return ""


def _extract_gpu_model(text):
    """
    Извлекает нормализованный токен видеокарты из названия ПК.
    Пример: 'RTX 5070Ti 16Gb' → 'RTX5070TI', 'RTX 5060 8Gb' → 'RTX5060'.
    """
    raw = _normalize_match_text(str(text or ""))
    # NVIDIA RTX/GTX
    m = re.search(r"\b(rtx|gtx)\s*(\d{3,4})\s*(ti|super)?\b", raw, flags=re.IGNORECASE)
    if m:
        parts = [m.group(1).upper(), m.group(2)]
        if m.group(3):
            parts.append(m.group(3).upper())
        return "".join(parts)
    # AMD RX/Radeon
    m = re.search(r"\b(rx)\s*(\d{3,4})\s*(xt|gre|xtx)?\b", raw, flags=re.IGNORECASE)
    if m:
        parts = ["RX", m.group(2)]
        if m.group(3):
            parts.append(m.group(3).upper())
        return "".join(parts)
    # Intel Arc
    m = re.search(r"\barc\s+(a\d{3})\b", raw, flags=re.IGNORECASE)
    if m:
        return "ARC" + m.group(1).upper()
    return ""


def calc_name_match(local_name, onliner_name):
    a = str(local_name or "").strip()
    b = str(onliner_name or "").strip()
    if not a or not b:
        return {"score": 0.0, "match": False, "reason": "no_name"}

    # ── Категорийный барьер: разные категории → сразу 0 ─────────────────────
    cat_a = _extract_product_category(a)
    cat_b = _extract_product_category(b)
    if cat_a and cat_b and cat_a != cat_b:
        return {"score": 0.02, "match": False, "reason": "category_mismatch"}
    # ─────────────────────────────────────────────────────────────────────────

    # ── TGPC ПК: строгая проверка по уникальному коду конфигурации ──────────
    local_tgpc_code = _extract_tgpc_pc_code(a)
    onl_tgpc_code = _extract_tgpc_pc_code(b)
    if local_tgpc_code:
        if onl_tgpc_code:
            if local_tgpc_code != onl_tgpc_code:
                # Разные коды → гарантированно разные товары
                return {"score": 0.04, "match": False, "reason": "tgpc_code_mismatch"}
            # Коды совпали — дополнительно сверяем GPU
            local_gpu = _extract_gpu_model(a)
            onl_gpu = _extract_gpu_model(b)
            if local_gpu and onl_gpu and local_gpu != onl_gpu:
                # Один и тот же код, но GPU отличается — Onliner вернул похожую конфигурацию
                return {"score": 0.35, "match": False, "reason": "tgpc_gpu_mismatch"}
            return {"score": 1.0, "match": True, "reason": "tgpc_code_exact"}
        # Наш код есть, у Onliner-продукта кода нет — неоднозначно,
        # не блокируем, но потолок оценки будет ограничен ниже
    # ────────────────────────────────────────────────────────────────────────

    art_local = str(extract_article(a) or "").upper()
    art_onl = str(extract_article(b) or "").upper()
    if art_local and art_onl and art_local == art_onl:
        return {"score": 1.0, "match": True, "reason": "article"}

    # Часто артикулы не в скобках, поэтому отдельно проверяем article-like токены.
    local_article_like = _article_like_tokens(a)
    onl_article_like = _article_like_tokens(b)
    if local_article_like and onl_article_like and (local_article_like & onl_article_like):
        local_capacity = _capacity_tokens(a)
        onl_capacity = _capacity_tokens(b)
        local_colors = _color_tokens(a)
        onl_colors = _color_tokens(b)
        score = 0.97
        if local_capacity and onl_capacity and not (local_capacity & onl_capacity):
            score = 0.83
        elif local_colors and onl_colors and (local_colors & onl_colors):
            score = 0.985
        return {"score": score, "match": True, "reason": "article_like"}

    local_paren_models = _model_hint_tokens(" ".join(_paren_chunks(a)))
    onl_paren_models = _model_hint_tokens(" ".join(_paren_chunks(b)))
    paren_intersection = _token_family_match(local_paren_models, onl_paren_models)
    if paren_intersection:
        return {"score": 0.94, "match": True, "reason": "paren_model"}

    local_models = _model_hint_tokens(a)
    onl_models = _model_hint_tokens(b)
    model_intersection = _token_family_match(local_models, onl_models)
    if model_intersection:
        base_score = 0.90 if local_paren_models and model_intersection.intersection(local_paren_models) else 0.84
        local_capacity = _capacity_tokens(a)
        onl_capacity = _capacity_tokens(b)
        local_colors = _color_tokens(a)
        onl_colors = _color_tokens(b)
        if local_capacity and onl_capacity and (local_capacity & onl_capacity):
            base_score = max(base_score, 0.92)
        if local_colors and onl_colors and (local_colors & onl_colors):
            base_score = min(0.97, base_score + 0.02)
        return {"score": base_score, "match": True, "reason": "model_token"}
    if local_article_like and onl_article_like and not _token_family_match(local_article_like, onl_article_like):
        return {"score": 0.18, "match": False, "reason": "article_conflict"}

    clean_a = _normalize_match_text(a)
    clean_b = _normalize_match_text(b)
    local_capacity = _capacity_tokens(clean_a)
    onl_capacity = _capacity_tokens(clean_b)
    local_colors = _color_tokens(clean_a)
    onl_colors = _color_tokens(clean_b)
    ta = set(_name_tokens(clean_a))
    tb = set(_name_tokens(clean_b))
    important_a = _important_name_tokens(a)
    important_b = _important_name_tokens(b)
    important_hits = _ordered_token_hits(important_a, important_b)
    brand_a = _normalize_compact_name(_preferred_brand_token(a))
    brand_b = _normalize_compact_name(_preferred_brand_token(b))
    same_brand = bool(brand_a and brand_b and brand_a == brand_b)
    overlap = 0.0
    if ta and tb:
        overlap = len(ta & tb) / max(1, min(len(ta), len(tb)))
    seq = SequenceMatcher(None, " ".join(sorted(ta))[:300], " ".join(sorted(tb))[:300]).ratio() if ta and tb else 0.0
    raw_seq = SequenceMatcher(None, clean_a[:260], clean_b[:260]).ratio()
    score = (0.58 * overlap) + (0.24 * seq) + (0.18 * raw_seq)

    # Легкий буст за вложенность нормализованных строк.
    ac = _normalize_compact_name(clean_a)
    bc = _normalize_compact_name(clean_b)
    if ac and bc and (ac in bc or bc in ac):
        score += 0.08
    if same_brand:
        score += 0.05
    if len(important_hits) >= 2:
        score += 0.12
        if same_brand:
            score += 0.08
    if len(important_hits) >= 3:
        score += 0.05
    if local_capacity and onl_capacity:
        if local_capacity & onl_capacity:
            score += 0.10
        else:
            score -= 0.14
    if local_colors and onl_colors and (local_colors & onl_colors):
        score += 0.03
    if local_models and onl_models and not model_intersection:
        score -= 0.22
    if local_paren_models and onl_models and not (local_paren_models & onl_models):
        score -= 0.10

    score = round(min(1.0, max(0.0, score)), 3)

    # Если у нашего товара есть TGPC-код, а у кандидата нет — снижаем потолок.
    # Такой кандидат может быть общей страницей серии, а не конкретной конфигурацией.
    if local_tgpc_code and not onl_tgpc_code:
        score = round(min(score, 0.72), 3)

    ok = score >= 0.64 or overlap >= 0.78 or raw_seq >= 0.84 or (same_brand and len(important_hits) >= 2 and score >= 0.72)
    reason = "tokens"
    if same_brand and len(important_hits) >= 2:
        reason = "brand_model_tokens"
    return {"score": float(score), "match": bool(ok), "reason": reason}


def _harden_base_verify_result(oid, local_name, verify_result):
    """
    Усилить проверку "Проверить база":
    если ID существует в каталоге, но название явно не совпадает,
    понижаем статус до unverified/mismatch.
    """
    result = dict(verify_result or {})
    status = str(result.get("status", "")).strip().lower()
    if status != "match":
        return result

    catalog_name = str(result.get("catalog_name", "")).strip()
    if not catalog_name:
        return result

    cmp = calc_name_match(local_name, catalog_name)
    cmp_score = float(cmp.get("score", 0.0) or 0.0)
    art_local = _article_like_tokens(local_name)
    art_catalog = _article_like_tokens(catalog_name)
    article_intersection = bool(art_local and art_catalog and (art_local & art_catalog))

    # Жесткая защита: если артикулами конфликтуют (оба есть, но пересечения нет), это mismatch.
    if art_local and art_catalog and not article_intersection:
        guessed = lookup_catalog_match_details(local_name)
        result.update({
            "status": "mismatch",
            "score": round(min(cmp_score, 0.49), 3),
            "catalog_id": str((guessed or {}).get("id", "")).strip() or str(result.get("catalog_id", "")).strip(),
            "catalog_name": str((guessed or {}).get("model", "")).strip() or catalog_name,
            "url": str((guessed or {}).get("url", "")).strip() or str(result.get("url", "")).strip(),
        })
        return result

    if article_intersection or cmp_score >= 0.62:
        result["score"] = max(float(result.get("score", 0.0) or 0.0), round(cmp_score, 3))
        return result

    if cmp_score >= 0.48:
        result.update({"status": "unverified", "score": round(cmp_score, 3)})
        return result

    guessed = lookup_catalog_match_details(local_name)
    result.update({
        "status": "mismatch",
        "score": round(cmp_score, 3),
        "catalog_id": str((guessed or {}).get("id", "")).strip() or str(result.get("catalog_id", "")).strip(),
        "catalog_name": str((guessed or {}).get("model", "")).strip() or catalog_name,
        "url": str((guessed or {}).get("url", "")).strip() or str(result.get("url", "")).strip(),
    })
    return result


def _openai_autosort_predict_category(product_name, categories, local_hint=""):
    """
    AI-предложение категории для спорных кейсов автосортировки.
    Возвращает (category, confidence, reason) или ("", 0.0, reason).
    """
    if not OPENAI_API_KEY:
        return "", 0.0, "no_api_key"

    name = str(product_name or "").strip()
    valid_categories = [str(c).strip() for c in (categories or []) if str(c).strip()]
    if not name or not valid_categories:
        return "", 0.0, "bad_input"

    cache_key = f"{name.lower()}|{'|'.join(sorted(valid_categories))}"[:1500]
    with AI_CATEGORY_CACHE_LOCK:
        cached = AI_CATEGORY_CACHE.get(cache_key)
    if isinstance(cached, dict):
        return (
            str(cached.get("category", "")).strip(),
            float(cached.get("confidence", 0.0) or 0.0),
            str(cached.get("reason", "cache")).strip() or "cache",
        )

    system_prompt = (
        "You are a strict product categorization assistant for IT goods. "
        "Pick ONLY one category from the provided list. "
        "If uncertain, return empty category with low confidence. "
        "Respond as compact JSON only."
    )
    user_prompt = {
        "task": "categorize_product",
        "product_name": name,
        "allowed_categories": valid_categories,
        "local_hint": str(local_hint or "").strip(),
        "output_format": {
            "category": "string from allowed_categories or empty",
            "confidence": "number 0..1",
            "reason": "short string",
        },
    }

    try:
        resp = requests.post(
            "https://api.openai.com/v1/chat/completions",
            headers={
                "Authorization": f"Bearer {OPENAI_API_KEY}",
                "Content-Type": "application/json",
            },
            json={
                "model": OPENAI_AUTOSORT_MODEL,
                "temperature": 0,
                "response_format": {"type": "json_object"},
                "messages": [
                    {"role": "system", "content": system_prompt},
                    {"role": "user", "content": json.dumps(user_prompt, ensure_ascii=False)},
                ],
            },
            timeout=OPENAI_AUTOSORT_TIMEOUT_SEC,
        )
        if not resp.ok:
            return "", 0.0, f"http_{resp.status_code}"
        payload = resp.json() or {}
        choices = payload.get("choices") or []
        if not choices:
            return "", 0.0, "no_choices"
        content = str((((choices[0] or {}).get("message") or {}).get("content") or "")).strip()
        if not content:
            return "", 0.0, "empty_content"
        parsed = json.loads(content)
        category = str((parsed or {}).get("category", "")).strip()
        confidence = float((parsed or {}).get("confidence", 0.0) or 0.0)
        reason = str((parsed or {}).get("reason", "ai")).strip() or "ai"

        if category not in valid_categories:
            category = ""
            confidence = 0.0
            reason = "category_out_of_allowed"
        confidence = max(0.0, min(1.0, confidence))

        to_cache = {"category": category, "confidence": confidence, "reason": reason}
        with AI_CATEGORY_CACHE_LOCK:
            AI_CATEGORY_CACHE[cache_key] = to_cache
            if len(AI_CATEGORY_CACHE) > 2500:
                # Детеминированная чистка хвоста кэша.
                for k in list(AI_CATEGORY_CACHE.keys())[:400]:
                    AI_CATEGORY_CACHE.pop(k, None)

        return category, confidence, reason
    except Exception:
        return "", 0.0, "exception"


def normalize_onliner_id(value):
    if value is None:
        return ""
    text = str(value).strip()
    if not text or text.lower() in {"nan", "none"}:
        return ""
    if text.endswith(".0"):
        text = text[:-2]
    return text


def _count_rows_without_onliner_id(df):
    if df is None or getattr(df, "empty", False):
        return 0
    if "OnlinerID" not in df.columns:
        return len(df)
    return sum(1 for _, row in df.iterrows() if not normalize_onliner_id(row.get("OnlinerID", "")))


def _count_rows_with_duplicate_onliner_id(df):
    if df is None or getattr(df, "empty", False):
        return 0
    if "OnlinerID" not in df.columns:
        return 0
    counts = {}
    for _, row in df.iterrows():
        oid = normalize_onliner_id(row.get("OnlinerID", ""))
        if not oid:
            continue
        counts[oid] = counts.get(oid, 0) + 1
    duplicate_ids = {oid for oid, cnt in counts.items() if cnt > 1}
    if not duplicate_ids:
        return 0
    return sum(1 for _, row in df.iterrows() if normalize_onliner_id(row.get("OnlinerID", "")) in duplicate_ids)


def reconcile_ids_from_catalog(df):
    """
    Принудительно сверить OnlinerID по All_Catalog.
    Если по названию найден однозначный ID и он отличается от текущего — исправить.
    """
    if "OnlinerID" not in df.columns:
        return 0
    if "Ссылка" not in df.columns:
        df["Ссылка"] = ""
    # В pandas string-dtype прямое присваивание числам/float может падать.
    df["OnlinerID"] = df["OnlinerID"].astype("object")
    corrected = 0
    for i, row in df.iterrows():
        name = str(row.get("Название", "")).strip()
        if not name:
            continue
        catalog_id, catalog_url = lookup_id_from_catalog_sheet(name)
        if not catalog_id:
            continue
        current_id = normalize_onliner_id(row.get("OnlinerID", ""))
        if current_id != str(catalog_id):
            df.at[i, "OnlinerID"] = str(catalog_id)
            if catalog_url:
                df.at[i, "Ссылка"] = catalog_url
            corrected += 1
        elif catalog_url and not str(row.get("Ссылка", "")).strip():
            df.at[i, "Ссылка"] = catalog_url
    return corrected


def enforce_catalog_consistency(df, session_dir=None):
    """
    Жесткая сверка с All_Catalog:
    - если найден однозначный CatalogID, он считается эталоном;
    - если текущий OnlinerID отличается — исправляем;
    - если есть артикул, но каталог не дал соответствие — ID очищаем (безопасный режим).
    Дополнительно пишет отчет по спорным строкам.
    """
    if "OnlinerID" not in df.columns:
        return {
            "checked": 0,
            "set_from_catalog": 0,
            "corrected_conflicts": 0,
            "cleared_unverified": 0,
            "report_rows": 0,
        }

    if "Ссылка" not in df.columns:
        df["Ссылка"] = ""
    df["OnlinerID"] = df["OnlinerID"].astype("object")

    checked = 0
    set_from_catalog = 0
    corrected_conflicts = 0
    cleared_unverified = 0
    report_rows = []

    for i, row in df.iterrows():
        name = str(row.get("Название", "")).strip()
        if not name:
            continue
        checked += 1
        current_id = normalize_onliner_id(row.get("OnlinerID", ""))
        article = get_article_from_name(name)
        catalog_id, catalog_url = lookup_id_from_catalog_sheet(name)

        if catalog_id:
            catalog_id = str(catalog_id).strip()
            if not current_id:
                df.at[i, "OnlinerID"] = catalog_id
                set_from_catalog += 1
            elif current_id != catalog_id:
                report_rows.append({
                    "row": int(i) + 2,
                    "supplier": str(row.get("Поставщик", "")).strip(),
                    "name": name,
                    "article": article,
                    "current_id": current_id,
                    "catalog_id": catalog_id,
                    "action": "corrected_to_catalog",
                })
                df.at[i, "OnlinerID"] = catalog_id
                corrected_conflicts += 1
            if catalog_url:
                df.at[i, "Ссылка"] = str(catalog_url).strip()
            continue

        # Нет однозначного соответствия в каталоге.
        # Безопасный режим: если у товара есть артикул, но каталог не подтверждает ID —
        # снимаем ID, чтобы не отправить неверную позицию в магазин.
        if article and current_id:
            report_rows.append({
                "row": int(i) + 2,
                "supplier": str(row.get("Поставщик", "")).strip(),
                "name": name,
                "article": article,
                "current_id": current_id,
                "catalog_id": "",
                "action": "cleared_unverified_no_catalog_match",
            })
            df.at[i, "OnlinerID"] = ""
            df.at[i, "Ссылка"] = ""
            cleared_unverified += 1

    if session_dir:
        session_dir = Path(session_dir)
        try:
            report_df = pd.DataFrame(report_rows)
            report_path = session_dir / "id_quality_report.csv"
            report_df.to_csv(report_path, index=False, encoding="utf-8-sig")
            summary = {
                "checked": checked,
                "set_from_catalog": set_from_catalog,
                "corrected_conflicts": corrected_conflicts,
                "cleared_unverified": cleared_unverified,
                "report_rows": len(report_rows),
                "report_file": str(report_path),
            }
            with open(session_dir / "id_quality_report.json", "w", encoding="utf-8") as f:
                json.dump(summary, f, ensure_ascii=False, indent=2)
        except Exception as e:
            print(f"Не удалось сохранить ID quality report: {e}")

    return {
        "checked": checked,
        "set_from_catalog": set_from_catalog,
        "corrected_conflicts": corrected_conflicts,
        "cleared_unverified": cleared_unverified,
        "report_rows": len(report_rows),
    }


def _normalize_name_key(name):
    text = str(name or "").strip().lower()
    text = re.sub(r"^\[[^\]]+\]\s*", "", text)
    text = re.sub(r"\s+", " ", text)
    # No truncation: avoid collisions between long similar names.
    return text


def build_item_category_keys(row):
    """Стабильные ключи для категорий: только имя (+поставщик), без oid/art."""
    keys = []
    name = str(row.get("Название", "")).strip()
    supplier = str(row.get("Поставщик", "")).strip().lower()
    name_key = _normalize_name_key(name)
    if supplier and name_key:
        keys.append(f"sname:{supplier}:{name_key}")
    if name_key:
        keys.append(f"name:{name_key}")
    # unique keep order
    seen = set()
    out = []
    for k in keys:
        if k and k not in seen:
            seen.add(k)
            out.append(k)
    return out


def build_item_category_key(row):
    keys = build_item_category_keys(row)
    return keys[0] if keys else ""


def infer_category(name):
    """Определить укрупненную категорию из названия товара."""
    text = str(name or "").strip().lower()
    if not text:
        return "Без категории"

    norm = re.sub(r"[^a-zа-я0-9\+\s\-]", " ", text)
    norm = re.sub(r"\s+", " ", norm).strip()

    # Корпуса с формулировкой "без БП" не должны попадать в "Блок питания".
    has_case_words = bool(re.search(r"\bкорпус\b|\bcase\b|midi[\s\-]?tower|mini[\s\-]?tower", norm))
    no_psu_hint = bool(re.search(r"\bбез\s+бп\b|\bбез\s+блока\s+питания\b|\bno\s*psu\b|\bwithout\s+psu\b", norm))
    if has_case_words:
        return "Корпус"

    # Явный приоритет для БП, чтобы бренды типа PcCooler не уводили в "Кулер".
    if re.search(r"\bбп\b|блок питания|power supply|\bpsu\b", norm) and not no_psu_hint:
        return "Блок питания"

    category_rules = [
        # 1. Системный блок / ПЭВМ — до Процессора и Матплаты
        ("Системный блок", [r"системный блок", r"\bпэвм\b", r"\btgpc\b",
                            r"iven\s+(?:office|home|pro|ultra)"]),
        # 2. Сумки/чехлы/аксессуары — до Ноутбука
        ("Аксессуары", [r"\bсумк", r"чехол для ноутбука", r"чехол для планшета",
                        r"laptop bag", r"notebook bag", r"laptop case", r"\bшасси\b",
                        r"подставка для ноутбука"]),
        # 3. Оперативная память — до Ноутбука
        #    (иначе "Оперативная память для ноутбука" -> Ноутбук)
        ("Оперативная память", [r"\bddr[345]\b", r"оперативн", r"\bram\b",
                                r"so[\s\-]?dimm", r"\bdimm\b"]),
        # 4. Охлаждение — до Ноутбука
        #    (иначе "Охлаждение для ноутбука" -> Ноутбук)
        ("Охлаждение", [r"жидкостн", r"\baio\b", r"water[\s\-]?cool",
                        r"система\s+водяного\s+охлаждения", r"водяного\s+охлаждения",
                        r"охлажд", r"термопаст", r"термопроклад",
                        r"водян", r"сжо", r"радиатор", r"вентилятор"]),
        # 5. Ноутбук — до Видеокарты/SSD,
        #    но (?<!для ) — чтобы "SSD для ноутбука" / "Память для ноутбука" НЕ попали сюда
        ("Ноутбук", [r"(?<!для )ноутбук", r"\blaptop\b", r"\bnotebook\b"]),
        # 6. Кулер — до Процессора (иначе «Кулер для процессора …» уходит в CPU из-за «процессора»)
        ("Кулер", [r"\bкулер\b", r"cooler"]),
        # 7. Процессор
        ("Процессор", [r"(?<!для\s)процессор", r"\bcpu\b", r"\bintel core\b", r"\bryzen\b"]),
        # 8. Материнская плата
        ("Материнская плата", [r"материн", r"\bmotherboard\b", r"\bmb\b",
                               r"\bb[34567]\d{2}m?\b", r"\bh[456]\d{2}m?\b",
                               r"\bz[67]\d{2}\b", r"\bx[45667]\d{2}\b",
                               r"\ba[3567]\d{2}\b"]),
        # 9. SSD / HDD
        ("SSD", [r"\bssd\b", r"nvme", r"m\.?2", r"твердотельн"]),
        ("Жесткий диск", [r"\bhdd\b", r"жестк", r"винчестер"]),
        # 10. Видеокарта
        ("Видеокарта", [r"видеокарт", r"\bgpu\b", r"geforce", r"radeon",
                        r"\brtx\b", r"\bgtx\b", r"\brx\s?\d{3,4}\b"]),
        # 11. Корпус / БП / Монитор
        ("Корпус", [r"\bкорпус\b", r"midi[\s\-]?tower", r"mini[\s\-]?tower",
                    r"full[\s\-]?tower"]),
        ("Блок питания", [r"\bбп\b", r"блок питания", r"power supply", r"\bpsu\b"]),
        ("Монитор", [r"монитор", r"display"]),
        ("Принтер и МФУ", [r"\bпринтер\b", r"\bпринтеры\b", r"\bмфу\b", r"\bmfp\b"]),
        # 12. Коврики — до Мыши
        #    (иначе "Коврик для мыши" -> Мышь из-за слова "мыши")
        ("Аксессуары", [r"коврик", r"mouse[\s\-]?pad", r"mousepad"]),
        # 13. Периферия
        ("Клавиатура", [r"клавиатур"]),
        ("Мышь", [r"\bмышь\b", r"\bмыши\b", r"\bmouse\b",
                  r"игровая мышь", r"беспроводная мышь"]),
        ("Наушники", [r"наушник", r"гарнитур"]),
        ("Акустика", [r"колонк", r"акустик", r"soundbar"]),
        # 14. Сеть / периферия
        ("Сеть", [r"роутер", r"маршрутизатор", r"коммутатор",
                  r"точка доступа", r"wifi", r"wi[\s\-]fi"]),
        ("Накопители USB", [r"\busb\b", r"flash", r"флеш", r"накопител"]),
        ("Кабели и переходники", [r"кабель", r"переходник", r"адаптер",
                                  r"патч[\s\-]?корд"]),
    ]

    for category_name, patterns in category_rules:
        for pattern in patterns:
            if re.search(pattern, norm):
                return category_name

    tokens = re.findall(r"[a-zа-я0-9\+\-]+", norm)
    if not tokens:
        return "Без категории"
    return tokens[0].upper()


def get_effective_category(row, overrides=None):
    if overrides is None:
        overrides = load_category_overrides()
    for key in build_item_category_keys(row):
        manual = str(overrides.get(key, "")).strip()
        if manual:
            return manual
    return infer_category(row.get("Название", ""))


def row_category(row, overrides=None):
    existing = str(row.get("Категория", "")).strip()
    if existing:
        return existing
    return get_effective_category(row, overrides)


def normalize_catalog_category_name(raw_name, available_categories=None):
    """
    Нормализация категории из All_Catalog (колонка A) к внутренним именам.
    """
    text = str(raw_name or "").strip()
    if not text:
        return ""
    available_categories = set(available_categories or [])
    low_to_real = {str(c).strip().lower(): str(c).strip() for c in available_categories if str(c).strip()}
    direct = low_to_real.get(text.lower())
    if direct:
        return direct

    aliases = {
        "процессоры": "Процессор",
        "процессор": "Процессор",
        "cpu": "Процессор",
        "кулеры": "Кулер",
        "кулер": "Кулер",
        "охлаждение": "Охлаждение",
        "сжо": "Охлаждение",
        "материнские платы": "Материнская плата",
        "материнская плата": "Материнская плата",
        "mb": "Материнская плата",
        "оперативная память": "Оперативная память",
        "ram": "Оперативная память",
        "ssd": "SSD",
        "жесткие диски": "Жесткий диск",
        "жесткий диск": "Жесткий диск",
        "hdd": "Жесткий диск",
        "видеокарты": "Видеокарта",
        "видеокарта": "Видеокарта",
        "gpu": "Видеокарта",
        "блоки питания": "Блок питания",
        "блок питания": "Блок питания",
        "бп": "Блок питания",
        "корпуса": "Корпус",
        "корпус": "Корпус",
        "мониторы": "Монитор",
        "монитор": "Монитор",
        "принтеры и мфу": "Принтер и МФУ",
        "принтеры": "Принтер и МФУ",
        "принтер": "Принтер и МФУ",
        "мфу": "Принтер и МФУ",
        "ноутбуки": "Ноутбук",
        "ноутбук": "Ноутбук",
        "системные блоки": "Системный блок",
        "системный блок": "Системный блок",
    }
    alias = aliases.get(text.lower(), "")
    if alias:
        if alias.lower() in low_to_real:
            return low_to_real.get(alias.lower(), alias)
        return alias

    inferred = infer_category(text)
    if inferred and inferred != "Без категории":
        if inferred.lower() in low_to_real:
            return low_to_real.get(inferred.lower(), inferred)
        return inferred
    return ""


def ensure_category_column(df, overrides=None):
    if overrides is None:
        overrides = load_category_overrides()
    df = df.copy()
    df["Категория"] = df.apply(lambda row: row_category(row, overrides), axis=1)
    return df


def normalize_consolidated_columns(df):
    if df is None or df.empty:
        return df
    rename_map = {}
    for col in df.columns:
        text = str(col)
        compact = text.replace("\xa0", "").strip()
        if text == "РќР°Р·РІР°РЅРёРµ":
            rename_map[col] = "Название"
        elif text == "Р¦РµРЅР°":
            rename_map[col] = "Цена"
        elif text == "РџРѕСЃС‚Р°РІС‰РёРє":
            rename_map[col] = "Поставщик"
        elif text == "Р“Р°СЂР°РЅС‚РёСЏ":
            rename_map[col] = "Гарантия"
        elif compact in {"РРЦ", "Р Р Р¦"} or text == "Р\xa0Р\xa0Р¦":
            rename_map[col] = "РРЦ"
    if rename_map:
        df = df.rename(columns=rename_map)
    return df


def round_price_to_90(value):
    """
    Округлить цену до ближайшего десятка.
    Последняя цифра 1-4 округляется вниз, 5-9 — вверх.
    Примеры: 302 -> 300, 441 -> 440, 583 -> 580, 1311 -> 1310
    """
    v = pd.to_numeric(value, errors="coerce")
    if pd.isna(v):
        return np.nan
    v = float(v)
    if v <= 0:
        return 0.0
    whole = int(math.floor(v))
    last_digit = whole % 10
    if last_digit <= 4:
        rounded = whole - last_digit
    else:
        rounded = whole + (10 - last_digit)
    return float(rounded)


def load_visibility_map(session_dir):
    _ = session_dir  # compatibility: visibility now global, not per-session
    if not CATEGORY_VISIBILITY_FILE.exists():
        return {}
    try:
        with open(CATEGORY_VISIBILITY_FILE, "r", encoding="utf-8") as f:
            data = json.load(f)
            if isinstance(data, dict):
                return data
    except Exception:
        pass
    return {}


def save_visibility_map(session_dir, visibility_map):
    _ = session_dir  # compatibility: visibility now global, not per-session
    with open(CATEGORY_VISIBILITY_FILE, "w", encoding="utf-8") as f:
        json.dump(visibility_map, f, ensure_ascii=False, indent=2)


def apply_saved_markups_to_df(df):
    if df.empty:
        return df
    markups = load_category_markups()
    if not markups:
        return df
    if "РРЦ" not in df.columns:
        df["РРЦ"] = ""
    if "Цена без скидки" not in df.columns:
        df["Цена без скидки"] = ""
    # Allow writing numeric RRC values regardless of source column dtype (str/string).
    df["РРЦ"] = df["РРЦ"].astype("object")
    df["Цена без скидки"] = df["Цена без скидки"].astype("object")
    overrides = load_category_overrides()
    for i, row in df.iterrows():
        category = row_category(row, overrides)
        if category not in markups:
            continue
        base_price = pd.to_numeric(row.get("Цена", np.nan), errors="coerce")
        if pd.isna(base_price):
            continue
        cfg = get_category_markup_config(markups, category)
        percent = cfg["percent"]
        threshold = cfg.get("threshold", 0.0)
        min_profit = cfg.get("min_profit", 0.0)
        calc_base = float(base_price)
        if cfg["base_mode"] in {"onliner_min", "onliner_avg", "onliner_max"}:
            oid = normalize_onliner_id(row.get("OnlinerID", ""))
            stats = get_onliner_market_stats_from_cache_only(oid) if oid else {}
            market_price = None
            if cfg["base_mode"] == "onliner_min":
                market_price = stats.get("min")
            elif cfg["base_mode"] == "onliner_avg":
                market_price = stats.get("avg")
            else:
                market_price = stats.get("max")
            if market_price is not None:
                calc_base = float(market_price)
        rrc, no_discount_price = calc_rrc_and_no_discount(
            calc_base,
            percent,
            threshold=threshold,
            min_profit=min_profit,
            no_discount_percent=cfg.get("no_discount_percent", 0.0),
        )
        df.at[i, "РРЦ"] = rrc
        df.at[i, "Цена без скидки"] = no_discount_price
    return df


def apply_visibility_filter(df, session_dir):
    if df.empty or "Поставщик" not in df.columns or "Название" not in df.columns:
        return df
    visibility_map = load_visibility_map(session_dir)
    if not visibility_map:
        return df

    overrides = load_category_overrides()
    mask = []
    for _, row in df.iterrows():
        supplier = str(row.get("Поставщик", "")).strip()
        category = row_category(row, overrides)
        hidden_for_supplier = set(visibility_map.get(supplier, []))
        mask.append(category not in hidden_for_supplier)
    return df[pd.Series(mask, index=df.index)].copy()


def _safe_json_value(value):
    if value is None:
        return ""
    try:
        if pd.isna(value):
            return ""
    except Exception:
        pass
    if isinstance(value, (np.floating, float)):
        fv = float(value)
        if not math.isfinite(fv):
            return ""
        return round(fv, 2)
    if isinstance(value, (np.integer, int)):
        return int(value)
    if isinstance(value, str):
        return value.strip()
    return value


def _delivery_days_from_row(row):
    value = row.get("\u0414\u043d\u0435\u0439 \u0434\u043e\u0441\u0442\u0430\u0432\u043a\u0438", row.get("\u041f\u043e\u0434 \u0437\u0430\u043a\u0430\u0437", "2"))
    value = _safe_json_value(value)
    text = str(value).strip()
    if not text:
        return "2"
    match = re.search(r"(\d+)", text)
    if match:
        return match.group(1)
    return text


def write_consolidated_json(df, json_path):

    cons_data = []
    for _, row in df.iterrows():
        cons_data.append([
            _safe_json_value(row.get("OnlinerID", "")),
            _safe_json_value(row.get("Название", "")),
            _safe_json_value(row.get("Цена", 0)),
            _safe_json_value(row.get("Поставщик", "")),
            _safe_json_value(row.get("Гарантия", "")),
            _delivery_days_from_row(row),
            _safe_json_value(row.get("РРЦ", "")),
            _safe_json_value(row.get("Цена без скидки", "")),
        ])
    with open(str(json_path), "w", encoding="utf-8") as f:
        json.dump({"data": cons_data}, f, ensure_ascii=False, allow_nan=False)


_cons_df_cache = {}  # {path_str: (mtime, df)}

def read_consolidated_df(session_dir):
    cons_path = Path(session_dir) / "consolidated_price.xlsx"
    with CONSOLIDATED_IO_LOCK:
        try:
            mtime = cons_path.stat().st_mtime
        except OSError:
            mtime = None
        cached = _cons_df_cache.get(str(cons_path))
        if cached and cached[0] == mtime and mtime is not None:
            return cached[1].copy()
        df = pd.read_excel(cons_path)
        _cons_df_cache[str(cons_path)] = (mtime, df)
        return df.copy()


def write_consolidated_df(session_dir, df):
    cons_path = Path(session_dir) / "consolidated_price.xlsx"
    tmp_path = Path(session_dir) / "consolidated_price.tmp.xlsx"
    with CONSOLIDATED_IO_LOCK:
        df.to_excel(tmp_path, index=False)
        os.replace(tmp_path, cons_path)
        _cons_df_cache.pop(str(cons_path), None)  # invalidate cache


def _create_session_dir():
    session_id = str(uuid.uuid4())[:8]
    session_dir = UPLOAD_DIR / session_id
    session_dir.mkdir(exist_ok=True)
    return session_id, session_dir


def _finalize_processed_session(session_id, session_dir, output_path):
    global LAST_ACTIVE_SESSION_DIR
    session["session_id"] = session_id
    session["output_path"] = str(output_path)
    session["session_dir"] = str(session_dir)
    LAST_ACTIVE_SESSION_DIR = str(session_dir)


def _process_supplier_files(file_entries, session_id=None, session_dir=None):
    app_settings = load_app_settings()
    if session_id and session_dir:
        session_id = str(session_id)
        session_dir = Path(session_dir)
        session_dir.mkdir(exist_ok=True)
    else:
        session_id, session_dir = _create_session_dir()
    all_frames = []
    supplier_names = set()

    for entry in file_entries:
        filepath = Path(entry.get("filepath", ""))
        if not filepath.exists():
            continue
        display_name = str(entry.get("display_name", filepath.name) or filepath.name)
        supplier_name = str(entry.get("supplier_name", "") or "").strip() or "Unknown"
        try:
            df = parse_generic_excel(filepath, supplier_name)
            if not df.empty:
                all_frames.append(df)
                supplier_names.add(supplier_name)
                print(f"Загружен {display_name}: {len(df)} товаров, поставщик: {supplier_name}")
        except Exception as e:
            print(f"Ошибка парсинга {display_name}: {e}")
            import traceback
            traceback.print_exc()

    if not all_frames:
        shutil.rmtree(session_dir, ignore_errors=True)
        raise ValueError("Не удалось обработать файлы")

    all_data = pd.concat(all_frames, ignore_index=True)
    print(f"Всего загружено: {len(all_data)} товаров")
    print(f"Колонки: {list(all_data.columns)}")
    if "onliner_id" in all_data.columns:
        print(f"С OnlinerID: {all_data['onliner_id'].notna().sum()}")

    consolidated_df = consolidate_simple(all_data)
    consolidated_df = normalize_consolidated_columns(consolidated_df)
    consolidated_df = ensure_category_column(consolidated_df)
    consolidated_df = apply_saved_markups_to_df(consolidated_df)

    manual_bindings = load_manual_id_bindings()
    id_cache, id_cache_changed = _sanitize_id_cache(load_id_cache())
    if id_cache_changed:
        save_id_cache(id_cache)
    id_fanout = build_id_fanout_map(id_cache)
    if "Ссылка" not in consolidated_df.columns:
        consolidated_df["Ссылка"] = ""
    for i, row in consolidated_df.iterrows():
        name = row.get("Название", "")
        name_key = _normalize_name_key(name)
        supplier_name = str(row.get("Поставщик", "") or "").strip().upper()
        is_iven_supplier = supplier_name in {"IVEN"}
        is_ntech_supplier = supplier_name in {"N-TECH", "NTECH"}

        # manual_bindings — ручные правки пользователя — всегда имеют приоритет,
        # даже если поставщик прислал свой (возможно неверный) ID в Excel.
        if not is_iven_supplier:
            manual = manual_bindings.get(name_key) if name_key else None
            if isinstance(manual, dict):
                if bool(manual.get("blocked", False)):
                    consolidated_df.at[i, "OnlinerID"] = ""
                    consolidated_df.at[i, "Ссылка"] = ""
                    continue
                mid = normalize_onliner_id(manual.get("id", ""))
                if mid:
                    consolidated_df.at[i, "OnlinerID"] = mid
                    murl = str(manual.get("url", "")).strip()
                    if murl:
                        consolidated_df.at[i, "Ссылка"] = murl
                    continue

        # id_cache — автоматический кэш — применяем только если ID ещё нет
        if not is_iven_supplier and not is_ntech_supplier:
            oid = row.get("OnlinerID")
            if not oid or str(oid).strip() == "" or str(oid) == "nan":
                cache_key = _get_id_cache_key_for_name(name)
                if cache_key in id_cache:
                    cached = id_cache[cache_key]
                    if is_trusted_cached_id(cache_key, cached, id_fanout=id_fanout):
                        cached_id = normalize_onliner_id(cached.get("id", ""))
                        if cached_id:
                            consolidated_df.at[i, "OnlinerID"] = cached_id

    output_path = session_dir / "consolidated_price.xlsx"
    consolidated_df.to_excel(output_path, index=False)
    write_consolidated_json(consolidated_df, session_dir / "consolidated.json")

    # Пополняем локальную БД из свежего прайса (всё кроме N-Tech / TGPC)
    threading.Thread(
        target=db_populate_from_df,
        args=(consolidated_df, "price_load"),
        kwargs={"skip_suppliers": ["N-Tech", "TGPC", "N-TECH", "NTECH"]},
        daemon=True,
    ).start()

    snapshot_diff = {}
    if len(supplier_names) == 1:
        supplier_name = list(supplier_names)[0]
        snapshots = load_supplier_snapshots()
        previous_snapshot = (((snapshots or {}).get("suppliers") or {}).get(supplier_name) or {})
        current_snapshot = build_supplier_snapshot(consolidated_df, supplier_name)
        if previous_snapshot:
            snapshot_diff = compare_supplier_snapshot(previous_snapshot, current_snapshot)
            snapshot_diff["supplier"] = supplier_name
        save_session_supplier_diff(session_dir, snapshot_diff)
        snapshots.setdefault("suppliers", {})[supplier_name] = current_snapshot
        save_supplier_snapshots(snapshots)
    else:
        save_session_supplier_diff(session_dir, {})

    stats = {
        "total": len(all_data),
        "suppliers": len(supplier_names),
        "consolidated": len(consolidated_df),
        "matched": int(all_data["onliner_id"].notna().sum()) if "onliner_id" in all_data.columns else 0,
        "without_id": _count_rows_without_onliner_id(consolidated_df),
        "duplicate_id_rows": _count_rows_with_duplicate_onliner_id(consolidated_df),
        "show_checks_block": _coerce_bool((((app_settings or {}).get("ui") or {}).get("show_checks_block", True)), default=True),
        "snapshot_diff": snapshot_diff,
    }

    try:
        _maybe_cleanup_old_uploads(exclude_dirs=[session_dir, LAST_ACTIVE_SESSION_DIR], min_interval_sec=60)
    except Exception:
        pass

    return {
        "session_id": session_id,
        "session_dir": session_dir,
        "output_path": output_path,
        "stats": stats,
    }


@app.route("/upload", methods=["POST"])
def upload():
    files = request.files.getlist("files")
    if not files or all(not f.filename for f in files):
        return redirect(url_for("index", error="Не загружено ни одного файла"))

    session_id, session_dir = _create_session_dir()
    supplier_mapping = {}
    for key in request.form:
        if key.startswith("supplier_"):
            fname = key.replace("supplier_", "")
            supplier_mapping[fname] = request.form[key].strip()

    file_entries = []
    for file in files:
        if not file.filename:
            continue
        fname_enc = file.filename
        from urllib.parse import quote, unquote
        for enc_fname, sup_name in supplier_mapping.items():
            if unquote(enc_fname) == fname_enc or enc_fname == fname_enc:
                supplier_name = sup_name
                break
        else:
            supplier_name = "Unknown"
        
        if not supplier_name:
            supplier_name = "Unknown"

        # Сохраняем с UUID-именем + оригинальным расширением
        # (secure_filename убивает кириллицу и может удалить расширение)
        _orig_ext = Path(file.filename).suffix.lower()
        if _orig_ext not in {".xls", ".xlsx", ".xlsb", ".xlsm", ".csv"}:
            _orig_ext = ".xlsx"
        safe_fname = str(uuid.uuid4())[:8] + _orig_ext
        filepath = session_dir / safe_fname
        file.save(str(filepath))

        file_entries.append({
            "filepath": filepath,
            "display_name": file.filename,
            "supplier_name": supplier_name,
        })

    try:
        result = _process_supplier_files(file_entries, session_id=session_id, session_dir=session_dir)
    except Exception as _upload_err:
        import traceback as _tb_mod
        _tb = _tb_mod.format_exc()
        print("[UPLOAD ERROR] " + str(_upload_err) + "\n" + _tb, flush=True)
        try:
            import datetime
            with open("upload_error.log", "a", encoding="utf-8") as _ef:
                _ef.write("--- " + str(datetime.datetime.now()) + " ---\n" + str(_upload_err) + "\n" + _tb + "\n")
        except Exception:
            pass
        return redirect(url_for("index", error="Не удалось обработать файлы: " + str(_upload_err)[:120]))

    _finalize_processed_session(result["session_id"], result["session_dir"], result["output_path"])
    return render_template_string(RESULT_PAGE, stats=result["stats"])


@app.route("/api/consolidated")
def api_consolidated():
    session_dir = session.get("session_dir")
    if not session_dir:
        return jsonify({"data": []})
    cons_path = Path(session_dir) / "consolidated_price.xlsx"
    if not cons_path.exists():
        return jsonify({"data": []})
    df = read_consolidated_df(session_dir)
    df = ensure_category_column(df)
    df = apply_visibility_filter(df, session_dir)
    cons_data = []
    for i, row in df.iterrows():
        cons_data.append([
            _safe_json_value(row.get("OnlinerID", "")),
            _safe_json_value(row.get("Название", "")),
            _safe_json_value(row.get("Цена", 0)),
            _safe_json_value(row.get("Поставщик", "")),
            _safe_json_value(row.get("Гарантия", "")),
            _delivery_days_from_row(row),
            _safe_json_value(row.get("РРЦ", "")),
            _safe_json_value(row.get("Цена без скидки", "")),
            int(i),
            _safe_json_value(row_category(row)),
        ])
    response = jsonify({"data": cons_data})
    response.headers["Cache-Control"] = "no-store, no-cache, must-revalidate, max-age=0"
    response.headers["Pragma"] = "no-cache"
    response.headers["Expires"] = "0"
    return response


@app.route("/api/stats")
def api_stats():
    session_dir = session.get("session_dir")
    if not session_dir:
        return jsonify({"without_id": 0, "duplicate_id_rows": 0})
    cons_path = Path(session_dir) / "consolidated_price.xlsx"
    if not cons_path.exists():
        return jsonify({"without_id": 0, "duplicate_id_rows": 0})
    df = read_consolidated_df(session_dir)
    return jsonify({
        "without_id": _count_rows_without_onliner_id(df),
        "duplicate_id_rows": _count_rows_with_duplicate_onliner_id(df),
    })


@app.route("/api/manual-id-confirm-batch", methods=["POST"])
def api_manual_id_confirm_batch():
    session_dir = session.get("session_dir")
    if not session_dir:
        return jsonify({"status": "error", "message": "Нет активной сессии"}), 400

    payload = request.get_json(silent=True) or {}
    source = str(payload.get("source", "ui")).strip() or "ui"
    items_raw = payload.get("items", [])
    if not isinstance(items_raw, list) or not items_raw:
        return jsonify({"status": "error", "message": "Нет выбранных строк"}), 400

    df = read_consolidated_df(session_dir)
    if "OnlinerID" not in df.columns:
        return jsonify({"status": "error", "message": "В прайсе нет колонки OnlinerID"}), 400
    if "Ссылка" not in df.columns:
        df["Ссылка"] = ""
    df["OnlinerID"] = df["OnlinerID"].astype("object")
    df["Ссылка"] = df["Ссылка"].astype("object")

    id_cache, id_cache_changed = _sanitize_id_cache(load_id_cache())
    manual_bindings = load_manual_id_bindings()
    # Strict anti-duplicate mode: one OnlinerID cannot be assigned to different product names.
    max_distinct_name_keys_per_id = 1
    id_to_name_keys = {}
    if "Название" in df.columns:
        for _, row in df.iterrows():
            existing_oid = normalize_onliner_id(row.get("OnlinerID", ""))
            if not existing_oid:
                continue
            existing_name_key = _normalize_name_key(row.get("Название", ""))
            if not existing_name_key:
                continue
            bucket = id_to_name_keys.setdefault(existing_oid, set())
            bucket.add(existing_name_key)
    updated = 0
    blocked_duplicates = []
    touched_queue_keys = set()
    journal_entry = {
        "ts": int(time.time()),
        "action": "manual_id_confirm_batch",
        "session_dir": str(session_dir),
        "source": source,
        "changes": [],
    }

    for raw in items_raw[:1000]:
        if not isinstance(raw, dict):
            continue
        item_name = str(raw.get("name", "")).strip()
        oid = normalize_onliner_id(raw.get("onliner_id", ""))
        final_url = str(raw.get("url", "")).strip()
        row_idx = raw.get("row_idx", None)
        allow_duplicate_id = _coerce_bool(raw.get("allow_duplicate_id"), False)
        if not item_name or not oid:
            continue

        target_name_key = _normalize_name_key(item_name)
        existing_name_keys = set(id_to_name_keys.get(oid, set()))
        if target_name_key:
            existing_name_keys.discard(target_name_key)
        if len(existing_name_keys) >= max_distinct_name_keys_per_id and not allow_duplicate_id:
            blocked_duplicates.append({
                "name": item_name,
                "onliner_id": oid,
                "known_items_with_same_id": int(len(existing_name_keys) + (1 if target_name_key else 0)),
            })
            continue

        if not final_url:
            try:
                info = fetch_onliner_product_info(
                    oid,
                    force_refresh=False,
                    use_cache_on_error=True,
                    product_name_hint=item_name,
                )
                final_url = str((info or {}).get("url", "")).strip()
            except Exception:
                final_url = ""

        # Manual confirmation should be exact-name only.
        # Do not push manual decisions into broad id_cache keys,
        # otherwise similar models may receive phantom duplicate IDs.
        item_cache_key = ""

        if target_name_key:
            manual_bindings[target_name_key] = {"id": oid, "url": final_url}
            touched_queue_keys.add(target_name_key)
            id_to_name_keys.setdefault(oid, set()).add(target_name_key)

        try:
            if row_idx is not None and str(row_idx).strip() != "":
                row_idx_int = int(row_idx)
                if row_idx_int in df.index:
                    old_id = normalize_onliner_id(df.at[row_idx_int, "OnlinerID"])
                    old_url = str(df.at[row_idx_int, "Ссылка"]).strip() if "Ссылка" in df.columns else ""
                    df.at[row_idx_int, "OnlinerID"] = oid
                    if final_url:
                        df.at[row_idx_int, "Ссылка"] = final_url
                    journal_entry["changes"].append({
                        "row_idx": int(row_idx_int),
                        "name": item_name,
                        "old_onliner_id": old_id,
                        "old_url": old_url,
                        "new_onliner_id": oid,
                        "new_url": final_url,
                    })
                    updated += 1
        except Exception:
            continue

    if touched_queue_keys:
        review_queue = load_review_queue()
        queue_changed = False
        for name_key in list(touched_queue_keys):
            if name_key in review_queue:
                review_queue.pop(name_key, None)
                queue_changed = True
        if queue_changed:
            save_review_queue(review_queue)
    if id_cache_changed:
        save_id_cache(id_cache)
    save_manual_id_bindings(manual_bindings)
    if journal_entry["changes"]:
        append_id_change_journal(journal_entry)
    write_consolidated_df(session_dir, df)
    write_consolidated_json(df, Path(session_dir) / "consolidated.json")
    if blocked_duplicates and updated <= 0:
        sample = blocked_duplicates[0]
        return jsonify({
            "status": "error",
            "code": "duplicate_id_protection",
            "updated": 0,
            "blocked": blocked_duplicates[:20],
            "message": (
                f"Защита от дублей: ID {sample.get('onliner_id', '')} уже связан с несколькими товарами. "
                "Сохранение остановлено."
            ),
        }), 409
    if blocked_duplicates:
        return jsonify({
            "status": "ok",
            "updated": updated,
            "blocked": blocked_duplicates[:20],
            "message": (
                f"Сохранено: {updated}. Защита от дублей отклонила ещё {len(blocked_duplicates)} "
                "назначений с повторяющимися OnlinerID."
            ),
        })
    return jsonify({"status": "ok", "updated": updated})


@app.route("/api/manual-id-clear", methods=["POST"])
def api_manual_id_clear():
    session_dir = session.get("session_dir")
    if not session_dir:
        return jsonify({"status": "error", "message": "Нет активной сессии"}), 400

    payload = request.get_json(silent=True) or {}
    source = str(payload.get("source", "ui")).strip() or "ui"
    item = payload.get("item", {})
    if not isinstance(item, dict):
        return jsonify({"status": "error", "message": "Некорректный payload"}), 400
    item_name = str(item.get("name", "")).strip()
    row_idx = item.get("row_idx", None)
    if not item_name or row_idx is None or str(row_idx).strip() == "":
        return jsonify({"status": "error", "message": "Нужно имя товара и row_idx"}), 400

    try:
        row_idx_int = int(row_idx)
    except Exception:
        return jsonify({"status": "error", "message": "Некорректный row_idx"}), 400

    df = read_consolidated_df(session_dir)
    if "OnlinerID" not in df.columns:
        return jsonify({"status": "error", "message": "В прайсе нет колонки OnlinerID"}), 400
    if row_idx_int not in df.index:
        return jsonify({"status": "error", "message": "Строка не найдена"}), 404
    if "Ссылка" not in df.columns:
        df["Ссылка"] = ""
    df["OnlinerID"] = df["OnlinerID"].astype("object")
    df["Ссылка"] = df["Ссылка"].astype("object")

    old_id = normalize_onliner_id(df.at[row_idx_int, "OnlinerID"])
    old_url = str(df.at[row_idx_int, "Ссылка"]).strip()
    df.at[row_idx_int, "OnlinerID"] = ""
    df.at[row_idx_int, "Ссылка"] = ""

    id_cache, _ = _sanitize_id_cache(load_id_cache())
    cache_key = _get_id_cache_key_for_name(item_name)
    if cache_key and cache_key in id_cache:
        id_cache.pop(cache_key, None)
    save_id_cache(id_cache)

    manual_bindings = load_manual_id_bindings()
    name_key = _normalize_name_key(item_name)
    if name_key:
        # blocked=True prevents future auto-restore from stale cache for this name.
        manual_bindings[name_key] = {"id": "", "url": "", "blocked": True}
        save_manual_id_bindings(manual_bindings)
        review_queue = load_review_queue()
        if name_key in review_queue:
            review_queue.pop(name_key, None)
            save_review_queue(review_queue)

    append_id_change_journal({
        "ts": int(time.time()),
        "action": "manual_id_clear",
        "session_dir": str(session_dir),
        "source": source,
        "changes": [{
            "row_idx": int(row_idx_int),
            "name": item_name,
            "old_onliner_id": old_id,
            "old_url": old_url,
            "new_onliner_id": "",
            "new_url": "",
        }],
    })

    write_consolidated_df(session_dir, df)
    write_consolidated_json(df, Path(session_dir) / "consolidated.json")
    return jsonify({"status": "ok", "cleared": 1})


def build_iven_id_index(df):
    """Строит индекс name_key → {id, name, url} из ВСЕХ строк прайса, у которых есть OnlinerID.
    Используется как источник ID для N-Tech и других поставщиков без ID.
    Исключает N-Tech и TGPC (они сами являются целью поиска ID).
    """
    index = {}
    if df is None or df.empty:
        return index
    # Исключаем поставщиков, которые сами являются целью подбора
    SKIP_SUPPLIERS = {"N-TECH", "NTECH", "TGPC"}
    supplier_col = "Поставщик" if "Поставщик" in df.columns else None
    total_added = 0
    for _, row in df.iterrows():
        oid = normalize_onliner_id(row.get("OnlinerID", ""))
        if not oid:
            continue
        name = str(row.get("Название", "")).strip()
        if not name:
            continue
        if supplier_col:
            sup = str(row.get(supplier_col, "")).strip().upper()
            if sup in SKIP_SUPPLIERS:
                continue
        url = str(row.get("Ссылка", "")).strip()
        key = _normalize_name_key(name)
        if key and key not in index:
            index[key] = {"id": oid, "name": name, "url": url}
            total_added += 1
    return index


def lookup_iven_match(product_name, iven_index, threshold=0.85):
    """Ищет лучшее совпадение для product_name в iven_index.
    Возвращает dict {id, name, url, score} или None если score < threshold.
    """
    if not product_name or not iven_index:
        return None
    best_score = 0.0
    best_rec   = None
    for key, rec in iven_index.items():
        cmp = calc_name_match(product_name, rec["name"])
        sc  = float(cmp.get("score", 0.0) or 0.0)
        if sc > best_score:
            best_score = sc
            best_rec   = rec
    if best_score >= threshold and best_rec:
        return {**best_rec, "score": round(best_score, 3)}
    return None


def _autofill_iven_bridge_worker(session_dir, ignore_manual_cache=False, prefer_b2b=False):
    """Автоподбор ID для N-Tech через локальную SQLite БД.
    Автоприменяет только точные совпадения из индекса базы (source == db_exact).
    Все остальные кандидаты отправляет на ручную модерацию.
    БД пополняется автоматически при загрузке каждого прайса (IVEN, BN, Tradex).
    """
    global autofill_iven_status
    try:
        db_st = db_stats()
        db_total = db_st.get("total_products", 0)
        print(f"[iven_bridge] БД: {db_total} товаров, {db_st.get('total_names', 0)} имён", flush=True)

        df = read_consolidated_df(session_dir)
        if df.empty:
            with AUTOFILL_IVEN_LOCK:
                autofill_iven_status.update({"running": False, "message": "Нет данных", "finished_at": int(time.time())})
            return

        if "OnlinerID" not in df.columns:
            df["OnlinerID"] = ""
        if "Ссылка" not in df.columns:
            df["Ссылка"] = ""
        df["OnlinerID"] = df["OnlinerID"].astype("object")
        df["Ссылка"]    = df["Ссылка"].astype("object")

        # Если БД пустая — сначала заполняем из текущего прайса
        if db_total == 0:
            print("[iven_bridge] БД пустая, пополняю из текущего прайса...", flush=True)
            db_populate_from_df(df, "price_load", skip_suppliers=["N-Tech", "TGPC"])
            db_st = db_stats()
            db_total = db_st.get("total_products", 0)
            if db_total == 0:
                with AUTOFILL_IVEN_LOCK:
                    autofill_iven_status.update({
                        "running": False,
                        "message": "БД пуста. Загрузи прайс IVEN/BN чтобы заполнить базу.",
                        "finished_at": int(time.time()),
                    })
                return

        # Собираем задачи: все товары без OnlinerID, не TGPC ПК.
        # После API-очистки значение может быть NaN, поэтому опираемся только
        # на normalize_onliner_id, а не на truthy-проверки и не на поставщика.
        tasks = []
        for row_idx, row in df.iterrows():
            oid = normalize_onliner_id(row.get("OnlinerID", ""))
            if oid:
                continue
            name = str(row.get("Название", "")).strip()
            if not name or _is_tgpc_pc_name(name):
                continue
            tasks.append((int(row_idx), name))

        with AUTOFILL_IVEN_LOCK:
            autofill_iven_status.update({
                "total": len(tasks), "done": 0, "applied": 0, "skipped": 0, "percent": 0,
                "message": (
                    f"Найдено {len(tasks)} товаров без ID. БД: {db_total} позиций..."
                    + (" Режим: B2B без кеша." if prefer_b2b else "")
                ),
            })

        if not tasks:
            with AUTOFILL_IVEN_LOCK:
                autofill_iven_status.update({
                    "running": False,
                    "message": "Все товары уже имеют OnlinerID.",
                    "finished_at": int(time.time()),
                })
            return

        manual_bindings = load_manual_id_bindings()
        id_cache        = load_id_cache()
        applied = 0
        skipped = 0
        journal_changes = []
        matches_log  = []   # для отчёта в UI
        no_match_log = []

        for done_idx, (row_idx, name) in enumerate(tasks, start=1):
            pct = max(1, int(round(done_idx / len(tasks) * 100)))
            with AUTOFILL_IVEN_LOCK:
                autofill_iven_status.update({
                    "done": done_idx - 1, "applied": applied, "skipped": skipped, "percent": pct,
                    "message": f"Ищу {done_idx}/{len(tasks)}: {name[:60]}",
                })

            name_key = _normalize_name_key(name)
            if (not ignore_manual_cache) and name_key and name_key in manual_bindings:
                cached_manual = manual_bindings.get(name_key) or {}
                manual_id = normalize_onliner_id(cached_manual.get("id", ""))
                manual_url = str(cached_manual.get("url", "")).strip()
                if manual_id:
                    old_id = normalize_onliner_id(df.at[row_idx, "OnlinerID"])
                    old_url = str(df.at[row_idx, "Ссылка"]).strip() if "Ссылка" in df.columns else ""
                    df.at[row_idx, "OnlinerID"] = manual_id
                    df.at[row_idx, "Ссылка"] = manual_url
                    journal_changes.append({
                        "row_idx": row_idx, "name": name,
                        "old_onliner_id": old_id, "old_url": old_url,
                        "new_onliner_id": manual_id, "new_url": manual_url,
                        "matched_name": "manual_cache",
                        "reason": "db_bridge manual_cache",
                    })
                    matches_log.append({
                        "name": name,
                        "row_idx": row_idx,
                        "matched_name": "Ручная привязка из вечного кеша",
                        "score": 1.0,
                        "id": manual_id,
                        "url": manual_url,
                        "source": "manual_cache",
                    })
                    applied += 1
                    continue
                skipped += 1
                continue

            match = db_find_id_for_name(name, threshold=0.40, allow_b2b=prefer_b2b)
            top_cands = db_find_top_candidates(name, top_n=3, min_score=0.40, allow_b2b=prefer_b2b)

            if not match:
                skipped += 1
                no_match_log.append({
                    "name": name,
                    "row_idx": row_idx,
                    "candidates": top_cands,
                })
                continue

            oid = match["id"]
            url = match["url"]
            sc  = float(match["score"] or 0.0)
            match_source = str(match.get("source", "") or "").strip()
            matched_name = match.get("name", "")

            if match_source == "db_exact":
                print(f"[iven_bridge] ✓ auto exact {name[:55]} → {matched_name[:45]} | id={oid} score={sc}", flush=True)

                if name_key:
                    manual_bindings[name_key] = {"id": oid, "url": url}
                art = _get_id_cache_key_for_name(name)
                if art:
                    id_cache[art] = {"id": oid, "url": url}

                old_id  = normalize_onliner_id(df.at[row_idx, "OnlinerID"])
                old_url = str(df.at[row_idx, "Ссылка"]).strip() if "Ссылка" in df.columns else ""
                df.at[row_idx, "OnlinerID"] = oid
                df.at[row_idx, "Ссылка"]    = url
                journal_changes.append({
                    "row_idx": row_idx, "name": name,
                    "old_onliner_id": old_id, "old_url": old_url,
                    "new_onliner_id": oid, "new_url": url,
                    "matched_name": matched_name,
                    "reason": f"db_bridge {match_source or 'exact'} score={round(sc, 3)}",
                })
                matches_log.append({
                    "name": name,
                    "row_idx": row_idx,
                    "matched_name": matched_name,
                    "score": round(sc, 3),
                    "id": oid,
                    "url": url,
                    "source": match_source,
                })
                applied += 1
                continue

            merged_candidates = []
            seen_candidate_ids = set()

            def _push_candidate(cid, cname, curl, cscore):
                cid = normalize_onliner_id(cid)
                if not cid or cid in seen_candidate_ids:
                    return
                seen_candidate_ids.add(cid)
                merged_candidates.append({
                    "id": cid,
                    "name": str(cname or "").strip(),
                    "url": str(curl or "").strip(),
                    "score": round(float(cscore or 0.0), 3),
                })

            _push_candidate(oid, matched_name, url, sc)
            for cand in top_cands:
                _push_candidate(cand.get("id", ""), cand.get("name", ""), cand.get("url", ""), cand.get("score", 0.0))

            skipped += 1
            no_match_log.append({
                "name": name,
                "row_idx": row_idx,
                "best_score": round(sc, 3),
                "best_id": oid,
                "best_name": matched_name,
                "best_source": match_source,
                "needs_manual": True,
                "candidates": merged_candidates[:5],
            })
            print(f"[iven_bridge] ? review {name[:55]} → {matched_name[:45]} | id={oid} source={match_source} score={sc}", flush=True)

        save_manual_id_bindings(manual_bindings)
        save_id_cache(id_cache)
        if journal_changes:
            append_id_change_journal({
                "ts": int(time.time()),
                "action": "autofill_iven_bridge",
                "session_dir": str(session_dir),
                "source": "api_autofill_iven_bridge",
                "changes": journal_changes,
            })
        if applied > 0:
            write_consolidated_df(session_dir, df)
            write_consolidated_json(df, Path(session_dir) / "consolidated.json")

        mode_suffix = " Режим: B2B без кеша." if prefer_b2b else ""
        msg = f"Готово. Автоподобрано 100%: {applied}, на ручную проверку: {len(no_match_log)}.{mode_suffix}"
        print(f"[iven_bridge] {msg}", flush=True)
        with AUTOFILL_IVEN_LOCK:
            autofill_iven_status.update({
                "running": False,
                "done": len(tasks), "applied": applied, "skipped": skipped, "percent": 100,
                "message": msg,
                "finished_at": int(time.time()),
                "matches": matches_log,
                "no_match": no_match_log,
            })
    except Exception as e:
        import traceback
        print(f"[iven_bridge] ОШИБКА: {e}", flush=True)
        traceback.print_exc()
        with AUTOFILL_IVEN_LOCK:
            autofill_iven_status.update({
                "running": False,
                "message": f"Ошибка IVEN-бриджа: {str(e)[:180]}",
                "finished_at": int(time.time()),
            })


def _autofill_tgpc_pc_worker(session_dir, max_items=0):
    global autofill_tgpc_pc_status
    df = read_consolidated_df(session_dir)
    report_items = []
    try:
        if df.empty:
            with AUTOFILL_TGPC_PC_LOCK:
                autofill_tgpc_pc_status.update({
                    "running": False,
                    "message": "Нет данных для обработки",
                    "finished_at": int(time.time()),
                })
            return
        if "OnlinerID" not in df.columns:
            df["OnlinerID"] = ""
        if "Ссылка" not in df.columns:
            df["Ссылка"] = ""
        df["OnlinerID"] = df["OnlinerID"].astype("object")
        df["Ссылка"] = df["Ссылка"].astype("object")

        settings = load_app_settings()
        no_id_cfg = (settings.get("no_id_search") or {})
        limit = max(10, min(int(no_id_cfg.get("max_candidates", 80) or 80), 80))

        tasks = []
        for row_idx, row in df.iterrows():
            current_id = normalize_onliner_id(row.get("OnlinerID", ""))
            if current_id:
                continue
            name = str(row.get("Название", "")).strip()
            if not name or not _is_tgpc_pc_name(name):
                continue
            tasks.append((int(row_idx), str(name), str(row_category(row) or "").strip()))

        if max_items and len(tasks) > int(max_items):
            tasks = tasks[:int(max_items)]

        with AUTOFILL_TGPC_PC_LOCK:
            autofill_tgpc_pc_status.update({
                "total": int(len(tasks)),
                "done": 0,
                "applied": 0,
                "skipped": 0,
                "percent": 0,
                "items": [],
                "message": f"Найдено TGPC ПЭВМ без ID: {len(tasks)}. Начинаю подбор...",
            })

        id_cache = load_id_cache()
        manual_bindings = load_manual_id_bindings()
        journal_entry = {
            "ts": int(time.time()),
            "action": "autofill_tgpc_pc_ids",
            "session_dir": str(session_dir),
            "source": "db_autofill_tgpc_pc_ids",
            "changes": [],
        }

        applied = 0
        skipped = 0
        for done_idx, (row_idx, name, category) in enumerate(tasks, start=1):
            in_progress_percent = max(1, int(round(((done_idx - 1) / len(tasks)) * 100))) if tasks else 100
            with AUTOFILL_TGPC_PC_LOCK:
                autofill_tgpc_pc_status.update({
                    "done": int(done_idx - 1),
                    "applied": int(applied),
                    "skipped": int(skipped),
                    "percent": int(in_progress_percent),
                    "message": f"Проверяю {done_idx} из {len(tasks)}: {name[:72]}",
                })
            try:
                candidates = db_search_tgpc_pc_candidates(name, limit=min(limit, 24))
            except Exception:
                skipped += 1
                candidates = []

            if candidates:
                top = candidates[0] if isinstance(candidates[0], dict) else {}
                top_id = normalize_onliner_id(top.get("id", ""))
                top_url = str(top.get("url", "")).strip()
                top_score = float(top.get("score", 0) or 0)
                second_score = 0.0
                if len(candidates) > 1 and isinstance(candidates[1], dict):
                    try:
                        second_score = float(candidates[1].get("score", 0) or 0)
                    except Exception:
                        second_score = 0.0

                confident = bool(
                    top_id and (
                        top_score >= 0.95
                        or (top_score >= 0.92 and (top_score - second_score) >= 0.05)
                    )
                )
                if confident:
                    item_cache_key = _get_id_cache_key_for_name(name)
                    if item_cache_key:
                        id_cache[item_cache_key] = {"id": top_id, "url": top_url}
                    target_name_key = _normalize_name_key(name)
                    if target_name_key:
                        manual_bindings[target_name_key] = {"id": top_id, "url": top_url}

                    old_id = normalize_onliner_id(df.at[row_idx, "OnlinerID"])
                    old_url = str(df.at[row_idx, "Ссылка"]).strip() if "Ссылка" in df.columns else ""
                    df.at[row_idx, "OnlinerID"] = top_id
                    if top_url:
                        df.at[row_idx, "Ссылка"] = top_url
                    journal_entry["changes"].append({
                        "row_idx": int(row_idx),
                        "name": name,
                        "old_onliner_id": old_id,
                        "old_url": old_url,
                        "new_onliner_id": top_id,
                        "new_url": top_url,
                        "score": round(top_score, 4),
                    })
                    applied += 1
                    report_items.append({
                        "row_idx": int(row_idx),
                        "name": name,
                        "status": "matched",
                        "onliner_id": top_id,
                        "onliner_name": str(top.get("name", "")).strip(),
                        "score": round(top_score, 3),
                    })
                else:
                    skipped += 1
                    report_items.append({
                        "row_idx": int(row_idx),
                        "name": name,
                        "status": "skipped",
                        "onliner_id": top_id,
                        "onliner_name": str(top.get("name", "")).strip(),
                        "score": round(top_score, 3),
                    })
            else:
                skipped += 1
                report_items.append({
                    "row_idx": int(row_idx),
                    "name": name,
                    "status": "not_found",
                    "onliner_id": "",
                    "onliner_name": "",
                    "score": 0.0,
                })

            percent = int(round((done_idx / len(tasks)) * 100)) if tasks else 100
            with AUTOFILL_TGPC_PC_LOCK:
                autofill_tgpc_pc_status.update({
                    "done": int(done_idx),
                    "applied": int(applied),
                    "skipped": int(skipped),
                    "percent": int(percent),
                    "items": report_items[-20:],
                    "message": f"Обработано {done_idx} из {len(tasks)}. Подставлено: {applied}.",
                })

        save_id_cache(id_cache)
        save_manual_id_bindings(manual_bindings)
        if journal_entry["changes"]:
            append_id_change_journal(journal_entry)
        write_consolidated_df(session_dir, df)
        write_consolidated_json(df, Path(session_dir) / "consolidated.json")
        with AUTOFILL_TGPC_PC_LOCK:
            autofill_tgpc_pc_status.update({
                "running": False,
                "done": int(len(tasks)),
                "applied": int(applied),
                "skipped": int(skipped),
                "percent": 100 if tasks else 0,
                "items": report_items[-50:],
                "finished_at": int(time.time()),
                "message": f"Автоподбор завершён: подставлено {applied} из {len(tasks)} TGPC ПЭВМ.",
            })
    except Exception as e:
        with AUTOFILL_TGPC_PC_LOCK:
            autofill_tgpc_pc_status.update({
                "running": False,
                "items": report_items[-50:],
                "done": int(len(report_items)),
                "finished_at": int(time.time()),
                "message": "Ошибка автоподбора TGPC ПЭВМ: " + str(e)[:180],
            })


@app.route("/api/autofill-tgpc-pc-ids", methods=["POST"])
def api_autofill_tgpc_pc_ids():
    global autofill_tgpc_pc_status
    session_dir = session.get("session_dir")
    if not session_dir:
        return jsonify({"status": "error", "message": "Нет активной сессии"}), 400
    cons_path = Path(session_dir) / "consolidated_price.xlsx"
    if not cons_path.exists():
        return jsonify({"status": "error", "message": "Нет данных для обработки"}), 400
    payload = request.get_json(silent=True) or {}
    try:
        max_items = int(payload.get("limit", 0) or 0)
    except Exception:
        max_items = 0
    max_items = max(0, min(max_items, 200))

    with AUTOFILL_TGPC_PC_LOCK:
        if autofill_tgpc_pc_status.get("running"):
            return jsonify({"status": "already_running"})
        autofill_tgpc_pc_status = {
            "running": True,
            "total": 0,
            "done": 0,
            "applied": 0,
            "skipped": 0,
            "percent": 0,
            "items": [],
            "started_at": int(time.time()),
            "finished_at": 0,
            "message": "Подготовка автоподбора TGPC ПЭВМ...",
        }

    threading.Thread(target=_autofill_tgpc_pc_worker, args=(str(session_dir), max_items), daemon=True).start()
    return jsonify({"status": "started"})


@app.route("/api/autofill-tgpc-pc-status")
def api_autofill_tgpc_pc_status():
    with AUTOFILL_TGPC_PC_LOCK:
        payload = dict(autofill_tgpc_pc_status)
        payload["items"] = list(payload.get("items", []) or [])
        response = jsonify(payload)
        response.headers["Cache-Control"] = "no-store, no-cache, must-revalidate, max-age=0"
        response.headers["Pragma"] = "no-cache"
        response.headers["Expires"] = "0"
        return response


@app.route("/api/autofill-iven-bridge", methods=["POST"])
def api_autofill_iven_bridge():
    global autofill_iven_status
    session_dir = session.get("session_dir")
    if not session_dir:
        return jsonify({"status": "error", "message": "Нет активной сессии"}), 400
    cons_path = Path(session_dir) / "consolidated_price.xlsx"
    if not cons_path.exists():
        return jsonify({"status": "error", "message": "Нет данных для обработки"}), 400
    payload = request.get_json(silent=True) or {}
    ignore_manual_cache = _coerce_bool(payload.get("ignore_manual_cache", False), default=False)
    prefer_b2b = _coerce_bool(payload.get("prefer_b2b", False), default=False)
    with AUTOFILL_IVEN_LOCK:
        if autofill_iven_status.get("running"):
            return jsonify({"status": "already_running"})
        autofill_iven_status = {
            "running": True,
            "total": 0, "done": 0, "applied": 0, "skipped": 0, "percent": 0,
            "started_at": int(time.time()), "finished_at": 0,
            "message": "Подготовка IVEN-бриджа..." + (" Режим: B2B без кеша." if prefer_b2b else ""),
            "matches": [],
            "no_match": [],
            "report_mode": "iven",
            "report_title": "Отчёт подбора IVEN-бридж",
            "report_subtitle": "Сопоставление N-Tech товаров с базой Onliner ID",
        }
    threading.Thread(
        target=_autofill_iven_bridge_worker,
        args=(str(session_dir), ignore_manual_cache, prefer_b2b),
        daemon=True,
    ).start()
    return jsonify({"status": "started"})


@app.route("/api/autofill-iven-status")
def api_autofill_iven_status():
    with AUTOFILL_IVEN_LOCK:
        payload = dict(autofill_iven_status)
    resp = jsonify(payload)
    resp.headers["Cache-Control"] = "no-store, no-cache, must-revalidate, max-age=0"
    resp.headers["Pragma"] = "no-cache"
    return resp


@app.route("/api/iven-reject-match", methods=["POST"])
def api_iven_reject_match():
    """Удалить OnlinerID у товара N-Tech (из отчёта IVEN-бридж).
    Body: {"name": "...", "row_idx": 123}
    """
    session_dir = session.get("session_dir")
    if not session_dir:
        return jsonify({"status": "error", "message": "Нет активной сессии"}), 400
    payload = request.get_json(silent=True) or {}
    item_name = str(payload.get("name", "")).strip()
    row_idx = payload.get("row_idx")
    if not item_name:
        return jsonify({"status": "error", "message": "name required"}), 400
    try:
        df = read_consolidated_df(session_dir)
        df["OnlinerID"] = df["OnlinerID"].astype("object")
        if "Ссылка" in df.columns:
            df["Ссылка"] = df["Ссылка"].astype("object")
        cleared = 0
        if row_idx is not None:
            try:
                ri = int(row_idx)
                if ri in df.index:
                    df.at[ri, "OnlinerID"] = np.nan
                    if "Ссылка" in df.columns:
                        df.at[ri, "Ссылка"] = ""
                    cleared = 1
            except Exception:
                pass
        # Also clear by name match if row_idx not found
        if cleared == 0 and item_name:
            name_key = _normalize_name_key(item_name)
            if "Название" in df.columns:
                for idx, row in df.iterrows():
                    if _normalize_name_key(str(row.get("Название", ""))) == name_key:
                        df.at[idx, "OnlinerID"] = np.nan
                        if "Ссылка" in df.columns:
                            df.at[idx, "Ссылка"] = ""
                        cleared += 1
                        break
        # Remove from manual bindings if present
        name_key = _normalize_name_key(item_name)
        manual_bindings = load_manual_id_bindings()
        if name_key and name_key in manual_bindings:
            del manual_bindings[name_key]
            save_manual_id_bindings(manual_bindings)
        write_consolidated_df(session_dir, df)
        write_consolidated_json(df, Path(session_dir) / "consolidated.json")
        return jsonify({"status": "ok", "cleared": cleared})
    except Exception as e:
        return jsonify({"status": "error", "message": str(e)}), 500


@app.route("/api/onliner-db-stats")
def api_onliner_db_stats():
    stats = db_stats()
    resp = jsonify(stats)
    resp.headers["Cache-Control"] = "no-store"
    return resp


@app.route("/api/onliner-db-search")
def api_onliner_db_search():
    query = str(request.args.get("q", "") or "").strip()
    if not query:
        return jsonify({"items": []})
    items = []
    seen = set()
    query_lower = query.lower()
    compact = re.sub(r"[^a-z0-9]+", "", query_lower)
    try:
        with _db_connection() as conn:
            rows = []
            if query.isdigit():
                rows.extend(conn.execute(
                    "SELECT oc.onliner_id, oc.name, oc.url, oc.source "
                    "FROM onliner_catalog oc WHERE oc.onliner_id = ? LIMIT 20",
                    (query,),
                ).fetchall())
            rows.extend(conn.execute(
                "SELECT ni.onliner_id, ni.raw_name, oc.url, oc.source "
                "FROM name_index ni "
                "LEFT JOIN onliner_catalog oc ON oc.onliner_id = ni.onliner_id "
                "WHERE lower(ni.raw_name) LIKE ? LIMIT 40",
                (f"%{query_lower}%",),
            ).fetchall())
            if compact and compact != query_lower:
                rows.extend(conn.execute(
                    "SELECT ni.onliner_id, ni.raw_name, oc.url, oc.source "
                    "FROM name_index ni "
                    "LEFT JOIN onliner_catalog oc ON oc.onliner_id = ni.onliner_id "
                    "WHERE replace(replace(replace(lower(ni.raw_name), '-', ''), ' ', ''), '\"', '') LIKE ? LIMIT 40",
                    (f"%{compact}%",),
                ).fetchall())
            for row in rows:
                oid = normalize_onliner_id(row[0] if not isinstance(row, sqlite3.Row) else row["onliner_id"])
                raw_name = str((row[1] if not isinstance(row, sqlite3.Row) else row["raw_name"]) or "").strip()
                if not oid or not raw_name or oid in seen:
                    continue
                seen.add(oid)
                items.append({
                    "id": oid,
                    "name": raw_name,
                    "url": str((row[2] if not isinstance(row, sqlite3.Row) else row["url"]) or "").strip(),
                    "source": str((row[3] if not isinstance(row, sqlite3.Row) else row["source"]) or "").strip(),
                })
    except Exception as e:
        return jsonify({"items": [], "message": str(e)}), 500
    items.sort(key=lambda item: (
        0 if query_lower in str(item.get("name", "")).lower() else 1,
        len(str(item.get("name", ""))),
        str(item.get("name", "")).lower(),
    ))
    return jsonify({"items": items[:30]})


@app.route("/api/onliner-db-rebuild", methods=["POST"])
def api_onliner_db_rebuild():
    """Пересоздаёт БД из текущего сессионного прайса."""
    session_dir = session.get("session_dir")
    if not session_dir:
        return jsonify({"status": "error", "message": "Нет активной сессии"}), 400
    try:
        df = read_consolidated_df(session_dir)
        products, names = db_populate_from_df(
            df, "price_load", skip_suppliers=["N-Tech", "TGPC"]
        )
        return jsonify({"status": "ok", "products": products, "names": names})
    except Exception as e:
        return jsonify({"status": "error", "message": str(e)}), 500


@app.route("/api/onliner-db-import-gsheet", methods=["POST"])
def api_onliner_db_import_gsheet():
    """Скачивает CSV из Google Sheets и импортирует в SQLite.
    Body: {"sheet_id": "...", "sheet_name": "..."}
    """
    with _CATALOG_IMPORT_LOCK:
        if _catalog_import_status.get("running"):
            return jsonify({"status": "already_running", "message": "Импорт уже запущен"}), 409

    payload = request.get_json(silent=True) or {}
    sheet_id   = str(payload.get("sheet_id", "")).strip()
    sheet_name = str(payload.get("sheet_name", "All_Catalog")).strip() or "All_Catalog"
    force_refresh = bool(payload.get("force_refresh", False))
    if not sheet_id:
        return jsonify({"status": "error", "message": "sheet_id required"}), 400

    def _download_and_import():
        import tempfile, urllib.error
        # Try primary export URL; fall back to gviz/tq if it fails
        urls = [
            (f"https://docs.google.com/spreadsheets/d/{sheet_id}"
             f"/export?format=csv&sheet={urllib.parse.quote(sheet_name)}"),
            (f"https://docs.google.com/spreadsheets/d/{sheet_id}"
             f"/gviz/tq?tqx=out:csv&sheet={urllib.parse.quote(sheet_name)}"),
        ]
        safe_sheet = re.sub(r"[^a-zA-Z0-9._-]+", "_", sheet_name).strip("._-") or "sheet"
        cache_name = f"{sheet_id}_{safe_sheet}.csv"
        cache_path = ONLINER_DB_GSHEET_CACHE_DIR / cache_name
        with _CATALOG_IMPORT_LOCK:
            _catalog_import_status.update({
                "running": True, "total": 0, "done": 0, "inserted": 0,
                "skipped": 0, "percent": 0, "finished_at": None,
                "message": "Подключаюсь к Google Sheets…",
            })
        try:
            ONLINER_DB_GSHEET_CACHE_DIR.mkdir(parents=True, exist_ok=True)
            now_ts = int(time.time())
            cache_fresh = False
            if cache_path.exists():
                try:
                    age_sec = max(0, now_ts - int(cache_path.stat().st_mtime))
                    cache_fresh = (age_sec <= ONLINER_DB_GSHEET_CACHE_TTL_SEC) and (cache_path.stat().st_size > 0)
                except Exception:
                    cache_fresh = False

            if cache_fresh and not force_refresh:
                with _CATALOG_IMPORT_LOCK:
                    _catalog_import_status.update({
                        "message": "Использую локальный кэш Google Sheets (без повторного скачивания)…",
                        "percent": 5,
                    })
            else:
                tmp = tempfile.NamedTemporaryFile(delete=False, suffix=".csv")
                tmp.close()
                downloaded = False
                last_err = None
                for url in urls:
                    try:
                        req = urllib.request.Request(
                            url,
                            headers={"User-Agent": "Mozilla/5.0 (compatible; PriceMixer/1.0)"},
                        )
                        # Stream download in 128 KB chunks — shows download progress
                        CHUNK = 131072
                        with urllib.request.urlopen(req, timeout=300) as resp:
                            total_size = int(resp.headers.get("Content-Length") or 0)
                            received = 0
                            with open(tmp.name, "wb") as f:
                                while True:
                                    chunk = resp.read(CHUNK)
                                    if not chunk:
                                        break
                                    f.write(chunk)
                                    received += len(chunk)
                                    mb = received / 1024 / 1024
                                    total_mb = total_size / 1024 / 1024 if total_size else 0
                                    msg = (
                                        f"Скачано {mb:.1f} / {total_mb:.1f} МБ…"
                                        if total_size else f"Скачано {mb:.1f} МБ…"
                                    )
                                    pct = int(received / total_size * 15) if total_size else 5
                                    with _CATALOG_IMPORT_LOCK:
                                        _catalog_import_status.update({
                                            "message": msg, "percent": pct
                                        })
                        downloaded = True
                        break
                    except Exception as e:
                        last_err = e
                        continue

                if not downloaded:
                    raise Exception(f"Не удалось скачать файл: {last_err}")
                try:
                    import shutil
                    shutil.move(tmp.name, str(cache_path))
                except Exception:
                    # If move failed, keep temp path as fallback import source
                    cache_path = Path(tmp.name)
                with _CATALOG_IMPORT_LOCK:
                    _catalog_import_status.update({
                        "message": "Файл скачан и сохранен в кэш. Начинаю импорт…",
                        "percent": 16,
                    })

            _catalog_import_worker(str(cache_path), ".csv", cleanup_file=False)
        except Exception as e:
            with _CATALOG_IMPORT_LOCK:
                _catalog_import_status.update({
                    "running": False,
                    "message": f"Ошибка скачивания: {e}",
                    "finished_at": int(time.time()),
                })

    threading.Thread(target=_download_and_import, daemon=True).start()
    return jsonify({"status": "started", "message": "Скачиваю и импортирую…"})


@app.route("/api/onliner-db-import-csv", methods=["POST"])
def api_onliner_db_import_csv():
    """Принимает CSV или XLSX с каталогом Onliner и импортирует в SQLite БД.
    Ожидаемые колонки (0-based): A(0)=Category, C(2)=Model, E(4)=onliner_id, H(7)=FullName.
    """
    with _CATALOG_IMPORT_LOCK:
        if _catalog_import_status.get("running"):
            return jsonify({"status": "already_running",
                            "message": "Импорт уже запущен"}), 409

    file = request.files.get("file")
    if not file or not file.filename:
        return jsonify({"status": "error", "message": "Файл не передан"}), 400

    filename = file.filename.lower()
    if filename.endswith(".xlsx"):
        ext = ".xlsx"
    elif filename.endswith(".xls"):
        ext = ".xls"
    elif filename.endswith(".csv"):
        ext = ".csv"
    else:
        return jsonify({"status": "error",
                        "message": "Поддерживаются только CSV и XLSX"}), 400

    import tempfile, os
    tmp = tempfile.NamedTemporaryFile(delete=False, suffix=ext)
    file.save(tmp.name)
    tmp.close()

    t = threading.Thread(
        target=_catalog_import_worker,
        args=(tmp.name, ext),
        daemon=True,
    )
    t.start()
    return jsonify({"status": "started", "message": "Импорт запущен"})


@app.route("/api/onliner-db-import-status")
def api_onliner_db_import_status():
    with _CATALOG_IMPORT_LOCK:
        payload = dict(_catalog_import_status)
    resp = jsonify(payload)
    resp.headers["Cache-Control"] = "no-store, no-cache, must-revalidate, max-age=0"
    return resp


@app.route("/api/manual-id-rollback-last", methods=["POST"])
def api_manual_id_rollback_last():
    session_dir = session.get("session_dir")
    if not session_dir:
        return jsonify({"status": "error", "message": "Нет активной сессии"}), 400

    rows = load_id_change_journal()
    if not rows:
        return jsonify({"status": "error", "message": "Журнал замен пуст"}), 400

    last_idx = -1
    for i in range(len(rows) - 1, -1, -1):
        rec = rows[i]
        if str(rec.get("session_dir", "")).strip() == str(session_dir):
            last_idx = i
            break
    if last_idx < 0:
        return jsonify({"status": "error", "message": "Для текущей сессии нет записей отката"}), 400

    rec = rows.pop(last_idx)
    changes = rec.get("changes", []) if isinstance(rec, dict) else []
    if not isinstance(changes, list) or not changes:
        save_id_change_journal(rows)
        return jsonify({"status": "error", "message": "В записи нет изменений для отката"}), 400

    df = read_consolidated_df(session_dir)
    if "OnlinerID" not in df.columns:
        return jsonify({"status": "error", "message": "В прайсе нет OnlinerID"}), 400
    if "Ссылка" not in df.columns:
        df["Ссылка"] = ""
    df["OnlinerID"] = df["OnlinerID"].astype("object")
    df["Ссылка"] = df["Ссылка"].astype("object")

    restored = 0
    for ch in changes:
        try:
            row_idx = int(ch.get("row_idx"))
        except Exception:
            continue
        if row_idx not in df.index:
            continue
        old_id = normalize_onliner_id(ch.get("old_onliner_id", ""))
        old_url = str(ch.get("old_url", "")).strip()
        df.at[row_idx, "OnlinerID"] = old_id
        df.at[row_idx, "Ссылка"] = old_url
        restored += 1

    write_consolidated_df(session_dir, df)
    write_consolidated_json(df, Path(session_dir) / "consolidated.json")
    save_id_change_journal(rows)
    return jsonify({"status": "ok", "restored": restored})


@app.route("/api/clear-invalid-onliner-ids", methods=["POST"])
def api_clear_invalid_onliner_ids():
    session_dir = session.get("session_dir")
    if not session_dir:
        return jsonify({"status": "error", "message": "Нет активной сессии"}), 400
    payload = request.get_json(silent=True) or {}
    items = payload.get("items", [])
    if not isinstance(items, list) or not items:
        return jsonify({"status": "ok", "cleared": 0})

    keys_to_clear = set()
    ids_to_clear = set()
    for it in items[:2000]:
        key = str((it or {}).get("key", "")).strip()
        oid = normalize_onliner_id((it or {}).get("onliner_id", ""))
        if key:
            keys_to_clear.add(key)
        if oid:
            ids_to_clear.add(oid)

    if not keys_to_clear and not ids_to_clear:
        return jsonify({"status": "ok", "cleared": 0})

    df = read_consolidated_df(session_dir)
    if "OnlinerID" not in df.columns:
        return jsonify({"status": "ok", "cleared": 0})
    if "Ссылка" not in df.columns:
        df["Ссылка"] = ""
    df["OnlinerID"] = df["OnlinerID"].astype("object")

    cleared = 0
    touched_name_keys = []
    touched_articles = []
    for i, row in df.iterrows():
        row_key = build_item_category_key(row)
        row_oid = normalize_onliner_id(row.get("OnlinerID", ""))
        should_clear = False
        if row_key and row_key in keys_to_clear:
            should_clear = True
        elif row_oid and row_oid in ids_to_clear:
            should_clear = True
        if not should_clear:
            continue
        if row_oid:
            name = str(row.get("Название", "")).strip()
            touched_name_keys.append(_normalize_name_key(name))
            touched_articles.append(_get_id_cache_key_for_name(name))
        df.at[i, "OnlinerID"] = ""
        df.at[i, "Ссылка"] = ""
        cleared += 1

    if cleared > 0:
        write_consolidated_df(session_dir, df)
        write_consolidated_json(df, Path(session_dir) / "consolidated.json")

        # Чистим кэши ручных/артикульных привязок для этих товаров, чтобы старый ID не возвращался.
        id_cache = load_id_cache()
        changed_id_cache = False
        for art in touched_articles:
            a = str(art or "").strip()
            if not a:
                continue
            rec = id_cache.get(a)
            if isinstance(rec, dict):
                rid = normalize_onliner_id(rec.get("id", ""))
                if rid and rid in ids_to_clear:
                    id_cache.pop(a, None)
                    changed_id_cache = True
        if changed_id_cache:
            save_id_cache(id_cache)

        bindings = load_manual_id_bindings()
        changed_bindings = False
        for nk in touched_name_keys:
            k = str(nk or "").strip()
            if not k:
                continue
            rec = bindings.get(k)
            if isinstance(rec, dict):
                rid = normalize_onliner_id(rec.get("id", ""))
                if rid and rid in ids_to_clear:
                    bindings.pop(k, None)
                    changed_bindings = True
        if changed_bindings:
            save_manual_id_bindings(bindings)

    return jsonify({"status": "ok", "cleared": int(cleared)})


@app.route("/api/clear-all-nonpc-onliner-ids", methods=["POST"])
def api_clear_all_nonpc_onliner_ids():
    session_dir = session.get("session_dir")
    if not session_dir:
        return jsonify({"status": "error", "message": "Нет активной сессии"}), 400
    cons_path = Path(session_dir) / "consolidated_price.xlsx"
    if not cons_path.exists():
        return jsonify({"status": "error", "message": "Нет данных для обработки"}), 400

    try:
        df = read_consolidated_df(session_dir)
        if "OnlinerID" not in df.columns:
            return jsonify({"status": "ok", "cleared": 0, "kept_pc": 0, "message": "В прайсе нет OnlinerID."})
        if "Ссылка" not in df.columns:
            df["Ссылка"] = ""
        df["OnlinerID"] = df["OnlinerID"].astype("object")
        df["Ссылка"] = df["Ссылка"].astype("object")

        cleared = 0
        kept_pc = 0
        skipped_other_suppliers = 0
        journal_changes = []
        affected_name_keys = set()
        affected_articles = set()
        ntech_supplier_names = {"N-TECH", "NTECH"}

        for idx, row in df.iterrows():
            oid = normalize_onliner_id(row.get("OnlinerID", ""))
            if not oid:
                continue
            supplier = str(row.get("Поставщик", "")).strip().upper()
            if supplier not in ntech_supplier_names:
                skipped_other_suppliers += 1
                continue
            name = str(row.get("Название", "")).strip()
            if _is_tgpc_pc_name(name):
                kept_pc += 1
                continue
            old_url = str(row.get("Ссылка", "")).strip()
            df.at[idx, "OnlinerID"] = np.nan
            df.at[idx, "Ссылка"] = ""
            cleared += 1
            name_key = _normalize_name_key(name)
            if name_key:
                affected_name_keys.add(name_key)
            article_key = str(_get_id_cache_key_for_name(name) or "").strip()
            if article_key:
                affected_articles.add(article_key)
            journal_changes.append({
                "row_idx": int(idx),
                "name": name,
                "old_onliner_id": oid,
                "old_url": old_url,
                "new_onliner_id": "",
                "new_url": "",
                "reason": "bulk_clear_nonpc_before_rematch",
            })

        write_consolidated_df(session_dir, df)
        write_consolidated_json(df, Path(session_dir) / "consolidated.json")

        if journal_changes:
            append_id_change_journal({
                "ts": int(time.time()),
                "action": "clear_all_nonpc_onliner_ids",
                "session_dir": str(session_dir),
                "source": "api_clear_all_nonpc_onliner_ids",
                "changes": journal_changes,
            })

        queue = load_review_queue()
        changed_queue = False
        for name_key in list(affected_name_keys):
            if name_key in queue:
                queue.pop(name_key, None)
                changed_queue = True
        if changed_queue:
            save_review_queue(queue)

        bindings = load_manual_id_bindings()
        changed_bindings = False
        cleared_manual_bindings = 0
        for name_key in list(affected_name_keys):
            if name_key in bindings:
                bindings.pop(name_key, None)
                changed_bindings = True
                cleared_manual_bindings += 1
        if changed_bindings:
            save_manual_id_bindings(bindings)

        id_cache = load_id_cache()
        changed_id_cache = False
        cleared_id_cache = 0
        for article_key in list(affected_articles):
            if article_key in id_cache:
                id_cache.pop(article_key, None)
                changed_id_cache = True
                cleared_id_cache += 1
        if changed_id_cache:
            save_id_cache(id_cache)

        return jsonify({
            "status": "ok",
            "cleared": int(cleared),
            "kept_pc": int(kept_pc),
            "skipped_other_suppliers": int(skipped_other_suppliers),
            "cleared_manual_bindings": int(cleared_manual_bindings),
            "cleared_id_cache": int(cleared_id_cache),
            "message": f"N-Tech: очищено ID {cleared}. ПЭВМ сохранено: {kept_pc}. Кэш N-Tech тоже очищен. Остальные поставщики не тронуты.",
        })
    except Exception as e:
        return jsonify({"status": "error", "message": str(e)[:220]}), 500


@app.route("/api/clear-ntech-duplicate-onliner-ids", methods=["POST"])
def api_clear_ntech_duplicate_onliner_ids():
    session_dir = session.get("session_dir")
    if not session_dir:
        return jsonify({"status": "error", "message": "Нет активной сессии"}), 400
    cons_path = Path(session_dir) / "consolidated_price.xlsx"
    if not cons_path.exists():
        return jsonify({"status": "error", "message": "Нет данных для обработки"}), 400

    try:
        df = read_consolidated_df(session_dir)
        if "OnlinerID" not in df.columns:
            return jsonify({"status": "ok", "cleared": 0, "duplicate_ids": 0, "message": "В прайсе нет OnlinerID."})
        if "Ссылка" not in df.columns:
            df["Ссылка"] = ""
        df["OnlinerID"] = df["OnlinerID"].astype("object")
        df["Ссылка"] = df["Ссылка"].astype("object")

        ntech_supplier_names = {"N-TECH", "NTECH"}
        ntech_rows = []
        id_counts = {}
        for idx, row in df.iterrows():
            supplier = str(row.get("Поставщик", "")).strip().upper()
            if supplier not in ntech_supplier_names:
                continue
            oid = normalize_onliner_id(row.get("OnlinerID", ""))
            if not oid:
                continue
            ntech_rows.append((idx, row, oid))
            id_counts[oid] = int(id_counts.get(oid, 0)) + 1

        duplicate_ids = {oid for oid, cnt in id_counts.items() if cnt > 1}
        if not duplicate_ids:
            return jsonify({
                "status": "ok",
                "cleared": 0,
                "duplicate_ids": 0,
                "message": "У N-Tech не найдено дублирующихся OnlinerID.",
            })

        cleared = 0
        journal_changes = []
        affected_name_keys = set()
        affected_articles = set()

        for idx, row, oid in ntech_rows:
            if oid not in duplicate_ids:
                continue
            name = str(row.get("Название", "")).strip()
            old_url = str(row.get("Ссылка", "")).strip()
            df.at[idx, "OnlinerID"] = np.nan
            df.at[idx, "Ссылка"] = ""
            cleared += 1
            name_key = _normalize_name_key(name)
            if name_key:
                affected_name_keys.add(name_key)
            article_key = str(_get_id_cache_key_for_name(name) or "").strip()
            if article_key:
                affected_articles.add(article_key)
            journal_changes.append({
                "row_idx": int(idx),
                "name": name,
                "old_onliner_id": oid,
                "old_url": old_url,
                "new_onliner_id": "",
                "new_url": "",
                "reason": "bulk_clear_ntech_duplicate_ids",
            })

        write_consolidated_df(session_dir, df)
        write_consolidated_json(df, Path(session_dir) / "consolidated.json")

        append_id_change_journal({
            "ts": int(time.time()),
            "action": "clear_ntech_duplicate_onliner_ids",
            "session_dir": str(session_dir),
            "source": "api_clear_ntech_duplicate_onliner_ids",
            "changes": journal_changes,
        })

        queue = load_review_queue()
        queue_changed = False
        for name_key in list(affected_name_keys):
            if name_key in queue:
                queue.pop(name_key, None)
                queue_changed = True
        if queue_changed:
            save_review_queue(queue)

        bindings = load_manual_id_bindings()
        changed_bindings = False
        cleared_manual_bindings = 0
        for name_key in list(affected_name_keys):
            if name_key in bindings:
                bindings.pop(name_key, None)
                changed_bindings = True
                cleared_manual_bindings += 1
        if changed_bindings:
            save_manual_id_bindings(bindings)

        id_cache = load_id_cache()
        changed_id_cache = False
        cleared_id_cache = 0
        for article_key in list(affected_articles):
            if article_key in id_cache:
                id_cache.pop(article_key, None)
                changed_id_cache = True
                cleared_id_cache += 1
        if changed_id_cache:
            save_id_cache(id_cache)

        return jsonify({
            "status": "ok",
            "cleared": int(cleared),
            "duplicate_ids": int(len(duplicate_ids)),
            "cleared_manual_bindings": int(cleared_manual_bindings),
            "cleared_id_cache": int(cleared_id_cache),
            "message": (
                f"N-Tech: очищено строк-дублей {cleared} "
                f"(уникальных ID: {len(duplicate_ids)})."
            ),
        })
    except Exception as e:
        return jsonify({"status": "error", "message": str(e)[:220]}), 500


def _verify_all_ids_one(row_idx, row):
    app_settings = load_app_settings()
    verify_cfg = (app_settings.get("verify_id") or {})
    local_name = str(row.get("Название", "")).strip()
    oid = normalize_onliner_id(row.get("OnlinerID", ""))
    supplier = str(row.get("Поставщик", "")).strip()
    category = str(row_category(row)).strip()
    if not oid:
        return None

    info = fetch_onliner_product_info(
        oid,
        force_refresh=_coerce_bool(verify_cfg.get("force_refresh_api", True), default=True),
        use_cache_on_error=True,
        product_name_hint=local_name,
    )
    api_name = str(info.get("name", "")).strip()
    api_url = str(info.get("url", "")).strip()
    source = str(info.get("source", "")).strip()
    manual_ok = _coerce_bool(verify_cfg.get("trust_manual_confirmed", True), default=True) and is_manually_confirmed_id(local_name, oid)

    if manual_ok:
        return {
            "row_idx": int(row_idx),
            "onliner_id": oid,
            "name": local_name,
            "supplier": supplier,
            "category": category,
            "api_name": api_name,
            "api_url": api_url,
            "score": 1.0,
            "reason": "manual_confirmed",
            "reason_label": "ID подтвержден вручную и сохранен в постоянный кеш",
            "status": "match",
            "status_label": "OK",
            "source": (source + "|manual_confirmed").strip("|"),
            "needs_review": False,
        }

    if not api_name:
        api_no_name_status = str(verify_cfg.get("api_no_name_status", "review")).strip().lower()
        is_mismatch = api_no_name_status == "mismatch"
        return {
            "row_idx": int(row_idx),
            "onliner_id": oid,
            "name": local_name,
            "supplier": supplier,
            "category": category,
            "api_name": "",
            "api_url": api_url,
            "score": 0.0,
            "reason": "api_no_name",
            "reason_label": "API не вернул название товара по текущему ID",
            "status": "mismatch" if is_mismatch else "review",
            "status_label": "Проверить",
            "source": source,
            "needs_review": True,
        }

    cmp = calc_name_match(local_name, api_name)
    score = round(float(cmp.get("score", 0.0) or 0.0), 3)
    is_match = bool(cmp.get("match"))
    threshold = _coerce_float(verify_cfg.get("match_threshold", 0.74), 0.74, min_value=0.1, max_value=0.99)
    if score >= threshold:
        is_match = True
    if _coerce_bool(verify_cfg.get("require_article_or_model_priority", False), default=False):
        if str(cmp.get("reason", "") or "") not in {"article", "article_like", "paren_model", "model_token"}:
            is_match = False
    return {
        "row_idx": int(row_idx),
        "onliner_id": oid,
        "name": local_name,
        "supplier": supplier,
        "category": category,
        "api_name": api_name,
        "api_url": api_url,
        "score": score,
        "reason": str(cmp.get("reason", "") or ""),
        "reason_label": "Совпало" if is_match else "Название товара не совпало с Onliner по текущему ID",
        "status": "match" if is_match else "mismatch",
        "status_label": "OK" if is_match else "Несовпадение",
        "source": source,
        "needs_review": not is_match,
    }


def _verify_all_ids_worker(session_dir):
    global verify_all_ids_status
    try:
        df = read_consolidated_df(session_dir)
        df = ensure_category_column(df)
        df = apply_visibility_filter(df, session_dir)
        if "OnlinerID" not in df.columns:
            with VERIFY_ALL_IDS_LOCK:
                verify_all_ids_status.update({
                    "running": False,
                    "total": 0,
                    "done": 0,
                    "matched": 0,
                    "mismatched": 0,
                    "errors": 0,
                    "items": [],
                    "report_items": [],
                    "finished_at": int(time.time()),
                    "message": "В текущем прайсе нет колонки OnlinerID.",
                })
            return

        tasks = []
        skipped_tgpc_pc = 0
        for i, row in df.iterrows():
            oid = normalize_onliner_id(row.get("OnlinerID", ""))
            if not oid:
                continue
            # TGPC ПЭВМ верифицируются отдельной функцией автоподбора — пропускаем
            name = str(row.get("Название", "")).strip()
            if _is_tgpc_pc_name(name):
                skipped_tgpc_pc += 1
                continue
            tasks.append((int(i), row.copy()))

        skip_note = f" (TGPC ПЭВМ пропущено: {skipped_tgpc_pc})" if skipped_tgpc_pc else ""
        with VERIFY_ALL_IDS_LOCK:
            verify_all_ids_status.update({
                "total": len(tasks),
                "done": 0,
                "matched": 0,
                "mismatched": 0,
                "errors": 0,
                "items": [],
                "report_items": [],
                "message": f"Проверка ID запущена{skip_note}.",
            })

        if not tasks:
            with VERIFY_ALL_IDS_LOCK:
                verify_all_ids_status.update({
                    "running": False,
                    "finished_at": int(time.time()),
                    "message": "В текущем прайсе нет товаров с OnlinerID.",
                })
            return

        result_items = []
        report_items = []
        matched = 0
        mismatched = 0
        errors = 0
        workers = max(1, min(get_onliner_api_max_workers(default=8), 10, len(tasks)))
        with ThreadPoolExecutor(max_workers=workers) as ex:
            futures = {ex.submit(_verify_all_ids_one, row_idx, row): row_idx for row_idx, row in tasks}
            for fut in as_completed(futures):
                try:
                    item = fut.result()
                except Exception:
                    item = None
                    errors += 1
                else:
                    if item is None:
                        pass
                    else:
                        report_items.append(item)
                        if item.get("needs_review"):
                            mismatched += 1
                            result_items.append(item)
                        else:
                            matched += 1
                finally:
                    with VERIFY_ALL_IDS_LOCK:
                        verify_all_ids_status["done"] = int(verify_all_ids_status.get("done", 0) or 0) + 1
                        verify_all_ids_status["matched"] = int(matched)
                        verify_all_ids_status["mismatched"] = int(mismatched)
                        verify_all_ids_status["errors"] = int(errors)

        result_items.sort(key=lambda x: (float(x.get("score", 0.0) or 0.0), str(x.get("name", "")).lower()))
        report_items.sort(key=lambda x: (
            0 if str(x.get("status", "")).strip().lower() == "mismatch" else 1,
            0 if str(x.get("status", "")).strip().lower() == "review" else 1,
            str(x.get("name", "")).lower(),
        ))
        with VERIFY_ALL_IDS_LOCK:
            verify_all_ids_status.update({
                "running": False,
                "matched": int(matched),
                "mismatched": int(mismatched),
                "errors": int(errors),
                "items": result_items,
                "report_items": report_items,
                "finished_at": int(time.time()),
                "message": f"Проверено ID: {len(tasks)}. Несовпадений: {mismatched}.{skip_note}",
            })
    except Exception as e:
        with VERIFY_ALL_IDS_LOCK:
            verify_all_ids_status.update({
                "running": False,
                "finished_at": int(time.time()),
                "message": "Ошибка проверки ID: " + str(e)[:180],
            })


def _validate_clean_ids_worker(session_dir):
    """Валидация + очистка ID:
    Фаза 1 (параллельно): проверяет соответствие каждого OnlinerID по Onliner API.
      - score >= 0.65  → подтверждает в manual_bindings
      - score < 0.65   → очищает ID, ставит в список на поиск кандидатов
    Фаза 2 (последовательно): для очищенных ищет топ-3 кандидата → очередь проверки.
    """
    global validate_clean_ids_status
    CLEAR_THRESHOLD = 0.65
    try:
        df = read_consolidated_df(session_dir)
        df = ensure_category_column(df)
        if "OnlinerID" not in df.columns:
            with VALIDATE_CLEAN_IDS_LOCK:
                validate_clean_ids_status.update({
                    "running": False, "finished_at": int(time.time()),
                    "message": "В текущем прайсе нет колонки OnlinerID.",
                })
            return

        manual_bindings = load_manual_id_bindings()
        review_queue = load_review_queue()
        app_settings = load_app_settings()
        no_id_cfg = (app_settings.get("no_id_search") or {})
        limit_cands = max(10, min(int(no_id_cfg.get("max_candidates", 80) or 80), 80))

        # Собираем задачи: только товары с ID и не TGPC ПК
        tasks = []
        for i, row in df.iterrows():
            oid = normalize_onliner_id(row.get("OnlinerID", ""))
            if not oid:
                continue
            name = str(row.get("Название", "")).strip()
            if not name:
                continue
            if _is_tgpc_pc_name(name):
                continue
            tasks.append((int(i), row.copy()))

        with VALIDATE_CLEAN_IDS_LOCK:
            validate_clean_ids_status.update({
                "total": len(tasks), "done": 0,
                "confirmed": 0, "cleared": 0, "queued": 0, "errors": 0,
                "message": f"Фаза 1: проверяю {len(tasks)} товаров...",
            })

        if not tasks:
            with VALIDATE_CLEAN_IDS_LOCK:
                validate_clean_ids_status.update({
                    "running": False, "finished_at": int(time.time()),
                    "message": "Нет товаров с OnlinerID для проверки (или все TGPC ПЭВМ).",
                })
            return

        confirmed = 0
        cleared_items = []   # list of (row_idx, name, old_id, api_name, score)
        confirmed_rows = []  # for report: {name, api_name, onliner_id, score}
        skipped_rows = []    # API недоступен — ID не трогали
        skipped_api = 0
        errors = 0
        done = 0
        journal_changes = []

        # ─── Фаза 1: параллельная проверка существующих ID ───────────────────
        # Для валидации используем кэш (force_refresh=False) чтобы не перегружать
        # Onliner API 1600+ одновременными запросами.
        # Кэш обновляется не чаще раза в 7 дней (ONLINER_PRODUCT_CACHE_TTL).
        # Загружаем кэш один раз — потоки читают его без доп. запросов к диску
        product_cache = load_onliner_product_cache()

        def _fetch_product_name_hard_timeout(oid, hard_timeout=8):
            """Получает название товара по ID с ЖЕЛЕЗНЫМ таймаутом через отдельный поток.
            Возвращает (name, url, status):
              status == 'ok'            — JSON получен, name непустой
              status == 'not_found'   — HTTP 404 (товара нет — ID неверный)
              status == 'empty_payload' — 200, но без названия (не очищаем ID)
              status == 'http_error'  — другой HTTP-код
              status == 'error'       — сеть/парсинг
              status == 'timeout'     — зависание / очередь
            """
            import urllib.error as _urlerr
            _q = queue.Queue()
            def _fetch_worker():
                try:
                    url = f"https://catalog.api.onliner.by/products/{oid}"
                    req = urllib.request.Request(url, headers={
                        "User-Agent": "Mozilla/5.0",
                        "Accept": "application/json",
                    })
                    with urllib.request.urlopen(req, timeout=6) as resp:
                        data = json.loads(resp.read().decode("utf-8", errors="replace")) or {}
                    name_ = str(data.get("full_name") or data.get("name") or "").strip()
                    url_  = str(data.get("html_url") or "").strip()
                    if name_:
                        _q.put((name_, url_, "ok"))
                    else:
                        _q.put(("", url_, "empty_payload"))
                except _urlerr.HTTPError as e:
                    if e.code == 404:
                        _q.put(("", "", "not_found"))
                    else:
                        print(f"[validate] HTTP {e.code} oid={oid}", flush=True)
                        _q.put(("", "", "http_error"))
                except Exception as e:
                    print(f"[validate] fetch_error oid={oid}: {e}", flush=True)
                    _q.put(("", "", "error"))
            t = threading.Thread(target=_fetch_worker, daemon=True)
            t.start()
            try:
                return _q.get(timeout=hard_timeout)
            except queue.Empty:
                print(f"[validate] ТАЙМАУТ {hard_timeout}с для oid={oid}", flush=True)
                return ("", "", "timeout")

        def _validate_one(row_idx, row):
            """Быстрая проверка: сначала кэш, потом один запрос с железным таймаутом.
            Поля record_confirm / mutate_df_clear:
              — mutate_df_clear=True только при явно неверном ID (404) или низком score.
              — при сбое API / таймауте оба False (ID в прайсе НЕ трогаем).
            """
            local_name = str(row.get("Название", "")).strip()
            oid = normalize_onliner_id(row.get("OnlinerID", ""))
            if not oid:
                return None

            if is_manually_confirmed_id(local_name, oid):
                return {
                    "row_idx": int(row_idx), "onliner_id": oid,
                    "name": local_name, "api_name": "", "api_url": "",
                    "score": 1.0, "reason": "manual_confirmed",
                    "record_confirm": True, "mutate_df_clear": False,
                }

            now_ts = int(time.time())
            api_name = ""
            api_url = ""
            cached = product_cache.get(oid)
            if isinstance(cached, dict) and now_ts - int(cached.get("updated_at", 0)) <= ONLINER_PRODUCT_CACHE_TTL:
                api_name = str(cached.get("name", "")).strip()
                api_url = str(cached.get("url", "")).strip()
                if not api_name:
                    # Устаревший пустой кэш — не считаем это «товар не найден»
                    return {
                        "row_idx": int(row_idx), "onliner_id": oid,
                        "name": local_name, "api_name": "", "api_url": api_url,
                        "score": 0.0, "reason": "api_unreachable_cached_empty",
                        "record_confirm": False, "mutate_df_clear": False,
                    }
            else:
                api_name, api_url, fetch_status = _fetch_product_name_hard_timeout(oid, hard_timeout=9)
                if fetch_status == "not_found":
                    return {
                        "row_idx": int(row_idx), "onliner_id": oid,
                        "name": local_name, "api_name": "", "api_url": "",
                        "score": 0.0, "reason": "api_not_found",
                        "record_confirm": False, "mutate_df_clear": True,
                    }
                if fetch_status in ("timeout", "http_error", "error", "empty_payload"):
                    return {
                        "row_idx": int(row_idx), "onliner_id": oid,
                        "name": local_name, "api_name": "", "api_url": api_url,
                        "score": 0.0, "reason": "api_unreachable_" + fetch_status,
                        "record_confirm": False, "mutate_df_clear": False,
                    }
                if fetch_status == "ok" and api_name:
                    with ONLINER_PRODUCT_CACHE_LOCK:
                        product_cache[oid] = {
                            "updated_at": now_ts, "name": api_name, "url": api_url,
                        }

            if not api_name:
                return {
                    "row_idx": int(row_idx), "onliner_id": oid,
                    "name": local_name, "api_name": "", "api_url": api_url,
                    "score": 0.0, "reason": "api_unreachable_no_name",
                    "record_confirm": False, "mutate_df_clear": False,
                }

            cmp = calc_name_match(local_name, api_name)
            score = round(float(cmp.get("score", 0.0) or 0.0), 3)
            if score >= CLEAR_THRESHOLD:
                return {
                    "row_idx": int(row_idx), "onliner_id": oid,
                    "name": local_name, "api_name": api_name, "api_url": api_url,
                    "score": score, "reason": str(cmp.get("reason", "") or ""),
                    "record_confirm": True, "mutate_df_clear": False,
                }
            return {
                "row_idx": int(row_idx), "onliner_id": oid,
                "name": local_name, "api_name": api_name, "api_url": api_url,
                "score": score, "reason": str(cmp.get("reason", "") or ""),
                "record_confirm": False, "mutate_df_clear": True,
            }

        # ─── Фаза 1: последовательная проверка (без ThreadPoolExecutor) ───────────
        # Параллелизм убран намеренно: на macOS и Windows при 6-8 одновременных
        # daemon-потоков q.get(timeout=N) нестабильно возвращает Empty → вечный зависон.
        # При последовательной обработке каждый _fetch_product_name_hard_timeout
        # гарантированно завершится за ≤9 сек.
        print(f"[validate] Старт Фазы 1: {len(tasks)} товаров", flush=True)
        for task_i, (row_idx, row) in enumerate(tasks):
            name     = str(row.get("Название", "")).strip()
            oid      = normalize_onliner_id(row.get("OnlinerID", ""))
            name_key = _normalize_name_key(name)
            result   = None
            try:
                result = _validate_one(row_idx, row)
            except Exception as exc:
                print(f"[validate] ОШИБКА #{task_i+1}: {name[:50]} | {exc}", flush=True)
                errors += 1

            done += 1
            if done % 50 == 0 or done == 1:
                print(f"[validate] Фаза 1: {done}/{len(tasks)} — {name[:60]}", flush=True)

            with VALIDATE_CLEAN_IDS_LOCK:
                validate_clean_ids_status.update({
                    "done": done,
                    "confirmed": confirmed,
                    "cleared": len(cleared_items),
                    "skipped_api": skipped_api,
                    "errors": errors,
                    "message": f"Фаза 1: {done}/{len(tasks)} — {name[:55]}",
                })

            if result is None:
                continue

            score = float(result.get("score", 0.0) or 0.0)
            is_manual = result.get("reason") == "manual_confirmed"
            do_confirm = bool(result.get("record_confirm"))
            do_clear = bool(result.get("mutate_df_clear"))

            if not do_confirm and not do_clear:
                skipped_api += 1
                skipped_rows.append({
                    "name": name, "onliner_id": oid,
                    "reason": str(result.get("reason", "") or "api_unreachable"),
                })
                continue

            if do_confirm:
                if name_key and not is_manual:
                    manual_bindings[name_key] = {
                        "id": oid,
                        "url": str(result.get("api_url", "")).strip(),
                    }
                confirmed += 1
                confirmed_rows.append({
                    "name": name, "onliner_id": oid,
                    "api_name": str(result.get("api_name", "")).strip(),
                    "score": round(score, 3),
                })

            if not do_clear:
                continue

            old_url = str(df.at[row_idx, "Ссылка"]).strip() if "Ссылка" in df.columns else ""
            # OnlinerID может быть float-колонкой (NaN) — записываем NaN, не ""
            try:
                df.at[row_idx, "OnlinerID"] = np.nan
            except Exception:
                try:
                    df["OnlinerID"] = df["OnlinerID"].astype(object)
                    df.at[row_idx, "OnlinerID"] = ""
                except Exception:
                    pass
            if "Ссылка" in df.columns:
                try:
                    df.at[row_idx, "Ссылка"] = np.nan
                except Exception:
                    try:
                        df["Ссылка"] = df["Ссылка"].astype(object)
                        df.at[row_idx, "Ссылка"] = ""
                    except Exception:
                        pass
            _reason = str(result.get("reason", "") or "")
            if _reason == "api_not_found":
                _jr = "validate_clean api_not_found"
            else:
                _jr = f"validate_clean score={round(score, 3)}"
            journal_changes.append({
                "row_idx": row_idx, "name": name,
                "old_onliner_id": oid, "old_url": old_url,
                "new_onliner_id": "", "new_url": "",
                "reason": _jr,
            })
            cleared_items.append((
                row_idx, name, name_key, oid,
                str(result.get("api_name", "")).strip(),
                round(score, 3),
                _reason,
            ))

        print(
            f"[validate] Фаза 1 завершена: подтверждено={confirmed}, очищено={len(cleared_items)}, "
            f"пропущено (API): {skipped_api}, ошибок={errors}",
            flush=True,
        )

        # ─── Фаза 2: ищем кандидатов для очищенных товаров ──────────────────
        queued = 0
        if cleared_items:
            with VALIDATE_CLEAN_IDS_LOCK:
                validate_clean_ids_status.update({
                    "message": f"Фаза 2: ищу кандидатов для {len(cleared_items)} очищенных товаров...",
                })
            for ci_idx, (row_idx, name, name_key, old_id, api_name, score, _clr_reason) in enumerate(cleared_items, 1):
                with VALIDATE_CLEAN_IDS_LOCK:
                    validate_clean_ids_status["message"] = (
                        f"Фаза 2: кандидаты {ci_idx}/{len(cleared_items)} — {name[:50]}"
                    )
                try:
                    candidates = search_onliner_candidates(
                        name, category_name="", query="",
                        limit=min(limit_cands, 10), max_queries=3, timeout_sec=6,
                    )
                    top3 = [
                        {
                            "id": normalize_onliner_id(c.get("id", "")),
                            "name": str(c.get("name", "")).strip(),
                            "score": round(float(c.get("score", 0) or 0), 3),
                            "url": str(c.get("url", "")).strip(),
                        }
                        for c in (candidates[:3] if candidates else [])
                        if normalize_onliner_id(c.get("id", ""))
                    ]
                except Exception:
                    top3 = []

                if name_key:
                    review_queue[name_key] = {
                        "name": name,
                        "cleared_id": old_id,
                        "cleared_score": score,
                        "onliner_name": api_name,
                        "candidates": top3,
                        "added_at": int(time.time()),
                    }
                if top3:
                    queued += 1
                with VALIDATE_CLEAN_IDS_LOCK:
                    validate_clean_ids_status["queued"] = queued

        # ─── Сохраняем результаты ────────────────────────────────────────────
        save_manual_id_bindings(manual_bindings)
        save_review_queue(review_queue)
        if journal_changes:
            append_id_change_journal({
                "ts": int(time.time()),
                "action": "validate_clean_ids",
                "session_dir": str(session_dir),
                "source": "api_validate_clean_ids",
                "changes": journal_changes,
            })
        write_consolidated_df(session_dir, df)
        write_consolidated_json(df, Path(session_dir) / "consolidated.json")

        cleared_count = len(cleared_items)
        # Build cleared_rows for UI report
        cleared_rows_report = []
        for tup in cleared_items:
            row_idx, name, name_key, old_id, api_name, score, clr_reason = (
                tup if len(tup) >= 7 else (*tup[:6], "")
            )
            if clr_reason == "api_not_found":
                api_disp = "HTTP 404 — товара с этим ID нет в каталоге Onliner"
            elif api_name:
                api_disp = api_name
            else:
                api_disp = "—"
            cleared_rows_report.append({
                "name": name, "onliner_id": old_id, "api_name": api_disp,
                "score": score, "clear_reason": clr_reason,
            })
        with VALIDATE_CLEAN_IDS_LOCK:
            validate_clean_ids_status.update({
                "running": False,
                "done": len(tasks),
                "confirmed": confirmed,
                "cleared": cleared_count,
                "skipped_api": skipped_api,
                "queued": queued,
                "errors": errors,
                "finished_at": int(time.time()),
                "cleared_rows":    cleared_rows_report,
                "confirmed_rows":  confirmed_rows,
                "skipped_rows":    skipped_rows[:500],
                "message": (
                    f"Готово. Подтверждено: {confirmed}, очищено: {cleared_count}"
                    + (f", пропущено (сбой API, ID сохранён): {skipped_api}" if skipped_api else "")
                    + (f", ошибок: {errors}" if errors else "")
                    + "."
                ),
            })
    except Exception as e:
        with VALIDATE_CLEAN_IDS_LOCK:
            validate_clean_ids_status.update({
                "running": False,
                "finished_at": int(time.time()),
                "message": "Ошибка валидации: " + str(e)[:180],
            })


def _validate_clean_ids_db_worker(session_dir):
    """Быстрая локальная сверка OnlinerID по SQLite-базе без Onliner API.
    Чистит только явные мусорные ID:
      - текущий ID есть в БД, но имя товара плохо совпадает;
      - точный name_key в БД указывает на другой OnlinerID.
    Если ID нет в локальной БД и точного имени нет — строку не трогаем.
    """
    global validate_clean_ids_status
    CLEAR_THRESHOLD = 0.65
    try:
        df = read_consolidated_df(session_dir)
        df = ensure_category_column(df)
        if "OnlinerID" not in df.columns:
            with VALIDATE_CLEAN_IDS_LOCK:
                validate_clean_ids_status.update({
                    "running": False,
                    "finished_at": int(time.time()),
                    "message": "В текущем прайсе нет колонки OnlinerID.",
                })
            return

        manual_bindings = load_manual_id_bindings()
        review_queue = load_review_queue()
        app_settings = load_app_settings()
        no_id_cfg = (app_settings.get("no_id_search") or {})
        limit_cands = max(10, min(int(no_id_cfg.get("max_candidates", 80) or 80), 80))

        tasks = []
        for i, row in df.iterrows():
            oid = normalize_onliner_id(row.get("OnlinerID", ""))
            if not oid:
                continue
            name = str(row.get("Название", "")).strip()
            if not name or _is_tgpc_pc_name(name):
                continue
            tasks.append((int(i), row.copy()))

        with VALIDATE_CLEAN_IDS_LOCK:
            validate_clean_ids_status.update({
                "total": len(tasks),
                "done": 0,
                "confirmed": 0,
                "cleared": 0,
                "queued": 0,
                "errors": 0,
                "skipped_api": 0,
                "mode": "db",
                "mode_label": "Локальная БД 150k",
                "skipped_label": "Пропуск = ID или имя не найдены в локальной БД, поэтому ID оставили без изменений.",
                "message": f"Локальная сверка: проверяю {len(tasks)} товаров...",
            })

        if not tasks:
            with VALIDATE_CLEAN_IDS_LOCK:
                validate_clean_ids_status.update({
                    "running": False,
                    "finished_at": int(time.time()),
                    "message": "Нет товаров с OnlinerID для локальной проверки (или все TGPC ПЭВМ).",
                })
            return

        confirmed = 0
        cleared_items = []
        confirmed_rows = []
        skipped_rows = []
        skipped_local = 0
        errors = 0
        done = 0
        journal_changes = []

        for row_idx, row in tasks:
            name = str(row.get("Название", "")).strip()
            oid = normalize_onliner_id(row.get("OnlinerID", ""))
            name_key = _normalize_name_key(name)
            try:
                if is_manually_confirmed_id(name, oid):
                    result = {
                        "status": "confirm",
                        "db_name": "",
                        "score": 1.0,
                        "reason": "manual_confirmed",
                        "exact_match": None,
                    }
                else:
                    current_db = db_get_product_by_id(oid)
                    exact_match = db_find_exact_id_for_name(name)
                    if current_db:
                        db_name = str(current_db.get("name", "")).strip()
                        cmp = calc_name_match(name, db_name)
                        score = round(float(cmp.get("score", 0.0) or 0.0), 3)
                        if score >= CLEAR_THRESHOLD:
                            result = {
                                "status": "confirm",
                                "db_name": db_name,
                                "score": score,
                                "reason": str(cmp.get("reason", "") or ""),
                                "exact_match": exact_match,
                            }
                        else:
                            result = {
                                "status": "clear",
                                "db_name": db_name,
                                "score": score,
                                "reason": "db_id_name_mismatch",
                                "exact_match": exact_match,
                            }
                    elif exact_match and normalize_onliner_id(exact_match.get("id", "")) != oid:
                        result = {
                            "status": "clear",
                            "db_name": str(exact_match.get("name", "")).strip(),
                            "score": 0.0,
                            "reason": "db_exact_points_other_id",
                            "exact_match": exact_match,
                        }
                    else:
                        result = {
                            "status": "skip",
                            "db_name": "",
                            "score": 0.0,
                            "reason": "db_missing_or_uncertain",
                            "exact_match": exact_match,
                        }
            except Exception as exc:
                print(f"[validate-db] ОШИБКА {name[:60]} | {exc}", flush=True)
                errors += 1
                result = None

            done += 1
            if done % 100 == 0 or done == 1:
                print(f"[validate-db] {done}/{len(tasks)} — {name[:60]}", flush=True)

            if result is None:
                with VALIDATE_CLEAN_IDS_LOCK:
                    validate_clean_ids_status.update({
                        "done": done,
                        "confirmed": confirmed,
                        "cleared": len(cleared_items),
                        "skipped_api": skipped_local,
                        "errors": errors,
                        "message": f"Локальная сверка: {done}/{len(tasks)} — {name[:55]}",
                    })
                continue

            status = str(result.get("status", "") or "")
            score = round(float(result.get("score", 0.0) or 0.0), 3)
            db_name = str(result.get("db_name", "") or "").strip()
            reason = str(result.get("reason", "") or "").strip()
            exact_match = result.get("exact_match") if isinstance(result.get("exact_match"), dict) else None

            if status == "skip":
                skipped_local += 1
                skipped_rows.append({
                    "name": name,
                    "onliner_id": oid,
                    "reason": reason,
                })
            elif status == "confirm":
                if name_key and reason != "manual_confirmed":
                    manual_bindings[name_key] = {
                        "id": oid,
                        "url": str((db_get_product_by_id(oid) or {}).get("url", "")).strip(),
                    }
                confirmed += 1
                confirmed_rows.append({
                    "name": name,
                    "onliner_id": oid,
                    "api_name": db_name or "Локальная БД",
                    "score": score,
                })
            elif status == "clear":
                old_url = str(df.at[row_idx, "Ссылка"]).strip() if "Ссылка" in df.columns else ""
                try:
                    df.at[row_idx, "OnlinerID"] = np.nan
                except Exception:
                    df["OnlinerID"] = df["OnlinerID"].astype(object)
                    df.at[row_idx, "OnlinerID"] = ""
                if "Ссылка" in df.columns:
                    try:
                        df.at[row_idx, "Ссылка"] = np.nan
                    except Exception:
                        df["Ссылка"] = df["Ссылка"].astype(object)
                        df.at[row_idx, "Ссылка"] = ""
                journal_changes.append({
                    "row_idx": row_idx,
                    "name": name,
                    "old_onliner_id": oid,
                    "old_url": old_url,
                    "new_onliner_id": "",
                    "new_url": "",
                    "reason": f"validate_clean_db {reason} score={score}",
                })
                cleared_items.append((
                    row_idx,
                    name,
                    name_key,
                    oid,
                    db_name,
                    score,
                    reason,
                    exact_match,
                ))

            with VALIDATE_CLEAN_IDS_LOCK:
                validate_clean_ids_status.update({
                    "done": done,
                    "confirmed": confirmed,
                    "cleared": len(cleared_items),
                    "skipped_api": skipped_local,
                    "errors": errors,
                    "message": f"Локальная сверка: {done}/{len(tasks)} — {name[:55]}",
                })

        queued = 0
        if cleared_items:
            with VALIDATE_CLEAN_IDS_LOCK:
                validate_clean_ids_status.update({
                    "message": f"Локальная сверка: ищу кандидатов для {len(cleared_items)} очищенных товаров...",
                })
            for ci_idx, (row_idx, name, name_key, old_id, db_name, score, clr_reason, exact_match) in enumerate(cleared_items, 1):
                with VALIDATE_CLEAN_IDS_LOCK:
                    validate_clean_ids_status["message"] = f"Локальная сверка: кандидаты {ci_idx}/{len(cleared_items)} — {name[:50]}"
                top_cands = []
                seen_ids = set()

                def _append_candidate(cid, cname, curl, cscore, csource):
                    cid = normalize_onliner_id(cid)
                    if not cid or cid in seen_ids:
                        return
                    seen_ids.add(cid)
                    top_cands.append({
                        "id": cid,
                        "name": str(cname or "").strip(),
                        "score": round(float(cscore or 0.0), 3),
                        "url": str(curl or "").strip(),
                        "source": str(csource or "").strip(),
                    })

                if exact_match:
                    _append_candidate(
                        exact_match.get("id", ""),
                        exact_match.get("name", ""),
                        exact_match.get("url", ""),
                        1.0,
                        exact_match.get("source", "db_exact"),
                    )
                for cand in db_find_top_candidates(name, top_n=5, min_score=0.40):
                    _append_candidate(
                        cand.get("id", ""),
                        cand.get("name", ""),
                        cand.get("url", ""),
                        cand.get("score", 0.0),
                        cand.get("source", "db_fuzzy"),
                    )

                if name_key:
                    review_queue[name_key] = {
                        "name": name,
                        "cleared_id": old_id,
                        "cleared_score": score,
                        "onliner_name": db_name,
                        "candidates": top_cands[:5],
                        "added_at": int(time.time()),
                    }
                if top_cands:
                    queued += 1
                with VALIDATE_CLEAN_IDS_LOCK:
                    validate_clean_ids_status["queued"] = queued

        save_manual_id_bindings(manual_bindings)
        save_review_queue(review_queue)
        if journal_changes:
            append_id_change_journal({
                "ts": int(time.time()),
                "action": "validate_clean_ids_db",
                "session_dir": str(session_dir),
                "source": "api_validate_clean_ids_db",
                "changes": journal_changes,
            })
        write_consolidated_df(session_dir, df)
        write_consolidated_json(df, Path(session_dir) / "consolidated.json")

        cleared_rows_report = []
        for row_idx, name, name_key, old_id, db_name, score, clr_reason, exact_match in cleared_items:
            if clr_reason == "db_exact_points_other_id" and exact_match:
                db_disp = f"Локальная БД знает этот товар как ID {normalize_onliner_id(exact_match.get('id', ''))}"
            else:
                db_disp = db_name or "—"
            cleared_rows_report.append({
                "name": name,
                "onliner_id": old_id,
                "api_name": db_disp,
                "score": score,
                "clear_reason": clr_reason,
            })

        with VALIDATE_CLEAN_IDS_LOCK:
            validate_clean_ids_status.update({
                "running": False,
                "done": len(tasks),
                "confirmed": confirmed,
                "cleared": len(cleared_items),
                "skipped_api": skipped_local,
                "queued": queued,
                "errors": errors,
                "finished_at": int(time.time()),
                "cleared_rows": cleared_rows_report,
                "confirmed_rows": confirmed_rows,
                "skipped_rows": skipped_rows[:500],
                "message": (
                    f"Локальная сверка готова. Подтверждено: {confirmed}, очищено: {len(cleared_items)}"
                    + (f", пропущено: {skipped_local}" if skipped_local else "")
                    + (f", ошибок: {errors}" if errors else "")
                    + "."
                ),
            })
    except Exception as e:
        with VALIDATE_CLEAN_IDS_LOCK:
            validate_clean_ids_status.update({
                "running": False,
                "finished_at": int(time.time()),
                "message": "Ошибка локальной проверки: " + str(e)[:180],
            })


@app.route("/api/id-replace-candidates", methods=["POST"])
def api_id_replace_candidates():
    app_settings = load_app_settings()
    no_id_cfg = (app_settings.get("no_id_search") or {})
    payload = request.get_json(silent=True) or {}
    name = str(payload.get("name", "")).strip()
    category = str(payload.get("category", "")).strip()
    query = str(payload.get("query", "")).strip()
    current_id = normalize_onliner_id(payload.get("onliner_id", ""))
    exclude_current = _coerce_bool(payload.get("exclude_current", False), default=False)
    try:
        limit = int(payload.get("limit", 80))
    except Exception:
        limit = 80
    limit = max(10, min(limit, int(no_id_cfg.get("max_candidates", 80) or 80), 150))

    if not name and not query:
        return jsonify({"items": []})

    items = []
    seen = set()

    # 1) Быстрый приоритет: текущий ID (для сравнения) сразу в список.
    # Только локальная БД: без внешних API-запросов.
    if current_id and not exclude_current:
        db_item = db_get_product_by_id(current_id) or {}
        cur_name = str(db_item.get("name", "")).strip()
        cur_url = str(db_item.get("url", "")).strip()
        hints = _category_path_hints(category)
        if hints and cur_url and not any(h in cur_url for h in hints):
            cur_name = ""
            cur_url = ""
        items.append({
            "id": current_id,
            "name": cur_name or f"Текущий ID {current_id}",
            "url": cur_url,
            "score": 0.0,
            "source": "current",
        })
        seen.add(current_id)

    # 2) Только локальная SQLite БД (без B2B и без публичного catalog API).
    local_name = name or query
    local_top = db_find_top_candidates(local_name, top_n=limit, min_score=0.12, allow_b2b=False)
    exact = db_find_exact_id_for_name(local_name)
    local_candidates = []
    if isinstance(exact, dict):
        local_candidates.append(exact)
    if isinstance(local_top, list):
        local_candidates.extend(local_top)
    for c in local_candidates:
        cid = normalize_onliner_id(c.get("id", ""))
        if not cid or cid in seen:
            continue
        items.append({
            "id": cid,
            "name": str(c.get("name", "")).strip(),
            "url": str(c.get("url", "")).strip(),
            "score": round(float(c.get("score", 0.0) or 0.0), 3),
            "source": "local_db",
        })
        seen.add(cid)
        if len(items) >= limit:
            break

    return jsonify({"items": items[:limit]})


def _build_duplicate_onliner_id_issues(df):
    if "OnlinerID" not in df.columns:
        return 0, []

    grouped = {}
    for i, row in df.iterrows():
        oid = normalize_onliner_id(row.get("OnlinerID", ""))
        if not oid:
            continue
        name = str(row.get("Название", "")).strip()
        name_key = _normalize_name_key(name)
        if not name_key:
            continue
        grouped.setdefault(oid, []).append({
            "row_idx": int(i),
            "name": name,
            "name_key": name_key,
            "supplier": str(row.get("Поставщик", "")).strip(),
            "category": str(row_category(row)).strip(),
        })

    product_cache = load_onliner_product_cache()
    issues = []
    problem_ids = 0
    for oid, rows in grouped.items():
        if len(rows) < 2:
            continue
        distinct_name_map = {}
        for item in rows:
            key = str(item.get("name_key") or "").strip()
            if key and key not in distinct_name_map:
                distinct_name_map[key] = str(item.get("name") or "").strip()
        if len(distinct_name_map) < 2:
            continue

        confirmed_rows = []
        pending_rows = []
        for item in rows:
            if is_manually_confirmed_id(item.get("name", ""), oid):
                confirmed_rows.append(item)
            else:
                pending_rows.append(item)

        # Если текущий ID уже подтвержден вручную у части строк,
        # не тревожим их повторно: оставляем в проверке только остальные строки.
        if pending_rows:
            rows_for_review = pending_rows
        else:
            # Если все строки с этим ID уже вручную подтверждены, считаем конфликт обработанным.
            continue

        problem_ids += 1
        cached_info = product_cache.get(oid) if isinstance(product_cache, dict) else {}
        api_name = str((cached_info or {}).get("name", "")).strip()
        api_url = str((cached_info or {}).get("url", "")).strip()
        distinct_names = [n for n in distinct_name_map.values() if n]

        for item in rows_for_review:
            current_name = str(item.get("name") or "").strip()
            current_key = str(item.get("name_key") or "").strip()
            other_names = [n for k, n in distinct_name_map.items() if k != current_key and n]
            reason_parts = []
            if api_name:
                reason_parts.append(f"Текущий ID ведет на {api_name}")
            reason = f"Этот OnlinerID используется у {len(distinct_name_map)} разных товаров"
            if other_names:
                shown = ", ".join(other_names[:2])
                if len(other_names) > 2:
                    shown += ", ..."
                reason += f": {shown}"
            if confirmed_rows:
                reason += f". Уже подтверждено вручную: {len(confirmed_rows)}"
            reason_parts.append(reason)
            reason_label = ". ".join(part for part in reason_parts if part)
            issues.append({
                "row_idx": int(item.get("row_idx", -1)),
                "onliner_id": oid,
                "name": current_name,
                "supplier": str(item.get("supplier") or "").strip(),
                "category": str(item.get("category") or "").strip(),
                "api_name": api_name,
                "api_url": api_url,
                "score": 0.0,
                "reason": "duplicate_onliner_id",
                "reason_label": reason_label,
                "status": "mismatch",
                "status_label": "Одинаковый ID",
                "duplicate_id_count": int(problem_ids),
                "duplicate_row_count": int(len(rows)),
                "duplicate_name_count": int(len(distinct_name_map)),
                "other_names": other_names[:4],
                "needs_review": True,
            })

    issues.sort(key=lambda x: (str(x.get("onliner_id") or ""), str(x.get("name") or "").lower()))
    return int(problem_ids), issues


def apply_export_duplicate_id_filter(df, supplier_names=None):
    supplier_list = _normalize_supplier_name_list(supplier_names or [])
    if df is None or df.empty or not supplier_list:
        return df
    supplier_lookup = {name.strip().lower() for name in supplier_list if str(name or "").strip()}
    if not supplier_lookup:
        return df
    _, issues = _build_duplicate_onliner_id_issues(df)
    if not issues:
        return df
    drop_indexes = set()
    for issue in issues:
        supplier = str(issue.get("supplier") or "").strip().lower()
        row_idx = issue.get("row_idx")
        if supplier in supplier_lookup and isinstance(row_idx, int):
            drop_indexes.add(int(row_idx))
    if not drop_indexes:
        return df
    return df.drop(index=list(drop_indexes), errors="ignore").copy()


def apply_export_keep_lowest_price_per_onliner_id(df):
    if df is None or df.empty:
        return df
    id_col = "OnlinerID" if "OnlinerID" in df.columns else ("onliner_id" if "onliner_id" in df.columns else None)
    if not id_col:
        return df
    price_col = "Цена" if "Цена" in df.columns else ("price" if "price" in df.columns else None)
    temp = df.copy()
    temp["_oid_norm"] = temp[id_col].apply(normalize_onliner_id)
    has_id_mask = temp["_oid_norm"].astype(str) != ""
    if not has_id_mask.any():
        return df
    if price_col:
        raw_prices = temp[price_col].astype(str).str.replace("\xa0", "", regex=False).str.replace(" ", "", regex=False).str.replace(",", ".", regex=False)
        temp["_price_num"] = pd.to_numeric(raw_prices, errors="coerce")
    else:
        temp["_price_num"] = np.nan
    keep_idx = set()
    for _, grp in temp[has_id_mask].groupby("_oid_norm", sort=False):
        grp_with_price = grp[grp["_price_num"].notna()]
        if not grp_with_price.empty:
            keep_idx.add(int(grp_with_price["_price_num"].idxmin()))
        else:
            keep_idx.add(int(grp.index[0]))
    drop_mask = has_id_mask & ~temp.index.isin(keep_idx)
    if not drop_mask.any():
        return df
    return df[~drop_mask].copy()


def _is_pc_export_row(row):
    name = str(row.get("Название", "") or row.get("product_name", "") or "").strip()
    low_name = name.lower()
    if _is_tgpc_pc_name(name):
        return True
    if low_name.startswith("компьютер "):
        return True
    if low_name.startswith("пэвм "):
        return True
    if low_name.startswith("системный блок "):
        return True
    if "iven" in low_name and "компьютер" in low_name:
        return True
    category = str(row.get("Категория", "") or row.get("category", "") or row_category(row) or "").strip().lower()
    if any(token in category for token in [
        "пэвм",
        "системный блок",
        "компьютер",
        "компьютеры / tgpc",
        "готовые решения tgpc",
    ]):
        return True
    link = str(row.get("Ссылка", "") or row.get("url", "") or "").strip().lower()
    if any(token in link for token in ["/desktoppc/", "/computer/", "/monoblock/", "/nettop/"]):
        return True
    onliner_name = str(row.get("Onliner", "") or row.get("onliner_name", "") or "").strip().lower()
    if "iven" in onliner_name and ("superpower" in onliner_name or "gaming" in onliner_name):
        return True
    return False


def apply_export_only_pc_filter(df, supplier_names=None):
    supplier_list = _normalize_supplier_name_list(supplier_names or [])
    if df is None or df.empty or not supplier_list:
        return df
    supplier_lookup = {name.strip().lower() for name in supplier_list if str(name or "").strip()}
    if not supplier_lookup:
        return df
    supplier_col = "Поставщик" if "Поставщик" in df.columns else ("supplier" if "supplier" in df.columns else None)
    if not supplier_col:
        return df
    keep_mask = df.apply(
        lambda row: str(row.get(supplier_col, "") or "").strip().lower() not in supplier_lookup or _is_pc_export_row(row),
        axis=1,
    )
    return df[keep_mask].copy()


@app.route("/api/check-duplicate-onliner-ids", methods=["POST"])
def api_check_duplicate_onliner_ids():
    session_dir = session.get("session_dir")
    if not session_dir:
        return jsonify({"status": "error", "message": "Нет активной сессии"}), 400
    cons_path = Path(session_dir) / "consolidated_price.xlsx"
    if not cons_path.exists():
        return jsonify({"status": "error", "message": "Сводный прайс не найден"}), 400

    df = read_consolidated_df(session_dir)
    df = ensure_category_column(df)
    df = apply_visibility_filter(df, session_dir)
    if df.empty:
        return jsonify({
            "status": "ok",
            "problem_ids": 0,
            "problem_rows": 0,
            "items": [],
            "message": "В текущем прайсе нет строк для проверки.",
        })

    problem_ids, issues = _build_duplicate_onliner_id_issues(df)
    if not issues:
        return jsonify({
            "status": "ok",
            "problem_ids": 0,
            "problem_rows": 0,
            "items": [],
            "message": "Одинаковых OnlinerID у разных товаров не найдено.",
        })

    return jsonify({
        "status": "ok",
        "problem_ids": int(problem_ids),
        "problem_rows": int(len(issues)),
        "items": issues,
        "message": f"Найдено одинаковых OnlinerID: {problem_ids}. Строк для проверки: {len(issues)}.",
    })


@app.route("/api/verify-all-ids-start", methods=["POST"])
def api_verify_all_ids_start():
    global verify_all_ids_status
    session_dir = session.get("session_dir")
    if not session_dir:
        return jsonify({"status": "error", "message": "Нет активной сессии"}), 400
    cons_path = Path(session_dir) / "consolidated_price.xlsx"
    if not cons_path.exists():
        return jsonify({"status": "error", "message": "Нет данных"}), 400

    with VERIFY_ALL_IDS_LOCK:
        if verify_all_ids_status.get("running"):
            return jsonify({"status": "already_running"})
        verify_all_ids_status = {
            "running": True,
            "total": 0,
            "done": 0,
            "matched": 0,
            "mismatched": 0,
            "errors": 0,
            "items": [],
            "report_items": [],
            "started_at": int(time.time()),
            "finished_at": 0,
            "message": "Подготовка проверки ID...",
        }

    threading.Thread(target=_verify_all_ids_worker, args=(str(session_dir),), daemon=True).start()
    return jsonify({"status": "started"})


@app.route("/api/verify-all-ids-status")
def api_verify_all_ids_status():
    with VERIFY_ALL_IDS_LOCK:
        st = dict(verify_all_ids_status)
        st["items"] = list(st.get("items", []) or [])
        return jsonify(st)


@app.route("/api/validate-clean-ids-start", methods=["POST"])
def api_validate_clean_ids_start():
    global validate_clean_ids_status
    session_dir = session.get("session_dir")
    if not session_dir:
        return jsonify({"status": "error", "message": "Нет активной сессии"}), 400
    cons_path = Path(session_dir) / "consolidated_price.xlsx"
    if not cons_path.exists():
        return jsonify({"status": "error", "message": "Нет данных"}), 400

    with VALIDATE_CLEAN_IDS_LOCK:
        if validate_clean_ids_status.get("running"):
            return jsonify({"status": "already_running"})
        validate_clean_ids_status = {
            "running": True,
            "total": 0, "done": 0,
            "confirmed": 0, "cleared": 0, "skipped_api": 0, "queued": 0, "errors": 0,
            "mode": "api",
            "mode_label": "Onliner API",
            "skipped_label": "Пропуск = API не ответил, ID не меняли.",
            "started_at": int(time.time()),
            "finished_at": 0,
            "message": "Подготовка валидации...",
        }

    threading.Thread(target=_validate_clean_ids_worker, args=(str(session_dir),), daemon=True).start()
    return jsonify({"status": "started"})


@app.route("/api/validate-clean-ids-db-start", methods=["POST"])
def api_validate_clean_ids_db_start():
    global validate_clean_ids_status
    session_dir = session.get("session_dir")
    if not session_dir:
        return jsonify({"status": "error", "message": "Нет активной сессии"}), 400
    cons_path = Path(session_dir) / "consolidated_price.xlsx"
    if not cons_path.exists():
        return jsonify({"status": "error", "message": "Нет данных"}), 400

    with VALIDATE_CLEAN_IDS_LOCK:
        if validate_clean_ids_status.get("running"):
            return jsonify({"status": "already_running"})
        validate_clean_ids_status = {
            "running": True,
            "total": 0, "done": 0,
            "confirmed": 0, "cleared": 0, "skipped_api": 0, "queued": 0, "errors": 0,
            "mode": "db",
            "mode_label": "Локальная БД 150k",
            "skipped_label": "Пропуск = ID или имя не найдены в локальной БД, поэтому ID оставили без изменений.",
            "started_at": int(time.time()),
            "finished_at": 0,
            "message": "Подготовка локальной сверки...",
        }

    threading.Thread(target=_validate_clean_ids_db_worker, args=(str(session_dir),), daemon=True).start()
    return jsonify({"status": "started"})


@app.route("/api/validate-clean-ids-status")
def api_validate_clean_ids_status():
    with VALIDATE_CLEAN_IDS_LOCK:
        return jsonify(dict(validate_clean_ids_status))


@app.route("/api/cpu-review-queue-start", methods=["POST"])
def api_cpu_review_queue_start():
    global autofill_iven_status
    session_dir = session.get("session_dir")
    if not session_dir:
        return jsonify({"status": "error", "message": "Нет активной сессии"}), 400
    cons_path = Path(session_dir) / "consolidated_price.xlsx"
    if not cons_path.exists():
        return jsonify({"status": "error", "message": "Нет данных"}), 400

    df = read_consolidated_df(session_dir)
    df = ensure_category_column(df)
    if df.empty:
        return jsonify({"status": "error", "message": "Прайс пуст"}), 400

    queue = load_review_queue()
    scanned = 0
    queued = 0
    no_model = 0
    no_candidates = 0
    skipped_with_id = 0
    skipped_non_ntech = 0
    ntech_supplier_names = {"N-TECH", "NTECH"}
    now_ts = int(time.time())
    cpu_report_items = []

    for row_idx, row in df.iterrows():
        supplier = str(row.get("Поставщик", "") or "").strip().upper()
        if supplier not in ntech_supplier_names:
            skipped_non_ntech += 1
            continue
        category = normalize_catalog_category_name(str(row_category(row) or "").strip())
        looks_like_cpu = _looks_like_cpu_name(str(row.get("Название", "") or "").strip())
        if not (category == "Процессор" or looks_like_cpu):
            continue
        oid = normalize_onliner_id(row.get("OnlinerID", ""))
        if oid:
            skipped_with_id += 1
            continue
        name = str(row.get("Название", "") or "").strip()
        if not name:
            continue
        scanned += 1
        local_brand, local_model = _cpu_brand_model_key(name)
        if not local_brand or not local_model:
            no_model += 1
            cpu_report_items.append({
                "name": name,
                "row_idx": int(row_idx),
                "category": "Процессор",
                "supplier": str(row.get("Поставщик", "") or "").strip(),
                "cpu_brand": local_brand.upper() if local_brand else "",
                "cpu_model": local_model.upper() if local_model else "",
                "cpu_issue": "no_model",
                "cpu_issue_label": "Не удалось выделить модель CPU",
                "candidates": [],
            })
            continue
        candidates = _find_cpu_review_candidates(name, top_n=5)
        name_key = _normalize_name_key(name)
        if not name_key:
            continue
        if not candidates:
            no_candidates += 1
            cpu_report_items.append({
                "name": name,
                "row_idx": int(row_idx),
                "category": "Процессор",
                "supplier": str(row.get("Поставщик", "") or "").strip(),
                "cpu_brand": local_brand.upper(),
                "cpu_model": local_model.upper(),
                "cpu_issue": "no_candidates",
                "cpu_issue_label": "Модель найдена, но кандидатов в БД нет",
                "candidates": [],
            })
            continue
        queue[name_key] = {
            "name": name,
            "category": "Процессор",
            "supplier": str(row.get("Поставщик", "") or "").strip(),
            "cpu_brand": local_brand.upper(),
            "cpu_model": local_model.upper(),
            "cleared_id": "",
            "cleared_score": 1.0,
            "onliner_name": f"{local_brand.upper()} {local_model}",
            "candidates": list(candidates),
            "added_at": now_ts,
            "reason": "cpu_brand_model_manual",
            "reason_label": "Процессор: совпадение только по производителю и модели. Подтверждение только вручную.",
        }
        cpu_report_items.append({
            "name": name,
            "row_idx": int(row_idx),
            "category": "Процессор",
            "supplier": str(row.get("Поставщик", "") or "").strip(),
            "cpu_brand": local_brand.upper(),
            "cpu_model": local_model.upper(),
            "cpu_issue": "queued",
            "cpu_issue_label": "Есть кандидаты, отправлено в ручную очередь",
            "best_source": str((candidates[0] or {}).get("source", "") or "").strip() if candidates else "",
            "candidates": list(candidates),
        })
        queued += 1

    save_review_queue(queue)
    report_payload = {
        "matches": [],
        "no_match": cpu_report_items,
        "report_mode": "cpu",
        "report_title": "Отчёт CPU N-Tech",
        "report_subtitle": (
            f"Обработано CPU: {scanned}. "
            f"В очереди: {queued}, без модели: {no_model}, без кандидатов: {no_candidates}."
        ),
    }
    if scanned <= 0:
        payload = {
            "status": "ok",
            "scanned": 0,
            "queued": 0,
            "no_model": 0,
            "no_candidates": 0,
            "skipped_with_id": int(skipped_with_id),
            "skipped_non_ntech": int(skipped_non_ntech),
            "message": "В текущем прайсе N-Tech нет товаров категории «Процессор» без ID. Сейчас CPU встречаются только внутри ПЭВМ/системных блоков.",
            **report_payload,
        }
        with AUTOFILL_IVEN_LOCK:
            autofill_iven_status.update({
                "running": False,
                "total": 0,
                "done": 0,
                "applied": 0,
                "skipped": 0,
                "percent": 100,
                "started_at": now_ts,
                "finished_at": int(time.time()),
                "message": payload["message"],
                "matches": [],
                "no_match": [],
                "report_mode": "cpu",
                "report_title": "Отчёт CPU N-Tech",
                "report_subtitle": "В текущем прайсе CPU без ID не найдено.",
            })
        return jsonify(payload)
    payload = {
        "status": "ok",
        "scanned": int(scanned),
        "queued": int(queued),
        "no_model": int(no_model),
        "no_candidates": int(no_candidates),
        "skipped_with_id": int(skipped_with_id),
        "skipped_non_ntech": int(skipped_non_ntech),
        "message": (
            f"Процессоры N-Tech: в ручную очередь добавлено {queued}."
            + (f" Без модели: {no_model}." if no_model else "")
            + (f" Без кандидатов: {no_candidates}." if no_candidates else "")
        ),
        **report_payload,
    }
    with AUTOFILL_IVEN_LOCK:
        autofill_iven_status.update({
            "running": False,
            "total": int(scanned),
            "done": int(scanned),
            "applied": 0,
            "skipped": int(no_model + no_candidates),
            "percent": 100,
            "started_at": now_ts,
            "finished_at": int(time.time()),
            "message": payload["message"],
            "matches": [],
            "no_match": cpu_report_items,
            "report_mode": "cpu",
            "report_title": "Отчёт CPU N-Tech",
            "report_subtitle": report_payload["report_subtitle"],
        })
    return jsonify(payload)


@app.route("/api/motherboard-review-queue-start", methods=["POST"])
def api_motherboard_review_queue_start():
    global autofill_iven_status
    session_dir = session.get("session_dir")
    if not session_dir:
        return jsonify({"status": "error", "message": "Нет активной сессии"}), 400
    cons_path = Path(session_dir) / "consolidated_price.xlsx"
    if not cons_path.exists():
        return jsonify({"status": "error", "message": "Нет данных"}), 400

    df = read_consolidated_df(session_dir)
    df = ensure_category_column(df)
    if df.empty:
        return jsonify({"status": "error", "message": "Прайс пуст"}), 400

    queue = load_review_queue()
    scanned = 0
    queued = 0
    no_model = 0
    no_candidates = 0
    skipped_with_id = 0
    skipped_non_ntech = 0
    ntech_supplier_names = {"N-TECH", "NTECH"}
    now_ts = int(time.time())
    board_report_items = []

    for row_idx, row in df.iterrows():
        supplier = str(row.get("Поставщик", "") or "").strip().upper()
        if supplier not in ntech_supplier_names:
            skipped_non_ntech += 1
            continue
        name = str(row.get("Название", "") or "").strip()
        if not name or not re.match(r"^\s*MB\s+", name, flags=re.IGNORECASE):
            continue
        oid = normalize_onliner_id(row.get("OnlinerID", ""))
        if oid:
            skipped_with_id += 1
            continue

        scanned += 1
        board = _board_brand_model_key(name)
        local_brand = board.get("brand", "")
        local_model = board.get("model", "")
        if not local_brand or not local_model:
            no_model += 1
            board_report_items.append({
                "name": name,
                "row_idx": int(row_idx),
                "category": "Материнская плата",
                "supplier": str(row.get("Поставщик", "") or "").strip(),
                "board_brand": local_brand.upper() if local_brand else "",
                "board_model": local_model.upper() if local_model else "",
                "board_issue": "no_model",
                "board_issue_label": "Не удалось выделить модель платы",
                "candidates": [],
            })
            continue

        candidates = _find_board_review_candidates(name, top_n=5)
        name_key = _normalize_name_key(name)
        if not name_key:
            continue
        if not candidates:
            no_candidates += 1
            board_report_items.append({
                "name": name,
                "row_idx": int(row_idx),
                "category": "Материнская плата",
                "supplier": str(row.get("Поставщик", "") or "").strip(),
                "board_brand": local_brand.upper(),
                "board_model": local_model.upper(),
                "board_issue": "no_candidates",
                "board_issue_label": "Модель найдена, но кандидатов в БД нет",
                "candidates": [],
            })
            continue

        queue[name_key] = {
            "name": name,
            "category": "Материнская плата",
            "supplier": str(row.get("Поставщик", "") or "").strip(),
            "board_brand": local_brand.upper(),
            "board_model": local_model.upper(),
            "cleared_id": "",
            "cleared_score": 1.0,
            "onliner_name": f"{local_brand.upper()} {local_model}",
            "candidates": list(candidates),
            "added_at": now_ts,
            "reason": "board_brand_model_manual",
            "reason_label": "Материнская плата: совпадение по бренду и модели. Подтверждение только вручную.",
        }
        board_report_items.append({
            "name": name,
            "row_idx": int(row_idx),
            "category": "Материнская плата",
            "supplier": str(row.get("Поставщик", "") or "").strip(),
            "board_brand": local_brand.upper(),
            "board_model": local_model.upper(),
            "board_issue": "queued",
            "board_issue_label": "Есть кандидаты, отправлено в ручную очередь",
            "best_source": str((candidates[0] or {}).get("source", "") or "").strip() if candidates else "",
            "candidates": list(candidates),
        })
        queued += 1

    save_review_queue(queue)
    report_payload = {
        "matches": [],
        "no_match": board_report_items,
        "report_mode": "board",
        "report_title": "Отчёт материнских плат N-Tech",
        "report_subtitle": (
            f"Обработано плат: {scanned}. "
            f"В очереди: {queued}, без модели: {no_model}, без кандидатов: {no_candidates}."
        ),
    }
    if scanned <= 0:
        payload = {
            "status": "ok",
            "scanned": 0,
            "queued": 0,
            "no_model": 0,
            "no_candidates": 0,
            "skipped_with_id": int(skipped_with_id),
            "skipped_non_ntech": int(skipped_non_ntech),
            "message": "В текущем прайсе N-Tech нет материнских плат формата MB без ID.",
            **report_payload,
        }
        with AUTOFILL_IVEN_LOCK:
            autofill_iven_status.update({
                "running": False,
                "total": 0,
                "done": 0,
                "applied": 0,
                "skipped": 0,
                "percent": 100,
                "started_at": now_ts,
                "finished_at": int(time.time()),
                "message": payload["message"],
                "matches": [],
                "no_match": [],
                "report_mode": "board",
                "report_title": "Отчёт материнских плат N-Tech",
                "report_subtitle": "В текущем прайсе материнские платы без ID не найдены.",
            })
        return jsonify(payload)

    payload = {
        "status": "ok",
        "scanned": int(scanned),
        "queued": int(queued),
        "no_model": int(no_model),
        "no_candidates": int(no_candidates),
        "skipped_with_id": int(skipped_with_id),
        "skipped_non_ntech": int(skipped_non_ntech),
        "message": (
            f"Материнки N-Tech: в ручную очередь добавлено {queued}."
            + (f" Без модели: {no_model}." if no_model else "")
            + (f" Без кандидатов: {no_candidates}." if no_candidates else "")
        ),
        **report_payload,
    }
    with AUTOFILL_IVEN_LOCK:
        autofill_iven_status.update({
            "running": False,
            "total": int(scanned),
            "done": int(scanned),
            "applied": 0,
            "skipped": int(no_model + no_candidates),
            "percent": 100,
            "started_at": now_ts,
            "finished_at": int(time.time()),
            "message": payload["message"],
            "matches": [],
            "no_match": board_report_items,
            "report_mode": "board",
            "report_title": "Отчёт материнских плат N-Tech",
            "report_subtitle": report_payload["report_subtitle"],
        })
    return jsonify(payload)


@app.route("/api/monitor-review-queue-start", methods=["POST"])
def api_monitor_review_queue_start():
    global autofill_iven_status
    session_dir = session.get("session_dir")
    if not session_dir:
        return jsonify({"status": "error", "message": "Нет активной сессии"}), 400
    cons_path = Path(session_dir) / "consolidated_price.xlsx"
    if not cons_path.exists():
        return jsonify({"status": "error", "message": "Нет данных"}), 400

    df = read_consolidated_df(session_dir)
    df = ensure_category_column(df)
    if df.empty:
        return jsonify({"status": "error", "message": "Прайс пуст"}), 400

    queue = load_review_queue()
    scanned = 0
    queued = 0
    no_model = 0
    no_candidates = 0
    skipped_with_id = 0
    skipped_non_ntech = 0
    ntech_supplier_names = {"N-TECH", "NTECH"}
    now_ts = int(time.time())
    monitor_report_items = []

    for row_idx, row in df.iterrows():
        supplier = str(row.get("Поставщик", "") or "").strip().upper()
        if supplier not in ntech_supplier_names:
            skipped_non_ntech += 1
            continue
        name = str(row.get("Название", "") or "").strip()
        category = normalize_catalog_category_name(str(row_category(row) or "").strip())
        looks_like_monitor = bool(re.match(r'^\s*\d{2}(?:\.\d)?\s*"', name))
        if not name or not (category == "Монитор" or looks_like_monitor):
            continue
        oid = normalize_onliner_id(row.get("OnlinerID", ""))
        if oid:
            skipped_with_id += 1
            continue

        scanned += 1
        monitor = _monitor_brand_model_key(name)
        local_brand = monitor.get("brand", "")
        local_model = monitor.get("model", "")
        if not local_brand or not local_model:
            no_model += 1
            monitor_report_items.append({
                "name": name,
                "row_idx": int(row_idx),
                "category": "Монитор",
                "supplier": str(row.get("Поставщик", "") or "").strip(),
                "monitor_brand": local_brand.upper() if local_brand else "",
                "monitor_model": monitor.get("model_text", ""),
                "monitor_issue": "no_model",
                "monitor_issue_label": "Не удалось выделить модель монитора",
                "candidates": [],
            })
            continue

        candidates = _find_monitor_review_candidates(name, top_n=5)
        name_key = _normalize_name_key(name)
        if not name_key:
            continue
        if not candidates:
            no_candidates += 1
            monitor_report_items.append({
                "name": name,
                "row_idx": int(row_idx),
                "category": "Монитор",
                "supplier": str(row.get("Поставщик", "") or "").strip(),
                "monitor_brand": local_brand.upper(),
                "monitor_model": monitor.get("model_text", ""),
                "monitor_issue": "no_candidates",
                "monitor_issue_label": "Модель найдена, но кандидатов в БД нет",
                "candidates": [],
            })
            continue

        queue[name_key] = {
            "name": name,
            "category": "Монитор",
            "supplier": str(row.get("Поставщик", "") or "").strip(),
            "monitor_brand": local_brand.upper(),
            "monitor_model": monitor.get("model_text", ""),
            "cleared_id": "",
            "cleared_score": 1.0,
            "onliner_name": f"{local_brand.upper()} {monitor.get('model_text', '')}",
            "candidates": list(candidates),
            "added_at": now_ts,
            "reason": "monitor_brand_model_manual",
            "reason_label": "Монитор: совпадение по бренду и модели. Подтверждение только вручную.",
        }
        monitor_report_items.append({
            "name": name,
            "row_idx": int(row_idx),
            "category": "Монитор",
            "supplier": str(row.get("Поставщик", "") or "").strip(),
            "monitor_brand": local_brand.upper(),
            "monitor_model": monitor.get("model_text", ""),
            "monitor_issue": "queued",
            "monitor_issue_label": "Есть кандидаты, отправлено в ручную очередь",
            "best_source": str((candidates[0] or {}).get("source", "") or "").strip() if candidates else "",
            "candidates": list(candidates),
        })
        queued += 1

    save_review_queue(queue)
    report_payload = {
        "matches": [],
        "no_match": monitor_report_items,
        "report_mode": "monitor",
        "report_title": "Отчёт мониторов N-Tech",
        "report_subtitle": (
            f"Обработано мониторов: {scanned}. "
            f"В очереди: {queued}, без модели: {no_model}, без кандидатов: {no_candidates}."
        ),
    }
    if scanned <= 0:
        payload = {
            "status": "ok",
            "scanned": 0,
            "queued": 0,
            "no_model": 0,
            "no_candidates": 0,
            "skipped_with_id": int(skipped_with_id),
            "skipped_non_ntech": int(skipped_non_ntech),
            "message": "В текущем прайсе N-Tech нет мониторов без ID.",
            **report_payload,
        }
        with AUTOFILL_IVEN_LOCK:
            autofill_iven_status.update({
                "running": False,
                "total": 0,
                "done": 0,
                "applied": 0,
                "skipped": 0,
                "percent": 100,
                "started_at": now_ts,
                "finished_at": int(time.time()),
                "message": payload["message"],
                "matches": [],
                "no_match": [],
                "report_mode": "monitor",
                "report_title": "Отчёт мониторов N-Tech",
                "report_subtitle": "В текущем прайсе мониторы без ID не найдены.",
            })
        return jsonify(payload)

    payload = {
        "status": "ok",
        "scanned": int(scanned),
        "queued": int(queued),
        "no_model": int(no_model),
        "no_candidates": int(no_candidates),
        "skipped_with_id": int(skipped_with_id),
        "skipped_non_ntech": int(skipped_non_ntech),
        "message": (
            f"Мониторы N-Tech: в ручную очередь добавлено {queued}."
            + (f" Без модели: {no_model}." if no_model else "")
            + (f" Без кандидатов: {no_candidates}." if no_candidates else "")
        ),
        **report_payload,
    }
    with AUTOFILL_IVEN_LOCK:
        autofill_iven_status.update({
            "running": False,
            "total": int(scanned),
            "done": int(scanned),
            "applied": 0,
            "skipped": int(no_model + no_candidates),
            "percent": 100,
            "started_at": now_ts,
            "finished_at": int(time.time()),
            "message": payload["message"],
            "matches": [],
            "no_match": monitor_report_items,
            "report_mode": "monitor",
            "report_title": "Отчёт мониторов N-Tech",
            "report_subtitle": report_payload["report_subtitle"],
        })
    return jsonify(payload)


@app.route("/api/gpu-review-queue-start", methods=["POST"])
def api_gpu_review_queue_start():
    global autofill_iven_status
    session_dir = session.get("session_dir")
    if not session_dir:
        return jsonify({"status": "error", "message": "Нет активной сессии"}), 400
    cons_path = Path(session_dir) / "consolidated_price.xlsx"
    if not cons_path.exists():
        return jsonify({"status": "error", "message": "Нет данных"}), 400

    df = read_consolidated_df(session_dir)
    df = ensure_category_column(df)
    if df.empty:
        return jsonify({"status": "error", "message": "Прайс пуст"}), 400

    queue = load_review_queue()
    scanned = 0
    queued = 0
    no_model = 0
    no_candidates = 0
    skipped_with_id = 0
    skipped_non_ntech = 0
    ntech_supplier_names = {"N-TECH", "NTECH"}
    now_ts = int(time.time())
    gpu_report_items = []

    for row_idx, row in df.iterrows():
        supplier = str(row.get("Поставщик", "") or "").strip().upper()
        if supplier not in ntech_supplier_names:
            skipped_non_ntech += 1
            continue
        name = str(row.get("Название", "") or "").strip()
        category = normalize_catalog_category_name(str(row_category(row) or "").strip())
        looks_like_gpu = bool(re.search(r"видеокарта|geforce|radeon|(?:^|[^a-z0-9])rtx\s*\d{4}|(?:^|[^a-z0-9])rx\s*\d{3,4}", name, flags=re.IGNORECASE))
        if not name or not (category == "Видеокарта" or looks_like_gpu):
            continue
        oid = normalize_onliner_id(row.get("OnlinerID", ""))
        if oid:
            skipped_with_id += 1
            continue

        scanned += 1
        gpu = _gpu_brand_model_key(name)
        local_vendor = gpu.get("vendor", "")
        local_model = gpu.get("gpu_model", "")
        if not local_vendor or not local_model:
            no_model += 1
            gpu_report_items.append({
                "name": name,
                "row_idx": int(row_idx),
                "category": "Видеокарта",
                "supplier": str(row.get("Поставщик", "") or "").strip(),
                "gpu_vendor": local_vendor.upper() if local_vendor else "",
                "gpu_model": local_model.upper() if local_model else "",
                "gpu_sku": str(gpu.get("sku", "") or "").upper(),
                "gpu_issue": "no_model",
                "gpu_issue_label": "Не удалось выделить модель видеокарты",
                "candidates": [],
            })
            continue

        candidates = _find_gpu_review_candidates(name, top_n=5)
        name_key = _normalize_name_key(name)
        if not name_key:
            continue
        if not candidates:
            no_candidates += 1
            gpu_report_items.append({
                "name": name,
                "row_idx": int(row_idx),
                "category": "Видеокарта",
                "supplier": str(row.get("Поставщик", "") or "").strip(),
                "gpu_vendor": local_vendor.upper(),
                "gpu_model": local_model.upper(),
                "gpu_sku": str(gpu.get("sku", "") or "").upper(),
                "gpu_issue": "no_candidates",
                "gpu_issue_label": "Модель найдена, но кандидатов в БД нет",
                "candidates": [],
            })
            continue

        queue[name_key] = {
            "name": name,
            "category": "Видеокарта",
            "supplier": str(row.get("Поставщик", "") or "").strip(),
            "gpu_vendor": local_vendor.upper(),
            "gpu_model": local_model.upper(),
            "gpu_sku": str(gpu.get("sku", "") or "").upper(),
            "cleared_id": "",
            "cleared_score": 1.0,
            "onliner_name": f"{local_vendor.upper()} {local_model.upper()}",
            "candidates": list(candidates),
            "added_at": now_ts,
            "reason": "gpu_brand_model_manual",
            "reason_label": "Видеокарта: совпадение по вендору, GPU и серии. Подтверждение только вручную.",
        }
        gpu_report_items.append({
            "name": name,
            "row_idx": int(row_idx),
            "category": "Видеокарта",
            "supplier": str(row.get("Поставщик", "") or "").strip(),
            "gpu_vendor": local_vendor.upper(),
            "gpu_model": local_model.upper(),
            "gpu_sku": str(gpu.get("sku", "") or "").upper(),
            "gpu_issue": "queued",
            "gpu_issue_label": "Есть кандидаты, отправлено в ручную очередь",
            "best_source": str((candidates[0] or {}).get("source", "") or "").strip() if candidates else "",
            "candidates": list(candidates),
        })
        queued += 1

    save_review_queue(queue)
    report_payload = {
        "matches": [],
        "no_match": gpu_report_items,
        "report_mode": "gpu",
        "report_title": "Отчёт видеокарт N-Tech",
        "report_subtitle": (
            f"Обработано видеокарт: {scanned}. "
            f"В очереди: {queued}, без модели: {no_model}, без кандидатов: {no_candidates}."
        ),
    }
    if scanned <= 0:
        payload = {
            "status": "ok",
            "scanned": 0,
            "queued": 0,
            "no_model": 0,
            "no_candidates": 0,
            "skipped_with_id": int(skipped_with_id),
            "skipped_non_ntech": int(skipped_non_ntech),
            "message": "В текущем прайсе N-Tech нет видеокарт без ID.",
            **report_payload,
        }
        with AUTOFILL_IVEN_LOCK:
            autofill_iven_status.update({
                "running": False,
                "total": 0,
                "done": 0,
                "applied": 0,
                "skipped": 0,
                "percent": 100,
                "started_at": now_ts,
                "finished_at": int(time.time()),
                "message": payload["message"],
                "matches": [],
                "no_match": [],
                "report_mode": "gpu",
                "report_title": "Отчёт видеокарт N-Tech",
                "report_subtitle": "В текущем прайсе видеокарты без ID не найдены.",
            })
        return jsonify(payload)

    payload = {
        "status": "ok",
        "scanned": int(scanned),
        "queued": int(queued),
        "no_model": int(no_model),
        "no_candidates": int(no_candidates),
        "skipped_with_id": int(skipped_with_id),
        "skipped_non_ntech": int(skipped_non_ntech),
        "message": (
            f"Видеокарты N-Tech: в ручную очередь добавлено {queued}."
            + (f" Без модели: {no_model}." if no_model else "")
            + (f" Без кандидатов: {no_candidates}." if no_candidates else "")
        ),
        **report_payload,
    }
    with AUTOFILL_IVEN_LOCK:
        autofill_iven_status.update({
            "running": False,
            "total": int(scanned),
            "done": int(scanned),
            "applied": 0,
            "skipped": int(no_model + no_candidates),
            "percent": 100,
            "started_at": now_ts,
            "finished_at": int(time.time()),
            "message": payload["message"],
            "matches": [],
            "no_match": gpu_report_items,
            "report_mode": "gpu",
            "report_title": "Отчёт видеокарт N-Tech",
            "report_subtitle": report_payload["report_subtitle"],
        })
    return jsonify(payload)


@app.route("/api/ram-review-queue-start", methods=["POST"])
def api_ram_review_queue_start():
    global autofill_iven_status
    session_dir = session.get("session_dir")
    if not session_dir:
        return jsonify({"status": "error", "message": "Нет активной сессии"}), 400
    cons_path = Path(session_dir) / "consolidated_price.xlsx"
    if not cons_path.exists():
        return jsonify({"status": "error", "message": "Нет данных"}), 400

    df = read_consolidated_df(session_dir)
    df = ensure_category_column(df)
    if df.empty:
        return jsonify({"status": "error", "message": "Прайс пуст"}), 400

    queue = load_review_queue()
    scanned = 0
    queued = 0
    no_model = 0
    no_candidates = 0
    skipped_with_id = 0
    skipped_non_ntech = 0
    ntech_supplier_names = {"N-TECH", "NTECH"}
    now_ts = int(time.time())
    ram_report_items = []

    for row_idx, row in df.iterrows():
        supplier = str(row.get("Поставщик", "") or "").strip().upper()
        if supplier not in ntech_supplier_names:
            skipped_non_ntech += 1
            continue
        name = str(row.get("Название", "") or "").strip()
        category = normalize_catalog_category_name(str(row_category(row) or "").strip())
        looks_like_ram = bool(
            re.match(r"^\s*ddr[345]\b", name, flags=re.IGNORECASE)
            and not re.search(r"\bпэвм\b|\bкомпьютер\b|\bsoc[-\s]|\bматерин", name, flags=re.IGNORECASE)
        )
        if not name or not (category == "Оперативная память" or looks_like_ram):
            continue
        oid = normalize_onliner_id(row.get("OnlinerID", ""))
        if oid:
            skipped_with_id += 1
            continue

        scanned += 1
        ram = _ram_brand_model_key(name)
        local_brand = ram.get("brand", "")
        local_sku = ram.get("sku", "")
        if not local_brand or (not local_sku and not ram.get("capacity_gb")):
            no_model += 1
            ram_report_items.append({
                "name": name,
                "row_idx": int(row_idx),
                "category": "Оперативная память",
                "supplier": str(row.get("Поставщик", "") or "").strip(),
                "ram_brand": local_brand.upper() if local_brand else "",
                "ram_sku": local_sku.upper() if local_sku else "",
                "ram_issue": "no_model",
                "ram_issue_label": "Не удалось выделить модель памяти",
                "candidates": [],
            })
            continue

        candidates = _find_ram_review_candidates(name, top_n=5)
        name_key = _normalize_name_key(name)
        if not name_key:
            continue
        if not candidates:
            no_candidates += 1
            ram_report_items.append({
                "name": name,
                "row_idx": int(row_idx),
                "category": "Оперативная память",
                "supplier": str(row.get("Поставщик", "") or "").strip(),
                "ram_brand": local_brand.upper(),
                "ram_sku": local_sku.upper(),
                "ram_issue": "no_candidates",
                "ram_issue_label": "Модель найдена, но кандидатов в БД нет",
                "candidates": [],
            })
            continue

        queue[name_key] = {
            "name": name,
            "category": "Оперативная память",
            "supplier": str(row.get("Поставщик", "") or "").strip(),
            "ram_brand": local_brand.upper(),
            "ram_sku": local_sku.upper(),
            "cleared_id": "",
            "cleared_score": 1.0,
            "onliner_name": f"{local_brand.upper()} {local_sku.upper()}",
            "candidates": list(candidates),
            "added_at": now_ts,
            "reason": "ram_brand_model_manual",
            "reason_label": "Оперативная память: совпадение по бренду и коду модуля. Подтверждение только вручную.",
        }
        ram_report_items.append({
            "name": name,
            "row_idx": int(row_idx),
            "category": "Оперативная память",
            "supplier": str(row.get("Поставщик", "") or "").strip(),
            "ram_brand": local_brand.upper(),
            "ram_sku": local_sku.upper(),
            "ram_issue": "queued",
            "ram_issue_label": "Есть кандидаты, отправлено в ручную очередь",
            "best_source": str((candidates[0] or {}).get("source", "") or "").strip() if candidates else "",
            "candidates": list(candidates),
        })
        queued += 1

    save_review_queue(queue)
    report_payload = {
        "matches": [],
        "no_match": ram_report_items,
        "report_mode": "ram",
        "report_title": "Отчёт оперативной памяти N-Tech",
        "report_subtitle": (
            f"Обработано памяти: {scanned}. "
            f"В очереди: {queued}, без модели: {no_model}, без кандидатов: {no_candidates}."
        ),
    }
    if scanned <= 0:
        payload = {
            "status": "ok",
            "scanned": 0,
            "queued": 0,
            "no_model": 0,
            "no_candidates": 0,
            "skipped_with_id": int(skipped_with_id),
            "skipped_non_ntech": int(skipped_non_ntech),
            "message": "В текущем прайсе N-Tech нет оперативной памяти без ID.",
            **report_payload,
        }
        with AUTOFILL_IVEN_LOCK:
            autofill_iven_status.update({
                "running": False,
                "total": 0,
                "done": 0,
                "applied": 0,
                "skipped": 0,
                "percent": 100,
                "started_at": now_ts,
                "finished_at": int(time.time()),
                "message": payload["message"],
                "matches": [],
                "no_match": [],
                "report_mode": "ram",
                "report_title": "Отчёт оперативной памяти N-Tech",
                "report_subtitle": "В текущем прайсе оперативная память без ID не найдена.",
            })
        return jsonify(payload)

    payload = {
        "status": "ok",
        "scanned": int(scanned),
        "queued": int(queued),
        "no_model": int(no_model),
        "no_candidates": int(no_candidates),
        "skipped_with_id": int(skipped_with_id),
        "skipped_non_ntech": int(skipped_non_ntech),
        "message": (
            f"Оперативка N-Tech: в ручную очередь добавлено {queued}."
            + (f" Без модели: {no_model}." if no_model else "")
            + (f" Без кандидатов: {no_candidates}." if no_candidates else "")
        ),
        **report_payload,
    }
    with AUTOFILL_IVEN_LOCK:
        autofill_iven_status.update({
            "running": False,
            "total": int(scanned),
            "done": int(scanned),
            "applied": 0,
            "skipped": int(no_model + no_candidates),
            "percent": 100,
            "started_at": now_ts,
            "finished_at": int(time.time()),
            "message": payload["message"],
            "matches": [],
            "no_match": ram_report_items,
            "report_mode": "ram",
            "report_title": "Отчёт оперативной памяти N-Tech",
            "report_subtitle": report_payload["report_subtitle"],
        })
    return jsonify(payload)


@app.route("/api/ssd-review-queue-start", methods=["POST"])
def api_ssd_review_queue_start():
    global autofill_iven_status
    session_dir = session.get("session_dir")
    if not session_dir:
        return jsonify({"status": "error", "message": "Нет активной сессии"}), 400
    cons_path = Path(session_dir) / "consolidated_price.xlsx"
    if not cons_path.exists():
        return jsonify({"status": "error", "message": "Нет данных"}), 400

    df = read_consolidated_df(session_dir)
    df = ensure_category_column(df)
    if df.empty:
        return jsonify({"status": "error", "message": "Прайс пуст"}), 400

    queue = load_review_queue()
    scanned = 0
    queued = 0
    no_model = 0
    no_candidates = 0
    skipped_with_id = 0
    skipped_non_ntech = 0
    ntech_supplier_names = {"N-TECH", "NTECH"}
    now_ts = int(time.time())
    ssd_report_items = []

    for row_idx, row in df.iterrows():
        supplier = str(row.get("Поставщик", "") or "").strip().upper()
        if supplier not in ntech_supplier_names:
            skipped_non_ntech += 1
            continue
        name = str(row.get("Название", "") or "").strip()
        if not name:
            continue
        category = normalize_catalog_category_name(str(row_category(row) or "").strip())
        looks_like_ssd = bool(
            re.search(r"(?:^|[^a-z0-9])ssd(?=$|[^a-z0-9])|nvme|m\.?2|твердотельн", name, flags=re.IGNORECASE)
            and not re.search(r"\bпэвм\b|\bкомпьютер\b|\bноутбук\b", name, flags=re.IGNORECASE)
        )
        if not (category == "SSD" or looks_like_ssd):
            continue
        oid = normalize_onliner_id(row.get("OnlinerID", ""))
        if oid:
            skipped_with_id += 1
            continue

        scanned += 1
        ssd = _ssd_brand_model_key(name)
        local_brand = ssd.get("brand", "")
        local_code = str(ssd.get("code", "") or "").strip()
        local_model = str(ssd.get("model", "") or "").strip()
        if not local_code and not local_brand:
            no_model += 1
            ssd_report_items.append({
                "name": name,
                "row_idx": int(row_idx),
                "category": "SSD",
                "supplier": str(row.get("Поставщик", "") or "").strip(),
                "ssd_brand": local_brand.upper() if local_brand else "",
                "ssd_model": local_model.upper() if local_model else "",
                "ssd_code": "",
                "ssd_issue": "no_model",
                "ssd_issue_label": "Не удалось выделить бренд/модель SSD",
                "candidates": [],
            })
            continue

        candidates = _find_ssd_review_candidates(name, top_n=5)
        name_key = _normalize_name_key(name)
        if not name_key:
            continue
        if not candidates:
            no_candidates += 1
            ssd_report_items.append({
                "name": name,
                "row_idx": int(row_idx),
                "category": "SSD",
                "supplier": str(row.get("Поставщик", "") or "").strip(),
                "ssd_brand": local_brand.upper() if local_brand else "",
                "ssd_model": local_model.upper() if local_model else "",
                "ssd_code": local_code.upper(),
                "ssd_issue": "no_candidates",
                "ssd_issue_label": "Кандидаты по коду/модели в БД не найдены",
                "candidates": [],
            })
            continue

        queue[name_key] = {
            "name": name,
            "category": "SSD",
            "supplier": str(row.get("Поставщик", "") or "").strip(),
            "ssd_brand": local_brand.upper() if local_brand else "",
            "ssd_model": local_model.upper() if local_model else "",
            "ssd_code": local_code.upper(),
            "cleared_id": "",
            "cleared_score": 1.0,
            "onliner_name": f"{local_brand.upper()} {local_code.upper()}".strip(),
            "candidates": list(candidates),
            "added_at": now_ts,
            "reason": "ssd_bracket_code_manual",
            "reason_label": "SSD: приоритет точному коду в скобках, fallback по бренду+модели/объёму. Подтверждение только вручную.",
        }
        ssd_report_items.append({
            "name": name,
            "row_idx": int(row_idx),
            "category": "SSD",
            "supplier": str(row.get("Поставщик", "") or "").strip(),
            "ssd_brand": local_brand.upper() if local_brand else "",
            "ssd_model": local_model.upper() if local_model else "",
            "ssd_code": local_code.upper(),
            "ssd_issue": "queued",
            "ssd_issue_label": "Есть кандидаты, отправлено в ручную очередь",
            "best_source": str((candidates[0] or {}).get("source", "") or "").strip() if candidates else "",
            "candidates": list(candidates),
        })
        queued += 1

    save_review_queue(queue)
    report_payload = {
        "matches": [],
        "no_match": ssd_report_items,
        "report_mode": "ssd",
        "report_title": "Отчёт SSD N-Tech",
        "report_subtitle": (
            f"Обработано SSD: {scanned}. "
            f"В очереди: {queued}, без модели: {no_model}, без кандидатов: {no_candidates}."
        ),
    }
    if scanned <= 0:
        payload = {
            "status": "ok",
            "scanned": 0,
            "queued": 0,
            "no_model": 0,
            "no_candidates": 0,
            "skipped_with_id": int(skipped_with_id),
            "skipped_non_ntech": int(skipped_non_ntech),
            "message": "В текущем прайсе N-Tech нет SSD без ID.",
            **report_payload,
        }
        with AUTOFILL_IVEN_LOCK:
            autofill_iven_status.update({
                "running": False,
                "total": 0,
                "done": 0,
                "applied": 0,
                "skipped": 0,
                "percent": 100,
                "started_at": now_ts,
                "finished_at": int(time.time()),
                "message": payload["message"],
                "matches": [],
                "no_match": [],
                "report_mode": "ssd",
                "report_title": "Отчёт SSD N-Tech",
                "report_subtitle": "В текущем прайсе SSD без ID не найдены.",
            })
        return jsonify(payload)

    payload = {
        "status": "ok",
        "scanned": int(scanned),
        "queued": int(queued),
        "no_model": int(no_model),
        "no_candidates": int(no_candidates),
        "skipped_with_id": int(skipped_with_id),
        "skipped_non_ntech": int(skipped_non_ntech),
        "message": (
            f"SSD N-Tech: в ручную очередь добавлено {queued}."
            + (f" Без модели: {no_model}." if no_model else "")
            + (f" Без кандидатов: {no_candidates}." if no_candidates else "")
        ),
        **report_payload,
    }
    with AUTOFILL_IVEN_LOCK:
        autofill_iven_status.update({
            "running": False,
            "total": int(scanned),
            "done": int(scanned),
            "applied": 0,
            "skipped": int(no_model + no_candidates),
            "percent": 100,
            "started_at": now_ts,
            "finished_at": int(time.time()),
            "message": payload["message"],
            "matches": [],
            "no_match": ssd_report_items,
            "report_mode": "ssd",
            "report_title": "Отчёт SSD N-Tech",
            "report_subtitle": report_payload["report_subtitle"],
        })
    return jsonify(payload)


@app.route("/api/psu-review-queue-start", methods=["POST"])
def api_psu_review_queue_start():
    global autofill_iven_status
    session_dir = session.get("session_dir")
    if not session_dir:
        return jsonify({"status": "error", "message": "Нет активной сессии"}), 400
    cons_path = Path(session_dir) / "consolidated_price.xlsx"
    if not cons_path.exists():
        return jsonify({"status": "error", "message": "Нет данных"}), 400

    df = read_consolidated_df(session_dir)
    df = ensure_category_column(df)
    if df.empty:
        return jsonify({"status": "error", "message": "Прайс пуст"}), 400

    queue = load_review_queue()
    scanned = 0
    queued = 0
    no_model = 0
    no_candidates = 0
    skipped_with_id = 0
    skipped_non_ntech = 0
    ntech_supplier_names = {"N-TECH", "NTECH"}
    now_ts = int(time.time())
    psu_report_items = []

    for row_idx, row in df.iterrows():
        supplier = str(row.get("Поставщик", "") or "").strip().upper()
        if supplier not in ntech_supplier_names:
            skipped_non_ntech += 1
            continue
        name = str(row.get("Название", "") or "").strip()
        category = normalize_catalog_category_name(str(row_category(row) or "").strip())
        looks_like_psu = bool(
            re.search(r"^\s*бп\b|блок\s*питания|80\s*plus|(?:^|[^a-z0-9])psu(?=$|[^a-z0-9])", name, flags=re.IGNORECASE)
            and re.search(r"\b\d{3,4}\s*w\b", name, flags=re.IGNORECASE)
        )
        if not name or not (category == "Блок питания" or looks_like_psu):
            continue
        oid = normalize_onliner_id(row.get("OnlinerID", ""))
        if oid:
            skipped_with_id += 1
            continue

        scanned += 1
        psu = _psu_brand_model_key(name)
        local_brand = psu.get("brand", "")
        local_watt = psu.get("watt", "")
        local_code = str(psu.get("code", "") or "").upper()
        if not local_brand or not local_watt:
            no_model += 1
            psu_report_items.append({
                "name": name,
                "row_idx": int(row_idx),
                "category": "Блок питания",
                "supplier": str(row.get("Поставщик", "") or "").strip(),
                "psu_brand": local_brand.upper() if local_brand else "",
                "psu_model": (f"{local_watt}W" if local_watt else ""),
                "psu_code": local_code,
                "psu_issue": "no_model",
                "psu_issue_label": "Не удалось выделить бренд/мощность БП",
                "candidates": [],
            })
            continue

        candidates = _find_psu_review_candidates(name, top_n=5)
        name_key = _normalize_name_key(name)
        if not name_key:
            continue
        if not candidates:
            no_candidates += 1
            psu_report_items.append({
                "name": name,
                "row_idx": int(row_idx),
                "category": "Блок питания",
                "supplier": str(row.get("Поставщик", "") or "").strip(),
                "psu_brand": local_brand.upper(),
                "psu_model": f"{local_watt}W",
                "psu_code": local_code,
                "psu_issue": "no_candidates",
                "psu_issue_label": "Бренд/мощность найдены, но кандидатов в БД нет",
                "candidates": [],
            })
            continue

        queue[name_key] = {
            "name": name,
            "category": "Блок питания",
            "supplier": str(row.get("Поставщик", "") or "").strip(),
            "psu_brand": local_brand.upper(),
            "psu_model": f"{local_watt}W",
            "psu_code": local_code,
            "cleared_id": "",
            "cleared_score": 1.0,
            "onliner_name": f"{local_brand.upper()} {local_watt}W",
            "candidates": list(candidates),
            "added_at": now_ts,
            "reason": "psu_brand_power_manual",
            "reason_label": "Блок питания: строгий матч по бренду, мощности, 80 PLUS/модульности/коду. Подтверждение только вручную.",
        }
        psu_report_items.append({
            "name": name,
            "row_idx": int(row_idx),
            "category": "Блок питания",
            "supplier": str(row.get("Поставщик", "") or "").strip(),
            "psu_brand": local_brand.upper(),
            "psu_model": f"{local_watt}W",
            "psu_code": local_code,
            "psu_issue": "queued",
            "psu_issue_label": "Есть кандидаты, отправлено в ручную очередь",
            "best_source": str((candidates[0] or {}).get("source", "") or "").strip() if candidates else "",
            "candidates": list(candidates),
        })
        queued += 1

    save_review_queue(queue)
    report_payload = {
        "matches": [],
        "no_match": psu_report_items,
        "report_mode": "psu",
        "report_title": "Отчёт блоков питания N-Tech",
        "report_subtitle": (
            f"Обработано БП: {scanned}. "
            f"В очереди: {queued}, без модели: {no_model}, без кандидатов: {no_candidates}."
        ),
    }
    if scanned <= 0:
        payload = {
            "status": "ok",
            "scanned": 0,
            "queued": 0,
            "no_model": 0,
            "no_candidates": 0,
            "skipped_with_id": int(skipped_with_id),
            "skipped_non_ntech": int(skipped_non_ntech),
            "message": "В текущем прайсе N-Tech нет блоков питания без ID.",
            **report_payload,
        }
        with AUTOFILL_IVEN_LOCK:
            autofill_iven_status.update({
                "running": False,
                "total": 0,
                "done": 0,
                "applied": 0,
                "skipped": 0,
                "percent": 100,
                "started_at": now_ts,
                "finished_at": int(time.time()),
                "message": payload["message"],
                "matches": [],
                "no_match": [],
                "report_mode": "psu",
                "report_title": "Отчёт блоков питания N-Tech",
                "report_subtitle": "В текущем прайсе блоки питания без ID не найдены.",
            })
        return jsonify(payload)

    payload = {
        "status": "ok",
        "scanned": int(scanned),
        "queued": int(queued),
        "no_model": int(no_model),
        "no_candidates": int(no_candidates),
        "skipped_with_id": int(skipped_with_id),
        "skipped_non_ntech": int(skipped_non_ntech),
        "message": (
            f"Блоки питания N-Tech: в ручную очередь добавлено {queued}."
            + (f" Без модели: {no_model}." if no_model else "")
            + (f" Без кандидатов: {no_candidates}." if no_candidates else "")
        ),
        **report_payload,
    }
    with AUTOFILL_IVEN_LOCK:
        autofill_iven_status.update({
            "running": False,
            "total": int(scanned),
            "done": int(scanned),
            "applied": 0,
            "skipped": int(no_model + no_candidates),
            "percent": 100,
            "started_at": now_ts,
            "finished_at": int(time.time()),
            "message": payload["message"],
            "matches": [],
            "no_match": psu_report_items,
            "report_mode": "psu",
            "report_title": "Отчёт блоков питания N-Tech",
            "report_subtitle": report_payload["report_subtitle"],
        })
    return jsonify(payload)


@app.route("/api/case-review-queue-start", methods=["POST"])
def api_case_review_queue_start():
    global autofill_iven_status
    session_dir = session.get("session_dir")
    if not session_dir:
        return jsonify({"status": "error", "message": "Нет активной сессии"}), 400
    cons_path = Path(session_dir) / "consolidated_price.xlsx"
    if not cons_path.exists():
        return jsonify({"status": "error", "message": "Нет данных"}), 400

    df = read_consolidated_df(session_dir)
    df = ensure_category_column(df)
    if df.empty:
        return jsonify({"status": "error", "message": "Прайс пуст"}), 400

    queue = load_review_queue()
    scanned = 0
    queued = 0
    no_model = 0
    no_candidates = 0
    skipped_with_id = 0
    skipped_non_ntech = 0
    ntech_supplier_names = {"N-TECH", "NTECH"}
    now_ts = int(time.time())
    case_report_items = []

    for row_idx, row in df.iterrows():
        supplier = str(row.get("Поставщик", "") or "").strip().upper()
        if supplier not in ntech_supplier_names:
            skipped_non_ntech += 1
            continue
        name = str(row.get("Название", "") or "").strip()
        category = normalize_catalog_category_name(str(row_category(row) or "").strip())
        looks_like_case = _looks_like_case_name(name)
        if not name or not (category == "Корпус" or looks_like_case):
            continue
        oid = normalize_onliner_id(row.get("OnlinerID", ""))
        if oid:
            skipped_with_id += 1
            continue

        scanned += 1
        case_meta = _case_brand_model_key(name)
        local_brand = case_meta.get("brand", "")
        local_code = str(case_meta.get("code", "") or "").upper()
        local_series = str(case_meta.get("series", "") or "").upper()
        if not local_brand:
            no_model += 1
            case_report_items.append({
                "name": name,
                "row_idx": int(row_idx),
                "category": "Корпус",
                "supplier": str(row.get("Поставщик", "") or "").strip(),
                "case_brand": local_brand.upper() if local_brand else "",
                "case_model": local_series,
                "case_code": local_code,
                "case_issue": "no_model",
                "case_issue_label": "Не удалось выделить бренд/серию корпуса",
                "candidates": [],
            })
            continue

        candidates = _find_case_review_candidates(name, top_n=5)
        name_key = _normalize_name_key(name)
        if not name_key:
            continue
        if not candidates:
            no_candidates += 1
            case_report_items.append({
                "name": name,
                "row_idx": int(row_idx),
                "category": "Корпус",
                "supplier": str(row.get("Поставщик", "") or "").strip(),
                "case_brand": local_brand.upper(),
                "case_model": local_series,
                "case_code": local_code,
                "case_issue": "no_candidates",
                "case_issue_label": "Модель найдена, но кандидатов в БД нет",
                "candidates": [],
            })
            continue

        queue[name_key] = {
            "name": name,
            "category": "Корпус",
            "supplier": str(row.get("Поставщик", "") or "").strip(),
            "case_brand": local_brand.upper(),
            "case_model": local_series,
            "case_code": local_code,
            "cleared_id": "",
            "cleared_score": 1.0,
            "onliner_name": f"{local_brand.upper()} {local_series or local_code}",
            "candidates": list(candidates),
            "added_at": now_ts,
            "reason": "case_brand_model_manual",
            "reason_label": "Корпус: строгий матч по бренду, серии/коду, форм-фактору и признаку с БП/без БП. Подтверждение только вручную.",
        }
        case_report_items.append({
            "name": name,
            "row_idx": int(row_idx),
            "category": "Корпус",
            "supplier": str(row.get("Поставщик", "") or "").strip(),
            "case_brand": local_brand.upper(),
            "case_model": local_series,
            "case_code": local_code,
            "case_issue": "queued",
            "case_issue_label": "Есть кандидаты, отправлено в ручную очередь",
            "best_source": str((candidates[0] or {}).get("source", "") or "").strip() if candidates else "",
            "candidates": list(candidates),
        })
        queued += 1

    save_review_queue(queue)
    report_payload = {
        "matches": [],
        "no_match": case_report_items,
        "report_mode": "case",
        "report_title": "Отчёт корпусов N-Tech",
        "report_subtitle": (
            f"Обработано корпусов: {scanned}. "
            f"В очереди: {queued}, без модели: {no_model}, без кандидатов: {no_candidates}."
        ),
    }
    if scanned <= 0:
        payload = {
            "status": "ok",
            "scanned": 0,
            "queued": 0,
            "no_model": 0,
            "no_candidates": 0,
            "skipped_with_id": int(skipped_with_id),
            "skipped_non_ntech": int(skipped_non_ntech),
            "message": "В текущем прайсе N-Tech нет корпусов без ID.",
            **report_payload,
        }
        with AUTOFILL_IVEN_LOCK:
            autofill_iven_status.update({
                "running": False,
                "total": 0,
                "done": 0,
                "applied": 0,
                "skipped": 0,
                "percent": 100,
                "started_at": now_ts,
                "finished_at": int(time.time()),
                "message": payload["message"],
                "matches": [],
                "no_match": [],
                "report_mode": "case",
                "report_title": "Отчёт корпусов N-Tech",
                "report_subtitle": "В текущем прайсе корпуса без ID не найдены.",
            })
        return jsonify(payload)

    payload = {
        "status": "ok",
        "scanned": int(scanned),
        "queued": int(queued),
        "no_model": int(no_model),
        "no_candidates": int(no_candidates),
        "skipped_with_id": int(skipped_with_id),
        "skipped_non_ntech": int(skipped_non_ntech),
        "message": (
            f"Корпуса N-Tech: в ручную очередь добавлено {queued}."
            + (f" Без модели: {no_model}." if no_model else "")
            + (f" Без кандидатов: {no_candidates}." if no_candidates else "")
        ),
        **report_payload,
    }
    with AUTOFILL_IVEN_LOCK:
        autofill_iven_status.update({
            "running": False,
            "total": int(scanned),
            "done": int(scanned),
            "applied": 0,
            "skipped": int(no_model + no_candidates),
            "percent": 100,
            "started_at": now_ts,
            "finished_at": int(time.time()),
            "message": payload["message"],
            "matches": [],
            "no_match": case_report_items,
            "report_mode": "case",
            "report_title": "Отчёт корпусов N-Tech",
            "report_subtitle": report_payload["report_subtitle"],
        })
    return jsonify(payload)


@app.route("/api/hdd-review-queue-start", methods=["POST"])
def api_hdd_review_queue_start():
    global autofill_iven_status
    session_dir = session.get("session_dir")
    if not session_dir:
        return jsonify({"status": "error", "message": "Нет активной сессии"}), 400
    cons_path = Path(session_dir) / "consolidated_price.xlsx"
    if not cons_path.exists():
        return jsonify({"status": "error", "message": "Нет данных"}), 400

    df = read_consolidated_df(session_dir)
    df = ensure_category_column(df)
    if df.empty:
        return jsonify({"status": "error", "message": "Прайс пуст"}), 400

    queue = load_review_queue()
    scanned = 0
    queued = 0
    no_model = 0
    no_candidates = 0
    skipped_with_id = 0
    skipped_non_ntech = 0
    ntech_supplier_names = {"N-TECH", "NTECH"}
    now_ts = int(time.time())
    hdd_report_items = []

    for row_idx, row in df.iterrows():
        supplier = str(row.get("Поставщик", "") or "").strip().upper()
        if supplier not in ntech_supplier_names:
            skipped_non_ntech += 1
            continue
        name = str(row.get("Название", "") or "").strip()
        category = normalize_catalog_category_name(str(row_category(row) or "").strip())
        looks_like_hdd = _looks_like_hdd_name(name)
        if not name or not (category == "Жесткий диск" or looks_like_hdd):
            continue
        oid = normalize_onliner_id(row.get("OnlinerID", ""))
        if oid:
            skipped_with_id += 1
            continue

        scanned += 1
        hdd_meta = _hdd_brand_model_key(name)
        local_brand = str(hdd_meta.get("brand", "") or "").strip()
        local_code = str(hdd_meta.get("code", "") or "").upper()
        local_capacity = str(hdd_meta.get("capacity", "") or "").strip()
        if not local_brand and not local_code:
            no_model += 1
            hdd_report_items.append({
                "name": name,
                "row_idx": int(row_idx),
                "category": "Жесткий диск",
                "supplier": str(row.get("Поставщик", "") or "").strip(),
                "hdd_brand": local_brand.upper() if local_brand else "",
                "hdd_code": "",
                "hdd_capacity": local_capacity,
                "hdd_issue": "no_model",
                "hdd_issue_label": "Не удалось выделить бренд/артикул HDD",
                "candidates": [],
            })
            continue

        candidates = _find_hdd_review_candidates(name, top_n=5)
        name_key = _normalize_name_key(name)
        if not name_key:
            continue
        if not candidates:
            no_candidates += 1
            hdd_report_items.append({
                "name": name,
                "row_idx": int(row_idx),
                "category": "Жесткий диск",
                "supplier": str(row.get("Поставщик", "") or "").strip(),
                "hdd_brand": local_brand.upper() if local_brand else "",
                "hdd_code": local_code,
                "hdd_capacity": local_capacity,
                "hdd_issue": "no_candidates",
                "hdd_issue_label": "Модель найдена, но кандидатов в БД нет",
                "candidates": [],
            })
            continue

        queue[name_key] = {
            "name": name,
            "category": "Жесткий диск",
            "supplier": str(row.get("Поставщик", "") or "").strip(),
            "hdd_brand": local_brand.upper() if local_brand else "",
            "hdd_code": local_code,
            "hdd_capacity": local_capacity,
            "cleared_id": "",
            "cleared_score": 1.0,
            "onliner_name": f"{local_brand.upper()} {local_code or local_capacity}".strip(),
            "candidates": list(candidates),
            "added_at": now_ts,
            "reason": "hdd_article_manual",
            "reason_label": "HDD: совпадение по бренду, артикулу в скобках, объёму и типу (внутр./внеш.). Подтверждение только вручную.",
        }
        hdd_report_items.append({
            "name": name,
            "row_idx": int(row_idx),
            "category": "Жесткий диск",
            "supplier": str(row.get("Поставщик", "") or "").strip(),
            "hdd_brand": local_brand.upper() if local_brand else "",
            "hdd_code": local_code,
            "hdd_capacity": local_capacity,
            "hdd_issue": "queued",
            "hdd_issue_label": "Есть кандидаты, отправлено в ручную очередь",
            "best_source": str((candidates[0] or {}).get("source", "") or "").strip() if candidates else "",
            "candidates": list(candidates),
        })
        queued += 1

    save_review_queue(queue)
    report_payload = {
        "matches": [],
        "no_match": hdd_report_items,
        "report_mode": "hdd",
        "report_title": "Отчёт HDD N-Tech",
        "report_subtitle": (
            f"Обработано HDD: {scanned}. "
            f"В очереди: {queued}, без модели: {no_model}, без кандидатов: {no_candidates}."
        ),
    }
    if scanned <= 0:
        payload = {
            "status": "ok",
            "scanned": 0,
            "queued": 0,
            "no_model": 0,
            "no_candidates": 0,
            "skipped_with_id": int(skipped_with_id),
            "skipped_non_ntech": int(skipped_non_ntech),
            "message": "В текущем прайсе N-Tech нет HDD без ID.",
            **report_payload,
        }
        with AUTOFILL_IVEN_LOCK:
            autofill_iven_status.update({
                "running": False,
                "total": 0,
                "done": 0,
                "applied": 0,
                "skipped": 0,
                "percent": 100,
                "started_at": now_ts,
                "finished_at": int(time.time()),
                "message": payload["message"],
                "matches": [],
                "no_match": [],
                "report_mode": "hdd",
                "report_title": "Отчёт HDD N-Tech",
                "report_subtitle": "В текущем прайсе HDD без ID не найдены.",
            })
        return jsonify(payload)

    payload = {
        "status": "ok",
        "scanned": int(scanned),
        "queued": int(queued),
        "no_model": int(no_model),
        "no_candidates": int(no_candidates),
        "skipped_with_id": int(skipped_with_id),
        "skipped_non_ntech": int(skipped_non_ntech),
        "message": (
            f"HDD N-Tech: в ручную очередь добавлено {queued}."
            + (f" Без модели: {no_model}." if no_model else "")
            + (f" Без кандидатов: {no_candidates}." if no_candidates else "")
        ),
        **report_payload,
    }
    with AUTOFILL_IVEN_LOCK:
        autofill_iven_status.update({
            "running": False,
            "total": int(scanned),
            "done": int(scanned),
            "applied": 0,
            "skipped": int(no_model + no_candidates),
            "percent": 100,
            "started_at": now_ts,
            "finished_at": int(time.time()),
            "message": payload["message"],
            "matches": [],
            "no_match": hdd_report_items,
            "report_mode": "hdd",
            "report_title": "Отчёт HDD N-Tech",
            "report_subtitle": report_payload["report_subtitle"],
        })
    return jsonify(payload)


@app.route("/api/cooler-review-queue-start", methods=["POST"])
def api_cooler_review_queue_start():
    global autofill_iven_status
    session_dir = session.get("session_dir")
    if not session_dir:
        return jsonify({"status": "error", "message": "Нет активной сессии"}), 400
    cons_path = Path(session_dir) / "consolidated_price.xlsx"
    if not cons_path.exists():
        return jsonify({"status": "error", "message": "Нет данных"}), 400

    df = read_consolidated_df(session_dir)
    df = ensure_category_column(df)
    if df.empty:
        return jsonify({"status": "error", "message": "Прайс пуст"}), 400

    queue = load_review_queue()
    scanned = 0
    queued = 0
    no_model = 0
    no_candidates = 0
    skipped_with_id = 0
    skipped_non_ntech = 0
    ntech_supplier_names = {"N-TECH", "NTECH"}
    now_ts = int(time.time())
    cooler_report_items = []

    for row_idx, row in df.iterrows():
        supplier = str(row.get("Поставщик", "") or "").strip().upper()
        if supplier not in ntech_supplier_names:
            skipped_non_ntech += 1
            continue
        name = str(row.get("Название", "") or "").strip()
        category = normalize_catalog_category_name(str(row_category(row) or "").strip())
        air_like = _looks_like_cooler_name(name)
        liq_like = _looks_like_liquid_cpu_cooling_name(name)
        if not name:
            continue
        if category == "Охлаждение" and not (air_like or liq_like):
            continue
        if not (category == "Кулер" or air_like or liq_like):
            continue
        oid = normalize_onliner_id(row.get("OnlinerID", ""))
        if oid:
            skipped_with_id += 1
            continue

        scanned += 1
        cooler_meta = _cooler_brand_model_key(name)
        local_brand = str(cooler_meta.get("brand", "") or "").strip()
        local_code = str(cooler_meta.get("code", "") or "").upper()
        local_tdp = str(cooler_meta.get("tdp", "") or "").strip()
        if not local_brand and not local_code:
            no_model += 1
            cooler_report_items.append({
                "name": name,
                "row_idx": int(row_idx),
                "category": "Охлаждение",
                "supplier": str(row.get("Поставщик", "") or "").strip(),
                "cooler_brand": local_brand.upper() if local_brand else "",
                "cooler_code": "",
                "cooler_tdp": local_tdp,
                "cooler_issue": "no_model",
                "cooler_issue_label": "Не удалось выделить бренд/код охлаждения",
                "candidates": [],
            })
            continue

        candidates = _find_cooler_review_candidates(name, top_n=5)
        name_key = _normalize_name_key(name)
        if not name_key:
            continue
        if not candidates:
            no_candidates += 1
            cooler_report_items.append({
                "name": name,
                "row_idx": int(row_idx),
                "category": "Охлаждение",
                "supplier": str(row.get("Поставщик", "") or "").strip(),
                "cooler_brand": local_brand.upper() if local_brand else "",
                "cooler_code": local_code,
                "cooler_tdp": local_tdp,
                "cooler_issue": "no_candidates",
                "cooler_issue_label": "Модель найдена, но кандидатов в БД нет",
                "candidates": [],
            })
            continue

        queue[name_key] = {
            "name": name,
            "category": "Охлаждение",
            "supplier": str(row.get("Поставщик", "") or "").strip(),
            "cooler_brand": local_brand.upper() if local_brand else "",
            "cooler_code": local_code,
            "cooler_tdp": local_tdp,
            "cleared_id": "",
            "cleared_score": 1.0,
            "onliner_name": f"{local_brand.upper()} {local_code or local_tdp}".strip(),
            "candidates": list(candidates),
            "added_at": now_ts,
            "reason": "cooler_article_manual",
            "reason_label": "Охлаждение (кулер / СЖО): бренд, артикул, TDP, цвет. Подтверждение только вручную.",
        }
        cooler_report_items.append({
            "name": name,
            "row_idx": int(row_idx),
            "category": "Охлаждение",
            "supplier": str(row.get("Поставщик", "") or "").strip(),
            "cooler_brand": local_brand.upper() if local_brand else "",
            "cooler_code": local_code,
            "cooler_tdp": local_tdp,
            "cooler_issue": "queued",
            "cooler_issue_label": "Есть кандидаты, отправлено в ручную очередь",
            "best_source": str((candidates[0] or {}).get("source", "") or "").strip() if candidates else "",
            "candidates": list(candidates),
        })
        queued += 1

    save_review_queue(queue)
    report_payload = {
        "matches": [],
        "no_match": cooler_report_items,
        "report_mode": "cooler",
        "report_title": "Отчёт охлаждения N-Tech",
        "report_subtitle": (
            f"Обработано позиций охлаждения: {scanned}. "
            f"В очереди: {queued}, без модели: {no_model}, без кандидатов: {no_candidates}."
        ),
    }
    if scanned <= 0:
        payload = {
            "status": "ok",
            "scanned": 0,
            "queued": 0,
            "no_model": 0,
            "no_candidates": 0,
            "skipped_with_id": int(skipped_with_id),
            "skipped_non_ntech": int(skipped_non_ntech),
            "message": "В текущем прайсе N-Tech нет позиций охлаждения без ID.",
            **report_payload,
        }
        with AUTOFILL_IVEN_LOCK:
            autofill_iven_status.update({
                "running": False,
                "total": 0,
                "done": 0,
                "applied": 0,
                "skipped": 0,
                "percent": 100,
                "started_at": now_ts,
                "finished_at": int(time.time()),
                "message": payload["message"],
                "matches": [],
                "no_match": [],
                "report_mode": "cooler",
                "report_title": "Отчёт охлаждения N-Tech",
                "report_subtitle": "В текущем прайсе охлаждение без ID не найдено.",
            })
        return jsonify(payload)

    payload = {
        "status": "ok",
        "scanned": int(scanned),
        "queued": int(queued),
        "no_model": int(no_model),
        "no_candidates": int(no_candidates),
        "skipped_with_id": int(skipped_with_id),
        "skipped_non_ntech": int(skipped_non_ntech),
        "message": (
            f"Охлаждение N-Tech: в ручную очередь добавлено {queued}."
            + (f" Без модели: {no_model}." if no_model else "")
            + (f" Без кандидатов: {no_candidates}." if no_candidates else "")
        ),
        **report_payload,
    }
    with AUTOFILL_IVEN_LOCK:
        autofill_iven_status.update({
            "running": False,
            "total": int(scanned),
            "done": int(scanned),
            "applied": 0,
            "skipped": int(no_model + no_candidates),
            "percent": 100,
            "started_at": now_ts,
            "finished_at": int(time.time()),
            "message": payload["message"],
            "matches": [],
            "no_match": cooler_report_items,
            "report_mode": "cooler",
            "report_title": "Отчёт охлаждения N-Tech",
            "report_subtitle": report_payload["report_subtitle"],
        })
    return jsonify(payload)


@app.route("/api/printer-review-queue-start", methods=["POST"])
def api_printer_review_queue_start():
    global autofill_iven_status
    session_dir = session.get("session_dir")
    if not session_dir:
        return jsonify({"status": "error", "message": "Нет активной сессии"}), 400
    cons_path = Path(session_dir) / "consolidated_price.xlsx"
    if not cons_path.exists():
        return jsonify({"status": "error", "message": "Нет данных"}), 400

    df = read_consolidated_df(session_dir)
    df = ensure_category_column(df)
    if df.empty:
        return jsonify({"status": "error", "message": "Прайс пуст"}), 400

    queue = load_review_queue()
    scanned = 0
    queued = 0
    no_model = 0
    no_candidates = 0
    skipped_with_id = 0
    skipped_non_ntech = 0
    ntech_supplier_names = {"N-TECH", "NTECH"}
    now_ts = int(time.time())
    printer_report_items = []

    for row_idx, row in df.iterrows():
        supplier = str(row.get("Поставщик", "") or "").strip().upper()
        if supplier not in ntech_supplier_names:
            skipped_non_ntech += 1
            continue
        name = str(row.get("Название", "") or "").strip()
        category = normalize_catalog_category_name(str(row_category(row) or "").strip())
        looks = _looks_like_printer_or_mfp_name(name)
        if not name:
            continue
        if not (category == "Принтер и МФУ" or looks):
            continue
        oid = normalize_onliner_id(row.get("OnlinerID", ""))
        if oid:
            skipped_with_id += 1
            continue

        scanned += 1
        pm_meta = _printer_mfp_brand_model_key(name)
        local_brand = str(pm_meta.get("brand", "") or "").strip()
        local_article = str(pm_meta.get("article", "") or "").strip()
        local_model = str(pm_meta.get("model_display", "") or "").strip()
        local_mc = str(pm_meta.get("model_compact", "") or "").strip()
        if not local_brand and not local_article and len(local_mc) < 5:
            no_model += 1
            printer_report_items.append({
                "name": name,
                "row_idx": int(row_idx),
                "category": "Принтер и МФУ",
                "supplier": str(row.get("Поставщик", "") or "").strip(),
                "printer_brand": local_brand.upper() if local_brand else "",
                "printer_article": local_article.upper() if local_article else "",
                "printer_model": local_model,
                "printer_issue": "no_model",
                "printer_issue_label": "Не удалось выделить бренд / модель / артикул принтера или МФУ",
                "candidates": [],
            })
            continue

        candidates = _find_printer_review_candidates(name, top_n=5)
        name_key = _normalize_name_key(name)
        if not name_key:
            continue
        if not candidates:
            no_candidates += 1
            printer_report_items.append({
                "name": name,
                "row_idx": int(row_idx),
                "category": "Принтер и МФУ",
                "supplier": str(row.get("Поставщик", "") or "").strip(),
                "printer_brand": local_brand.upper() if local_brand else "",
                "printer_article": local_article.upper() if local_article else "",
                "printer_model": local_model,
                "printer_issue": "no_candidates",
                "printer_issue_label": "Модель найдена, но кандидатов в БД нет",
                "candidates": [],
            })
            continue

        queue[name_key] = {
            "name": name,
            "category": "Принтер и МФУ",
            "supplier": str(row.get("Поставщик", "") or "").strip(),
            "printer_brand": local_brand.upper() if local_brand else "",
            "printer_article": local_article.upper() if local_article else "",
            "printer_model": local_model,
            "cleared_id": "",
            "cleared_score": 1.0,
            "onliner_name": f"{local_brand.upper()} {local_model or local_article}".strip(),
            "candidates": list(candidates),
            "added_at": now_ts,
            "reason": "printer_article_manual",
            "reason_label": "Принтер / МФУ: бренд, модель, артикул в скобках. Подтверждение только вручную.",
        }
        printer_report_items.append({
            "name": name,
            "row_idx": int(row_idx),
            "category": "Принтер и МФУ",
            "supplier": str(row.get("Поставщик", "") or "").strip(),
            "printer_brand": local_brand.upper() if local_brand else "",
            "printer_article": local_article.upper() if local_article else "",
            "printer_model": local_model,
            "printer_issue": "queued",
            "printer_issue_label": "Есть кандидаты, отправлено в ручную очередь",
            "best_source": str((candidates[0] or {}).get("source", "") or "").strip() if candidates else "",
            "candidates": list(candidates),
        })
        queued += 1

    save_review_queue(queue)
    report_payload = {
        "matches": [],
        "no_match": printer_report_items,
        "report_mode": "printer",
        "report_title": "Отчёт принтеров и МФУ N-Tech",
        "report_subtitle": (
            f"Обработано принтеров и МФУ: {scanned}. "
            f"В очереди: {queued}, без модели: {no_model}, без кандидатов: {no_candidates}."
        ),
    }
    if scanned <= 0:
        payload = {
            "status": "ok",
            "scanned": 0,
            "queued": 0,
            "no_model": 0,
            "no_candidates": 0,
            "skipped_with_id": int(skipped_with_id),
            "skipped_non_ntech": int(skipped_non_ntech),
            "message": "В текущем прайсе N-Tech нет принтеров или МФУ без ID.",
            **report_payload,
        }
        with AUTOFILL_IVEN_LOCK:
            autofill_iven_status.update({
                "running": False,
                "total": 0,
                "done": 0,
                "applied": 0,
                "skipped": 0,
                "percent": 100,
                "started_at": now_ts,
                "finished_at": int(time.time()),
                "message": payload["message"],
                "matches": [],
                "no_match": [],
                "report_mode": "printer",
                "report_title": "Отчёт принтеров и МФУ N-Tech",
                "report_subtitle": "В текущем прайсе принтеры / МФУ без ID не найдены.",
            })
        return jsonify(payload)

    payload = {
        "status": "ok",
        "scanned": int(scanned),
        "queued": int(queued),
        "no_model": int(no_model),
        "no_candidates": int(no_candidates),
        "skipped_with_id": int(skipped_with_id),
        "skipped_non_ntech": int(skipped_non_ntech),
        "message": (
            f"Принтеры и МФУ N-Tech: в ручную очередь добавлено {queued}."
            + (f" Без модели: {no_model}." if no_model else "")
            + (f" Без кандидатов: {no_candidates}." if no_candidates else "")
        ),
        **report_payload,
    }
    with AUTOFILL_IVEN_LOCK:
        autofill_iven_status.update({
            "running": False,
            "total": int(scanned),
            "done": int(scanned),
            "applied": 0,
            "skipped": int(no_model + no_candidates),
            "percent": 100,
            "started_at": now_ts,
            "finished_at": int(time.time()),
            "message": payload["message"],
            "matches": [],
            "no_match": printer_report_items,
            "report_mode": "printer",
            "report_title": "Отчёт принтеров и МФУ N-Tech",
            "report_subtitle": report_payload["report_subtitle"],
        })
    return jsonify(payload)


@app.route("/api/peripheral-review-queue-start", methods=["POST"])
def api_peripheral_review_queue_start():
    global autofill_iven_status
    session_dir = session.get("session_dir")
    if not session_dir:
        return jsonify({"status": "error", "message": "Нет активной сессии"}), 400
    cons_path = Path(session_dir) / "consolidated_price.xlsx"
    if not cons_path.exists():
        return jsonify({"status": "error", "message": "Нет данных"}), 400

    df = read_consolidated_df(session_dir)
    df = ensure_category_column(df)
    if df.empty:
        return jsonify({"status": "error", "message": "Прайс пуст"}), 400

    queue = load_review_queue()
    scanned = 0
    queued = 0
    no_candidates = 0
    skipped_with_id = 0
    skipped_non_ntech = 0
    ntech_supplier_names = {"N-TECH", "NTECH"}
    now_ts = int(time.time())
    peripheral_report_items = []

    for row_idx, row in df.iterrows():
        supplier = str(row.get("Поставщик", "") or "").strip().upper()
        if supplier not in ntech_supplier_names:
            skipped_non_ntech += 1
            continue
        name = str(row.get("Название", "") or "").strip()
        category = normalize_catalog_category_name(str(row_category(row) or "").strip())
        if not name:
            continue
        if category not in {"Клавиатура", "Мышь", "Наушники", "Акустика"} and not _looks_like_peripheral_name(name):
            continue
        oid = normalize_onliner_id(row.get("OnlinerID", ""))
        if oid:
            skipped_with_id += 1
            continue

        scanned += 1
        candidates = _find_peripheral_review_candidates(name, top_n=5)
        name_key = _normalize_name_key(name)
        if not name_key:
            continue
        report_category = category if category in {"Клавиатура", "Мышь", "Наушники", "Акустика"} else "Периферия"
        if not candidates:
            no_candidates += 1
            peripheral_report_items.append({
                "name": name,
                "row_idx": int(row_idx),
                "category": report_category,
                "supplier": str(row.get("Поставщик", "") or "").strip(),
                "peripheral_issue": "no_candidates",
                "peripheral_issue_label": "Кандидатов в БД не найдено",
                "candidates": [],
            })
            continue

        queue[name_key] = {
            "name": name,
            "category": report_category,
            "supplier": str(row.get("Поставщик", "") or "").strip(),
            "cleared_id": "",
            "cleared_score": 1.0,
            "onliner_name": name,
            "candidates": list(candidates),
            "added_at": now_ts,
            "reason": "peripheral_manual",
            "reason_label": "Периферия N-Tech: кандидаты найдены, требуется ручное подтверждение.",
        }
        peripheral_report_items.append({
            "name": name,
            "row_idx": int(row_idx),
            "category": report_category,
            "supplier": str(row.get("Поставщик", "") or "").strip(),
            "peripheral_issue": "queued",
            "peripheral_issue_label": "Есть кандидаты, отправлено в ручную очередь",
            "best_source": str((candidates[0] or {}).get("source", "") or "").strip() if candidates else "",
            "candidates": list(candidates),
        })
        queued += 1

    save_review_queue(queue)
    report_payload = {
        "matches": [],
        "no_match": peripheral_report_items,
        "report_mode": "peripheral",
        "report_title": "Отчёт периферии N-Tech",
        "report_subtitle": (
            f"Обработано позиций периферии: {scanned}. "
            f"В очереди: {queued}, без кандидатов: {no_candidates}."
        ),
    }
    if scanned <= 0:
        payload = {
            "status": "ok",
            "scanned": 0,
            "queued": 0,
            "no_candidates": 0,
            "skipped_with_id": int(skipped_with_id),
            "skipped_non_ntech": int(skipped_non_ntech),
            "message": "В текущем прайсе N-Tech нет периферии без ID.",
            **report_payload,
        }
        with AUTOFILL_IVEN_LOCK:
            autofill_iven_status.update({
                "running": False,
                "total": 0,
                "done": 0,
                "applied": 0,
                "skipped": 0,
                "percent": 100,
                "started_at": now_ts,
                "finished_at": int(time.time()),
                "message": payload["message"],
                "matches": [],
                "no_match": [],
                "report_mode": "peripheral",
                "report_title": "Отчёт периферии N-Tech",
                "report_subtitle": "В текущем прайсе периферия без ID не найдена.",
            })
        return jsonify(payload)

    payload = {
        "status": "ok",
        "scanned": int(scanned),
        "queued": int(queued),
        "no_candidates": int(no_candidates),
        "skipped_with_id": int(skipped_with_id),
        "skipped_non_ntech": int(skipped_non_ntech),
        "message": (
            f"Периферия N-Tech: в ручную очередь добавлено {queued}."
            + (f" Без кандидатов: {no_candidates}." if no_candidates else "")
        ),
        **report_payload,
    }
    with AUTOFILL_IVEN_LOCK:
        autofill_iven_status.update({
            "running": False,
            "total": int(scanned),
            "done": int(scanned),
            "applied": 0,
            "skipped": int(no_candidates),
            "percent": 100,
            "started_at": now_ts,
            "finished_at": int(time.time()),
            "message": payload["message"],
            "matches": [],
            "no_match": peripheral_report_items,
            "report_mode": "peripheral",
            "report_title": "Отчёт периферии N-Tech",
            "report_subtitle": report_payload["report_subtitle"],
        })
    return jsonify(payload)


@app.route("/api/review-queue")
def api_review_queue():
    session_dir = session.get("session_dir")
    queue = load_review_queue()
    if not queue:
        return jsonify({"items": []})

    # Если есть активная сессия — обогащаем актуальными row_idx из текущего DataFrame
    result = []
    stale_keys = set()
    if session_dir:
        try:
            df = read_consolidated_df(session_dir)
            name_to_row = {}
            name_to_has_id = {}
            for i, row in df.iterrows():
                nk = _normalize_name_key(str(row.get("Название", "")))
                if nk:
                    name_to_row[nk] = int(i)
                    name_to_has_id[nk] = bool(normalize_onliner_id(row.get("OnlinerID", "")))
        except Exception:
            name_to_row = {}
            name_to_has_id = {}
    else:
        name_to_row = {}
        name_to_has_id = {}

    for name_key, entry in queue.items():
        if name_to_has_id.get(name_key):
            stale_keys.add(name_key)
            continue
        item = dict(entry)
        item["name_key"] = name_key
        item["row_idx"] = name_to_row.get(name_key)
        result.append(item)

    if stale_keys:
        for name_key in stale_keys:
            queue.pop(name_key, None)
        save_review_queue(queue)

    result.sort(key=lambda x: x.get("added_at", 0), reverse=True)
    return jsonify({"items": result})


@app.route("/api/review-queue-pick", methods=["POST"])
def api_review_queue_pick():
    payload = request.get_json(silent=True) or {}
    name_key = str(payload.get("name_key", "")).strip()
    oid = normalize_onliner_id(payload.get("onliner_id", ""))
    url = str(payload.get("url", "")).strip()
    name = str(payload.get("name", "")).strip()

    if not name_key:
        return jsonify({"status": "error", "message": "name_key обязателен"}), 400

    queue = load_review_queue()
    entry = queue.get(name_key, {})
    if not name:
        name = str(entry.get("name", "")).strip()

    if oid:
        # Пользователь выбрал кандидата — сохраняем в manual_bindings
        manual_bindings = load_manual_id_bindings()
        manual_bindings[name_key] = {"id": oid, "url": url}
        save_manual_id_bindings(manual_bindings)

        # Применяем к текущей сессии, если она есть
        session_dir = session.get("session_dir")
        if session_dir:
            try:
                df = read_consolidated_df(session_dir)
                if "OnlinerID" not in df.columns:
                    df["OnlinerID"] = ""
                if "Ссылка" not in df.columns:
                    df["Ссылка"] = ""
                for i, row in df.iterrows():
                    nk = _normalize_name_key(str(row.get("Название", "")))
                    if nk == name_key:
                        df.at[i, "OnlinerID"] = oid
                        if url:
                            df.at[i, "Ссылка"] = url
                write_consolidated_df(session_dir, df)
                write_consolidated_json(df, Path(session_dir) / "consolidated.json")
                append_id_change_journal({
                    "ts": int(time.time()),
                    "action": "review_queue_pick",
                    "source": "api_review_queue_pick",
                    "changes": [{"name": name, "new_onliner_id": oid, "new_url": url}],
                })
            except Exception:
                pass

    # Убираем из очереди (независимо от того, выбрал или пропустил)
    queue.pop(name_key, None)
    save_review_queue(queue)
    return jsonify({"status": "ok", "remaining": len(queue)})


@app.route("/api/review-queue-clear", methods=["POST"])
def api_review_queue_clear():
    save_review_queue({})
    return jsonify({"status": "ok"})


@app.route("/api/categories")
def api_categories():
    session_dir = session.get("session_dir")
    if not session_dir:
        return jsonify({"categories": []})
    cons_path = Path(session_dir) / "consolidated_price.xlsx"
    if not cons_path.exists():
        return jsonify({"categories": []})

    df = read_consolidated_df(session_dir)
    if "Название" not in df.columns:
        return jsonify({"categories": []})

    df = ensure_category_column(df)
    df = apply_visibility_filter(df, session_dir)
    overrides = load_category_overrides()
    category_counts = {}
    for category in df.apply(lambda row: row_category(row, overrides), axis=1):
        category_counts[category] = category_counts.get(category, 0) + 1

    items = [
        {"name": name, "count": count}
        for name, count in sorted(category_counts.items(), key=lambda x: _category_sort_key(x[0]))
    ]
    return jsonify({"categories": items})


@app.route("/api/category-catalog")
def api_category_catalog():
    session_dir = session.get("session_dir")
    if not session_dir:
        return jsonify({"categories": CATEGORY_PRIORITY})
    cons_path = Path(session_dir) / "consolidated_price.xlsx"
    all_cats = set(CATEGORY_PRIORITY)
    overrides = load_category_overrides()
    all_cats.update(v for v in overrides.values() if str(v).strip())
    all_cats.update(k for k in load_category_markups().keys() if str(k).strip())
    if cons_path.exists():
        df = read_consolidated_df(session_dir)
        df = ensure_category_column(df, overrides)
        for _, row in df.iterrows():
            all_cats.add(row_category(row, overrides))
    return jsonify({"categories": sorted(all_cats, key=_category_sort_key)})


@app.route("/api/suppliers")
def api_suppliers():
    session_dir = session.get("session_dir")
    if not session_dir:
        return jsonify({"suppliers": []})
    cons_path = Path(session_dir) / "consolidated_price.xlsx"
    if not cons_path.exists():
        return jsonify({"suppliers": []})
    df = read_consolidated_df(session_dir)
    suppliers = sorted({str(s).strip() for s in df.get("Поставщик", pd.Series(dtype=str)).dropna().tolist() if str(s).strip()})
    return jsonify({"suppliers": suppliers})


@app.route("/api/supplier-categories")
def api_supplier_categories():
    session_dir = session.get("session_dir")
    if not session_dir:
        return jsonify({"categories": []})
    supplier = str(request.args.get("supplier", "")).strip()
    if not supplier:
        return jsonify({"categories": []})

    cons_path = Path(session_dir) / "consolidated_price.xlsx"
    if not cons_path.exists():
        return jsonify({"categories": []})

    df = read_consolidated_df(session_dir)
    if "Поставщик" not in df.columns:
        return jsonify({"categories": []})
    df = df[df["Поставщик"].astype(str).str.strip() == supplier]
    df = ensure_category_column(df)
    visibility_map = load_visibility_map(session_dir)
    hidden_set = set(visibility_map.get(supplier, []))

    counts = {}
    for _, row in df.iterrows():
        cat = row_category(row)
        counts[cat] = counts.get(cat, 0) + 1

    items = []
    for name, count in counts.items():
        items.append({"name": name, "count": count, "hidden": name in hidden_set})
    items.sort(key=lambda x: (1 if x.get("hidden") else 0, _category_sort_key(str(x.get("name", "")).strip())))
    return jsonify({"status": "ok", "categories": items})


@app.route("/api/category-visibility", methods=["POST"])
def api_category_visibility():
    session_dir = session.get("session_dir")
    if not session_dir:
        return jsonify({"status": "error", "message": "Нет активной сессии"}), 400

    payload = request.get_json(silent=True) or {}
    supplier = str(payload.get("supplier", "")).strip()
    categories = payload.get("categories", [])
    hidden = bool(payload.get("hidden", True))

    if not supplier:
        return jsonify({"status": "error", "message": "Поставщик не выбран"}), 400
    if not isinstance(categories, list):
        return jsonify({"status": "error", "message": "Некорректный список категорий"}), 400

    categories = [str(x).strip() for x in categories if str(x).strip()]
    if not categories:
        return jsonify({"status": "error", "message": "Категории не выбраны"}), 400

    visibility_map = load_visibility_map(session_dir)
    hidden_set = set(visibility_map.get(supplier, []))

    if hidden:
        hidden_set.update(categories)
    else:
        hidden_set.difference_update(categories)

    if hidden_set:
        visibility_map[supplier] = sorted(hidden_set, key=_category_sort_key)
    else:
        visibility_map.pop(supplier, None)

    save_visibility_map(session_dir, visibility_map)
    categories_out = [{"name": name, "hidden": True} for name in hidden_set]
    categories_out.sort(key=lambda x: (1 if x.get("hidden") else 0, _category_sort_key(str(x.get("name", "")).strip())))
    return jsonify({
        "status": "ok",
        "supplier": supplier,
        "hidden": hidden,
        "categories": categories_out,
    })


@app.route("/api/apply-markup", methods=["POST"])
def api_apply_markup():
    session_dir = session.get("session_dir")
    if not session_dir:
        return jsonify({"status": "error", "message": "No session"})

    cons_path = Path(session_dir) / "consolidated_price.xlsx"
    cons_json_path = Path(session_dir) / "consolidated.json"
    if not cons_path.exists():
        return jsonify({"status": "error", "message": "No data"})

    payload = request.get_json(silent=True) or {}
    categories = payload.get("categories", [])
    percent = payload.get("percent", None)
    threshold = payload.get("threshold", 0)
    min_profit = payload.get("min_profit", 0)
    no_discount_percent = payload.get("no_discount_percent", 0)
    base_mode = str(payload.get("base_mode", "wholesale")).strip().lower()
    if base_mode not in {"wholesale", "onliner_min", "onliner_avg", "onliner_max"}:
        base_mode = "wholesale"
    if not isinstance(categories, list) or not categories:
        return jsonify({"status": "error", "message": "Категории не выбраны"})
    try:
        percent = float(percent)
    except Exception:
        return jsonify({"status": "error", "message": "Некорректный процент"})
    try:
        threshold = float(threshold)
    except Exception:
        return jsonify({"status": "error", "message": "Некорректный порог опта"})
    try:
        min_profit = float(min_profit)
    except Exception:
        return jsonify({"status": "error", "message": "Некорректная мин. прибыль"})
    try:
        no_discount_percent = float(no_discount_percent)
    except Exception:
        return jsonify({"status": "error", "message": "Некорректный процент для цены без скидки"})
    if percent < 0:
        return jsonify({"status": "error", "message": "Процент не может быть отрицательным"})
    if threshold < 0:
        return jsonify({"status": "error", "message": "Порог опта не может быть отрицательным"})
    if min_profit < 0:
        return jsonify({"status": "error", "message": "Мин. прибыль не может быть отрицательной"})
    if no_discount_percent < 0:
        return jsonify({"status": "error", "message": "Процент цены без скидки не может быть отрицательным"})

    df = read_consolidated_df(session_dir)
    if "Название" not in df.columns or "Цена" not in df.columns:
        return jsonify({"status": "error", "message": "В файле нет нужных колонок"})
    if "РРЦ" not in df.columns:
        df["РРЦ"] = ""
    if "Цена без скидки" not in df.columns:
        df["Цена без скидки"] = ""
    df["РРЦ"] = df["РРЦ"].astype("object")
    df["Цена без скидки"] = df["Цена без скидки"].astype("object")
    df = ensure_category_column(df)

    selected = set(str(c).strip() for c in categories if str(c).strip())
    market_map = {}
    market_checked = 0
    if base_mode in {"onliner_min", "onliner_avg", "onliner_max"}:
        ids = []
        id_hints_bulk = {}
        for _, row in df.iterrows():
            category = row_category(row)
            if category not in selected:
                continue
            oid = normalize_onliner_id(row.get("OnlinerID", ""))
            if oid:
                ids.append(oid)
                if oid not in id_hints_bulk:
                    id_hints_bulk[oid] = {
                        "name": str(row.get("Название", "") or ""),
                        "category": category,
                    }
        # Protect against extremely heavy external checks on huge selections.
        unique_ids = list(dict.fromkeys(ids))[:400]
        market_checked = len(unique_ids)
        market_map = get_onliner_market_stats_bulk(unique_ids, max_workers=22, id_hints=id_hints_bulk)

    updated = 0
    eligible = 0
    missing_market = 0
    for i, row in df.iterrows():
        category = row_category(row)
        if category not in selected:
            continue
        eligible += 1
        base_price = pd.to_numeric(row.get("Цена", np.nan), errors="coerce")
        if pd.isna(base_price):
            continue
        calc_base = float(base_price)
        if base_mode in {"onliner_min", "onliner_avg", "onliner_max"}:
            oid = normalize_onliner_id(row.get("OnlinerID", ""))
            stats = market_map.get(oid, {}) if oid else {}
            if base_mode == "onliner_min":
                market_price = stats.get("min")
            elif base_mode == "onliner_avg":
                market_price = stats.get("avg")
            else:
                market_price = stats.get("max")
            if market_price is None:
                missing_market += 1
                continue
            calc_base = float(market_price)
        rrc, no_discount_price = calc_rrc_and_no_discount(
            calc_base,
            percent,
            threshold=threshold,
            min_profit=min_profit,
            no_discount_percent=no_discount_percent,
        )
        df.at[i, "РРЦ"] = rrc
        df.at[i, "Цена без скидки"] = no_discount_price
        updated += 1

    write_consolidated_df(session_dir, df)
    write_consolidated_json(df, cons_json_path)
    # Запоминаем последнюю наценку для выбранных категорий.
    markups = load_category_markups()
    for c in selected:
        markups[c] = {
            "percent": percent,
            "threshold": threshold,
            "min_profit": min_profit,
            "no_discount_percent": no_discount_percent,
            "base_mode": base_mode,
        }
    save_category_markups(markups)
    return jsonify({
        "status": "ok",
        "updated": updated,
        "eligible": eligible,
        "total": len(df),
        "percent": percent,
        "threshold": threshold,
        "min_profit": min_profit,
        "no_discount_percent": no_discount_percent,
        "base_mode": base_mode,
        "market_checked": market_checked,
        "missing_market": missing_market,
    })


@app.route("/api/markup-preview", methods=["POST"])
def api_markup_preview():
    session_dir = session.get("session_dir")
    if not session_dir:
        return jsonify({"items": []})

    cons_path = Path(session_dir) / "consolidated_price.xlsx"
    if not cons_path.exists():
        return jsonify({"items": []})

    payload = request.get_json(silent=True) or {}
    categories = payload.get("categories", [])
    percent = payload.get("percent", 0)
    limit = payload.get("limit", 8)

    try:
        percent = float(percent)
    except Exception:
        percent = 0.0
    try:
        limit = int(limit)
    except Exception:
        limit = 8
    limit = max(1, min(limit, 20))

    selected = {str(c).strip() for c in categories if str(c).strip()}
    if not selected:
        return jsonify({"items": []})

    df = read_consolidated_df(session_dir)
    df = ensure_category_column(df)
    df = apply_visibility_filter(df, session_dir)
    if "Название" not in df.columns or "Цена" not in df.columns:
        return jsonify({"items": []})

    items = []
    for i, row in df.iterrows():
        category = row_category(row)
        if category not in selected:
            continue
        price = pd.to_numeric(row.get("Цена", np.nan), errors="coerce")
        if pd.isna(price):
            continue
        old_rrc = pd.to_numeric(row.get("РРЦ", np.nan), errors="coerce")
        calc_rrc = round_price_to_90(float(price) * (1.0 + percent / 100.0))
        items.append({
            "category": category,
            "name": str(row.get("Название", "")),
            "price": round(float(price), 2),
            "old_rrc": "" if pd.isna(old_rrc) else round(float(old_rrc), 2),
            "new_rrc": round(float(calc_rrc), 2),
        })
        if len(items) >= limit:
            break

    return jsonify({"items": items})


@app.route("/api/category-override-items")
def api_category_override_items():
    session_dir = session.get("session_dir")
    if not session_dir:
        return jsonify({"items": []})

    cons_path = Path(session_dir) / "consolidated_price.xlsx"
    if not cons_path.exists():
        return jsonify({"items": []})

    query = str(request.args.get("q", "")).strip().lower()
    limit = int(request.args.get("limit", 40) or 40)
    limit = max(1, min(limit, 200))

    df = read_consolidated_df(session_dir)
    df = ensure_category_column(df)
    if "Название" not in df.columns:
        return jsonify({"items": []})

    overrides = load_category_overrides()
    result = []
    for _, row in df.iterrows():
        name = str(row.get("Название", ""))
        if not name:
            continue
        if query and query not in name.lower():
            continue
        key = build_item_category_key(row)
        auto_cat = infer_category(name)
        eff_cat = row_category(row, overrides)
        result.append({
            "key": key,
            "name": name,
            "supplier": str(row.get("Поставщик", "")),
            "auto_category": auto_cat,
            "category": eff_cat,
            "manual": (eff_cat != auto_cat),
        })
        if len(result) >= limit:
            break

    return jsonify({"items": result})


@app.route("/api/category-override-set", methods=["POST"])
def api_category_override_set():
    session_dir = session.get("session_dir")
    payload = request.get_json(silent=True) or {}
    item_key = str(payload.get("item_key", "")).strip()
    target_category = str(payload.get("target_category", "")).strip()

    if not item_key:
        return jsonify({"status": "error", "message": "Товар не выбран"})
    if not target_category:
        return jsonify({"status": "error", "message": "Категория не выбрана"})

    overrides = load_category_overrides()
    overrides[item_key] = target_category

    if session_dir:
        cons_path = Path(session_dir) / "consolidated_price.xlsx"
        if cons_path.exists():
            df = read_consolidated_df(session_dir)
            df = ensure_category_column(df, overrides)
            changed = 0
            for i, row in df.iterrows():
                keys = set(build_item_category_keys(row))
                if item_key in keys:
                    df.at[i, "Категория"] = target_category
                    for k in keys:
                        overrides[k] = target_category
                    changed += 1
            if changed:
                write_consolidated_df(session_dir, df)
                write_consolidated_json(df, Path(session_dir) / "consolidated.json")
    save_category_overrides(overrides)
    return jsonify({"status": "ok"})


@app.route("/api/category-preview-items", methods=["POST"])
def api_category_preview_items():
    session_dir = session.get("session_dir")
    if not session_dir:
        return jsonify({"items": []})

    cons_path = Path(session_dir) / "consolidated_price.xlsx"
    if not cons_path.exists():
        return jsonify({"items": []})

    payload = request.get_json(silent=True) or {}
    categories = payload.get("categories", [])
    with_market = bool(payload.get("with_market", False))
    allow_stale_market = bool(payload.get("allow_stale_market", True))
    try:
        limit = int(payload.get("limit", 4000))
    except Exception:
        limit = 4000
    limit = max(1, min(limit, 10000))
    try:
        max_market_checks = int(payload.get("max_market_checks", 300))
    except Exception:
        max_market_checks = 300
    max_market_checks = max(1, min(max_market_checks, 800))
    selected = {str(c).strip() for c in categories if str(c).strip()}
    if not selected:
        return jsonify({"items": []})

    df = read_consolidated_df(session_dir)
    df = ensure_category_column(df)
    df = apply_visibility_filter(df, session_dir)

    overrides = load_category_overrides()
    items = []
    onliner_ids = []
    for i, row in df.iterrows():
        category = row_category(row, overrides)
        if category not in selected:
            continue
        price = pd.to_numeric(row.get("Цена", np.nan), errors="coerce")
        rrc = pd.to_numeric(row.get("РРЦ", np.nan), errors="coerce")
        oid = normalize_onliner_id(row.get("OnlinerID", ""))
        if with_market and oid:
            onliner_ids.append(oid)
        item = {
            "key": build_item_category_key(row),
            "row_idx": int(i),
            "onliner_id": oid,
            "name": str(row.get("Название", "")),
            "supplier": str(row.get("Поставщик", "")),
            "category": category,
            "price": "" if pd.isna(price) else round(float(price), 2),
            "rrc": "" if pd.isna(rrc) else round(float(rrc), 2),
        }
        items.append(item)
        if len(items) >= limit:
            break
    market_map = {}
    market_checked = 0
    preview_row_count = len(items)
    rows_with_onliner_id = sum(1 for it in items if str(it.get("onliner_id") or "").strip())
    market_unique_onliner_ids = 0
    if with_market and onliner_ids:
        unique_ids = list(dict.fromkeys(onliner_ids))
        if not allow_stale_market:
            unique_ids = unique_ids[:max_market_checks]
        market_unique_onliner_ids = len(unique_ids)
        market_checked = market_unique_onliner_ids
        cache = load_onliner_market_cache()
        for oid in unique_ids:
            market_map[oid] = get_onliner_market_stats_from_cache_only(oid, cache=cache, allow_stale=allow_stale_market)
    missing_market = 0
    missing_market_ids = set()
    no_onliner_id = 0
    for it in items:
        oid = it.get("onliner_id", "")
        stats = market_map.get(oid, {}) if oid else {}
        mmin = stats.get("min")
        mavg = stats.get("avg")
        mmax = stats.get("max")
        if with_market and not oid:
            no_onliner_id += 1
        if with_market and oid and (mmin is None and mavg is None):
            missing_market += 1
            missing_market_ids.add(oid)
        it["market_min"] = "" if mmin is None else round(float(mmin), 2)
        it["market_avg"] = "" if mavg is None else round(float(mavg), 2)
        it["market_max"] = "" if mmax is None else round(float(mmax), 2)
        it["market_offers"] = int(stats.get("offers", 0) or 0) if stats else 0
        it["min_competitors"] = int(stats.get("min_competitors", 0) or 0) if stats else 0
        it["avg_competitors"] = int(stats.get("avg_competitors", 0) or 0) if stats else 0

    items.sort(key=lambda x: (x["category"], x["name"].lower()))
    return jsonify({
        "items": items,
        "preview_row_count": preview_row_count,
        "market_rows_with_onliner_id": rows_with_onliner_id,
        "market_unique_onliner_ids": market_unique_onliner_ids,
        "market_checked": market_checked,
        "missing_market": missing_market,
        "missing_market_ids": len(missing_market_ids),
        "no_onliner_id": no_onliner_id,
    })


def _market_refresh_worker(session_dir, categories):
    try:
        cons_path = Path(session_dir) / "consolidated_price.xlsx"
        if not cons_path.exists():
            with MARKET_REFRESH_LOCK:
                market_refresh_status.update({"running": False, "finished_at": int(time.time())})
            return

        df = read_consolidated_df(session_dir)
        df = ensure_category_column(df)
        selected = {str(c).strip() for c in categories if str(c).strip()}
        if selected:
            df = df[df.apply(lambda r: row_category(r) in selected, axis=1)]

        cat_to_ids = {}
        for _, row in df.iterrows():
            cat = row_category(row)
            oid = normalize_onliner_id(row.get("OnlinerID", ""))
            if not oid:
                continue
            cat_to_ids.setdefault(cat, set()).add(oid)

        all_ids = sorted(set().union(*cat_to_ids.values())) if cat_to_ids else []
        with MARKET_REFRESH_LOCK:
            market_refresh_status["total"] = len(all_ids)
            market_refresh_status["done"] = 0
            market_refresh_status["success"] = 0
            market_refresh_status["errors"] = 0
            market_refresh_status["recent_errors"] = []
            market_refresh_status["categories"] = {
                cat: {"done": 0, "total": len(ids), "percent": 0, "errors": 0, "recent_errors": []}
                for cat, ids in cat_to_ids.items()
            }

        cache = load_onliner_market_cache()
        now = int(time.time())
        id_to_cats = {}
        for cat, ids in cat_to_ids.items():
            for oid in ids:
                id_to_cats.setdefault(oid, []).append(cat)

        id_hints = {}
        for _, row in df.iterrows():
            oid = normalize_onliner_id(row.get("OnlinerID", ""))
            if not oid or oid in id_hints:
                continue
            id_hints[oid] = {
                "name": str(row.get("Название", "") or ""),
                "category": row_category(row),
            }

        def _fetch_market_for_refresh(oid):
            h = id_hints.get(oid) or {}
            return fetch_onliner_market_stats(
                oid,
                product_name=str(h.get("name", "") or ""),
                category_name=str(h.get("category", "") or ""),
            )

        success_count = 0
        error_count = 0
        with ThreadPoolExecutor(max_workers=MARKET_REFRESH_POOL_WORKERS) as ex:
            fut_to_oid = {ex.submit(_fetch_market_for_refresh, oid): oid for oid in all_ids}
            done_count = 0
            for fut in as_completed(fut_to_oid):
                oid = fut_to_oid[fut]
                try:
                    stats = fut.result()
                except Exception:
                    stats = {"min": None, "avg": None, "max": None, "offers": 0, "min_competitors": 0, "avg_competitors": 0, "_error": True}
                had_values_before = market_stats_has_values(cache.get(oid))
                has_values_now = market_stats_has_values(stats)
                error_reason = str(stats.get("_error_reason", "") or "").strip()
                if has_values_now:
                    cache[oid] = {"updated_at": now, **stats}
                    success_count += 1
                elif not had_values_before:
                    cache[oid] = {"updated_at": now, **stats}
                    error_count += 1
                else:
                    error_count += 1
                done_count += 1
                with MARKET_REFRESH_LOCK:
                    market_refresh_status["done"] = done_count
                    market_refresh_status["success"] = success_count
                    market_refresh_status["errors"] = error_count
                    for cat in id_to_cats.get(oid, []):
                        st = market_refresh_status["categories"].get(cat)
                        if not st:
                            continue
                        st["done"] += 1
                        st["percent"] = int(round((st["done"] / max(st["total"], 1)) * 100))
                        if not has_values_now:
                            st["errors"] = int(st.get("errors", 0)) + 1
                            if error_reason:
                                line = f"{cat}: {oid} -> {error_reason}"
                                recent = list(st.get("recent_errors", []) or [])
                                recent.append(line)
                                st["recent_errors"] = recent[-5:]
                                all_recent = list(market_refresh_status.get("recent_errors", []) or [])
                                all_recent.append(line)
                                market_refresh_status["recent_errors"] = all_recent[-12:]

        save_onliner_market_cache(cache)
    finally:
        with MARKET_REFRESH_LOCK:
            market_refresh_status["running"] = False
            market_refresh_status["finished_at"] = int(time.time())


def _collect_known_onliner_ids(max_ids=AUTO_REFRESH_MAX_IDS, session_dir=None):
    ids = []
    # 0) Current consolidated session IDs (preferred, all categories in current price).
    try:
        sdir = str(session_dir or "").strip()
        if sdir:
            cons_path = Path(sdir) / "consolidated_price.xlsx"
            if cons_path.exists():
                df = read_consolidated_df(sdir)
                for _, row in df.iterrows():
                    oid = normalize_onliner_id(row.get("OnlinerID", ""))
                    if oid:
                        ids.append(oid)
    except Exception:
        pass
    # 1) existing market cache ids
    cache = load_onliner_market_cache()
    ids.extend(cache.keys())
    # 2) id cache ids
    id_cache = load_id_cache()
    if isinstance(id_cache, dict):
        for _, rec in id_cache.items():
            if not isinstance(rec, dict):
                continue
            oid = normalize_onliner_id(rec.get("id", ""))
            if oid:
                ids.append(oid)
    out = []
    seen = set()
    for oid in ids:
        if oid and oid not in seen:
            seen.add(oid)
            out.append(oid)
        if len(out) >= max_ids:
            break
    return out


def _market_id_hints_from_session(session_dir):
    """Первая строка прайса по каждому OnlinerID → подсказки для B2B-поиска раздела/производителя."""
    hints = {}
    try:
        sdir = str(session_dir or "").strip()
        if not sdir:
            return hints
        cons_path = Path(sdir) / "consolidated_price.xlsx"
        if not cons_path.exists():
            return hints
        df = read_consolidated_df(sdir)
        df = ensure_category_column(df)
        for _, row in df.iterrows():
            oid = normalize_onliner_id(row.get("OnlinerID", ""))
            if not oid or oid in hints:
                continue
            hints[oid] = {
                "name": str(row.get("Название", "") or ""),
                "category": row_category(row),
            }
    except Exception:
        pass
    return hints


def _auto_market_refresh_loop():
    # Runs while app process is alive.
    while True:
        try:
            settings = load_auto_refresh_settings()
            if not settings.get("enabled"):
                time.sleep(AUTO_REFRESH_POLL_SEC)
                continue

            with MARKET_REFRESH_LOCK:
                manual_running = bool(market_refresh_status.get("running"))
            if manual_running:
                time.sleep(AUTO_REFRESH_POLL_SEC)
                continue

            interval_hours = int(settings.get("interval_hours", 12) or 12)
            if interval_hours not in AUTO_REFRESH_ALLOWED_HOURS:
                interval_hours = 12
            now = int(time.time())
            last_run = int(settings.get("last_run_ts", 0) or 0)
            due = (last_run <= 0) or (now - last_run >= interval_hours * 3600)
            if not due:
                time.sleep(AUTO_REFRESH_POLL_SEC)
                continue

            ids = _collect_known_onliner_ids(session_dir=LAST_ACTIVE_SESSION_DIR)
            if not ids:
                settings["last_run_ts"] = now
                settings["last_status"] = "idle"
                settings["last_count"] = 0
                settings["last_message"] = "Нет товаров с OnlinerID для автообновления."
                save_auto_refresh_settings(settings)
                time.sleep(AUTO_REFRESH_POLL_SEC)
                continue

            settings["last_started_ts"] = now
            settings["last_status"] = "running"
            settings["last_message"] = f"Автообновление запущено, товаров: {len(ids)}"
            save_auto_refresh_settings(settings)

            # refresh in bounded batches without touching UI status
            cache = load_onliner_market_cache()
            auto_hints = _market_id_hints_from_session(LAST_ACTIVE_SESSION_DIR)

            def _auto_fetch_market(oid):
                h = auto_hints.get(oid) or {}
                return fetch_onliner_market_stats(
                    oid,
                    product_name=str(h.get("name", "") or ""),
                    category_name=str(h.get("category", "") or ""),
                )

            with ThreadPoolExecutor(max_workers=min(MARKET_REFRESH_POOL_WORKERS, max(4, len(ids)))) as ex:
                fut_to_oid = {ex.submit(_auto_fetch_market, oid): oid for oid in ids}
                for fut in as_completed(fut_to_oid):
                    oid = fut_to_oid[fut]
                    try:
                        stats = fut.result()
                    except Exception:
                        stats = {"min": None, "avg": None, "max": None, "offers": 0, "min_competitors": 0, "avg_competitors": 0, "_error": True}
                    had_values_before = market_stats_has_values(cache.get(oid))
                    has_values_now = market_stats_has_values(stats)
                    if has_values_now or not had_values_before:
                        cache[oid] = {"updated_at": now, **stats}
            save_onliner_market_cache(cache)

            done_ts = int(time.time())
            latest = load_auto_refresh_settings()
            latest["last_run_ts"] = done_ts
            latest["last_status"] = "ok"
            latest["last_count"] = len(ids)
            latest["last_message"] = f"Автообновление завершено. Обновлено ID: {len(ids)}"
            save_auto_refresh_settings(latest)
        except Exception as e:
            latest = load_auto_refresh_settings()
            latest["last_status"] = "error"
            latest["last_message"] = f"Ошибка автообновления: {str(e)[:160]}"
            save_auto_refresh_settings(latest)
        time.sleep(AUTO_REFRESH_POLL_SEC)


@app.route("/api/auto-refresh-settings")
def api_auto_refresh_settings():
    settings = load_auto_refresh_settings()
    now = int(time.time())
    interval_hours = int(settings.get("interval_hours", 12) or 12)
    if interval_hours not in AUTO_REFRESH_ALLOWED_HOURS:
        interval_hours = 12
    last_run_ts = int(settings.get("last_run_ts", 0) or 0)
    next_run_ts = 0
    if settings.get("enabled"):
        next_run_ts = (last_run_ts + interval_hours * 3600) if last_run_ts > 0 else now
    next_in_sec = max(0, next_run_ts - now) if next_run_ts else 0
    return jsonify({
        "enabled": bool(settings.get("enabled")),
        "interval_hours": interval_hours,
        "last_run_ts": last_run_ts,
        "last_started_ts": int(settings.get("last_started_ts", 0) or 0),
        "last_status": str(settings.get("last_status", "idle")),
        "last_count": int(settings.get("last_count", 0) or 0),
        "last_message": str(settings.get("last_message", "")),
        "next_run_ts": int(next_run_ts or 0),
        "next_in_sec": int(next_in_sec or 0),
    })


@app.route("/api/auto-refresh-settings", methods=["POST"])
def api_auto_refresh_settings_update():
    payload = request.get_json(silent=True) or {}
    current = load_auto_refresh_settings()
    was_enabled = bool(current.get("enabled"))
    if "enabled" in payload:
        current["enabled"] = bool(payload.get("enabled"))
    if "interval_hours" in payload:
        try:
            interval = int(payload.get("interval_hours"))
        except Exception:
            interval = current.get("interval_hours", 12)
        if interval in AUTO_REFRESH_ALLOWED_HOURS:
            current["interval_hours"] = interval
    if current.get("enabled") and int(current.get("last_run_ts", 0) or 0) <= 0:
        # First enable: run ASAP.
        current["last_run_ts"] = 0
        current["last_message"] = "Автообновление включено. Ближайший запуск — в ближайшие секунды."
    elif not current.get("enabled"):
        current["last_message"] = "Автообновление выключено."
    save_auto_refresh_settings(current)

    # Immediate full refresh when toggled ON: refresh all categories now.
    if current.get("enabled") and not was_enabled:
        sdir = session.get("session_dir") or LAST_ACTIVE_SESSION_DIR
        started = _start_market_refresh(sdir, [])
        latest = load_auto_refresh_settings()
        if started.get("status") == "started":
            latest["last_status"] = "running"
            latest["last_started_ts"] = int(time.time())
            latest["last_message"] = "Автообновление включено. Мгновенный запуск по всем категориям."
        elif started.get("status") == "already_running":
            latest["last_status"] = "running"
            latest["last_message"] = "Автообновление включено. Уже идет текущее обновление."
        else:
            latest["last_status"] = "error"
            latest["last_message"] = "Автообновление включено, но старт не выполнен: " + str(started.get("message", "нет активной сессии"))
        save_auto_refresh_settings(latest)

    return jsonify({"status": "ok"})


@app.route("/api/app-settings")
def api_app_settings():
    return jsonify({"status": "ok", "settings": load_app_settings()})


@app.route("/api/app-settings", methods=["POST"])
def api_app_settings_update():
    payload = request.get_json(silent=True) or {}
    current = load_app_settings()
    merged = _deep_merge_dict(current, payload if isinstance(payload, dict) else {})
    saved = save_app_settings(merged)
    return jsonify({"status": "ok", "settings": saved})


@app.route("/api/onliner-b2b-test", methods=["POST"])
def api_onliner_b2b_test():
    try:
        token_info = onliner_b2b_get_token(force_refresh=True)
        resp = onliner_b2b_request("GET", "/shop")
        preview = {}
        try:
            preview = resp.json() if resp.content else {}
        except Exception:
            preview = {}
        return jsonify({
            "status": "ok",
            "token_type": str(token_info.get("token_type", "Bearer") or "Bearer"),
            "expires_in": int(token_info.get("expires_in", 0) or 0),
            "http_status": int(resp.status_code or 0),
            "response_preview": preview if isinstance(preview, (dict, list)) else {},
        })
    except Exception as e:
        return jsonify({"status": "error", "message": str(e)[:300]}), 400


@app.route("/api/onliner-b2b-probe", methods=["POST"])
def api_onliner_b2b_probe():
    try:
        token_info = onliner_b2b_get_token(force_refresh=True)

        def _json_payload(resp):
            try:
                return resp.json() if resp.content else {}
            except Exception:
                return {}

        def _pick_items(payload):
            if isinstance(payload, list):
                return payload
            if isinstance(payload, dict):
                for key in ("items", "sections", "manufacturers", "products", "data", "results"):
                    value = payload.get(key)
                    if isinstance(value, list):
                        return value
                    if isinstance(value, dict):
                        nested = _pick_items(value)
                        if nested:
                            return nested
                if payload and all(isinstance(v, dict) for v in payload.values()):
                    return list(payload.values())
                numeric_like_keys = [str(k).strip() for k in payload.keys()]
                if payload and all(re.fullmatch(r"\d+", key or "") for key in numeric_like_keys):
                    normalized = []
                    for raw_key, raw_value in payload.items():
                        item_id = str(raw_key).strip()
                        if isinstance(raw_value, dict):
                            entry = dict(raw_value)
                            entry.setdefault("id", item_id)
                        else:
                            entry = {"id": item_id, "name": str(raw_value or "").strip()}
                        normalized.append(entry)
                    return normalized
            return []

        def _item_id(item):
            if not isinstance(item, dict):
                return ""
            for key in ("id", "section_id", "manufacturer_id", "product_id", "sectionId", "manufacturerId", "productId"):
                value = item.get(key)
                if value not in (None, ""):
                    return str(value).strip()
            urls = item.get("url") or item.get("href") or item.get("link")
            if isinstance(urls, str):
                m = re.search(r"/(\d+)(?:/)?$", urls.strip())
                if m:
                    return str(m.group(1)).strip()
            return ""

        def _item_name(item):
            if not isinstance(item, dict):
                return ""
            for key in ("name", "full_name", "title", "key", "slug"):
                value = item.get(key)
                if value not in (None, ""):
                    return str(value).strip()
            return ""

        section_resp = onliner_b2b_request("GET", "/sections")
        section_payload = _json_payload(section_resp)
        sections = _pick_items(section_payload)
        section_sample = sections[:3] if isinstance(sections, list) else []
        section_payload_type = type(section_payload).__name__
        section_payload_keys = list(section_payload.keys())[:10] if isinstance(section_payload, dict) else []

        result = {
            "status": "ok",
            "token_type": str(token_info.get("token_type", "Bearer") or "Bearer"),
            "expires_in": int(token_info.get("expires_in", 0) or 0),
            "sections_http_status": int(section_resp.status_code or 0),
            "sections_count": len(sections),
            "sections_sample": section_sample,
            "sections_payload_type": section_payload_type,
            "sections_payload_keys": section_payload_keys,
            "sections_preview": section_payload if isinstance(section_payload, (dict, list)) else {},
            "manufacturers_http_status": 0,
            "manufacturers_count": 0,
            "manufacturers_sample": [],
            "products_http_status": 0,
            "products_count": 0,
            "products_sample": [],
            "message": "",
        }

        first_section = sections[0] if sections else {}
        first_section_id = _item_id(first_section)
        if not first_section_id:
            result["message"] = "B2B вернул разделы, но не удалось выделить section id для дальнейшей проверки. Проверь sections_preview."
            return jsonify(result)

        manufacturers_resp = onliner_b2b_request("GET", f"/sections/{first_section_id}/manufacturers")
        manufacturers_payload = _json_payload(manufacturers_resp)
        manufacturers = _pick_items(manufacturers_payload)
        result["manufacturers_http_status"] = int(manufacturers_resp.status_code or 0)
        result["manufacturers_count"] = len(manufacturers)
        result["manufacturers_sample"] = manufacturers[:3] if isinstance(manufacturers, list) else []

        first_manufacturer = manufacturers[0] if manufacturers else {}
        first_manufacturer_id = _item_id(first_manufacturer)
        if not first_manufacturer_id:
            result["message"] = "B2B вернул производителей, но не удалось выделить manufacturer id для проверки товаров."
            return jsonify(result)

        products_resp = onliner_b2b_request("GET", f"/sections/{first_section_id}/manufacturers/{first_manufacturer_id}/products")
        products_payload = _json_payload(products_resp)
        products = _pick_items(products_payload)
        result["products_http_status"] = int(products_resp.status_code or 0)
        result["products_count"] = len(products)
        result["products_sample"] = products[:3] if isinstance(products, list) else []
        result["message"] = (
            "B2B данные получены. "
            f"Раздел: {_item_name(first_section) or first_section_id}; "
            f"производитель: {_item_name(first_manufacturer) or first_manufacturer_id}."
        )
        return jsonify(result)
    except Exception as e:
        return jsonify({"status": "error", "message": str(e)[:300]}), 400


@app.route("/api/source-fetch-start", methods=["POST"])
def api_source_fetch_start():
    payload = request.get_json(silent=True) or {}
    source_key = str(payload.get("source", "")).strip().lower()
    client_key = _get_api_source_status_key()
    settings = load_app_settings()
    cfg = (((settings or {}).get("api_sources") or {}).get(source_key) or {})
    if not cfg:
        return jsonify({"status": "error", "message": "Неизвестный источник"})
    runtime = _get_source_runtime(source_key, client_key=client_key)
    if runtime.get("running"):
        return jsonify({"status": "ok", "state": _serialize_source_runtime(runtime)})
    _update_source_runtime(
        source_key,
        client_key=client_key,
        running=True,
        ready=False,
        progress=0,
        downloaded=0,
        total_bytes=0,
        status="starting",
        message="Подготавливаю выгрузку...",
        started_at=time.time(),
        finished_at=0,
    )
    thread = threading.Thread(target=_fetch_api_source_worker, args=(source_key, client_key), daemon=True)
    thread.start()
    return jsonify({"status": "ok", "state": _serialize_source_runtime(_get_source_runtime(source_key, client_key=client_key))})


@app.route("/api/source-fetch-status")
def api_source_fetch_status():
    client_key = _get_api_source_status_key()
    source_key = str(request.args.get("source", "")).strip().lower()
    if source_key:
        state = _serialize_source_runtime(_get_source_runtime(source_key, client_key=client_key))
        return jsonify({"status": "ok", "state": state, "history": get_api_fetch_history(limit=20)})
    settings = load_app_settings()
    items = []
    for src in _iter_api_sources_for_ui(settings):
        runtime = _serialize_source_runtime(_get_source_runtime(src["key"], client_key=client_key))
        runtime.update({
            "label": src["label"],
            "supplier": src["supplier"],
            "enabled": src["enabled"],
            "configured": src["configured"],
            "mode": src["mode"],
        })
        items.append(runtime)
    return jsonify({"status": "ok", "items": items, "history": get_api_fetch_history(limit=20)})


@app.route("/api/source-process", methods=["POST"])
def api_source_process():
    payload = request.get_json(silent=True) or {}
    source_key = str(payload.get("source", "")).strip().lower()
    runtime = _get_source_runtime(source_key, client_key=_get_api_source_status_key())
    file_path = Path(str(runtime.get("file_path", "") or ""))
    supplier = str(runtime.get("supplier", "") or source_key.upper()).strip() or source_key.upper()
    label = str(runtime.get("label", source_key.upper()) or source_key.upper())
    started_at = int(time.time())
    if not file_path.exists():
        return jsonify({"status": "error", "message": "Сначала выгрузи прайс"}), 400
    try:
        result = _process_supplier_files([{
            "filepath": file_path,
            "display_name": runtime.get("file_name", file_path.name),
            "supplier_name": supplier,
        }])
    except Exception as e:
        finished_at = int(time.time())
        append_api_fetch_history({
            "source_key": source_key,
            "label": label,
            "supplier": supplier,
            "event_type": "process",
            "status": "error",
            "message": str(e) or "Не удалось обработать прайс",
            "file_name": file_path.name,
            "file_size": int(file_path.stat().st_size if file_path.exists() else 0),
            "started_at": started_at,
            "finished_at": finished_at,
            "duration_sec": max(0, finished_at - started_at),
        })
        return jsonify({"status": "error", "message": str(e) or "Не удалось обработать прайс"}), 500
    _finalize_processed_session(result["session_id"], result["session_dir"], result["output_path"])
    finished_at = int(time.time())
    stats = result.get("stats") or {}
    append_api_fetch_history({
        "source_key": source_key,
        "label": label,
        "supplier": supplier,
        "event_type": "process",
        "status": "ok",
        "message": "Прайс обработан",
        "file_name": file_path.name,
        "file_size": int(file_path.stat().st_size if file_path.exists() else 0),
        "items_count": int(stats.get("consolidated", 0) or 0),
        "without_id_count": int(stats.get("without_id", 0) or 0),
        "started_at": started_at,
        "finished_at": finished_at,
        "duration_sec": max(0, finished_at - started_at),
        "session_id": str(result.get("session_id", "") or ""),
    })
    return jsonify({"status": "ok", "redirect_url": url_for("result_page", sid=result["session_id"])})


@app.route("/api/source-process-batch", methods=["POST"])
def api_source_process_batch():
    payload = request.get_json(silent=True) or {}
    client_key = _get_api_source_status_key()
    requested_sources = payload.get("sources")
    if isinstance(requested_sources, list):
        source_keys = [str(item or "").strip().lower() for item in requested_sources if str(item or "").strip()]
    else:
        source_keys = []
    with SOURCE_FETCH_LOCK:
        client_state = dict(source_fetch_statuses.get(client_key, {}) or {})
    if not source_keys:
        source_keys = [str(k or "").strip().lower() for k, v in client_state.items() if isinstance(v, dict) and v.get("ready")]
    dedup = []
    seen = set()
    for key in source_keys:
        if key and key not in seen:
            seen.add(key)
            dedup.append(key)
    source_keys = dedup
    if not source_keys:
        return jsonify({"status": "error", "message": "Нет готовых API-прайсов для обработки."}), 400

    started_at = int(time.time())
    file_entries = []
    per_source_meta = []
    for source_key in source_keys:
        runtime = _get_source_runtime(source_key, client_key=client_key)
        file_path = Path(str(runtime.get("file_path", "") or ""))
        if not file_path.exists():
            continue
        supplier = str(runtime.get("supplier", "") or source_key.upper()).strip() or source_key.upper()
        label = str(runtime.get("label", source_key.upper()) or source_key.upper())
        file_entries.append({
            "filepath": file_path,
            "display_name": runtime.get("file_name", file_path.name),
            "supplier_name": supplier,
        })
        per_source_meta.append({
            "source_key": source_key,
            "supplier": supplier,
            "label": label,
            "file_name": file_path.name,
            "file_size": int(file_path.stat().st_size if file_path.exists() else 0),
        })
    if not file_entries:
        return jsonify({"status": "error", "message": "Готовые файлы не найдены. Сначала нажми «Выгрузить»."}), 400

    try:
        result = _process_supplier_files(file_entries)
    except Exception as e:
        finished_at = int(time.time())
        err_msg = str(e) or "Не удалось обработать API-прайсы"
        for meta in per_source_meta:
            append_api_fetch_history({
                "source_key": meta["source_key"],
                "label": meta["label"],
                "supplier": meta["supplier"],
                "event_type": "process",
                "status": "error",
                "message": "Пакетная обработка: " + err_msg,
                "file_name": meta["file_name"],
                "file_size": meta["file_size"],
                "started_at": started_at,
                "finished_at": finished_at,
                "duration_sec": max(0, finished_at - started_at),
            })
        return jsonify({"status": "error", "message": err_msg}), 500

    _finalize_processed_session(result["session_id"], result["session_dir"], result["output_path"])
    finished_at = int(time.time())
    stats = result.get("stats") or {}
    for meta in per_source_meta:
        append_api_fetch_history({
            "source_key": meta["source_key"],
            "label": meta["label"],
            "supplier": meta["supplier"],
            "event_type": "process",
            "status": "ok",
            "message": "Пакетная обработка API-источников",
            "file_name": meta["file_name"],
            "file_size": meta["file_size"],
            "items_count": int(stats.get("consolidated", 0) or 0),
            "without_id_count": int(stats.get("without_id", 0) or 0),
            "started_at": started_at,
            "finished_at": finished_at,
            "duration_sec": max(0, finished_at - started_at),
            "session_id": str(result.get("session_id", "") or ""),
        })
    return jsonify({
        "status": "ok",
        "processed_sources": source_keys,
        "redirect_url": url_for("result_page", sid=result["session_id"]),
    })


@app.route("/api/market-refresh-start", methods=["POST"])
def api_market_refresh_start():
    session_dir = session.get("session_dir")
    if not session_dir:
        return jsonify({"status": "error", "message": "No session"})
    payload = request.get_json(silent=True) or {}
    categories = payload.get("categories", [])
    if not isinstance(categories, list):
        categories = []
    return jsonify(_start_market_refresh(str(session_dir), categories))


def _start_market_refresh(session_dir, categories):
    session_dir = str(session_dir or "").strip()
    if not session_dir:
        return {"status": "error", "message": "No session"}
    cons_path = Path(session_dir) / "consolidated_price.xlsx"
    if not cons_path.exists():
        return {"status": "error", "message": "No data"}
    if not isinstance(categories, list):
        categories = []
    with MARKET_REFRESH_LOCK:
        if market_refresh_status.get("running"):
            return {"status": "already_running"}
        market_refresh_status["running"] = True
        market_refresh_status["started_at"] = int(time.time())
        market_refresh_status["finished_at"] = 0
        market_refresh_status["total"] = 0
        market_refresh_status["done"] = 0
        market_refresh_status["success"] = 0
        market_refresh_status["errors"] = 0
        market_refresh_status["categories"] = {}
    threading.Thread(
        target=_market_refresh_worker,
        args=(session_dir, categories),
        daemon=True,
    ).start()
    return {"status": "started"}


@app.route("/api/market-refresh-status")
def api_market_refresh_status():
    with MARKET_REFRESH_LOCK:
        st = dict(market_refresh_status)
        cats = st.get("categories", {}) or {}
        cats_pct = {
            k: {
                "done": int(v.get("done", 0)),
                "total": int(v.get("total", 0)),
                "percent": int(v.get("percent", 0)),
                "errors": int(v.get("errors", 0) or 0),
                "recent_errors": list(v.get("recent_errors", []) or []),
            }
            for k, v in cats.items()
        }
        total = int(st.get("total", 0))
        done = int(st.get("done", 0))
        overall = int(round((done / max(total, 1)) * 100)) if total else 0
        return jsonify({
            "running": bool(st.get("running")),
            "total": total,
            "done": done,
            "success": int(st.get("success", 0) or 0),
            "errors": int(st.get("errors", 0) or 0),
            "overall_percent": overall,
            "categories": cats_pct,
            "recent_errors": list(st.get("recent_errors", []) or []),
            "started_at": int(st.get("started_at", 0)),
            "finished_at": int(st.get("finished_at", 0)),
        })


@app.route("/api/onliner-offers/<onliner_id>")
def api_onliner_offers(onliner_id):
    oid = normalize_onliner_id(onliner_id)
    if not oid:
        return jsonify({"status": "error", "message": "Пустой OnlinerID"})
    product, product_error = _fetch_onliner_product_payload(oid)
    if not product:
        return jsonify({"status": "error", "message": product_error or "Товар не найден"})
    prices_obj = product.get("prices") or {}
    offers_count = int(((prices_obj.get("offers") or {}).get("count") or 0))
    positions_url = str(prices_obj.get("url", "")).strip()
    if not positions_url:
        return jsonify({
            "status": "ok",
            "offers_count": offers_count,
            "positions_count": 0,
            "unique_sellers_count": 0,
            "offers": [],
            "note": "API товара найден, но детализация офферов отсутствует.",
        })
    try:
        headers = {"User-Agent": "Mozilla/5.0", "Accept": "application/json"}
        rp = onliner_api_get(positions_url, timeout=12, headers=headers)
        if not rp.ok:
            return jsonify({"status": "error", "message": f"positions: http {rp.status_code}"})
        payload = rp.json() or {}
    except Exception:
        return jsonify({"status": "error", "message": "positions: timeout/connection"})
    offers = _extract_offer_rows(payload)
    unique_sellers = set()
    for row in offers:
        unique_sellers.add((str(row.get("seller_id", "")).strip(), str(row.get("seller_name", "")).strip()))
    return jsonify({
        "status": "ok",
        "offers_count": offers_count,
        "positions_count": len(offers),
        "unique_sellers_count": len(unique_sellers),
        "offers": offers[:120],
        "note": "Сравните offers.count, число позиций API и уникальные магазины. На сайте Onliner цифры могут отличаться.",
    })


@app.route("/api/category-markups")
def api_category_markups():
    return jsonify({"markups": load_category_markups()})


@app.route("/api/category-override-bulk", methods=["POST"])
def api_category_override_bulk():
    session_dir = session.get("session_dir")
    payload = request.get_json(silent=True) or {}
    item_keys = payload.get("item_keys", [])
    target_category = str(payload.get("target_category", "")).strip()
    if not isinstance(item_keys, list) or not item_keys:
        return jsonify({"status": "error", "message": "Не выбраны товары"})
    if not target_category:
        return jsonify({"status": "error", "message": "Не выбрана целевая категория"})

    keys = [str(k).strip() for k in item_keys if str(k).strip()]
    if not keys:
        return jsonify({"status": "error", "message": "Не выбраны товары"})

    overrides = load_category_overrides()
    for key in keys:
        overrides[key] = target_category

    updated_rows = 0
    if session_dir:
        cons_path = Path(session_dir) / "consolidated_price.xlsx"
        if cons_path.exists():
            df = read_consolidated_df(session_dir)
            df = ensure_category_column(df, overrides)
            key_set = set(keys)
            for i, row in df.iterrows():
                row_keys = set(build_item_category_keys(row))
                if key_set.intersection(row_keys):
                    df.at[i, "Категория"] = target_category
                    for k in row_keys:
                        overrides[k] = target_category
                    updated_rows += 1
            if updated_rows:
                write_consolidated_df(session_dir, df)
                write_consolidated_json(df, Path(session_dir) / "consolidated.json")
    save_category_overrides(overrides)

    return jsonify({"status": "ok", "updated": len(keys), "updated_rows": updated_rows})


@app.route("/api/category-autosort-preview", methods=["POST"])
def api_category_autosort_preview():
    session_dir = session.get("session_dir")
    if not session_dir:
        return jsonify({"items": [], "checked": 0, "skipped": 0})
    cons_path = Path(session_dir) / "consolidated_price.xlsx"
    if not cons_path.exists():
        return jsonify({"items": [], "checked": 0, "skipped": 0})

    payload = request.get_json(silent=True) or {}
    categories = payload.get("categories", [])
    selected = {str(c).strip() for c in categories if str(c).strip()} if isinstance(categories, list) else set()
    try:
        min_confidence = float(payload.get("min_confidence", 0.64))
    except Exception:
        min_confidence = 0.64
    min_confidence = max(0.50, min(0.95, min_confidence))

    df = read_consolidated_df(session_dir)
    if "Название" not in df.columns:
        return jsonify({"items": [], "checked": 0, "skipped": 0})
    df = ensure_category_column(df)
    df = apply_visibility_filter(df, session_dir)

    overrides = load_category_overrides()

    # Локальное обучение категорий только по уже загруженному прайсу:
    # "как у вас руками отсортировано" + текущие назначенные категории.
    category_token_counts = {}
    category_item_counts = {}
    for _, row in df.iterrows():
        cat = row_category(row, overrides)
        if not cat:
            continue
        toks = set(_name_tokens(row.get("Название", "")))
        if not toks:
            continue
        bucket = category_token_counts.setdefault(cat, {})
        for t in toks:
            bucket[t] = int(bucket.get(t, 0)) + 1
        category_item_counts[cat] = int(category_item_counts.get(cat, 0)) + 1

    # Частые токены по категориям + idf-подобный вес по редкости токена.
    token_category_df = {}
    for cat, tok_map in category_token_counts.items():
        for tok in tok_map.keys():
            token_category_df[tok] = int(token_category_df.get(tok, 0)) + 1

    def _predict_local_category(name):
        toks = set(_name_tokens(name))
        if not toks:
            return "", 0.0

        best_cat = ""
        best_score = 0.0
        second_score = 0.0
        total_cats = max(1, len(category_token_counts))

        for cat, tok_map in category_token_counts.items():
            cat_size = int(category_item_counts.get(cat, 0))
            if cat_size < 2:
                continue

            raw = 0.0
            hit = 0
            for t in toks:
                cnt = int(tok_map.get(t, 0))
                if cnt <= 0:
                    continue
                hit += 1
                # редкие по категориям токены сильнее помогают классификации
                idf = np.log1p((1.0 + total_cats) / (1.0 + int(token_category_df.get(t, 0))))
                raw += (cnt / max(1.0, float(cat_size))) * (1.0 + float(idf))

            if hit == 0:
                continue

            coverage = hit / max(1.0, float(len(toks)))
            score = (0.72 * raw) + (0.28 * coverage)

            if score > best_score:
                second_score = best_score
                best_score = score
                best_cat = cat
            elif score > second_score:
                second_score = score

        if not best_cat:
            return "", 0.0

        # Уверенность: нормализуем победителя и добавляем разрыв от 2-го места.
        gap = max(0.0, best_score - second_score)
        confidence = min(0.99, (best_score / (best_score + 0.8)) * 0.82 + min(0.17, gap))
        return best_cat, round(float(confidence), 3)

    checked = 0
    skipped = 0
    ai_checked = 0
    ai_suggested = 0
    ai_unavailable = 0
    proposals = {}
    conflict_keys = set()
    ai_candidates = []

    for _, row in df.iterrows():
        current_category = row_category(row, overrides)
        if selected and current_category not in selected:
            continue
        checked += 1

        name = str(row.get("Название", "")).strip()
        oid = normalize_onliner_id(row.get("OnlinerID", ""))
        # Режим AI-first: локальный классификатор используем только как подсказку для промпта.
        target_category, confidence = _predict_local_category(name)
        ai_candidates.append({
            "row": row,
            "name": name,
            "oid": oid,
            "current_category": current_category,
            "local_target": target_category,
            "local_confidence": confidence,
        })
        continue

    # AI fallback only for unresolved/low-confidence rows.
    if ai_candidates:
        valid_categories = sorted([str(c).strip() for c in category_item_counts.keys() if str(c).strip()], key=_category_sort_key)
        ai_batch = ai_candidates[:OPENAI_AUTOSORT_MAX_ITEMS]

        prepared = []
        for item in ai_batch:
            row = item.get("row")
            if row is None:
                skipped += 1
                continue
            name = str(item.get("name", "")).strip()
            current_category = str(item.get("current_category", "")).strip()
            local_target = str(item.get("local_target", "")).strip()
            local_confidence = float(item.get("local_confidence", 0.0) or 0.0)

            row_keys = build_item_category_keys(row)
            if any(str(overrides.get(k, "")).strip() for k in row_keys):
                skipped += 1
                continue

            item_key = build_item_category_key(row)
            if not item_key:
                skipped += 1
                continue

            local_hint = (
                f"local_target={local_target}; local_confidence={round(local_confidence, 3)}; "
                f"current_category={current_category}"
            )
            prepared.append({
                "item": item,
                "row": row,
                "name": name,
                "item_key": item_key,
                "current_category": current_category,
                "local_hint": local_hint,
            })

        ai_checked += len(prepared)
        if not OPENAI_API_KEY and prepared:
            ai_unavailable += len(prepared)

        def _ask_ai(entry):
            ai_category, ai_confidence, ai_reason = _openai_autosort_predict_category(
                entry["name"],
                valid_categories,
                local_hint=entry["local_hint"],
            )
            return entry, ai_category, ai_confidence, ai_reason

        if prepared:
            workers = max(1, min(OPENAI_AUTOSORT_MAX_WORKERS, len(prepared)))
            with ThreadPoolExecutor(max_workers=workers) as ex:
                futs = [ex.submit(_ask_ai, entry) for entry in prepared]
                for fut in as_completed(futs):
                    entry, ai_category, ai_confidence, ai_reason = fut.result()
                    item = entry["item"]
                    row = entry["row"]
                    item_key = entry["item_key"]
                    name = entry["name"]
                    current_category = entry["current_category"]

                    if not ai_category or ai_confidence < max(0.56, min_confidence):
                        skipped += 1
                        continue
                    if ai_category == current_category:
                        skipped += 1
                        continue

                    existing = proposals.get(item_key)
                    if existing and str(existing.get("target_category", "")).strip() != ai_category:
                        conflict_keys.add(item_key)
                        continue
                    if existing:
                        continue

                    proposals[item_key] = {
                        "item_key": item_key,
                        "onliner_id": str(item.get("oid", "")).strip(),
                        "name": name,
                        "supplier": str(row.get("Поставщик", "")).strip(),
                        "current_category": current_category,
                        "target_category": ai_category,
                        "catalog_category": "",
                        "source": f"AI classifier ({ai_reason})",
                        "confidence": round(float(ai_confidence), 3),
                    }
                    ai_suggested += 1

    for key in list(conflict_keys):
        proposals.pop(key, None)

    proposal_keys = set(proposals.keys())
    affected_rows = {k: 0 for k in proposal_keys}
    if proposal_keys:
        for _, row in df.iterrows():
            row_keys = set(build_item_category_keys(row))
            for k in proposal_keys.intersection(row_keys):
                affected_rows[k] += 1

    items = []
    for item_key, rec in proposals.items():
        out = dict(rec)
        out["affected_rows"] = int(affected_rows.get(item_key, 1) or 1)
        items.append(out)
    items.sort(key=lambda x: (_category_sort_key(x.get("target_category", "")), _category_sort_key(x.get("current_category", "")), str(x.get("name", "")).lower()))

    return jsonify({
        "items": items,
        "checked": int(checked),
        "skipped": int(skipped + len(conflict_keys)),
        "ai_checked": int(ai_checked),
        "ai_suggested": int(ai_suggested),
        "ai_unavailable": int(ai_unavailable),
    })


@app.route("/api/category-autosort-apply", methods=["POST"])
def api_category_autosort_apply():
    session_dir = session.get("session_dir")
    if not session_dir:
        return jsonify({"status": "error", "message": "No session"})
    cons_path = Path(session_dir) / "consolidated_price.xlsx"
    if not cons_path.exists():
        return jsonify({"status": "error", "message": "No data"})

    payload = request.get_json(silent=True) or {}
    items = payload.get("items", [])
    if not isinstance(items, list) or not items:
        return jsonify({"status": "error", "message": "Нет выбранных позиций"})

    target_by_key = {}
    for it in items:
        if not isinstance(it, dict):
            continue
        item_key = str(it.get("item_key", "")).strip()
        target_category = str(it.get("target_category", "")).strip()
        if not item_key or not target_category:
            continue
        target_by_key[item_key] = target_category
    if not target_by_key:
        return jsonify({"status": "error", "message": "Нет корректных данных для применения"})

    overrides = load_category_overrides()
    for item_key, target_category in target_by_key.items():
        overrides[item_key] = target_category

    df = read_consolidated_df(session_dir)
    df = ensure_category_column(df, overrides)
    updated_rows = 0
    target_key_set = set(target_by_key.keys())
    for i, row in df.iterrows():
        row_keys = set(build_item_category_keys(row))
        matched = [target_by_key[k] for k in row_keys if k in target_key_set]
        if not matched:
            continue
        target_category = matched[0]
        if len(matched) > 1:
            # Теоретически возможна коллизия: берем первую (детерминированно).
            target_category = sorted(matched)[0]
        current_category = str(df.at[i, "Категория"]).strip() if "Категория" in df.columns else row_category(row, overrides)
        if current_category != target_category:
            df.at[i, "Категория"] = target_category
            updated_rows += 1
        # Расширяем ручную привязку на все ключи этой строки.
        for rk in row_keys:
            overrides[rk] = target_category

    write_consolidated_df(session_dir, df)
    write_consolidated_json(df, Path(session_dir) / "consolidated.json")
    save_category_overrides(overrides)

    return jsonify({
        "status": "ok",
        "updated_keys": int(len(target_by_key)),
        "updated_rows": int(updated_rows),
    })


@app.route("/api/resolve-start", methods=["POST"])
def api_resolve_start():
    global resolve_status
    if resolve_status["running"]:
        return jsonify({"status": "already_running"})

    session_dir = session.get("session_dir")
    if not session_dir:
        return jsonify({"status": "error", "message": "No session"})

    cons_path = Path(session_dir) / "consolidated_price.xlsx"
    if not cons_path.exists():
        return jsonify({"status": "error", "message": "No data"})

    df = read_consolidated_df(session_dir)
    
    id_to_name = {}
    for _, row in df.iterrows():
        oid = row.get("OnlinerID")
        name = row.get("Название", "")
        if oid and str(oid).strip() and str(oid) != "nan":
            id_to_name[str(oid)] = name
    
    all_ids = list(id_to_name.keys())
    cache = load_url_cache()
    uncached = [oid for oid in all_ids if oid not in cache]

    resolve_status = {
        "running": True,
        "resolved": 0,
        "total": len(uncached),
        "cached": len(cache),
    }

    def _run_resolve():
        global resolve_status
        try:
            def progress(done, total):
                resolve_status["resolved"] = done
                resolve_status["cached"] = len(cache)

            resolve_onliner_urls(uncached, cache=cache, max_workers=5, progress_callback=progress, id_to_name=id_to_name)
            resolve_status["resolved"] = resolve_status["total"]
            resolve_status["cached"] = len(cache)
            
            df = read_consolidated_df(session_dir)
            for i, row in df.iterrows():
                oid = row.get("OnlinerID")
                if oid and str(oid) in cache:
                    df.at[i, "Ссылка"] = cache.get(str(oid), "")
            write_consolidated_df(session_dir, df)
            
            cons_json_path = Path(session_dir) / "consolidated.json"
            write_consolidated_json(df, cons_json_path)
        finally:
            resolve_status["running"] = False

    thread = threading.Thread(target=_run_resolve, daemon=True)
    thread.start()

    return jsonify({"status": "started", "total": len(uncached)})


@app.route("/api/resolve-status")
def api_resolve_status():
    return jsonify(resolve_status)


def _parse_google_spreadsheet_id(url_or_id):
    s = str(url_or_id or "").strip()
    if not s:
        return ""
    m = re.search(r"/spreadsheets/d/([a-zA-Z0-9_-]+)", s)
    if m:
        return m.group(1).strip()
    s2 = s.strip()
    if re.match(r"^[a-zA-Z0-9_-]{20,}$", s2):
        return s2
    return ""


def _resolve_service_account_json_path(raw):
    """
    Ищем JSON ключ сервисного аккаунта. Возвращает (Path|None, подсказка_для_ошибки).
    """
    raw = str(raw or "").strip()
    raw = raw.strip('"').strip("'").strip("\u200b").strip()
    raw = os.path.expanduser(raw)
    if not raw:
        return None, "В настройках пустое поле пути к JSON — введите путь и нажмите «Сохранить настройки»."
    tried = []
    candidates = []
    p0 = Path(raw)
    if p0.is_absolute():
        candidates.append(p0)
    else:
        base_app = Path(__file__).resolve().parent
        candidates.append((base_app / raw))
        candidates.append((Path.cwd() / raw))
    seen = set()
    for p in candidates:
        try:
            rp = p.resolve()
        except Exception:
            rp = p
        key = str(rp)
        if key in seen:
            continue
        seen.add(key)
        tried.append(key)
        if rp.is_file():
            return rp, ""
    return None, "Файл не найден. Проверьте путь и что настройки сохранены. Искали: " + " · ".join(tried)


def _prepare_consolidated_for_export(session_dir):
    """Тот же набор фильтров, что и для /download. Возвращает (DataFrame|None, имя_файла.xlsx)."""
    settings = load_app_settings()
    export_cfg = settings.get("export", {})
    include_without_id = bool(export_cfg.get("include_without_id", True))
    keep_lowest_id_price = bool(export_cfg.get("keep_lowest_price_per_onliner_id", False))
    base_name = str(export_cfg.get("price_name", "consolidated_price")).strip() or "consolidated_price"
    exclude_duplicate_id_suppliers = export_cfg.get("exclude_duplicate_id_suppliers", [])
    only_pc_suppliers = export_cfg.get("only_pc_suppliers", [])
    only_pc_price_name = str(export_cfg.get("only_pc_price_name", "N-tech_TGPC_Beznal")).strip() or "N-tech_TGPC_Beznal"
    download_name = f"{base_name}.xlsx"
    sd = str(session_dir or "").strip()
    if not sd:
        return None, download_name
    cons_path = Path(sd) / "consolidated_price.xlsx"
    if not cons_path.exists():
        return None, download_name
    df = read_consolidated_df(sd)
    filtered = apply_visibility_filter(df, sd)
    if not include_without_id and "OnlinerID" in filtered.columns:
        mask = filtered["OnlinerID"].apply(lambda v: bool(normalize_onliner_id(v)))
        filtered = filtered[mask].copy()
    if keep_lowest_id_price:
        filtered = apply_export_keep_lowest_price_per_onliner_id(filtered)
    filtered = apply_export_duplicate_id_filter(filtered, exclude_duplicate_id_suppliers)
    before_only_pc_len = len(filtered)
    filtered = apply_export_only_pc_filter(filtered, only_pc_suppliers)
    if only_pc_suppliers and len(filtered) != before_only_pc_len:
        download_name = f"{only_pc_price_name}.xlsx"
    return filtered, download_name


def _export_column_is_onliner_id(name):
    """Колонка с Onliner ID (в т.ч. с пробелами / другим регистром)."""
    raw = str(name or "").strip().lower().replace("\xa0", " ")
    compact = re.sub(r"[\s_]+", "", raw)
    if compact == "onlinerid":
        return True
    if "onliner" in compact and compact.endswith("id"):
        return True
    return False


def _normalize_export_column_name(name):
    raw = str(name or "").strip().lower().replace("\xa0", " ")
    return re.sub(r"\s+", " ", raw)


def _export_column_is_money(name):
    normalized = _normalize_export_column_name(name)
    if not normalized:
        return False
    money_cols = {
        "цена",
        "лучшая цена",
        "ррц",
        "цена без скидки",
        "цена без ндс",
        "цена с ндс",
    }
    if normalized in money_cols:
        return True
    return ("цена" in normalized) and ("ссылка" not in normalized)


def _coerce_money_value(v):
    if v is None:
        return ""
    if isinstance(v, bool):
        return ""
    if isinstance(v, (int, float, np.integer, np.floating)):
        fv = float(v)
        if not math.isfinite(fv):
            return ""
        return f"{fv:.2f}"
    s = str(v).strip()
    if not s:
        return ""
    s = s.replace("\xa0", "").replace(" ", "")
    # RU decimal comma and Excel/Sheets artifacts are normalized to dot.
    s = s.replace(",", ".")
    try:
        fv = float(s)
        if not math.isfinite(fv):
            return ""
        return f"{fv:.2f}"
    except Exception:
        return str(v)


def _dataframe_to_sheet_values(df):
    if df is None or df.empty:
        return []
    original_cols = list(df.columns)
    normalized_map = {_normalize_export_column_name(c): c for c in original_cols}

    # Fixed Google Sheets layout:
    # A: empty, B: Название, C: Цена, D: Поставщик, E: Гарантия,
    # F: Дней доставки, G: РРЦ, H: Цена без скидки
    target_layout = [
        ("", []),
        ("Название", ["наименование", "название"]),
        # Оптовая цена из миксера:
        # приоритет "Опт цена"/"Лучшая цена", fallback на "Цена",
        # но не на сервисные денежные колонки.
        ("Цена", ["опт цена", "лучшая цена", "цена"]),
        ("Поставщик", ["поставщик"]),
        ("Гарантия", ["гарантия"]),
        ("Дней доставки", ["дней доставки", "дни доставки", "доставка дней", "срок поставки"]),
        ("РРЦ", ["ррц"]),
        ("Цена без скидки", ["цена без скидки"]),
        ("OnlinerID", ["onlinerid", "onliner id"]),
    ]
    resolved_columns = []
    for _header_name, aliases in target_layout:
        picked = None
        for alias in aliases:
            if alias in normalized_map:
                picked = normalized_map[alias]
                break
        resolved_columns.append(picked)

    headers = [item[0] for item in target_layout]
    rows = [headers]
    for _, row in df.iterrows():
        line = []
        for idx, src_col in enumerate(resolved_columns):
            if idx == 0:
                line.append("")
                continue
            if not src_col:
                line.append("")
                continue
            col_name = src_col
            v = row.get(src_col)
            if _export_column_is_onliner_id(col_name):
                line.append(normalize_onliner_id(v))
                continue
            if _export_column_is_money(col_name):
                line.append(_coerce_money_value(v))
                continue
            if v is None:
                line.append("")
                continue
            if isinstance(v, float) and math.isnan(v):
                line.append("")
                continue
            try:
                if pd.isna(v):
                    line.append("")
                    continue
            except (ValueError, TypeError):
                pass
            try:
                if hasattr(v, "isoformat") and not isinstance(v, (str, bytes, int, float, bool)):
                    line.append(str(v.isoformat()))
                else:
                    s = str(v)
                    line.append("" if s.lower() == "nan" else s)
            except Exception:
                line.append(str(v))
        rows.append(line)
    return rows


@app.route("/download")
def download():
    settings = load_app_settings()
    export_cfg = settings.get("export", {})
    base_name = str(export_cfg.get("price_name", "consolidated_price")).strip() or "consolidated_price"
    download_name = f"{base_name}.xlsx"
    session_dir = session.get("session_dir")
    if session_dir:
        filtered, download_name = _prepare_consolidated_for_export(session_dir)
        if filtered is not None:
            visible_path = Path(session_dir) / "consolidated_price_visible.xlsx"
            filtered.to_excel(visible_path, index=False)
            return send_file(str(visible_path), as_attachment=True, download_name=download_name)
    output_path = session.get("output_path")
    if output_path and os.path.exists(output_path):
        return send_file(output_path, as_attachment=True, download_name=download_name)
    return redirect(url_for("index", error="Файл не найден. Загрузите прайсы заново."))


@app.route("/api/export-google-sheets", methods=["POST"])
def api_export_google_sheets():
    try:
        import gspread
        from gspread.utils import rowcol_to_a1
        from gspread.exceptions import WorksheetNotFound, SpreadsheetNotFound, APIError
    except ImportError:
        return jsonify({"status": "error", "message": "Не установлен пакет gspread."}), 500

    session_dir = session.get("session_dir")
    if not session_dir:
        return jsonify({"status": "error", "message": "Нет активной сессии"}), 400
    settings = load_app_settings()
    ex = settings.get("export", {})
    sheet_ref = str(ex.get("google_sheets_spreadsheet_url_or_id", "") or "").strip()
    tab = str(ex.get("google_sheets_tab", "Прайс N-Tech") or "Прайс N-Tech").strip() or "Прайс N-Tech"
    sa_raw = str(ex.get("google_sheets_service_account_json", "") or "").strip()
    sid = _parse_google_spreadsheet_id(sheet_ref)
    if not sid:
        return jsonify({
            "status": "error",
            "message": "В настройках не указана ссылка или ID таблицы (поле «Google Таблица — ссылка или ID»).",
        }), 400
    sa_path, sa_err = _resolve_service_account_json_path(sa_raw)
    if not sa_path:
        return jsonify({
            "status": "error",
            "message": sa_err or "Укажите путь к JSON сервисного аккаунта и убедитесь, что файл существует.",
        }), 400

    filtered, _ = _prepare_consolidated_for_export(session_dir)
    if filtered is None:
        return jsonify({"status": "error", "message": "Нет сводного прайса (consolidated_price.xlsx) в текущей сессии."}), 400
    if filtered.empty:
        return jsonify({"status": "error", "message": "Сводный прайс пуст после применения фильтров выгрузки."}), 400

    values = _dataframe_to_sheet_values(filtered)
    if len(values) < 2:
        return jsonify({"status": "error", "message": "Нет строк данных для выгрузки."}), 400

    try:
        gc = gspread.service_account(filename=str(sa_path))
        sh = gc.open_by_key(sid)
    except SpreadsheetNotFound:
        return jsonify({
            "status": "error",
            "message": "Таблица не найдена. Проверьте ID/ссылку и что файл расшарен на e-mail сервисного аккаунта из JSON (доступ «Редактор»).",
        }), 400
    except APIError as e:
        return jsonify({"status": "error", "message": f"Google Sheets API: {e}"}), 400
    except Exception as e:
        return jsonify({"status": "error", "message": f"Не удалось открыть таблицу: {e}"}), 400

    created = False
    try:
        ws = sh.worksheet(tab)
    except WorksheetNotFound:
        ncols0 = max(len(values[0]), 26)
        nrows0 = max(len(values) + 200, 1000)
        ws = sh.add_worksheet(title=tab[:99], rows=nrows0, cols=ncols0)
        created = True
    if not created:
        try:
            ws.clear()
        except Exception:
            pass

    ncols = len(values[0])
    nvals = len(values)
    # Лист мог быть создан раньше с маленькой сеткой (напр. 4000 строк) — расширяем под весь прайс.
    pad_rows = max(nvals + 200, 1000)
    pad_cols = max(ncols + 2, 26)
    try:
        cur_r = int(ws.row_count)
        cur_c = int(ws.col_count)
    except Exception:
        cur_r, cur_c = pad_rows, pad_cols
    if cur_r < pad_rows or cur_c < pad_cols:
        try:
            ws.resize(rows=max(pad_rows, cur_r), cols=max(pad_cols, cur_c))
        except APIError as e:
            return jsonify({"status": "error", "message": f"Не удалось расширить лист под данные ({nvals} строк): {e}"}), 500
        except Exception as e:
            return jsonify({"status": "error", "message": f"Не удалось расширить лист под данные: {e}"}), 500

    batch = 4000
    try:
        for i in range(0, len(values), batch):
            chunk = values[i : i + batch]
            top = i + 1
            bottom = i + len(chunk)
            rng = f"{rowcol_to_a1(top, 1)}:{rowcol_to_a1(bottom, ncols)}"
            ws.update(chunk, rng, value_input_option="USER_ENTERED")
    except APIError as e:
        return jsonify({"status": "error", "message": f"Ошибка записи в лист: {e}"}), 500
    except Exception as e:
        return jsonify({"status": "error", "message": f"Ошибка записи в лист: {e}"}), 500

    try:
        full_rng = f"A1:{rowcol_to_a1(len(values), ncols)}"
        ws.format(full_rng, {
            "horizontalAlignment": "CENTER",
            "verticalAlignment": "MIDDLE",
        })
    except Exception:
        pass

    url = f"https://docs.google.com/spreadsheets/d/{sid}/edit"
    nrows = len(values) - 1
    return jsonify({
        "status": "ok",
        "message": f"Готово: {nrows} строк, {ncols} колонок на листе «{tab}». {url}",
        "spreadsheet_url": url,
        "rows": nrows,
        "columns": ncols,
        "sheet_title": tab,
    })


@app.route("/download/id-quality-report")
def download_id_quality_report():
    session_dir = session.get("session_dir")
    if not session_dir:
        return redirect(url_for("index", error="Нет активной сессии"))
    path = Path(session_dir) / "id_quality_report.csv"
    if not path.exists():
        return redirect(url_for("index", error="ID quality report не найден"))
    return send_file(str(path), as_attachment=True, download_name="id_quality_report.csv")


@app.route("/api/id-quality-report")
def api_id_quality_report():
    session_dir = session.get("session_dir")
    if not session_dir:
        return jsonify({"status": "no_session"})
    summary_path = Path(session_dir) / "id_quality_report.json"
    report_path = Path(session_dir) / "id_quality_report.csv"
    if not summary_path.exists():
        return jsonify({"status": "not_found"})
    try:
        with open(summary_path, "r", encoding="utf-8") as f:
            summary = json.load(f)
    except Exception:
        return jsonify({"status": "error"})
    summary["status"] = "ok"
    summary["has_csv"] = report_path.exists()
    return jsonify(summary)


@app.route("/api/preexport-quality-check")
def api_preexport_quality_check():
    session_dir = session.get("session_dir")
    if not session_dir:
        return jsonify({"status": "error", "message": "Нет активной сессии"}), 400
    cons_path = Path(session_dir) / "consolidated_price.xlsx"
    if not cons_path.exists():
        return jsonify({"status": "error", "message": "Сводный прайс не найден"}), 400

    df = read_consolidated_df(session_dir)
    df = ensure_category_column(df)
    df = apply_visibility_filter(df, session_dir)
    if df.empty:
        return jsonify({
            "status": "ok",
            "checked": 0,
            "missing_id_count": 0,
            "suspicious_price_count": 0,
            "duplicate_count": 0,
            "missing_id_samples": [],
            "suspicious_price_samples": [],
            "duplicate_samples": [],
        })

    missing_id_samples = []
    suspicious_price_samples = []
    duplicate_samples = []

    # 1) Без ID
    missing_mask = []
    for _, row in df.iterrows():
        oid = normalize_onliner_id(row.get("OnlinerID", ""))
        is_missing = not bool(oid)
        missing_mask.append(is_missing)
        if is_missing and len(missing_id_samples) < 8:
            missing_id_samples.append(f"[{str(row.get('Категория', '')).strip()}] {str(row.get('Название', '')).strip()}")
    missing_id_count = int(sum(1 for x in missing_mask if x))

    # 2) Подозрительная цена по бизнес-логике маржи:
    # сравниваем оптовую "Цена" и розничную "РРЦ".
    # Флагим, если:
    # - опт/РРЦ невалидны,
    # - РРЦ <= опта,
    # - слишком маленькая разница (абсолютная или процентная).
    MIN_MARGIN_PCT = 5.0
    MIN_MARGIN_ABS = 20.0

    raw_wholesale = df.get("Цена", None)
    if raw_wholesale is None:
        raw_wholesale = df.get("Лучшая цена", pd.Series(dtype=float))
    raw_rrc = df.get("РРЦ", pd.Series(dtype=float))

    def _normalize_price_series(series_like):
        return pd.Series(series_like).astype(str).str.replace("\u00a0", "", regex=False).str.replace(" ", "", regex=False).str.replace(",", ".", regex=False)

    wholesale = pd.to_numeric(_normalize_price_series(raw_wholesale), errors="coerce")
    rrc = pd.to_numeric(_normalize_price_series(raw_rrc), errors="coerce")

    suspicious_mask = []
    for i, row in df.iterrows():
        p = wholesale.loc[i] if i in wholesale.index else np.nan
        r = rrc.loc[i] if i in rrc.index else np.nan
        cat = str(row.get("Категория", "")).strip()
        bad = False
        reason = ""
        if not np.isfinite(p) or p <= 0:
            bad = True
            reason = "невалидный опт"
        elif not np.isfinite(r) or r <= 0:
            bad = True
            reason = "невалидный РРЦ"
        else:
            margin_abs = float(r - p)
            margin_pct = float((margin_abs / p) * 100.0) if p > 0 else -999.0
            if margin_abs <= 0:
                bad = True
                reason = "РРЦ <= опт"
            # Низкая маржа: считаем проблемой, только если одновременно
            # и абсолютная, и процентная маржа ниже порога.
            elif margin_abs < MIN_MARGIN_ABS and margin_pct < MIN_MARGIN_PCT:
                bad = True
                reason = f"низкая маржа (<{MIN_MARGIN_ABS} руб и <{MIN_MARGIN_PCT}%)"
        suspicious_mask.append(bad)
        if bad and len(suspicious_price_samples) < 8:
            raw_opt = row.get("Цена", row.get("Лучшая цена", ""))
            raw_rrc_val = row.get("РРЦ", "")
            shown_opt = ("" if not np.isfinite(p) else round(float(p), 2))
            shown_rrc = ("" if not np.isfinite(r) else round(float(r), 2))
            margin_txt = ""
            if np.isfinite(p) and p > 0 and np.isfinite(r):
                m_abs = float(r - p)
                m_pct = float((m_abs / p) * 100.0)
                margin_txt = f", маржа={round(m_abs, 2)} ({round(m_pct, 2)}%)"
            suspicious_price_samples.append(
                f"[{cat}] {str(row.get('Название', '')).strip()} | причина: {reason or 'проверка цены'} | опт={shown_opt}, РРЦ={shown_rrc}{margin_txt}"
                + (
                    f" (raw опт: {str(raw_opt).strip()}, raw РРЦ: {str(raw_rrc_val).strip()})"
                    if ((not np.isfinite(p) and str(raw_opt).strip()) or (not np.isfinite(r) and str(raw_rrc_val).strip()))
                    else ""
                )
            )
    suspicious_price_count = int(sum(1 for x in suspicious_mask if x))

    # 3) Дубли (поставщик + нормализованное имя)
    dup_map = {}
    for _, row in df.iterrows():
        supplier = str(row.get("Поставщик", "")).strip().lower()
        name_key = _normalize_name_key(row.get("Название", ""))
        if not name_key:
            continue
        key = f"{supplier}|{name_key}"
        dup_map[key] = int(dup_map.get(key, 0)) + 1
    duplicate_count = int(sum(v for v in dup_map.values() if v > 1))
    if duplicate_count:
        for _, row in df.iterrows():
            supplier = str(row.get("Поставщик", "")).strip()
            name = str(row.get("Название", "")).strip()
            key = f"{supplier.lower()}|{_normalize_name_key(name)}"
            if dup_map.get(key, 0) > 1:
                duplicate_samples.append(f"[{supplier}] {name} (x{dup_map.get(key, 0)})")
                if len(duplicate_samples) >= 8:
                    break

    return jsonify({
        "status": "ok",
        "checked": int(len(df)),
        "missing_id_count": missing_id_count,
        "suspicious_price_count": suspicious_price_count,
        "duplicate_count": duplicate_count,
        "missing_id_samples": missing_id_samples,
        "suspicious_price_samples": suspicious_price_samples,
        "duplicate_samples": duplicate_samples,
    })


@app.route("/api/reapply-saved-markups", methods=["POST"])
def api_reapply_saved_markups():
    session_dir = session.get("session_dir")
    if not session_dir:
        return jsonify({"status": "error", "message": "Нет активной сессии"}), 400
    cons_path = Path(session_dir) / "consolidated_price.xlsx"
    if not cons_path.exists():
        return jsonify({"status": "error", "message": "Сводный прайс не найден"}), 400

    df = read_consolidated_df(session_dir)
    if df.empty:
        return jsonify({"status": "ok", "updated_rows": 0})

    if "РРЦ" not in df.columns:
        df["РРЦ"] = ""

    before_rrc = pd.to_numeric(df["РРЦ"], errors="coerce")
    df2 = apply_saved_markups_to_df(df.copy())
    after_rrc = pd.to_numeric(df2.get("РРЦ", pd.Series(dtype=float)), errors="coerce")

    changed = 0
    for i in df2.index:
        b = before_rrc.loc[i] if i in before_rrc.index else np.nan
        a = after_rrc.loc[i] if i in after_rrc.index else np.nan
        if (pd.isna(b) and pd.notna(a)) or (pd.notna(b) and pd.notna(a) and float(b) != float(a)):
            changed += 1

    write_consolidated_df(session_dir, df2)
    write_consolidated_json(df2, Path(session_dir) / "consolidated.json")
    return jsonify({"status": "ok", "updated_rows": int(changed)})


if __name__ == "__main__":
    print("=" * 50)
    print("Price Mixer Web")
    print("Открой в браузере: http://localhost:5001")
    print("=" * 50)
    init_onliner_db()
    # Stable local run mode: no Flask reloader double-process.
    app.run(debug=False, use_reloader=False, load_dotenv=False, host="127.0.0.1", port=5001)


