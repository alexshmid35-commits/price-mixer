# Price Mixer — статус и дальнейший план

Дата фиксации: 2026-07-24

## Технический статус

Пункты 1–6 текущего плана реализованы и автоматически проверены:

- observability внешних API и background jobs;
- ежедневный verified backup schedule;
- физическое разделение code/runtime и copy-only migration;
- SQLite durable queue и отдельный worker;
- изолированный E2E/preflight;
- deploy-ready systemd/nginx/runbook/smoke контур.

Итоговый контроль миграционной ветки: `838 passed`, `3 E2E passed`,
production smoke `4/4`, local smoke `4/4`.

## Архитектурный спринт 1–8

Завершён 2026-07-24:

1. SQL/JSON/XLSX-пути и фоновые задания проаудированы.
2. `session_products` стал каноническим рабочим состоянием; изменения строк
   синхронны, JSON объединяется в отложенный snapshot, XLSX строится при
   экспорте.
3. Прямые HTTP-маршруты удалены из `app.py` и перенесены в blueprints.
4. Matching разделён на движок, общие признаки и 12 category plugins.
5. Durable jobs получили idempotent enqueue, cancel/resume и защиту от
   запоздалого завершения старым worker.
6. Добавлена единая схема полей товара и wire-позиций.
7. Добавлены обязательный quality gate, архитектурные проверки, Ruff, mypy,
   JavaScript syntax check и GitHub Actions.
8. Unit/E2E/benchmark и миграция копий реальных сессий прошли успешно.

Старые SQL-сессии, которые ранее содержали только видимые строки, помечаются
как неполные и восстанавливаются из JSON. Проверка на копии реальной сессии
восстановила 20 565 строк за 0,56 секунды без расхождений.

## Архитектурный спринт — runtime сессий

Завершён 2026-07-24:

1. SQL-first чтение и запись, совместимые JSON-снимки и отложенный XLSX
   объединены в `SessionDataRuntime`.
2. `app.py` оставляет прежние публичные функции, но они стали тонкими
   делегирующими обёртками без выбора хранилища внутри.
3. Неполная SQL-сессия не считается канонической и безопасно читается из
   JSON/XLSX; полная SQL-сессия всегда имеет приоритет.
4. Горячий путь нормализации категорий и преобразования wire-строк переведён
   на `ProductField` и `ProductWireIndex`.
5. Добавлены контрактные тесты канонической/legacy-записи, fallback,
   определения живой сессии, отложенного XLSX и coalesced JSON snapshot.

Контроль после выделения слоя: `812 passed`, `3 E2E passed`. На 100 000 строк
страница имеет p95 5,2 мс, поиск p95 39,8 мс.

## Архитектурный спринт — быстрый dashboard и export

Завершён 2026-07-24:

1. `/result`, `/api/stats` и категорийные счётчики сведены в единый
   `SessionReadModel`.
2. Готовая dashboard-проекция сохраняется в SQLite по полному revision-token,
   включая настройки, ручные ID, market cache и состояния категорий.
3. Первый запрос после перезапуска читает проекцию из SQLite; повторные
   запросы используют ограниченный RAM-кэш.
4. XLSX и Google Sheets используют общий `ExportRuntime` и одну подготовленную
   версию DataFrame.
5. После изменения ID, категории или цены централизованно инвалидируются
   read-model, export, quality и paging caches.
6. Одновременные одинаковые запросы coalesced: проекция и export готовятся
   одним worker запроса, остальные ожидают тот же результат.
7. Начальная HTML-страница и последующий `/api/stats` теперь показывают
   одинаковые значения.
8. Добавлены schema migration, persistence, concurrency, architecture и
   export contract tests.

На реальной сессии из 5 515 строк `/result` ускорен с p50 145,8 мс до 2,4 мс,
`/api/stats` — с p50 75,3 мс до 1,6 мс. После холодного перезапуска первый
`/result` занял 22,1 мс, первый `/api/stats` — 2,3 мс. Контроль:
`824 passed`, `3 E2E passed`.

## Архитектурный спринт — SQLite и доставка экспорта

Завершён 2026-07-24:

1. Добавлен воспроизводимый `scripts/profile_sqlite.py` с query plans,
   состоянием WAL и машиночитаемым JSON-отчётом.
2. `onliner_products.db`, `session_products.db` и `jobs.db` используют единый
   connection policy: WAL, `synchronous=NORMAL`, `busy_timeout=30000`.
3. Durable worker раз в час выполняет `wal_checkpoint(PASSIVE)`,
   `analysis_limit=1000` и `PRAGMA optimize` вне HTTP-запросов.
4. Для категорий без ID добавлен составной SQL-индекс; временный B-tree из
   частого query plan устранён.
5. Реальный замер 80 товаров на 1/4/8/12 workers выбрал 4 workers:
   7,24 / 10,66 / 10,21 / 10,15 товара в секунду соответственно.
6. CSS/JS получили content-hash в URL и годовой immutable-cache; старые
   непомеченные URL сохраняют безопасный cache на 5 минут.
7. XLSX-рендеринг вынесен в `ExportDeliveryRuntime` и сохраняется как
   неизменяемый артефакт по ревизии сессии.
8. Повторное скачивание реального XLSX ускорено примерно с 300 мс до
   2,1–2,6 мс без изменения 4 658 экспортируемых строк.

Live-контроль: schema 8, новый индекс активен, обслуживание трёх БД завершено
с `busy=0`, `838 passed`, `3 E2E passed`, свежие web/worker логи без ошибок.

## Архитектурный спринт — границы приложения 1–8

Завершён 2026-07-24:

1. `benchmark_local.py` фиксирует 5k/25k/100k строк, CPU, Python peak memory,
   RSS и сравнивает повторный прогон с baseline при регрессии более 20%.
2. Ручной специализированный поиск ID вынесен в `ManualIdSearchRuntime`.
3. Канонический ремонт категорий вынесен в `CategoryRepairRuntime`.
4. Жизненный цикл «Допарсить очередь», запуск парсера и monitor вынесены в
   `SortingReparseRuntime`.
5. Ручная загрузка прайсов оркестрируется независимым `UploadRuntime`.
6. Process-local сервисы создаются один раз потокобезопасным
   `ServiceContainer`, без разрозненных lazy-глобалов.
7. Горячий upload pipeline использует `ProductField`; добавлено каноническое
   поле ссылки. Неявный lazy-доступ к `_legacy.py` из пакета сервисов удалён,
   старый код доступен только через явный фасад `mixer.py`.
8. CI запускает performance smoke вместе с quality/E2E.

`app.py` уменьшен с 5 667 до 5 418 строк. Baseline 100 000 строк:
warm page p95 0,555 мс, warm search p95 0,523 мс. Повторный прогон не показал
регрессий. Контроль: `855 passed`, `3 E2E passed`.

## Ручная приёмка

Ручная проверка на небольшом реальном прайсе завершена пользователем
2026-07-24. Подтверждены:

1. вход и загрузку файла;
2. количество строк/поставщиков и товары с/без Onliner ID;
3. ручной выбор/очистку ID и review queue;
4. N-Tech проверки и карточки отчётов;
5. visibility/override/наценку категории;
6. скачивание итогового XLSX;
7. при необходимости — экспорт Google Sheets и API-источник с реальными
   настройками.

E2E-порт 5011 поднимается только на время `npm run test:e2e` и затем
останавливается.

## Достигнут ли максимум после перехода на SQLite

Нет. Переход каталога Onliner и постоянных состояний на SQLite убрал важный
файловый bottleneck, но не исчерпал резерв производительности.

Уже сделано хорошо:

- каталог примерно на 800 тысяч вариантов хранится в SQLite;
- для поиска используется FTS5, для параллельного чтения включён WAL;
- основные очереди, ручные привязки и результаты зелёного подбора сохраняются
  надёжно;
- главная таблица использует серверную пагинацию и не рисует весь прайс в DOM;
- в зелёном подборе есть дедупликация одинаковых товаров, пул до 8 workers и
  пакетная запись результатов.

Оставшиеся узкие места по текущему коду:

1. рабочий прайс сессии всё ещё хранится одновременно в pandas, JSON и XLSX;
2. первый запрос новой ревизии прайса всё ещё проходит все строки, но
   повторные страницы и одинаковые поиски используют ограниченный кэш;
3. изменение ID может приводить к повторной сериализации JSON и XLSX;
4. первый расчёт кандидатов новой модели всё ещё требует SQL-поиска и
   category-specific Python-scoring;
5. `app.py` содержит около 5 400 строк, а оставшиеся runtime callbacks
   затрудняют безопасную оптимизацию и повторное использование кода.

SQLite пока подходит лучше всего: приложение работает на одном компьютере,
каталог в основном читается, а одновременных записывающих пользователей нет.
PostgreSQL не даст автоматического ускорения локальной версии. Он понадобится,
если появятся несколько web-процессов/серверов, много одновременных
пользователей и записей, удалённый общий доступ, репликация или требования HA.

## Приоритетный roadmap

Порядок обязателен: сначала измерить, затем менять горячий путь. Переносить всё
в другую БД без профиля нагрузки не нужно.

### Этап 1 — воспроизводимый baseline и профилирование

- добавить `scripts/benchmark_local.py` без внешних API;
- подготовить обезличенные наборы на 3–5, 25 и 100 тысяч строк;
- измерять время загрузки, открытие результата, p50/p95 таблицы и поиска,
  полный зелёный подбор, подтверждение ID и экспорт XLSX;
- фиксировать CPU, пиковую память, число SQL-запросов и объём чтения/записи;
- добавить performance smoke в CI без жёсткой зависимости от мощности машины;
- отдельно проверить `EXPLAIN QUERY PLAN`, `ANALYZE`/`PRAGMA optimize` и
  реальные индексы на самых частых запросах.

Критерий завершения: baseline сохраняется в машиночитаемый JSON, повторный
прогон сравнивает результат с базой и показывает регрессию более 20%.

### Этап 2 — быстрые изменения с низким риском

- [x] кэшировать метаданные таблицы, нормализованные строки и ключи сортировки
  по версии сессии, а не пересчитывать их при каждом листании;
- [x] не строить одновременно одинаковые payload для `/api/stats` и
  стартовой страницы;
- [x] оставить `no-store` для пользовательских API/HTML и разрешить короткий
  безопасный кэш для CSS/JS; immutable versioned-cache остаётся следующим
  необязательным шагом;
- [x] настроить `busy_timeout`, проверить размер WAL и выполнять обслуживание БД
  по событию/расписанию, а не в горячем запросе;
- [x] убрать лишние DataFrame `.copy()` и полные записи XLSX из операций, которым
  достаточно изменить одну строку состояния;
- [x] замерить 1/4/8/12 workers зелёного подбора и выбрать значение по факту, а не
  увеличивать параллелизм вслепую.

Критерий завершения: на наборе 25 тысяч строк p95 листания/поиска не хуже
300 мс на текущем компьютере, повторные действия не создают лишний XLSX, а
полный regression/E2E остаётся зелёным.

### Этап 3 — SQL-хранилище рабочего прайса

- [x] сделать таблицу `session_products` индексированным каноническим read-model;
- [x] хранить `session_id`, индекс строки, Onliner ID, название, поставщика, цены,
  гарантию, доставку, категорию и revision;
- [x] добавить индексы под фильтры: с/без ID, поставщик, категория, дубликаты и
  сортировка; для поиска по названию использовать FTS5;
- [x] выполнять фильтрацию, сортировку, счётчики и пагинацию SQL-запросами;
- [x] обновлять ID/категорию точечно в транзакции;
- [x] JSON оставить временным совместимым snapshot, а XLSX формировать только при
  скачивании/явном checkpoint;
- [x] миграцию провести с построчной сверкой и
  возможностью отката, после приёмки убрать старый внутренний путь.

Критерий завершения: результат экспорта и счётчики совпадают со старым
движком на тестовых и реальных прайсах, а горячие операции не читают и не
перезаписывают весь XLSX.

### Этап 4 — ускорение и качество зелёного подбора

- [x] кэшировать кандидатов по `catalog_revision + normalized_name + category`;
- [x] отделить извлечение кандидатов из SQLite от category-specific scoring;
- [x] исключить повторные запросы для одинаковых
  моделей и заранее вычислять часто используемые нормализованные признаки;
- [x] сохранять подтверждённые/отклонённые соответствия и считать
  precision, auto-confirm rate и false-positive rate по категориям;
- [x] использовать историю ручных решений как проверяемые правила, а не как
  безусловный глобальный ID;
- [x] добавить возобновление прерванного задания и безопасную отмену.

Критерий завершения: ускорение полного подбора минимум на 30% относительно
baseline без ухудшения precision и без роста ложных автоподтверждений.

### Этап 5 — чистая архитектура

- [ ] оставить в `app.py` создание приложения, зависимости и регистрацию
  blueprint; маршруты переносить по одному вместе с тестами. Экспортная
  доставка, ID-поиск, category repair, parser queue и upload orchestration уже
  вынесены в отдельные runtime;
- [x] разделить `review_candidates.py` на движок, общие признаки и плагины
  категорий;
- [x] запретить неявный импорт `_legacy.py`; дальнейший перенос самого parser
  ядра выполнять отдельно с golden tests;
- [x] ввести единые модели/константы полей товара в канонических горячих путях;
  оставшиеся compatibility-модули переводить при их следующем изменении;
- [x] включить обязательные Ruff, JavaScript syntax check, unit, E2E и smoke в
  один pre-push/CI-контур;
- [x] добавить type-check для новых сервисов и запрет циклических зависимостей.

Критерий завершения: новые маршруты не добавляются в `app.py`, публичные
сервисы имеют контрактные тесты, а удаление legacy не меняет пользовательские
сценарии.

### Этап 6 — новая функциональность

После этапов 1–3, чтобы функции не закрепляли старый медленный data path:

- история запусков зелёного подбора с повторным открытием отчёта;
- bulk approve/reject с preview, undo и журналом автора/времени;
- сохранённые профили поставщиков: колонки, валюта, гарантия, доставка,
  категории и наценки;
- dry-run импорта с отчётом ошибок до изменения рабочей сессии;
- шаблоны экспорта: XLSX/Google Sheets, состав колонок и правила округления;
- dashboard качества: без ID, спорные ID, дубли, точность и скорость подбора;
- сравнение двух загрузок и понятный changelog по товарам/ценам/ID;
- архив сессий с поиском и безопасным восстановлением.

## Ближайший практический спринт

1. [x] Реализовать baseline/benchmark и сохранить результаты текущей версии.
2. [x] Исправить кэш статических файлов и повторный расчёт метаданных таблицы.
3. [x] Снять повторные замеры и зафиксировать выигрыш.
4. [x] Спроектировать `session_products` и сделать SQL-прототип
   `/api/consolidated-page` за feature flag.
5. [x] Сверить SQL-прототип с текущим JSON на реальном прайсе.
6. Только после дополнительной пользовательской приёмки включать точечные
   SQL-обновления и убирать XLSX из
   горячего пути.

## Deployment-этап

Выполнено 2026-07-24:

- создан `backups/verified-2026-07-24T0845`;
- backup содержит 13 manifest-объектов state/DB, занимает 761,57 МБ;
- независимый `verify` проверил 13/13 объектов, SQLite `quick_check` и хэши;
- restore-plan успешно построен;
- `.env` и Google service-account в backup не включены;
- dry-run copy-only migration успешно построил 13 операций без удаления
  исходников.

Пользователь уточнил, что пока приложение продолжает работать только локально
на `http://127.0.0.1:5001`. Сервер повторно запущен после backup; локальный
smoke прошёл `4/4`. Миграция runtime в production-пути, systemd, nginx и TLS
отложены до отдельной команды на Linux deployment.

После появления целевого Linux-сервера останется:

1. получить и проверить адрес сервера, SSH-доступ и реальный домен;
2. подготовить каталоги и права;
3. повторить dry-run с Linux-путями и выполнить copy-only migration;
4. установить env/secrets вне каталога кода;
5. установить и включить web, durable worker и backup timer services;
6. проверить nginx/TLS, `deploy/check_production.py` и
   `deploy/smoke_production.py`;
7. провести малый реальный smoke, затем тяжёлый контрольный прайс;
8. зафиксировать rollback release и внешнее защищённое хранение backup.

Не подставлять вместо реальных реквизитов шаблонные
`price-mixer.example.com` и `/opt/price-mixer/current`. Перед продолжением
проверить целевую машину и пути.
